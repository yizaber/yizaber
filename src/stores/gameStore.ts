import { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
  winner: null,
};

// 游戏Store接口
interface GameStore {
  // 状态
  gameState: GameState;
  players: Record<string, MechaData>;
  effects: Effect[];
  
  // 游戏流程控制
  startGame: () => void;
  pauseGame: () => void;
  resumeGame: () => void;
  endGame: (winner: string) => void;
  resetGame: () => void;
  nextRound: () => void;
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => void;
  updatePlayerState: (playerId: string, state: MechaData['state']) => void;
  updatePlayerFacing: (playerId: string, facingRight: boolean) => void;
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => void;
  updatePlayerDefending: (playerId: string, isDefending: boolean) => void;
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => void;
  dealDamage: (targetId: string, damage: number, attackerId?: string) => void;
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => void;
  setAnimationFrame: (playerId: string, frame: number) => void;
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => string;
  updateEffects: (deltaTime: number) => void;
  removeEffect: (effectId: string) => void;
  clearEffects: () => void;
  
  // 游戏时间
  tick: (deltaTime: number) => void;
}

// 创建Game Store
export const useGameStore = create<GameStore>((set, get) => ({
  // 初始状态
  gameState: initialGameState,
  players: {
    player1: createInitialMecha('player1', 'BLUE', 200),
    player2: createInitialMecha('player2', 'RED', 550),
  },
  effects: [],
  
  // 游戏流程控制
  startGame: () => set(produce(state => {
    state.gameState.phase = 'PLAYING';
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.winner = null;
  })),
  
  pauseGame: () => set(produce(state => {
    if (state.gameState.phase === 'PLAYING') {
      state.gameState.phase = 'PAUSED';
    }
  })),
  
  resumeGame: () => set(produce(state => {
    if (state.gameState.phase === 'PAUSED') {
      state.gameState.phase = 'PLAYING';
    }
  })),
  
  endGame: (winner: string) => set(produce(state => {
    state.gameState.phase = 'RESULT';
    state.gameState.winner = winner;
    state.players[winner].score += 1;
  })),
  
  resetGame: () => set(produce(state => {
    state.gameState = initialGameState;
    state.players = {
      player1: createInitialMecha('player1', 'BLUE', 200),
      player2: createInitialMecha('player2', 'RED', 550),
    };
    state.effects = [];
  })),
  
  nextRound: () => set(produce(state => {
    state.gameState.round += 1;
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.phase = 'PLAYING';
    state.gameState.winner = null;
    
    // 重置玩家位置和状态
    Object.keys(state.players).forEach(playerId => {
      const player = state.players[playerId];
      player.health = player.maxHealth;
      player.position = playerId === 'player1' ? { x: 200, y: 400 } : { x: 550, y: 400 };
      player.velocity = { x: 0, y: 0 };
      player.state = 'IDLE';
      player.isDefending = false;
      player.isGrounded = false;
      player.attackCooldown = 0;
    });
    
    state.effects = [];
  })),
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => set(produce(state => {
    state.players[playerId].position = position;
    state.players[playerId].velocity = velocity;
  })),
  
  updatePlayerState: (playerId: string, mechaState: MechaData['state']) => set(produce(state => {
    state.players[playerId].state = mechaState;
  })),
  
  updatePlayerFacing: (playerId: string, facingRight: boolean) => set(produce(state => {
    state.players[playerId].facingRight = facingRight;
  })),
  
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => set(produce(state => {
    state.players[playerId].isGrounded = isGrounded;
  })),
  
  updatePlayerDefending: (playerId: string, isDefending: boolean) => set(produce(state => {
    state.players[playerId].isDefending = isDefending;
  })),
  
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => set(produce(state => {
    state.players[playerId].attackCooldown = cooldown;
  })),
  
  dealDamage: (targetId: string, damage: number) => set(produce(state => {
    const target = state.players[targetId];
    const actualDamage = target.isDefending ? Math.floor(damage * 0.5) : damage;
    target.health = Math.max(0, target.health - actualDamage);
    
    // 添加受击特效
    state.effects.push({
      id: `hit-${Date.now()}`,
      type: 'HIT',
      position: { ...target.position },
      duration: 300,
      timer: 0,
      frame: 0,
    });
    
    // 检查游戏结束
    if (target.health === 0) {
      const winnerId = targetId === 'player1' ? 'player2' : 'player1';
      state.gameState.phase = 'RESULT';
      state.gameState.winner = winnerId;
      state.players[winnerId].score += 1;
    }
  })),
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => set(produce(state => {
    const player = state.players[playerId];
    player.animationTimer += deltaTime;
  })),
  
  setAnimationFrame: (playerId: string, frame: number) => set(produce(state => {
    state.players[playerId].animationFrame = frame;
  })),
  
  // 特效
  addEffect: (effect) => set(produce(state => {
    const id = `effect-${Date.now()}-${Math.random().toString(36).substr(2, 9)}`;
    state.effects.push({
      ...effect,
      id,
      timer: 0,
    });
    return id;
  })),
  
  updateEffects: (deltaTime: number) => set(produce(state => {
    state.effects = state.effects.filter(effect => {
      effect.timer += deltaTime;
      return effect.timer < effect.duration;
    });
  })),
  
  removeEffect: (effectId: string) => set(produce(state => {
    state.effects = state.effects.filter(e => e.id !== effectId);
  })),
  
  clearEffects: () => set(produce(state => {
    state.effects = [];
  })),
  
  // 游戏时间
  tick: (deltaTime: number) => set(produce(state => {
    if (state.gameState.phase === 'PLAYING') {
      state.gameState.timeRemaining = Math.max(0, state.gameState.timeRemaining - deltaTime / 1000);
      
      // 时间到，判定胜负
      if (state.gameState.timeRemaining === 0) {
        const p1Health = state.players.player1.health;
        const p2Health = state.players.player2.health;
        
        if (p1Health > p2Health) {
          state.gameState.winner = 'player1';
          state.players.player1.score += 1;
        } else if (p2Health > p1Health) {
          state.gameState.winner = 'player2';
          state.players.player2.score += 1;
        }
        // 平局不加分
        
        state.gameState.phase = 'RESULT';
      }
    }
  })),
}));
importimport { create } from 'zustimport { create } from 'zustand';
import { produceimport { create } from 'zustand';
import { produce } from 'immer';
import { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameStateimport { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
import { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  Gameimport { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
import { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
}import { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
importimport { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHAimport { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';
import { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据import { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha =import { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type:import { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: numberimport { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
import { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIGimport { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
import { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
import { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
import { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
import { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealthimport { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.import { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: configimport { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed:import { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForceimport { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    positionimport { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400import { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: {import { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y:import { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    stateimport { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingimport { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
import { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
import { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
import { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
import { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
import { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
import { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
import { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};
import { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
import { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameStateimport { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'import { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1import { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3import { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAMEimport { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
import { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
  winner: null,
}import { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
  winner: null,
};

// 游戏Storeimport { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
  winner: null,
};

// 游戏Store接口
interface GameStore {
  //import { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
  winner: null,
};

// 游戏Store接口
interface GameStore {
  // 状态
  gameState:import { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
  winner: null,
};

// 游戏Store接口
interface GameStore {
  // 状态
  gameState: GameState;
  players:import { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
  winner: null,
};

// 游戏Store接口
interface GameStore {
  // 状态
  gameState: GameState;
  players: Record<string, MechaData>import { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
  winner: null,
};

// 游戏Store接口
interface GameStore {
  // 状态
  gameState: GameState;
  players: Record<string, MechaData>;
  effects: Effect[]import { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
  winner: null,
};

// 游戏Store接口
interface GameStore {
  // 状态
  gameState: GameState;
  players: Record<string, MechaData>;
  effects: Effect[];
  
  //import { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
  winner: null,
};

// 游戏Store接口
interface GameStore {
  // 状态
  gameState: GameState;
  players: Record<string, MechaData>;
  effects: Effect[];
  
  // 游戏流程控制
  startimport { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
  winner: null,
};

// 游戏Store接口
interface GameStore {
  // 状态
  gameState: GameState;
  players: Record<string, MechaData>;
  effects: Effect[];
  
  // 游戏流程控制
  startGame: () => void;
import { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
  winner: null,
};

// 游戏Store接口
interface GameStore {
  // 状态
  gameState: GameState;
  players: Record<string, MechaData>;
  effects: Effect[];
  
  // 游戏流程控制
  startGame: () => void;
  pauseGame: () => voidimport { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
  winner: null,
};

// 游戏Store接口
interface GameStore {
  // 状态
  gameState: GameState;
  players: Record<string, MechaData>;
  effects: Effect[];
  
  // 游戏流程控制
  startGame: () => void;
  pauseGame: () => void;
  resumeGame: ()import { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
  winner: null,
};

// 游戏Store接口
interface GameStore {
  // 状态
  gameState: GameState;
  players: Record<string, MechaData>;
  effects: Effect[];
  
  // 游戏流程控制
  startGame: () => void;
  pauseGame: () => void;
  resumeGame: () => void;
  endGameimport { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
  winner: null,
};

// 游戏Store接口
interface GameStore {
  // 状态
  gameState: GameState;
  players: Record<string, MechaData>;
  effects: Effect[];
  
  // 游戏流程控制
  startGame: () => void;
  pauseGame: () => void;
  resumeGame: () => void;
  endGame: (winner: string | nullimport { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
  winner: null,
};

// 游戏Store接口
interface GameStore {
  // 状态
  gameState: GameState;
  players: Record<string, MechaData>;
  effects: Effect[];
  
  // 游戏流程控制
  startGame: () => void;
  pauseGame: () => void;
  resumeGame: () => void;
  endGame: (winner: string | null) => void;
  resetimport { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
  winner: null,
};

// 游戏Store接口
interface GameStore {
  // 状态
  gameState: GameState;
  players: Record<string, MechaData>;
  effects: Effect[];
  
  // 游戏流程控制
  startGame: () => void;
  pauseGame: () => void;
  resumeGame: () => void;
  endGame: (winner: string | null) => void;
  resetGame: () => void;
import { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
  winner: null,
};

// 游戏Store接口
interface GameStore {
  // 状态
  gameState: GameState;
  players: Record<string, MechaData>;
  effects: Effect[];
  
  // 游戏流程控制
  startGame: () => void;
  pauseGame: () => void;
  resumeGame: () => void;
  endGame: (winner: string | null) => void;
  resetGame: () => void;
  nextRound: () => void;
import { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
  winner: null,
};

// 游戏Store接口
interface GameStore {
  // 状态
  gameState: GameState;
  players: Record<string, MechaData>;
  effects: Effect[];
  
  // 游戏流程控制
  startGame: () => void;
  pauseGame: () => void;
  resumeGame: () => void;
  endGame: (winner: string | null) => void;
  resetGame: () => void;
  nextRound: () => void;
  
  // 玩家import { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
  winner: null,
};

// 游戏Store接口
interface GameStore {
  // 状态
  gameState: GameState;
  players: Record<string, MechaData>;
  effects: Effect[];
  
  // 游戏流程控制
  startGame: () => void;
  pauseGame: () => void;
  resumeGame: () => void;
  endGame: (winner: string | null) => void;
  resetGame: () => void;
  nextRound: () => void;
  
  // 玩家操作
  updatePlayerPosition: (import { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
  winner: null,
};

// 游戏Store接口
interface GameStore {
  // 状态
  gameState: GameState;
  players: Record<string, MechaData>;
  effects: Effect[];
  
  // 游戏流程控制
  startGame: () => void;
  pauseGame: () => void;
  resumeGame: () => void;
  endGame: (winner: string | null) => void;
  resetGame: () => void;
  nextRound: () => void;
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position:import { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
  winner: null,
};

// 游戏Store接口
interface GameStore {
  // 状态
  gameState: GameState;
  players: Record<string, MechaData>;
  effects: Effect[];
  
  // 游戏流程控制
  startGame: () => void;
  pauseGame: () => void;
  resumeGame: () => void;
  endGame: (winner: string | null) => void;
  resetGame: () => void;
  nextRound: () => void;
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vectorimport { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
  winner: null,
};

// 游戏Store接口
interface GameStore {
  // 状态
  gameState: GameState;
  players: Record<string, MechaData>;
  effects: Effect[];
  
  // 游戏流程控制
  startGame: () => void;
  pauseGame: () => void;
  resumeGame: () => void;
  endGame: (winner: string | null) => void;
  resetGame: () => void;
  nextRound: () => void;
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => void;
import { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
  winner: null,
};

// 游戏Store接口
interface GameStore {
  // 状态
  gameState: GameState;
  players: Record<string, MechaData>;
  effects: Effect[];
  
  // 游戏流程控制
  startGame: () => void;
  pauseGame: () => void;
  resumeGame: () => void;
  endGame: (winner: string | null) => void;
  resetGame: () => void;
  nextRound: () => void;
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => void;
  updatePlayerState: (playerimport { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
  winner: null,
};

// 游戏Store接口
interface GameStore {
  // 状态
  gameState: GameState;
  players: Record<string, MechaData>;
  effects: Effect[];
  
  // 游戏流程控制
  startGame: () => void;
  pauseGame: () => void;
  resumeGame: () => void;
  endGame: (winner: string | null) => void;
  resetGame: () => void;
  nextRound: () => void;
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => void;
  updatePlayerState: (playerId: string, state: Meimport { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
  winner: null,
};

// 游戏Store接口
interface GameStore {
  // 状态
  gameState: GameState;
  players: Record<string, MechaData>;
  effects: Effect[];
  
  // 游戏流程控制
  startGame: () => void;
  pauseGame: () => void;
  resumeGame: () => void;
  endGame: (winner: string | null) => void;
  resetGame: () => void;
  nextRound: () => void;
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => void;
  updatePlayerState: (playerId: string, state: MechaData['state']) => voidimport { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
  winner: null,
};

// 游戏Store接口
interface GameStore {
  // 状态
  gameState: GameState;
  players: Record<string, MechaData>;
  effects: Effect[];
  
  // 游戏流程控制
  startGame: () => void;
  pauseGame: () => void;
  resumeGame: () => void;
  endGame: (winner: string | null) => void;
  resetGame: () => void;
  nextRound: () => void;
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => void;
  updatePlayerState: (playerId: string, state: MechaData['state']) => void;
  updatePlayerFacing:import { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
  winner: null,
};

// 游戏Store接口
interface GameStore {
  // 状态
  gameState: GameState;
  players: Record<string, MechaData>;
  effects: Effect[];
  
  // 游戏流程控制
  startGame: () => void;
  pauseGame: () => void;
  resumeGame: () => void;
  endGame: (winner: string | null) => void;
  resetGame: () => void;
  nextRound: () => void;
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => void;
  updatePlayerState: (playerId: string, state: MechaData['state']) => void;
  updatePlayerFacing: (playerId: string, facingimport { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
  winner: null,
};

// 游戏Store接口
interface GameStore {
  // 状态
  gameState: GameState;
  players: Record<string, MechaData>;
  effects: Effect[];
  
  // 游戏流程控制
  startGame: () => void;
  pauseGame: () => void;
  resumeGame: () => void;
  endGame: (winner: string | null) => void;
  resetGame: () => void;
  nextRound: () => void;
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => void;
  updatePlayerState: (playerId: string, state: MechaData['state']) => void;
  updatePlayerFacing: (playerId: string, facingRight: boolean) => void;
import { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
  winner: null,
};

// 游戏Store接口
interface GameStore {
  // 状态
  gameState: GameState;
  players: Record<string, MechaData>;
  effects: Effect[];
  
  // 游戏流程控制
  startGame: () => void;
  pauseGame: () => void;
  resumeGame: () => void;
  endGame: (winner: string | null) => void;
  resetGame: () => void;
  nextRound: () => void;
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => void;
  updatePlayerState: (playerId: string, state: MechaData['state']) => void;
  updatePlayerFacing: (playerId: string, facingRight: boolean) => void;
  updatePlayerGrounded: (playerimport { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
  winner: null,
};

// 游戏Store接口
interface GameStore {
  // 状态
  gameState: GameState;
  players: Record<string, MechaData>;
  effects: Effect[];
  
  // 游戏流程控制
  startGame: () => void;
  pauseGame: () => void;
  resumeGame: () => void;
  endGame: (winner: string | null) => void;
  resetGame: () => void;
  nextRound: () => void;
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => void;
  updatePlayerState: (playerId: string, state: MechaData['state']) => void;
  updatePlayerFacing: (playerId: string, facingRight: boolean) => void;
  updatePlayerGrounded: (playerId: string, isGroundedimport { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
  winner: null,
};

// 游戏Store接口
interface GameStore {
  // 状态
  gameState: GameState;
  players: Record<string, MechaData>;
  effects: Effect[];
  
  // 游戏流程控制
  startGame: () => void;
  pauseGame: () => void;
  resumeGame: () => void;
  endGame: (winner: string | null) => void;
  resetGame: () => void;
  nextRound: () => void;
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => void;
  updatePlayerState: (playerId: string, state: MechaData['state']) => void;
  updatePlayerFacing: (playerId: string, facingRight: boolean) => void;
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => void;
import { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
  winner: null,
};

// 游戏Store接口
interface GameStore {
  // 状态
  gameState: GameState;
  players: Record<string, MechaData>;
  effects: Effect[];
  
  // 游戏流程控制
  startGame: () => void;
  pauseGame: () => void;
  resumeGame: () => void;
  endGame: (winner: string | null) => void;
  resetGame: () => void;
  nextRound: () => void;
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => void;
  updatePlayerState: (playerId: string, state: MechaData['state']) => void;
  updatePlayerFacing: (playerId: string, facingRight: boolean) => void;
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => void;
  updatePlayerDefending: (import { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
  winner: null,
};

// 游戏Store接口
interface GameStore {
  // 状态
  gameState: GameState;
  players: Record<string, MechaData>;
  effects: Effect[];
  
  // 游戏流程控制
  startGame: () => void;
  pauseGame: () => void;
  resumeGame: () => void;
  endGame: (winner: string | null) => void;
  resetGame: () => void;
  nextRound: () => void;
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => void;
  updatePlayerState: (playerId: string, state: MechaData['state']) => void;
  updatePlayerFacing: (playerId: string, facingRight: boolean) => void;
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => void;
  updatePlayerDefending: (playerId: string, isDefimport { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
  winner: null,
};

// 游戏Store接口
interface GameStore {
  // 状态
  gameState: GameState;
  players: Record<string, MechaData>;
  effects: Effect[];
  
  // 游戏流程控制
  startGame: () => void;
  pauseGame: () => void;
  resumeGame: () => void;
  endGame: (winner: string | null) => void;
  resetGame: () => void;
  nextRound: () => void;
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => void;
  updatePlayerState: (playerId: string, state: MechaData['state']) => void;
  updatePlayerFacing: (playerId: string, facingRight: boolean) => void;
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => void;
  updatePlayerDefending: (playerId: string, isDefending: boolean) => void;
import { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
  winner: null,
};

// 游戏Store接口
interface GameStore {
  // 状态
  gameState: GameState;
  players: Record<string, MechaData>;
  effects: Effect[];
  
  // 游戏流程控制
  startGame: () => void;
  pauseGame: () => void;
  resumeGame: () => void;
  endGame: (winner: string | null) => void;
  resetGame: () => void;
  nextRound: () => void;
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => void;
  updatePlayerState: (playerId: string, state: MechaData['state']) => void;
  updatePlayerFacing: (playerId: string, facingRight: boolean) => void;
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => void;
  updatePlayerDefending: (playerId: string, isDefending: boolean) => void;
  setPlayerAttackCooldown: (import { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
  winner: null,
};

// 游戏Store接口
interface GameStore {
  // 状态
  gameState: GameState;
  players: Record<string, MechaData>;
  effects: Effect[];
  
  // 游戏流程控制
  startGame: () => void;
  pauseGame: () => void;
  resumeGame: () => void;
  endGame: (winner: string | null) => void;
  resetGame: () => void;
  nextRound: () => void;
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => void;
  updatePlayerState: (playerId: string, state: MechaData['state']) => void;
  updatePlayerFacing: (playerId: string, facingRight: boolean) => void;
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => void;
  updatePlayerDefending: (playerId: string, isDefending: boolean) => void;
  setPlayerAttackCooldown: (playerId: string, cooldown:import { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
  winner: null,
};

// 游戏Store接口
interface GameStore {
  // 状态
  gameState: GameState;
  players: Record<string, MechaData>;
  effects: Effect[];
  
  // 游戏流程控制
  startGame: () => void;
  pauseGame: () => void;
  resumeGame: () => void;
  endGame: (winner: string | null) => void;
  resetGame: () => void;
  nextRound: () => void;
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => void;
  updatePlayerState: (playerId: string, state: MechaData['state']) => void;
  updatePlayerFacing: (playerId: string, facingRight: boolean) => void;
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => void;
  updatePlayerDefending: (playerId: string, isDefending: boolean) => void;
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => void;
  dealimport { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
  winner: null,
};

// 游戏Store接口
interface GameStore {
  // 状态
  gameState: GameState;
  players: Record<string, MechaData>;
  effects: Effect[];
  
  // 游戏流程控制
  startGame: () => void;
  pauseGame: () => void;
  resumeGame: () => void;
  endGame: (winner: string | null) => void;
  resetGame: () => void;
  nextRound: () => void;
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => void;
  updatePlayerState: (playerId: string, state: MechaData['state']) => void;
  updatePlayerFacing: (playerId: string, facingRight: boolean) => void;
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => void;
  updatePlayerDefending: (playerId: string, isDefending: boolean) => void;
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => void;
  dealDamage: (targetId: stringimport { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
  winner: null,
};

// 游戏Store接口
interface GameStore {
  // 状态
  gameState: GameState;
  players: Record<string, MechaData>;
  effects: Effect[];
  
  // 游戏流程控制
  startGame: () => void;
  pauseGame: () => void;
  resumeGame: () => void;
  endGame: (winner: string | null) => void;
  resetGame: () => void;
  nextRound: () => void;
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => void;
  updatePlayerState: (playerId: string, state: MechaData['state']) => void;
  updatePlayerFacing: (playerId: string, facingRight: boolean) => void;
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => void;
  updatePlayerDefending: (playerId: string, isDefending: boolean) => void;
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => void;
  dealDamage: (targetId: string, damage: number, attackerId?:import { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
  winner: null,
};

// 游戏Store接口
interface GameStore {
  // 状态
  gameState: GameState;
  players: Record<string, MechaData>;
  effects: Effect[];
  
  // 游戏流程控制
  startGame: () => void;
  pauseGame: () => void;
  resumeGame: () => void;
  endGame: (winner: string | null) => void;
  resetGame: () => void;
  nextRound: () => void;
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => void;
  updatePlayerState: (playerId: string, state: MechaData['state']) => void;
  updatePlayerFacing: (playerId: string, facingRight: boolean) => void;
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => void;
  updatePlayerDefending: (playerId: string, isDefending: boolean) => void;
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => void;
  dealDamage: (targetId: string, damage: number, attackerId?: string) => void;
  
import { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
  winner: null,
};

// 游戏Store接口
interface GameStore {
  // 状态
  gameState: GameState;
  players: Record<string, MechaData>;
  effects: Effect[];
  
  // 游戏流程控制
  startGame: () => void;
  pauseGame: () => void;
  resumeGame: () => void;
  endGame: (winner: string | null) => void;
  resetGame: () => void;
  nextRound: () => void;
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => void;
  updatePlayerState: (playerId: string, state: MechaData['state']) => void;
  updatePlayerFacing: (playerId: string, facingRight: boolean) => void;
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => void;
  updatePlayerDefending: (playerId: string, isDefending: boolean) => void;
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => void;
  dealDamage: (targetId: string, damage: number, attackerId?: string) => void;
  
  // 动画
  updateimport { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
  winner: null,
};

// 游戏Store接口
interface GameStore {
  // 状态
  gameState: GameState;
  players: Record<string, MechaData>;
  effects: Effect[];
  
  // 游戏流程控制
  startGame: () => void;
  pauseGame: () => void;
  resumeGame: () => void;
  endGame: (winner: string | null) => void;
  resetGame: () => void;
  nextRound: () => void;
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => void;
  updatePlayerState: (playerId: string, state: MechaData['state']) => void;
  updatePlayerFacing: (playerId: string, facingRight: boolean) => void;
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => void;
  updatePlayerDefending: (playerId: string, isDefending: boolean) => void;
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => void;
  dealDamage: (targetId: string, damage: number, attackerId?: string) => void;
  
  // 动画
  updateAnimation: (playerId: stringimport { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
  winner: null,
};

// 游戏Store接口
interface GameStore {
  // 状态
  gameState: GameState;
  players: Record<string, MechaData>;
  effects: Effect[];
  
  // 游戏流程控制
  startGame: () => void;
  pauseGame: () => void;
  resumeGame: () => void;
  endGame: (winner: string | null) => void;
  resetGame: () => void;
  nextRound: () => void;
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => void;
  updatePlayerState: (playerId: string, state: MechaData['state']) => void;
  updatePlayerFacing: (playerId: string, facingRight: boolean) => void;
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => void;
  updatePlayerDefending: (playerId: string, isDefending: boolean) => void;
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => void;
  dealDamage: (targetId: string, damage: number, attackerId?: string) => void;
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) =>import { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
  winner: null,
};

// 游戏Store接口
interface GameStore {
  // 状态
  gameState: GameState;
  players: Record<string, MechaData>;
  effects: Effect[];
  
  // 游戏流程控制
  startGame: () => void;
  pauseGame: () => void;
  resumeGame: () => void;
  endGame: (winner: string | null) => void;
  resetGame: () => void;
  nextRound: () => void;
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => void;
  updatePlayerState: (playerId: string, state: MechaData['state']) => void;
  updatePlayerFacing: (playerId: string, facingRight: boolean) => void;
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => void;
  updatePlayerDefending: (playerId: string, isDefending: boolean) => void;
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => void;
  dealDamage: (targetId: string, damage: number, attackerId?: string) => void;
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => void;
  setAnimationFrameimport { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
  winner: null,
};

// 游戏Store接口
interface GameStore {
  // 状态
  gameState: GameState;
  players: Record<string, MechaData>;
  effects: Effect[];
  
  // 游戏流程控制
  startGame: () => void;
  pauseGame: () => void;
  resumeGame: () => void;
  endGame: (winner: string | null) => void;
  resetGame: () => void;
  nextRound: () => void;
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => void;
  updatePlayerState: (playerId: string, state: MechaData['state']) => void;
  updatePlayerFacing: (playerId: string, facingRight: boolean) => void;
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => void;
  updatePlayerDefending: (playerId: string, isDefending: boolean) => void;
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => void;
  dealDamage: (targetId: string, damage: number, attackerId?: string) => void;
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => void;
  setAnimationFrame: (playerId: string, frameimport { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
  winner: null,
};

// 游戏Store接口
interface GameStore {
  // 状态
  gameState: GameState;
  players: Record<string, MechaData>;
  effects: Effect[];
  
  // 游戏流程控制
  startGame: () => void;
  pauseGame: () => void;
  resumeGame: () => void;
  endGame: (winner: string | null) => void;
  resetGame: () => void;
  nextRound: () => void;
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => void;
  updatePlayerState: (playerId: string, state: MechaData['state']) => void;
  updatePlayerFacing: (playerId: string, facingRight: boolean) => void;
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => void;
  updatePlayerDefending: (playerId: string, isDefending: boolean) => void;
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => void;
  dealDamage: (targetId: string, damage: number, attackerId?: string) => void;
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => void;
  setAnimationFrame: (playerId: string, frame: number) => void;
import { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
  winner: null,
};

// 游戏Store接口
interface GameStore {
  // 状态
  gameState: GameState;
  players: Record<string, MechaData>;
  effects: Effect[];
  
  // 游戏流程控制
  startGame: () => void;
  pauseGame: () => void;
  resumeGame: () => void;
  endGame: (winner: string | null) => void;
  resetGame: () => void;
  nextRound: () => void;
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => void;
  updatePlayerState: (playerId: string, state: MechaData['state']) => void;
  updatePlayerFacing: (playerId: string, facingRight: boolean) => void;
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => void;
  updatePlayerDefending: (playerId: string, isDefending: boolean) => void;
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => void;
  dealDamage: (targetId: string, damage: number, attackerId?: string) => void;
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => void;
  setAnimationFrame: (playerId: string, frame: number) => void;
  
  // 特效import { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
  winner: null,
};

// 游戏Store接口
interface GameStore {
  // 状态
  gameState: GameState;
  players: Record<string, MechaData>;
  effects: Effect[];
  
  // 游戏流程控制
  startGame: () => void;
  pauseGame: () => void;
  resumeGame: () => void;
  endGame: (winner: string | null) => void;
  resetGame: () => void;
  nextRound: () => void;
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => void;
  updatePlayerState: (playerId: string, state: MechaData['state']) => void;
  updatePlayerFacing: (playerId: string, facingRight: boolean) => void;
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => void;
  updatePlayerDefending: (playerId: string, isDefending: boolean) => void;
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => void;
  dealDamage: (targetId: string, damage: number, attackerId?: string) => void;
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => void;
  setAnimationFrame: (playerId: string, frame: number) => void;
  
  // 特效
  addEffect: (effectimport { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
  winner: null,
};

// 游戏Store接口
interface GameStore {
  // 状态
  gameState: GameState;
  players: Record<string, MechaData>;
  effects: Effect[];
  
  // 游戏流程控制
  startGame: () => void;
  pauseGame: () => void;
  resumeGame: () => void;
  endGame: (winner: string | null) => void;
  resetGame: () => void;
  nextRound: () => void;
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => void;
  updatePlayerState: (playerId: string, state: MechaData['state']) => void;
  updatePlayerFacing: (playerId: string, facingRight: boolean) => void;
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => void;
  updatePlayerDefending: (playerId: string, isDefending: boolean) => void;
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => void;
  dealDamage: (targetId: string, damage: number, attackerId?: string) => void;
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => void;
  setAnimationFrame: (playerId: string, frame: number) => void;
  
  // 特效
  addEffect: (effect: Omit<Effect, 'idimport { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
  winner: null,
};

// 游戏Store接口
interface GameStore {
  // 状态
  gameState: GameState;
  players: Record<string, MechaData>;
  effects: Effect[];
  
  // 游戏流程控制
  startGame: () => void;
  pauseGame: () => void;
  resumeGame: () => void;
  endGame: (winner: string | null) => void;
  resetGame: () => void;
  nextRound: () => void;
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => void;
  updatePlayerState: (playerId: string, state: MechaData['state']) => void;
  updatePlayerFacing: (playerId: string, facingRight: boolean) => void;
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => void;
  updatePlayerDefending: (playerId: string, isDefending: boolean) => void;
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => void;
  dealDamage: (targetId: string, damage: number, attackerId?: string) => void;
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => void;
  setAnimationFrame: (playerId: string, frame: number) => void;
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) =>import { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
  winner: null,
};

// 游戏Store接口
interface GameStore {
  // 状态
  gameState: GameState;
  players: Record<string, MechaData>;
  effects: Effect[];
  
  // 游戏流程控制
  startGame: () => void;
  pauseGame: () => void;
  resumeGame: () => void;
  endGame: (winner: string | null) => void;
  resetGame: () => void;
  nextRound: () => void;
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => void;
  updatePlayerState: (playerId: string, state: MechaData['state']) => void;
  updatePlayerFacing: (playerId: string, facingRight: boolean) => void;
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => void;
  updatePlayerDefending: (playerId: string, isDefending: boolean) => void;
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => void;
  dealDamage: (targetId: string, damage: number, attackerId?: string) => void;
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => void;
  setAnimationFrame: (playerId: string, frame: number) => void;
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => string;
  updateEffects:import { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
  winner: null,
};

// 游戏Store接口
interface GameStore {
  // 状态
  gameState: GameState;
  players: Record<string, MechaData>;
  effects: Effect[];
  
  // 游戏流程控制
  startGame: () => void;
  pauseGame: () => void;
  resumeGame: () => void;
  endGame: (winner: string | null) => void;
  resetGame: () => void;
  nextRound: () => void;
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => void;
  updatePlayerState: (playerId: string, state: MechaData['state']) => void;
  updatePlayerFacing: (playerId: string, facingRight: boolean) => void;
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => void;
  updatePlayerDefending: (playerId: string, isDefending: boolean) => void;
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => void;
  dealDamage: (targetId: string, damage: number, attackerId?: string) => void;
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => void;
  setAnimationFrame: (playerId: string, frame: number) => void;
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => string;
  updateEffects: (deltaTime: number) =>import { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
  winner: null,
};

// 游戏Store接口
interface GameStore {
  // 状态
  gameState: GameState;
  players: Record<string, MechaData>;
  effects: Effect[];
  
  // 游戏流程控制
  startGame: () => void;
  pauseGame: () => void;
  resumeGame: () => void;
  endGame: (winner: string | null) => void;
  resetGame: () => void;
  nextRound: () => void;
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => void;
  updatePlayerState: (playerId: string, state: MechaData['state']) => void;
  updatePlayerFacing: (playerId: string, facingRight: boolean) => void;
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => void;
  updatePlayerDefending: (playerId: string, isDefending: boolean) => void;
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => void;
  dealDamage: (targetId: string, damage: number, attackerId?: string) => void;
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => void;
  setAnimationFrame: (playerId: string, frame: number) => void;
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => string;
  updateEffects: (deltaTime: number) => void;
  removeEffect:import { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
  winner: null,
};

// 游戏Store接口
interface GameStore {
  // 状态
  gameState: GameState;
  players: Record<string, MechaData>;
  effects: Effect[];
  
  // 游戏流程控制
  startGame: () => void;
  pauseGame: () => void;
  resumeGame: () => void;
  endGame: (winner: string | null) => void;
  resetGame: () => void;
  nextRound: () => void;
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => void;
  updatePlayerState: (playerId: string, state: MechaData['state']) => void;
  updatePlayerFacing: (playerId: string, facingRight: boolean) => void;
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => void;
  updatePlayerDefending: (playerId: string, isDefending: boolean) => void;
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => void;
  dealDamage: (targetId: string, damage: number, attackerId?: string) => void;
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => void;
  setAnimationFrame: (playerId: string, frame: number) => void;
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => string;
  updateEffects: (deltaTime: number) => void;
  removeEffect: (effectId: string) =>import { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
  winner: null,
};

// 游戏Store接口
interface GameStore {
  // 状态
  gameState: GameState;
  players: Record<string, MechaData>;
  effects: Effect[];
  
  // 游戏流程控制
  startGame: () => void;
  pauseGame: () => void;
  resumeGame: () => void;
  endGame: (winner: string | null) => void;
  resetGame: () => void;
  nextRound: () => void;
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => void;
  updatePlayerState: (playerId: string, state: MechaData['state']) => void;
  updatePlayerFacing: (playerId: string, facingRight: boolean) => void;
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => void;
  updatePlayerDefending: (playerId: string, isDefending: boolean) => void;
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => void;
  dealDamage: (targetId: string, damage: number, attackerId?: string) => void;
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => void;
  setAnimationFrame: (playerId: string, frame: number) => void;
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => string;
  updateEffects: (deltaTime: number) => void;
  removeEffect: (effectId: string) => void;
  clearEffects:import { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
  winner: null,
};

// 游戏Store接口
interface GameStore {
  // 状态
  gameState: GameState;
  players: Record<string, MechaData>;
  effects: Effect[];
  
  // 游戏流程控制
  startGame: () => void;
  pauseGame: () => void;
  resumeGame: () => void;
  endGame: (winner: string | null) => void;
  resetGame: () => void;
  nextRound: () => void;
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => void;
  updatePlayerState: (playerId: string, state: MechaData['state']) => void;
  updatePlayerFacing: (playerId: string, facingRight: boolean) => void;
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => void;
  updatePlayerDefending: (playerId: string, isDefending: boolean) => void;
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => void;
  dealDamage: (targetId: string, damage: number, attackerId?: string) => void;
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => void;
  setAnimationFrame: (playerId: string, frame: number) => void;
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => string;
  updateEffects: (deltaTime: number) => void;
  removeEffect: (effectId: string) => void;
  clearEffects: () => void;
  
import { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
  winner: null,
};

// 游戏Store接口
interface GameStore {
  // 状态
  gameState: GameState;
  players: Record<string, MechaData>;
  effects: Effect[];
  
  // 游戏流程控制
  startGame: () => void;
  pauseGame: () => void;
  resumeGame: () => void;
  endGame: (winner: string | null) => void;
  resetGame: () => void;
  nextRound: () => void;
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => void;
  updatePlayerState: (playerId: string, state: MechaData['state']) => void;
  updatePlayerFacing: (playerId: string, facingRight: boolean) => void;
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => void;
  updatePlayerDefending: (playerId: string, isDefending: boolean) => void;
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => void;
  dealDamage: (targetId: string, damage: number, attackerId?: string) => void;
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => void;
  setAnimationFrame: (playerId: string, frame: number) => void;
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => string;
  updateEffects: (deltaTime: number) => void;
  removeEffect: (effectId: string) => void;
  clearEffects: () => void;
  
  // 游戏时间
  tickimport { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
  winner: null,
};

// 游戏Store接口
interface GameStore {
  // 状态
  gameState: GameState;
  players: Record<string, MechaData>;
  effects: Effect[];
  
  // 游戏流程控制
  startGame: () => void;
  pauseGame: () => void;
  resumeGame: () => void;
  endGame: (winner: string | null) => void;
  resetGame: () => void;
  nextRound: () => void;
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => void;
  updatePlayerState: (playerId: string, state: MechaData['state']) => void;
  updatePlayerFacing: (playerId: string, facingRight: boolean) => void;
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => void;
  updatePlayerDefending: (playerId: string, isDefending: boolean) => void;
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => void;
  dealDamage: (targetId: string, damage: number, attackerId?: string) => void;
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => void;
  setAnimationFrame: (playerId: string, frame: number) => void;
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => string;
  updateEffects: (deltaTime: number) => void;
  removeEffect: (effectId: string) => void;
  clearEffects: () => void;
  
  // 游戏时间
  tick: (deltaTime: number)import { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
  winner: null,
};

// 游戏Store接口
interface GameStore {
  // 状态
  gameState: GameState;
  players: Record<string, MechaData>;
  effects: Effect[];
  
  // 游戏流程控制
  startGame: () => void;
  pauseGame: () => void;
  resumeGame: () => void;
  endGame: (winner: string | null) => void;
  resetGame: () => void;
  nextRound: () => void;
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => void;
  updatePlayerState: (playerId: string, state: MechaData['state']) => void;
  updatePlayerFacing: (playerId: string, facingRight: boolean) => void;
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => void;
  updatePlayerDefending: (playerId: string, isDefending: boolean) => void;
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => void;
  dealDamage: (targetId: string, damage: number, attackerId?: string) => void;
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => void;
  setAnimationFrame: (playerId: string, frame: number) => void;
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => string;
  updateEffects: (deltaTime: number) => void;
  removeEffect: (effectId: string) => void;
  clearEffects: () => void;
  
  // 游戏时间
  tick: (deltaTime: number) => void;
}

import { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
  winner: null,
};

// 游戏Store接口
interface GameStore {
  // 状态
  gameState: GameState;
  players: Record<string, MechaData>;
  effects: Effect[];
  
  // 游戏流程控制
  startGame: () => void;
  pauseGame: () => void;
  resumeGame: () => void;
  endGame: (winner: string | null) => void;
  resetGame: () => void;
  nextRound: () => void;
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => void;
  updatePlayerState: (playerId: string, state: MechaData['state']) => void;
  updatePlayerFacing: (playerId: string, facingRight: boolean) => void;
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => void;
  updatePlayerDefending: (playerId: string, isDefending: boolean) => void;
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => void;
  dealDamage: (targetId: string, damage: number, attackerId?: string) => void;
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => void;
  setAnimationFrame: (playerId: string, frame: number) => void;
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => string;
  updateEffects: (deltaTime: number) => void;
  removeEffect: (effectId: string) => void;
  clearEffects: () => void;
  
  // 游戏时间
  tick: (deltaTime: number) => void;
}

// 创建Game Store
exportimport { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
  winner: null,
};

// 游戏Store接口
interface GameStore {
  // 状态
  gameState: GameState;
  players: Record<string, MechaData>;
  effects: Effect[];
  
  // 游戏流程控制
  startGame: () => void;
  pauseGame: () => void;
  resumeGame: () => void;
  endGame: (winner: string | null) => void;
  resetGame: () => void;
  nextRound: () => void;
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => void;
  updatePlayerState: (playerId: string, state: MechaData['state']) => void;
  updatePlayerFacing: (playerId: string, facingRight: boolean) => void;
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => void;
  updatePlayerDefending: (playerId: string, isDefending: boolean) => void;
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => void;
  dealDamage: (targetId: string, damage: number, attackerId?: string) => void;
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => void;
  setAnimationFrame: (playerId: string, frame: number) => void;
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => string;
  updateEffects: (deltaTime: number) => void;
  removeEffect: (effectId: string) => void;
  clearEffects: () => void;
  
  // 游戏时间
  tick: (deltaTime: number) => void;
}

// 创建Game Store
export const useGameStore = create<Gameimport { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
  winner: null,
};

// 游戏Store接口
interface GameStore {
  // 状态
  gameState: GameState;
  players: Record<string, MechaData>;
  effects: Effect[];
  
  // 游戏流程控制
  startGame: () => void;
  pauseGame: () => void;
  resumeGame: () => void;
  endGame: (winner: string | null) => void;
  resetGame: () => void;
  nextRound: () => void;
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => void;
  updatePlayerState: (playerId: string, state: MechaData['state']) => void;
  updatePlayerFacing: (playerId: string, facingRight: boolean) => void;
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => void;
  updatePlayerDefending: (playerId: string, isDefending: boolean) => void;
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => void;
  dealDamage: (targetId: string, damage: number, attackerId?: string) => void;
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => void;
  setAnimationFrame: (playerId: string, frame: number) => void;
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => string;
  updateEffects: (deltaTime: number) => void;
  removeEffect: (effectId: string) => void;
  clearEffects: () => void;
  
  // 游戏时间
  tick: (deltaTime: number) => void;
}

// 创建Game Store
export const useGameStore = create<GameStore>((set, get) =>import { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
  winner: null,
};

// 游戏Store接口
interface GameStore {
  // 状态
  gameState: GameState;
  players: Record<string, MechaData>;
  effects: Effect[];
  
  // 游戏流程控制
  startGame: () => void;
  pauseGame: () => void;
  resumeGame: () => void;
  endGame: (winner: string | null) => void;
  resetGame: () => void;
  nextRound: () => void;
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => void;
  updatePlayerState: (playerId: string, state: MechaData['state']) => void;
  updatePlayerFacing: (playerId: string, facingRight: boolean) => void;
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => void;
  updatePlayerDefending: (playerId: string, isDefending: boolean) => void;
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => void;
  dealDamage: (targetId: string, damage: number, attackerId?: string) => void;
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => void;
  setAnimationFrame: (playerId: string, frame: number) => void;
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => string;
  updateEffects: (deltaTime: number) => void;
  removeEffect: (effectId: string) => void;
  clearEffects: () => void;
  
  // 游戏时间
  tick: (deltaTime: number) => void;
}

// 创建Game Store
export const useGameStore = create<GameStore>((set, get) => ({
  // 初始import { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
  winner: null,
};

// 游戏Store接口
interface GameStore {
  // 状态
  gameState: GameState;
  players: Record<string, MechaData>;
  effects: Effect[];
  
  // 游戏流程控制
  startGame: () => void;
  pauseGame: () => void;
  resumeGame: () => void;
  endGame: (winner: string | null) => void;
  resetGame: () => void;
  nextRound: () => void;
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => void;
  updatePlayerState: (playerId: string, state: MechaData['state']) => void;
  updatePlayerFacing: (playerId: string, facingRight: boolean) => void;
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => void;
  updatePlayerDefending: (playerId: string, isDefending: boolean) => void;
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => void;
  dealDamage: (targetId: string, damage: number, attackerId?: string) => void;
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => void;
  setAnimationFrame: (playerId: string, frame: number) => void;
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => string;
  updateEffects: (deltaTime: number) => void;
  removeEffect: (effectId: string) => void;
  clearEffects: () => void;
  
  // 游戏时间
  tick: (deltaTime: number) => void;
}

// 创建Game Store
export const useGameStore = create<GameStore>((set, get) => ({
  // 初始状态
  gameState: initialimport { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
  winner: null,
};

// 游戏Store接口
interface GameStore {
  // 状态
  gameState: GameState;
  players: Record<string, MechaData>;
  effects: Effect[];
  
  // 游戏流程控制
  startGame: () => void;
  pauseGame: () => void;
  resumeGame: () => void;
  endGame: (winner: string | null) => void;
  resetGame: () => void;
  nextRound: () => void;
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => void;
  updatePlayerState: (playerId: string, state: MechaData['state']) => void;
  updatePlayerFacing: (playerId: string, facingRight: boolean) => void;
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => void;
  updatePlayerDefending: (playerId: string, isDefending: boolean) => void;
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => void;
  dealDamage: (targetId: string, damage: number, attackerId?: string) => void;
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => void;
  setAnimationFrame: (playerId: string, frame: number) => void;
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => string;
  updateEffects: (deltaTime: number) => void;
  removeEffect: (effectId: string) => void;
  clearEffects: () => void;
  
  // 游戏时间
  tick: (deltaTime: number) => void;
}

// 创建Game Store
export const useGameStore = create<GameStore>((set, get) => ({
  // 初始状态
  gameState: initialGameState,
  players:import { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
  winner: null,
};

// 游戏Store接口
interface GameStore {
  // 状态
  gameState: GameState;
  players: Record<string, MechaData>;
  effects: Effect[];
  
  // 游戏流程控制
  startGame: () => void;
  pauseGame: () => void;
  resumeGame: () => void;
  endGame: (winner: string | null) => void;
  resetGame: () => void;
  nextRound: () => void;
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => void;
  updatePlayerState: (playerId: string, state: MechaData['state']) => void;
  updatePlayerFacing: (playerId: string, facingRight: boolean) => void;
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => void;
  updatePlayerDefending: (playerId: string, isDefending: boolean) => void;
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => void;
  dealDamage: (targetId: string, damage: number, attackerId?: string) => void;
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => void;
  setAnimationFrame: (playerId: string, frame: number) => void;
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => string;
  updateEffects: (deltaTime: number) => void;
  removeEffect: (effectId: string) => void;
  clearEffects: () => void;
  
  // 游戏时间
  tick: (deltaTime: number) => void;
}

// 创建Game Store
export const useGameStore = create<GameStore>((set, get) => ({
  // 初始状态
  gameState: initialGameState,
  players: {
    player1: createimport { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
  winner: null,
};

// 游戏Store接口
interface GameStore {
  // 状态
  gameState: GameState;
  players: Record<string, MechaData>;
  effects: Effect[];
  
  // 游戏流程控制
  startGame: () => void;
  pauseGame: () => void;
  resumeGame: () => void;
  endGame: (winner: string | null) => void;
  resetGame: () => void;
  nextRound: () => void;
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => void;
  updatePlayerState: (playerId: string, state: MechaData['state']) => void;
  updatePlayerFacing: (playerId: string, facingRight: boolean) => void;
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => void;
  updatePlayerDefending: (playerId: string, isDefending: boolean) => void;
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => void;
  dealDamage: (targetId: string, damage: number, attackerId?: string) => void;
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => void;
  setAnimationFrame: (playerId: string, frame: number) => void;
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => string;
  updateEffects: (deltaTime: number) => void;
  removeEffect: (effectId: string) => void;
  clearEffects: () => void;
  
  // 游戏时间
  tick: (deltaTime: number) => void;
}

// 创建Game Store
export const useGameStore = create<GameStore>((set, get) => ({
  // 初始状态
  gameState: initialGameState,
  players: {
    player1: createInitialMecha('player1',import { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
  winner: null,
};

// 游戏Store接口
interface GameStore {
  // 状态
  gameState: GameState;
  players: Record<string, MechaData>;
  effects: Effect[];
  
  // 游戏流程控制
  startGame: () => void;
  pauseGame: () => void;
  resumeGame: () => void;
  endGame: (winner: string | null) => void;
  resetGame: () => void;
  nextRound: () => void;
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => void;
  updatePlayerState: (playerId: string, state: MechaData['state']) => void;
  updatePlayerFacing: (playerId: string, facingRight: boolean) => void;
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => void;
  updatePlayerDefending: (playerId: string, isDefending: boolean) => void;
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => void;
  dealDamage: (targetId: string, damage: number, attackerId?: string) => void;
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => void;
  setAnimationFrame: (playerId: string, frame: number) => void;
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => string;
  updateEffects: (deltaTime: number) => void;
  removeEffect: (effectId: string) => void;
  clearEffects: () => void;
  
  // 游戏时间
  tick: (deltaTime: number) => void;
}

// 创建Game Store
export const useGameStore = create<GameStore>((set, get) => ({
  // 初始状态
  gameState: initialGameState,
  players: {
    player1: createInitialMecha('player1', 'BLUE', 200),
import { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
  winner: null,
};

// 游戏Store接口
interface GameStore {
  // 状态
  gameState: GameState;
  players: Record<string, MechaData>;
  effects: Effect[];
  
  // 游戏流程控制
  startGame: () => void;
  pauseGame: () => void;
  resumeGame: () => void;
  endGame: (winner: string | null) => void;
  resetGame: () => void;
  nextRound: () => void;
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => void;
  updatePlayerState: (playerId: string, state: MechaData['state']) => void;
  updatePlayerFacing: (playerId: string, facingRight: boolean) => void;
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => void;
  updatePlayerDefending: (playerId: string, isDefending: boolean) => void;
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => void;
  dealDamage: (targetId: string, damage: number, attackerId?: string) => void;
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => void;
  setAnimationFrame: (playerId: string, frame: number) => void;
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => string;
  updateEffects: (deltaTime: number) => void;
  removeEffect: (effectId: string) => void;
  clearEffects: () => void;
  
  // 游戏时间
  tick: (deltaTime: number) => void;
}

// 创建Game Store
export const useGameStore = create<GameStore>((set, get) => ({
  // 初始状态
  gameState: initialGameState,
  players: {
    player1: createInitialMecha('player1', 'BLUE', 200),
    player2: createInitialMechaimport { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
  winner: null,
};

// 游戏Store接口
interface GameStore {
  // 状态
  gameState: GameState;
  players: Record<string, MechaData>;
  effects: Effect[];
  
  // 游戏流程控制
  startGame: () => void;
  pauseGame: () => void;
  resumeGame: () => void;
  endGame: (winner: string | null) => void;
  resetGame: () => void;
  nextRound: () => void;
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => void;
  updatePlayerState: (playerId: string, state: MechaData['state']) => void;
  updatePlayerFacing: (playerId: string, facingRight: boolean) => void;
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => void;
  updatePlayerDefending: (playerId: string, isDefending: boolean) => void;
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => void;
  dealDamage: (targetId: string, damage: number, attackerId?: string) => void;
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => void;
  setAnimationFrame: (playerId: string, frame: number) => void;
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => string;
  updateEffects: (deltaTime: number) => void;
  removeEffect: (effectId: string) => void;
  clearEffects: () => void;
  
  // 游戏时间
  tick: (deltaTime: number) => void;
}

// 创建Game Store
export const useGameStore = create<GameStore>((set, get) => ({
  // 初始状态
  gameState: initialGameState,
  players: {
    player1: createInitialMecha('player1', 'BLUE', 200),
    player2: createInitialMecha('player2', 'RED',import { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
  winner: null,
};

// 游戏Store接口
interface GameStore {
  // 状态
  gameState: GameState;
  players: Record<string, MechaData>;
  effects: Effect[];
  
  // 游戏流程控制
  startGame: () => void;
  pauseGame: () => void;
  resumeGame: () => void;
  endGame: (winner: string | null) => void;
  resetGame: () => void;
  nextRound: () => void;
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => void;
  updatePlayerState: (playerId: string, state: MechaData['state']) => void;
  updatePlayerFacing: (playerId: string, facingRight: boolean) => void;
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => void;
  updatePlayerDefending: (playerId: string, isDefending: boolean) => void;
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => void;
  dealDamage: (targetId: string, damage: number, attackerId?: string) => void;
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => void;
  setAnimationFrame: (playerId: string, frame: number) => void;
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => string;
  updateEffects: (deltaTime: number) => void;
  removeEffect: (effectId: string) => void;
  clearEffects: () => void;
  
  // 游戏时间
  tick: (deltaTime: number) => void;
}

// 创建Game Store
export const useGameStore = create<GameStore>((set, get) => ({
  // 初始状态
  gameState: initialGameState,
  players: {
    player1: createInitialMecha('player1', 'BLUE', 200),
    player2: createInitialMecha('player2', 'RED', 550),
  },
import { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
  winner: null,
};

// 游戏Store接口
interface GameStore {
  // 状态
  gameState: GameState;
  players: Record<string, MechaData>;
  effects: Effect[];
  
  // 游戏流程控制
  startGame: () => void;
  pauseGame: () => void;
  resumeGame: () => void;
  endGame: (winner: string | null) => void;
  resetGame: () => void;
  nextRound: () => void;
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => void;
  updatePlayerState: (playerId: string, state: MechaData['state']) => void;
  updatePlayerFacing: (playerId: string, facingRight: boolean) => void;
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => void;
  updatePlayerDefending: (playerId: string, isDefending: boolean) => void;
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => void;
  dealDamage: (targetId: string, damage: number, attackerId?: string) => void;
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => void;
  setAnimationFrame: (playerId: string, frame: number) => void;
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => string;
  updateEffects: (deltaTime: number) => void;
  removeEffect: (effectId: string) => void;
  clearEffects: () => void;
  
  // 游戏时间
  tick: (deltaTime: number) => void;
}

// 创建Game Store
export const useGameStore = create<GameStore>((set, get) => ({
  // 初始状态
  gameState: initialGameState,
  players: {
    player1: createInitialMecha('player1', 'BLUE', 200),
    player2: createInitialMecha('player2', 'RED', 550),
  },
  effects: [],
import { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
  winner: null,
};

// 游戏Store接口
interface GameStore {
  // 状态
  gameState: GameState;
  players: Record<string, MechaData>;
  effects: Effect[];
  
  // 游戏流程控制
  startGame: () => void;
  pauseGame: () => void;
  resumeGame: () => void;
  endGame: (winner: string | null) => void;
  resetGame: () => void;
  nextRound: () => void;
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => void;
  updatePlayerState: (playerId: string, state: MechaData['state']) => void;
  updatePlayerFacing: (playerId: string, facingRight: boolean) => void;
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => void;
  updatePlayerDefending: (playerId: string, isDefending: boolean) => void;
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => void;
  dealDamage: (targetId: string, damage: number, attackerId?: string) => void;
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => void;
  setAnimationFrame: (playerId: string, frame: number) => void;
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => string;
  updateEffects: (deltaTime: number) => void;
  removeEffect: (effectId: string) => void;
  clearEffects: () => void;
  
  // 游戏时间
  tick: (deltaTime: number) => void;
}

// 创建Game Store
export const useGameStore = create<GameStore>((set, get) => ({
  // 初始状态
  gameState: initialGameState,
  players: {
    player1: createInitialMecha('player1', 'BLUE', 200),
    player2: createInitialMecha('player2', 'RED', 550),
  },
  effects: [],
  
  // 游戏流程import { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
  winner: null,
};

// 游戏Store接口
interface GameStore {
  // 状态
  gameState: GameState;
  players: Record<string, MechaData>;
  effects: Effect[];
  
  // 游戏流程控制
  startGame: () => void;
  pauseGame: () => void;
  resumeGame: () => void;
  endGame: (winner: string | null) => void;
  resetGame: () => void;
  nextRound: () => void;
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => void;
  updatePlayerState: (playerId: string, state: MechaData['state']) => void;
  updatePlayerFacing: (playerId: string, facingRight: boolean) => void;
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => void;
  updatePlayerDefending: (playerId: string, isDefending: boolean) => void;
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => void;
  dealDamage: (targetId: string, damage: number, attackerId?: string) => void;
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => void;
  setAnimationFrame: (playerId: string, frame: number) => void;
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => string;
  updateEffects: (deltaTime: number) => void;
  removeEffect: (effectId: string) => void;
  clearEffects: () => void;
  
  // 游戏时间
  tick: (deltaTime: number) => void;
}

// 创建Game Store
export const useGameStore = create<GameStore>((set, get) => ({
  // 初始状态
  gameState: initialGameState,
  players: {
    player1: createInitialMecha('player1', 'BLUE', 200),
    player2: createInitialMecha('player2', 'RED', 550),
  },
  effects: [],
  
  // 游戏流程控制
  startGame: ()import { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
  winner: null,
};

// 游戏Store接口
interface GameStore {
  // 状态
  gameState: GameState;
  players: Record<string, MechaData>;
  effects: Effect[];
  
  // 游戏流程控制
  startGame: () => void;
  pauseGame: () => void;
  resumeGame: () => void;
  endGame: (winner: string | null) => void;
  resetGame: () => void;
  nextRound: () => void;
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => void;
  updatePlayerState: (playerId: string, state: MechaData['state']) => void;
  updatePlayerFacing: (playerId: string, facingRight: boolean) => void;
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => void;
  updatePlayerDefending: (playerId: string, isDefending: boolean) => void;
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => void;
  dealDamage: (targetId: string, damage: number, attackerId?: string) => void;
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => void;
  setAnimationFrame: (playerId: string, frame: number) => void;
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => string;
  updateEffects: (deltaTime: number) => void;
  removeEffect: (effectId: string) => void;
  clearEffects: () => void;
  
  // 游戏时间
  tick: (deltaTime: number) => void;
}

// 创建Game Store
export const useGameStore = create<GameStore>((set, get) => ({
  // 初始状态
  gameState: initialGameState,
  players: {
    player1: createInitialMecha('player1', 'BLUE', 200),
    player2: createInitialMecha('player2', 'RED', 550),
  },
  effects: [],
  
  // 游戏流程控制
  startGame: () => set(produce(state => {
import { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
  winner: null,
};

// 游戏Store接口
interface GameStore {
  // 状态
  gameState: GameState;
  players: Record<string, MechaData>;
  effects: Effect[];
  
  // 游戏流程控制
  startGame: () => void;
  pauseGame: () => void;
  resumeGame: () => void;
  endGame: (winner: string | null) => void;
  resetGame: () => void;
  nextRound: () => void;
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => void;
  updatePlayerState: (playerId: string, state: MechaData['state']) => void;
  updatePlayerFacing: (playerId: string, facingRight: boolean) => void;
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => void;
  updatePlayerDefending: (playerId: string, isDefending: boolean) => void;
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => void;
  dealDamage: (targetId: string, damage: number, attackerId?: string) => void;
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => void;
  setAnimationFrame: (playerId: string, frame: number) => void;
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => string;
  updateEffects: (deltaTime: number) => void;
  removeEffect: (effectId: string) => void;
  clearEffects: () => void;
  
  // 游戏时间
  tick: (deltaTime: number) => void;
}

// 创建Game Store
export const useGameStore = create<GameStore>((set, get) => ({
  // 初始状态
  gameState: initialGameState,
  players: {
    player1: createInitialMecha('player1', 'BLUE', 200),
    player2: createInitialMecha('player2', 'RED', 550),
  },
  effects: [],
  
  // 游戏流程控制
  startGame: () => set(produce(state => {
    state.gameState.phase = 'import { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
  winner: null,
};

// 游戏Store接口
interface GameStore {
  // 状态
  gameState: GameState;
  players: Record<string, MechaData>;
  effects: Effect[];
  
  // 游戏流程控制
  startGame: () => void;
  pauseGame: () => void;
  resumeGame: () => void;
  endGame: (winner: string | null) => void;
  resetGame: () => void;
  nextRound: () => void;
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => void;
  updatePlayerState: (playerId: string, state: MechaData['state']) => void;
  updatePlayerFacing: (playerId: string, facingRight: boolean) => void;
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => void;
  updatePlayerDefending: (playerId: string, isDefending: boolean) => void;
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => void;
  dealDamage: (targetId: string, damage: number, attackerId?: string) => void;
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => void;
  setAnimationFrame: (playerId: string, frame: number) => void;
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => string;
  updateEffects: (deltaTime: number) => void;
  removeEffect: (effectId: string) => void;
  clearEffects: () => void;
  
  // 游戏时间
  tick: (deltaTime: number) => void;
}

// 创建Game Store
export const useGameStore = create<GameStore>((set, get) => ({
  // 初始状态
  gameState: initialGameState,
  players: {
    player1: createInitialMecha('player1', 'BLUE', 200),
    player2: createInitialMecha('player2', 'RED', 550),
  },
  effects: [],
  
  // 游戏流程控制
  startGame: () => set(produce(state => {
    state.gameState.phase = 'PLAYING';
    stateimport { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
  winner: null,
};

// 游戏Store接口
interface GameStore {
  // 状态
  gameState: GameState;
  players: Record<string, MechaData>;
  effects: Effect[];
  
  // 游戏流程控制
  startGame: () => void;
  pauseGame: () => void;
  resumeGame: () => void;
  endGame: (winner: string | null) => void;
  resetGame: () => void;
  nextRound: () => void;
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => void;
  updatePlayerState: (playerId: string, state: MechaData['state']) => void;
  updatePlayerFacing: (playerId: string, facingRight: boolean) => void;
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => void;
  updatePlayerDefending: (playerId: string, isDefending: boolean) => void;
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => void;
  dealDamage: (targetId: string, damage: number, attackerId?: string) => void;
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => void;
  setAnimationFrame: (playerId: string, frame: number) => void;
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => string;
  updateEffects: (deltaTime: number) => void;
  removeEffect: (effectId: string) => void;
  clearEffects: () => void;
  
  // 游戏时间
  tick: (deltaTime: number) => void;
}

// 创建Game Store
export const useGameStore = create<GameStore>((set, get) => ({
  // 初始状态
  gameState: initialGameState,
  players: {
    player1: createInitialMecha('player1', 'BLUE', 200),
    player2: createInitialMecha('player2', 'RED', 550),
  },
  effects: [],
  
  // 游戏流程控制
  startGame: () => set(produce(state => {
    state.gameState.phase = 'PLAYING';
    state.gameState.timeRemaining = GAME_CONSTANTimport { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
  winner: null,
};

// 游戏Store接口
interface GameStore {
  // 状态
  gameState: GameState;
  players: Record<string, MechaData>;
  effects: Effect[];
  
  // 游戏流程控制
  startGame: () => void;
  pauseGame: () => void;
  resumeGame: () => void;
  endGame: (winner: string | null) => void;
  resetGame: () => void;
  nextRound: () => void;
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => void;
  updatePlayerState: (playerId: string, state: MechaData['state']) => void;
  updatePlayerFacing: (playerId: string, facingRight: boolean) => void;
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => void;
  updatePlayerDefending: (playerId: string, isDefending: boolean) => void;
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => void;
  dealDamage: (targetId: string, damage: number, attackerId?: string) => void;
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => void;
  setAnimationFrame: (playerId: string, frame: number) => void;
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => string;
  updateEffects: (deltaTime: number) => void;
  removeEffect: (effectId: string) => void;
  clearEffects: () => void;
  
  // 游戏时间
  tick: (deltaTime: number) => void;
}

// 创建Game Store
export const useGameStore = create<GameStore>((set, get) => ({
  // 初始状态
  gameState: initialGameState,
  players: {
    player1: createInitialMecha('player1', 'BLUE', 200),
    player2: createInitialMecha('player2', 'RED', 550),
  },
  effects: [],
  
  // 游戏流程控制
  startGame: () => set(produce(state => {
    state.gameState.phase = 'PLAYING';
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    stateimport { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
  winner: null,
};

// 游戏Store接口
interface GameStore {
  // 状态
  gameState: GameState;
  players: Record<string, MechaData>;
  effects: Effect[];
  
  // 游戏流程控制
  startGame: () => void;
  pauseGame: () => void;
  resumeGame: () => void;
  endGame: (winner: string | null) => void;
  resetGame: () => void;
  nextRound: () => void;
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => void;
  updatePlayerState: (playerId: string, state: MechaData['state']) => void;
  updatePlayerFacing: (playerId: string, facingRight: boolean) => void;
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => void;
  updatePlayerDefending: (playerId: string, isDefending: boolean) => void;
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => void;
  dealDamage: (targetId: string, damage: number, attackerId?: string) => void;
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => void;
  setAnimationFrame: (playerId: string, frame: number) => void;
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => string;
  updateEffects: (deltaTime: number) => void;
  removeEffect: (effectId: string) => void;
  clearEffects: () => void;
  
  // 游戏时间
  tick: (deltaTime: number) => void;
}

// 创建Game Store
export const useGameStore = create<GameStore>((set, get) => ({
  // 初始状态
  gameState: initialGameState,
  players: {
    player1: createInitialMecha('player1', 'BLUE', 200),
    player2: createInitialMecha('player2', 'RED', 550),
  },
  effects: [],
  
  // 游戏流程控制
  startGame: () => set(produce(state => {
    state.gameState.phase = 'PLAYING';
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.winner = null;
import { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
  winner: null,
};

// 游戏Store接口
interface GameStore {
  // 状态
  gameState: GameState;
  players: Record<string, MechaData>;
  effects: Effect[];
  
  // 游戏流程控制
  startGame: () => void;
  pauseGame: () => void;
  resumeGame: () => void;
  endGame: (winner: string | null) => void;
  resetGame: () => void;
  nextRound: () => void;
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => void;
  updatePlayerState: (playerId: string, state: MechaData['state']) => void;
  updatePlayerFacing: (playerId: string, facingRight: boolean) => void;
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => void;
  updatePlayerDefending: (playerId: string, isDefending: boolean) => void;
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => void;
  dealDamage: (targetId: string, damage: number, attackerId?: string) => void;
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => void;
  setAnimationFrame: (playerId: string, frame: number) => void;
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => string;
  updateEffects: (deltaTime: number) => void;
  removeEffect: (effectId: string) => void;
  clearEffects: () => void;
  
  // 游戏时间
  tick: (deltaTime: number) => void;
}

// 创建Game Store
export const useGameStore = create<GameStore>((set, get) => ({
  // 初始状态
  gameState: initialGameState,
  players: {
    player1: createInitialMecha('player1', 'BLUE', 200),
    player2: createInitialMecha('player2', 'RED', 550),
  },
  effects: [],
  
  // 游戏流程控制
  startGame: () => set(produce(state => {
    state.gameState.phase = 'PLAYING';
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.winner = null;
  })),
  
import { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
  winner: null,
};

// 游戏Store接口
interface GameStore {
  // 状态
  gameState: GameState;
  players: Record<string, MechaData>;
  effects: Effect[];
  
  // 游戏流程控制
  startGame: () => void;
  pauseGame: () => void;
  resumeGame: () => void;
  endGame: (winner: string | null) => void;
  resetGame: () => void;
  nextRound: () => void;
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => void;
  updatePlayerState: (playerId: string, state: MechaData['state']) => void;
  updatePlayerFacing: (playerId: string, facingRight: boolean) => void;
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => void;
  updatePlayerDefending: (playerId: string, isDefending: boolean) => void;
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => void;
  dealDamage: (targetId: string, damage: number, attackerId?: string) => void;
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => void;
  setAnimationFrame: (playerId: string, frame: number) => void;
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => string;
  updateEffects: (deltaTime: number) => void;
  removeEffect: (effectId: string) => void;
  clearEffects: () => void;
  
  // 游戏时间
  tick: (deltaTime: number) => void;
}

// 创建Game Store
export const useGameStore = create<GameStore>((set, get) => ({
  // 初始状态
  gameState: initialGameState,
  players: {
    player1: createInitialMecha('player1', 'BLUE', 200),
    player2: createInitialMecha('player2', 'RED', 550),
  },
  effects: [],
  
  // 游戏流程控制
  startGame: () => set(produce(state => {
    state.gameState.phase = 'PLAYING';
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.winner = null;
  })),
  
  pauseGame: () => setimport { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
  winner: null,
};

// 游戏Store接口
interface GameStore {
  // 状态
  gameState: GameState;
  players: Record<string, MechaData>;
  effects: Effect[];
  
  // 游戏流程控制
  startGame: () => void;
  pauseGame: () => void;
  resumeGame: () => void;
  endGame: (winner: string | null) => void;
  resetGame: () => void;
  nextRound: () => void;
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => void;
  updatePlayerState: (playerId: string, state: MechaData['state']) => void;
  updatePlayerFacing: (playerId: string, facingRight: boolean) => void;
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => void;
  updatePlayerDefending: (playerId: string, isDefending: boolean) => void;
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => void;
  dealDamage: (targetId: string, damage: number, attackerId?: string) => void;
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => void;
  setAnimationFrame: (playerId: string, frame: number) => void;
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => string;
  updateEffects: (deltaTime: number) => void;
  removeEffect: (effectId: string) => void;
  clearEffects: () => void;
  
  // 游戏时间
  tick: (deltaTime: number) => void;
}

// 创建Game Store
export const useGameStore = create<GameStore>((set, get) => ({
  // 初始状态
  gameState: initialGameState,
  players: {
    player1: createInitialMecha('player1', 'BLUE', 200),
    player2: createInitialMecha('player2', 'RED', 550),
  },
  effects: [],
  
  // 游戏流程控制
  startGame: () => set(produce(state => {
    state.gameState.phase = 'PLAYING';
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.winner = null;
  })),
  
  pauseGame: () => set(produce(state => {
    ifimport { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
  winner: null,
};

// 游戏Store接口
interface GameStore {
  // 状态
  gameState: GameState;
  players: Record<string, MechaData>;
  effects: Effect[];
  
  // 游戏流程控制
  startGame: () => void;
  pauseGame: () => void;
  resumeGame: () => void;
  endGame: (winner: string | null) => void;
  resetGame: () => void;
  nextRound: () => void;
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => void;
  updatePlayerState: (playerId: string, state: MechaData['state']) => void;
  updatePlayerFacing: (playerId: string, facingRight: boolean) => void;
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => void;
  updatePlayerDefending: (playerId: string, isDefending: boolean) => void;
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => void;
  dealDamage: (targetId: string, damage: number, attackerId?: string) => void;
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => void;
  setAnimationFrame: (playerId: string, frame: number) => void;
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => string;
  updateEffects: (deltaTime: number) => void;
  removeEffect: (effectId: string) => void;
  clearEffects: () => void;
  
  // 游戏时间
  tick: (deltaTime: number) => void;
}

// 创建Game Store
export const useGameStore = create<GameStore>((set, get) => ({
  // 初始状态
  gameState: initialGameState,
  players: {
    player1: createInitialMecha('player1', 'BLUE', 200),
    player2: createInitialMecha('player2', 'RED', 550),
  },
  effects: [],
  
  // 游戏流程控制
  startGame: () => set(produce(state => {
    state.gameState.phase = 'PLAYING';
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.winner = null;
  })),
  
  pauseGame: () => set(produce(state => {
    if (state.gameState.phase === 'import { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
  winner: null,
};

// 游戏Store接口
interface GameStore {
  // 状态
  gameState: GameState;
  players: Record<string, MechaData>;
  effects: Effect[];
  
  // 游戏流程控制
  startGame: () => void;
  pauseGame: () => void;
  resumeGame: () => void;
  endGame: (winner: string | null) => void;
  resetGame: () => void;
  nextRound: () => void;
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => void;
  updatePlayerState: (playerId: string, state: MechaData['state']) => void;
  updatePlayerFacing: (playerId: string, facingRight: boolean) => void;
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => void;
  updatePlayerDefending: (playerId: string, isDefending: boolean) => void;
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => void;
  dealDamage: (targetId: string, damage: number, attackerId?: string) => void;
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => void;
  setAnimationFrame: (playerId: string, frame: number) => void;
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => string;
  updateEffects: (deltaTime: number) => void;
  removeEffect: (effectId: string) => void;
  clearEffects: () => void;
  
  // 游戏时间
  tick: (deltaTime: number) => void;
}

// 创建Game Store
export const useGameStore = create<GameStore>((set, get) => ({
  // 初始状态
  gameState: initialGameState,
  players: {
    player1: createInitialMecha('player1', 'BLUE', 200),
    player2: createInitialMecha('player2', 'RED', 550),
  },
  effects: [],
  
  // 游戏流程控制
  startGame: () => set(produce(state => {
    state.gameState.phase = 'PLAYING';
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.winner = null;
  })),
  
  pauseGame: () => set(produce(state => {
    if (state.gameState.phase === 'PLAYING') {
      stateimport { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
  winner: null,
};

// 游戏Store接口
interface GameStore {
  // 状态
  gameState: GameState;
  players: Record<string, MechaData>;
  effects: Effect[];
  
  // 游戏流程控制
  startGame: () => void;
  pauseGame: () => void;
  resumeGame: () => void;
  endGame: (winner: string | null) => void;
  resetGame: () => void;
  nextRound: () => void;
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => void;
  updatePlayerState: (playerId: string, state: MechaData['state']) => void;
  updatePlayerFacing: (playerId: string, facingRight: boolean) => void;
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => void;
  updatePlayerDefending: (playerId: string, isDefending: boolean) => void;
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => void;
  dealDamage: (targetId: string, damage: number, attackerId?: string) => void;
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => void;
  setAnimationFrame: (playerId: string, frame: number) => void;
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => string;
  updateEffects: (deltaTime: number) => void;
  removeEffect: (effectId: string) => void;
  clearEffects: () => void;
  
  // 游戏时间
  tick: (deltaTime: number) => void;
}

// 创建Game Store
export const useGameStore = create<GameStore>((set, get) => ({
  // 初始状态
  gameState: initialGameState,
  players: {
    player1: createInitialMecha('player1', 'BLUE', 200),
    player2: createInitialMecha('player2', 'RED', 550),
  },
  effects: [],
  
  // 游戏流程控制
  startGame: () => set(produce(state => {
    state.gameState.phase = 'PLAYING';
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.winner = null;
  })),
  
  pauseGame: () => set(produce(state => {
    if (state.gameState.phase === 'PLAYING') {
      state.gameState.phase = 'PAUSEDimport { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
  winner: null,
};

// 游戏Store接口
interface GameStore {
  // 状态
  gameState: GameState;
  players: Record<string, MechaData>;
  effects: Effect[];
  
  // 游戏流程控制
  startGame: () => void;
  pauseGame: () => void;
  resumeGame: () => void;
  endGame: (winner: string | null) => void;
  resetGame: () => void;
  nextRound: () => void;
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => void;
  updatePlayerState: (playerId: string, state: MechaData['state']) => void;
  updatePlayerFacing: (playerId: string, facingRight: boolean) => void;
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => void;
  updatePlayerDefending: (playerId: string, isDefending: boolean) => void;
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => void;
  dealDamage: (targetId: string, damage: number, attackerId?: string) => void;
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => void;
  setAnimationFrame: (playerId: string, frame: number) => void;
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => string;
  updateEffects: (deltaTime: number) => void;
  removeEffect: (effectId: string) => void;
  clearEffects: () => void;
  
  // 游戏时间
  tick: (deltaTime: number) => void;
}

// 创建Game Store
export const useGameStore = create<GameStore>((set, get) => ({
  // 初始状态
  gameState: initialGameState,
  players: {
    player1: createInitialMecha('player1', 'BLUE', 200),
    player2: createInitialMecha('player2', 'RED', 550),
  },
  effects: [],
  
  // 游戏流程控制
  startGame: () => set(produce(state => {
    state.gameState.phase = 'PLAYING';
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.winner = null;
  })),
  
  pauseGame: () => set(produce(state => {
    if (state.gameState.phase === 'PLAYING') {
      state.gameState.phase = 'PAUSED';
    }
  }import { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
  winner: null,
};

// 游戏Store接口
interface GameStore {
  // 状态
  gameState: GameState;
  players: Record<string, MechaData>;
  effects: Effect[];
  
  // 游戏流程控制
  startGame: () => void;
  pauseGame: () => void;
  resumeGame: () => void;
  endGame: (winner: string | null) => void;
  resetGame: () => void;
  nextRound: () => void;
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => void;
  updatePlayerState: (playerId: string, state: MechaData['state']) => void;
  updatePlayerFacing: (playerId: string, facingRight: boolean) => void;
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => void;
  updatePlayerDefending: (playerId: string, isDefending: boolean) => void;
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => void;
  dealDamage: (targetId: string, damage: number, attackerId?: string) => void;
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => void;
  setAnimationFrame: (playerId: string, frame: number) => void;
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => string;
  updateEffects: (deltaTime: number) => void;
  removeEffect: (effectId: string) => void;
  clearEffects: () => void;
  
  // 游戏时间
  tick: (deltaTime: number) => void;
}

// 创建Game Store
export const useGameStore = create<GameStore>((set, get) => ({
  // 初始状态
  gameState: initialGameState,
  players: {
    player1: createInitialMecha('player1', 'BLUE', 200),
    player2: createInitialMecha('player2', 'RED', 550),
  },
  effects: [],
  
  // 游戏流程控制
  startGame: () => set(produce(state => {
    state.gameState.phase = 'PLAYING';
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.winner = null;
  })),
  
  pauseGame: () => set(produce(state => {
    if (state.gameState.phase === 'PLAYING') {
      state.gameState.phase = 'PAUSED';
    }
  })),
  
  resumeimport { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
  winner: null,
};

// 游戏Store接口
interface GameStore {
  // 状态
  gameState: GameState;
  players: Record<string, MechaData>;
  effects: Effect[];
  
  // 游戏流程控制
  startGame: () => void;
  pauseGame: () => void;
  resumeGame: () => void;
  endGame: (winner: string | null) => void;
  resetGame: () => void;
  nextRound: () => void;
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => void;
  updatePlayerState: (playerId: string, state: MechaData['state']) => void;
  updatePlayerFacing: (playerId: string, facingRight: boolean) => void;
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => void;
  updatePlayerDefending: (playerId: string, isDefending: boolean) => void;
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => void;
  dealDamage: (targetId: string, damage: number, attackerId?: string) => void;
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => void;
  setAnimationFrame: (playerId: string, frame: number) => void;
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => string;
  updateEffects: (deltaTime: number) => void;
  removeEffect: (effectId: string) => void;
  clearEffects: () => void;
  
  // 游戏时间
  tick: (deltaTime: number) => void;
}

// 创建Game Store
export const useGameStore = create<GameStore>((set, get) => ({
  // 初始状态
  gameState: initialGameState,
  players: {
    player1: createInitialMecha('player1', 'BLUE', 200),
    player2: createInitialMecha('player2', 'RED', 550),
  },
  effects: [],
  
  // 游戏流程控制
  startGame: () => set(produce(state => {
    state.gameState.phase = 'PLAYING';
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.winner = null;
  })),
  
  pauseGame: () => set(produce(state => {
    if (state.gameState.phase === 'PLAYING') {
      state.gameState.phase = 'PAUSED';
    }
  })),
  
  resumeGame: () => set(produceimport { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
  winner: null,
};

// 游戏Store接口
interface GameStore {
  // 状态
  gameState: GameState;
  players: Record<string, MechaData>;
  effects: Effect[];
  
  // 游戏流程控制
  startGame: () => void;
  pauseGame: () => void;
  resumeGame: () => void;
  endGame: (winner: string | null) => void;
  resetGame: () => void;
  nextRound: () => void;
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => void;
  updatePlayerState: (playerId: string, state: MechaData['state']) => void;
  updatePlayerFacing: (playerId: string, facingRight: boolean) => void;
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => void;
  updatePlayerDefending: (playerId: string, isDefending: boolean) => void;
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => void;
  dealDamage: (targetId: string, damage: number, attackerId?: string) => void;
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => void;
  setAnimationFrame: (playerId: string, frame: number) => void;
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => string;
  updateEffects: (deltaTime: number) => void;
  removeEffect: (effectId: string) => void;
  clearEffects: () => void;
  
  // 游戏时间
  tick: (deltaTime: number) => void;
}

// 创建Game Store
export const useGameStore = create<GameStore>((set, get) => ({
  // 初始状态
  gameState: initialGameState,
  players: {
    player1: createInitialMecha('player1', 'BLUE', 200),
    player2: createInitialMecha('player2', 'RED', 550),
  },
  effects: [],
  
  // 游戏流程控制
  startGame: () => set(produce(state => {
    state.gameState.phase = 'PLAYING';
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.winner = null;
  })),
  
  pauseGame: () => set(produce(state => {
    if (state.gameState.phase === 'PLAYING') {
      state.gameState.phase = 'PAUSED';
    }
  })),
  
  resumeGame: () => set(produce(state => {
    if (import { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
  winner: null,
};

// 游戏Store接口
interface GameStore {
  // 状态
  gameState: GameState;
  players: Record<string, MechaData>;
  effects: Effect[];
  
  // 游戏流程控制
  startGame: () => void;
  pauseGame: () => void;
  resumeGame: () => void;
  endGame: (winner: string | null) => void;
  resetGame: () => void;
  nextRound: () => void;
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => void;
  updatePlayerState: (playerId: string, state: MechaData['state']) => void;
  updatePlayerFacing: (playerId: string, facingRight: boolean) => void;
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => void;
  updatePlayerDefending: (playerId: string, isDefending: boolean) => void;
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => void;
  dealDamage: (targetId: string, damage: number, attackerId?: string) => void;
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => void;
  setAnimationFrame: (playerId: string, frame: number) => void;
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => string;
  updateEffects: (deltaTime: number) => void;
  removeEffect: (effectId: string) => void;
  clearEffects: () => void;
  
  // 游戏时间
  tick: (deltaTime: number) => void;
}

// 创建Game Store
export const useGameStore = create<GameStore>((set, get) => ({
  // 初始状态
  gameState: initialGameState,
  players: {
    player1: createInitialMecha('player1', 'BLUE', 200),
    player2: createInitialMecha('player2', 'RED', 550),
  },
  effects: [],
  
  // 游戏流程控制
  startGame: () => set(produce(state => {
    state.gameState.phase = 'PLAYING';
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.winner = null;
  })),
  
  pauseGame: () => set(produce(state => {
    if (state.gameState.phase === 'PLAYING') {
      state.gameState.phase = 'PAUSED';
    }
  })),
  
  resumeGame: () => set(produce(state => {
    if (state.gameState.phase === 'PAimport { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
  winner: null,
};

// 游戏Store接口
interface GameStore {
  // 状态
  gameState: GameState;
  players: Record<string, MechaData>;
  effects: Effect[];
  
  // 游戏流程控制
  startGame: () => void;
  pauseGame: () => void;
  resumeGame: () => void;
  endGame: (winner: string | null) => void;
  resetGame: () => void;
  nextRound: () => void;
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => void;
  updatePlayerState: (playerId: string, state: MechaData['state']) => void;
  updatePlayerFacing: (playerId: string, facingRight: boolean) => void;
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => void;
  updatePlayerDefending: (playerId: string, isDefending: boolean) => void;
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => void;
  dealDamage: (targetId: string, damage: number, attackerId?: string) => void;
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => void;
  setAnimationFrame: (playerId: string, frame: number) => void;
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => string;
  updateEffects: (deltaTime: number) => void;
  removeEffect: (effectId: string) => void;
  clearEffects: () => void;
  
  // 游戏时间
  tick: (deltaTime: number) => void;
}

// 创建Game Store
export const useGameStore = create<GameStore>((set, get) => ({
  // 初始状态
  gameState: initialGameState,
  players: {
    player1: createInitialMecha('player1', 'BLUE', 200),
    player2: createInitialMecha('player2', 'RED', 550),
  },
  effects: [],
  
  // 游戏流程控制
  startGame: () => set(produce(state => {
    state.gameState.phase = 'PLAYING';
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.winner = null;
  })),
  
  pauseGame: () => set(produce(state => {
    if (state.gameState.phase === 'PLAYING') {
      state.gameState.phase = 'PAUSED';
    }
  })),
  
  resumeGame: () => set(produce(state => {
    if (state.gameState.phase === 'PAUSED') {
      state.gameimport { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
  winner: null,
};

// 游戏Store接口
interface GameStore {
  // 状态
  gameState: GameState;
  players: Record<string, MechaData>;
  effects: Effect[];
  
  // 游戏流程控制
  startGame: () => void;
  pauseGame: () => void;
  resumeGame: () => void;
  endGame: (winner: string | null) => void;
  resetGame: () => void;
  nextRound: () => void;
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => void;
  updatePlayerState: (playerId: string, state: MechaData['state']) => void;
  updatePlayerFacing: (playerId: string, facingRight: boolean) => void;
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => void;
  updatePlayerDefending: (playerId: string, isDefending: boolean) => void;
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => void;
  dealDamage: (targetId: string, damage: number, attackerId?: string) => void;
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => void;
  setAnimationFrame: (playerId: string, frame: number) => void;
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => string;
  updateEffects: (deltaTime: number) => void;
  removeEffect: (effectId: string) => void;
  clearEffects: () => void;
  
  // 游戏时间
  tick: (deltaTime: number) => void;
}

// 创建Game Store
export const useGameStore = create<GameStore>((set, get) => ({
  // 初始状态
  gameState: initialGameState,
  players: {
    player1: createInitialMecha('player1', 'BLUE', 200),
    player2: createInitialMecha('player2', 'RED', 550),
  },
  effects: [],
  
  // 游戏流程控制
  startGame: () => set(produce(state => {
    state.gameState.phase = 'PLAYING';
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.winner = null;
  })),
  
  pauseGame: () => set(produce(state => {
    if (state.gameState.phase === 'PLAYING') {
      state.gameState.phase = 'PAUSED';
    }
  })),
  
  resumeGame: () => set(produce(state => {
    if (state.gameState.phase === 'PAUSED') {
      state.gameState.phase = 'PLAYING'import { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
  winner: null,
};

// 游戏Store接口
interface GameStore {
  // 状态
  gameState: GameState;
  players: Record<string, MechaData>;
  effects: Effect[];
  
  // 游戏流程控制
  startGame: () => void;
  pauseGame: () => void;
  resumeGame: () => void;
  endGame: (winner: string | null) => void;
  resetGame: () => void;
  nextRound: () => void;
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => void;
  updatePlayerState: (playerId: string, state: MechaData['state']) => void;
  updatePlayerFacing: (playerId: string, facingRight: boolean) => void;
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => void;
  updatePlayerDefending: (playerId: string, isDefending: boolean) => void;
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => void;
  dealDamage: (targetId: string, damage: number, attackerId?: string) => void;
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => void;
  setAnimationFrame: (playerId: string, frame: number) => void;
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => string;
  updateEffects: (deltaTime: number) => void;
  removeEffect: (effectId: string) => void;
  clearEffects: () => void;
  
  // 游戏时间
  tick: (deltaTime: number) => void;
}

// 创建Game Store
export const useGameStore = create<GameStore>((set, get) => ({
  // 初始状态
  gameState: initialGameState,
  players: {
    player1: createInitialMecha('player1', 'BLUE', 200),
    player2: createInitialMecha('player2', 'RED', 550),
  },
  effects: [],
  
  // 游戏流程控制
  startGame: () => set(produce(state => {
    state.gameState.phase = 'PLAYING';
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.winner = null;
  })),
  
  pauseGame: () => set(produce(state => {
    if (state.gameState.phase === 'PLAYING') {
      state.gameState.phase = 'PAUSED';
    }
  })),
  
  resumeGame: () => set(produce(state => {
    if (state.gameState.phase === 'PAUSED') {
      state.gameState.phase = 'PLAYING';
    }
  }import { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
  winner: null,
};

// 游戏Store接口
interface GameStore {
  // 状态
  gameState: GameState;
  players: Record<string, MechaData>;
  effects: Effect[];
  
  // 游戏流程控制
  startGame: () => void;
  pauseGame: () => void;
  resumeGame: () => void;
  endGame: (winner: string | null) => void;
  resetGame: () => void;
  nextRound: () => void;
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => void;
  updatePlayerState: (playerId: string, state: MechaData['state']) => void;
  updatePlayerFacing: (playerId: string, facingRight: boolean) => void;
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => void;
  updatePlayerDefending: (playerId: string, isDefending: boolean) => void;
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => void;
  dealDamage: (targetId: string, damage: number, attackerId?: string) => void;
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => void;
  setAnimationFrame: (playerId: string, frame: number) => void;
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => string;
  updateEffects: (deltaTime: number) => void;
  removeEffect: (effectId: string) => void;
  clearEffects: () => void;
  
  // 游戏时间
  tick: (deltaTime: number) => void;
}

// 创建Game Store
export const useGameStore = create<GameStore>((set, get) => ({
  // 初始状态
  gameState: initialGameState,
  players: {
    player1: createInitialMecha('player1', 'BLUE', 200),
    player2: createInitialMecha('player2', 'RED', 550),
  },
  effects: [],
  
  // 游戏流程控制
  startGame: () => set(produce(state => {
    state.gameState.phase = 'PLAYING';
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.winner = null;
  })),
  
  pauseGame: () => set(produce(state => {
    if (state.gameState.phase === 'PLAYING') {
      state.gameState.phase = 'PAUSED';
    }
  })),
  
  resumeGame: () => set(produce(state => {
    if (state.gameState.phase === 'PAUSED') {
      state.gameState.phase = 'PLAYING';
    }
  })),
  
  endimport { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
  winner: null,
};

// 游戏Store接口
interface GameStore {
  // 状态
  gameState: GameState;
  players: Record<string, MechaData>;
  effects: Effect[];
  
  // 游戏流程控制
  startGame: () => void;
  pauseGame: () => void;
  resumeGame: () => void;
  endGame: (winner: string | null) => void;
  resetGame: () => void;
  nextRound: () => void;
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => void;
  updatePlayerState: (playerId: string, state: MechaData['state']) => void;
  updatePlayerFacing: (playerId: string, facingRight: boolean) => void;
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => void;
  updatePlayerDefending: (playerId: string, isDefending: boolean) => void;
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => void;
  dealDamage: (targetId: string, damage: number, attackerId?: string) => void;
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => void;
  setAnimationFrame: (playerId: string, frame: number) => void;
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => string;
  updateEffects: (deltaTime: number) => void;
  removeEffect: (effectId: string) => void;
  clearEffects: () => void;
  
  // 游戏时间
  tick: (deltaTime: number) => void;
}

// 创建Game Store
export const useGameStore = create<GameStore>((set, get) => ({
  // 初始状态
  gameState: initialGameState,
  players: {
    player1: createInitialMecha('player1', 'BLUE', 200),
    player2: createInitialMecha('player2', 'RED', 550),
  },
  effects: [],
  
  // 游戏流程控制
  startGame: () => set(produce(state => {
    state.gameState.phase = 'PLAYING';
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.winner = null;
  })),
  
  pauseGame: () => set(produce(state => {
    if (state.gameState.phase === 'PLAYING') {
      state.gameState.phase = 'PAUSED';
    }
  })),
  
  resumeGame: () => set(produce(state => {
    if (state.gameState.phase === 'PAUSED') {
      state.gameState.phase = 'PLAYING';
    }
  })),
  
  endGame: (winner: string |import { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
  winner: null,
};

// 游戏Store接口
interface GameStore {
  // 状态
  gameState: GameState;
  players: Record<string, MechaData>;
  effects: Effect[];
  
  // 游戏流程控制
  startGame: () => void;
  pauseGame: () => void;
  resumeGame: () => void;
  endGame: (winner: string | null) => void;
  resetGame: () => void;
  nextRound: () => void;
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => void;
  updatePlayerState: (playerId: string, state: MechaData['state']) => void;
  updatePlayerFacing: (playerId: string, facingRight: boolean) => void;
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => void;
  updatePlayerDefending: (playerId: string, isDefending: boolean) => void;
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => void;
  dealDamage: (targetId: string, damage: number, attackerId?: string) => void;
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => void;
  setAnimationFrame: (playerId: string, frame: number) => void;
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => string;
  updateEffects: (deltaTime: number) => void;
  removeEffect: (effectId: string) => void;
  clearEffects: () => void;
  
  // 游戏时间
  tick: (deltaTime: number) => void;
}

// 创建Game Store
export const useGameStore = create<GameStore>((set, get) => ({
  // 初始状态
  gameState: initialGameState,
  players: {
    player1: createInitialMecha('player1', 'BLUE', 200),
    player2: createInitialMecha('player2', 'RED', 550),
  },
  effects: [],
  
  // 游戏流程控制
  startGame: () => set(produce(state => {
    state.gameState.phase = 'PLAYING';
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.winner = null;
  })),
  
  pauseGame: () => set(produce(state => {
    if (state.gameState.phase === 'PLAYING') {
      state.gameState.phase = 'PAUSED';
    }
  })),
  
  resumeGame: () => set(produce(state => {
    if (state.gameState.phase === 'PAUSED') {
      state.gameState.phase = 'PLAYING';
    }
  })),
  
  endGame: (winner: string | null) => set(produce(stateimport { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
  winner: null,
};

// 游戏Store接口
interface GameStore {
  // 状态
  gameState: GameState;
  players: Record<string, MechaData>;
  effects: Effect[];
  
  // 游戏流程控制
  startGame: () => void;
  pauseGame: () => void;
  resumeGame: () => void;
  endGame: (winner: string | null) => void;
  resetGame: () => void;
  nextRound: () => void;
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => void;
  updatePlayerState: (playerId: string, state: MechaData['state']) => void;
  updatePlayerFacing: (playerId: string, facingRight: boolean) => void;
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => void;
  updatePlayerDefending: (playerId: string, isDefending: boolean) => void;
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => void;
  dealDamage: (targetId: string, damage: number, attackerId?: string) => void;
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => void;
  setAnimationFrame: (playerId: string, frame: number) => void;
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => string;
  updateEffects: (deltaTime: number) => void;
  removeEffect: (effectId: string) => void;
  clearEffects: () => void;
  
  // 游戏时间
  tick: (deltaTime: number) => void;
}

// 创建Game Store
export const useGameStore = create<GameStore>((set, get) => ({
  // 初始状态
  gameState: initialGameState,
  players: {
    player1: createInitialMecha('player1', 'BLUE', 200),
    player2: createInitialMecha('player2', 'RED', 550),
  },
  effects: [],
  
  // 游戏流程控制
  startGame: () => set(produce(state => {
    state.gameState.phase = 'PLAYING';
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.winner = null;
  })),
  
  pauseGame: () => set(produce(state => {
    if (state.gameState.phase === 'PLAYING') {
      state.gameState.phase = 'PAUSED';
    }
  })),
  
  resumeGame: () => set(produce(state => {
    if (state.gameState.phase === 'PAUSED') {
      state.gameState.phase = 'PLAYING';
    }
  })),
  
  endGame: (winner: string | null) => set(produce(state => {
    state.gameStateimport { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
  winner: null,
};

// 游戏Store接口
interface GameStore {
  // 状态
  gameState: GameState;
  players: Record<string, MechaData>;
  effects: Effect[];
  
  // 游戏流程控制
  startGame: () => void;
  pauseGame: () => void;
  resumeGame: () => void;
  endGame: (winner: string | null) => void;
  resetGame: () => void;
  nextRound: () => void;
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => void;
  updatePlayerState: (playerId: string, state: MechaData['state']) => void;
  updatePlayerFacing: (playerId: string, facingRight: boolean) => void;
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => void;
  updatePlayerDefending: (playerId: string, isDefending: boolean) => void;
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => void;
  dealDamage: (targetId: string, damage: number, attackerId?: string) => void;
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => void;
  setAnimationFrame: (playerId: string, frame: number) => void;
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => string;
  updateEffects: (deltaTime: number) => void;
  removeEffect: (effectId: string) => void;
  clearEffects: () => void;
  
  // 游戏时间
  tick: (deltaTime: number) => void;
}

// 创建Game Store
export const useGameStore = create<GameStore>((set, get) => ({
  // 初始状态
  gameState: initialGameState,
  players: {
    player1: createInitialMecha('player1', 'BLUE', 200),
    player2: createInitialMecha('player2', 'RED', 550),
  },
  effects: [],
  
  // 游戏流程控制
  startGame: () => set(produce(state => {
    state.gameState.phase = 'PLAYING';
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.winner = null;
  })),
  
  pauseGame: () => set(produce(state => {
    if (state.gameState.phase === 'PLAYING') {
      state.gameState.phase = 'PAUSED';
    }
  })),
  
  resumeGame: () => set(produce(state => {
    if (state.gameState.phase === 'PAUSED') {
      state.gameState.phase = 'PLAYING';
    }
  })),
  
  endGame: (winner: string | null) => set(produce(state => {
    state.gameState.phase = 'RESULT';
import { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
  winner: null,
};

// 游戏Store接口
interface GameStore {
  // 状态
  gameState: GameState;
  players: Record<string, MechaData>;
  effects: Effect[];
  
  // 游戏流程控制
  startGame: () => void;
  pauseGame: () => void;
  resumeGame: () => void;
  endGame: (winner: string | null) => void;
  resetGame: () => void;
  nextRound: () => void;
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => void;
  updatePlayerState: (playerId: string, state: MechaData['state']) => void;
  updatePlayerFacing: (playerId: string, facingRight: boolean) => void;
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => void;
  updatePlayerDefending: (playerId: string, isDefending: boolean) => void;
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => void;
  dealDamage: (targetId: string, damage: number, attackerId?: string) => void;
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => void;
  setAnimationFrame: (playerId: string, frame: number) => void;
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => string;
  updateEffects: (deltaTime: number) => void;
  removeEffect: (effectId: string) => void;
  clearEffects: () => void;
  
  // 游戏时间
  tick: (deltaTime: number) => void;
}

// 创建Game Store
export const useGameStore = create<GameStore>((set, get) => ({
  // 初始状态
  gameState: initialGameState,
  players: {
    player1: createInitialMecha('player1', 'BLUE', 200),
    player2: createInitialMecha('player2', 'RED', 550),
  },
  effects: [],
  
  // 游戏流程控制
  startGame: () => set(produce(state => {
    state.gameState.phase = 'PLAYING';
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.winner = null;
  })),
  
  pauseGame: () => set(produce(state => {
    if (state.gameState.phase === 'PLAYING') {
      state.gameState.phase = 'PAUSED';
    }
  })),
  
  resumeGame: () => set(produce(state => {
    if (state.gameState.phase === 'PAUSED') {
      state.gameState.phase = 'PLAYING';
    }
  })),
  
  endGame: (winner: string | null) => set(produce(state => {
    state.gameState.phase = 'RESULT';
    state.gameState.winner =import { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
  winner: null,
};

// 游戏Store接口
interface GameStore {
  // 状态
  gameState: GameState;
  players: Record<string, MechaData>;
  effects: Effect[];
  
  // 游戏流程控制
  startGame: () => void;
  pauseGame: () => void;
  resumeGame: () => void;
  endGame: (winner: string | null) => void;
  resetGame: () => void;
  nextRound: () => void;
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => void;
  updatePlayerState: (playerId: string, state: MechaData['state']) => void;
  updatePlayerFacing: (playerId: string, facingRight: boolean) => void;
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => void;
  updatePlayerDefending: (playerId: string, isDefending: boolean) => void;
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => void;
  dealDamage: (targetId: string, damage: number, attackerId?: string) => void;
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => void;
  setAnimationFrame: (playerId: string, frame: number) => void;
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => string;
  updateEffects: (deltaTime: number) => void;
  removeEffect: (effectId: string) => void;
  clearEffects: () => void;
  
  // 游戏时间
  tick: (deltaTime: number) => void;
}

// 创建Game Store
export const useGameStore = create<GameStore>((set, get) => ({
  // 初始状态
  gameState: initialGameState,
  players: {
    player1: createInitialMecha('player1', 'BLUE', 200),
    player2: createInitialMecha('player2', 'RED', 550),
  },
  effects: [],
  
  // 游戏流程控制
  startGame: () => set(produce(state => {
    state.gameState.phase = 'PLAYING';
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.winner = null;
  })),
  
  pauseGame: () => set(produce(state => {
    if (state.gameState.phase === 'PLAYING') {
      state.gameState.phase = 'PAUSED';
    }
  })),
  
  resumeGame: () => set(produce(state => {
    if (state.gameState.phase === 'PAUSED') {
      state.gameState.phase = 'PLAYING';
    }
  })),
  
  endGame: (winner: string | null) => set(produce(state => {
    state.gameState.phase = 'RESULT';
    state.gameState.winner = winner;
    if (winnerimport { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
  winner: null,
};

// 游戏Store接口
interface GameStore {
  // 状态
  gameState: GameState;
  players: Record<string, MechaData>;
  effects: Effect[];
  
  // 游戏流程控制
  startGame: () => void;
  pauseGame: () => void;
  resumeGame: () => void;
  endGame: (winner: string | null) => void;
  resetGame: () => void;
  nextRound: () => void;
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => void;
  updatePlayerState: (playerId: string, state: MechaData['state']) => void;
  updatePlayerFacing: (playerId: string, facingRight: boolean) => void;
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => void;
  updatePlayerDefending: (playerId: string, isDefending: boolean) => void;
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => void;
  dealDamage: (targetId: string, damage: number, attackerId?: string) => void;
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => void;
  setAnimationFrame: (playerId: string, frame: number) => void;
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => string;
  updateEffects: (deltaTime: number) => void;
  removeEffect: (effectId: string) => void;
  clearEffects: () => void;
  
  // 游戏时间
  tick: (deltaTime: number) => void;
}

// 创建Game Store
export const useGameStore = create<GameStore>((set, get) => ({
  // 初始状态
  gameState: initialGameState,
  players: {
    player1: createInitialMecha('player1', 'BLUE', 200),
    player2: createInitialMecha('player2', 'RED', 550),
  },
  effects: [],
  
  // 游戏流程控制
  startGame: () => set(produce(state => {
    state.gameState.phase = 'PLAYING';
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.winner = null;
  })),
  
  pauseGame: () => set(produce(state => {
    if (state.gameState.phase === 'PLAYING') {
      state.gameState.phase = 'PAUSED';
    }
  })),
  
  resumeGame: () => set(produce(state => {
    if (state.gameState.phase === 'PAUSED') {
      state.gameState.phase = 'PLAYING';
    }
  })),
  
  endGame: (winner: string | null) => set(produce(state => {
    state.gameState.phase = 'RESULT';
    state.gameState.winner = winner;
    if (winner && state.players[winner]) {
import { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
  winner: null,
};

// 游戏Store接口
interface GameStore {
  // 状态
  gameState: GameState;
  players: Record<string, MechaData>;
  effects: Effect[];
  
  // 游戏流程控制
  startGame: () => void;
  pauseGame: () => void;
  resumeGame: () => void;
  endGame: (winner: string | null) => void;
  resetGame: () => void;
  nextRound: () => void;
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => void;
  updatePlayerState: (playerId: string, state: MechaData['state']) => void;
  updatePlayerFacing: (playerId: string, facingRight: boolean) => void;
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => void;
  updatePlayerDefending: (playerId: string, isDefending: boolean) => void;
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => void;
  dealDamage: (targetId: string, damage: number, attackerId?: string) => void;
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => void;
  setAnimationFrame: (playerId: string, frame: number) => void;
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => string;
  updateEffects: (deltaTime: number) => void;
  removeEffect: (effectId: string) => void;
  clearEffects: () => void;
  
  // 游戏时间
  tick: (deltaTime: number) => void;
}

// 创建Game Store
export const useGameStore = create<GameStore>((set, get) => ({
  // 初始状态
  gameState: initialGameState,
  players: {
    player1: createInitialMecha('player1', 'BLUE', 200),
    player2: createInitialMecha('player2', 'RED', 550),
  },
  effects: [],
  
  // 游戏流程控制
  startGame: () => set(produce(state => {
    state.gameState.phase = 'PLAYING';
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.winner = null;
  })),
  
  pauseGame: () => set(produce(state => {
    if (state.gameState.phase === 'PLAYING') {
      state.gameState.phase = 'PAUSED';
    }
  })),
  
  resumeGame: () => set(produce(state => {
    if (state.gameState.phase === 'PAUSED') {
      state.gameState.phase = 'PLAYING';
    }
  })),
  
  endGame: (winner: string | null) => set(produce(state => {
    state.gameState.phase = 'RESULT';
    state.gameState.winner = winner;
    if (winner && state.players[winner]) {
      state.players[winner].scoreimport { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
  winner: null,
};

// 游戏Store接口
interface GameStore {
  // 状态
  gameState: GameState;
  players: Record<string, MechaData>;
  effects: Effect[];
  
  // 游戏流程控制
  startGame: () => void;
  pauseGame: () => void;
  resumeGame: () => void;
  endGame: (winner: string | null) => void;
  resetGame: () => void;
  nextRound: () => void;
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => void;
  updatePlayerState: (playerId: string, state: MechaData['state']) => void;
  updatePlayerFacing: (playerId: string, facingRight: boolean) => void;
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => void;
  updatePlayerDefending: (playerId: string, isDefending: boolean) => void;
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => void;
  dealDamage: (targetId: string, damage: number, attackerId?: string) => void;
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => void;
  setAnimationFrame: (playerId: string, frame: number) => void;
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => string;
  updateEffects: (deltaTime: number) => void;
  removeEffect: (effectId: string) => void;
  clearEffects: () => void;
  
  // 游戏时间
  tick: (deltaTime: number) => void;
}

// 创建Game Store
export const useGameStore = create<GameStore>((set, get) => ({
  // 初始状态
  gameState: initialGameState,
  players: {
    player1: createInitialMecha('player1', 'BLUE', 200),
    player2: createInitialMecha('player2', 'RED', 550),
  },
  effects: [],
  
  // 游戏流程控制
  startGame: () => set(produce(state => {
    state.gameState.phase = 'PLAYING';
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.winner = null;
  })),
  
  pauseGame: () => set(produce(state => {
    if (state.gameState.phase === 'PLAYING') {
      state.gameState.phase = 'PAUSED';
    }
  })),
  
  resumeGame: () => set(produce(state => {
    if (state.gameState.phase === 'PAUSED') {
      state.gameState.phase = 'PLAYING';
    }
  })),
  
  endGame: (winner: string | null) => set(produce(state => {
    state.gameState.phase = 'RESULT';
    state.gameState.winner = winner;
    if (winner && state.players[winner]) {
      state.players[winner].score += 1;
    }
import { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
  winner: null,
};

// 游戏Store接口
interface GameStore {
  // 状态
  gameState: GameState;
  players: Record<string, MechaData>;
  effects: Effect[];
  
  // 游戏流程控制
  startGame: () => void;
  pauseGame: () => void;
  resumeGame: () => void;
  endGame: (winner: string | null) => void;
  resetGame: () => void;
  nextRound: () => void;
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => void;
  updatePlayerState: (playerId: string, state: MechaData['state']) => void;
  updatePlayerFacing: (playerId: string, facingRight: boolean) => void;
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => void;
  updatePlayerDefending: (playerId: string, isDefending: boolean) => void;
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => void;
  dealDamage: (targetId: string, damage: number, attackerId?: string) => void;
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => void;
  setAnimationFrame: (playerId: string, frame: number) => void;
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => string;
  updateEffects: (deltaTime: number) => void;
  removeEffect: (effectId: string) => void;
  clearEffects: () => void;
  
  // 游戏时间
  tick: (deltaTime: number) => void;
}

// 创建Game Store
export const useGameStore = create<GameStore>((set, get) => ({
  // 初始状态
  gameState: initialGameState,
  players: {
    player1: createInitialMecha('player1', 'BLUE', 200),
    player2: createInitialMecha('player2', 'RED', 550),
  },
  effects: [],
  
  // 游戏流程控制
  startGame: () => set(produce(state => {
    state.gameState.phase = 'PLAYING';
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.winner = null;
  })),
  
  pauseGame: () => set(produce(state => {
    if (state.gameState.phase === 'PLAYING') {
      state.gameState.phase = 'PAUSED';
    }
  })),
  
  resumeGame: () => set(produce(state => {
    if (state.gameState.phase === 'PAUSED') {
      state.gameState.phase = 'PLAYING';
    }
  })),
  
  endGame: (winner: string | null) => set(produce(state => {
    state.gameState.phase = 'RESULT';
    state.gameState.winner = winner;
    if (winner && state.players[winner]) {
      state.players[winner].score += 1;
    }
  })),
  
import { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
  winner: null,
};

// 游戏Store接口
interface GameStore {
  // 状态
  gameState: GameState;
  players: Record<string, MechaData>;
  effects: Effect[];
  
  // 游戏流程控制
  startGame: () => void;
  pauseGame: () => void;
  resumeGame: () => void;
  endGame: (winner: string | null) => void;
  resetGame: () => void;
  nextRound: () => void;
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => void;
  updatePlayerState: (playerId: string, state: MechaData['state']) => void;
  updatePlayerFacing: (playerId: string, facingRight: boolean) => void;
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => void;
  updatePlayerDefending: (playerId: string, isDefending: boolean) => void;
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => void;
  dealDamage: (targetId: string, damage: number, attackerId?: string) => void;
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => void;
  setAnimationFrame: (playerId: string, frame: number) => void;
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => string;
  updateEffects: (deltaTime: number) => void;
  removeEffect: (effectId: string) => void;
  clearEffects: () => void;
  
  // 游戏时间
  tick: (deltaTime: number) => void;
}

// 创建Game Store
export const useGameStore = create<GameStore>((set, get) => ({
  // 初始状态
  gameState: initialGameState,
  players: {
    player1: createInitialMecha('player1', 'BLUE', 200),
    player2: createInitialMecha('player2', 'RED', 550),
  },
  effects: [],
  
  // 游戏流程控制
  startGame: () => set(produce(state => {
    state.gameState.phase = 'PLAYING';
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.winner = null;
  })),
  
  pauseGame: () => set(produce(state => {
    if (state.gameState.phase === 'PLAYING') {
      state.gameState.phase = 'PAUSED';
    }
  })),
  
  resumeGame: () => set(produce(state => {
    if (state.gameState.phase === 'PAUSED') {
      state.gameState.phase = 'PLAYING';
    }
  })),
  
  endGame: (winner: string | null) => set(produce(state => {
    state.gameState.phase = 'RESULT';
    state.gameState.winner = winner;
    if (winner && state.players[winner]) {
      state.players[winner].score += 1;
    }
  })),
  
  resetGame: () => setimport { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
  winner: null,
};

// 游戏Store接口
interface GameStore {
  // 状态
  gameState: GameState;
  players: Record<string, MechaData>;
  effects: Effect[];
  
  // 游戏流程控制
  startGame: () => void;
  pauseGame: () => void;
  resumeGame: () => void;
  endGame: (winner: string | null) => void;
  resetGame: () => void;
  nextRound: () => void;
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => void;
  updatePlayerState: (playerId: string, state: MechaData['state']) => void;
  updatePlayerFacing: (playerId: string, facingRight: boolean) => void;
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => void;
  updatePlayerDefending: (playerId: string, isDefending: boolean) => void;
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => void;
  dealDamage: (targetId: string, damage: number, attackerId?: string) => void;
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => void;
  setAnimationFrame: (playerId: string, frame: number) => void;
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => string;
  updateEffects: (deltaTime: number) => void;
  removeEffect: (effectId: string) => void;
  clearEffects: () => void;
  
  // 游戏时间
  tick: (deltaTime: number) => void;
}

// 创建Game Store
export const useGameStore = create<GameStore>((set, get) => ({
  // 初始状态
  gameState: initialGameState,
  players: {
    player1: createInitialMecha('player1', 'BLUE', 200),
    player2: createInitialMecha('player2', 'RED', 550),
  },
  effects: [],
  
  // 游戏流程控制
  startGame: () => set(produce(state => {
    state.gameState.phase = 'PLAYING';
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.winner = null;
  })),
  
  pauseGame: () => set(produce(state => {
    if (state.gameState.phase === 'PLAYING') {
      state.gameState.phase = 'PAUSED';
    }
  })),
  
  resumeGame: () => set(produce(state => {
    if (state.gameState.phase === 'PAUSED') {
      state.gameState.phase = 'PLAYING';
    }
  })),
  
  endGame: (winner: string | null) => set(produce(state => {
    state.gameState.phase = 'RESULT';
    state.gameState.winner = winner;
    if (winner && state.players[winner]) {
      state.players[winner].score += 1;
    }
  })),
  
  resetGame: () => set(produce(state => {
    stateimport { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
  winner: null,
};

// 游戏Store接口
interface GameStore {
  // 状态
  gameState: GameState;
  players: Record<string, MechaData>;
  effects: Effect[];
  
  // 游戏流程控制
  startGame: () => void;
  pauseGame: () => void;
  resumeGame: () => void;
  endGame: (winner: string | null) => void;
  resetGame: () => void;
  nextRound: () => void;
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => void;
  updatePlayerState: (playerId: string, state: MechaData['state']) => void;
  updatePlayerFacing: (playerId: string, facingRight: boolean) => void;
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => void;
  updatePlayerDefending: (playerId: string, isDefending: boolean) => void;
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => void;
  dealDamage: (targetId: string, damage: number, attackerId?: string) => void;
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => void;
  setAnimationFrame: (playerId: string, frame: number) => void;
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => string;
  updateEffects: (deltaTime: number) => void;
  removeEffect: (effectId: string) => void;
  clearEffects: () => void;
  
  // 游戏时间
  tick: (deltaTime: number) => void;
}

// 创建Game Store
export const useGameStore = create<GameStore>((set, get) => ({
  // 初始状态
  gameState: initialGameState,
  players: {
    player1: createInitialMecha('player1', 'BLUE', 200),
    player2: createInitialMecha('player2', 'RED', 550),
  },
  effects: [],
  
  // 游戏流程控制
  startGame: () => set(produce(state => {
    state.gameState.phase = 'PLAYING';
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.winner = null;
  })),
  
  pauseGame: () => set(produce(state => {
    if (state.gameState.phase === 'PLAYING') {
      state.gameState.phase = 'PAUSED';
    }
  })),
  
  resumeGame: () => set(produce(state => {
    if (state.gameState.phase === 'PAUSED') {
      state.gameState.phase = 'PLAYING';
    }
  })),
  
  endGame: (winner: string | null) => set(produce(state => {
    state.gameState.phase = 'RESULT';
    state.gameState.winner = winner;
    if (winner && state.players[winner]) {
      state.players[winner].score += 1;
    }
  })),
  
  resetGame: () => set(produce(state => {
    state.gameState = initialGameState;
import { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
  winner: null,
};

// 游戏Store接口
interface GameStore {
  // 状态
  gameState: GameState;
  players: Record<string, MechaData>;
  effects: Effect[];
  
  // 游戏流程控制
  startGame: () => void;
  pauseGame: () => void;
  resumeGame: () => void;
  endGame: (winner: string | null) => void;
  resetGame: () => void;
  nextRound: () => void;
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => void;
  updatePlayerState: (playerId: string, state: MechaData['state']) => void;
  updatePlayerFacing: (playerId: string, facingRight: boolean) => void;
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => void;
  updatePlayerDefending: (playerId: string, isDefending: boolean) => void;
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => void;
  dealDamage: (targetId: string, damage: number, attackerId?: string) => void;
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => void;
  setAnimationFrame: (playerId: string, frame: number) => void;
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => string;
  updateEffects: (deltaTime: number) => void;
  removeEffect: (effectId: string) => void;
  clearEffects: () => void;
  
  // 游戏时间
  tick: (deltaTime: number) => void;
}

// 创建Game Store
export const useGameStore = create<GameStore>((set, get) => ({
  // 初始状态
  gameState: initialGameState,
  players: {
    player1: createInitialMecha('player1', 'BLUE', 200),
    player2: createInitialMecha('player2', 'RED', 550),
  },
  effects: [],
  
  // 游戏流程控制
  startGame: () => set(produce(state => {
    state.gameState.phase = 'PLAYING';
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.winner = null;
  })),
  
  pauseGame: () => set(produce(state => {
    if (state.gameState.phase === 'PLAYING') {
      state.gameState.phase = 'PAUSED';
    }
  })),
  
  resumeGame: () => set(produce(state => {
    if (state.gameState.phase === 'PAUSED') {
      state.gameState.phase = 'PLAYING';
    }
  })),
  
  endGame: (winner: string | null) => set(produce(state => {
    state.gameState.phase = 'RESULT';
    state.gameState.winner = winner;
    if (winner && state.players[winner]) {
      state.players[winner].score += 1;
    }
  })),
  
  resetGame: () => set(produce(state => {
    state.gameState = initialGameState;
    state.players = {
      playerimport { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
  winner: null,
};

// 游戏Store接口
interface GameStore {
  // 状态
  gameState: GameState;
  players: Record<string, MechaData>;
  effects: Effect[];
  
  // 游戏流程控制
  startGame: () => void;
  pauseGame: () => void;
  resumeGame: () => void;
  endGame: (winner: string | null) => void;
  resetGame: () => void;
  nextRound: () => void;
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => void;
  updatePlayerState: (playerId: string, state: MechaData['state']) => void;
  updatePlayerFacing: (playerId: string, facingRight: boolean) => void;
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => void;
  updatePlayerDefending: (playerId: string, isDefending: boolean) => void;
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => void;
  dealDamage: (targetId: string, damage: number, attackerId?: string) => void;
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => void;
  setAnimationFrame: (playerId: string, frame: number) => void;
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => string;
  updateEffects: (deltaTime: number) => void;
  removeEffect: (effectId: string) => void;
  clearEffects: () => void;
  
  // 游戏时间
  tick: (deltaTime: number) => void;
}

// 创建Game Store
export const useGameStore = create<GameStore>((set, get) => ({
  // 初始状态
  gameState: initialGameState,
  players: {
    player1: createInitialMecha('player1', 'BLUE', 200),
    player2: createInitialMecha('player2', 'RED', 550),
  },
  effects: [],
  
  // 游戏流程控制
  startGame: () => set(produce(state => {
    state.gameState.phase = 'PLAYING';
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.winner = null;
  })),
  
  pauseGame: () => set(produce(state => {
    if (state.gameState.phase === 'PLAYING') {
      state.gameState.phase = 'PAUSED';
    }
  })),
  
  resumeGame: () => set(produce(state => {
    if (state.gameState.phase === 'PAUSED') {
      state.gameState.phase = 'PLAYING';
    }
  })),
  
  endGame: (winner: string | null) => set(produce(state => {
    state.gameState.phase = 'RESULT';
    state.gameState.winner = winner;
    if (winner && state.players[winner]) {
      state.players[winner].score += 1;
    }
  })),
  
  resetGame: () => set(produce(state => {
    state.gameState = initialGameState;
    state.players = {
      player1: createInitialMecha('import { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
  winner: null,
};

// 游戏Store接口
interface GameStore {
  // 状态
  gameState: GameState;
  players: Record<string, MechaData>;
  effects: Effect[];
  
  // 游戏流程控制
  startGame: () => void;
  pauseGame: () => void;
  resumeGame: () => void;
  endGame: (winner: string | null) => void;
  resetGame: () => void;
  nextRound: () => void;
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => void;
  updatePlayerState: (playerId: string, state: MechaData['state']) => void;
  updatePlayerFacing: (playerId: string, facingRight: boolean) => void;
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => void;
  updatePlayerDefending: (playerId: string, isDefending: boolean) => void;
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => void;
  dealDamage: (targetId: string, damage: number, attackerId?: string) => void;
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => void;
  setAnimationFrame: (playerId: string, frame: number) => void;
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => string;
  updateEffects: (deltaTime: number) => void;
  removeEffect: (effectId: string) => void;
  clearEffects: () => void;
  
  // 游戏时间
  tick: (deltaTime: number) => void;
}

// 创建Game Store
export const useGameStore = create<GameStore>((set, get) => ({
  // 初始状态
  gameState: initialGameState,
  players: {
    player1: createInitialMecha('player1', 'BLUE', 200),
    player2: createInitialMecha('player2', 'RED', 550),
  },
  effects: [],
  
  // 游戏流程控制
  startGame: () => set(produce(state => {
    state.gameState.phase = 'PLAYING';
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.winner = null;
  })),
  
  pauseGame: () => set(produce(state => {
    if (state.gameState.phase === 'PLAYING') {
      state.gameState.phase = 'PAUSED';
    }
  })),
  
  resumeGame: () => set(produce(state => {
    if (state.gameState.phase === 'PAUSED') {
      state.gameState.phase = 'PLAYING';
    }
  })),
  
  endGame: (winner: string | null) => set(produce(state => {
    state.gameState.phase = 'RESULT';
    state.gameState.winner = winner;
    if (winner && state.players[winner]) {
      state.players[winner].score += 1;
    }
  })),
  
  resetGame: () => set(produce(state => {
    state.gameState = initialGameState;
    state.players = {
      player1: createInitialMecha('player1', 'BLUE', 200import { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
  winner: null,
};

// 游戏Store接口
interface GameStore {
  // 状态
  gameState: GameState;
  players: Record<string, MechaData>;
  effects: Effect[];
  
  // 游戏流程控制
  startGame: () => void;
  pauseGame: () => void;
  resumeGame: () => void;
  endGame: (winner: string | null) => void;
  resetGame: () => void;
  nextRound: () => void;
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => void;
  updatePlayerState: (playerId: string, state: MechaData['state']) => void;
  updatePlayerFacing: (playerId: string, facingRight: boolean) => void;
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => void;
  updatePlayerDefending: (playerId: string, isDefending: boolean) => void;
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => void;
  dealDamage: (targetId: string, damage: number, attackerId?: string) => void;
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => void;
  setAnimationFrame: (playerId: string, frame: number) => void;
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => string;
  updateEffects: (deltaTime: number) => void;
  removeEffect: (effectId: string) => void;
  clearEffects: () => void;
  
  // 游戏时间
  tick: (deltaTime: number) => void;
}

// 创建Game Store
export const useGameStore = create<GameStore>((set, get) => ({
  // 初始状态
  gameState: initialGameState,
  players: {
    player1: createInitialMecha('player1', 'BLUE', 200),
    player2: createInitialMecha('player2', 'RED', 550),
  },
  effects: [],
  
  // 游戏流程控制
  startGame: () => set(produce(state => {
    state.gameState.phase = 'PLAYING';
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.winner = null;
  })),
  
  pauseGame: () => set(produce(state => {
    if (state.gameState.phase === 'PLAYING') {
      state.gameState.phase = 'PAUSED';
    }
  })),
  
  resumeGame: () => set(produce(state => {
    if (state.gameState.phase === 'PAUSED') {
      state.gameState.phase = 'PLAYING';
    }
  })),
  
  endGame: (winner: string | null) => set(produce(state => {
    state.gameState.phase = 'RESULT';
    state.gameState.winner = winner;
    if (winner && state.players[winner]) {
      state.players[winner].score += 1;
    }
  })),
  
  resetGame: () => set(produce(state => {
    state.gameState = initialGameState;
    state.players = {
      player1: createInitialMecha('player1', 'BLUE', 200),
      player2: createimport { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
  winner: null,
};

// 游戏Store接口
interface GameStore {
  // 状态
  gameState: GameState;
  players: Record<string, MechaData>;
  effects: Effect[];
  
  // 游戏流程控制
  startGame: () => void;
  pauseGame: () => void;
  resumeGame: () => void;
  endGame: (winner: string | null) => void;
  resetGame: () => void;
  nextRound: () => void;
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => void;
  updatePlayerState: (playerId: string, state: MechaData['state']) => void;
  updatePlayerFacing: (playerId: string, facingRight: boolean) => void;
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => void;
  updatePlayerDefending: (playerId: string, isDefending: boolean) => void;
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => void;
  dealDamage: (targetId: string, damage: number, attackerId?: string) => void;
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => void;
  setAnimationFrame: (playerId: string, frame: number) => void;
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => string;
  updateEffects: (deltaTime: number) => void;
  removeEffect: (effectId: string) => void;
  clearEffects: () => void;
  
  // 游戏时间
  tick: (deltaTime: number) => void;
}

// 创建Game Store
export const useGameStore = create<GameStore>((set, get) => ({
  // 初始状态
  gameState: initialGameState,
  players: {
    player1: createInitialMecha('player1', 'BLUE', 200),
    player2: createInitialMecha('player2', 'RED', 550),
  },
  effects: [],
  
  // 游戏流程控制
  startGame: () => set(produce(state => {
    state.gameState.phase = 'PLAYING';
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.winner = null;
  })),
  
  pauseGame: () => set(produce(state => {
    if (state.gameState.phase === 'PLAYING') {
      state.gameState.phase = 'PAUSED';
    }
  })),
  
  resumeGame: () => set(produce(state => {
    if (state.gameState.phase === 'PAUSED') {
      state.gameState.phase = 'PLAYING';
    }
  })),
  
  endGame: (winner: string | null) => set(produce(state => {
    state.gameState.phase = 'RESULT';
    state.gameState.winner = winner;
    if (winner && state.players[winner]) {
      state.players[winner].score += 1;
    }
  })),
  
  resetGame: () => set(produce(state => {
    state.gameState = initialGameState;
    state.players = {
      player1: createInitialMecha('player1', 'BLUE', 200),
      player2: createInitialMecha('player2',import { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
  winner: null,
};

// 游戏Store接口
interface GameStore {
  // 状态
  gameState: GameState;
  players: Record<string, MechaData>;
  effects: Effect[];
  
  // 游戏流程控制
  startGame: () => void;
  pauseGame: () => void;
  resumeGame: () => void;
  endGame: (winner: string | null) => void;
  resetGame: () => void;
  nextRound: () => void;
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => void;
  updatePlayerState: (playerId: string, state: MechaData['state']) => void;
  updatePlayerFacing: (playerId: string, facingRight: boolean) => void;
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => void;
  updatePlayerDefending: (playerId: string, isDefending: boolean) => void;
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => void;
  dealDamage: (targetId: string, damage: number, attackerId?: string) => void;
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => void;
  setAnimationFrame: (playerId: string, frame: number) => void;
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => string;
  updateEffects: (deltaTime: number) => void;
  removeEffect: (effectId: string) => void;
  clearEffects: () => void;
  
  // 游戏时间
  tick: (deltaTime: number) => void;
}

// 创建Game Store
export const useGameStore = create<GameStore>((set, get) => ({
  // 初始状态
  gameState: initialGameState,
  players: {
    player1: createInitialMecha('player1', 'BLUE', 200),
    player2: createInitialMecha('player2', 'RED', 550),
  },
  effects: [],
  
  // 游戏流程控制
  startGame: () => set(produce(state => {
    state.gameState.phase = 'PLAYING';
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.winner = null;
  })),
  
  pauseGame: () => set(produce(state => {
    if (state.gameState.phase === 'PLAYING') {
      state.gameState.phase = 'PAUSED';
    }
  })),
  
  resumeGame: () => set(produce(state => {
    if (state.gameState.phase === 'PAUSED') {
      state.gameState.phase = 'PLAYING';
    }
  })),
  
  endGame: (winner: string | null) => set(produce(state => {
    state.gameState.phase = 'RESULT';
    state.gameState.winner = winner;
    if (winner && state.players[winner]) {
      state.players[winner].score += 1;
    }
  })),
  
  resetGame: () => set(produce(state => {
    state.gameState = initialGameState;
    state.players = {
      player1: createInitialMecha('player1', 'BLUE', 200),
      player2: createInitialMecha('player2', 'RED', 550),
    }import { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
  winner: null,
};

// 游戏Store接口
interface GameStore {
  // 状态
  gameState: GameState;
  players: Record<string, MechaData>;
  effects: Effect[];
  
  // 游戏流程控制
  startGame: () => void;
  pauseGame: () => void;
  resumeGame: () => void;
  endGame: (winner: string | null) => void;
  resetGame: () => void;
  nextRound: () => void;
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => void;
  updatePlayerState: (playerId: string, state: MechaData['state']) => void;
  updatePlayerFacing: (playerId: string, facingRight: boolean) => void;
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => void;
  updatePlayerDefending: (playerId: string, isDefending: boolean) => void;
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => void;
  dealDamage: (targetId: string, damage: number, attackerId?: string) => void;
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => void;
  setAnimationFrame: (playerId: string, frame: number) => void;
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => string;
  updateEffects: (deltaTime: number) => void;
  removeEffect: (effectId: string) => void;
  clearEffects: () => void;
  
  // 游戏时间
  tick: (deltaTime: number) => void;
}

// 创建Game Store
export const useGameStore = create<GameStore>((set, get) => ({
  // 初始状态
  gameState: initialGameState,
  players: {
    player1: createInitialMecha('player1', 'BLUE', 200),
    player2: createInitialMecha('player2', 'RED', 550),
  },
  effects: [],
  
  // 游戏流程控制
  startGame: () => set(produce(state => {
    state.gameState.phase = 'PLAYING';
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.winner = null;
  })),
  
  pauseGame: () => set(produce(state => {
    if (state.gameState.phase === 'PLAYING') {
      state.gameState.phase = 'PAUSED';
    }
  })),
  
  resumeGame: () => set(produce(state => {
    if (state.gameState.phase === 'PAUSED') {
      state.gameState.phase = 'PLAYING';
    }
  })),
  
  endGame: (winner: string | null) => set(produce(state => {
    state.gameState.phase = 'RESULT';
    state.gameState.winner = winner;
    if (winner && state.players[winner]) {
      state.players[winner].score += 1;
    }
  })),
  
  resetGame: () => set(produce(state => {
    state.gameState = initialGameState;
    state.players = {
      player1: createInitialMecha('player1', 'BLUE', 200),
      player2: createInitialMecha('player2', 'RED', 550),
    };
    state.effects = []import { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
  winner: null,
};

// 游戏Store接口
interface GameStore {
  // 状态
  gameState: GameState;
  players: Record<string, MechaData>;
  effects: Effect[];
  
  // 游戏流程控制
  startGame: () => void;
  pauseGame: () => void;
  resumeGame: () => void;
  endGame: (winner: string | null) => void;
  resetGame: () => void;
  nextRound: () => void;
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => void;
  updatePlayerState: (playerId: string, state: MechaData['state']) => void;
  updatePlayerFacing: (playerId: string, facingRight: boolean) => void;
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => void;
  updatePlayerDefending: (playerId: string, isDefending: boolean) => void;
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => void;
  dealDamage: (targetId: string, damage: number, attackerId?: string) => void;
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => void;
  setAnimationFrame: (playerId: string, frame: number) => void;
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => string;
  updateEffects: (deltaTime: number) => void;
  removeEffect: (effectId: string) => void;
  clearEffects: () => void;
  
  // 游戏时间
  tick: (deltaTime: number) => void;
}

// 创建Game Store
export const useGameStore = create<GameStore>((set, get) => ({
  // 初始状态
  gameState: initialGameState,
  players: {
    player1: createInitialMecha('player1', 'BLUE', 200),
    player2: createInitialMecha('player2', 'RED', 550),
  },
  effects: [],
  
  // 游戏流程控制
  startGame: () => set(produce(state => {
    state.gameState.phase = 'PLAYING';
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.winner = null;
  })),
  
  pauseGame: () => set(produce(state => {
    if (state.gameState.phase === 'PLAYING') {
      state.gameState.phase = 'PAUSED';
    }
  })),
  
  resumeGame: () => set(produce(state => {
    if (state.gameState.phase === 'PAUSED') {
      state.gameState.phase = 'PLAYING';
    }
  })),
  
  endGame: (winner: string | null) => set(produce(state => {
    state.gameState.phase = 'RESULT';
    state.gameState.winner = winner;
    if (winner && state.players[winner]) {
      state.players[winner].score += 1;
    }
  })),
  
  resetGame: () => set(produce(state => {
    state.gameState = initialGameState;
    state.players = {
      player1: createInitialMecha('player1', 'BLUE', 200),
      player2: createInitialMecha('player2', 'RED', 550),
    };
    state.effects = [];
  })),
import { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
  winner: null,
};

// 游戏Store接口
interface GameStore {
  // 状态
  gameState: GameState;
  players: Record<string, MechaData>;
  effects: Effect[];
  
  // 游戏流程控制
  startGame: () => void;
  pauseGame: () => void;
  resumeGame: () => void;
  endGame: (winner: string | null) => void;
  resetGame: () => void;
  nextRound: () => void;
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => void;
  updatePlayerState: (playerId: string, state: MechaData['state']) => void;
  updatePlayerFacing: (playerId: string, facingRight: boolean) => void;
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => void;
  updatePlayerDefending: (playerId: string, isDefending: boolean) => void;
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => void;
  dealDamage: (targetId: string, damage: number, attackerId?: string) => void;
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => void;
  setAnimationFrame: (playerId: string, frame: number) => void;
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => string;
  updateEffects: (deltaTime: number) => void;
  removeEffect: (effectId: string) => void;
  clearEffects: () => void;
  
  // 游戏时间
  tick: (deltaTime: number) => void;
}

// 创建Game Store
export const useGameStore = create<GameStore>((set, get) => ({
  // 初始状态
  gameState: initialGameState,
  players: {
    player1: createInitialMecha('player1', 'BLUE', 200),
    player2: createInitialMecha('player2', 'RED', 550),
  },
  effects: [],
  
  // 游戏流程控制
  startGame: () => set(produce(state => {
    state.gameState.phase = 'PLAYING';
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.winner = null;
  })),
  
  pauseGame: () => set(produce(state => {
    if (state.gameState.phase === 'PLAYING') {
      state.gameState.phase = 'PAUSED';
    }
  })),
  
  resumeGame: () => set(produce(state => {
    if (state.gameState.phase === 'PAUSED') {
      state.gameState.phase = 'PLAYING';
    }
  })),
  
  endGame: (winner: string | null) => set(produce(state => {
    state.gameState.phase = 'RESULT';
    state.gameState.winner = winner;
    if (winner && state.players[winner]) {
      state.players[winner].score += 1;
    }
  })),
  
  resetGame: () => set(produce(state => {
    state.gameState = initialGameState;
    state.players = {
      player1: createInitialMecha('player1', 'BLUE', 200),
      player2: createInitialMecha('player2', 'RED', 550),
    };
    state.effects = [];
  })),
  
  nextRound:import { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
  winner: null,
};

// 游戏Store接口
interface GameStore {
  // 状态
  gameState: GameState;
  players: Record<string, MechaData>;
  effects: Effect[];
  
  // 游戏流程控制
  startGame: () => void;
  pauseGame: () => void;
  resumeGame: () => void;
  endGame: (winner: string | null) => void;
  resetGame: () => void;
  nextRound: () => void;
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => void;
  updatePlayerState: (playerId: string, state: MechaData['state']) => void;
  updatePlayerFacing: (playerId: string, facingRight: boolean) => void;
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => void;
  updatePlayerDefending: (playerId: string, isDefending: boolean) => void;
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => void;
  dealDamage: (targetId: string, damage: number, attackerId?: string) => void;
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => void;
  setAnimationFrame: (playerId: string, frame: number) => void;
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => string;
  updateEffects: (deltaTime: number) => void;
  removeEffect: (effectId: string) => void;
  clearEffects: () => void;
  
  // 游戏时间
  tick: (deltaTime: number) => void;
}

// 创建Game Store
export const useGameStore = create<GameStore>((set, get) => ({
  // 初始状态
  gameState: initialGameState,
  players: {
    player1: createInitialMecha('player1', 'BLUE', 200),
    player2: createInitialMecha('player2', 'RED', 550),
  },
  effects: [],
  
  // 游戏流程控制
  startGame: () => set(produce(state => {
    state.gameState.phase = 'PLAYING';
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.winner = null;
  })),
  
  pauseGame: () => set(produce(state => {
    if (state.gameState.phase === 'PLAYING') {
      state.gameState.phase = 'PAUSED';
    }
  })),
  
  resumeGame: () => set(produce(state => {
    if (state.gameState.phase === 'PAUSED') {
      state.gameState.phase = 'PLAYING';
    }
  })),
  
  endGame: (winner: string | null) => set(produce(state => {
    state.gameState.phase = 'RESULT';
    state.gameState.winner = winner;
    if (winner && state.players[winner]) {
      state.players[winner].score += 1;
    }
  })),
  
  resetGame: () => set(produce(state => {
    state.gameState = initialGameState;
    state.players = {
      player1: createInitialMecha('player1', 'BLUE', 200),
      player2: createInitialMecha('player2', 'RED', 550),
    };
    state.effects = [];
  })),
  
  nextRound: () => set(produce(state =>import { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
  winner: null,
};

// 游戏Store接口
interface GameStore {
  // 状态
  gameState: GameState;
  players: Record<string, MechaData>;
  effects: Effect[];
  
  // 游戏流程控制
  startGame: () => void;
  pauseGame: () => void;
  resumeGame: () => void;
  endGame: (winner: string | null) => void;
  resetGame: () => void;
  nextRound: () => void;
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => void;
  updatePlayerState: (playerId: string, state: MechaData['state']) => void;
  updatePlayerFacing: (playerId: string, facingRight: boolean) => void;
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => void;
  updatePlayerDefending: (playerId: string, isDefending: boolean) => void;
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => void;
  dealDamage: (targetId: string, damage: number, attackerId?: string) => void;
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => void;
  setAnimationFrame: (playerId: string, frame: number) => void;
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => string;
  updateEffects: (deltaTime: number) => void;
  removeEffect: (effectId: string) => void;
  clearEffects: () => void;
  
  // 游戏时间
  tick: (deltaTime: number) => void;
}

// 创建Game Store
export const useGameStore = create<GameStore>((set, get) => ({
  // 初始状态
  gameState: initialGameState,
  players: {
    player1: createInitialMecha('player1', 'BLUE', 200),
    player2: createInitialMecha('player2', 'RED', 550),
  },
  effects: [],
  
  // 游戏流程控制
  startGame: () => set(produce(state => {
    state.gameState.phase = 'PLAYING';
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.winner = null;
  })),
  
  pauseGame: () => set(produce(state => {
    if (state.gameState.phase === 'PLAYING') {
      state.gameState.phase = 'PAUSED';
    }
  })),
  
  resumeGame: () => set(produce(state => {
    if (state.gameState.phase === 'PAUSED') {
      state.gameState.phase = 'PLAYING';
    }
  })),
  
  endGame: (winner: string | null) => set(produce(state => {
    state.gameState.phase = 'RESULT';
    state.gameState.winner = winner;
    if (winner && state.players[winner]) {
      state.players[winner].score += 1;
    }
  })),
  
  resetGame: () => set(produce(state => {
    state.gameState = initialGameState;
    state.players = {
      player1: createInitialMecha('player1', 'BLUE', 200),
      player2: createInitialMecha('player2', 'RED', 550),
    };
    state.effects = [];
  })),
  
  nextRound: () => set(produce(state => {
    state.gameState.roundimport { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
  winner: null,
};

// 游戏Store接口
interface GameStore {
  // 状态
  gameState: GameState;
  players: Record<string, MechaData>;
  effects: Effect[];
  
  // 游戏流程控制
  startGame: () => void;
  pauseGame: () => void;
  resumeGame: () => void;
  endGame: (winner: string | null) => void;
  resetGame: () => void;
  nextRound: () => void;
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => void;
  updatePlayerState: (playerId: string, state: MechaData['state']) => void;
  updatePlayerFacing: (playerId: string, facingRight: boolean) => void;
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => void;
  updatePlayerDefending: (playerId: string, isDefending: boolean) => void;
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => void;
  dealDamage: (targetId: string, damage: number, attackerId?: string) => void;
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => void;
  setAnimationFrame: (playerId: string, frame: number) => void;
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => string;
  updateEffects: (deltaTime: number) => void;
  removeEffect: (effectId: string) => void;
  clearEffects: () => void;
  
  // 游戏时间
  tick: (deltaTime: number) => void;
}

// 创建Game Store
export const useGameStore = create<GameStore>((set, get) => ({
  // 初始状态
  gameState: initialGameState,
  players: {
    player1: createInitialMecha('player1', 'BLUE', 200),
    player2: createInitialMecha('player2', 'RED', 550),
  },
  effects: [],
  
  // 游戏流程控制
  startGame: () => set(produce(state => {
    state.gameState.phase = 'PLAYING';
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.winner = null;
  })),
  
  pauseGame: () => set(produce(state => {
    if (state.gameState.phase === 'PLAYING') {
      state.gameState.phase = 'PAUSED';
    }
  })),
  
  resumeGame: () => set(produce(state => {
    if (state.gameState.phase === 'PAUSED') {
      state.gameState.phase = 'PLAYING';
    }
  })),
  
  endGame: (winner: string | null) => set(produce(state => {
    state.gameState.phase = 'RESULT';
    state.gameState.winner = winner;
    if (winner && state.players[winner]) {
      state.players[winner].score += 1;
    }
  })),
  
  resetGame: () => set(produce(state => {
    state.gameState = initialGameState;
    state.players = {
      player1: createInitialMecha('player1', 'BLUE', 200),
      player2: createInitialMecha('player2', 'RED', 550),
    };
    state.effects = [];
  })),
  
  nextRound: () => set(produce(state => {
    state.gameState.round += 1;
    stateimport { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
  winner: null,
};

// 游戏Store接口
interface GameStore {
  // 状态
  gameState: GameState;
  players: Record<string, MechaData>;
  effects: Effect[];
  
  // 游戏流程控制
  startGame: () => void;
  pauseGame: () => void;
  resumeGame: () => void;
  endGame: (winner: string | null) => void;
  resetGame: () => void;
  nextRound: () => void;
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => void;
  updatePlayerState: (playerId: string, state: MechaData['state']) => void;
  updatePlayerFacing: (playerId: string, facingRight: boolean) => void;
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => void;
  updatePlayerDefending: (playerId: string, isDefending: boolean) => void;
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => void;
  dealDamage: (targetId: string, damage: number, attackerId?: string) => void;
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => void;
  setAnimationFrame: (playerId: string, frame: number) => void;
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => string;
  updateEffects: (deltaTime: number) => void;
  removeEffect: (effectId: string) => void;
  clearEffects: () => void;
  
  // 游戏时间
  tick: (deltaTime: number) => void;
}

// 创建Game Store
export const useGameStore = create<GameStore>((set, get) => ({
  // 初始状态
  gameState: initialGameState,
  players: {
    player1: createInitialMecha('player1', 'BLUE', 200),
    player2: createInitialMecha('player2', 'RED', 550),
  },
  effects: [],
  
  // 游戏流程控制
  startGame: () => set(produce(state => {
    state.gameState.phase = 'PLAYING';
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.winner = null;
  })),
  
  pauseGame: () => set(produce(state => {
    if (state.gameState.phase === 'PLAYING') {
      state.gameState.phase = 'PAUSED';
    }
  })),
  
  resumeGame: () => set(produce(state => {
    if (state.gameState.phase === 'PAUSED') {
      state.gameState.phase = 'PLAYING';
    }
  })),
  
  endGame: (winner: string | null) => set(produce(state => {
    state.gameState.phase = 'RESULT';
    state.gameState.winner = winner;
    if (winner && state.players[winner]) {
      state.players[winner].score += 1;
    }
  })),
  
  resetGame: () => set(produce(state => {
    state.gameState = initialGameState;
    state.players = {
      player1: createInitialMecha('player1', 'BLUE', 200),
      player2: createInitialMecha('player2', 'RED', 550),
    };
    state.effects = [];
  })),
  
  nextRound: () => set(produce(state => {
    state.gameState.round += 1;
    state.gameState.timeRemaining = GAME_CONSTANTimport { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
  winner: null,
};

// 游戏Store接口
interface GameStore {
  // 状态
  gameState: GameState;
  players: Record<string, MechaData>;
  effects: Effect[];
  
  // 游戏流程控制
  startGame: () => void;
  pauseGame: () => void;
  resumeGame: () => void;
  endGame: (winner: string | null) => void;
  resetGame: () => void;
  nextRound: () => void;
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => void;
  updatePlayerState: (playerId: string, state: MechaData['state']) => void;
  updatePlayerFacing: (playerId: string, facingRight: boolean) => void;
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => void;
  updatePlayerDefending: (playerId: string, isDefending: boolean) => void;
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => void;
  dealDamage: (targetId: string, damage: number, attackerId?: string) => void;
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => void;
  setAnimationFrame: (playerId: string, frame: number) => void;
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => string;
  updateEffects: (deltaTime: number) => void;
  removeEffect: (effectId: string) => void;
  clearEffects: () => void;
  
  // 游戏时间
  tick: (deltaTime: number) => void;
}

// 创建Game Store
export const useGameStore = create<GameStore>((set, get) => ({
  // 初始状态
  gameState: initialGameState,
  players: {
    player1: createInitialMecha('player1', 'BLUE', 200),
    player2: createInitialMecha('player2', 'RED', 550),
  },
  effects: [],
  
  // 游戏流程控制
  startGame: () => set(produce(state => {
    state.gameState.phase = 'PLAYING';
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.winner = null;
  })),
  
  pauseGame: () => set(produce(state => {
    if (state.gameState.phase === 'PLAYING') {
      state.gameState.phase = 'PAUSED';
    }
  })),
  
  resumeGame: () => set(produce(state => {
    if (state.gameState.phase === 'PAUSED') {
      state.gameState.phase = 'PLAYING';
    }
  })),
  
  endGame: (winner: string | null) => set(produce(state => {
    state.gameState.phase = 'RESULT';
    state.gameState.winner = winner;
    if (winner && state.players[winner]) {
      state.players[winner].score += 1;
    }
  })),
  
  resetGame: () => set(produce(state => {
    state.gameState = initialGameState;
    state.players = {
      player1: createInitialMecha('player1', 'BLUE', 200),
      player2: createInitialMecha('player2', 'RED', 550),
    };
    state.effects = [];
  })),
  
  nextRound: () => set(produce(state => {
    state.gameState.round += 1;
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    stateimport { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
  winner: null,
};

// 游戏Store接口
interface GameStore {
  // 状态
  gameState: GameState;
  players: Record<string, MechaData>;
  effects: Effect[];
  
  // 游戏流程控制
  startGame: () => void;
  pauseGame: () => void;
  resumeGame: () => void;
  endGame: (winner: string | null) => void;
  resetGame: () => void;
  nextRound: () => void;
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => void;
  updatePlayerState: (playerId: string, state: MechaData['state']) => void;
  updatePlayerFacing: (playerId: string, facingRight: boolean) => void;
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => void;
  updatePlayerDefending: (playerId: string, isDefending: boolean) => void;
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => void;
  dealDamage: (targetId: string, damage: number, attackerId?: string) => void;
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => void;
  setAnimationFrame: (playerId: string, frame: number) => void;
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => string;
  updateEffects: (deltaTime: number) => void;
  removeEffect: (effectId: string) => void;
  clearEffects: () => void;
  
  // 游戏时间
  tick: (deltaTime: number) => void;
}

// 创建Game Store
export const useGameStore = create<GameStore>((set, get) => ({
  // 初始状态
  gameState: initialGameState,
  players: {
    player1: createInitialMecha('player1', 'BLUE', 200),
    player2: createInitialMecha('player2', 'RED', 550),
  },
  effects: [],
  
  // 游戏流程控制
  startGame: () => set(produce(state => {
    state.gameState.phase = 'PLAYING';
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.winner = null;
  })),
  
  pauseGame: () => set(produce(state => {
    if (state.gameState.phase === 'PLAYING') {
      state.gameState.phase = 'PAUSED';
    }
  })),
  
  resumeGame: () => set(produce(state => {
    if (state.gameState.phase === 'PAUSED') {
      state.gameState.phase = 'PLAYING';
    }
  })),
  
  endGame: (winner: string | null) => set(produce(state => {
    state.gameState.phase = 'RESULT';
    state.gameState.winner = winner;
    if (winner && state.players[winner]) {
      state.players[winner].score += 1;
    }
  })),
  
  resetGame: () => set(produce(state => {
    state.gameState = initialGameState;
    state.players = {
      player1: createInitialMecha('player1', 'BLUE', 200),
      player2: createInitialMecha('player2', 'RED', 550),
    };
    state.effects = [];
  })),
  
  nextRound: () => set(produce(state => {
    state.gameState.round += 1;
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.phase = 'PLAYINGimport { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
  winner: null,
};

// 游戏Store接口
interface GameStore {
  // 状态
  gameState: GameState;
  players: Record<string, MechaData>;
  effects: Effect[];
  
  // 游戏流程控制
  startGame: () => void;
  pauseGame: () => void;
  resumeGame: () => void;
  endGame: (winner: string | null) => void;
  resetGame: () => void;
  nextRound: () => void;
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => void;
  updatePlayerState: (playerId: string, state: MechaData['state']) => void;
  updatePlayerFacing: (playerId: string, facingRight: boolean) => void;
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => void;
  updatePlayerDefending: (playerId: string, isDefending: boolean) => void;
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => void;
  dealDamage: (targetId: string, damage: number, attackerId?: string) => void;
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => void;
  setAnimationFrame: (playerId: string, frame: number) => void;
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => string;
  updateEffects: (deltaTime: number) => void;
  removeEffect: (effectId: string) => void;
  clearEffects: () => void;
  
  // 游戏时间
  tick: (deltaTime: number) => void;
}

// 创建Game Store
export const useGameStore = create<GameStore>((set, get) => ({
  // 初始状态
  gameState: initialGameState,
  players: {
    player1: createInitialMecha('player1', 'BLUE', 200),
    player2: createInitialMecha('player2', 'RED', 550),
  },
  effects: [],
  
  // 游戏流程控制
  startGame: () => set(produce(state => {
    state.gameState.phase = 'PLAYING';
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.winner = null;
  })),
  
  pauseGame: () => set(produce(state => {
    if (state.gameState.phase === 'PLAYING') {
      state.gameState.phase = 'PAUSED';
    }
  })),
  
  resumeGame: () => set(produce(state => {
    if (state.gameState.phase === 'PAUSED') {
      state.gameState.phase = 'PLAYING';
    }
  })),
  
  endGame: (winner: string | null) => set(produce(state => {
    state.gameState.phase = 'RESULT';
    state.gameState.winner = winner;
    if (winner && state.players[winner]) {
      state.players[winner].score += 1;
    }
  })),
  
  resetGame: () => set(produce(state => {
    state.gameState = initialGameState;
    state.players = {
      player1: createInitialMecha('player1', 'BLUE', 200),
      player2: createInitialMecha('player2', 'RED', 550),
    };
    state.effects = [];
  })),
  
  nextRound: () => set(produce(state => {
    state.gameState.round += 1;
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.phase = 'PLAYING';
    state.gameStateimport { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
  winner: null,
};

// 游戏Store接口
interface GameStore {
  // 状态
  gameState: GameState;
  players: Record<string, MechaData>;
  effects: Effect[];
  
  // 游戏流程控制
  startGame: () => void;
  pauseGame: () => void;
  resumeGame: () => void;
  endGame: (winner: string | null) => void;
  resetGame: () => void;
  nextRound: () => void;
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => void;
  updatePlayerState: (playerId: string, state: MechaData['state']) => void;
  updatePlayerFacing: (playerId: string, facingRight: boolean) => void;
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => void;
  updatePlayerDefending: (playerId: string, isDefending: boolean) => void;
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => void;
  dealDamage: (targetId: string, damage: number, attackerId?: string) => void;
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => void;
  setAnimationFrame: (playerId: string, frame: number) => void;
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => string;
  updateEffects: (deltaTime: number) => void;
  removeEffect: (effectId: string) => void;
  clearEffects: () => void;
  
  // 游戏时间
  tick: (deltaTime: number) => void;
}

// 创建Game Store
export const useGameStore = create<GameStore>((set, get) => ({
  // 初始状态
  gameState: initialGameState,
  players: {
    player1: createInitialMecha('player1', 'BLUE', 200),
    player2: createInitialMecha('player2', 'RED', 550),
  },
  effects: [],
  
  // 游戏流程控制
  startGame: () => set(produce(state => {
    state.gameState.phase = 'PLAYING';
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.winner = null;
  })),
  
  pauseGame: () => set(produce(state => {
    if (state.gameState.phase === 'PLAYING') {
      state.gameState.phase = 'PAUSED';
    }
  })),
  
  resumeGame: () => set(produce(state => {
    if (state.gameState.phase === 'PAUSED') {
      state.gameState.phase = 'PLAYING';
    }
  })),
  
  endGame: (winner: string | null) => set(produce(state => {
    state.gameState.phase = 'RESULT';
    state.gameState.winner = winner;
    if (winner && state.players[winner]) {
      state.players[winner].score += 1;
    }
  })),
  
  resetGame: () => set(produce(state => {
    state.gameState = initialGameState;
    state.players = {
      player1: createInitialMecha('player1', 'BLUE', 200),
      player2: createInitialMecha('player2', 'RED', 550),
    };
    state.effects = [];
  })),
  
  nextRound: () => set(produce(state => {
    state.gameState.round += 1;
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.phase = 'PLAYING';
    state.gameState.winner = null;
    
import { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
  winner: null,
};

// 游戏Store接口
interface GameStore {
  // 状态
  gameState: GameState;
  players: Record<string, MechaData>;
  effects: Effect[];
  
  // 游戏流程控制
  startGame: () => void;
  pauseGame: () => void;
  resumeGame: () => void;
  endGame: (winner: string | null) => void;
  resetGame: () => void;
  nextRound: () => void;
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => void;
  updatePlayerState: (playerId: string, state: MechaData['state']) => void;
  updatePlayerFacing: (playerId: string, facingRight: boolean) => void;
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => void;
  updatePlayerDefending: (playerId: string, isDefending: boolean) => void;
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => void;
  dealDamage: (targetId: string, damage: number, attackerId?: string) => void;
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => void;
  setAnimationFrame: (playerId: string, frame: number) => void;
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => string;
  updateEffects: (deltaTime: number) => void;
  removeEffect: (effectId: string) => void;
  clearEffects: () => void;
  
  // 游戏时间
  tick: (deltaTime: number) => void;
}

// 创建Game Store
export const useGameStore = create<GameStore>((set, get) => ({
  // 初始状态
  gameState: initialGameState,
  players: {
    player1: createInitialMecha('player1', 'BLUE', 200),
    player2: createInitialMecha('player2', 'RED', 550),
  },
  effects: [],
  
  // 游戏流程控制
  startGame: () => set(produce(state => {
    state.gameState.phase = 'PLAYING';
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.winner = null;
  })),
  
  pauseGame: () => set(produce(state => {
    if (state.gameState.phase === 'PLAYING') {
      state.gameState.phase = 'PAUSED';
    }
  })),
  
  resumeGame: () => set(produce(state => {
    if (state.gameState.phase === 'PAUSED') {
      state.gameState.phase = 'PLAYING';
    }
  })),
  
  endGame: (winner: string | null) => set(produce(state => {
    state.gameState.phase = 'RESULT';
    state.gameState.winner = winner;
    if (winner && state.players[winner]) {
      state.players[winner].score += 1;
    }
  })),
  
  resetGame: () => set(produce(state => {
    state.gameState = initialGameState;
    state.players = {
      player1: createInitialMecha('player1', 'BLUE', 200),
      player2: createInitialMecha('player2', 'RED', 550),
    };
    state.effects = [];
  })),
  
  nextRound: () => set(produce(state => {
    state.gameState.round += 1;
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.phase = 'PLAYING';
    state.gameState.winner = null;
    
    // 重置玩家位置和状态import { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
  winner: null,
};

// 游戏Store接口
interface GameStore {
  // 状态
  gameState: GameState;
  players: Record<string, MechaData>;
  effects: Effect[];
  
  // 游戏流程控制
  startGame: () => void;
  pauseGame: () => void;
  resumeGame: () => void;
  endGame: (winner: string | null) => void;
  resetGame: () => void;
  nextRound: () => void;
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => void;
  updatePlayerState: (playerId: string, state: MechaData['state']) => void;
  updatePlayerFacing: (playerId: string, facingRight: boolean) => void;
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => void;
  updatePlayerDefending: (playerId: string, isDefending: boolean) => void;
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => void;
  dealDamage: (targetId: string, damage: number, attackerId?: string) => void;
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => void;
  setAnimationFrame: (playerId: string, frame: number) => void;
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => string;
  updateEffects: (deltaTime: number) => void;
  removeEffect: (effectId: string) => void;
  clearEffects: () => void;
  
  // 游戏时间
  tick: (deltaTime: number) => void;
}

// 创建Game Store
export const useGameStore = create<GameStore>((set, get) => ({
  // 初始状态
  gameState: initialGameState,
  players: {
    player1: createInitialMecha('player1', 'BLUE', 200),
    player2: createInitialMecha('player2', 'RED', 550),
  },
  effects: [],
  
  // 游戏流程控制
  startGame: () => set(produce(state => {
    state.gameState.phase = 'PLAYING';
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.winner = null;
  })),
  
  pauseGame: () => set(produce(state => {
    if (state.gameState.phase === 'PLAYING') {
      state.gameState.phase = 'PAUSED';
    }
  })),
  
  resumeGame: () => set(produce(state => {
    if (state.gameState.phase === 'PAUSED') {
      state.gameState.phase = 'PLAYING';
    }
  })),
  
  endGame: (winner: string | null) => set(produce(state => {
    state.gameState.phase = 'RESULT';
    state.gameState.winner = winner;
    if (winner && state.players[winner]) {
      state.players[winner].score += 1;
    }
  })),
  
  resetGame: () => set(produce(state => {
    state.gameState = initialGameState;
    state.players = {
      player1: createInitialMecha('player1', 'BLUE', 200),
      player2: createInitialMecha('player2', 'RED', 550),
    };
    state.effects = [];
  })),
  
  nextRound: () => set(produce(state => {
    state.gameState.round += 1;
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.phase = 'PLAYING';
    state.gameState.winner = null;
    
    // 重置玩家位置和状态
    Object.keys(state.playersimport { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
  winner: null,
};

// 游戏Store接口
interface GameStore {
  // 状态
  gameState: GameState;
  players: Record<string, MechaData>;
  effects: Effect[];
  
  // 游戏流程控制
  startGame: () => void;
  pauseGame: () => void;
  resumeGame: () => void;
  endGame: (winner: string | null) => void;
  resetGame: () => void;
  nextRound: () => void;
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => void;
  updatePlayerState: (playerId: string, state: MechaData['state']) => void;
  updatePlayerFacing: (playerId: string, facingRight: boolean) => void;
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => void;
  updatePlayerDefending: (playerId: string, isDefending: boolean) => void;
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => void;
  dealDamage: (targetId: string, damage: number, attackerId?: string) => void;
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => void;
  setAnimationFrame: (playerId: string, frame: number) => void;
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => string;
  updateEffects: (deltaTime: number) => void;
  removeEffect: (effectId: string) => void;
  clearEffects: () => void;
  
  // 游戏时间
  tick: (deltaTime: number) => void;
}

// 创建Game Store
export const useGameStore = create<GameStore>((set, get) => ({
  // 初始状态
  gameState: initialGameState,
  players: {
    player1: createInitialMecha('player1', 'BLUE', 200),
    player2: createInitialMecha('player2', 'RED', 550),
  },
  effects: [],
  
  // 游戏流程控制
  startGame: () => set(produce(state => {
    state.gameState.phase = 'PLAYING';
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.winner = null;
  })),
  
  pauseGame: () => set(produce(state => {
    if (state.gameState.phase === 'PLAYING') {
      state.gameState.phase = 'PAUSED';
    }
  })),
  
  resumeGame: () => set(produce(state => {
    if (state.gameState.phase === 'PAUSED') {
      state.gameState.phase = 'PLAYING';
    }
  })),
  
  endGame: (winner: string | null) => set(produce(state => {
    state.gameState.phase = 'RESULT';
    state.gameState.winner = winner;
    if (winner && state.players[winner]) {
      state.players[winner].score += 1;
    }
  })),
  
  resetGame: () => set(produce(state => {
    state.gameState = initialGameState;
    state.players = {
      player1: createInitialMecha('player1', 'BLUE', 200),
      player2: createInitialMecha('player2', 'RED', 550),
    };
    state.effects = [];
  })),
  
  nextRound: () => set(produce(state => {
    state.gameState.round += 1;
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.phase = 'PLAYING';
    state.gameState.winner = null;
    
    // 重置玩家位置和状态
    Object.keys(state.players).forEach(playerId =>import { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
  winner: null,
};

// 游戏Store接口
interface GameStore {
  // 状态
  gameState: GameState;
  players: Record<string, MechaData>;
  effects: Effect[];
  
  // 游戏流程控制
  startGame: () => void;
  pauseGame: () => void;
  resumeGame: () => void;
  endGame: (winner: string | null) => void;
  resetGame: () => void;
  nextRound: () => void;
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => void;
  updatePlayerState: (playerId: string, state: MechaData['state']) => void;
  updatePlayerFacing: (playerId: string, facingRight: boolean) => void;
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => void;
  updatePlayerDefending: (playerId: string, isDefending: boolean) => void;
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => void;
  dealDamage: (targetId: string, damage: number, attackerId?: string) => void;
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => void;
  setAnimationFrame: (playerId: string, frame: number) => void;
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => string;
  updateEffects: (deltaTime: number) => void;
  removeEffect: (effectId: string) => void;
  clearEffects: () => void;
  
  // 游戏时间
  tick: (deltaTime: number) => void;
}

// 创建Game Store
export const useGameStore = create<GameStore>((set, get) => ({
  // 初始状态
  gameState: initialGameState,
  players: {
    player1: createInitialMecha('player1', 'BLUE', 200),
    player2: createInitialMecha('player2', 'RED', 550),
  },
  effects: [],
  
  // 游戏流程控制
  startGame: () => set(produce(state => {
    state.gameState.phase = 'PLAYING';
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.winner = null;
  })),
  
  pauseGame: () => set(produce(state => {
    if (state.gameState.phase === 'PLAYING') {
      state.gameState.phase = 'PAUSED';
    }
  })),
  
  resumeGame: () => set(produce(state => {
    if (state.gameState.phase === 'PAUSED') {
      state.gameState.phase = 'PLAYING';
    }
  })),
  
  endGame: (winner: string | null) => set(produce(state => {
    state.gameState.phase = 'RESULT';
    state.gameState.winner = winner;
    if (winner && state.players[winner]) {
      state.players[winner].score += 1;
    }
  })),
  
  resetGame: () => set(produce(state => {
    state.gameState = initialGameState;
    state.players = {
      player1: createInitialMecha('player1', 'BLUE', 200),
      player2: createInitialMecha('player2', 'RED', 550),
    };
    state.effects = [];
  })),
  
  nextRound: () => set(produce(state => {
    state.gameState.round += 1;
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.phase = 'PLAYING';
    state.gameState.winner = null;
    
    // 重置玩家位置和状态
    Object.keys(state.players).forEach(playerId => {
      const player =import { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
  winner: null,
};

// 游戏Store接口
interface GameStore {
  // 状态
  gameState: GameState;
  players: Record<string, MechaData>;
  effects: Effect[];
  
  // 游戏流程控制
  startGame: () => void;
  pauseGame: () => void;
  resumeGame: () => void;
  endGame: (winner: string | null) => void;
  resetGame: () => void;
  nextRound: () => void;
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => void;
  updatePlayerState: (playerId: string, state: MechaData['state']) => void;
  updatePlayerFacing: (playerId: string, facingRight: boolean) => void;
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => void;
  updatePlayerDefending: (playerId: string, isDefending: boolean) => void;
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => void;
  dealDamage: (targetId: string, damage: number, attackerId?: string) => void;
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => void;
  setAnimationFrame: (playerId: string, frame: number) => void;
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => string;
  updateEffects: (deltaTime: number) => void;
  removeEffect: (effectId: string) => void;
  clearEffects: () => void;
  
  // 游戏时间
  tick: (deltaTime: number) => void;
}

// 创建Game Store
export const useGameStore = create<GameStore>((set, get) => ({
  // 初始状态
  gameState: initialGameState,
  players: {
    player1: createInitialMecha('player1', 'BLUE', 200),
    player2: createInitialMecha('player2', 'RED', 550),
  },
  effects: [],
  
  // 游戏流程控制
  startGame: () => set(produce(state => {
    state.gameState.phase = 'PLAYING';
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.winner = null;
  })),
  
  pauseGame: () => set(produce(state => {
    if (state.gameState.phase === 'PLAYING') {
      state.gameState.phase = 'PAUSED';
    }
  })),
  
  resumeGame: () => set(produce(state => {
    if (state.gameState.phase === 'PAUSED') {
      state.gameState.phase = 'PLAYING';
    }
  })),
  
  endGame: (winner: string | null) => set(produce(state => {
    state.gameState.phase = 'RESULT';
    state.gameState.winner = winner;
    if (winner && state.players[winner]) {
      state.players[winner].score += 1;
    }
  })),
  
  resetGame: () => set(produce(state => {
    state.gameState = initialGameState;
    state.players = {
      player1: createInitialMecha('player1', 'BLUE', 200),
      player2: createInitialMecha('player2', 'RED', 550),
    };
    state.effects = [];
  })),
  
  nextRound: () => set(produce(state => {
    state.gameState.round += 1;
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.phase = 'PLAYING';
    state.gameState.winner = null;
    
    // 重置玩家位置和状态
    Object.keys(state.players).forEach(playerId => {
      const player = state.players[playerId];
import { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
  winner: null,
};

// 游戏Store接口
interface GameStore {
  // 状态
  gameState: GameState;
  players: Record<string, MechaData>;
  effects: Effect[];
  
  // 游戏流程控制
  startGame: () => void;
  pauseGame: () => void;
  resumeGame: () => void;
  endGame: (winner: string | null) => void;
  resetGame: () => void;
  nextRound: () => void;
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => void;
  updatePlayerState: (playerId: string, state: MechaData['state']) => void;
  updatePlayerFacing: (playerId: string, facingRight: boolean) => void;
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => void;
  updatePlayerDefending: (playerId: string, isDefending: boolean) => void;
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => void;
  dealDamage: (targetId: string, damage: number, attackerId?: string) => void;
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => void;
  setAnimationFrame: (playerId: string, frame: number) => void;
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => string;
  updateEffects: (deltaTime: number) => void;
  removeEffect: (effectId: string) => void;
  clearEffects: () => void;
  
  // 游戏时间
  tick: (deltaTime: number) => void;
}

// 创建Game Store
export const useGameStore = create<GameStore>((set, get) => ({
  // 初始状态
  gameState: initialGameState,
  players: {
    player1: createInitialMecha('player1', 'BLUE', 200),
    player2: createInitialMecha('player2', 'RED', 550),
  },
  effects: [],
  
  // 游戏流程控制
  startGame: () => set(produce(state => {
    state.gameState.phase = 'PLAYING';
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.winner = null;
  })),
  
  pauseGame: () => set(produce(state => {
    if (state.gameState.phase === 'PLAYING') {
      state.gameState.phase = 'PAUSED';
    }
  })),
  
  resumeGame: () => set(produce(state => {
    if (state.gameState.phase === 'PAUSED') {
      state.gameState.phase = 'PLAYING';
    }
  })),
  
  endGame: (winner: string | null) => set(produce(state => {
    state.gameState.phase = 'RESULT';
    state.gameState.winner = winner;
    if (winner && state.players[winner]) {
      state.players[winner].score += 1;
    }
  })),
  
  resetGame: () => set(produce(state => {
    state.gameState = initialGameState;
    state.players = {
      player1: createInitialMecha('player1', 'BLUE', 200),
      player2: createInitialMecha('player2', 'RED', 550),
    };
    state.effects = [];
  })),
  
  nextRound: () => set(produce(state => {
    state.gameState.round += 1;
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.phase = 'PLAYING';
    state.gameState.winner = null;
    
    // 重置玩家位置和状态
    Object.keys(state.players).forEach(playerId => {
      const player = state.players[playerId];
      player.health = player.maximport { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
  winner: null,
};

// 游戏Store接口
interface GameStore {
  // 状态
  gameState: GameState;
  players: Record<string, MechaData>;
  effects: Effect[];
  
  // 游戏流程控制
  startGame: () => void;
  pauseGame: () => void;
  resumeGame: () => void;
  endGame: (winner: string | null) => void;
  resetGame: () => void;
  nextRound: () => void;
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => void;
  updatePlayerState: (playerId: string, state: MechaData['state']) => void;
  updatePlayerFacing: (playerId: string, facingRight: boolean) => void;
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => void;
  updatePlayerDefending: (playerId: string, isDefending: boolean) => void;
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => void;
  dealDamage: (targetId: string, damage: number, attackerId?: string) => void;
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => void;
  setAnimationFrame: (playerId: string, frame: number) => void;
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => string;
  updateEffects: (deltaTime: number) => void;
  removeEffect: (effectId: string) => void;
  clearEffects: () => void;
  
  // 游戏时间
  tick: (deltaTime: number) => void;
}

// 创建Game Store
export const useGameStore = create<GameStore>((set, get) => ({
  // 初始状态
  gameState: initialGameState,
  players: {
    player1: createInitialMecha('player1', 'BLUE', 200),
    player2: createInitialMecha('player2', 'RED', 550),
  },
  effects: [],
  
  // 游戏流程控制
  startGame: () => set(produce(state => {
    state.gameState.phase = 'PLAYING';
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.winner = null;
  })),
  
  pauseGame: () => set(produce(state => {
    if (state.gameState.phase === 'PLAYING') {
      state.gameState.phase = 'PAUSED';
    }
  })),
  
  resumeGame: () => set(produce(state => {
    if (state.gameState.phase === 'PAUSED') {
      state.gameState.phase = 'PLAYING';
    }
  })),
  
  endGame: (winner: string | null) => set(produce(state => {
    state.gameState.phase = 'RESULT';
    state.gameState.winner = winner;
    if (winner && state.players[winner]) {
      state.players[winner].score += 1;
    }
  })),
  
  resetGame: () => set(produce(state => {
    state.gameState = initialGameState;
    state.players = {
      player1: createInitialMecha('player1', 'BLUE', 200),
      player2: createInitialMecha('player2', 'RED', 550),
    };
    state.effects = [];
  })),
  
  nextRound: () => set(produce(state => {
    state.gameState.round += 1;
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.phase = 'PLAYING';
    state.gameState.winner = null;
    
    // 重置玩家位置和状态
    Object.keys(state.players).forEach(playerId => {
      const player = state.players[playerId];
      player.health = player.maxHealth;
      player.positionimport { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
  winner: null,
};

// 游戏Store接口
interface GameStore {
  // 状态
  gameState: GameState;
  players: Record<string, MechaData>;
  effects: Effect[];
  
  // 游戏流程控制
  startGame: () => void;
  pauseGame: () => void;
  resumeGame: () => void;
  endGame: (winner: string | null) => void;
  resetGame: () => void;
  nextRound: () => void;
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => void;
  updatePlayerState: (playerId: string, state: MechaData['state']) => void;
  updatePlayerFacing: (playerId: string, facingRight: boolean) => void;
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => void;
  updatePlayerDefending: (playerId: string, isDefending: boolean) => void;
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => void;
  dealDamage: (targetId: string, damage: number, attackerId?: string) => void;
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => void;
  setAnimationFrame: (playerId: string, frame: number) => void;
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => string;
  updateEffects: (deltaTime: number) => void;
  removeEffect: (effectId: string) => void;
  clearEffects: () => void;
  
  // 游戏时间
  tick: (deltaTime: number) => void;
}

// 创建Game Store
export const useGameStore = create<GameStore>((set, get) => ({
  // 初始状态
  gameState: initialGameState,
  players: {
    player1: createInitialMecha('player1', 'BLUE', 200),
    player2: createInitialMecha('player2', 'RED', 550),
  },
  effects: [],
  
  // 游戏流程控制
  startGame: () => set(produce(state => {
    state.gameState.phase = 'PLAYING';
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.winner = null;
  })),
  
  pauseGame: () => set(produce(state => {
    if (state.gameState.phase === 'PLAYING') {
      state.gameState.phase = 'PAUSED';
    }
  })),
  
  resumeGame: () => set(produce(state => {
    if (state.gameState.phase === 'PAUSED') {
      state.gameState.phase = 'PLAYING';
    }
  })),
  
  endGame: (winner: string | null) => set(produce(state => {
    state.gameState.phase = 'RESULT';
    state.gameState.winner = winner;
    if (winner && state.players[winner]) {
      state.players[winner].score += 1;
    }
  })),
  
  resetGame: () => set(produce(state => {
    state.gameState = initialGameState;
    state.players = {
      player1: createInitialMecha('player1', 'BLUE', 200),
      player2: createInitialMecha('player2', 'RED', 550),
    };
    state.effects = [];
  })),
  
  nextRound: () => set(produce(state => {
    state.gameState.round += 1;
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.phase = 'PLAYING';
    state.gameState.winner = null;
    
    // 重置玩家位置和状态
    Object.keys(state.players).forEach(playerId => {
      const player = state.players[playerId];
      player.health = player.maxHealth;
      player.position = playerId === 'playerimport { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
  winner: null,
};

// 游戏Store接口
interface GameStore {
  // 状态
  gameState: GameState;
  players: Record<string, MechaData>;
  effects: Effect[];
  
  // 游戏流程控制
  startGame: () => void;
  pauseGame: () => void;
  resumeGame: () => void;
  endGame: (winner: string | null) => void;
  resetGame: () => void;
  nextRound: () => void;
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => void;
  updatePlayerState: (playerId: string, state: MechaData['state']) => void;
  updatePlayerFacing: (playerId: string, facingRight: boolean) => void;
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => void;
  updatePlayerDefending: (playerId: string, isDefending: boolean) => void;
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => void;
  dealDamage: (targetId: string, damage: number, attackerId?: string) => void;
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => void;
  setAnimationFrame: (playerId: string, frame: number) => void;
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => string;
  updateEffects: (deltaTime: number) => void;
  removeEffect: (effectId: string) => void;
  clearEffects: () => void;
  
  // 游戏时间
  tick: (deltaTime: number) => void;
}

// 创建Game Store
export const useGameStore = create<GameStore>((set, get) => ({
  // 初始状态
  gameState: initialGameState,
  players: {
    player1: createInitialMecha('player1', 'BLUE', 200),
    player2: createInitialMecha('player2', 'RED', 550),
  },
  effects: [],
  
  // 游戏流程控制
  startGame: () => set(produce(state => {
    state.gameState.phase = 'PLAYING';
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.winner = null;
  })),
  
  pauseGame: () => set(produce(state => {
    if (state.gameState.phase === 'PLAYING') {
      state.gameState.phase = 'PAUSED';
    }
  })),
  
  resumeGame: () => set(produce(state => {
    if (state.gameState.phase === 'PAUSED') {
      state.gameState.phase = 'PLAYING';
    }
  })),
  
  endGame: (winner: string | null) => set(produce(state => {
    state.gameState.phase = 'RESULT';
    state.gameState.winner = winner;
    if (winner && state.players[winner]) {
      state.players[winner].score += 1;
    }
  })),
  
  resetGame: () => set(produce(state => {
    state.gameState = initialGameState;
    state.players = {
      player1: createInitialMecha('player1', 'BLUE', 200),
      player2: createInitialMecha('player2', 'RED', 550),
    };
    state.effects = [];
  })),
  
  nextRound: () => set(produce(state => {
    state.gameState.round += 1;
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.phase = 'PLAYING';
    state.gameState.winner = null;
    
    // 重置玩家位置和状态
    Object.keys(state.players).forEach(playerId => {
      const player = state.players[playerId];
      player.health = player.maxHealth;
      player.position = playerId === 'player1' ? { x:import { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
  winner: null,
};

// 游戏Store接口
interface GameStore {
  // 状态
  gameState: GameState;
  players: Record<string, MechaData>;
  effects: Effect[];
  
  // 游戏流程控制
  startGame: () => void;
  pauseGame: () => void;
  resumeGame: () => void;
  endGame: (winner: string | null) => void;
  resetGame: () => void;
  nextRound: () => void;
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => void;
  updatePlayerState: (playerId: string, state: MechaData['state']) => void;
  updatePlayerFacing: (playerId: string, facingRight: boolean) => void;
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => void;
  updatePlayerDefending: (playerId: string, isDefending: boolean) => void;
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => void;
  dealDamage: (targetId: string, damage: number, attackerId?: string) => void;
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => void;
  setAnimationFrame: (playerId: string, frame: number) => void;
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => string;
  updateEffects: (deltaTime: number) => void;
  removeEffect: (effectId: string) => void;
  clearEffects: () => void;
  
  // 游戏时间
  tick: (deltaTime: number) => void;
}

// 创建Game Store
export const useGameStore = create<GameStore>((set, get) => ({
  // 初始状态
  gameState: initialGameState,
  players: {
    player1: createInitialMecha('player1', 'BLUE', 200),
    player2: createInitialMecha('player2', 'RED', 550),
  },
  effects: [],
  
  // 游戏流程控制
  startGame: () => set(produce(state => {
    state.gameState.phase = 'PLAYING';
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.winner = null;
  })),
  
  pauseGame: () => set(produce(state => {
    if (state.gameState.phase === 'PLAYING') {
      state.gameState.phase = 'PAUSED';
    }
  })),
  
  resumeGame: () => set(produce(state => {
    if (state.gameState.phase === 'PAUSED') {
      state.gameState.phase = 'PLAYING';
    }
  })),
  
  endGame: (winner: string | null) => set(produce(state => {
    state.gameState.phase = 'RESULT';
    state.gameState.winner = winner;
    if (winner && state.players[winner]) {
      state.players[winner].score += 1;
    }
  })),
  
  resetGame: () => set(produce(state => {
    state.gameState = initialGameState;
    state.players = {
      player1: createInitialMecha('player1', 'BLUE', 200),
      player2: createInitialMecha('player2', 'RED', 550),
    };
    state.effects = [];
  })),
  
  nextRound: () => set(produce(state => {
    state.gameState.round += 1;
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.phase = 'PLAYING';
    state.gameState.winner = null;
    
    // 重置玩家位置和状态
    Object.keys(state.players).forEach(playerId => {
      const player = state.players[playerId];
      player.health = player.maxHealth;
      player.position = playerId === 'player1' ? { x: 200, y: 400import { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
  winner: null,
};

// 游戏Store接口
interface GameStore {
  // 状态
  gameState: GameState;
  players: Record<string, MechaData>;
  effects: Effect[];
  
  // 游戏流程控制
  startGame: () => void;
  pauseGame: () => void;
  resumeGame: () => void;
  endGame: (winner: string | null) => void;
  resetGame: () => void;
  nextRound: () => void;
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => void;
  updatePlayerState: (playerId: string, state: MechaData['state']) => void;
  updatePlayerFacing: (playerId: string, facingRight: boolean) => void;
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => void;
  updatePlayerDefending: (playerId: string, isDefending: boolean) => void;
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => void;
  dealDamage: (targetId: string, damage: number, attackerId?: string) => void;
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => void;
  setAnimationFrame: (playerId: string, frame: number) => void;
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => string;
  updateEffects: (deltaTime: number) => void;
  removeEffect: (effectId: string) => void;
  clearEffects: () => void;
  
  // 游戏时间
  tick: (deltaTime: number) => void;
}

// 创建Game Store
export const useGameStore = create<GameStore>((set, get) => ({
  // 初始状态
  gameState: initialGameState,
  players: {
    player1: createInitialMecha('player1', 'BLUE', 200),
    player2: createInitialMecha('player2', 'RED', 550),
  },
  effects: [],
  
  // 游戏流程控制
  startGame: () => set(produce(state => {
    state.gameState.phase = 'PLAYING';
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.winner = null;
  })),
  
  pauseGame: () => set(produce(state => {
    if (state.gameState.phase === 'PLAYING') {
      state.gameState.phase = 'PAUSED';
    }
  })),
  
  resumeGame: () => set(produce(state => {
    if (state.gameState.phase === 'PAUSED') {
      state.gameState.phase = 'PLAYING';
    }
  })),
  
  endGame: (winner: string | null) => set(produce(state => {
    state.gameState.phase = 'RESULT';
    state.gameState.winner = winner;
    if (winner && state.players[winner]) {
      state.players[winner].score += 1;
    }
  })),
  
  resetGame: () => set(produce(state => {
    state.gameState = initialGameState;
    state.players = {
      player1: createInitialMecha('player1', 'BLUE', 200),
      player2: createInitialMecha('player2', 'RED', 550),
    };
    state.effects = [];
  })),
  
  nextRound: () => set(produce(state => {
    state.gameState.round += 1;
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.phase = 'PLAYING';
    state.gameState.winner = null;
    
    // 重置玩家位置和状态
    Object.keys(state.players).forEach(playerId => {
      const player = state.players[playerId];
      player.health = player.maxHealth;
      player.position = playerId === 'player1' ? { x: 200, y: 400 } : { x: 550import { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
  winner: null,
};

// 游戏Store接口
interface GameStore {
  // 状态
  gameState: GameState;
  players: Record<string, MechaData>;
  effects: Effect[];
  
  // 游戏流程控制
  startGame: () => void;
  pauseGame: () => void;
  resumeGame: () => void;
  endGame: (winner: string | null) => void;
  resetGame: () => void;
  nextRound: () => void;
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => void;
  updatePlayerState: (playerId: string, state: MechaData['state']) => void;
  updatePlayerFacing: (playerId: string, facingRight: boolean) => void;
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => void;
  updatePlayerDefending: (playerId: string, isDefending: boolean) => void;
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => void;
  dealDamage: (targetId: string, damage: number, attackerId?: string) => void;
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => void;
  setAnimationFrame: (playerId: string, frame: number) => void;
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => string;
  updateEffects: (deltaTime: number) => void;
  removeEffect: (effectId: string) => void;
  clearEffects: () => void;
  
  // 游戏时间
  tick: (deltaTime: number) => void;
}

// 创建Game Store
export const useGameStore = create<GameStore>((set, get) => ({
  // 初始状态
  gameState: initialGameState,
  players: {
    player1: createInitialMecha('player1', 'BLUE', 200),
    player2: createInitialMecha('player2', 'RED', 550),
  },
  effects: [],
  
  // 游戏流程控制
  startGame: () => set(produce(state => {
    state.gameState.phase = 'PLAYING';
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.winner = null;
  })),
  
  pauseGame: () => set(produce(state => {
    if (state.gameState.phase === 'PLAYING') {
      state.gameState.phase = 'PAUSED';
    }
  })),
  
  resumeGame: () => set(produce(state => {
    if (state.gameState.phase === 'PAUSED') {
      state.gameState.phase = 'PLAYING';
    }
  })),
  
  endGame: (winner: string | null) => set(produce(state => {
    state.gameState.phase = 'RESULT';
    state.gameState.winner = winner;
    if (winner && state.players[winner]) {
      state.players[winner].score += 1;
    }
  })),
  
  resetGame: () => set(produce(state => {
    state.gameState = initialGameState;
    state.players = {
      player1: createInitialMecha('player1', 'BLUE', 200),
      player2: createInitialMecha('player2', 'RED', 550),
    };
    state.effects = [];
  })),
  
  nextRound: () => set(produce(state => {
    state.gameState.round += 1;
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.phase = 'PLAYING';
    state.gameState.winner = null;
    
    // 重置玩家位置和状态
    Object.keys(state.players).forEach(playerId => {
      const player = state.players[playerId];
      player.health = player.maxHealth;
      player.position = playerId === 'player1' ? { x: 200, y: 400 } : { x: 550, y: 400 }import { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
  winner: null,
};

// 游戏Store接口
interface GameStore {
  // 状态
  gameState: GameState;
  players: Record<string, MechaData>;
  effects: Effect[];
  
  // 游戏流程控制
  startGame: () => void;
  pauseGame: () => void;
  resumeGame: () => void;
  endGame: (winner: string | null) => void;
  resetGame: () => void;
  nextRound: () => void;
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => void;
  updatePlayerState: (playerId: string, state: MechaData['state']) => void;
  updatePlayerFacing: (playerId: string, facingRight: boolean) => void;
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => void;
  updatePlayerDefending: (playerId: string, isDefending: boolean) => void;
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => void;
  dealDamage: (targetId: string, damage: number, attackerId?: string) => void;
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => void;
  setAnimationFrame: (playerId: string, frame: number) => void;
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => string;
  updateEffects: (deltaTime: number) => void;
  removeEffect: (effectId: string) => void;
  clearEffects: () => void;
  
  // 游戏时间
  tick: (deltaTime: number) => void;
}

// 创建Game Store
export const useGameStore = create<GameStore>((set, get) => ({
  // 初始状态
  gameState: initialGameState,
  players: {
    player1: createInitialMecha('player1', 'BLUE', 200),
    player2: createInitialMecha('player2', 'RED', 550),
  },
  effects: [],
  
  // 游戏流程控制
  startGame: () => set(produce(state => {
    state.gameState.phase = 'PLAYING';
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.winner = null;
  })),
  
  pauseGame: () => set(produce(state => {
    if (state.gameState.phase === 'PLAYING') {
      state.gameState.phase = 'PAUSED';
    }
  })),
  
  resumeGame: () => set(produce(state => {
    if (state.gameState.phase === 'PAUSED') {
      state.gameState.phase = 'PLAYING';
    }
  })),
  
  endGame: (winner: string | null) => set(produce(state => {
    state.gameState.phase = 'RESULT';
    state.gameState.winner = winner;
    if (winner && state.players[winner]) {
      state.players[winner].score += 1;
    }
  })),
  
  resetGame: () => set(produce(state => {
    state.gameState = initialGameState;
    state.players = {
      player1: createInitialMecha('player1', 'BLUE', 200),
      player2: createInitialMecha('player2', 'RED', 550),
    };
    state.effects = [];
  })),
  
  nextRound: () => set(produce(state => {
    state.gameState.round += 1;
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.phase = 'PLAYING';
    state.gameState.winner = null;
    
    // 重置玩家位置和状态
    Object.keys(state.players).forEach(playerId => {
      const player = state.players[playerId];
      player.health = player.maxHealth;
      player.position = playerId === 'player1' ? { x: 200, y: 400 } : { x: 550, y: 400 };
      player.velocity =import { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
  winner: null,
};

// 游戏Store接口
interface GameStore {
  // 状态
  gameState: GameState;
  players: Record<string, MechaData>;
  effects: Effect[];
  
  // 游戏流程控制
  startGame: () => void;
  pauseGame: () => void;
  resumeGame: () => void;
  endGame: (winner: string | null) => void;
  resetGame: () => void;
  nextRound: () => void;
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => void;
  updatePlayerState: (playerId: string, state: MechaData['state']) => void;
  updatePlayerFacing: (playerId: string, facingRight: boolean) => void;
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => void;
  updatePlayerDefending: (playerId: string, isDefending: boolean) => void;
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => void;
  dealDamage: (targetId: string, damage: number, attackerId?: string) => void;
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => void;
  setAnimationFrame: (playerId: string, frame: number) => void;
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => string;
  updateEffects: (deltaTime: number) => void;
  removeEffect: (effectId: string) => void;
  clearEffects: () => void;
  
  // 游戏时间
  tick: (deltaTime: number) => void;
}

// 创建Game Store
export const useGameStore = create<GameStore>((set, get) => ({
  // 初始状态
  gameState: initialGameState,
  players: {
    player1: createInitialMecha('player1', 'BLUE', 200),
    player2: createInitialMecha('player2', 'RED', 550),
  },
  effects: [],
  
  // 游戏流程控制
  startGame: () => set(produce(state => {
    state.gameState.phase = 'PLAYING';
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.winner = null;
  })),
  
  pauseGame: () => set(produce(state => {
    if (state.gameState.phase === 'PLAYING') {
      state.gameState.phase = 'PAUSED';
    }
  })),
  
  resumeGame: () => set(produce(state => {
    if (state.gameState.phase === 'PAUSED') {
      state.gameState.phase = 'PLAYING';
    }
  })),
  
  endGame: (winner: string | null) => set(produce(state => {
    state.gameState.phase = 'RESULT';
    state.gameState.winner = winner;
    if (winner && state.players[winner]) {
      state.players[winner].score += 1;
    }
  })),
  
  resetGame: () => set(produce(state => {
    state.gameState = initialGameState;
    state.players = {
      player1: createInitialMecha('player1', 'BLUE', 200),
      player2: createInitialMecha('player2', 'RED', 550),
    };
    state.effects = [];
  })),
  
  nextRound: () => set(produce(state => {
    state.gameState.round += 1;
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.phase = 'PLAYING';
    state.gameState.winner = null;
    
    // 重置玩家位置和状态
    Object.keys(state.players).forEach(playerId => {
      const player = state.players[playerId];
      player.health = player.maxHealth;
      player.position = playerId === 'player1' ? { x: 200, y: 400 } : { x: 550, y: 400 };
      player.velocity = { x: 0, yimport { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
  winner: null,
};

// 游戏Store接口
interface GameStore {
  // 状态
  gameState: GameState;
  players: Record<string, MechaData>;
  effects: Effect[];
  
  // 游戏流程控制
  startGame: () => void;
  pauseGame: () => void;
  resumeGame: () => void;
  endGame: (winner: string | null) => void;
  resetGame: () => void;
  nextRound: () => void;
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => void;
  updatePlayerState: (playerId: string, state: MechaData['state']) => void;
  updatePlayerFacing: (playerId: string, facingRight: boolean) => void;
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => void;
  updatePlayerDefending: (playerId: string, isDefending: boolean) => void;
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => void;
  dealDamage: (targetId: string, damage: number, attackerId?: string) => void;
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => void;
  setAnimationFrame: (playerId: string, frame: number) => void;
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => string;
  updateEffects: (deltaTime: number) => void;
  removeEffect: (effectId: string) => void;
  clearEffects: () => void;
  
  // 游戏时间
  tick: (deltaTime: number) => void;
}

// 创建Game Store
export const useGameStore = create<GameStore>((set, get) => ({
  // 初始状态
  gameState: initialGameState,
  players: {
    player1: createInitialMecha('player1', 'BLUE', 200),
    player2: createInitialMecha('player2', 'RED', 550),
  },
  effects: [],
  
  // 游戏流程控制
  startGame: () => set(produce(state => {
    state.gameState.phase = 'PLAYING';
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.winner = null;
  })),
  
  pauseGame: () => set(produce(state => {
    if (state.gameState.phase === 'PLAYING') {
      state.gameState.phase = 'PAUSED';
    }
  })),
  
  resumeGame: () => set(produce(state => {
    if (state.gameState.phase === 'PAUSED') {
      state.gameState.phase = 'PLAYING';
    }
  })),
  
  endGame: (winner: string | null) => set(produce(state => {
    state.gameState.phase = 'RESULT';
    state.gameState.winner = winner;
    if (winner && state.players[winner]) {
      state.players[winner].score += 1;
    }
  })),
  
  resetGame: () => set(produce(state => {
    state.gameState = initialGameState;
    state.players = {
      player1: createInitialMecha('player1', 'BLUE', 200),
      player2: createInitialMecha('player2', 'RED', 550),
    };
    state.effects = [];
  })),
  
  nextRound: () => set(produce(state => {
    state.gameState.round += 1;
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.phase = 'PLAYING';
    state.gameState.winner = null;
    
    // 重置玩家位置和状态
    Object.keys(state.players).forEach(playerId => {
      const player = state.players[playerId];
      player.health = player.maxHealth;
      player.position = playerId === 'player1' ? { x: 200, y: 400 } : { x: 550, y: 400 };
      player.velocity = { x: 0, y: 0 };
      playerimport { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
  winner: null,
};

// 游戏Store接口
interface GameStore {
  // 状态
  gameState: GameState;
  players: Record<string, MechaData>;
  effects: Effect[];
  
  // 游戏流程控制
  startGame: () => void;
  pauseGame: () => void;
  resumeGame: () => void;
  endGame: (winner: string | null) => void;
  resetGame: () => void;
  nextRound: () => void;
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => void;
  updatePlayerState: (playerId: string, state: MechaData['state']) => void;
  updatePlayerFacing: (playerId: string, facingRight: boolean) => void;
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => void;
  updatePlayerDefending: (playerId: string, isDefending: boolean) => void;
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => void;
  dealDamage: (targetId: string, damage: number, attackerId?: string) => void;
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => void;
  setAnimationFrame: (playerId: string, frame: number) => void;
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => string;
  updateEffects: (deltaTime: number) => void;
  removeEffect: (effectId: string) => void;
  clearEffects: () => void;
  
  // 游戏时间
  tick: (deltaTime: number) => void;
}

// 创建Game Store
export const useGameStore = create<GameStore>((set, get) => ({
  // 初始状态
  gameState: initialGameState,
  players: {
    player1: createInitialMecha('player1', 'BLUE', 200),
    player2: createInitialMecha('player2', 'RED', 550),
  },
  effects: [],
  
  // 游戏流程控制
  startGame: () => set(produce(state => {
    state.gameState.phase = 'PLAYING';
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.winner = null;
  })),
  
  pauseGame: () => set(produce(state => {
    if (state.gameState.phase === 'PLAYING') {
      state.gameState.phase = 'PAUSED';
    }
  })),
  
  resumeGame: () => set(produce(state => {
    if (state.gameState.phase === 'PAUSED') {
      state.gameState.phase = 'PLAYING';
    }
  })),
  
  endGame: (winner: string | null) => set(produce(state => {
    state.gameState.phase = 'RESULT';
    state.gameState.winner = winner;
    if (winner && state.players[winner]) {
      state.players[winner].score += 1;
    }
  })),
  
  resetGame: () => set(produce(state => {
    state.gameState = initialGameState;
    state.players = {
      player1: createInitialMecha('player1', 'BLUE', 200),
      player2: createInitialMecha('player2', 'RED', 550),
    };
    state.effects = [];
  })),
  
  nextRound: () => set(produce(state => {
    state.gameState.round += 1;
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.phase = 'PLAYING';
    state.gameState.winner = null;
    
    // 重置玩家位置和状态
    Object.keys(state.players).forEach(playerId => {
      const player = state.players[playerId];
      player.health = player.maxHealth;
      player.position = playerId === 'player1' ? { x: 200, y: 400 } : { x: 550, y: 400 };
      player.velocity = { x: 0, y: 0 };
      player.state = 'IDLE';
import { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
  winner: null,
};

// 游戏Store接口
interface GameStore {
  // 状态
  gameState: GameState;
  players: Record<string, MechaData>;
  effects: Effect[];
  
  // 游戏流程控制
  startGame: () => void;
  pauseGame: () => void;
  resumeGame: () => void;
  endGame: (winner: string | null) => void;
  resetGame: () => void;
  nextRound: () => void;
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => void;
  updatePlayerState: (playerId: string, state: MechaData['state']) => void;
  updatePlayerFacing: (playerId: string, facingRight: boolean) => void;
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => void;
  updatePlayerDefending: (playerId: string, isDefending: boolean) => void;
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => void;
  dealDamage: (targetId: string, damage: number, attackerId?: string) => void;
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => void;
  setAnimationFrame: (playerId: string, frame: number) => void;
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => string;
  updateEffects: (deltaTime: number) => void;
  removeEffect: (effectId: string) => void;
  clearEffects: () => void;
  
  // 游戏时间
  tick: (deltaTime: number) => void;
}

// 创建Game Store
export const useGameStore = create<GameStore>((set, get) => ({
  // 初始状态
  gameState: initialGameState,
  players: {
    player1: createInitialMecha('player1', 'BLUE', 200),
    player2: createInitialMecha('player2', 'RED', 550),
  },
  effects: [],
  
  // 游戏流程控制
  startGame: () => set(produce(state => {
    state.gameState.phase = 'PLAYING';
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.winner = null;
  })),
  
  pauseGame: () => set(produce(state => {
    if (state.gameState.phase === 'PLAYING') {
      state.gameState.phase = 'PAUSED';
    }
  })),
  
  resumeGame: () => set(produce(state => {
    if (state.gameState.phase === 'PAUSED') {
      state.gameState.phase = 'PLAYING';
    }
  })),
  
  endGame: (winner: string | null) => set(produce(state => {
    state.gameState.phase = 'RESULT';
    state.gameState.winner = winner;
    if (winner && state.players[winner]) {
      state.players[winner].score += 1;
    }
  })),
  
  resetGame: () => set(produce(state => {
    state.gameState = initialGameState;
    state.players = {
      player1: createInitialMecha('player1', 'BLUE', 200),
      player2: createInitialMecha('player2', 'RED', 550),
    };
    state.effects = [];
  })),
  
  nextRound: () => set(produce(state => {
    state.gameState.round += 1;
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.phase = 'PLAYING';
    state.gameState.winner = null;
    
    // 重置玩家位置和状态
    Object.keys(state.players).forEach(playerId => {
      const player = state.players[playerId];
      player.health = player.maxHealth;
      player.position = playerId === 'player1' ? { x: 200, y: 400 } : { x: 550, y: 400 };
      player.velocity = { x: 0, y: 0 };
      player.state = 'IDLE';
      player.isDefending = falseimport { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
  winner: null,
};

// 游戏Store接口
interface GameStore {
  // 状态
  gameState: GameState;
  players: Record<string, MechaData>;
  effects: Effect[];
  
  // 游戏流程控制
  startGame: () => void;
  pauseGame: () => void;
  resumeGame: () => void;
  endGame: (winner: string | null) => void;
  resetGame: () => void;
  nextRound: () => void;
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => void;
  updatePlayerState: (playerId: string, state: MechaData['state']) => void;
  updatePlayerFacing: (playerId: string, facingRight: boolean) => void;
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => void;
  updatePlayerDefending: (playerId: string, isDefending: boolean) => void;
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => void;
  dealDamage: (targetId: string, damage: number, attackerId?: string) => void;
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => void;
  setAnimationFrame: (playerId: string, frame: number) => void;
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => string;
  updateEffects: (deltaTime: number) => void;
  removeEffect: (effectId: string) => void;
  clearEffects: () => void;
  
  // 游戏时间
  tick: (deltaTime: number) => void;
}

// 创建Game Store
export const useGameStore = create<GameStore>((set, get) => ({
  // 初始状态
  gameState: initialGameState,
  players: {
    player1: createInitialMecha('player1', 'BLUE', 200),
    player2: createInitialMecha('player2', 'RED', 550),
  },
  effects: [],
  
  // 游戏流程控制
  startGame: () => set(produce(state => {
    state.gameState.phase = 'PLAYING';
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.winner = null;
  })),
  
  pauseGame: () => set(produce(state => {
    if (state.gameState.phase === 'PLAYING') {
      state.gameState.phase = 'PAUSED';
    }
  })),
  
  resumeGame: () => set(produce(state => {
    if (state.gameState.phase === 'PAUSED') {
      state.gameState.phase = 'PLAYING';
    }
  })),
  
  endGame: (winner: string | null) => set(produce(state => {
    state.gameState.phase = 'RESULT';
    state.gameState.winner = winner;
    if (winner && state.players[winner]) {
      state.players[winner].score += 1;
    }
  })),
  
  resetGame: () => set(produce(state => {
    state.gameState = initialGameState;
    state.players = {
      player1: createInitialMecha('player1', 'BLUE', 200),
      player2: createInitialMecha('player2', 'RED', 550),
    };
    state.effects = [];
  })),
  
  nextRound: () => set(produce(state => {
    state.gameState.round += 1;
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.phase = 'PLAYING';
    state.gameState.winner = null;
    
    // 重置玩家位置和状态
    Object.keys(state.players).forEach(playerId => {
      const player = state.players[playerId];
      player.health = player.maxHealth;
      player.position = playerId === 'player1' ? { x: 200, y: 400 } : { x: 550, y: 400 };
      player.velocity = { x: 0, y: 0 };
      player.state = 'IDLE';
      player.isDefending = false;
      player.isGroundedimport { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
  winner: null,
};

// 游戏Store接口
interface GameStore {
  // 状态
  gameState: GameState;
  players: Record<string, MechaData>;
  effects: Effect[];
  
  // 游戏流程控制
  startGame: () => void;
  pauseGame: () => void;
  resumeGame: () => void;
  endGame: (winner: string | null) => void;
  resetGame: () => void;
  nextRound: () => void;
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => void;
  updatePlayerState: (playerId: string, state: MechaData['state']) => void;
  updatePlayerFacing: (playerId: string, facingRight: boolean) => void;
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => void;
  updatePlayerDefending: (playerId: string, isDefending: boolean) => void;
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => void;
  dealDamage: (targetId: string, damage: number, attackerId?: string) => void;
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => void;
  setAnimationFrame: (playerId: string, frame: number) => void;
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => string;
  updateEffects: (deltaTime: number) => void;
  removeEffect: (effectId: string) => void;
  clearEffects: () => void;
  
  // 游戏时间
  tick: (deltaTime: number) => void;
}

// 创建Game Store
export const useGameStore = create<GameStore>((set, get) => ({
  // 初始状态
  gameState: initialGameState,
  players: {
    player1: createInitialMecha('player1', 'BLUE', 200),
    player2: createInitialMecha('player2', 'RED', 550),
  },
  effects: [],
  
  // 游戏流程控制
  startGame: () => set(produce(state => {
    state.gameState.phase = 'PLAYING';
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.winner = null;
  })),
  
  pauseGame: () => set(produce(state => {
    if (state.gameState.phase === 'PLAYING') {
      state.gameState.phase = 'PAUSED';
    }
  })),
  
  resumeGame: () => set(produce(state => {
    if (state.gameState.phase === 'PAUSED') {
      state.gameState.phase = 'PLAYING';
    }
  })),
  
  endGame: (winner: string | null) => set(produce(state => {
    state.gameState.phase = 'RESULT';
    state.gameState.winner = winner;
    if (winner && state.players[winner]) {
      state.players[winner].score += 1;
    }
  })),
  
  resetGame: () => set(produce(state => {
    state.gameState = initialGameState;
    state.players = {
      player1: createInitialMecha('player1', 'BLUE', 200),
      player2: createInitialMecha('player2', 'RED', 550),
    };
    state.effects = [];
  })),
  
  nextRound: () => set(produce(state => {
    state.gameState.round += 1;
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.phase = 'PLAYING';
    state.gameState.winner = null;
    
    // 重置玩家位置和状态
    Object.keys(state.players).forEach(playerId => {
      const player = state.players[playerId];
      player.health = player.maxHealth;
      player.position = playerId === 'player1' ? { x: 200, y: 400 } : { x: 550, y: 400 };
      player.velocity = { x: 0, y: 0 };
      player.state = 'IDLE';
      player.isDefending = false;
      player.isGrounded = false;
      player.import { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
  winner: null,
};

// 游戏Store接口
interface GameStore {
  // 状态
  gameState: GameState;
  players: Record<string, MechaData>;
  effects: Effect[];
  
  // 游戏流程控制
  startGame: () => void;
  pauseGame: () => void;
  resumeGame: () => void;
  endGame: (winner: string | null) => void;
  resetGame: () => void;
  nextRound: () => void;
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => void;
  updatePlayerState: (playerId: string, state: MechaData['state']) => void;
  updatePlayerFacing: (playerId: string, facingRight: boolean) => void;
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => void;
  updatePlayerDefending: (playerId: string, isDefending: boolean) => void;
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => void;
  dealDamage: (targetId: string, damage: number, attackerId?: string) => void;
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => void;
  setAnimationFrame: (playerId: string, frame: number) => void;
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => string;
  updateEffects: (deltaTime: number) => void;
  removeEffect: (effectId: string) => void;
  clearEffects: () => void;
  
  // 游戏时间
  tick: (deltaTime: number) => void;
}

// 创建Game Store
export const useGameStore = create<GameStore>((set, get) => ({
  // 初始状态
  gameState: initialGameState,
  players: {
    player1: createInitialMecha('player1', 'BLUE', 200),
    player2: createInitialMecha('player2', 'RED', 550),
  },
  effects: [],
  
  // 游戏流程控制
  startGame: () => set(produce(state => {
    state.gameState.phase = 'PLAYING';
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.winner = null;
  })),
  
  pauseGame: () => set(produce(state => {
    if (state.gameState.phase === 'PLAYING') {
      state.gameState.phase = 'PAUSED';
    }
  })),
  
  resumeGame: () => set(produce(state => {
    if (state.gameState.phase === 'PAUSED') {
      state.gameState.phase = 'PLAYING';
    }
  })),
  
  endGame: (winner: string | null) => set(produce(state => {
    state.gameState.phase = 'RESULT';
    state.gameState.winner = winner;
    if (winner && state.players[winner]) {
      state.players[winner].score += 1;
    }
  })),
  
  resetGame: () => set(produce(state => {
    state.gameState = initialGameState;
    state.players = {
      player1: createInitialMecha('player1', 'BLUE', 200),
      player2: createInitialMecha('player2', 'RED', 550),
    };
    state.effects = [];
  })),
  
  nextRound: () => set(produce(state => {
    state.gameState.round += 1;
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.phase = 'PLAYING';
    state.gameState.winner = null;
    
    // 重置玩家位置和状态
    Object.keys(state.players).forEach(playerId => {
      const player = state.players[playerId];
      player.health = player.maxHealth;
      player.position = playerId === 'player1' ? { x: 200, y: 400 } : { x: 550, y: 400 };
      player.velocity = { x: 0, y: 0 };
      player.state = 'IDLE';
      player.isDefending = false;
      player.isGrounded = false;
      player.attackCooldown = 0;
import { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
  winner: null,
};

// 游戏Store接口
interface GameStore {
  // 状态
  gameState: GameState;
  players: Record<string, MechaData>;
  effects: Effect[];
  
  // 游戏流程控制
  startGame: () => void;
  pauseGame: () => void;
  resumeGame: () => void;
  endGame: (winner: string | null) => void;
  resetGame: () => void;
  nextRound: () => void;
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => void;
  updatePlayerState: (playerId: string, state: MechaData['state']) => void;
  updatePlayerFacing: (playerId: string, facingRight: boolean) => void;
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => void;
  updatePlayerDefending: (playerId: string, isDefending: boolean) => void;
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => void;
  dealDamage: (targetId: string, damage: number, attackerId?: string) => void;
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => void;
  setAnimationFrame: (playerId: string, frame: number) => void;
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => string;
  updateEffects: (deltaTime: number) => void;
  removeEffect: (effectId: string) => void;
  clearEffects: () => void;
  
  // 游戏时间
  tick: (deltaTime: number) => void;
}

// 创建Game Store
export const useGameStore = create<GameStore>((set, get) => ({
  // 初始状态
  gameState: initialGameState,
  players: {
    player1: createInitialMecha('player1', 'BLUE', 200),
    player2: createInitialMecha('player2', 'RED', 550),
  },
  effects: [],
  
  // 游戏流程控制
  startGame: () => set(produce(state => {
    state.gameState.phase = 'PLAYING';
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.winner = null;
  })),
  
  pauseGame: () => set(produce(state => {
    if (state.gameState.phase === 'PLAYING') {
      state.gameState.phase = 'PAUSED';
    }
  })),
  
  resumeGame: () => set(produce(state => {
    if (state.gameState.phase === 'PAUSED') {
      state.gameState.phase = 'PLAYING';
    }
  })),
  
  endGame: (winner: string | null) => set(produce(state => {
    state.gameState.phase = 'RESULT';
    state.gameState.winner = winner;
    if (winner && state.players[winner]) {
      state.players[winner].score += 1;
    }
  })),
  
  resetGame: () => set(produce(state => {
    state.gameState = initialGameState;
    state.players = {
      player1: createInitialMecha('player1', 'BLUE', 200),
      player2: createInitialMecha('player2', 'RED', 550),
    };
    state.effects = [];
  })),
  
  nextRound: () => set(produce(state => {
    state.gameState.round += 1;
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.phase = 'PLAYING';
    state.gameState.winner = null;
    
    // 重置玩家位置和状态
    Object.keys(state.players).forEach(playerId => {
      const player = state.players[playerId];
      player.health = player.maxHealth;
      player.position = playerId === 'player1' ? { x: 200, y: 400 } : { x: 550, y: 400 };
      player.velocity = { x: 0, y: 0 };
      player.state = 'IDLE';
      player.isDefending = false;
      player.isGrounded = false;
      player.attackCooldown = 0;
    });
    
import { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
  winner: null,
};

// 游戏Store接口
interface GameStore {
  // 状态
  gameState: GameState;
  players: Record<string, MechaData>;
  effects: Effect[];
  
  // 游戏流程控制
  startGame: () => void;
  pauseGame: () => void;
  resumeGame: () => void;
  endGame: (winner: string | null) => void;
  resetGame: () => void;
  nextRound: () => void;
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => void;
  updatePlayerState: (playerId: string, state: MechaData['state']) => void;
  updatePlayerFacing: (playerId: string, facingRight: boolean) => void;
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => void;
  updatePlayerDefending: (playerId: string, isDefending: boolean) => void;
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => void;
  dealDamage: (targetId: string, damage: number, attackerId?: string) => void;
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => void;
  setAnimationFrame: (playerId: string, frame: number) => void;
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => string;
  updateEffects: (deltaTime: number) => void;
  removeEffect: (effectId: string) => void;
  clearEffects: () => void;
  
  // 游戏时间
  tick: (deltaTime: number) => void;
}

// 创建Game Store
export const useGameStore = create<GameStore>((set, get) => ({
  // 初始状态
  gameState: initialGameState,
  players: {
    player1: createInitialMecha('player1', 'BLUE', 200),
    player2: createInitialMecha('player2', 'RED', 550),
  },
  effects: [],
  
  // 游戏流程控制
  startGame: () => set(produce(state => {
    state.gameState.phase = 'PLAYING';
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.winner = null;
  })),
  
  pauseGame: () => set(produce(state => {
    if (state.gameState.phase === 'PLAYING') {
      state.gameState.phase = 'PAUSED';
    }
  })),
  
  resumeGame: () => set(produce(state => {
    if (state.gameState.phase === 'PAUSED') {
      state.gameState.phase = 'PLAYING';
    }
  })),
  
  endGame: (winner: string | null) => set(produce(state => {
    state.gameState.phase = 'RESULT';
    state.gameState.winner = winner;
    if (winner && state.players[winner]) {
      state.players[winner].score += 1;
    }
  })),
  
  resetGame: () => set(produce(state => {
    state.gameState = initialGameState;
    state.players = {
      player1: createInitialMecha('player1', 'BLUE', 200),
      player2: createInitialMecha('player2', 'RED', 550),
    };
    state.effects = [];
  })),
  
  nextRound: () => set(produce(state => {
    state.gameState.round += 1;
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.phase = 'PLAYING';
    state.gameState.winner = null;
    
    // 重置玩家位置和状态
    Object.keys(state.players).forEach(playerId => {
      const player = state.players[playerId];
      player.health = player.maxHealth;
      player.position = playerId === 'player1' ? { x: 200, y: 400 } : { x: 550, y: 400 };
      player.velocity = { x: 0, y: 0 };
      player.state = 'IDLE';
      player.isDefending = false;
      player.isGrounded = false;
      player.attackCooldown = 0;
    });
    
    state.effects = [];
import { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
  winner: null,
};

// 游戏Store接口
interface GameStore {
  // 状态
  gameState: GameState;
  players: Record<string, MechaData>;
  effects: Effect[];
  
  // 游戏流程控制
  startGame: () => void;
  pauseGame: () => void;
  resumeGame: () => void;
  endGame: (winner: string | null) => void;
  resetGame: () => void;
  nextRound: () => void;
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => void;
  updatePlayerState: (playerId: string, state: MechaData['state']) => void;
  updatePlayerFacing: (playerId: string, facingRight: boolean) => void;
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => void;
  updatePlayerDefending: (playerId: string, isDefending: boolean) => void;
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => void;
  dealDamage: (targetId: string, damage: number, attackerId?: string) => void;
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => void;
  setAnimationFrame: (playerId: string, frame: number) => void;
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => string;
  updateEffects: (deltaTime: number) => void;
  removeEffect: (effectId: string) => void;
  clearEffects: () => void;
  
  // 游戏时间
  tick: (deltaTime: number) => void;
}

// 创建Game Store
export const useGameStore = create<GameStore>((set, get) => ({
  // 初始状态
  gameState: initialGameState,
  players: {
    player1: createInitialMecha('player1', 'BLUE', 200),
    player2: createInitialMecha('player2', 'RED', 550),
  },
  effects: [],
  
  // 游戏流程控制
  startGame: () => set(produce(state => {
    state.gameState.phase = 'PLAYING';
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.winner = null;
  })),
  
  pauseGame: () => set(produce(state => {
    if (state.gameState.phase === 'PLAYING') {
      state.gameState.phase = 'PAUSED';
    }
  })),
  
  resumeGame: () => set(produce(state => {
    if (state.gameState.phase === 'PAUSED') {
      state.gameState.phase = 'PLAYING';
    }
  })),
  
  endGame: (winner: string | null) => set(produce(state => {
    state.gameState.phase = 'RESULT';
    state.gameState.winner = winner;
    if (winner && state.players[winner]) {
      state.players[winner].score += 1;
    }
  })),
  
  resetGame: () => set(produce(state => {
    state.gameState = initialGameState;
    state.players = {
      player1: createInitialMecha('player1', 'BLUE', 200),
      player2: createInitialMecha('player2', 'RED', 550),
    };
    state.effects = [];
  })),
  
  nextRound: () => set(produce(state => {
    state.gameState.round += 1;
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.phase = 'PLAYING';
    state.gameState.winner = null;
    
    // 重置玩家位置和状态
    Object.keys(state.players).forEach(playerId => {
      const player = state.players[playerId];
      player.health = player.maxHealth;
      player.position = playerId === 'player1' ? { x: 200, y: 400 } : { x: 550, y: 400 };
      player.velocity = { x: 0, y: 0 };
      player.state = 'IDLE';
      player.isDefending = false;
      player.isGrounded = false;
      player.attackCooldown = 0;
    });
    
    state.effects = [];
  })),
  
import { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
  winner: null,
};

// 游戏Store接口
interface GameStore {
  // 状态
  gameState: GameState;
  players: Record<string, MechaData>;
  effects: Effect[];
  
  // 游戏流程控制
  startGame: () => void;
  pauseGame: () => void;
  resumeGame: () => void;
  endGame: (winner: string | null) => void;
  resetGame: () => void;
  nextRound: () => void;
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => void;
  updatePlayerState: (playerId: string, state: MechaData['state']) => void;
  updatePlayerFacing: (playerId: string, facingRight: boolean) => void;
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => void;
  updatePlayerDefending: (playerId: string, isDefending: boolean) => void;
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => void;
  dealDamage: (targetId: string, damage: number, attackerId?: string) => void;
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => void;
  setAnimationFrame: (playerId: string, frame: number) => void;
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => string;
  updateEffects: (deltaTime: number) => void;
  removeEffect: (effectId: string) => void;
  clearEffects: () => void;
  
  // 游戏时间
  tick: (deltaTime: number) => void;
}

// 创建Game Store
export const useGameStore = create<GameStore>((set, get) => ({
  // 初始状态
  gameState: initialGameState,
  players: {
    player1: createInitialMecha('player1', 'BLUE', 200),
    player2: createInitialMecha('player2', 'RED', 550),
  },
  effects: [],
  
  // 游戏流程控制
  startGame: () => set(produce(state => {
    state.gameState.phase = 'PLAYING';
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.winner = null;
  })),
  
  pauseGame: () => set(produce(state => {
    if (state.gameState.phase === 'PLAYING') {
      state.gameState.phase = 'PAUSED';
    }
  })),
  
  resumeGame: () => set(produce(state => {
    if (state.gameState.phase === 'PAUSED') {
      state.gameState.phase = 'PLAYING';
    }
  })),
  
  endGame: (winner: string | null) => set(produce(state => {
    state.gameState.phase = 'RESULT';
    state.gameState.winner = winner;
    if (winner && state.players[winner]) {
      state.players[winner].score += 1;
    }
  })),
  
  resetGame: () => set(produce(state => {
    state.gameState = initialGameState;
    state.players = {
      player1: createInitialMecha('player1', 'BLUE', 200),
      player2: createInitialMecha('player2', 'RED', 550),
    };
    state.effects = [];
  })),
  
  nextRound: () => set(produce(state => {
    state.gameState.round += 1;
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.phase = 'PLAYING';
    state.gameState.winner = null;
    
    // 重置玩家位置和状态
    Object.keys(state.players).forEach(playerId => {
      const player = state.players[playerId];
      player.health = player.maxHealth;
      player.position = playerId === 'player1' ? { x: 200, y: 400 } : { x: 550, y: 400 };
      player.velocity = { x: 0, y: 0 };
      player.state = 'IDLE';
      player.isDefending = false;
      player.isGrounded = false;
      player.attackCooldown = 0;
    });
    
    state.effects = [];
  })),
  
  // 玩家操作
  updateimport { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
  winner: null,
};

// 游戏Store接口
interface GameStore {
  // 状态
  gameState: GameState;
  players: Record<string, MechaData>;
  effects: Effect[];
  
  // 游戏流程控制
  startGame: () => void;
  pauseGame: () => void;
  resumeGame: () => void;
  endGame: (winner: string | null) => void;
  resetGame: () => void;
  nextRound: () => void;
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => void;
  updatePlayerState: (playerId: string, state: MechaData['state']) => void;
  updatePlayerFacing: (playerId: string, facingRight: boolean) => void;
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => void;
  updatePlayerDefending: (playerId: string, isDefending: boolean) => void;
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => void;
  dealDamage: (targetId: string, damage: number, attackerId?: string) => void;
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => void;
  setAnimationFrame: (playerId: string, frame: number) => void;
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => string;
  updateEffects: (deltaTime: number) => void;
  removeEffect: (effectId: string) => void;
  clearEffects: () => void;
  
  // 游戏时间
  tick: (deltaTime: number) => void;
}

// 创建Game Store
export const useGameStore = create<GameStore>((set, get) => ({
  // 初始状态
  gameState: initialGameState,
  players: {
    player1: createInitialMecha('player1', 'BLUE', 200),
    player2: createInitialMecha('player2', 'RED', 550),
  },
  effects: [],
  
  // 游戏流程控制
  startGame: () => set(produce(state => {
    state.gameState.phase = 'PLAYING';
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.winner = null;
  })),
  
  pauseGame: () => set(produce(state => {
    if (state.gameState.phase === 'PLAYING') {
      state.gameState.phase = 'PAUSED';
    }
  })),
  
  resumeGame: () => set(produce(state => {
    if (state.gameState.phase === 'PAUSED') {
      state.gameState.phase = 'PLAYING';
    }
  })),
  
  endGame: (winner: string | null) => set(produce(state => {
    state.gameState.phase = 'RESULT';
    state.gameState.winner = winner;
    if (winner && state.players[winner]) {
      state.players[winner].score += 1;
    }
  })),
  
  resetGame: () => set(produce(state => {
    state.gameState = initialGameState;
    state.players = {
      player1: createInitialMecha('player1', 'BLUE', 200),
      player2: createInitialMecha('player2', 'RED', 550),
    };
    state.effects = [];
  })),
  
  nextRound: () => set(produce(state => {
    state.gameState.round += 1;
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.phase = 'PLAYING';
    state.gameState.winner = null;
    
    // 重置玩家位置和状态
    Object.keys(state.players).forEach(playerId => {
      const player = state.players[playerId];
      player.health = player.maxHealth;
      player.position = playerId === 'player1' ? { x: 200, y: 400 } : { x: 550, y: 400 };
      player.velocity = { x: 0, y: 0 };
      player.state = 'IDLE';
      player.isDefending = false;
      player.isGrounded = false;
      player.attackCooldown = 0;
    });
    
    state.effects = [];
  })),
  
  // 玩家操作
  updatePlayerPosition: (playerId:import { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
  winner: null,
};

// 游戏Store接口
interface GameStore {
  // 状态
  gameState: GameState;
  players: Record<string, MechaData>;
  effects: Effect[];
  
  // 游戏流程控制
  startGame: () => void;
  pauseGame: () => void;
  resumeGame: () => void;
  endGame: (winner: string | null) => void;
  resetGame: () => void;
  nextRound: () => void;
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => void;
  updatePlayerState: (playerId: string, state: MechaData['state']) => void;
  updatePlayerFacing: (playerId: string, facingRight: boolean) => void;
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => void;
  updatePlayerDefending: (playerId: string, isDefending: boolean) => void;
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => void;
  dealDamage: (targetId: string, damage: number, attackerId?: string) => void;
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => void;
  setAnimationFrame: (playerId: string, frame: number) => void;
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => string;
  updateEffects: (deltaTime: number) => void;
  removeEffect: (effectId: string) => void;
  clearEffects: () => void;
  
  // 游戏时间
  tick: (deltaTime: number) => void;
}

// 创建Game Store
export const useGameStore = create<GameStore>((set, get) => ({
  // 初始状态
  gameState: initialGameState,
  players: {
    player1: createInitialMecha('player1', 'BLUE', 200),
    player2: createInitialMecha('player2', 'RED', 550),
  },
  effects: [],
  
  // 游戏流程控制
  startGame: () => set(produce(state => {
    state.gameState.phase = 'PLAYING';
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.winner = null;
  })),
  
  pauseGame: () => set(produce(state => {
    if (state.gameState.phase === 'PLAYING') {
      state.gameState.phase = 'PAUSED';
    }
  })),
  
  resumeGame: () => set(produce(state => {
    if (state.gameState.phase === 'PAUSED') {
      state.gameState.phase = 'PLAYING';
    }
  })),
  
  endGame: (winner: string | null) => set(produce(state => {
    state.gameState.phase = 'RESULT';
    state.gameState.winner = winner;
    if (winner && state.players[winner]) {
      state.players[winner].score += 1;
    }
  })),
  
  resetGame: () => set(produce(state => {
    state.gameState = initialGameState;
    state.players = {
      player1: createInitialMecha('player1', 'BLUE', 200),
      player2: createInitialMecha('player2', 'RED', 550),
    };
    state.effects = [];
  })),
  
  nextRound: () => set(produce(state => {
    state.gameState.round += 1;
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.phase = 'PLAYING';
    state.gameState.winner = null;
    
    // 重置玩家位置和状态
    Object.keys(state.players).forEach(playerId => {
      const player = state.players[playerId];
      player.health = player.maxHealth;
      player.position = playerId === 'player1' ? { x: 200, y: 400 } : { x: 550, y: 400 };
      player.velocity = { x: 0, y: 0 };
      player.state = 'IDLE';
      player.isDefending = false;
      player.isGrounded = false;
      player.attackCooldown = 0;
    });
    
    state.effects = [];
  })),
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2Dimport { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
  winner: null,
};

// 游戏Store接口
interface GameStore {
  // 状态
  gameState: GameState;
  players: Record<string, MechaData>;
  effects: Effect[];
  
  // 游戏流程控制
  startGame: () => void;
  pauseGame: () => void;
  resumeGame: () => void;
  endGame: (winner: string | null) => void;
  resetGame: () => void;
  nextRound: () => void;
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => void;
  updatePlayerState: (playerId: string, state: MechaData['state']) => void;
  updatePlayerFacing: (playerId: string, facingRight: boolean) => void;
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => void;
  updatePlayerDefending: (playerId: string, isDefending: boolean) => void;
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => void;
  dealDamage: (targetId: string, damage: number, attackerId?: string) => void;
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => void;
  setAnimationFrame: (playerId: string, frame: number) => void;
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => string;
  updateEffects: (deltaTime: number) => void;
  removeEffect: (effectId: string) => void;
  clearEffects: () => void;
  
  // 游戏时间
  tick: (deltaTime: number) => void;
}

// 创建Game Store
export const useGameStore = create<GameStore>((set, get) => ({
  // 初始状态
  gameState: initialGameState,
  players: {
    player1: createInitialMecha('player1', 'BLUE', 200),
    player2: createInitialMecha('player2', 'RED', 550),
  },
  effects: [],
  
  // 游戏流程控制
  startGame: () => set(produce(state => {
    state.gameState.phase = 'PLAYING';
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.winner = null;
  })),
  
  pauseGame: () => set(produce(state => {
    if (state.gameState.phase === 'PLAYING') {
      state.gameState.phase = 'PAUSED';
    }
  })),
  
  resumeGame: () => set(produce(state => {
    if (state.gameState.phase === 'PAUSED') {
      state.gameState.phase = 'PLAYING';
    }
  })),
  
  endGame: (winner: string | null) => set(produce(state => {
    state.gameState.phase = 'RESULT';
    state.gameState.winner = winner;
    if (winner && state.players[winner]) {
      state.players[winner].score += 1;
    }
  })),
  
  resetGame: () => set(produce(state => {
    state.gameState = initialGameState;
    state.players = {
      player1: createInitialMecha('player1', 'BLUE', 200),
      player2: createInitialMecha('player2', 'RED', 550),
    };
    state.effects = [];
  })),
  
  nextRound: () => set(produce(state => {
    state.gameState.round += 1;
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.phase = 'PLAYING';
    state.gameState.winner = null;
    
    // 重置玩家位置和状态
    Object.keys(state.players).forEach(playerId => {
      const player = state.players[playerId];
      player.health = player.maxHealth;
      player.position = playerId === 'player1' ? { x: 200, y: 400 } : { x: 550, y: 400 };
      player.velocity = { x: 0, y: 0 };
      player.state = 'IDLE';
      player.isDefending = false;
      player.isGrounded = false;
      player.attackCooldown = 0;
    });
    
    state.effects = [];
  })),
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D)import { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
  winner: null,
};

// 游戏Store接口
interface GameStore {
  // 状态
  gameState: GameState;
  players: Record<string, MechaData>;
  effects: Effect[];
  
  // 游戏流程控制
  startGame: () => void;
  pauseGame: () => void;
  resumeGame: () => void;
  endGame: (winner: string | null) => void;
  resetGame: () => void;
  nextRound: () => void;
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => void;
  updatePlayerState: (playerId: string, state: MechaData['state']) => void;
  updatePlayerFacing: (playerId: string, facingRight: boolean) => void;
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => void;
  updatePlayerDefending: (playerId: string, isDefending: boolean) => void;
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => void;
  dealDamage: (targetId: string, damage: number, attackerId?: string) => void;
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => void;
  setAnimationFrame: (playerId: string, frame: number) => void;
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => string;
  updateEffects: (deltaTime: number) => void;
  removeEffect: (effectId: string) => void;
  clearEffects: () => void;
  
  // 游戏时间
  tick: (deltaTime: number) => void;
}

// 创建Game Store
export const useGameStore = create<GameStore>((set, get) => ({
  // 初始状态
  gameState: initialGameState,
  players: {
    player1: createInitialMecha('player1', 'BLUE', 200),
    player2: createInitialMecha('player2', 'RED', 550),
  },
  effects: [],
  
  // 游戏流程控制
  startGame: () => set(produce(state => {
    state.gameState.phase = 'PLAYING';
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.winner = null;
  })),
  
  pauseGame: () => set(produce(state => {
    if (state.gameState.phase === 'PLAYING') {
      state.gameState.phase = 'PAUSED';
    }
  })),
  
  resumeGame: () => set(produce(state => {
    if (state.gameState.phase === 'PAUSED') {
      state.gameState.phase = 'PLAYING';
    }
  })),
  
  endGame: (winner: string | null) => set(produce(state => {
    state.gameState.phase = 'RESULT';
    state.gameState.winner = winner;
    if (winner && state.players[winner]) {
      state.players[winner].score += 1;
    }
  })),
  
  resetGame: () => set(produce(state => {
    state.gameState = initialGameState;
    state.players = {
      player1: createInitialMecha('player1', 'BLUE', 200),
      player2: createInitialMecha('player2', 'RED', 550),
    };
    state.effects = [];
  })),
  
  nextRound: () => set(produce(state => {
    state.gameState.round += 1;
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.phase = 'PLAYING';
    state.gameState.winner = null;
    
    // 重置玩家位置和状态
    Object.keys(state.players).forEach(playerId => {
      const player = state.players[playerId];
      player.health = player.maxHealth;
      player.position = playerId === 'player1' ? { x: 200, y: 400 } : { x: 550, y: 400 };
      player.velocity = { x: 0, y: 0 };
      player.state = 'IDLE';
      player.isDefending = false;
      player.isGrounded = false;
      player.attackCooldown = 0;
    });
    
    state.effects = [];
  })),
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => set(produce(state => {
import { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
  winner: null,
};

// 游戏Store接口
interface GameStore {
  // 状态
  gameState: GameState;
  players: Record<string, MechaData>;
  effects: Effect[];
  
  // 游戏流程控制
  startGame: () => void;
  pauseGame: () => void;
  resumeGame: () => void;
  endGame: (winner: string | null) => void;
  resetGame: () => void;
  nextRound: () => void;
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => void;
  updatePlayerState: (playerId: string, state: MechaData['state']) => void;
  updatePlayerFacing: (playerId: string, facingRight: boolean) => void;
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => void;
  updatePlayerDefending: (playerId: string, isDefending: boolean) => void;
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => void;
  dealDamage: (targetId: string, damage: number, attackerId?: string) => void;
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => void;
  setAnimationFrame: (playerId: string, frame: number) => void;
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => string;
  updateEffects: (deltaTime: number) => void;
  removeEffect: (effectId: string) => void;
  clearEffects: () => void;
  
  // 游戏时间
  tick: (deltaTime: number) => void;
}

// 创建Game Store
export const useGameStore = create<GameStore>((set, get) => ({
  // 初始状态
  gameState: initialGameState,
  players: {
    player1: createInitialMecha('player1', 'BLUE', 200),
    player2: createInitialMecha('player2', 'RED', 550),
  },
  effects: [],
  
  // 游戏流程控制
  startGame: () => set(produce(state => {
    state.gameState.phase = 'PLAYING';
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.winner = null;
  })),
  
  pauseGame: () => set(produce(state => {
    if (state.gameState.phase === 'PLAYING') {
      state.gameState.phase = 'PAUSED';
    }
  })),
  
  resumeGame: () => set(produce(state => {
    if (state.gameState.phase === 'PAUSED') {
      state.gameState.phase = 'PLAYING';
    }
  })),
  
  endGame: (winner: string | null) => set(produce(state => {
    state.gameState.phase = 'RESULT';
    state.gameState.winner = winner;
    if (winner && state.players[winner]) {
      state.players[winner].score += 1;
    }
  })),
  
  resetGame: () => set(produce(state => {
    state.gameState = initialGameState;
    state.players = {
      player1: createInitialMecha('player1', 'BLUE', 200),
      player2: createInitialMecha('player2', 'RED', 550),
    };
    state.effects = [];
  })),
  
  nextRound: () => set(produce(state => {
    state.gameState.round += 1;
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.phase = 'PLAYING';
    state.gameState.winner = null;
    
    // 重置玩家位置和状态
    Object.keys(state.players).forEach(playerId => {
      const player = state.players[playerId];
      player.health = player.maxHealth;
      player.position = playerId === 'player1' ? { x: 200, y: 400 } : { x: 550, y: 400 };
      player.velocity = { x: 0, y: 0 };
      player.state = 'IDLE';
      player.isDefending = false;
      player.isGrounded = false;
      player.attackCooldown = 0;
    });
    
    state.effects = [];
  })),
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => set(produce(state => {
    state.players[playerId].position =import { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
  winner: null,
};

// 游戏Store接口
interface GameStore {
  // 状态
  gameState: GameState;
  players: Record<string, MechaData>;
  effects: Effect[];
  
  // 游戏流程控制
  startGame: () => void;
  pauseGame: () => void;
  resumeGame: () => void;
  endGame: (winner: string | null) => void;
  resetGame: () => void;
  nextRound: () => void;
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => void;
  updatePlayerState: (playerId: string, state: MechaData['state']) => void;
  updatePlayerFacing: (playerId: string, facingRight: boolean) => void;
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => void;
  updatePlayerDefending: (playerId: string, isDefending: boolean) => void;
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => void;
  dealDamage: (targetId: string, damage: number, attackerId?: string) => void;
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => void;
  setAnimationFrame: (playerId: string, frame: number) => void;
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => string;
  updateEffects: (deltaTime: number) => void;
  removeEffect: (effectId: string) => void;
  clearEffects: () => void;
  
  // 游戏时间
  tick: (deltaTime: number) => void;
}

// 创建Game Store
export const useGameStore = create<GameStore>((set, get) => ({
  // 初始状态
  gameState: initialGameState,
  players: {
    player1: createInitialMecha('player1', 'BLUE', 200),
    player2: createInitialMecha('player2', 'RED', 550),
  },
  effects: [],
  
  // 游戏流程控制
  startGame: () => set(produce(state => {
    state.gameState.phase = 'PLAYING';
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.winner = null;
  })),
  
  pauseGame: () => set(produce(state => {
    if (state.gameState.phase === 'PLAYING') {
      state.gameState.phase = 'PAUSED';
    }
  })),
  
  resumeGame: () => set(produce(state => {
    if (state.gameState.phase === 'PAUSED') {
      state.gameState.phase = 'PLAYING';
    }
  })),
  
  endGame: (winner: string | null) => set(produce(state => {
    state.gameState.phase = 'RESULT';
    state.gameState.winner = winner;
    if (winner && state.players[winner]) {
      state.players[winner].score += 1;
    }
  })),
  
  resetGame: () => set(produce(state => {
    state.gameState = initialGameState;
    state.players = {
      player1: createInitialMecha('player1', 'BLUE', 200),
      player2: createInitialMecha('player2', 'RED', 550),
    };
    state.effects = [];
  })),
  
  nextRound: () => set(produce(state => {
    state.gameState.round += 1;
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.phase = 'PLAYING';
    state.gameState.winner = null;
    
    // 重置玩家位置和状态
    Object.keys(state.players).forEach(playerId => {
      const player = state.players[playerId];
      player.health = player.maxHealth;
      player.position = playerId === 'player1' ? { x: 200, y: 400 } : { x: 550, y: 400 };
      player.velocity = { x: 0, y: 0 };
      player.state = 'IDLE';
      player.isDefending = false;
      player.isGrounded = false;
      player.attackCooldown = 0;
    });
    
    state.effects = [];
  })),
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => set(produce(state => {
    state.players[playerId].position = position;
    state.players[playerimport { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
  winner: null,
};

// 游戏Store接口
interface GameStore {
  // 状态
  gameState: GameState;
  players: Record<string, MechaData>;
  effects: Effect[];
  
  // 游戏流程控制
  startGame: () => void;
  pauseGame: () => void;
  resumeGame: () => void;
  endGame: (winner: string | null) => void;
  resetGame: () => void;
  nextRound: () => void;
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => void;
  updatePlayerState: (playerId: string, state: MechaData['state']) => void;
  updatePlayerFacing: (playerId: string, facingRight: boolean) => void;
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => void;
  updatePlayerDefending: (playerId: string, isDefending: boolean) => void;
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => void;
  dealDamage: (targetId: string, damage: number, attackerId?: string) => void;
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => void;
  setAnimationFrame: (playerId: string, frame: number) => void;
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => string;
  updateEffects: (deltaTime: number) => void;
  removeEffect: (effectId: string) => void;
  clearEffects: () => void;
  
  // 游戏时间
  tick: (deltaTime: number) => void;
}

// 创建Game Store
export const useGameStore = create<GameStore>((set, get) => ({
  // 初始状态
  gameState: initialGameState,
  players: {
    player1: createInitialMecha('player1', 'BLUE', 200),
    player2: createInitialMecha('player2', 'RED', 550),
  },
  effects: [],
  
  // 游戏流程控制
  startGame: () => set(produce(state => {
    state.gameState.phase = 'PLAYING';
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.winner = null;
  })),
  
  pauseGame: () => set(produce(state => {
    if (state.gameState.phase === 'PLAYING') {
      state.gameState.phase = 'PAUSED';
    }
  })),
  
  resumeGame: () => set(produce(state => {
    if (state.gameState.phase === 'PAUSED') {
      state.gameState.phase = 'PLAYING';
    }
  })),
  
  endGame: (winner: string | null) => set(produce(state => {
    state.gameState.phase = 'RESULT';
    state.gameState.winner = winner;
    if (winner && state.players[winner]) {
      state.players[winner].score += 1;
    }
  })),
  
  resetGame: () => set(produce(state => {
    state.gameState = initialGameState;
    state.players = {
      player1: createInitialMecha('player1', 'BLUE', 200),
      player2: createInitialMecha('player2', 'RED', 550),
    };
    state.effects = [];
  })),
  
  nextRound: () => set(produce(state => {
    state.gameState.round += 1;
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.phase = 'PLAYING';
    state.gameState.winner = null;
    
    // 重置玩家位置和状态
    Object.keys(state.players).forEach(playerId => {
      const player = state.players[playerId];
      player.health = player.maxHealth;
      player.position = playerId === 'player1' ? { x: 200, y: 400 } : { x: 550, y: 400 };
      player.velocity = { x: 0, y: 0 };
      player.state = 'IDLE';
      player.isDefending = false;
      player.isGrounded = false;
      player.attackCooldown = 0;
    });
    
    state.effects = [];
  })),
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => set(produce(state => {
    state.players[playerId].position = position;
    state.players[playerId].velocity = velocity;
import { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
  winner: null,
};

// 游戏Store接口
interface GameStore {
  // 状态
  gameState: GameState;
  players: Record<string, MechaData>;
  effects: Effect[];
  
  // 游戏流程控制
  startGame: () => void;
  pauseGame: () => void;
  resumeGame: () => void;
  endGame: (winner: string | null) => void;
  resetGame: () => void;
  nextRound: () => void;
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => void;
  updatePlayerState: (playerId: string, state: MechaData['state']) => void;
  updatePlayerFacing: (playerId: string, facingRight: boolean) => void;
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => void;
  updatePlayerDefending: (playerId: string, isDefending: boolean) => void;
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => void;
  dealDamage: (targetId: string, damage: number, attackerId?: string) => void;
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => void;
  setAnimationFrame: (playerId: string, frame: number) => void;
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => string;
  updateEffects: (deltaTime: number) => void;
  removeEffect: (effectId: string) => void;
  clearEffects: () => void;
  
  // 游戏时间
  tick: (deltaTime: number) => void;
}

// 创建Game Store
export const useGameStore = create<GameStore>((set, get) => ({
  // 初始状态
  gameState: initialGameState,
  players: {
    player1: createInitialMecha('player1', 'BLUE', 200),
    player2: createInitialMecha('player2', 'RED', 550),
  },
  effects: [],
  
  // 游戏流程控制
  startGame: () => set(produce(state => {
    state.gameState.phase = 'PLAYING';
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.winner = null;
  })),
  
  pauseGame: () => set(produce(state => {
    if (state.gameState.phase === 'PLAYING') {
      state.gameState.phase = 'PAUSED';
    }
  })),
  
  resumeGame: () => set(produce(state => {
    if (state.gameState.phase === 'PAUSED') {
      state.gameState.phase = 'PLAYING';
    }
  })),
  
  endGame: (winner: string | null) => set(produce(state => {
    state.gameState.phase = 'RESULT';
    state.gameState.winner = winner;
    if (winner && state.players[winner]) {
      state.players[winner].score += 1;
    }
  })),
  
  resetGame: () => set(produce(state => {
    state.gameState = initialGameState;
    state.players = {
      player1: createInitialMecha('player1', 'BLUE', 200),
      player2: createInitialMecha('player2', 'RED', 550),
    };
    state.effects = [];
  })),
  
  nextRound: () => set(produce(state => {
    state.gameState.round += 1;
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.phase = 'PLAYING';
    state.gameState.winner = null;
    
    // 重置玩家位置和状态
    Object.keys(state.players).forEach(playerId => {
      const player = state.players[playerId];
      player.health = player.maxHealth;
      player.position = playerId === 'player1' ? { x: 200, y: 400 } : { x: 550, y: 400 };
      player.velocity = { x: 0, y: 0 };
      player.state = 'IDLE';
      player.isDefending = false;
      player.isGrounded = false;
      player.attackCooldown = 0;
    });
    
    state.effects = [];
  })),
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => set(produce(state => {
    state.players[playerId].position = position;
    state.players[playerId].velocity = velocity;
  })),
  
import { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
  winner: null,
};

// 游戏Store接口
interface GameStore {
  // 状态
  gameState: GameState;
  players: Record<string, MechaData>;
  effects: Effect[];
  
  // 游戏流程控制
  startGame: () => void;
  pauseGame: () => void;
  resumeGame: () => void;
  endGame: (winner: string | null) => void;
  resetGame: () => void;
  nextRound: () => void;
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => void;
  updatePlayerState: (playerId: string, state: MechaData['state']) => void;
  updatePlayerFacing: (playerId: string, facingRight: boolean) => void;
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => void;
  updatePlayerDefending: (playerId: string, isDefending: boolean) => void;
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => void;
  dealDamage: (targetId: string, damage: number, attackerId?: string) => void;
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => void;
  setAnimationFrame: (playerId: string, frame: number) => void;
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => string;
  updateEffects: (deltaTime: number) => void;
  removeEffect: (effectId: string) => void;
  clearEffects: () => void;
  
  // 游戏时间
  tick: (deltaTime: number) => void;
}

// 创建Game Store
export const useGameStore = create<GameStore>((set, get) => ({
  // 初始状态
  gameState: initialGameState,
  players: {
    player1: createInitialMecha('player1', 'BLUE', 200),
    player2: createInitialMecha('player2', 'RED', 550),
  },
  effects: [],
  
  // 游戏流程控制
  startGame: () => set(produce(state => {
    state.gameState.phase = 'PLAYING';
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.winner = null;
  })),
  
  pauseGame: () => set(produce(state => {
    if (state.gameState.phase === 'PLAYING') {
      state.gameState.phase = 'PAUSED';
    }
  })),
  
  resumeGame: () => set(produce(state => {
    if (state.gameState.phase === 'PAUSED') {
      state.gameState.phase = 'PLAYING';
    }
  })),
  
  endGame: (winner: string | null) => set(produce(state => {
    state.gameState.phase = 'RESULT';
    state.gameState.winner = winner;
    if (winner && state.players[winner]) {
      state.players[winner].score += 1;
    }
  })),
  
  resetGame: () => set(produce(state => {
    state.gameState = initialGameState;
    state.players = {
      player1: createInitialMecha('player1', 'BLUE', 200),
      player2: createInitialMecha('player2', 'RED', 550),
    };
    state.effects = [];
  })),
  
  nextRound: () => set(produce(state => {
    state.gameState.round += 1;
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.phase = 'PLAYING';
    state.gameState.winner = null;
    
    // 重置玩家位置和状态
    Object.keys(state.players).forEach(playerId => {
      const player = state.players[playerId];
      player.health = player.maxHealth;
      player.position = playerId === 'player1' ? { x: 200, y: 400 } : { x: 550, y: 400 };
      player.velocity = { x: 0, y: 0 };
      player.state = 'IDLE';
      player.isDefending = false;
      player.isGrounded = false;
      player.attackCooldown = 0;
    });
    
    state.effects = [];
  })),
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => set(produce(state => {
    state.players[playerId].position = position;
    state.players[playerId].velocity = velocity;
  })),
  
  updatePlayerState: (playerIdimport { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
  winner: null,
};

// 游戏Store接口
interface GameStore {
  // 状态
  gameState: GameState;
  players: Record<string, MechaData>;
  effects: Effect[];
  
  // 游戏流程控制
  startGame: () => void;
  pauseGame: () => void;
  resumeGame: () => void;
  endGame: (winner: string | null) => void;
  resetGame: () => void;
  nextRound: () => void;
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => void;
  updatePlayerState: (playerId: string, state: MechaData['state']) => void;
  updatePlayerFacing: (playerId: string, facingRight: boolean) => void;
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => void;
  updatePlayerDefending: (playerId: string, isDefending: boolean) => void;
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => void;
  dealDamage: (targetId: string, damage: number, attackerId?: string) => void;
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => void;
  setAnimationFrame: (playerId: string, frame: number) => void;
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => string;
  updateEffects: (deltaTime: number) => void;
  removeEffect: (effectId: string) => void;
  clearEffects: () => void;
  
  // 游戏时间
  tick: (deltaTime: number) => void;
}

// 创建Game Store
export const useGameStore = create<GameStore>((set, get) => ({
  // 初始状态
  gameState: initialGameState,
  players: {
    player1: createInitialMecha('player1', 'BLUE', 200),
    player2: createInitialMecha('player2', 'RED', 550),
  },
  effects: [],
  
  // 游戏流程控制
  startGame: () => set(produce(state => {
    state.gameState.phase = 'PLAYING';
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.winner = null;
  })),
  
  pauseGame: () => set(produce(state => {
    if (state.gameState.phase === 'PLAYING') {
      state.gameState.phase = 'PAUSED';
    }
  })),
  
  resumeGame: () => set(produce(state => {
    if (state.gameState.phase === 'PAUSED') {
      state.gameState.phase = 'PLAYING';
    }
  })),
  
  endGame: (winner: string | null) => set(produce(state => {
    state.gameState.phase = 'RESULT';
    state.gameState.winner = winner;
    if (winner && state.players[winner]) {
      state.players[winner].score += 1;
    }
  })),
  
  resetGame: () => set(produce(state => {
    state.gameState = initialGameState;
    state.players = {
      player1: createInitialMecha('player1', 'BLUE', 200),
      player2: createInitialMecha('player2', 'RED', 550),
    };
    state.effects = [];
  })),
  
  nextRound: () => set(produce(state => {
    state.gameState.round += 1;
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.phase = 'PLAYING';
    state.gameState.winner = null;
    
    // 重置玩家位置和状态
    Object.keys(state.players).forEach(playerId => {
      const player = state.players[playerId];
      player.health = player.maxHealth;
      player.position = playerId === 'player1' ? { x: 200, y: 400 } : { x: 550, y: 400 };
      player.velocity = { x: 0, y: 0 };
      player.state = 'IDLE';
      player.isDefending = false;
      player.isGrounded = false;
      player.attackCooldown = 0;
    });
    
    state.effects = [];
  })),
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => set(produce(state => {
    state.players[playerId].position = position;
    state.players[playerId].velocity = velocity;
  })),
  
  updatePlayerState: (playerId: string, mechaState:import { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
  winner: null,
};

// 游戏Store接口
interface GameStore {
  // 状态
  gameState: GameState;
  players: Record<string, MechaData>;
  effects: Effect[];
  
  // 游戏流程控制
  startGame: () => void;
  pauseGame: () => void;
  resumeGame: () => void;
  endGame: (winner: string | null) => void;
  resetGame: () => void;
  nextRound: () => void;
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => void;
  updatePlayerState: (playerId: string, state: MechaData['state']) => void;
  updatePlayerFacing: (playerId: string, facingRight: boolean) => void;
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => void;
  updatePlayerDefending: (playerId: string, isDefending: boolean) => void;
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => void;
  dealDamage: (targetId: string, damage: number, attackerId?: string) => void;
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => void;
  setAnimationFrame: (playerId: string, frame: number) => void;
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => string;
  updateEffects: (deltaTime: number) => void;
  removeEffect: (effectId: string) => void;
  clearEffects: () => void;
  
  // 游戏时间
  tick: (deltaTime: number) => void;
}

// 创建Game Store
export const useGameStore = create<GameStore>((set, get) => ({
  // 初始状态
  gameState: initialGameState,
  players: {
    player1: createInitialMecha('player1', 'BLUE', 200),
    player2: createInitialMecha('player2', 'RED', 550),
  },
  effects: [],
  
  // 游戏流程控制
  startGame: () => set(produce(state => {
    state.gameState.phase = 'PLAYING';
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.winner = null;
  })),
  
  pauseGame: () => set(produce(state => {
    if (state.gameState.phase === 'PLAYING') {
      state.gameState.phase = 'PAUSED';
    }
  })),
  
  resumeGame: () => set(produce(state => {
    if (state.gameState.phase === 'PAUSED') {
      state.gameState.phase = 'PLAYING';
    }
  })),
  
  endGame: (winner: string | null) => set(produce(state => {
    state.gameState.phase = 'RESULT';
    state.gameState.winner = winner;
    if (winner && state.players[winner]) {
      state.players[winner].score += 1;
    }
  })),
  
  resetGame: () => set(produce(state => {
    state.gameState = initialGameState;
    state.players = {
      player1: createInitialMecha('player1', 'BLUE', 200),
      player2: createInitialMecha('player2', 'RED', 550),
    };
    state.effects = [];
  })),
  
  nextRound: () => set(produce(state => {
    state.gameState.round += 1;
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.phase = 'PLAYING';
    state.gameState.winner = null;
    
    // 重置玩家位置和状态
    Object.keys(state.players).forEach(playerId => {
      const player = state.players[playerId];
      player.health = player.maxHealth;
      player.position = playerId === 'player1' ? { x: 200, y: 400 } : { x: 550, y: 400 };
      player.velocity = { x: 0, y: 0 };
      player.state = 'IDLE';
      player.isDefending = false;
      player.isGrounded = false;
      player.attackCooldown = 0;
    });
    
    state.effects = [];
  })),
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => set(produce(state => {
    state.players[playerId].position = position;
    state.players[playerId].velocity = velocity;
  })),
  
  updatePlayerState: (playerId: string, mechaState: MechaData['state']) =>import { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
  winner: null,
};

// 游戏Store接口
interface GameStore {
  // 状态
  gameState: GameState;
  players: Record<string, MechaData>;
  effects: Effect[];
  
  // 游戏流程控制
  startGame: () => void;
  pauseGame: () => void;
  resumeGame: () => void;
  endGame: (winner: string | null) => void;
  resetGame: () => void;
  nextRound: () => void;
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => void;
  updatePlayerState: (playerId: string, state: MechaData['state']) => void;
  updatePlayerFacing: (playerId: string, facingRight: boolean) => void;
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => void;
  updatePlayerDefending: (playerId: string, isDefending: boolean) => void;
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => void;
  dealDamage: (targetId: string, damage: number, attackerId?: string) => void;
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => void;
  setAnimationFrame: (playerId: string, frame: number) => void;
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => string;
  updateEffects: (deltaTime: number) => void;
  removeEffect: (effectId: string) => void;
  clearEffects: () => void;
  
  // 游戏时间
  tick: (deltaTime: number) => void;
}

// 创建Game Store
export const useGameStore = create<GameStore>((set, get) => ({
  // 初始状态
  gameState: initialGameState,
  players: {
    player1: createInitialMecha('player1', 'BLUE', 200),
    player2: createInitialMecha('player2', 'RED', 550),
  },
  effects: [],
  
  // 游戏流程控制
  startGame: () => set(produce(state => {
    state.gameState.phase = 'PLAYING';
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.winner = null;
  })),
  
  pauseGame: () => set(produce(state => {
    if (state.gameState.phase === 'PLAYING') {
      state.gameState.phase = 'PAUSED';
    }
  })),
  
  resumeGame: () => set(produce(state => {
    if (state.gameState.phase === 'PAUSED') {
      state.gameState.phase = 'PLAYING';
    }
  })),
  
  endGame: (winner: string | null) => set(produce(state => {
    state.gameState.phase = 'RESULT';
    state.gameState.winner = winner;
    if (winner && state.players[winner]) {
      state.players[winner].score += 1;
    }
  })),
  
  resetGame: () => set(produce(state => {
    state.gameState = initialGameState;
    state.players = {
      player1: createInitialMecha('player1', 'BLUE', 200),
      player2: createInitialMecha('player2', 'RED', 550),
    };
    state.effects = [];
  })),
  
  nextRound: () => set(produce(state => {
    state.gameState.round += 1;
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.phase = 'PLAYING';
    state.gameState.winner = null;
    
    // 重置玩家位置和状态
    Object.keys(state.players).forEach(playerId => {
      const player = state.players[playerId];
      player.health = player.maxHealth;
      player.position = playerId === 'player1' ? { x: 200, y: 400 } : { x: 550, y: 400 };
      player.velocity = { x: 0, y: 0 };
      player.state = 'IDLE';
      player.isDefending = false;
      player.isGrounded = false;
      player.attackCooldown = 0;
    });
    
    state.effects = [];
  })),
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => set(produce(state => {
    state.players[playerId].position = position;
    state.players[playerId].velocity = velocity;
  })),
  
  updatePlayerState: (playerId: string, mechaState: MechaData['state']) => set(produce(state => {
import { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
  winner: null,
};

// 游戏Store接口
interface GameStore {
  // 状态
  gameState: GameState;
  players: Record<string, MechaData>;
  effects: Effect[];
  
  // 游戏流程控制
  startGame: () => void;
  pauseGame: () => void;
  resumeGame: () => void;
  endGame: (winner: string | null) => void;
  resetGame: () => void;
  nextRound: () => void;
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => void;
  updatePlayerState: (playerId: string, state: MechaData['state']) => void;
  updatePlayerFacing: (playerId: string, facingRight: boolean) => void;
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => void;
  updatePlayerDefending: (playerId: string, isDefending: boolean) => void;
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => void;
  dealDamage: (targetId: string, damage: number, attackerId?: string) => void;
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => void;
  setAnimationFrame: (playerId: string, frame: number) => void;
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => string;
  updateEffects: (deltaTime: number) => void;
  removeEffect: (effectId: string) => void;
  clearEffects: () => void;
  
  // 游戏时间
  tick: (deltaTime: number) => void;
}

// 创建Game Store
export const useGameStore = create<GameStore>((set, get) => ({
  // 初始状态
  gameState: initialGameState,
  players: {
    player1: createInitialMecha('player1', 'BLUE', 200),
    player2: createInitialMecha('player2', 'RED', 550),
  },
  effects: [],
  
  // 游戏流程控制
  startGame: () => set(produce(state => {
    state.gameState.phase = 'PLAYING';
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.winner = null;
  })),
  
  pauseGame: () => set(produce(state => {
    if (state.gameState.phase === 'PLAYING') {
      state.gameState.phase = 'PAUSED';
    }
  })),
  
  resumeGame: () => set(produce(state => {
    if (state.gameState.phase === 'PAUSED') {
      state.gameState.phase = 'PLAYING';
    }
  })),
  
  endGame: (winner: string | null) => set(produce(state => {
    state.gameState.phase = 'RESULT';
    state.gameState.winner = winner;
    if (winner && state.players[winner]) {
      state.players[winner].score += 1;
    }
  })),
  
  resetGame: () => set(produce(state => {
    state.gameState = initialGameState;
    state.players = {
      player1: createInitialMecha('player1', 'BLUE', 200),
      player2: createInitialMecha('player2', 'RED', 550),
    };
    state.effects = [];
  })),
  
  nextRound: () => set(produce(state => {
    state.gameState.round += 1;
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.phase = 'PLAYING';
    state.gameState.winner = null;
    
    // 重置玩家位置和状态
    Object.keys(state.players).forEach(playerId => {
      const player = state.players[playerId];
      player.health = player.maxHealth;
      player.position = playerId === 'player1' ? { x: 200, y: 400 } : { x: 550, y: 400 };
      player.velocity = { x: 0, y: 0 };
      player.state = 'IDLE';
      player.isDefending = false;
      player.isGrounded = false;
      player.attackCooldown = 0;
    });
    
    state.effects = [];
  })),
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => set(produce(state => {
    state.players[playerId].position = position;
    state.players[playerId].velocity = velocity;
  })),
  
  updatePlayerState: (playerId: string, mechaState: MechaData['state']) => set(produce(state => {
    state.players[playerId].stateimport { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
  winner: null,
};

// 游戏Store接口
interface GameStore {
  // 状态
  gameState: GameState;
  players: Record<string, MechaData>;
  effects: Effect[];
  
  // 游戏流程控制
  startGame: () => void;
  pauseGame: () => void;
  resumeGame: () => void;
  endGame: (winner: string | null) => void;
  resetGame: () => void;
  nextRound: () => void;
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => void;
  updatePlayerState: (playerId: string, state: MechaData['state']) => void;
  updatePlayerFacing: (playerId: string, facingRight: boolean) => void;
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => void;
  updatePlayerDefending: (playerId: string, isDefending: boolean) => void;
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => void;
  dealDamage: (targetId: string, damage: number, attackerId?: string) => void;
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => void;
  setAnimationFrame: (playerId: string, frame: number) => void;
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => string;
  updateEffects: (deltaTime: number) => void;
  removeEffect: (effectId: string) => void;
  clearEffects: () => void;
  
  // 游戏时间
  tick: (deltaTime: number) => void;
}

// 创建Game Store
export const useGameStore = create<GameStore>((set, get) => ({
  // 初始状态
  gameState: initialGameState,
  players: {
    player1: createInitialMecha('player1', 'BLUE', 200),
    player2: createInitialMecha('player2', 'RED', 550),
  },
  effects: [],
  
  // 游戏流程控制
  startGame: () => set(produce(state => {
    state.gameState.phase = 'PLAYING';
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.winner = null;
  })),
  
  pauseGame: () => set(produce(state => {
    if (state.gameState.phase === 'PLAYING') {
      state.gameState.phase = 'PAUSED';
    }
  })),
  
  resumeGame: () => set(produce(state => {
    if (state.gameState.phase === 'PAUSED') {
      state.gameState.phase = 'PLAYING';
    }
  })),
  
  endGame: (winner: string | null) => set(produce(state => {
    state.gameState.phase = 'RESULT';
    state.gameState.winner = winner;
    if (winner && state.players[winner]) {
      state.players[winner].score += 1;
    }
  })),
  
  resetGame: () => set(produce(state => {
    state.gameState = initialGameState;
    state.players = {
      player1: createInitialMecha('player1', 'BLUE', 200),
      player2: createInitialMecha('player2', 'RED', 550),
    };
    state.effects = [];
  })),
  
  nextRound: () => set(produce(state => {
    state.gameState.round += 1;
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.phase = 'PLAYING';
    state.gameState.winner = null;
    
    // 重置玩家位置和状态
    Object.keys(state.players).forEach(playerId => {
      const player = state.players[playerId];
      player.health = player.maxHealth;
      player.position = playerId === 'player1' ? { x: 200, y: 400 } : { x: 550, y: 400 };
      player.velocity = { x: 0, y: 0 };
      player.state = 'IDLE';
      player.isDefending = false;
      player.isGrounded = false;
      player.attackCooldown = 0;
    });
    
    state.effects = [];
  })),
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => set(produce(state => {
    state.players[playerId].position = position;
    state.players[playerId].velocity = velocity;
  })),
  
  updatePlayerState: (playerId: string, mechaState: MechaData['state']) => set(produce(state => {
    state.players[playerId].state = mechaState;
  }import { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
  winner: null,
};

// 游戏Store接口
interface GameStore {
  // 状态
  gameState: GameState;
  players: Record<string, MechaData>;
  effects: Effect[];
  
  // 游戏流程控制
  startGame: () => void;
  pauseGame: () => void;
  resumeGame: () => void;
  endGame: (winner: string | null) => void;
  resetGame: () => void;
  nextRound: () => void;
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => void;
  updatePlayerState: (playerId: string, state: MechaData['state']) => void;
  updatePlayerFacing: (playerId: string, facingRight: boolean) => void;
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => void;
  updatePlayerDefending: (playerId: string, isDefending: boolean) => void;
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => void;
  dealDamage: (targetId: string, damage: number, attackerId?: string) => void;
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => void;
  setAnimationFrame: (playerId: string, frame: number) => void;
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => string;
  updateEffects: (deltaTime: number) => void;
  removeEffect: (effectId: string) => void;
  clearEffects: () => void;
  
  // 游戏时间
  tick: (deltaTime: number) => void;
}

// 创建Game Store
export const useGameStore = create<GameStore>((set, get) => ({
  // 初始状态
  gameState: initialGameState,
  players: {
    player1: createInitialMecha('player1', 'BLUE', 200),
    player2: createInitialMecha('player2', 'RED', 550),
  },
  effects: [],
  
  // 游戏流程控制
  startGame: () => set(produce(state => {
    state.gameState.phase = 'PLAYING';
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.winner = null;
  })),
  
  pauseGame: () => set(produce(state => {
    if (state.gameState.phase === 'PLAYING') {
      state.gameState.phase = 'PAUSED';
    }
  })),
  
  resumeGame: () => set(produce(state => {
    if (state.gameState.phase === 'PAUSED') {
      state.gameState.phase = 'PLAYING';
    }
  })),
  
  endGame: (winner: string | null) => set(produce(state => {
    state.gameState.phase = 'RESULT';
    state.gameState.winner = winner;
    if (winner && state.players[winner]) {
      state.players[winner].score += 1;
    }
  })),
  
  resetGame: () => set(produce(state => {
    state.gameState = initialGameState;
    state.players = {
      player1: createInitialMecha('player1', 'BLUE', 200),
      player2: createInitialMecha('player2', 'RED', 550),
    };
    state.effects = [];
  })),
  
  nextRound: () => set(produce(state => {
    state.gameState.round += 1;
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.phase = 'PLAYING';
    state.gameState.winner = null;
    
    // 重置玩家位置和状态
    Object.keys(state.players).forEach(playerId => {
      const player = state.players[playerId];
      player.health = player.maxHealth;
      player.position = playerId === 'player1' ? { x: 200, y: 400 } : { x: 550, y: 400 };
      player.velocity = { x: 0, y: 0 };
      player.state = 'IDLE';
      player.isDefending = false;
      player.isGrounded = false;
      player.attackCooldown = 0;
    });
    
    state.effects = [];
  })),
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => set(produce(state => {
    state.players[playerId].position = position;
    state.players[playerId].velocity = velocity;
  })),
  
  updatePlayerState: (playerId: string, mechaState: MechaData['state']) => set(produce(state => {
    state.players[playerId].state = mechaState;
  })),
  
  updateimport { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
  winner: null,
};

// 游戏Store接口
interface GameStore {
  // 状态
  gameState: GameState;
  players: Record<string, MechaData>;
  effects: Effect[];
  
  // 游戏流程控制
  startGame: () => void;
  pauseGame: () => void;
  resumeGame: () => void;
  endGame: (winner: string | null) => void;
  resetGame: () => void;
  nextRound: () => void;
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => void;
  updatePlayerState: (playerId: string, state: MechaData['state']) => void;
  updatePlayerFacing: (playerId: string, facingRight: boolean) => void;
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => void;
  updatePlayerDefending: (playerId: string, isDefending: boolean) => void;
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => void;
  dealDamage: (targetId: string, damage: number, attackerId?: string) => void;
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => void;
  setAnimationFrame: (playerId: string, frame: number) => void;
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => string;
  updateEffects: (deltaTime: number) => void;
  removeEffect: (effectId: string) => void;
  clearEffects: () => void;
  
  // 游戏时间
  tick: (deltaTime: number) => void;
}

// 创建Game Store
export const useGameStore = create<GameStore>((set, get) => ({
  // 初始状态
  gameState: initialGameState,
  players: {
    player1: createInitialMecha('player1', 'BLUE', 200),
    player2: createInitialMecha('player2', 'RED', 550),
  },
  effects: [],
  
  // 游戏流程控制
  startGame: () => set(produce(state => {
    state.gameState.phase = 'PLAYING';
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.winner = null;
  })),
  
  pauseGame: () => set(produce(state => {
    if (state.gameState.phase === 'PLAYING') {
      state.gameState.phase = 'PAUSED';
    }
  })),
  
  resumeGame: () => set(produce(state => {
    if (state.gameState.phase === 'PAUSED') {
      state.gameState.phase = 'PLAYING';
    }
  })),
  
  endGame: (winner: string | null) => set(produce(state => {
    state.gameState.phase = 'RESULT';
    state.gameState.winner = winner;
    if (winner && state.players[winner]) {
      state.players[winner].score += 1;
    }
  })),
  
  resetGame: () => set(produce(state => {
    state.gameState = initialGameState;
    state.players = {
      player1: createInitialMecha('player1', 'BLUE', 200),
      player2: createInitialMecha('player2', 'RED', 550),
    };
    state.effects = [];
  })),
  
  nextRound: () => set(produce(state => {
    state.gameState.round += 1;
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.phase = 'PLAYING';
    state.gameState.winner = null;
    
    // 重置玩家位置和状态
    Object.keys(state.players).forEach(playerId => {
      const player = state.players[playerId];
      player.health = player.maxHealth;
      player.position = playerId === 'player1' ? { x: 200, y: 400 } : { x: 550, y: 400 };
      player.velocity = { x: 0, y: 0 };
      player.state = 'IDLE';
      player.isDefending = false;
      player.isGrounded = false;
      player.attackCooldown = 0;
    });
    
    state.effects = [];
  })),
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => set(produce(state => {
    state.players[playerId].position = position;
    state.players[playerId].velocity = velocity;
  })),
  
  updatePlayerState: (playerId: string, mechaState: MechaData['state']) => set(produce(state => {
    state.players[playerId].state = mechaState;
  })),
  
  updatePlayerFacing: (playerId:import { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
  winner: null,
};

// 游戏Store接口
interface GameStore {
  // 状态
  gameState: GameState;
  players: Record<string, MechaData>;
  effects: Effect[];
  
  // 游戏流程控制
  startGame: () => void;
  pauseGame: () => void;
  resumeGame: () => void;
  endGame: (winner: string | null) => void;
  resetGame: () => void;
  nextRound: () => void;
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => void;
  updatePlayerState: (playerId: string, state: MechaData['state']) => void;
  updatePlayerFacing: (playerId: string, facingRight: boolean) => void;
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => void;
  updatePlayerDefending: (playerId: string, isDefending: boolean) => void;
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => void;
  dealDamage: (targetId: string, damage: number, attackerId?: string) => void;
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => void;
  setAnimationFrame: (playerId: string, frame: number) => void;
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => string;
  updateEffects: (deltaTime: number) => void;
  removeEffect: (effectId: string) => void;
  clearEffects: () => void;
  
  // 游戏时间
  tick: (deltaTime: number) => void;
}

// 创建Game Store
export const useGameStore = create<GameStore>((set, get) => ({
  // 初始状态
  gameState: initialGameState,
  players: {
    player1: createInitialMecha('player1', 'BLUE', 200),
    player2: createInitialMecha('player2', 'RED', 550),
  },
  effects: [],
  
  // 游戏流程控制
  startGame: () => set(produce(state => {
    state.gameState.phase = 'PLAYING';
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.winner = null;
  })),
  
  pauseGame: () => set(produce(state => {
    if (state.gameState.phase === 'PLAYING') {
      state.gameState.phase = 'PAUSED';
    }
  })),
  
  resumeGame: () => set(produce(state => {
    if (state.gameState.phase === 'PAUSED') {
      state.gameState.phase = 'PLAYING';
    }
  })),
  
  endGame: (winner: string | null) => set(produce(state => {
    state.gameState.phase = 'RESULT';
    state.gameState.winner = winner;
    if (winner && state.players[winner]) {
      state.players[winner].score += 1;
    }
  })),
  
  resetGame: () => set(produce(state => {
    state.gameState = initialGameState;
    state.players = {
      player1: createInitialMecha('player1', 'BLUE', 200),
      player2: createInitialMecha('player2', 'RED', 550),
    };
    state.effects = [];
  })),
  
  nextRound: () => set(produce(state => {
    state.gameState.round += 1;
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.phase = 'PLAYING';
    state.gameState.winner = null;
    
    // 重置玩家位置和状态
    Object.keys(state.players).forEach(playerId => {
      const player = state.players[playerId];
      player.health = player.maxHealth;
      player.position = playerId === 'player1' ? { x: 200, y: 400 } : { x: 550, y: 400 };
      player.velocity = { x: 0, y: 0 };
      player.state = 'IDLE';
      player.isDefending = false;
      player.isGrounded = false;
      player.attackCooldown = 0;
    });
    
    state.effects = [];
  })),
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => set(produce(state => {
    state.players[playerId].position = position;
    state.players[playerId].velocity = velocity;
  })),
  
  updatePlayerState: (playerId: string, mechaState: MechaData['state']) => set(produce(state => {
    state.players[playerId].state = mechaState;
  })),
  
  updatePlayerFacing: (playerId: string, facingRight: boolean)import { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
  winner: null,
};

// 游戏Store接口
interface GameStore {
  // 状态
  gameState: GameState;
  players: Record<string, MechaData>;
  effects: Effect[];
  
  // 游戏流程控制
  startGame: () => void;
  pauseGame: () => void;
  resumeGame: () => void;
  endGame: (winner: string | null) => void;
  resetGame: () => void;
  nextRound: () => void;
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => void;
  updatePlayerState: (playerId: string, state: MechaData['state']) => void;
  updatePlayerFacing: (playerId: string, facingRight: boolean) => void;
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => void;
  updatePlayerDefending: (playerId: string, isDefending: boolean) => void;
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => void;
  dealDamage: (targetId: string, damage: number, attackerId?: string) => void;
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => void;
  setAnimationFrame: (playerId: string, frame: number) => void;
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => string;
  updateEffects: (deltaTime: number) => void;
  removeEffect: (effectId: string) => void;
  clearEffects: () => void;
  
  // 游戏时间
  tick: (deltaTime: number) => void;
}

// 创建Game Store
export const useGameStore = create<GameStore>((set, get) => ({
  // 初始状态
  gameState: initialGameState,
  players: {
    player1: createInitialMecha('player1', 'BLUE', 200),
    player2: createInitialMecha('player2', 'RED', 550),
  },
  effects: [],
  
  // 游戏流程控制
  startGame: () => set(produce(state => {
    state.gameState.phase = 'PLAYING';
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.winner = null;
  })),
  
  pauseGame: () => set(produce(state => {
    if (state.gameState.phase === 'PLAYING') {
      state.gameState.phase = 'PAUSED';
    }
  })),
  
  resumeGame: () => set(produce(state => {
    if (state.gameState.phase === 'PAUSED') {
      state.gameState.phase = 'PLAYING';
    }
  })),
  
  endGame: (winner: string | null) => set(produce(state => {
    state.gameState.phase = 'RESULT';
    state.gameState.winner = winner;
    if (winner && state.players[winner]) {
      state.players[winner].score += 1;
    }
  })),
  
  resetGame: () => set(produce(state => {
    state.gameState = initialGameState;
    state.players = {
      player1: createInitialMecha('player1', 'BLUE', 200),
      player2: createInitialMecha('player2', 'RED', 550),
    };
    state.effects = [];
  })),
  
  nextRound: () => set(produce(state => {
    state.gameState.round += 1;
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.phase = 'PLAYING';
    state.gameState.winner = null;
    
    // 重置玩家位置和状态
    Object.keys(state.players).forEach(playerId => {
      const player = state.players[playerId];
      player.health = player.maxHealth;
      player.position = playerId === 'player1' ? { x: 200, y: 400 } : { x: 550, y: 400 };
      player.velocity = { x: 0, y: 0 };
      player.state = 'IDLE';
      player.isDefending = false;
      player.isGrounded = false;
      player.attackCooldown = 0;
    });
    
    state.effects = [];
  })),
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => set(produce(state => {
    state.players[playerId].position = position;
    state.players[playerId].velocity = velocity;
  })),
  
  updatePlayerState: (playerId: string, mechaState: MechaData['state']) => set(produce(state => {
    state.players[playerId].state = mechaState;
  })),
  
  updatePlayerFacing: (playerId: string, facingRight: boolean) => set(produce(state => {
import { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
  winner: null,
};

// 游戏Store接口
interface GameStore {
  // 状态
  gameState: GameState;
  players: Record<string, MechaData>;
  effects: Effect[];
  
  // 游戏流程控制
  startGame: () => void;
  pauseGame: () => void;
  resumeGame: () => void;
  endGame: (winner: string | null) => void;
  resetGame: () => void;
  nextRound: () => void;
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => void;
  updatePlayerState: (playerId: string, state: MechaData['state']) => void;
  updatePlayerFacing: (playerId: string, facingRight: boolean) => void;
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => void;
  updatePlayerDefending: (playerId: string, isDefending: boolean) => void;
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => void;
  dealDamage: (targetId: string, damage: number, attackerId?: string) => void;
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => void;
  setAnimationFrame: (playerId: string, frame: number) => void;
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => string;
  updateEffects: (deltaTime: number) => void;
  removeEffect: (effectId: string) => void;
  clearEffects: () => void;
  
  // 游戏时间
  tick: (deltaTime: number) => void;
}

// 创建Game Store
export const useGameStore = create<GameStore>((set, get) => ({
  // 初始状态
  gameState: initialGameState,
  players: {
    player1: createInitialMecha('player1', 'BLUE', 200),
    player2: createInitialMecha('player2', 'RED', 550),
  },
  effects: [],
  
  // 游戏流程控制
  startGame: () => set(produce(state => {
    state.gameState.phase = 'PLAYING';
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.winner = null;
  })),
  
  pauseGame: () => set(produce(state => {
    if (state.gameState.phase === 'PLAYING') {
      state.gameState.phase = 'PAUSED';
    }
  })),
  
  resumeGame: () => set(produce(state => {
    if (state.gameState.phase === 'PAUSED') {
      state.gameState.phase = 'PLAYING';
    }
  })),
  
  endGame: (winner: string | null) => set(produce(state => {
    state.gameState.phase = 'RESULT';
    state.gameState.winner = winner;
    if (winner && state.players[winner]) {
      state.players[winner].score += 1;
    }
  })),
  
  resetGame: () => set(produce(state => {
    state.gameState = initialGameState;
    state.players = {
      player1: createInitialMecha('player1', 'BLUE', 200),
      player2: createInitialMecha('player2', 'RED', 550),
    };
    state.effects = [];
  })),
  
  nextRound: () => set(produce(state => {
    state.gameState.round += 1;
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.phase = 'PLAYING';
    state.gameState.winner = null;
    
    // 重置玩家位置和状态
    Object.keys(state.players).forEach(playerId => {
      const player = state.players[playerId];
      player.health = player.maxHealth;
      player.position = playerId === 'player1' ? { x: 200, y: 400 } : { x: 550, y: 400 };
      player.velocity = { x: 0, y: 0 };
      player.state = 'IDLE';
      player.isDefending = false;
      player.isGrounded = false;
      player.attackCooldown = 0;
    });
    
    state.effects = [];
  })),
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => set(produce(state => {
    state.players[playerId].position = position;
    state.players[playerId].velocity = velocity;
  })),
  
  updatePlayerState: (playerId: string, mechaState: MechaData['state']) => set(produce(state => {
    state.players[playerId].state = mechaState;
  })),
  
  updatePlayerFacing: (playerId: string, facingRight: boolean) => set(produce(state => {
    state.players[playerId].fimport { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
  winner: null,
};

// 游戏Store接口
interface GameStore {
  // 状态
  gameState: GameState;
  players: Record<string, MechaData>;
  effects: Effect[];
  
  // 游戏流程控制
  startGame: () => void;
  pauseGame: () => void;
  resumeGame: () => void;
  endGame: (winner: string | null) => void;
  resetGame: () => void;
  nextRound: () => void;
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => void;
  updatePlayerState: (playerId: string, state: MechaData['state']) => void;
  updatePlayerFacing: (playerId: string, facingRight: boolean) => void;
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => void;
  updatePlayerDefending: (playerId: string, isDefending: boolean) => void;
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => void;
  dealDamage: (targetId: string, damage: number, attackerId?: string) => void;
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => void;
  setAnimationFrame: (playerId: string, frame: number) => void;
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => string;
  updateEffects: (deltaTime: number) => void;
  removeEffect: (effectId: string) => void;
  clearEffects: () => void;
  
  // 游戏时间
  tick: (deltaTime: number) => void;
}

// 创建Game Store
export const useGameStore = create<GameStore>((set, get) => ({
  // 初始状态
  gameState: initialGameState,
  players: {
    player1: createInitialMecha('player1', 'BLUE', 200),
    player2: createInitialMecha('player2', 'RED', 550),
  },
  effects: [],
  
  // 游戏流程控制
  startGame: () => set(produce(state => {
    state.gameState.phase = 'PLAYING';
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.winner = null;
  })),
  
  pauseGame: () => set(produce(state => {
    if (state.gameState.phase === 'PLAYING') {
      state.gameState.phase = 'PAUSED';
    }
  })),
  
  resumeGame: () => set(produce(state => {
    if (state.gameState.phase === 'PAUSED') {
      state.gameState.phase = 'PLAYING';
    }
  })),
  
  endGame: (winner: string | null) => set(produce(state => {
    state.gameState.phase = 'RESULT';
    state.gameState.winner = winner;
    if (winner && state.players[winner]) {
      state.players[winner].score += 1;
    }
  })),
  
  resetGame: () => set(produce(state => {
    state.gameState = initialGameState;
    state.players = {
      player1: createInitialMecha('player1', 'BLUE', 200),
      player2: createInitialMecha('player2', 'RED', 550),
    };
    state.effects = [];
  })),
  
  nextRound: () => set(produce(state => {
    state.gameState.round += 1;
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.phase = 'PLAYING';
    state.gameState.winner = null;
    
    // 重置玩家位置和状态
    Object.keys(state.players).forEach(playerId => {
      const player = state.players[playerId];
      player.health = player.maxHealth;
      player.position = playerId === 'player1' ? { x: 200, y: 400 } : { x: 550, y: 400 };
      player.velocity = { x: 0, y: 0 };
      player.state = 'IDLE';
      player.isDefending = false;
      player.isGrounded = false;
      player.attackCooldown = 0;
    });
    
    state.effects = [];
  })),
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => set(produce(state => {
    state.players[playerId].position = position;
    state.players[playerId].velocity = velocity;
  })),
  
  updatePlayerState: (playerId: string, mechaState: MechaData['state']) => set(produce(state => {
    state.players[playerId].state = mechaState;
  })),
  
  updatePlayerFacing: (playerId: string, facingRight: boolean) => set(produce(state => {
    state.players[playerId].facingRight = facingRight;
import { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
  winner: null,
};

// 游戏Store接口
interface GameStore {
  // 状态
  gameState: GameState;
  players: Record<string, MechaData>;
  effects: Effect[];
  
  // 游戏流程控制
  startGame: () => void;
  pauseGame: () => void;
  resumeGame: () => void;
  endGame: (winner: string | null) => void;
  resetGame: () => void;
  nextRound: () => void;
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => void;
  updatePlayerState: (playerId: string, state: MechaData['state']) => void;
  updatePlayerFacing: (playerId: string, facingRight: boolean) => void;
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => void;
  updatePlayerDefending: (playerId: string, isDefending: boolean) => void;
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => void;
  dealDamage: (targetId: string, damage: number, attackerId?: string) => void;
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => void;
  setAnimationFrame: (playerId: string, frame: number) => void;
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => string;
  updateEffects: (deltaTime: number) => void;
  removeEffect: (effectId: string) => void;
  clearEffects: () => void;
  
  // 游戏时间
  tick: (deltaTime: number) => void;
}

// 创建Game Store
export const useGameStore = create<GameStore>((set, get) => ({
  // 初始状态
  gameState: initialGameState,
  players: {
    player1: createInitialMecha('player1', 'BLUE', 200),
    player2: createInitialMecha('player2', 'RED', 550),
  },
  effects: [],
  
  // 游戏流程控制
  startGame: () => set(produce(state => {
    state.gameState.phase = 'PLAYING';
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.winner = null;
  })),
  
  pauseGame: () => set(produce(state => {
    if (state.gameState.phase === 'PLAYING') {
      state.gameState.phase = 'PAUSED';
    }
  })),
  
  resumeGame: () => set(produce(state => {
    if (state.gameState.phase === 'PAUSED') {
      state.gameState.phase = 'PLAYING';
    }
  })),
  
  endGame: (winner: string | null) => set(produce(state => {
    state.gameState.phase = 'RESULT';
    state.gameState.winner = winner;
    if (winner && state.players[winner]) {
      state.players[winner].score += 1;
    }
  })),
  
  resetGame: () => set(produce(state => {
    state.gameState = initialGameState;
    state.players = {
      player1: createInitialMecha('player1', 'BLUE', 200),
      player2: createInitialMecha('player2', 'RED', 550),
    };
    state.effects = [];
  })),
  
  nextRound: () => set(produce(state => {
    state.gameState.round += 1;
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.phase = 'PLAYING';
    state.gameState.winner = null;
    
    // 重置玩家位置和状态
    Object.keys(state.players).forEach(playerId => {
      const player = state.players[playerId];
      player.health = player.maxHealth;
      player.position = playerId === 'player1' ? { x: 200, y: 400 } : { x: 550, y: 400 };
      player.velocity = { x: 0, y: 0 };
      player.state = 'IDLE';
      player.isDefending = false;
      player.isGrounded = false;
      player.attackCooldown = 0;
    });
    
    state.effects = [];
  })),
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => set(produce(state => {
    state.players[playerId].position = position;
    state.players[playerId].velocity = velocity;
  })),
  
  updatePlayerState: (playerId: string, mechaState: MechaData['state']) => set(produce(state => {
    state.players[playerId].state = mechaState;
  })),
  
  updatePlayerFacing: (playerId: string, facingRight: boolean) => set(produce(state => {
    state.players[playerId].facingRight = facingRight;
  })),
  
import { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
  winner: null,
};

// 游戏Store接口
interface GameStore {
  // 状态
  gameState: GameState;
  players: Record<string, MechaData>;
  effects: Effect[];
  
  // 游戏流程控制
  startGame: () => void;
  pauseGame: () => void;
  resumeGame: () => void;
  endGame: (winner: string | null) => void;
  resetGame: () => void;
  nextRound: () => void;
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => void;
  updatePlayerState: (playerId: string, state: MechaData['state']) => void;
  updatePlayerFacing: (playerId: string, facingRight: boolean) => void;
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => void;
  updatePlayerDefending: (playerId: string, isDefending: boolean) => void;
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => void;
  dealDamage: (targetId: string, damage: number, attackerId?: string) => void;
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => void;
  setAnimationFrame: (playerId: string, frame: number) => void;
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => string;
  updateEffects: (deltaTime: number) => void;
  removeEffect: (effectId: string) => void;
  clearEffects: () => void;
  
  // 游戏时间
  tick: (deltaTime: number) => void;
}

// 创建Game Store
export const useGameStore = create<GameStore>((set, get) => ({
  // 初始状态
  gameState: initialGameState,
  players: {
    player1: createInitialMecha('player1', 'BLUE', 200),
    player2: createInitialMecha('player2', 'RED', 550),
  },
  effects: [],
  
  // 游戏流程控制
  startGame: () => set(produce(state => {
    state.gameState.phase = 'PLAYING';
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.winner = null;
  })),
  
  pauseGame: () => set(produce(state => {
    if (state.gameState.phase === 'PLAYING') {
      state.gameState.phase = 'PAUSED';
    }
  })),
  
  resumeGame: () => set(produce(state => {
    if (state.gameState.phase === 'PAUSED') {
      state.gameState.phase = 'PLAYING';
    }
  })),
  
  endGame: (winner: string | null) => set(produce(state => {
    state.gameState.phase = 'RESULT';
    state.gameState.winner = winner;
    if (winner && state.players[winner]) {
      state.players[winner].score += 1;
    }
  })),
  
  resetGame: () => set(produce(state => {
    state.gameState = initialGameState;
    state.players = {
      player1: createInitialMecha('player1', 'BLUE', 200),
      player2: createInitialMecha('player2', 'RED', 550),
    };
    state.effects = [];
  })),
  
  nextRound: () => set(produce(state => {
    state.gameState.round += 1;
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.phase = 'PLAYING';
    state.gameState.winner = null;
    
    // 重置玩家位置和状态
    Object.keys(state.players).forEach(playerId => {
      const player = state.players[playerId];
      player.health = player.maxHealth;
      player.position = playerId === 'player1' ? { x: 200, y: 400 } : { x: 550, y: 400 };
      player.velocity = { x: 0, y: 0 };
      player.state = 'IDLE';
      player.isDefending = false;
      player.isGrounded = false;
      player.attackCooldown = 0;
    });
    
    state.effects = [];
  })),
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => set(produce(state => {
    state.players[playerId].position = position;
    state.players[playerId].velocity = velocity;
  })),
  
  updatePlayerState: (playerId: string, mechaState: MechaData['state']) => set(produce(state => {
    state.players[playerId].state = mechaState;
  })),
  
  updatePlayerFacing: (playerId: string, facingRight: boolean) => set(produce(state => {
    state.players[playerId].facingRight = facingRight;
  })),
  
  updatePlayerGrounded: (import { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
  winner: null,
};

// 游戏Store接口
interface GameStore {
  // 状态
  gameState: GameState;
  players: Record<string, MechaData>;
  effects: Effect[];
  
  // 游戏流程控制
  startGame: () => void;
  pauseGame: () => void;
  resumeGame: () => void;
  endGame: (winner: string | null) => void;
  resetGame: () => void;
  nextRound: () => void;
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => void;
  updatePlayerState: (playerId: string, state: MechaData['state']) => void;
  updatePlayerFacing: (playerId: string, facingRight: boolean) => void;
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => void;
  updatePlayerDefending: (playerId: string, isDefending: boolean) => void;
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => void;
  dealDamage: (targetId: string, damage: number, attackerId?: string) => void;
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => void;
  setAnimationFrame: (playerId: string, frame: number) => void;
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => string;
  updateEffects: (deltaTime: number) => void;
  removeEffect: (effectId: string) => void;
  clearEffects: () => void;
  
  // 游戏时间
  tick: (deltaTime: number) => void;
}

// 创建Game Store
export const useGameStore = create<GameStore>((set, get) => ({
  // 初始状态
  gameState: initialGameState,
  players: {
    player1: createInitialMecha('player1', 'BLUE', 200),
    player2: createInitialMecha('player2', 'RED', 550),
  },
  effects: [],
  
  // 游戏流程控制
  startGame: () => set(produce(state => {
    state.gameState.phase = 'PLAYING';
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.winner = null;
  })),
  
  pauseGame: () => set(produce(state => {
    if (state.gameState.phase === 'PLAYING') {
      state.gameState.phase = 'PAUSED';
    }
  })),
  
  resumeGame: () => set(produce(state => {
    if (state.gameState.phase === 'PAUSED') {
      state.gameState.phase = 'PLAYING';
    }
  })),
  
  endGame: (winner: string | null) => set(produce(state => {
    state.gameState.phase = 'RESULT';
    state.gameState.winner = winner;
    if (winner && state.players[winner]) {
      state.players[winner].score += 1;
    }
  })),
  
  resetGame: () => set(produce(state => {
    state.gameState = initialGameState;
    state.players = {
      player1: createInitialMecha('player1', 'BLUE', 200),
      player2: createInitialMecha('player2', 'RED', 550),
    };
    state.effects = [];
  })),
  
  nextRound: () => set(produce(state => {
    state.gameState.round += 1;
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.phase = 'PLAYING';
    state.gameState.winner = null;
    
    // 重置玩家位置和状态
    Object.keys(state.players).forEach(playerId => {
      const player = state.players[playerId];
      player.health = player.maxHealth;
      player.position = playerId === 'player1' ? { x: 200, y: 400 } : { x: 550, y: 400 };
      player.velocity = { x: 0, y: 0 };
      player.state = 'IDLE';
      player.isDefending = false;
      player.isGrounded = false;
      player.attackCooldown = 0;
    });
    
    state.effects = [];
  })),
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => set(produce(state => {
    state.players[playerId].position = position;
    state.players[playerId].velocity = velocity;
  })),
  
  updatePlayerState: (playerId: string, mechaState: MechaData['state']) => set(produce(state => {
    state.players[playerId].state = mechaState;
  })),
  
  updatePlayerFacing: (playerId: string, facingRight: boolean) => set(produce(state => {
    state.players[playerId].facingRight = facingRight;
  })),
  
  updatePlayerGrounded: (playerId: string, isGroundimport { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
  winner: null,
};

// 游戏Store接口
interface GameStore {
  // 状态
  gameState: GameState;
  players: Record<string, MechaData>;
  effects: Effect[];
  
  // 游戏流程控制
  startGame: () => void;
  pauseGame: () => void;
  resumeGame: () => void;
  endGame: (winner: string | null) => void;
  resetGame: () => void;
  nextRound: () => void;
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => void;
  updatePlayerState: (playerId: string, state: MechaData['state']) => void;
  updatePlayerFacing: (playerId: string, facingRight: boolean) => void;
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => void;
  updatePlayerDefending: (playerId: string, isDefending: boolean) => void;
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => void;
  dealDamage: (targetId: string, damage: number, attackerId?: string) => void;
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => void;
  setAnimationFrame: (playerId: string, frame: number) => void;
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => string;
  updateEffects: (deltaTime: number) => void;
  removeEffect: (effectId: string) => void;
  clearEffects: () => void;
  
  // 游戏时间
  tick: (deltaTime: number) => void;
}

// 创建Game Store
export const useGameStore = create<GameStore>((set, get) => ({
  // 初始状态
  gameState: initialGameState,
  players: {
    player1: createInitialMecha('player1', 'BLUE', 200),
    player2: createInitialMecha('player2', 'RED', 550),
  },
  effects: [],
  
  // 游戏流程控制
  startGame: () => set(produce(state => {
    state.gameState.phase = 'PLAYING';
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.winner = null;
  })),
  
  pauseGame: () => set(produce(state => {
    if (state.gameState.phase === 'PLAYING') {
      state.gameState.phase = 'PAUSED';
    }
  })),
  
  resumeGame: () => set(produce(state => {
    if (state.gameState.phase === 'PAUSED') {
      state.gameState.phase = 'PLAYING';
    }
  })),
  
  endGame: (winner: string | null) => set(produce(state => {
    state.gameState.phase = 'RESULT';
    state.gameState.winner = winner;
    if (winner && state.players[winner]) {
      state.players[winner].score += 1;
    }
  })),
  
  resetGame: () => set(produce(state => {
    state.gameState = initialGameState;
    state.players = {
      player1: createInitialMecha('player1', 'BLUE', 200),
      player2: createInitialMecha('player2', 'RED', 550),
    };
    state.effects = [];
  })),
  
  nextRound: () => set(produce(state => {
    state.gameState.round += 1;
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.phase = 'PLAYING';
    state.gameState.winner = null;
    
    // 重置玩家位置和状态
    Object.keys(state.players).forEach(playerId => {
      const player = state.players[playerId];
      player.health = player.maxHealth;
      player.position = playerId === 'player1' ? { x: 200, y: 400 } : { x: 550, y: 400 };
      player.velocity = { x: 0, y: 0 };
      player.state = 'IDLE';
      player.isDefending = false;
      player.isGrounded = false;
      player.attackCooldown = 0;
    });
    
    state.effects = [];
  })),
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => set(produce(state => {
    state.players[playerId].position = position;
    state.players[playerId].velocity = velocity;
  })),
  
  updatePlayerState: (playerId: string, mechaState: MechaData['state']) => set(produce(state => {
    state.players[playerId].state = mechaState;
  })),
  
  updatePlayerFacing: (playerId: string, facingRight: boolean) => set(produce(state => {
    state.players[playerId].facingRight = facingRight;
  })),
  
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => set(proimport { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
  winner: null,
};

// 游戏Store接口
interface GameStore {
  // 状态
  gameState: GameState;
  players: Record<string, MechaData>;
  effects: Effect[];
  
  // 游戏流程控制
  startGame: () => void;
  pauseGame: () => void;
  resumeGame: () => void;
  endGame: (winner: string | null) => void;
  resetGame: () => void;
  nextRound: () => void;
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => void;
  updatePlayerState: (playerId: string, state: MechaData['state']) => void;
  updatePlayerFacing: (playerId: string, facingRight: boolean) => void;
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => void;
  updatePlayerDefending: (playerId: string, isDefending: boolean) => void;
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => void;
  dealDamage: (targetId: string, damage: number, attackerId?: string) => void;
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => void;
  setAnimationFrame: (playerId: string, frame: number) => void;
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => string;
  updateEffects: (deltaTime: number) => void;
  removeEffect: (effectId: string) => void;
  clearEffects: () => void;
  
  // 游戏时间
  tick: (deltaTime: number) => void;
}

// 创建Game Store
export const useGameStore = create<GameStore>((set, get) => ({
  // 初始状态
  gameState: initialGameState,
  players: {
    player1: createInitialMecha('player1', 'BLUE', 200),
    player2: createInitialMecha('player2', 'RED', 550),
  },
  effects: [],
  
  // 游戏流程控制
  startGame: () => set(produce(state => {
    state.gameState.phase = 'PLAYING';
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.winner = null;
  })),
  
  pauseGame: () => set(produce(state => {
    if (state.gameState.phase === 'PLAYING') {
      state.gameState.phase = 'PAUSED';
    }
  })),
  
  resumeGame: () => set(produce(state => {
    if (state.gameState.phase === 'PAUSED') {
      state.gameState.phase = 'PLAYING';
    }
  })),
  
  endGame: (winner: string | null) => set(produce(state => {
    state.gameState.phase = 'RESULT';
    state.gameState.winner = winner;
    if (winner && state.players[winner]) {
      state.players[winner].score += 1;
    }
  })),
  
  resetGame: () => set(produce(state => {
    state.gameState = initialGameState;
    state.players = {
      player1: createInitialMecha('player1', 'BLUE', 200),
      player2: createInitialMecha('player2', 'RED', 550),
    };
    state.effects = [];
  })),
  
  nextRound: () => set(produce(state => {
    state.gameState.round += 1;
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.phase = 'PLAYING';
    state.gameState.winner = null;
    
    // 重置玩家位置和状态
    Object.keys(state.players).forEach(playerId => {
      const player = state.players[playerId];
      player.health = player.maxHealth;
      player.position = playerId === 'player1' ? { x: 200, y: 400 } : { x: 550, y: 400 };
      player.velocity = { x: 0, y: 0 };
      player.state = 'IDLE';
      player.isDefending = false;
      player.isGrounded = false;
      player.attackCooldown = 0;
    });
    
    state.effects = [];
  })),
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => set(produce(state => {
    state.players[playerId].position = position;
    state.players[playerId].velocity = velocity;
  })),
  
  updatePlayerState: (playerId: string, mechaState: MechaData['state']) => set(produce(state => {
    state.players[playerId].state = mechaState;
  })),
  
  updatePlayerFacing: (playerId: string, facingRight: boolean) => set(produce(state => {
    state.players[playerId].facingRight = facingRight;
  })),
  
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => set(produce(state => {
    stateimport { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
  winner: null,
};

// 游戏Store接口
interface GameStore {
  // 状态
  gameState: GameState;
  players: Record<string, MechaData>;
  effects: Effect[];
  
  // 游戏流程控制
  startGame: () => void;
  pauseGame: () => void;
  resumeGame: () => void;
  endGame: (winner: string | null) => void;
  resetGame: () => void;
  nextRound: () => void;
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => void;
  updatePlayerState: (playerId: string, state: MechaData['state']) => void;
  updatePlayerFacing: (playerId: string, facingRight: boolean) => void;
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => void;
  updatePlayerDefending: (playerId: string, isDefending: boolean) => void;
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => void;
  dealDamage: (targetId: string, damage: number, attackerId?: string) => void;
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => void;
  setAnimationFrame: (playerId: string, frame: number) => void;
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => string;
  updateEffects: (deltaTime: number) => void;
  removeEffect: (effectId: string) => void;
  clearEffects: () => void;
  
  // 游戏时间
  tick: (deltaTime: number) => void;
}

// 创建Game Store
export const useGameStore = create<GameStore>((set, get) => ({
  // 初始状态
  gameState: initialGameState,
  players: {
    player1: createInitialMecha('player1', 'BLUE', 200),
    player2: createInitialMecha('player2', 'RED', 550),
  },
  effects: [],
  
  // 游戏流程控制
  startGame: () => set(produce(state => {
    state.gameState.phase = 'PLAYING';
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.winner = null;
  })),
  
  pauseGame: () => set(produce(state => {
    if (state.gameState.phase === 'PLAYING') {
      state.gameState.phase = 'PAUSED';
    }
  })),
  
  resumeGame: () => set(produce(state => {
    if (state.gameState.phase === 'PAUSED') {
      state.gameState.phase = 'PLAYING';
    }
  })),
  
  endGame: (winner: string | null) => set(produce(state => {
    state.gameState.phase = 'RESULT';
    state.gameState.winner = winner;
    if (winner && state.players[winner]) {
      state.players[winner].score += 1;
    }
  })),
  
  resetGame: () => set(produce(state => {
    state.gameState = initialGameState;
    state.players = {
      player1: createInitialMecha('player1', 'BLUE', 200),
      player2: createInitialMecha('player2', 'RED', 550),
    };
    state.effects = [];
  })),
  
  nextRound: () => set(produce(state => {
    state.gameState.round += 1;
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.phase = 'PLAYING';
    state.gameState.winner = null;
    
    // 重置玩家位置和状态
    Object.keys(state.players).forEach(playerId => {
      const player = state.players[playerId];
      player.health = player.maxHealth;
      player.position = playerId === 'player1' ? { x: 200, y: 400 } : { x: 550, y: 400 };
      player.velocity = { x: 0, y: 0 };
      player.state = 'IDLE';
      player.isDefending = false;
      player.isGrounded = false;
      player.attackCooldown = 0;
    });
    
    state.effects = [];
  })),
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => set(produce(state => {
    state.players[playerId].position = position;
    state.players[playerId].velocity = velocity;
  })),
  
  updatePlayerState: (playerId: string, mechaState: MechaData['state']) => set(produce(state => {
    state.players[playerId].state = mechaState;
  })),
  
  updatePlayerFacing: (playerId: string, facingRight: boolean) => set(produce(state => {
    state.players[playerId].facingRight = facingRight;
  })),
  
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => set(produce(state => {
    state.players[playerId].isGroundedimport { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
  winner: null,
};

// 游戏Store接口
interface GameStore {
  // 状态
  gameState: GameState;
  players: Record<string, MechaData>;
  effects: Effect[];
  
  // 游戏流程控制
  startGame: () => void;
  pauseGame: () => void;
  resumeGame: () => void;
  endGame: (winner: string | null) => void;
  resetGame: () => void;
  nextRound: () => void;
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => void;
  updatePlayerState: (playerId: string, state: MechaData['state']) => void;
  updatePlayerFacing: (playerId: string, facingRight: boolean) => void;
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => void;
  updatePlayerDefending: (playerId: string, isDefending: boolean) => void;
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => void;
  dealDamage: (targetId: string, damage: number, attackerId?: string) => void;
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => void;
  setAnimationFrame: (playerId: string, frame: number) => void;
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => string;
  updateEffects: (deltaTime: number) => void;
  removeEffect: (effectId: string) => void;
  clearEffects: () => void;
  
  // 游戏时间
  tick: (deltaTime: number) => void;
}

// 创建Game Store
export const useGameStore = create<GameStore>((set, get) => ({
  // 初始状态
  gameState: initialGameState,
  players: {
    player1: createInitialMecha('player1', 'BLUE', 200),
    player2: createInitialMecha('player2', 'RED', 550),
  },
  effects: [],
  
  // 游戏流程控制
  startGame: () => set(produce(state => {
    state.gameState.phase = 'PLAYING';
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.winner = null;
  })),
  
  pauseGame: () => set(produce(state => {
    if (state.gameState.phase === 'PLAYING') {
      state.gameState.phase = 'PAUSED';
    }
  })),
  
  resumeGame: () => set(produce(state => {
    if (state.gameState.phase === 'PAUSED') {
      state.gameState.phase = 'PLAYING';
    }
  })),
  
  endGame: (winner: string | null) => set(produce(state => {
    state.gameState.phase = 'RESULT';
    state.gameState.winner = winner;
    if (winner && state.players[winner]) {
      state.players[winner].score += 1;
    }
  })),
  
  resetGame: () => set(produce(state => {
    state.gameState = initialGameState;
    state.players = {
      player1: createInitialMecha('player1', 'BLUE', 200),
      player2: createInitialMecha('player2', 'RED', 550),
    };
    state.effects = [];
  })),
  
  nextRound: () => set(produce(state => {
    state.gameState.round += 1;
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.phase = 'PLAYING';
    state.gameState.winner = null;
    
    // 重置玩家位置和状态
    Object.keys(state.players).forEach(playerId => {
      const player = state.players[playerId];
      player.health = player.maxHealth;
      player.position = playerId === 'player1' ? { x: 200, y: 400 } : { x: 550, y: 400 };
      player.velocity = { x: 0, y: 0 };
      player.state = 'IDLE';
      player.isDefending = false;
      player.isGrounded = false;
      player.attackCooldown = 0;
    });
    
    state.effects = [];
  })),
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => set(produce(state => {
    state.players[playerId].position = position;
    state.players[playerId].velocity = velocity;
  })),
  
  updatePlayerState: (playerId: string, mechaState: MechaData['state']) => set(produce(state => {
    state.players[playerId].state = mechaState;
  })),
  
  updatePlayerFacing: (playerId: string, facingRight: boolean) => set(produce(state => {
    state.players[playerId].facingRight = facingRight;
  })),
  
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => set(produce(state => {
    state.players[playerId].isGrounded = isGrounded;
  }import { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
  winner: null,
};

// 游戏Store接口
interface GameStore {
  // 状态
  gameState: GameState;
  players: Record<string, MechaData>;
  effects: Effect[];
  
  // 游戏流程控制
  startGame: () => void;
  pauseGame: () => void;
  resumeGame: () => void;
  endGame: (winner: string | null) => void;
  resetGame: () => void;
  nextRound: () => void;
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => void;
  updatePlayerState: (playerId: string, state: MechaData['state']) => void;
  updatePlayerFacing: (playerId: string, facingRight: boolean) => void;
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => void;
  updatePlayerDefending: (playerId: string, isDefending: boolean) => void;
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => void;
  dealDamage: (targetId: string, damage: number, attackerId?: string) => void;
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => void;
  setAnimationFrame: (playerId: string, frame: number) => void;
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => string;
  updateEffects: (deltaTime: number) => void;
  removeEffect: (effectId: string) => void;
  clearEffects: () => void;
  
  // 游戏时间
  tick: (deltaTime: number) => void;
}

// 创建Game Store
export const useGameStore = create<GameStore>((set, get) => ({
  // 初始状态
  gameState: initialGameState,
  players: {
    player1: createInitialMecha('player1', 'BLUE', 200),
    player2: createInitialMecha('player2', 'RED', 550),
  },
  effects: [],
  
  // 游戏流程控制
  startGame: () => set(produce(state => {
    state.gameState.phase = 'PLAYING';
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.winner = null;
  })),
  
  pauseGame: () => set(produce(state => {
    if (state.gameState.phase === 'PLAYING') {
      state.gameState.phase = 'PAUSED';
    }
  })),
  
  resumeGame: () => set(produce(state => {
    if (state.gameState.phase === 'PAUSED') {
      state.gameState.phase = 'PLAYING';
    }
  })),
  
  endGame: (winner: string | null) => set(produce(state => {
    state.gameState.phase = 'RESULT';
    state.gameState.winner = winner;
    if (winner && state.players[winner]) {
      state.players[winner].score += 1;
    }
  })),
  
  resetGame: () => set(produce(state => {
    state.gameState = initialGameState;
    state.players = {
      player1: createInitialMecha('player1', 'BLUE', 200),
      player2: createInitialMecha('player2', 'RED', 550),
    };
    state.effects = [];
  })),
  
  nextRound: () => set(produce(state => {
    state.gameState.round += 1;
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.phase = 'PLAYING';
    state.gameState.winner = null;
    
    // 重置玩家位置和状态
    Object.keys(state.players).forEach(playerId => {
      const player = state.players[playerId];
      player.health = player.maxHealth;
      player.position = playerId === 'player1' ? { x: 200, y: 400 } : { x: 550, y: 400 };
      player.velocity = { x: 0, y: 0 };
      player.state = 'IDLE';
      player.isDefending = false;
      player.isGrounded = false;
      player.attackCooldown = 0;
    });
    
    state.effects = [];
  })),
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => set(produce(state => {
    state.players[playerId].position = position;
    state.players[playerId].velocity = velocity;
  })),
  
  updatePlayerState: (playerId: string, mechaState: MechaData['state']) => set(produce(state => {
    state.players[playerId].state = mechaState;
  })),
  
  updatePlayerFacing: (playerId: string, facingRight: boolean) => set(produce(state => {
    state.players[playerId].facingRight = facingRight;
  })),
  
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => set(produce(state => {
    state.players[playerId].isGrounded = isGrounded;
  })),
  
  updateimport { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
  winner: null,
};

// 游戏Store接口
interface GameStore {
  // 状态
  gameState: GameState;
  players: Record<string, MechaData>;
  effects: Effect[];
  
  // 游戏流程控制
  startGame: () => void;
  pauseGame: () => void;
  resumeGame: () => void;
  endGame: (winner: string | null) => void;
  resetGame: () => void;
  nextRound: () => void;
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => void;
  updatePlayerState: (playerId: string, state: MechaData['state']) => void;
  updatePlayerFacing: (playerId: string, facingRight: boolean) => void;
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => void;
  updatePlayerDefending: (playerId: string, isDefending: boolean) => void;
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => void;
  dealDamage: (targetId: string, damage: number, attackerId?: string) => void;
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => void;
  setAnimationFrame: (playerId: string, frame: number) => void;
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => string;
  updateEffects: (deltaTime: number) => void;
  removeEffect: (effectId: string) => void;
  clearEffects: () => void;
  
  // 游戏时间
  tick: (deltaTime: number) => void;
}

// 创建Game Store
export const useGameStore = create<GameStore>((set, get) => ({
  // 初始状态
  gameState: initialGameState,
  players: {
    player1: createInitialMecha('player1', 'BLUE', 200),
    player2: createInitialMecha('player2', 'RED', 550),
  },
  effects: [],
  
  // 游戏流程控制
  startGame: () => set(produce(state => {
    state.gameState.phase = 'PLAYING';
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.winner = null;
  })),
  
  pauseGame: () => set(produce(state => {
    if (state.gameState.phase === 'PLAYING') {
      state.gameState.phase = 'PAUSED';
    }
  })),
  
  resumeGame: () => set(produce(state => {
    if (state.gameState.phase === 'PAUSED') {
      state.gameState.phase = 'PLAYING';
    }
  })),
  
  endGame: (winner: string | null) => set(produce(state => {
    state.gameState.phase = 'RESULT';
    state.gameState.winner = winner;
    if (winner && state.players[winner]) {
      state.players[winner].score += 1;
    }
  })),
  
  resetGame: () => set(produce(state => {
    state.gameState = initialGameState;
    state.players = {
      player1: createInitialMecha('player1', 'BLUE', 200),
      player2: createInitialMecha('player2', 'RED', 550),
    };
    state.effects = [];
  })),
  
  nextRound: () => set(produce(state => {
    state.gameState.round += 1;
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.phase = 'PLAYING';
    state.gameState.winner = null;
    
    // 重置玩家位置和状态
    Object.keys(state.players).forEach(playerId => {
      const player = state.players[playerId];
      player.health = player.maxHealth;
      player.position = playerId === 'player1' ? { x: 200, y: 400 } : { x: 550, y: 400 };
      player.velocity = { x: 0, y: 0 };
      player.state = 'IDLE';
      player.isDefending = false;
      player.isGrounded = false;
      player.attackCooldown = 0;
    });
    
    state.effects = [];
  })),
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => set(produce(state => {
    state.players[playerId].position = position;
    state.players[playerId].velocity = velocity;
  })),
  
  updatePlayerState: (playerId: string, mechaState: MechaData['state']) => set(produce(state => {
    state.players[playerId].state = mechaState;
  })),
  
  updatePlayerFacing: (playerId: string, facingRight: boolean) => set(produce(state => {
    state.players[playerId].facingRight = facingRight;
  })),
  
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => set(produce(state => {
    state.players[playerId].isGrounded = isGrounded;
  })),
  
  updatePlayerDefending: (playerIdimport { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
  winner: null,
};

// 游戏Store接口
interface GameStore {
  // 状态
  gameState: GameState;
  players: Record<string, MechaData>;
  effects: Effect[];
  
  // 游戏流程控制
  startGame: () => void;
  pauseGame: () => void;
  resumeGame: () => void;
  endGame: (winner: string | null) => void;
  resetGame: () => void;
  nextRound: () => void;
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => void;
  updatePlayerState: (playerId: string, state: MechaData['state']) => void;
  updatePlayerFacing: (playerId: string, facingRight: boolean) => void;
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => void;
  updatePlayerDefending: (playerId: string, isDefending: boolean) => void;
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => void;
  dealDamage: (targetId: string, damage: number, attackerId?: string) => void;
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => void;
  setAnimationFrame: (playerId: string, frame: number) => void;
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => string;
  updateEffects: (deltaTime: number) => void;
  removeEffect: (effectId: string) => void;
  clearEffects: () => void;
  
  // 游戏时间
  tick: (deltaTime: number) => void;
}

// 创建Game Store
export const useGameStore = create<GameStore>((set, get) => ({
  // 初始状态
  gameState: initialGameState,
  players: {
    player1: createInitialMecha('player1', 'BLUE', 200),
    player2: createInitialMecha('player2', 'RED', 550),
  },
  effects: [],
  
  // 游戏流程控制
  startGame: () => set(produce(state => {
    state.gameState.phase = 'PLAYING';
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.winner = null;
  })),
  
  pauseGame: () => set(produce(state => {
    if (state.gameState.phase === 'PLAYING') {
      state.gameState.phase = 'PAUSED';
    }
  })),
  
  resumeGame: () => set(produce(state => {
    if (state.gameState.phase === 'PAUSED') {
      state.gameState.phase = 'PLAYING';
    }
  })),
  
  endGame: (winner: string | null) => set(produce(state => {
    state.gameState.phase = 'RESULT';
    state.gameState.winner = winner;
    if (winner && state.players[winner]) {
      state.players[winner].score += 1;
    }
  })),
  
  resetGame: () => set(produce(state => {
    state.gameState = initialGameState;
    state.players = {
      player1: createInitialMecha('player1', 'BLUE', 200),
      player2: createInitialMecha('player2', 'RED', 550),
    };
    state.effects = [];
  })),
  
  nextRound: () => set(produce(state => {
    state.gameState.round += 1;
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.phase = 'PLAYING';
    state.gameState.winner = null;
    
    // 重置玩家位置和状态
    Object.keys(state.players).forEach(playerId => {
      const player = state.players[playerId];
      player.health = player.maxHealth;
      player.position = playerId === 'player1' ? { x: 200, y: 400 } : { x: 550, y: 400 };
      player.velocity = { x: 0, y: 0 };
      player.state = 'IDLE';
      player.isDefending = false;
      player.isGrounded = false;
      player.attackCooldown = 0;
    });
    
    state.effects = [];
  })),
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => set(produce(state => {
    state.players[playerId].position = position;
    state.players[playerId].velocity = velocity;
  })),
  
  updatePlayerState: (playerId: string, mechaState: MechaData['state']) => set(produce(state => {
    state.players[playerId].state = mechaState;
  })),
  
  updatePlayerFacing: (playerId: string, facingRight: boolean) => set(produce(state => {
    state.players[playerId].facingRight = facingRight;
  })),
  
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => set(produce(state => {
    state.players[playerId].isGrounded = isGrounded;
  })),
  
  updatePlayerDefending: (playerId: string, isDefending:import { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
  winner: null,
};

// 游戏Store接口
interface GameStore {
  // 状态
  gameState: GameState;
  players: Record<string, MechaData>;
  effects: Effect[];
  
  // 游戏流程控制
  startGame: () => void;
  pauseGame: () => void;
  resumeGame: () => void;
  endGame: (winner: string | null) => void;
  resetGame: () => void;
  nextRound: () => void;
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => void;
  updatePlayerState: (playerId: string, state: MechaData['state']) => void;
  updatePlayerFacing: (playerId: string, facingRight: boolean) => void;
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => void;
  updatePlayerDefending: (playerId: string, isDefending: boolean) => void;
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => void;
  dealDamage: (targetId: string, damage: number, attackerId?: string) => void;
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => void;
  setAnimationFrame: (playerId: string, frame: number) => void;
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => string;
  updateEffects: (deltaTime: number) => void;
  removeEffect: (effectId: string) => void;
  clearEffects: () => void;
  
  // 游戏时间
  tick: (deltaTime: number) => void;
}

// 创建Game Store
export const useGameStore = create<GameStore>((set, get) => ({
  // 初始状态
  gameState: initialGameState,
  players: {
    player1: createInitialMecha('player1', 'BLUE', 200),
    player2: createInitialMecha('player2', 'RED', 550),
  },
  effects: [],
  
  // 游戏流程控制
  startGame: () => set(produce(state => {
    state.gameState.phase = 'PLAYING';
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.winner = null;
  })),
  
  pauseGame: () => set(produce(state => {
    if (state.gameState.phase === 'PLAYING') {
      state.gameState.phase = 'PAUSED';
    }
  })),
  
  resumeGame: () => set(produce(state => {
    if (state.gameState.phase === 'PAUSED') {
      state.gameState.phase = 'PLAYING';
    }
  })),
  
  endGame: (winner: string | null) => set(produce(state => {
    state.gameState.phase = 'RESULT';
    state.gameState.winner = winner;
    if (winner && state.players[winner]) {
      state.players[winner].score += 1;
    }
  })),
  
  resetGame: () => set(produce(state => {
    state.gameState = initialGameState;
    state.players = {
      player1: createInitialMecha('player1', 'BLUE', 200),
      player2: createInitialMecha('player2', 'RED', 550),
    };
    state.effects = [];
  })),
  
  nextRound: () => set(produce(state => {
    state.gameState.round += 1;
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.phase = 'PLAYING';
    state.gameState.winner = null;
    
    // 重置玩家位置和状态
    Object.keys(state.players).forEach(playerId => {
      const player = state.players[playerId];
      player.health = player.maxHealth;
      player.position = playerId === 'player1' ? { x: 200, y: 400 } : { x: 550, y: 400 };
      player.velocity = { x: 0, y: 0 };
      player.state = 'IDLE';
      player.isDefending = false;
      player.isGrounded = false;
      player.attackCooldown = 0;
    });
    
    state.effects = [];
  })),
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => set(produce(state => {
    state.players[playerId].position = position;
    state.players[playerId].velocity = velocity;
  })),
  
  updatePlayerState: (playerId: string, mechaState: MechaData['state']) => set(produce(state => {
    state.players[playerId].state = mechaState;
  })),
  
  updatePlayerFacing: (playerId: string, facingRight: boolean) => set(produce(state => {
    state.players[playerId].facingRight = facingRight;
  })),
  
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => set(produce(state => {
    state.players[playerId].isGrounded = isGrounded;
  })),
  
  updatePlayerDefending: (playerId: string, isDefending: boolean) => set(produce(stateimport { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
  winner: null,
};

// 游戏Store接口
interface GameStore {
  // 状态
  gameState: GameState;
  players: Record<string, MechaData>;
  effects: Effect[];
  
  // 游戏流程控制
  startGame: () => void;
  pauseGame: () => void;
  resumeGame: () => void;
  endGame: (winner: string | null) => void;
  resetGame: () => void;
  nextRound: () => void;
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => void;
  updatePlayerState: (playerId: string, state: MechaData['state']) => void;
  updatePlayerFacing: (playerId: string, facingRight: boolean) => void;
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => void;
  updatePlayerDefending: (playerId: string, isDefending: boolean) => void;
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => void;
  dealDamage: (targetId: string, damage: number, attackerId?: string) => void;
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => void;
  setAnimationFrame: (playerId: string, frame: number) => void;
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => string;
  updateEffects: (deltaTime: number) => void;
  removeEffect: (effectId: string) => void;
  clearEffects: () => void;
  
  // 游戏时间
  tick: (deltaTime: number) => void;
}

// 创建Game Store
export const useGameStore = create<GameStore>((set, get) => ({
  // 初始状态
  gameState: initialGameState,
  players: {
    player1: createInitialMecha('player1', 'BLUE', 200),
    player2: createInitialMecha('player2', 'RED', 550),
  },
  effects: [],
  
  // 游戏流程控制
  startGame: () => set(produce(state => {
    state.gameState.phase = 'PLAYING';
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.winner = null;
  })),
  
  pauseGame: () => set(produce(state => {
    if (state.gameState.phase === 'PLAYING') {
      state.gameState.phase = 'PAUSED';
    }
  })),
  
  resumeGame: () => set(produce(state => {
    if (state.gameState.phase === 'PAUSED') {
      state.gameState.phase = 'PLAYING';
    }
  })),
  
  endGame: (winner: string | null) => set(produce(state => {
    state.gameState.phase = 'RESULT';
    state.gameState.winner = winner;
    if (winner && state.players[winner]) {
      state.players[winner].score += 1;
    }
  })),
  
  resetGame: () => set(produce(state => {
    state.gameState = initialGameState;
    state.players = {
      player1: createInitialMecha('player1', 'BLUE', 200),
      player2: createInitialMecha('player2', 'RED', 550),
    };
    state.effects = [];
  })),
  
  nextRound: () => set(produce(state => {
    state.gameState.round += 1;
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.phase = 'PLAYING';
    state.gameState.winner = null;
    
    // 重置玩家位置和状态
    Object.keys(state.players).forEach(playerId => {
      const player = state.players[playerId];
      player.health = player.maxHealth;
      player.position = playerId === 'player1' ? { x: 200, y: 400 } : { x: 550, y: 400 };
      player.velocity = { x: 0, y: 0 };
      player.state = 'IDLE';
      player.isDefending = false;
      player.isGrounded = false;
      player.attackCooldown = 0;
    });
    
    state.effects = [];
  })),
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => set(produce(state => {
    state.players[playerId].position = position;
    state.players[playerId].velocity = velocity;
  })),
  
  updatePlayerState: (playerId: string, mechaState: MechaData['state']) => set(produce(state => {
    state.players[playerId].state = mechaState;
  })),
  
  updatePlayerFacing: (playerId: string, facingRight: boolean) => set(produce(state => {
    state.players[playerId].facingRight = facingRight;
  })),
  
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => set(produce(state => {
    state.players[playerId].isGrounded = isGrounded;
  })),
  
  updatePlayerDefending: (playerId: string, isDefending: boolean) => set(produce(state => {
    state.players[playerimport { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
  winner: null,
};

// 游戏Store接口
interface GameStore {
  // 状态
  gameState: GameState;
  players: Record<string, MechaData>;
  effects: Effect[];
  
  // 游戏流程控制
  startGame: () => void;
  pauseGame: () => void;
  resumeGame: () => void;
  endGame: (winner: string | null) => void;
  resetGame: () => void;
  nextRound: () => void;
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => void;
  updatePlayerState: (playerId: string, state: MechaData['state']) => void;
  updatePlayerFacing: (playerId: string, facingRight: boolean) => void;
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => void;
  updatePlayerDefending: (playerId: string, isDefending: boolean) => void;
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => void;
  dealDamage: (targetId: string, damage: number, attackerId?: string) => void;
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => void;
  setAnimationFrame: (playerId: string, frame: number) => void;
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => string;
  updateEffects: (deltaTime: number) => void;
  removeEffect: (effectId: string) => void;
  clearEffects: () => void;
  
  // 游戏时间
  tick: (deltaTime: number) => void;
}

// 创建Game Store
export const useGameStore = create<GameStore>((set, get) => ({
  // 初始状态
  gameState: initialGameState,
  players: {
    player1: createInitialMecha('player1', 'BLUE', 200),
    player2: createInitialMecha('player2', 'RED', 550),
  },
  effects: [],
  
  // 游戏流程控制
  startGame: () => set(produce(state => {
    state.gameState.phase = 'PLAYING';
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.winner = null;
  })),
  
  pauseGame: () => set(produce(state => {
    if (state.gameState.phase === 'PLAYING') {
      state.gameState.phase = 'PAUSED';
    }
  })),
  
  resumeGame: () => set(produce(state => {
    if (state.gameState.phase === 'PAUSED') {
      state.gameState.phase = 'PLAYING';
    }
  })),
  
  endGame: (winner: string | null) => set(produce(state => {
    state.gameState.phase = 'RESULT';
    state.gameState.winner = winner;
    if (winner && state.players[winner]) {
      state.players[winner].score += 1;
    }
  })),
  
  resetGame: () => set(produce(state => {
    state.gameState = initialGameState;
    state.players = {
      player1: createInitialMecha('player1', 'BLUE', 200),
      player2: createInitialMecha('player2', 'RED', 550),
    };
    state.effects = [];
  })),
  
  nextRound: () => set(produce(state => {
    state.gameState.round += 1;
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.phase = 'PLAYING';
    state.gameState.winner = null;
    
    // 重置玩家位置和状态
    Object.keys(state.players).forEach(playerId => {
      const player = state.players[playerId];
      player.health = player.maxHealth;
      player.position = playerId === 'player1' ? { x: 200, y: 400 } : { x: 550, y: 400 };
      player.velocity = { x: 0, y: 0 };
      player.state = 'IDLE';
      player.isDefending = false;
      player.isGrounded = false;
      player.attackCooldown = 0;
    });
    
    state.effects = [];
  })),
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => set(produce(state => {
    state.players[playerId].position = position;
    state.players[playerId].velocity = velocity;
  })),
  
  updatePlayerState: (playerId: string, mechaState: MechaData['state']) => set(produce(state => {
    state.players[playerId].state = mechaState;
  })),
  
  updatePlayerFacing: (playerId: string, facingRight: boolean) => set(produce(state => {
    state.players[playerId].facingRight = facingRight;
  })),
  
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => set(produce(state => {
    state.players[playerId].isGrounded = isGrounded;
  })),
  
  updatePlayerDefending: (playerId: string, isDefending: boolean) => set(produce(state => {
    state.players[playerId].isDefending = isimport { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
  winner: null,
};

// 游戏Store接口
interface GameStore {
  // 状态
  gameState: GameState;
  players: Record<string, MechaData>;
  effects: Effect[];
  
  // 游戏流程控制
  startGame: () => void;
  pauseGame: () => void;
  resumeGame: () => void;
  endGame: (winner: string | null) => void;
  resetGame: () => void;
  nextRound: () => void;
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => void;
  updatePlayerState: (playerId: string, state: MechaData['state']) => void;
  updatePlayerFacing: (playerId: string, facingRight: boolean) => void;
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => void;
  updatePlayerDefending: (playerId: string, isDefending: boolean) => void;
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => void;
  dealDamage: (targetId: string, damage: number, attackerId?: string) => void;
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => void;
  setAnimationFrame: (playerId: string, frame: number) => void;
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => string;
  updateEffects: (deltaTime: number) => void;
  removeEffect: (effectId: string) => void;
  clearEffects: () => void;
  
  // 游戏时间
  tick: (deltaTime: number) => void;
}

// 创建Game Store
export const useGameStore = create<GameStore>((set, get) => ({
  // 初始状态
  gameState: initialGameState,
  players: {
    player1: createInitialMecha('player1', 'BLUE', 200),
    player2: createInitialMecha('player2', 'RED', 550),
  },
  effects: [],
  
  // 游戏流程控制
  startGame: () => set(produce(state => {
    state.gameState.phase = 'PLAYING';
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.winner = null;
  })),
  
  pauseGame: () => set(produce(state => {
    if (state.gameState.phase === 'PLAYING') {
      state.gameState.phase = 'PAUSED';
    }
  })),
  
  resumeGame: () => set(produce(state => {
    if (state.gameState.phase === 'PAUSED') {
      state.gameState.phase = 'PLAYING';
    }
  })),
  
  endGame: (winner: string | null) => set(produce(state => {
    state.gameState.phase = 'RESULT';
    state.gameState.winner = winner;
    if (winner && state.players[winner]) {
      state.players[winner].score += 1;
    }
  })),
  
  resetGame: () => set(produce(state => {
    state.gameState = initialGameState;
    state.players = {
      player1: createInitialMecha('player1', 'BLUE', 200),
      player2: createInitialMecha('player2', 'RED', 550),
    };
    state.effects = [];
  })),
  
  nextRound: () => set(produce(state => {
    state.gameState.round += 1;
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.phase = 'PLAYING';
    state.gameState.winner = null;
    
    // 重置玩家位置和状态
    Object.keys(state.players).forEach(playerId => {
      const player = state.players[playerId];
      player.health = player.maxHealth;
      player.position = playerId === 'player1' ? { x: 200, y: 400 } : { x: 550, y: 400 };
      player.velocity = { x: 0, y: 0 };
      player.state = 'IDLE';
      player.isDefending = false;
      player.isGrounded = false;
      player.attackCooldown = 0;
    });
    
    state.effects = [];
  })),
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => set(produce(state => {
    state.players[playerId].position = position;
    state.players[playerId].velocity = velocity;
  })),
  
  updatePlayerState: (playerId: string, mechaState: MechaData['state']) => set(produce(state => {
    state.players[playerId].state = mechaState;
  })),
  
  updatePlayerFacing: (playerId: string, facingRight: boolean) => set(produce(state => {
    state.players[playerId].facingRight = facingRight;
  })),
  
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => set(produce(state => {
    state.players[playerId].isGrounded = isGrounded;
  })),
  
  updatePlayerDefending: (playerId: string, isDefending: boolean) => set(produce(state => {
    state.players[playerId].isDefending = isDefending;
  })),import { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
  winner: null,
};

// 游戏Store接口
interface GameStore {
  // 状态
  gameState: GameState;
  players: Record<string, MechaData>;
  effects: Effect[];
  
  // 游戏流程控制
  startGame: () => void;
  pauseGame: () => void;
  resumeGame: () => void;
  endGame: (winner: string | null) => void;
  resetGame: () => void;
  nextRound: () => void;
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => void;
  updatePlayerState: (playerId: string, state: MechaData['state']) => void;
  updatePlayerFacing: (playerId: string, facingRight: boolean) => void;
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => void;
  updatePlayerDefending: (playerId: string, isDefending: boolean) => void;
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => void;
  dealDamage: (targetId: string, damage: number, attackerId?: string) => void;
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => void;
  setAnimationFrame: (playerId: string, frame: number) => void;
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => string;
  updateEffects: (deltaTime: number) => void;
  removeEffect: (effectId: string) => void;
  clearEffects: () => void;
  
  // 游戏时间
  tick: (deltaTime: number) => void;
}

// 创建Game Store
export const useGameStore = create<GameStore>((set, get) => ({
  // 初始状态
  gameState: initialGameState,
  players: {
    player1: createInitialMecha('player1', 'BLUE', 200),
    player2: createInitialMecha('player2', 'RED', 550),
  },
  effects: [],
  
  // 游戏流程控制
  startGame: () => set(produce(state => {
    state.gameState.phase = 'PLAYING';
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.winner = null;
  })),
  
  pauseGame: () => set(produce(state => {
    if (state.gameState.phase === 'PLAYING') {
      state.gameState.phase = 'PAUSED';
    }
  })),
  
  resumeGame: () => set(produce(state => {
    if (state.gameState.phase === 'PAUSED') {
      state.gameState.phase = 'PLAYING';
    }
  })),
  
  endGame: (winner: string | null) => set(produce(state => {
    state.gameState.phase = 'RESULT';
    state.gameState.winner = winner;
    if (winner && state.players[winner]) {
      state.players[winner].score += 1;
    }
  })),
  
  resetGame: () => set(produce(state => {
    state.gameState = initialGameState;
    state.players = {
      player1: createInitialMecha('player1', 'BLUE', 200),
      player2: createInitialMecha('player2', 'RED', 550),
    };
    state.effects = [];
  })),
  
  nextRound: () => set(produce(state => {
    state.gameState.round += 1;
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.phase = 'PLAYING';
    state.gameState.winner = null;
    
    // 重置玩家位置和状态
    Object.keys(state.players).forEach(playerId => {
      const player = state.players[playerId];
      player.health = player.maxHealth;
      player.position = playerId === 'player1' ? { x: 200, y: 400 } : { x: 550, y: 400 };
      player.velocity = { x: 0, y: 0 };
      player.state = 'IDLE';
      player.isDefending = false;
      player.isGrounded = false;
      player.attackCooldown = 0;
    });
    
    state.effects = [];
  })),
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => set(produce(state => {
    state.players[playerId].position = position;
    state.players[playerId].velocity = velocity;
  })),
  
  updatePlayerState: (playerId: string, mechaState: MechaData['state']) => set(produce(state => {
    state.players[playerId].state = mechaState;
  })),
  
  updatePlayerFacing: (playerId: string, facingRight: boolean) => set(produce(state => {
    state.players[playerId].facingRight = facingRight;
  })),
  
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => set(produce(state => {
    state.players[playerId].isGrounded = isGrounded;
  })),
  
  updatePlayerDefending: (playerId: string, isDefending: boolean) => set(produce(state => {
    state.players[playerId].isDefending = isDefending;
  })),
  
  setimport { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
  winner: null,
};

// 游戏Store接口
interface GameStore {
  // 状态
  gameState: GameState;
  players: Record<string, MechaData>;
  effects: Effect[];
  
  // 游戏流程控制
  startGame: () => void;
  pauseGame: () => void;
  resumeGame: () => void;
  endGame: (winner: string | null) => void;
  resetGame: () => void;
  nextRound: () => void;
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => void;
  updatePlayerState: (playerId: string, state: MechaData['state']) => void;
  updatePlayerFacing: (playerId: string, facingRight: boolean) => void;
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => void;
  updatePlayerDefending: (playerId: string, isDefending: boolean) => void;
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => void;
  dealDamage: (targetId: string, damage: number, attackerId?: string) => void;
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => void;
  setAnimationFrame: (playerId: string, frame: number) => void;
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => string;
  updateEffects: (deltaTime: number) => void;
  removeEffect: (effectId: string) => void;
  clearEffects: () => void;
  
  // 游戏时间
  tick: (deltaTime: number) => void;
}

// 创建Game Store
export const useGameStore = create<GameStore>((set, get) => ({
  // 初始状态
  gameState: initialGameState,
  players: {
    player1: createInitialMecha('player1', 'BLUE', 200),
    player2: createInitialMecha('player2', 'RED', 550),
  },
  effects: [],
  
  // 游戏流程控制
  startGame: () => set(produce(state => {
    state.gameState.phase = 'PLAYING';
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.winner = null;
  })),
  
  pauseGame: () => set(produce(state => {
    if (state.gameState.phase === 'PLAYING') {
      state.gameState.phase = 'PAUSED';
    }
  })),
  
  resumeGame: () => set(produce(state => {
    if (state.gameState.phase === 'PAUSED') {
      state.gameState.phase = 'PLAYING';
    }
  })),
  
  endGame: (winner: string | null) => set(produce(state => {
    state.gameState.phase = 'RESULT';
    state.gameState.winner = winner;
    if (winner && state.players[winner]) {
      state.players[winner].score += 1;
    }
  })),
  
  resetGame: () => set(produce(state => {
    state.gameState = initialGameState;
    state.players = {
      player1: createInitialMecha('player1', 'BLUE', 200),
      player2: createInitialMecha('player2', 'RED', 550),
    };
    state.effects = [];
  })),
  
  nextRound: () => set(produce(state => {
    state.gameState.round += 1;
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.phase = 'PLAYING';
    state.gameState.winner = null;
    
    // 重置玩家位置和状态
    Object.keys(state.players).forEach(playerId => {
      const player = state.players[playerId];
      player.health = player.maxHealth;
      player.position = playerId === 'player1' ? { x: 200, y: 400 } : { x: 550, y: 400 };
      player.velocity = { x: 0, y: 0 };
      player.state = 'IDLE';
      player.isDefending = false;
      player.isGrounded = false;
      player.attackCooldown = 0;
    });
    
    state.effects = [];
  })),
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => set(produce(state => {
    state.players[playerId].position = position;
    state.players[playerId].velocity = velocity;
  })),
  
  updatePlayerState: (playerId: string, mechaState: MechaData['state']) => set(produce(state => {
    state.players[playerId].state = mechaState;
  })),
  
  updatePlayerFacing: (playerId: string, facingRight: boolean) => set(produce(state => {
    state.players[playerId].facingRight = facingRight;
  })),
  
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => set(produce(state => {
    state.players[playerId].isGrounded = isGrounded;
  })),
  
  updatePlayerDefending: (playerId: string, isDefending: boolean) => set(produce(state => {
    state.players[playerId].isDefending = isDefending;
  })),
  
  setPlayerAttackCooldown: (playerIdimport { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
  winner: null,
};

// 游戏Store接口
interface GameStore {
  // 状态
  gameState: GameState;
  players: Record<string, MechaData>;
  effects: Effect[];
  
  // 游戏流程控制
  startGame: () => void;
  pauseGame: () => void;
  resumeGame: () => void;
  endGame: (winner: string | null) => void;
  resetGame: () => void;
  nextRound: () => void;
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => void;
  updatePlayerState: (playerId: string, state: MechaData['state']) => void;
  updatePlayerFacing: (playerId: string, facingRight: boolean) => void;
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => void;
  updatePlayerDefending: (playerId: string, isDefending: boolean) => void;
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => void;
  dealDamage: (targetId: string, damage: number, attackerId?: string) => void;
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => void;
  setAnimationFrame: (playerId: string, frame: number) => void;
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => string;
  updateEffects: (deltaTime: number) => void;
  removeEffect: (effectId: string) => void;
  clearEffects: () => void;
  
  // 游戏时间
  tick: (deltaTime: number) => void;
}

// 创建Game Store
export const useGameStore = create<GameStore>((set, get) => ({
  // 初始状态
  gameState: initialGameState,
  players: {
    player1: createInitialMecha('player1', 'BLUE', 200),
    player2: createInitialMecha('player2', 'RED', 550),
  },
  effects: [],
  
  // 游戏流程控制
  startGame: () => set(produce(state => {
    state.gameState.phase = 'PLAYING';
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.winner = null;
  })),
  
  pauseGame: () => set(produce(state => {
    if (state.gameState.phase === 'PLAYING') {
      state.gameState.phase = 'PAUSED';
    }
  })),
  
  resumeGame: () => set(produce(state => {
    if (state.gameState.phase === 'PAUSED') {
      state.gameState.phase = 'PLAYING';
    }
  })),
  
  endGame: (winner: string | null) => set(produce(state => {
    state.gameState.phase = 'RESULT';
    state.gameState.winner = winner;
    if (winner && state.players[winner]) {
      state.players[winner].score += 1;
    }
  })),
  
  resetGame: () => set(produce(state => {
    state.gameState = initialGameState;
    state.players = {
      player1: createInitialMecha('player1', 'BLUE', 200),
      player2: createInitialMecha('player2', 'RED', 550),
    };
    state.effects = [];
  })),
  
  nextRound: () => set(produce(state => {
    state.gameState.round += 1;
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.phase = 'PLAYING';
    state.gameState.winner = null;
    
    // 重置玩家位置和状态
    Object.keys(state.players).forEach(playerId => {
      const player = state.players[playerId];
      player.health = player.maxHealth;
      player.position = playerId === 'player1' ? { x: 200, y: 400 } : { x: 550, y: 400 };
      player.velocity = { x: 0, y: 0 };
      player.state = 'IDLE';
      player.isDefending = false;
      player.isGrounded = false;
      player.attackCooldown = 0;
    });
    
    state.effects = [];
  })),
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => set(produce(state => {
    state.players[playerId].position = position;
    state.players[playerId].velocity = velocity;
  })),
  
  updatePlayerState: (playerId: string, mechaState: MechaData['state']) => set(produce(state => {
    state.players[playerId].state = mechaState;
  })),
  
  updatePlayerFacing: (playerId: string, facingRight: boolean) => set(produce(state => {
    state.players[playerId].facingRight = facingRight;
  })),
  
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => set(produce(state => {
    state.players[playerId].isGrounded = isGrounded;
  })),
  
  updatePlayerDefending: (playerId: string, isDefending: boolean) => set(produce(state => {
    state.players[playerId].isDefending = isDefending;
  })),
  
  setPlayerAttackCooldown: (playerId: string, cooldown: number)import { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
  winner: null,
};

// 游戏Store接口
interface GameStore {
  // 状态
  gameState: GameState;
  players: Record<string, MechaData>;
  effects: Effect[];
  
  // 游戏流程控制
  startGame: () => void;
  pauseGame: () => void;
  resumeGame: () => void;
  endGame: (winner: string | null) => void;
  resetGame: () => void;
  nextRound: () => void;
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => void;
  updatePlayerState: (playerId: string, state: MechaData['state']) => void;
  updatePlayerFacing: (playerId: string, facingRight: boolean) => void;
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => void;
  updatePlayerDefending: (playerId: string, isDefending: boolean) => void;
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => void;
  dealDamage: (targetId: string, damage: number, attackerId?: string) => void;
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => void;
  setAnimationFrame: (playerId: string, frame: number) => void;
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => string;
  updateEffects: (deltaTime: number) => void;
  removeEffect: (effectId: string) => void;
  clearEffects: () => void;
  
  // 游戏时间
  tick: (deltaTime: number) => void;
}

// 创建Game Store
export const useGameStore = create<GameStore>((set, get) => ({
  // 初始状态
  gameState: initialGameState,
  players: {
    player1: createInitialMecha('player1', 'BLUE', 200),
    player2: createInitialMecha('player2', 'RED', 550),
  },
  effects: [],
  
  // 游戏流程控制
  startGame: () => set(produce(state => {
    state.gameState.phase = 'PLAYING';
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.winner = null;
  })),
  
  pauseGame: () => set(produce(state => {
    if (state.gameState.phase === 'PLAYING') {
      state.gameState.phase = 'PAUSED';
    }
  })),
  
  resumeGame: () => set(produce(state => {
    if (state.gameState.phase === 'PAUSED') {
      state.gameState.phase = 'PLAYING';
    }
  })),
  
  endGame: (winner: string | null) => set(produce(state => {
    state.gameState.phase = 'RESULT';
    state.gameState.winner = winner;
    if (winner && state.players[winner]) {
      state.players[winner].score += 1;
    }
  })),
  
  resetGame: () => set(produce(state => {
    state.gameState = initialGameState;
    state.players = {
      player1: createInitialMecha('player1', 'BLUE', 200),
      player2: createInitialMecha('player2', 'RED', 550),
    };
    state.effects = [];
  })),
  
  nextRound: () => set(produce(state => {
    state.gameState.round += 1;
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.phase = 'PLAYING';
    state.gameState.winner = null;
    
    // 重置玩家位置和状态
    Object.keys(state.players).forEach(playerId => {
      const player = state.players[playerId];
      player.health = player.maxHealth;
      player.position = playerId === 'player1' ? { x: 200, y: 400 } : { x: 550, y: 400 };
      player.velocity = { x: 0, y: 0 };
      player.state = 'IDLE';
      player.isDefending = false;
      player.isGrounded = false;
      player.attackCooldown = 0;
    });
    
    state.effects = [];
  })),
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => set(produce(state => {
    state.players[playerId].position = position;
    state.players[playerId].velocity = velocity;
  })),
  
  updatePlayerState: (playerId: string, mechaState: MechaData['state']) => set(produce(state => {
    state.players[playerId].state = mechaState;
  })),
  
  updatePlayerFacing: (playerId: string, facingRight: boolean) => set(produce(state => {
    state.players[playerId].facingRight = facingRight;
  })),
  
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => set(produce(state => {
    state.players[playerId].isGrounded = isGrounded;
  })),
  
  updatePlayerDefending: (playerId: string, isDefending: boolean) => set(produce(state => {
    state.players[playerId].isDefending = isDefending;
  })),
  
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => set(produce(state => {
import { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
  winner: null,
};

// 游戏Store接口
interface GameStore {
  // 状态
  gameState: GameState;
  players: Record<string, MechaData>;
  effects: Effect[];
  
  // 游戏流程控制
  startGame: () => void;
  pauseGame: () => void;
  resumeGame: () => void;
  endGame: (winner: string | null) => void;
  resetGame: () => void;
  nextRound: () => void;
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => void;
  updatePlayerState: (playerId: string, state: MechaData['state']) => void;
  updatePlayerFacing: (playerId: string, facingRight: boolean) => void;
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => void;
  updatePlayerDefending: (playerId: string, isDefending: boolean) => void;
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => void;
  dealDamage: (targetId: string, damage: number, attackerId?: string) => void;
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => void;
  setAnimationFrame: (playerId: string, frame: number) => void;
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => string;
  updateEffects: (deltaTime: number) => void;
  removeEffect: (effectId: string) => void;
  clearEffects: () => void;
  
  // 游戏时间
  tick: (deltaTime: number) => void;
}

// 创建Game Store
export const useGameStore = create<GameStore>((set, get) => ({
  // 初始状态
  gameState: initialGameState,
  players: {
    player1: createInitialMecha('player1', 'BLUE', 200),
    player2: createInitialMecha('player2', 'RED', 550),
  },
  effects: [],
  
  // 游戏流程控制
  startGame: () => set(produce(state => {
    state.gameState.phase = 'PLAYING';
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.winner = null;
  })),
  
  pauseGame: () => set(produce(state => {
    if (state.gameState.phase === 'PLAYING') {
      state.gameState.phase = 'PAUSED';
    }
  })),
  
  resumeGame: () => set(produce(state => {
    if (state.gameState.phase === 'PAUSED') {
      state.gameState.phase = 'PLAYING';
    }
  })),
  
  endGame: (winner: string | null) => set(produce(state => {
    state.gameState.phase = 'RESULT';
    state.gameState.winner = winner;
    if (winner && state.players[winner]) {
      state.players[winner].score += 1;
    }
  })),
  
  resetGame: () => set(produce(state => {
    state.gameState = initialGameState;
    state.players = {
      player1: createInitialMecha('player1', 'BLUE', 200),
      player2: createInitialMecha('player2', 'RED', 550),
    };
    state.effects = [];
  })),
  
  nextRound: () => set(produce(state => {
    state.gameState.round += 1;
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.phase = 'PLAYING';
    state.gameState.winner = null;
    
    // 重置玩家位置和状态
    Object.keys(state.players).forEach(playerId => {
      const player = state.players[playerId];
      player.health = player.maxHealth;
      player.position = playerId === 'player1' ? { x: 200, y: 400 } : { x: 550, y: 400 };
      player.velocity = { x: 0, y: 0 };
      player.state = 'IDLE';
      player.isDefending = false;
      player.isGrounded = false;
      player.attackCooldown = 0;
    });
    
    state.effects = [];
  })),
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => set(produce(state => {
    state.players[playerId].position = position;
    state.players[playerId].velocity = velocity;
  })),
  
  updatePlayerState: (playerId: string, mechaState: MechaData['state']) => set(produce(state => {
    state.players[playerId].state = mechaState;
  })),
  
  updatePlayerFacing: (playerId: string, facingRight: boolean) => set(produce(state => {
    state.players[playerId].facingRight = facingRight;
  })),
  
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => set(produce(state => {
    state.players[playerId].isGrounded = isGrounded;
  })),
  
  updatePlayerDefending: (playerId: string, isDefending: boolean) => set(produce(state => {
    state.players[playerId].isDefending = isDefending;
  })),
  
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => set(produce(state => {
    state.players[playerId].attackimport { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
  winner: null,
};

// 游戏Store接口
interface GameStore {
  // 状态
  gameState: GameState;
  players: Record<string, MechaData>;
  effects: Effect[];
  
  // 游戏流程控制
  startGame: () => void;
  pauseGame: () => void;
  resumeGame: () => void;
  endGame: (winner: string | null) => void;
  resetGame: () => void;
  nextRound: () => void;
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => void;
  updatePlayerState: (playerId: string, state: MechaData['state']) => void;
  updatePlayerFacing: (playerId: string, facingRight: boolean) => void;
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => void;
  updatePlayerDefending: (playerId: string, isDefending: boolean) => void;
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => void;
  dealDamage: (targetId: string, damage: number, attackerId?: string) => void;
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => void;
  setAnimationFrame: (playerId: string, frame: number) => void;
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => string;
  updateEffects: (deltaTime: number) => void;
  removeEffect: (effectId: string) => void;
  clearEffects: () => void;
  
  // 游戏时间
  tick: (deltaTime: number) => void;
}

// 创建Game Store
export const useGameStore = create<GameStore>((set, get) => ({
  // 初始状态
  gameState: initialGameState,
  players: {
    player1: createInitialMecha('player1', 'BLUE', 200),
    player2: createInitialMecha('player2', 'RED', 550),
  },
  effects: [],
  
  // 游戏流程控制
  startGame: () => set(produce(state => {
    state.gameState.phase = 'PLAYING';
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.winner = null;
  })),
  
  pauseGame: () => set(produce(state => {
    if (state.gameState.phase === 'PLAYING') {
      state.gameState.phase = 'PAUSED';
    }
  })),
  
  resumeGame: () => set(produce(state => {
    if (state.gameState.phase === 'PAUSED') {
      state.gameState.phase = 'PLAYING';
    }
  })),
  
  endGame: (winner: string | null) => set(produce(state => {
    state.gameState.phase = 'RESULT';
    state.gameState.winner = winner;
    if (winner && state.players[winner]) {
      state.players[winner].score += 1;
    }
  })),
  
  resetGame: () => set(produce(state => {
    state.gameState = initialGameState;
    state.players = {
      player1: createInitialMecha('player1', 'BLUE', 200),
      player2: createInitialMecha('player2', 'RED', 550),
    };
    state.effects = [];
  })),
  
  nextRound: () => set(produce(state => {
    state.gameState.round += 1;
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.phase = 'PLAYING';
    state.gameState.winner = null;
    
    // 重置玩家位置和状态
    Object.keys(state.players).forEach(playerId => {
      const player = state.players[playerId];
      player.health = player.maxHealth;
      player.position = playerId === 'player1' ? { x: 200, y: 400 } : { x: 550, y: 400 };
      player.velocity = { x: 0, y: 0 };
      player.state = 'IDLE';
      player.isDefending = false;
      player.isGrounded = false;
      player.attackCooldown = 0;
    });
    
    state.effects = [];
  })),
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => set(produce(state => {
    state.players[playerId].position = position;
    state.players[playerId].velocity = velocity;
  })),
  
  updatePlayerState: (playerId: string, mechaState: MechaData['state']) => set(produce(state => {
    state.players[playerId].state = mechaState;
  })),
  
  updatePlayerFacing: (playerId: string, facingRight: boolean) => set(produce(state => {
    state.players[playerId].facingRight = facingRight;
  })),
  
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => set(produce(state => {
    state.players[playerId].isGrounded = isGrounded;
  })),
  
  updatePlayerDefending: (playerId: string, isDefending: boolean) => set(produce(state => {
    state.players[playerId].isDefending = isDefending;
  })),
  
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => set(produce(state => {
    state.players[playerId].attackCooldown = cooldown;
  }import { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
  winner: null,
};

// 游戏Store接口
interface GameStore {
  // 状态
  gameState: GameState;
  players: Record<string, MechaData>;
  effects: Effect[];
  
  // 游戏流程控制
  startGame: () => void;
  pauseGame: () => void;
  resumeGame: () => void;
  endGame: (winner: string | null) => void;
  resetGame: () => void;
  nextRound: () => void;
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => void;
  updatePlayerState: (playerId: string, state: MechaData['state']) => void;
  updatePlayerFacing: (playerId: string, facingRight: boolean) => void;
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => void;
  updatePlayerDefending: (playerId: string, isDefending: boolean) => void;
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => void;
  dealDamage: (targetId: string, damage: number, attackerId?: string) => void;
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => void;
  setAnimationFrame: (playerId: string, frame: number) => void;
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => string;
  updateEffects: (deltaTime: number) => void;
  removeEffect: (effectId: string) => void;
  clearEffects: () => void;
  
  // 游戏时间
  tick: (deltaTime: number) => void;
}

// 创建Game Store
export const useGameStore = create<GameStore>((set, get) => ({
  // 初始状态
  gameState: initialGameState,
  players: {
    player1: createInitialMecha('player1', 'BLUE', 200),
    player2: createInitialMecha('player2', 'RED', 550),
  },
  effects: [],
  
  // 游戏流程控制
  startGame: () => set(produce(state => {
    state.gameState.phase = 'PLAYING';
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.winner = null;
  })),
  
  pauseGame: () => set(produce(state => {
    if (state.gameState.phase === 'PLAYING') {
      state.gameState.phase = 'PAUSED';
    }
  })),
  
  resumeGame: () => set(produce(state => {
    if (state.gameState.phase === 'PAUSED') {
      state.gameState.phase = 'PLAYING';
    }
  })),
  
  endGame: (winner: string | null) => set(produce(state => {
    state.gameState.phase = 'RESULT';
    state.gameState.winner = winner;
    if (winner && state.players[winner]) {
      state.players[winner].score += 1;
    }
  })),
  
  resetGame: () => set(produce(state => {
    state.gameState = initialGameState;
    state.players = {
      player1: createInitialMecha('player1', 'BLUE', 200),
      player2: createInitialMecha('player2', 'RED', 550),
    };
    state.effects = [];
  })),
  
  nextRound: () => set(produce(state => {
    state.gameState.round += 1;
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.phase = 'PLAYING';
    state.gameState.winner = null;
    
    // 重置玩家位置和状态
    Object.keys(state.players).forEach(playerId => {
      const player = state.players[playerId];
      player.health = player.maxHealth;
      player.position = playerId === 'player1' ? { x: 200, y: 400 } : { x: 550, y: 400 };
      player.velocity = { x: 0, y: 0 };
      player.state = 'IDLE';
      player.isDefending = false;
      player.isGrounded = false;
      player.attackCooldown = 0;
    });
    
    state.effects = [];
  })),
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => set(produce(state => {
    state.players[playerId].position = position;
    state.players[playerId].velocity = velocity;
  })),
  
  updatePlayerState: (playerId: string, mechaState: MechaData['state']) => set(produce(state => {
    state.players[playerId].state = mechaState;
  })),
  
  updatePlayerFacing: (playerId: string, facingRight: boolean) => set(produce(state => {
    state.players[playerId].facingRight = facingRight;
  })),
  
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => set(produce(state => {
    state.players[playerId].isGrounded = isGrounded;
  })),
  
  updatePlayerDefending: (playerId: string, isDefending: boolean) => set(produce(state => {
    state.players[playerId].isDefending = isDefending;
  })),
  
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => set(produce(state => {
    state.players[playerId].attackCooldown = cooldown;
  })),
  
  dealimport { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
  winner: null,
};

// 游戏Store接口
interface GameStore {
  // 状态
  gameState: GameState;
  players: Record<string, MechaData>;
  effects: Effect[];
  
  // 游戏流程控制
  startGame: () => void;
  pauseGame: () => void;
  resumeGame: () => void;
  endGame: (winner: string | null) => void;
  resetGame: () => void;
  nextRound: () => void;
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => void;
  updatePlayerState: (playerId: string, state: MechaData['state']) => void;
  updatePlayerFacing: (playerId: string, facingRight: boolean) => void;
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => void;
  updatePlayerDefending: (playerId: string, isDefending: boolean) => void;
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => void;
  dealDamage: (targetId: string, damage: number, attackerId?: string) => void;
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => void;
  setAnimationFrame: (playerId: string, frame: number) => void;
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => string;
  updateEffects: (deltaTime: number) => void;
  removeEffect: (effectId: string) => void;
  clearEffects: () => void;
  
  // 游戏时间
  tick: (deltaTime: number) => void;
}

// 创建Game Store
export const useGameStore = create<GameStore>((set, get) => ({
  // 初始状态
  gameState: initialGameState,
  players: {
    player1: createInitialMecha('player1', 'BLUE', 200),
    player2: createInitialMecha('player2', 'RED', 550),
  },
  effects: [],
  
  // 游戏流程控制
  startGame: () => set(produce(state => {
    state.gameState.phase = 'PLAYING';
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.winner = null;
  })),
  
  pauseGame: () => set(produce(state => {
    if (state.gameState.phase === 'PLAYING') {
      state.gameState.phase = 'PAUSED';
    }
  })),
  
  resumeGame: () => set(produce(state => {
    if (state.gameState.phase === 'PAUSED') {
      state.gameState.phase = 'PLAYING';
    }
  })),
  
  endGame: (winner: string | null) => set(produce(state => {
    state.gameState.phase = 'RESULT';
    state.gameState.winner = winner;
    if (winner && state.players[winner]) {
      state.players[winner].score += 1;
    }
  })),
  
  resetGame: () => set(produce(state => {
    state.gameState = initialGameState;
    state.players = {
      player1: createInitialMecha('player1', 'BLUE', 200),
      player2: createInitialMecha('player2', 'RED', 550),
    };
    state.effects = [];
  })),
  
  nextRound: () => set(produce(state => {
    state.gameState.round += 1;
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.phase = 'PLAYING';
    state.gameState.winner = null;
    
    // 重置玩家位置和状态
    Object.keys(state.players).forEach(playerId => {
      const player = state.players[playerId];
      player.health = player.maxHealth;
      player.position = playerId === 'player1' ? { x: 200, y: 400 } : { x: 550, y: 400 };
      player.velocity = { x: 0, y: 0 };
      player.state = 'IDLE';
      player.isDefending = false;
      player.isGrounded = false;
      player.attackCooldown = 0;
    });
    
    state.effects = [];
  })),
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => set(produce(state => {
    state.players[playerId].position = position;
    state.players[playerId].velocity = velocity;
  })),
  
  updatePlayerState: (playerId: string, mechaState: MechaData['state']) => set(produce(state => {
    state.players[playerId].state = mechaState;
  })),
  
  updatePlayerFacing: (playerId: string, facingRight: boolean) => set(produce(state => {
    state.players[playerId].facingRight = facingRight;
  })),
  
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => set(produce(state => {
    state.players[playerId].isGrounded = isGrounded;
  })),
  
  updatePlayerDefending: (playerId: string, isDefending: boolean) => set(produce(state => {
    state.players[playerId].isDefending = isDefending;
  })),
  
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => set(produce(state => {
    state.players[playerId].attackCooldown = cooldown;
  })),
  
  dealDamage: (targetId: stringimport { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
  winner: null,
};

// 游戏Store接口
interface GameStore {
  // 状态
  gameState: GameState;
  players: Record<string, MechaData>;
  effects: Effect[];
  
  // 游戏流程控制
  startGame: () => void;
  pauseGame: () => void;
  resumeGame: () => void;
  endGame: (winner: string | null) => void;
  resetGame: () => void;
  nextRound: () => void;
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => void;
  updatePlayerState: (playerId: string, state: MechaData['state']) => void;
  updatePlayerFacing: (playerId: string, facingRight: boolean) => void;
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => void;
  updatePlayerDefending: (playerId: string, isDefending: boolean) => void;
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => void;
  dealDamage: (targetId: string, damage: number, attackerId?: string) => void;
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => void;
  setAnimationFrame: (playerId: string, frame: number) => void;
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => string;
  updateEffects: (deltaTime: number) => void;
  removeEffect: (effectId: string) => void;
  clearEffects: () => void;
  
  // 游戏时间
  tick: (deltaTime: number) => void;
}

// 创建Game Store
export const useGameStore = create<GameStore>((set, get) => ({
  // 初始状态
  gameState: initialGameState,
  players: {
    player1: createInitialMecha('player1', 'BLUE', 200),
    player2: createInitialMecha('player2', 'RED', 550),
  },
  effects: [],
  
  // 游戏流程控制
  startGame: () => set(produce(state => {
    state.gameState.phase = 'PLAYING';
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.winner = null;
  })),
  
  pauseGame: () => set(produce(state => {
    if (state.gameState.phase === 'PLAYING') {
      state.gameState.phase = 'PAUSED';
    }
  })),
  
  resumeGame: () => set(produce(state => {
    if (state.gameState.phase === 'PAUSED') {
      state.gameState.phase = 'PLAYING';
    }
  })),
  
  endGame: (winner: string | null) => set(produce(state => {
    state.gameState.phase = 'RESULT';
    state.gameState.winner = winner;
    if (winner && state.players[winner]) {
      state.players[winner].score += 1;
    }
  })),
  
  resetGame: () => set(produce(state => {
    state.gameState = initialGameState;
    state.players = {
      player1: createInitialMecha('player1', 'BLUE', 200),
      player2: createInitialMecha('player2', 'RED', 550),
    };
    state.effects = [];
  })),
  
  nextRound: () => set(produce(state => {
    state.gameState.round += 1;
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.phase = 'PLAYING';
    state.gameState.winner = null;
    
    // 重置玩家位置和状态
    Object.keys(state.players).forEach(playerId => {
      const player = state.players[playerId];
      player.health = player.maxHealth;
      player.position = playerId === 'player1' ? { x: 200, y: 400 } : { x: 550, y: 400 };
      player.velocity = { x: 0, y: 0 };
      player.state = 'IDLE';
      player.isDefending = false;
      player.isGrounded = false;
      player.attackCooldown = 0;
    });
    
    state.effects = [];
  })),
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => set(produce(state => {
    state.players[playerId].position = position;
    state.players[playerId].velocity = velocity;
  })),
  
  updatePlayerState: (playerId: string, mechaState: MechaData['state']) => set(produce(state => {
    state.players[playerId].state = mechaState;
  })),
  
  updatePlayerFacing: (playerId: string, facingRight: boolean) => set(produce(state => {
    state.players[playerId].facingRight = facingRight;
  })),
  
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => set(produce(state => {
    state.players[playerId].isGrounded = isGrounded;
  })),
  
  updatePlayerDefending: (playerId: string, isDefending: boolean) => set(produce(state => {
    state.players[playerId].isDefending = isDefending;
  })),
  
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => set(produce(state => {
    state.players[playerId].attackCooldown = cooldown;
  })),
  
  dealDamage: (targetId: string, damage: number) => setimport { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
  winner: null,
};

// 游戏Store接口
interface GameStore {
  // 状态
  gameState: GameState;
  players: Record<string, MechaData>;
  effects: Effect[];
  
  // 游戏流程控制
  startGame: () => void;
  pauseGame: () => void;
  resumeGame: () => void;
  endGame: (winner: string | null) => void;
  resetGame: () => void;
  nextRound: () => void;
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => void;
  updatePlayerState: (playerId: string, state: MechaData['state']) => void;
  updatePlayerFacing: (playerId: string, facingRight: boolean) => void;
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => void;
  updatePlayerDefending: (playerId: string, isDefending: boolean) => void;
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => void;
  dealDamage: (targetId: string, damage: number, attackerId?: string) => void;
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => void;
  setAnimationFrame: (playerId: string, frame: number) => void;
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => string;
  updateEffects: (deltaTime: number) => void;
  removeEffect: (effectId: string) => void;
  clearEffects: () => void;
  
  // 游戏时间
  tick: (deltaTime: number) => void;
}

// 创建Game Store
export const useGameStore = create<GameStore>((set, get) => ({
  // 初始状态
  gameState: initialGameState,
  players: {
    player1: createInitialMecha('player1', 'BLUE', 200),
    player2: createInitialMecha('player2', 'RED', 550),
  },
  effects: [],
  
  // 游戏流程控制
  startGame: () => set(produce(state => {
    state.gameState.phase = 'PLAYING';
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.winner = null;
  })),
  
  pauseGame: () => set(produce(state => {
    if (state.gameState.phase === 'PLAYING') {
      state.gameState.phase = 'PAUSED';
    }
  })),
  
  resumeGame: () => set(produce(state => {
    if (state.gameState.phase === 'PAUSED') {
      state.gameState.phase = 'PLAYING';
    }
  })),
  
  endGame: (winner: string | null) => set(produce(state => {
    state.gameState.phase = 'RESULT';
    state.gameState.winner = winner;
    if (winner && state.players[winner]) {
      state.players[winner].score += 1;
    }
  })),
  
  resetGame: () => set(produce(state => {
    state.gameState = initialGameState;
    state.players = {
      player1: createInitialMecha('player1', 'BLUE', 200),
      player2: createInitialMecha('player2', 'RED', 550),
    };
    state.effects = [];
  })),
  
  nextRound: () => set(produce(state => {
    state.gameState.round += 1;
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.phase = 'PLAYING';
    state.gameState.winner = null;
    
    // 重置玩家位置和状态
    Object.keys(state.players).forEach(playerId => {
      const player = state.players[playerId];
      player.health = player.maxHealth;
      player.position = playerId === 'player1' ? { x: 200, y: 400 } : { x: 550, y: 400 };
      player.velocity = { x: 0, y: 0 };
      player.state = 'IDLE';
      player.isDefending = false;
      player.isGrounded = false;
      player.attackCooldown = 0;
    });
    
    state.effects = [];
  })),
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => set(produce(state => {
    state.players[playerId].position = position;
    state.players[playerId].velocity = velocity;
  })),
  
  updatePlayerState: (playerId: string, mechaState: MechaData['state']) => set(produce(state => {
    state.players[playerId].state = mechaState;
  })),
  
  updatePlayerFacing: (playerId: string, facingRight: boolean) => set(produce(state => {
    state.players[playerId].facingRight = facingRight;
  })),
  
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => set(produce(state => {
    state.players[playerId].isGrounded = isGrounded;
  })),
  
  updatePlayerDefending: (playerId: string, isDefending: boolean) => set(produce(state => {
    state.players[playerId].isDefending = isDefending;
  })),
  
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => set(produce(state => {
    state.players[playerId].attackCooldown = cooldown;
  })),
  
  dealDamage: (targetId: string, damage: number) => set(produce(state => {
    constimport { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
  winner: null,
};

// 游戏Store接口
interface GameStore {
  // 状态
  gameState: GameState;
  players: Record<string, MechaData>;
  effects: Effect[];
  
  // 游戏流程控制
  startGame: () => void;
  pauseGame: () => void;
  resumeGame: () => void;
  endGame: (winner: string | null) => void;
  resetGame: () => void;
  nextRound: () => void;
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => void;
  updatePlayerState: (playerId: string, state: MechaData['state']) => void;
  updatePlayerFacing: (playerId: string, facingRight: boolean) => void;
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => void;
  updatePlayerDefending: (playerId: string, isDefending: boolean) => void;
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => void;
  dealDamage: (targetId: string, damage: number, attackerId?: string) => void;
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => void;
  setAnimationFrame: (playerId: string, frame: number) => void;
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => string;
  updateEffects: (deltaTime: number) => void;
  removeEffect: (effectId: string) => void;
  clearEffects: () => void;
  
  // 游戏时间
  tick: (deltaTime: number) => void;
}

// 创建Game Store
export const useGameStore = create<GameStore>((set, get) => ({
  // 初始状态
  gameState: initialGameState,
  players: {
    player1: createInitialMecha('player1', 'BLUE', 200),
    player2: createInitialMecha('player2', 'RED', 550),
  },
  effects: [],
  
  // 游戏流程控制
  startGame: () => set(produce(state => {
    state.gameState.phase = 'PLAYING';
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.winner = null;
  })),
  
  pauseGame: () => set(produce(state => {
    if (state.gameState.phase === 'PLAYING') {
      state.gameState.phase = 'PAUSED';
    }
  })),
  
  resumeGame: () => set(produce(state => {
    if (state.gameState.phase === 'PAUSED') {
      state.gameState.phase = 'PLAYING';
    }
  })),
  
  endGame: (winner: string | null) => set(produce(state => {
    state.gameState.phase = 'RESULT';
    state.gameState.winner = winner;
    if (winner && state.players[winner]) {
      state.players[winner].score += 1;
    }
  })),
  
  resetGame: () => set(produce(state => {
    state.gameState = initialGameState;
    state.players = {
      player1: createInitialMecha('player1', 'BLUE', 200),
      player2: createInitialMecha('player2', 'RED', 550),
    };
    state.effects = [];
  })),
  
  nextRound: () => set(produce(state => {
    state.gameState.round += 1;
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.phase = 'PLAYING';
    state.gameState.winner = null;
    
    // 重置玩家位置和状态
    Object.keys(state.players).forEach(playerId => {
      const player = state.players[playerId];
      player.health = player.maxHealth;
      player.position = playerId === 'player1' ? { x: 200, y: 400 } : { x: 550, y: 400 };
      player.velocity = { x: 0, y: 0 };
      player.state = 'IDLE';
      player.isDefending = false;
      player.isGrounded = false;
      player.attackCooldown = 0;
    });
    
    state.effects = [];
  })),
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => set(produce(state => {
    state.players[playerId].position = position;
    state.players[playerId].velocity = velocity;
  })),
  
  updatePlayerState: (playerId: string, mechaState: MechaData['state']) => set(produce(state => {
    state.players[playerId].state = mechaState;
  })),
  
  updatePlayerFacing: (playerId: string, facingRight: boolean) => set(produce(state => {
    state.players[playerId].facingRight = facingRight;
  })),
  
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => set(produce(state => {
    state.players[playerId].isGrounded = isGrounded;
  })),
  
  updatePlayerDefending: (playerId: string, isDefending: boolean) => set(produce(state => {
    state.players[playerId].isDefending = isDefending;
  })),
  
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => set(produce(state => {
    state.players[playerId].attackCooldown = cooldown;
  })),
  
  dealDamage: (targetId: string, damage: number) => set(produce(state => {
    const target = state.players[targetId];
import { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
  winner: null,
};

// 游戏Store接口
interface GameStore {
  // 状态
  gameState: GameState;
  players: Record<string, MechaData>;
  effects: Effect[];
  
  // 游戏流程控制
  startGame: () => void;
  pauseGame: () => void;
  resumeGame: () => void;
  endGame: (winner: string | null) => void;
  resetGame: () => void;
  nextRound: () => void;
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => void;
  updatePlayerState: (playerId: string, state: MechaData['state']) => void;
  updatePlayerFacing: (playerId: string, facingRight: boolean) => void;
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => void;
  updatePlayerDefending: (playerId: string, isDefending: boolean) => void;
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => void;
  dealDamage: (targetId: string, damage: number, attackerId?: string) => void;
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => void;
  setAnimationFrame: (playerId: string, frame: number) => void;
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => string;
  updateEffects: (deltaTime: number) => void;
  removeEffect: (effectId: string) => void;
  clearEffects: () => void;
  
  // 游戏时间
  tick: (deltaTime: number) => void;
}

// 创建Game Store
export const useGameStore = create<GameStore>((set, get) => ({
  // 初始状态
  gameState: initialGameState,
  players: {
    player1: createInitialMecha('player1', 'BLUE', 200),
    player2: createInitialMecha('player2', 'RED', 550),
  },
  effects: [],
  
  // 游戏流程控制
  startGame: () => set(produce(state => {
    state.gameState.phase = 'PLAYING';
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.winner = null;
  })),
  
  pauseGame: () => set(produce(state => {
    if (state.gameState.phase === 'PLAYING') {
      state.gameState.phase = 'PAUSED';
    }
  })),
  
  resumeGame: () => set(produce(state => {
    if (state.gameState.phase === 'PAUSED') {
      state.gameState.phase = 'PLAYING';
    }
  })),
  
  endGame: (winner: string | null) => set(produce(state => {
    state.gameState.phase = 'RESULT';
    state.gameState.winner = winner;
    if (winner && state.players[winner]) {
      state.players[winner].score += 1;
    }
  })),
  
  resetGame: () => set(produce(state => {
    state.gameState = initialGameState;
    state.players = {
      player1: createInitialMecha('player1', 'BLUE', 200),
      player2: createInitialMecha('player2', 'RED', 550),
    };
    state.effects = [];
  })),
  
  nextRound: () => set(produce(state => {
    state.gameState.round += 1;
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.phase = 'PLAYING';
    state.gameState.winner = null;
    
    // 重置玩家位置和状态
    Object.keys(state.players).forEach(playerId => {
      const player = state.players[playerId];
      player.health = player.maxHealth;
      player.position = playerId === 'player1' ? { x: 200, y: 400 } : { x: 550, y: 400 };
      player.velocity = { x: 0, y: 0 };
      player.state = 'IDLE';
      player.isDefending = false;
      player.isGrounded = false;
      player.attackCooldown = 0;
    });
    
    state.effects = [];
  })),
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => set(produce(state => {
    state.players[playerId].position = position;
    state.players[playerId].velocity = velocity;
  })),
  
  updatePlayerState: (playerId: string, mechaState: MechaData['state']) => set(produce(state => {
    state.players[playerId].state = mechaState;
  })),
  
  updatePlayerFacing: (playerId: string, facingRight: boolean) => set(produce(state => {
    state.players[playerId].facingRight = facingRight;
  })),
  
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => set(produce(state => {
    state.players[playerId].isGrounded = isGrounded;
  })),
  
  updatePlayerDefending: (playerId: string, isDefending: boolean) => set(produce(state => {
    state.players[playerId].isDefending = isDefending;
  })),
  
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => set(produce(state => {
    state.players[playerId].attackCooldown = cooldown;
  })),
  
  dealDamage: (targetId: string, damage: number) => set(produce(state => {
    const target = state.players[targetId];
    const actualDamage = target.isimport { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
  winner: null,
};

// 游戏Store接口
interface GameStore {
  // 状态
  gameState: GameState;
  players: Record<string, MechaData>;
  effects: Effect[];
  
  // 游戏流程控制
  startGame: () => void;
  pauseGame: () => void;
  resumeGame: () => void;
  endGame: (winner: string | null) => void;
  resetGame: () => void;
  nextRound: () => void;
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => void;
  updatePlayerState: (playerId: string, state: MechaData['state']) => void;
  updatePlayerFacing: (playerId: string, facingRight: boolean) => void;
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => void;
  updatePlayerDefending: (playerId: string, isDefending: boolean) => void;
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => void;
  dealDamage: (targetId: string, damage: number, attackerId?: string) => void;
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => void;
  setAnimationFrame: (playerId: string, frame: number) => void;
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => string;
  updateEffects: (deltaTime: number) => void;
  removeEffect: (effectId: string) => void;
  clearEffects: () => void;
  
  // 游戏时间
  tick: (deltaTime: number) => void;
}

// 创建Game Store
export const useGameStore = create<GameStore>((set, get) => ({
  // 初始状态
  gameState: initialGameState,
  players: {
    player1: createInitialMecha('player1', 'BLUE', 200),
    player2: createInitialMecha('player2', 'RED', 550),
  },
  effects: [],
  
  // 游戏流程控制
  startGame: () => set(produce(state => {
    state.gameState.phase = 'PLAYING';
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.winner = null;
  })),
  
  pauseGame: () => set(produce(state => {
    if (state.gameState.phase === 'PLAYING') {
      state.gameState.phase = 'PAUSED';
    }
  })),
  
  resumeGame: () => set(produce(state => {
    if (state.gameState.phase === 'PAUSED') {
      state.gameState.phase = 'PLAYING';
    }
  })),
  
  endGame: (winner: string | null) => set(produce(state => {
    state.gameState.phase = 'RESULT';
    state.gameState.winner = winner;
    if (winner && state.players[winner]) {
      state.players[winner].score += 1;
    }
  })),
  
  resetGame: () => set(produce(state => {
    state.gameState = initialGameState;
    state.players = {
      player1: createInitialMecha('player1', 'BLUE', 200),
      player2: createInitialMecha('player2', 'RED', 550),
    };
    state.effects = [];
  })),
  
  nextRound: () => set(produce(state => {
    state.gameState.round += 1;
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.phase = 'PLAYING';
    state.gameState.winner = null;
    
    // 重置玩家位置和状态
    Object.keys(state.players).forEach(playerId => {
      const player = state.players[playerId];
      player.health = player.maxHealth;
      player.position = playerId === 'player1' ? { x: 200, y: 400 } : { x: 550, y: 400 };
      player.velocity = { x: 0, y: 0 };
      player.state = 'IDLE';
      player.isDefending = false;
      player.isGrounded = false;
      player.attackCooldown = 0;
    });
    
    state.effects = [];
  })),
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => set(produce(state => {
    state.players[playerId].position = position;
    state.players[playerId].velocity = velocity;
  })),
  
  updatePlayerState: (playerId: string, mechaState: MechaData['state']) => set(produce(state => {
    state.players[playerId].state = mechaState;
  })),
  
  updatePlayerFacing: (playerId: string, facingRight: boolean) => set(produce(state => {
    state.players[playerId].facingRight = facingRight;
  })),
  
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => set(produce(state => {
    state.players[playerId].isGrounded = isGrounded;
  })),
  
  updatePlayerDefending: (playerId: string, isDefending: boolean) => set(produce(state => {
    state.players[playerId].isDefending = isDefending;
  })),
  
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => set(produce(state => {
    state.players[playerId].attackCooldown = cooldown;
  })),
  
  dealDamage: (targetId: string, damage: number) => set(produce(state => {
    const target = state.players[targetId];
    const actualDamage = target.isDefending ? Math.floor(damageimport { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
  winner: null,
};

// 游戏Store接口
interface GameStore {
  // 状态
  gameState: GameState;
  players: Record<string, MechaData>;
  effects: Effect[];
  
  // 游戏流程控制
  startGame: () => void;
  pauseGame: () => void;
  resumeGame: () => void;
  endGame: (winner: string | null) => void;
  resetGame: () => void;
  nextRound: () => void;
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => void;
  updatePlayerState: (playerId: string, state: MechaData['state']) => void;
  updatePlayerFacing: (playerId: string, facingRight: boolean) => void;
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => void;
  updatePlayerDefending: (playerId: string, isDefending: boolean) => void;
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => void;
  dealDamage: (targetId: string, damage: number, attackerId?: string) => void;
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => void;
  setAnimationFrame: (playerId: string, frame: number) => void;
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => string;
  updateEffects: (deltaTime: number) => void;
  removeEffect: (effectId: string) => void;
  clearEffects: () => void;
  
  // 游戏时间
  tick: (deltaTime: number) => void;
}

// 创建Game Store
export const useGameStore = create<GameStore>((set, get) => ({
  // 初始状态
  gameState: initialGameState,
  players: {
    player1: createInitialMecha('player1', 'BLUE', 200),
    player2: createInitialMecha('player2', 'RED', 550),
  },
  effects: [],
  
  // 游戏流程控制
  startGame: () => set(produce(state => {
    state.gameState.phase = 'PLAYING';
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.winner = null;
  })),
  
  pauseGame: () => set(produce(state => {
    if (state.gameState.phase === 'PLAYING') {
      state.gameState.phase = 'PAUSED';
    }
  })),
  
  resumeGame: () => set(produce(state => {
    if (state.gameState.phase === 'PAUSED') {
      state.gameState.phase = 'PLAYING';
    }
  })),
  
  endGame: (winner: string | null) => set(produce(state => {
    state.gameState.phase = 'RESULT';
    state.gameState.winner = winner;
    if (winner && state.players[winner]) {
      state.players[winner].score += 1;
    }
  })),
  
  resetGame: () => set(produce(state => {
    state.gameState = initialGameState;
    state.players = {
      player1: createInitialMecha('player1', 'BLUE', 200),
      player2: createInitialMecha('player2', 'RED', 550),
    };
    state.effects = [];
  })),
  
  nextRound: () => set(produce(state => {
    state.gameState.round += 1;
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.phase = 'PLAYING';
    state.gameState.winner = null;
    
    // 重置玩家位置和状态
    Object.keys(state.players).forEach(playerId => {
      const player = state.players[playerId];
      player.health = player.maxHealth;
      player.position = playerId === 'player1' ? { x: 200, y: 400 } : { x: 550, y: 400 };
      player.velocity = { x: 0, y: 0 };
      player.state = 'IDLE';
      player.isDefending = false;
      player.isGrounded = false;
      player.attackCooldown = 0;
    });
    
    state.effects = [];
  })),
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => set(produce(state => {
    state.players[playerId].position = position;
    state.players[playerId].velocity = velocity;
  })),
  
  updatePlayerState: (playerId: string, mechaState: MechaData['state']) => set(produce(state => {
    state.players[playerId].state = mechaState;
  })),
  
  updatePlayerFacing: (playerId: string, facingRight: boolean) => set(produce(state => {
    state.players[playerId].facingRight = facingRight;
  })),
  
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => set(produce(state => {
    state.players[playerId].isGrounded = isGrounded;
  })),
  
  updatePlayerDefending: (playerId: string, isDefending: boolean) => set(produce(state => {
    state.players[playerId].isDefending = isDefending;
  })),
  
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => set(produce(state => {
    state.players[playerId].attackCooldown = cooldown;
  })),
  
  dealDamage: (targetId: string, damage: number) => set(produce(state => {
    const target = state.players[targetId];
    const actualDamage = target.isDefending ? Math.floor(damage * 0.5) :import { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
  winner: null,
};

// 游戏Store接口
interface GameStore {
  // 状态
  gameState: GameState;
  players: Record<string, MechaData>;
  effects: Effect[];
  
  // 游戏流程控制
  startGame: () => void;
  pauseGame: () => void;
  resumeGame: () => void;
  endGame: (winner: string | null) => void;
  resetGame: () => void;
  nextRound: () => void;
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => void;
  updatePlayerState: (playerId: string, state: MechaData['state']) => void;
  updatePlayerFacing: (playerId: string, facingRight: boolean) => void;
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => void;
  updatePlayerDefending: (playerId: string, isDefending: boolean) => void;
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => void;
  dealDamage: (targetId: string, damage: number, attackerId?: string) => void;
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => void;
  setAnimationFrame: (playerId: string, frame: number) => void;
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => string;
  updateEffects: (deltaTime: number) => void;
  removeEffect: (effectId: string) => void;
  clearEffects: () => void;
  
  // 游戏时间
  tick: (deltaTime: number) => void;
}

// 创建Game Store
export const useGameStore = create<GameStore>((set, get) => ({
  // 初始状态
  gameState: initialGameState,
  players: {
    player1: createInitialMecha('player1', 'BLUE', 200),
    player2: createInitialMecha('player2', 'RED', 550),
  },
  effects: [],
  
  // 游戏流程控制
  startGame: () => set(produce(state => {
    state.gameState.phase = 'PLAYING';
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.winner = null;
  })),
  
  pauseGame: () => set(produce(state => {
    if (state.gameState.phase === 'PLAYING') {
      state.gameState.phase = 'PAUSED';
    }
  })),
  
  resumeGame: () => set(produce(state => {
    if (state.gameState.phase === 'PAUSED') {
      state.gameState.phase = 'PLAYING';
    }
  })),
  
  endGame: (winner: string | null) => set(produce(state => {
    state.gameState.phase = 'RESULT';
    state.gameState.winner = winner;
    if (winner && state.players[winner]) {
      state.players[winner].score += 1;
    }
  })),
  
  resetGame: () => set(produce(state => {
    state.gameState = initialGameState;
    state.players = {
      player1: createInitialMecha('player1', 'BLUE', 200),
      player2: createInitialMecha('player2', 'RED', 550),
    };
    state.effects = [];
  })),
  
  nextRound: () => set(produce(state => {
    state.gameState.round += 1;
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.phase = 'PLAYING';
    state.gameState.winner = null;
    
    // 重置玩家位置和状态
    Object.keys(state.players).forEach(playerId => {
      const player = state.players[playerId];
      player.health = player.maxHealth;
      player.position = playerId === 'player1' ? { x: 200, y: 400 } : { x: 550, y: 400 };
      player.velocity = { x: 0, y: 0 };
      player.state = 'IDLE';
      player.isDefending = false;
      player.isGrounded = false;
      player.attackCooldown = 0;
    });
    
    state.effects = [];
  })),
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => set(produce(state => {
    state.players[playerId].position = position;
    state.players[playerId].velocity = velocity;
  })),
  
  updatePlayerState: (playerId: string, mechaState: MechaData['state']) => set(produce(state => {
    state.players[playerId].state = mechaState;
  })),
  
  updatePlayerFacing: (playerId: string, facingRight: boolean) => set(produce(state => {
    state.players[playerId].facingRight = facingRight;
  })),
  
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => set(produce(state => {
    state.players[playerId].isGrounded = isGrounded;
  })),
  
  updatePlayerDefending: (playerId: string, isDefending: boolean) => set(produce(state => {
    state.players[playerId].isDefending = isDefending;
  })),
  
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => set(produce(state => {
    state.players[playerId].attackCooldown = cooldown;
  })),
  
  dealDamage: (targetId: string, damage: number) => set(produce(state => {
    const target = state.players[targetId];
    const actualDamage = target.isDefending ? Math.floor(damage * 0.5) : damage;
    target.health =import { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
  winner: null,
};

// 游戏Store接口
interface GameStore {
  // 状态
  gameState: GameState;
  players: Record<string, MechaData>;
  effects: Effect[];
  
  // 游戏流程控制
  startGame: () => void;
  pauseGame: () => void;
  resumeGame: () => void;
  endGame: (winner: string | null) => void;
  resetGame: () => void;
  nextRound: () => void;
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => void;
  updatePlayerState: (playerId: string, state: MechaData['state']) => void;
  updatePlayerFacing: (playerId: string, facingRight: boolean) => void;
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => void;
  updatePlayerDefending: (playerId: string, isDefending: boolean) => void;
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => void;
  dealDamage: (targetId: string, damage: number, attackerId?: string) => void;
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => void;
  setAnimationFrame: (playerId: string, frame: number) => void;
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => string;
  updateEffects: (deltaTime: number) => void;
  removeEffect: (effectId: string) => void;
  clearEffects: () => void;
  
  // 游戏时间
  tick: (deltaTime: number) => void;
}

// 创建Game Store
export const useGameStore = create<GameStore>((set, get) => ({
  // 初始状态
  gameState: initialGameState,
  players: {
    player1: createInitialMecha('player1', 'BLUE', 200),
    player2: createInitialMecha('player2', 'RED', 550),
  },
  effects: [],
  
  // 游戏流程控制
  startGame: () => set(produce(state => {
    state.gameState.phase = 'PLAYING';
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.winner = null;
  })),
  
  pauseGame: () => set(produce(state => {
    if (state.gameState.phase === 'PLAYING') {
      state.gameState.phase = 'PAUSED';
    }
  })),
  
  resumeGame: () => set(produce(state => {
    if (state.gameState.phase === 'PAUSED') {
      state.gameState.phase = 'PLAYING';
    }
  })),
  
  endGame: (winner: string | null) => set(produce(state => {
    state.gameState.phase = 'RESULT';
    state.gameState.winner = winner;
    if (winner && state.players[winner]) {
      state.players[winner].score += 1;
    }
  })),
  
  resetGame: () => set(produce(state => {
    state.gameState = initialGameState;
    state.players = {
      player1: createInitialMecha('player1', 'BLUE', 200),
      player2: createInitialMecha('player2', 'RED', 550),
    };
    state.effects = [];
  })),
  
  nextRound: () => set(produce(state => {
    state.gameState.round += 1;
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.phase = 'PLAYING';
    state.gameState.winner = null;
    
    // 重置玩家位置和状态
    Object.keys(state.players).forEach(playerId => {
      const player = state.players[playerId];
      player.health = player.maxHealth;
      player.position = playerId === 'player1' ? { x: 200, y: 400 } : { x: 550, y: 400 };
      player.velocity = { x: 0, y: 0 };
      player.state = 'IDLE';
      player.isDefending = false;
      player.isGrounded = false;
      player.attackCooldown = 0;
    });
    
    state.effects = [];
  })),
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => set(produce(state => {
    state.players[playerId].position = position;
    state.players[playerId].velocity = velocity;
  })),
  
  updatePlayerState: (playerId: string, mechaState: MechaData['state']) => set(produce(state => {
    state.players[playerId].state = mechaState;
  })),
  
  updatePlayerFacing: (playerId: string, facingRight: boolean) => set(produce(state => {
    state.players[playerId].facingRight = facingRight;
  })),
  
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => set(produce(state => {
    state.players[playerId].isGrounded = isGrounded;
  })),
  
  updatePlayerDefending: (playerId: string, isDefending: boolean) => set(produce(state => {
    state.players[playerId].isDefending = isDefending;
  })),
  
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => set(produce(state => {
    state.players[playerId].attackCooldown = cooldown;
  })),
  
  dealDamage: (targetId: string, damage: number) => set(produce(state => {
    const target = state.players[targetId];
    const actualDamage = target.isDefending ? Math.floor(damage * 0.5) : damage;
    target.health = Math.max(0, target.healthimport { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
  winner: null,
};

// 游戏Store接口
interface GameStore {
  // 状态
  gameState: GameState;
  players: Record<string, MechaData>;
  effects: Effect[];
  
  // 游戏流程控制
  startGame: () => void;
  pauseGame: () => void;
  resumeGame: () => void;
  endGame: (winner: string | null) => void;
  resetGame: () => void;
  nextRound: () => void;
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => void;
  updatePlayerState: (playerId: string, state: MechaData['state']) => void;
  updatePlayerFacing: (playerId: string, facingRight: boolean) => void;
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => void;
  updatePlayerDefending: (playerId: string, isDefending: boolean) => void;
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => void;
  dealDamage: (targetId: string, damage: number, attackerId?: string) => void;
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => void;
  setAnimationFrame: (playerId: string, frame: number) => void;
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => string;
  updateEffects: (deltaTime: number) => void;
  removeEffect: (effectId: string) => void;
  clearEffects: () => void;
  
  // 游戏时间
  tick: (deltaTime: number) => void;
}

// 创建Game Store
export const useGameStore = create<GameStore>((set, get) => ({
  // 初始状态
  gameState: initialGameState,
  players: {
    player1: createInitialMecha('player1', 'BLUE', 200),
    player2: createInitialMecha('player2', 'RED', 550),
  },
  effects: [],
  
  // 游戏流程控制
  startGame: () => set(produce(state => {
    state.gameState.phase = 'PLAYING';
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.winner = null;
  })),
  
  pauseGame: () => set(produce(state => {
    if (state.gameState.phase === 'PLAYING') {
      state.gameState.phase = 'PAUSED';
    }
  })),
  
  resumeGame: () => set(produce(state => {
    if (state.gameState.phase === 'PAUSED') {
      state.gameState.phase = 'PLAYING';
    }
  })),
  
  endGame: (winner: string | null) => set(produce(state => {
    state.gameState.phase = 'RESULT';
    state.gameState.winner = winner;
    if (winner && state.players[winner]) {
      state.players[winner].score += 1;
    }
  })),
  
  resetGame: () => set(produce(state => {
    state.gameState = initialGameState;
    state.players = {
      player1: createInitialMecha('player1', 'BLUE', 200),
      player2: createInitialMecha('player2', 'RED', 550),
    };
    state.effects = [];
  })),
  
  nextRound: () => set(produce(state => {
    state.gameState.round += 1;
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.phase = 'PLAYING';
    state.gameState.winner = null;
    
    // 重置玩家位置和状态
    Object.keys(state.players).forEach(playerId => {
      const player = state.players[playerId];
      player.health = player.maxHealth;
      player.position = playerId === 'player1' ? { x: 200, y: 400 } : { x: 550, y: 400 };
      player.velocity = { x: 0, y: 0 };
      player.state = 'IDLE';
      player.isDefending = false;
      player.isGrounded = false;
      player.attackCooldown = 0;
    });
    
    state.effects = [];
  })),
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => set(produce(state => {
    state.players[playerId].position = position;
    state.players[playerId].velocity = velocity;
  })),
  
  updatePlayerState: (playerId: string, mechaState: MechaData['state']) => set(produce(state => {
    state.players[playerId].state = mechaState;
  })),
  
  updatePlayerFacing: (playerId: string, facingRight: boolean) => set(produce(state => {
    state.players[playerId].facingRight = facingRight;
  })),
  
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => set(produce(state => {
    state.players[playerId].isGrounded = isGrounded;
  })),
  
  updatePlayerDefending: (playerId: string, isDefending: boolean) => set(produce(state => {
    state.players[playerId].isDefending = isDefending;
  })),
  
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => set(produce(state => {
    state.players[playerId].attackCooldown = cooldown;
  })),
  
  dealDamage: (targetId: string, damage: number) => set(produce(state => {
    const target = state.players[targetId];
    const actualDamage = target.isDefending ? Math.floor(damage * 0.5) : damage;
    target.health = Math.max(0, target.health - actualDamage);
    
import { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
  winner: null,
};

// 游戏Store接口
interface GameStore {
  // 状态
  gameState: GameState;
  players: Record<string, MechaData>;
  effects: Effect[];
  
  // 游戏流程控制
  startGame: () => void;
  pauseGame: () => void;
  resumeGame: () => void;
  endGame: (winner: string | null) => void;
  resetGame: () => void;
  nextRound: () => void;
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => void;
  updatePlayerState: (playerId: string, state: MechaData['state']) => void;
  updatePlayerFacing: (playerId: string, facingRight: boolean) => void;
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => void;
  updatePlayerDefending: (playerId: string, isDefending: boolean) => void;
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => void;
  dealDamage: (targetId: string, damage: number, attackerId?: string) => void;
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => void;
  setAnimationFrame: (playerId: string, frame: number) => void;
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => string;
  updateEffects: (deltaTime: number) => void;
  removeEffect: (effectId: string) => void;
  clearEffects: () => void;
  
  // 游戏时间
  tick: (deltaTime: number) => void;
}

// 创建Game Store
export const useGameStore = create<GameStore>((set, get) => ({
  // 初始状态
  gameState: initialGameState,
  players: {
    player1: createInitialMecha('player1', 'BLUE', 200),
    player2: createInitialMecha('player2', 'RED', 550),
  },
  effects: [],
  
  // 游戏流程控制
  startGame: () => set(produce(state => {
    state.gameState.phase = 'PLAYING';
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.winner = null;
  })),
  
  pauseGame: () => set(produce(state => {
    if (state.gameState.phase === 'PLAYING') {
      state.gameState.phase = 'PAUSED';
    }
  })),
  
  resumeGame: () => set(produce(state => {
    if (state.gameState.phase === 'PAUSED') {
      state.gameState.phase = 'PLAYING';
    }
  })),
  
  endGame: (winner: string | null) => set(produce(state => {
    state.gameState.phase = 'RESULT';
    state.gameState.winner = winner;
    if (winner && state.players[winner]) {
      state.players[winner].score += 1;
    }
  })),
  
  resetGame: () => set(produce(state => {
    state.gameState = initialGameState;
    state.players = {
      player1: createInitialMecha('player1', 'BLUE', 200),
      player2: createInitialMecha('player2', 'RED', 550),
    };
    state.effects = [];
  })),
  
  nextRound: () => set(produce(state => {
    state.gameState.round += 1;
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.phase = 'PLAYING';
    state.gameState.winner = null;
    
    // 重置玩家位置和状态
    Object.keys(state.players).forEach(playerId => {
      const player = state.players[playerId];
      player.health = player.maxHealth;
      player.position = playerId === 'player1' ? { x: 200, y: 400 } : { x: 550, y: 400 };
      player.velocity = { x: 0, y: 0 };
      player.state = 'IDLE';
      player.isDefending = false;
      player.isGrounded = false;
      player.attackCooldown = 0;
    });
    
    state.effects = [];
  })),
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => set(produce(state => {
    state.players[playerId].position = position;
    state.players[playerId].velocity = velocity;
  })),
  
  updatePlayerState: (playerId: string, mechaState: MechaData['state']) => set(produce(state => {
    state.players[playerId].state = mechaState;
  })),
  
  updatePlayerFacing: (playerId: string, facingRight: boolean) => set(produce(state => {
    state.players[playerId].facingRight = facingRight;
  })),
  
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => set(produce(state => {
    state.players[playerId].isGrounded = isGrounded;
  })),
  
  updatePlayerDefending: (playerId: string, isDefending: boolean) => set(produce(state => {
    state.players[playerId].isDefending = isDefending;
  })),
  
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => set(produce(state => {
    state.players[playerId].attackCooldown = cooldown;
  })),
  
  dealDamage: (targetId: string, damage: number) => set(produce(state => {
    const target = state.players[targetId];
    const actualDamage = target.isDefending ? Math.floor(damage * 0.5) : damage;
    target.health = Math.max(0, target.health - actualDamage);
    
    // 添加受击特效
import { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
  winner: null,
};

// 游戏Store接口
interface GameStore {
  // 状态
  gameState: GameState;
  players: Record<string, MechaData>;
  effects: Effect[];
  
  // 游戏流程控制
  startGame: () => void;
  pauseGame: () => void;
  resumeGame: () => void;
  endGame: (winner: string | null) => void;
  resetGame: () => void;
  nextRound: () => void;
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => void;
  updatePlayerState: (playerId: string, state: MechaData['state']) => void;
  updatePlayerFacing: (playerId: string, facingRight: boolean) => void;
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => void;
  updatePlayerDefending: (playerId: string, isDefending: boolean) => void;
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => void;
  dealDamage: (targetId: string, damage: number, attackerId?: string) => void;
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => void;
  setAnimationFrame: (playerId: string, frame: number) => void;
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => string;
  updateEffects: (deltaTime: number) => void;
  removeEffect: (effectId: string) => void;
  clearEffects: () => void;
  
  // 游戏时间
  tick: (deltaTime: number) => void;
}

// 创建Game Store
export const useGameStore = create<GameStore>((set, get) => ({
  // 初始状态
  gameState: initialGameState,
  players: {
    player1: createInitialMecha('player1', 'BLUE', 200),
    player2: createInitialMecha('player2', 'RED', 550),
  },
  effects: [],
  
  // 游戏流程控制
  startGame: () => set(produce(state => {
    state.gameState.phase = 'PLAYING';
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.winner = null;
  })),
  
  pauseGame: () => set(produce(state => {
    if (state.gameState.phase === 'PLAYING') {
      state.gameState.phase = 'PAUSED';
    }
  })),
  
  resumeGame: () => set(produce(state => {
    if (state.gameState.phase === 'PAUSED') {
      state.gameState.phase = 'PLAYING';
    }
  })),
  
  endGame: (winner: string | null) => set(produce(state => {
    state.gameState.phase = 'RESULT';
    state.gameState.winner = winner;
    if (winner && state.players[winner]) {
      state.players[winner].score += 1;
    }
  })),
  
  resetGame: () => set(produce(state => {
    state.gameState = initialGameState;
    state.players = {
      player1: createInitialMecha('player1', 'BLUE', 200),
      player2: createInitialMecha('player2', 'RED', 550),
    };
    state.effects = [];
  })),
  
  nextRound: () => set(produce(state => {
    state.gameState.round += 1;
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.phase = 'PLAYING';
    state.gameState.winner = null;
    
    // 重置玩家位置和状态
    Object.keys(state.players).forEach(playerId => {
      const player = state.players[playerId];
      player.health = player.maxHealth;
      player.position = playerId === 'player1' ? { x: 200, y: 400 } : { x: 550, y: 400 };
      player.velocity = { x: 0, y: 0 };
      player.state = 'IDLE';
      player.isDefending = false;
      player.isGrounded = false;
      player.attackCooldown = 0;
    });
    
    state.effects = [];
  })),
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => set(produce(state => {
    state.players[playerId].position = position;
    state.players[playerId].velocity = velocity;
  })),
  
  updatePlayerState: (playerId: string, mechaState: MechaData['state']) => set(produce(state => {
    state.players[playerId].state = mechaState;
  })),
  
  updatePlayerFacing: (playerId: string, facingRight: boolean) => set(produce(state => {
    state.players[playerId].facingRight = facingRight;
  })),
  
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => set(produce(state => {
    state.players[playerId].isGrounded = isGrounded;
  })),
  
  updatePlayerDefending: (playerId: string, isDefending: boolean) => set(produce(state => {
    state.players[playerId].isDefending = isDefending;
  })),
  
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => set(produce(state => {
    state.players[playerId].attackCooldown = cooldown;
  })),
  
  dealDamage: (targetId: string, damage: number) => set(produce(state => {
    const target = state.players[targetId];
    const actualDamage = target.isDefending ? Math.floor(damage * 0.5) : damage;
    target.health = Math.max(0, target.health - actualDamage);
    
    // 添加受击特效
    state.effects.push({
import { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
  winner: null,
};

// 游戏Store接口
interface GameStore {
  // 状态
  gameState: GameState;
  players: Record<string, MechaData>;
  effects: Effect[];
  
  // 游戏流程控制
  startGame: () => void;
  pauseGame: () => void;
  resumeGame: () => void;
  endGame: (winner: string | null) => void;
  resetGame: () => void;
  nextRound: () => void;
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => void;
  updatePlayerState: (playerId: string, state: MechaData['state']) => void;
  updatePlayerFacing: (playerId: string, facingRight: boolean) => void;
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => void;
  updatePlayerDefending: (playerId: string, isDefending: boolean) => void;
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => void;
  dealDamage: (targetId: string, damage: number, attackerId?: string) => void;
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => void;
  setAnimationFrame: (playerId: string, frame: number) => void;
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => string;
  updateEffects: (deltaTime: number) => void;
  removeEffect: (effectId: string) => void;
  clearEffects: () => void;
  
  // 游戏时间
  tick: (deltaTime: number) => void;
}

// 创建Game Store
export const useGameStore = create<GameStore>((set, get) => ({
  // 初始状态
  gameState: initialGameState,
  players: {
    player1: createInitialMecha('player1', 'BLUE', 200),
    player2: createInitialMecha('player2', 'RED', 550),
  },
  effects: [],
  
  // 游戏流程控制
  startGame: () => set(produce(state => {
    state.gameState.phase = 'PLAYING';
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.winner = null;
  })),
  
  pauseGame: () => set(produce(state => {
    if (state.gameState.phase === 'PLAYING') {
      state.gameState.phase = 'PAUSED';
    }
  })),
  
  resumeGame: () => set(produce(state => {
    if (state.gameState.phase === 'PAUSED') {
      state.gameState.phase = 'PLAYING';
    }
  })),
  
  endGame: (winner: string | null) => set(produce(state => {
    state.gameState.phase = 'RESULT';
    state.gameState.winner = winner;
    if (winner && state.players[winner]) {
      state.players[winner].score += 1;
    }
  })),
  
  resetGame: () => set(produce(state => {
    state.gameState = initialGameState;
    state.players = {
      player1: createInitialMecha('player1', 'BLUE', 200),
      player2: createInitialMecha('player2', 'RED', 550),
    };
    state.effects = [];
  })),
  
  nextRound: () => set(produce(state => {
    state.gameState.round += 1;
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.phase = 'PLAYING';
    state.gameState.winner = null;
    
    // 重置玩家位置和状态
    Object.keys(state.players).forEach(playerId => {
      const player = state.players[playerId];
      player.health = player.maxHealth;
      player.position = playerId === 'player1' ? { x: 200, y: 400 } : { x: 550, y: 400 };
      player.velocity = { x: 0, y: 0 };
      player.state = 'IDLE';
      player.isDefending = false;
      player.isGrounded = false;
      player.attackCooldown = 0;
    });
    
    state.effects = [];
  })),
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => set(produce(state => {
    state.players[playerId].position = position;
    state.players[playerId].velocity = velocity;
  })),
  
  updatePlayerState: (playerId: string, mechaState: MechaData['state']) => set(produce(state => {
    state.players[playerId].state = mechaState;
  })),
  
  updatePlayerFacing: (playerId: string, facingRight: boolean) => set(produce(state => {
    state.players[playerId].facingRight = facingRight;
  })),
  
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => set(produce(state => {
    state.players[playerId].isGrounded = isGrounded;
  })),
  
  updatePlayerDefending: (playerId: string, isDefending: boolean) => set(produce(state => {
    state.players[playerId].isDefending = isDefending;
  })),
  
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => set(produce(state => {
    state.players[playerId].attackCooldown = cooldown;
  })),
  
  dealDamage: (targetId: string, damage: number) => set(produce(state => {
    const target = state.players[targetId];
    const actualDamage = target.isDefending ? Math.floor(damage * 0.5) : damage;
    target.health = Math.max(0, target.health - actualDamage);
    
    // 添加受击特效
    state.effects.push({
      id: `hit-${Dateimport { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
  winner: null,
};

// 游戏Store接口
interface GameStore {
  // 状态
  gameState: GameState;
  players: Record<string, MechaData>;
  effects: Effect[];
  
  // 游戏流程控制
  startGame: () => void;
  pauseGame: () => void;
  resumeGame: () => void;
  endGame: (winner: string | null) => void;
  resetGame: () => void;
  nextRound: () => void;
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => void;
  updatePlayerState: (playerId: string, state: MechaData['state']) => void;
  updatePlayerFacing: (playerId: string, facingRight: boolean) => void;
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => void;
  updatePlayerDefending: (playerId: string, isDefending: boolean) => void;
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => void;
  dealDamage: (targetId: string, damage: number, attackerId?: string) => void;
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => void;
  setAnimationFrame: (playerId: string, frame: number) => void;
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => string;
  updateEffects: (deltaTime: number) => void;
  removeEffect: (effectId: string) => void;
  clearEffects: () => void;
  
  // 游戏时间
  tick: (deltaTime: number) => void;
}

// 创建Game Store
export const useGameStore = create<GameStore>((set, get) => ({
  // 初始状态
  gameState: initialGameState,
  players: {
    player1: createInitialMecha('player1', 'BLUE', 200),
    player2: createInitialMecha('player2', 'RED', 550),
  },
  effects: [],
  
  // 游戏流程控制
  startGame: () => set(produce(state => {
    state.gameState.phase = 'PLAYING';
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.winner = null;
  })),
  
  pauseGame: () => set(produce(state => {
    if (state.gameState.phase === 'PLAYING') {
      state.gameState.phase = 'PAUSED';
    }
  })),
  
  resumeGame: () => set(produce(state => {
    if (state.gameState.phase === 'PAUSED') {
      state.gameState.phase = 'PLAYING';
    }
  })),
  
  endGame: (winner: string | null) => set(produce(state => {
    state.gameState.phase = 'RESULT';
    state.gameState.winner = winner;
    if (winner && state.players[winner]) {
      state.players[winner].score += 1;
    }
  })),
  
  resetGame: () => set(produce(state => {
    state.gameState = initialGameState;
    state.players = {
      player1: createInitialMecha('player1', 'BLUE', 200),
      player2: createInitialMecha('player2', 'RED', 550),
    };
    state.effects = [];
  })),
  
  nextRound: () => set(produce(state => {
    state.gameState.round += 1;
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.phase = 'PLAYING';
    state.gameState.winner = null;
    
    // 重置玩家位置和状态
    Object.keys(state.players).forEach(playerId => {
      const player = state.players[playerId];
      player.health = player.maxHealth;
      player.position = playerId === 'player1' ? { x: 200, y: 400 } : { x: 550, y: 400 };
      player.velocity = { x: 0, y: 0 };
      player.state = 'IDLE';
      player.isDefending = false;
      player.isGrounded = false;
      player.attackCooldown = 0;
    });
    
    state.effects = [];
  })),
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => set(produce(state => {
    state.players[playerId].position = position;
    state.players[playerId].velocity = velocity;
  })),
  
  updatePlayerState: (playerId: string, mechaState: MechaData['state']) => set(produce(state => {
    state.players[playerId].state = mechaState;
  })),
  
  updatePlayerFacing: (playerId: string, facingRight: boolean) => set(produce(state => {
    state.players[playerId].facingRight = facingRight;
  })),
  
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => set(produce(state => {
    state.players[playerId].isGrounded = isGrounded;
  })),
  
  updatePlayerDefending: (playerId: string, isDefending: boolean) => set(produce(state => {
    state.players[playerId].isDefending = isDefending;
  })),
  
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => set(produce(state => {
    state.players[playerId].attackCooldown = cooldown;
  })),
  
  dealDamage: (targetId: string, damage: number) => set(produce(state => {
    const target = state.players[targetId];
    const actualDamage = target.isDefending ? Math.floor(damage * 0.5) : damage;
    target.health = Math.max(0, target.health - actualDamage);
    
    // 添加受击特效
    state.effects.push({
      id: `hit-${Date.now()}`,
      typeimport { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
  winner: null,
};

// 游戏Store接口
interface GameStore {
  // 状态
  gameState: GameState;
  players: Record<string, MechaData>;
  effects: Effect[];
  
  // 游戏流程控制
  startGame: () => void;
  pauseGame: () => void;
  resumeGame: () => void;
  endGame: (winner: string | null) => void;
  resetGame: () => void;
  nextRound: () => void;
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => void;
  updatePlayerState: (playerId: string, state: MechaData['state']) => void;
  updatePlayerFacing: (playerId: string, facingRight: boolean) => void;
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => void;
  updatePlayerDefending: (playerId: string, isDefending: boolean) => void;
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => void;
  dealDamage: (targetId: string, damage: number, attackerId?: string) => void;
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => void;
  setAnimationFrame: (playerId: string, frame: number) => void;
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => string;
  updateEffects: (deltaTime: number) => void;
  removeEffect: (effectId: string) => void;
  clearEffects: () => void;
  
  // 游戏时间
  tick: (deltaTime: number) => void;
}

// 创建Game Store
export const useGameStore = create<GameStore>((set, get) => ({
  // 初始状态
  gameState: initialGameState,
  players: {
    player1: createInitialMecha('player1', 'BLUE', 200),
    player2: createInitialMecha('player2', 'RED', 550),
  },
  effects: [],
  
  // 游戏流程控制
  startGame: () => set(produce(state => {
    state.gameState.phase = 'PLAYING';
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.winner = null;
  })),
  
  pauseGame: () => set(produce(state => {
    if (state.gameState.phase === 'PLAYING') {
      state.gameState.phase = 'PAUSED';
    }
  })),
  
  resumeGame: () => set(produce(state => {
    if (state.gameState.phase === 'PAUSED') {
      state.gameState.phase = 'PLAYING';
    }
  })),
  
  endGame: (winner: string | null) => set(produce(state => {
    state.gameState.phase = 'RESULT';
    state.gameState.winner = winner;
    if (winner && state.players[winner]) {
      state.players[winner].score += 1;
    }
  })),
  
  resetGame: () => set(produce(state => {
    state.gameState = initialGameState;
    state.players = {
      player1: createInitialMecha('player1', 'BLUE', 200),
      player2: createInitialMecha('player2', 'RED', 550),
    };
    state.effects = [];
  })),
  
  nextRound: () => set(produce(state => {
    state.gameState.round += 1;
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.phase = 'PLAYING';
    state.gameState.winner = null;
    
    // 重置玩家位置和状态
    Object.keys(state.players).forEach(playerId => {
      const player = state.players[playerId];
      player.health = player.maxHealth;
      player.position = playerId === 'player1' ? { x: 200, y: 400 } : { x: 550, y: 400 };
      player.velocity = { x: 0, y: 0 };
      player.state = 'IDLE';
      player.isDefending = false;
      player.isGrounded = false;
      player.attackCooldown = 0;
    });
    
    state.effects = [];
  })),
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => set(produce(state => {
    state.players[playerId].position = position;
    state.players[playerId].velocity = velocity;
  })),
  
  updatePlayerState: (playerId: string, mechaState: MechaData['state']) => set(produce(state => {
    state.players[playerId].state = mechaState;
  })),
  
  updatePlayerFacing: (playerId: string, facingRight: boolean) => set(produce(state => {
    state.players[playerId].facingRight = facingRight;
  })),
  
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => set(produce(state => {
    state.players[playerId].isGrounded = isGrounded;
  })),
  
  updatePlayerDefending: (playerId: string, isDefending: boolean) => set(produce(state => {
    state.players[playerId].isDefending = isDefending;
  })),
  
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => set(produce(state => {
    state.players[playerId].attackCooldown = cooldown;
  })),
  
  dealDamage: (targetId: string, damage: number) => set(produce(state => {
    const target = state.players[targetId];
    const actualDamage = target.isDefending ? Math.floor(damage * 0.5) : damage;
    target.health = Math.max(0, target.health - actualDamage);
    
    // 添加受击特效
    state.effects.push({
      id: `hit-${Date.now()}`,
      type: 'HIT',
      positionimport { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
  winner: null,
};

// 游戏Store接口
interface GameStore {
  // 状态
  gameState: GameState;
  players: Record<string, MechaData>;
  effects: Effect[];
  
  // 游戏流程控制
  startGame: () => void;
  pauseGame: () => void;
  resumeGame: () => void;
  endGame: (winner: string | null) => void;
  resetGame: () => void;
  nextRound: () => void;
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => void;
  updatePlayerState: (playerId: string, state: MechaData['state']) => void;
  updatePlayerFacing: (playerId: string, facingRight: boolean) => void;
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => void;
  updatePlayerDefending: (playerId: string, isDefending: boolean) => void;
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => void;
  dealDamage: (targetId: string, damage: number, attackerId?: string) => void;
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => void;
  setAnimationFrame: (playerId: string, frame: number) => void;
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => string;
  updateEffects: (deltaTime: number) => void;
  removeEffect: (effectId: string) => void;
  clearEffects: () => void;
  
  // 游戏时间
  tick: (deltaTime: number) => void;
}

// 创建Game Store
export const useGameStore = create<GameStore>((set, get) => ({
  // 初始状态
  gameState: initialGameState,
  players: {
    player1: createInitialMecha('player1', 'BLUE', 200),
    player2: createInitialMecha('player2', 'RED', 550),
  },
  effects: [],
  
  // 游戏流程控制
  startGame: () => set(produce(state => {
    state.gameState.phase = 'PLAYING';
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.winner = null;
  })),
  
  pauseGame: () => set(produce(state => {
    if (state.gameState.phase === 'PLAYING') {
      state.gameState.phase = 'PAUSED';
    }
  })),
  
  resumeGame: () => set(produce(state => {
    if (state.gameState.phase === 'PAUSED') {
      state.gameState.phase = 'PLAYING';
    }
  })),
  
  endGame: (winner: string | null) => set(produce(state => {
    state.gameState.phase = 'RESULT';
    state.gameState.winner = winner;
    if (winner && state.players[winner]) {
      state.players[winner].score += 1;
    }
  })),
  
  resetGame: () => set(produce(state => {
    state.gameState = initialGameState;
    state.players = {
      player1: createInitialMecha('player1', 'BLUE', 200),
      player2: createInitialMecha('player2', 'RED', 550),
    };
    state.effects = [];
  })),
  
  nextRound: () => set(produce(state => {
    state.gameState.round += 1;
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.phase = 'PLAYING';
    state.gameState.winner = null;
    
    // 重置玩家位置和状态
    Object.keys(state.players).forEach(playerId => {
      const player = state.players[playerId];
      player.health = player.maxHealth;
      player.position = playerId === 'player1' ? { x: 200, y: 400 } : { x: 550, y: 400 };
      player.velocity = { x: 0, y: 0 };
      player.state = 'IDLE';
      player.isDefending = false;
      player.isGrounded = false;
      player.attackCooldown = 0;
    });
    
    state.effects = [];
  })),
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => set(produce(state => {
    state.players[playerId].position = position;
    state.players[playerId].velocity = velocity;
  })),
  
  updatePlayerState: (playerId: string, mechaState: MechaData['state']) => set(produce(state => {
    state.players[playerId].state = mechaState;
  })),
  
  updatePlayerFacing: (playerId: string, facingRight: boolean) => set(produce(state => {
    state.players[playerId].facingRight = facingRight;
  })),
  
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => set(produce(state => {
    state.players[playerId].isGrounded = isGrounded;
  })),
  
  updatePlayerDefending: (playerId: string, isDefending: boolean) => set(produce(state => {
    state.players[playerId].isDefending = isDefending;
  })),
  
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => set(produce(state => {
    state.players[playerId].attackCooldown = cooldown;
  })),
  
  dealDamage: (targetId: string, damage: number) => set(produce(state => {
    const target = state.players[targetId];
    const actualDamage = target.isDefending ? Math.floor(damage * 0.5) : damage;
    target.health = Math.max(0, target.health - actualDamage);
    
    // 添加受击特效
    state.effects.push({
      id: `hit-${Date.now()}`,
      type: 'HIT',
      position: { ...target.position },
import { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
  winner: null,
};

// 游戏Store接口
interface GameStore {
  // 状态
  gameState: GameState;
  players: Record<string, MechaData>;
  effects: Effect[];
  
  // 游戏流程控制
  startGame: () => void;
  pauseGame: () => void;
  resumeGame: () => void;
  endGame: (winner: string | null) => void;
  resetGame: () => void;
  nextRound: () => void;
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => void;
  updatePlayerState: (playerId: string, state: MechaData['state']) => void;
  updatePlayerFacing: (playerId: string, facingRight: boolean) => void;
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => void;
  updatePlayerDefending: (playerId: string, isDefending: boolean) => void;
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => void;
  dealDamage: (targetId: string, damage: number, attackerId?: string) => void;
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => void;
  setAnimationFrame: (playerId: string, frame: number) => void;
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => string;
  updateEffects: (deltaTime: number) => void;
  removeEffect: (effectId: string) => void;
  clearEffects: () => void;
  
  // 游戏时间
  tick: (deltaTime: number) => void;
}

// 创建Game Store
export const useGameStore = create<GameStore>((set, get) => ({
  // 初始状态
  gameState: initialGameState,
  players: {
    player1: createInitialMecha('player1', 'BLUE', 200),
    player2: createInitialMecha('player2', 'RED', 550),
  },
  effects: [],
  
  // 游戏流程控制
  startGame: () => set(produce(state => {
    state.gameState.phase = 'PLAYING';
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.winner = null;
  })),
  
  pauseGame: () => set(produce(state => {
    if (state.gameState.phase === 'PLAYING') {
      state.gameState.phase = 'PAUSED';
    }
  })),
  
  resumeGame: () => set(produce(state => {
    if (state.gameState.phase === 'PAUSED') {
      state.gameState.phase = 'PLAYING';
    }
  })),
  
  endGame: (winner: string | null) => set(produce(state => {
    state.gameState.phase = 'RESULT';
    state.gameState.winner = winner;
    if (winner && state.players[winner]) {
      state.players[winner].score += 1;
    }
  })),
  
  resetGame: () => set(produce(state => {
    state.gameState = initialGameState;
    state.players = {
      player1: createInitialMecha('player1', 'BLUE', 200),
      player2: createInitialMecha('player2', 'RED', 550),
    };
    state.effects = [];
  })),
  
  nextRound: () => set(produce(state => {
    state.gameState.round += 1;
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.phase = 'PLAYING';
    state.gameState.winner = null;
    
    // 重置玩家位置和状态
    Object.keys(state.players).forEach(playerId => {
      const player = state.players[playerId];
      player.health = player.maxHealth;
      player.position = playerId === 'player1' ? { x: 200, y: 400 } : { x: 550, y: 400 };
      player.velocity = { x: 0, y: 0 };
      player.state = 'IDLE';
      player.isDefending = false;
      player.isGrounded = false;
      player.attackCooldown = 0;
    });
    
    state.effects = [];
  })),
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => set(produce(state => {
    state.players[playerId].position = position;
    state.players[playerId].velocity = velocity;
  })),
  
  updatePlayerState: (playerId: string, mechaState: MechaData['state']) => set(produce(state => {
    state.players[playerId].state = mechaState;
  })),
  
  updatePlayerFacing: (playerId: string, facingRight: boolean) => set(produce(state => {
    state.players[playerId].facingRight = facingRight;
  })),
  
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => set(produce(state => {
    state.players[playerId].isGrounded = isGrounded;
  })),
  
  updatePlayerDefending: (playerId: string, isDefending: boolean) => set(produce(state => {
    state.players[playerId].isDefending = isDefending;
  })),
  
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => set(produce(state => {
    state.players[playerId].attackCooldown = cooldown;
  })),
  
  dealDamage: (targetId: string, damage: number) => set(produce(state => {
    const target = state.players[targetId];
    const actualDamage = target.isDefending ? Math.floor(damage * 0.5) : damage;
    target.health = Math.max(0, target.health - actualDamage);
    
    // 添加受击特效
    state.effects.push({
      id: `hit-${Date.now()}`,
      type: 'HIT',
      position: { ...target.position },
      duration: 300,
import { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
  winner: null,
};

// 游戏Store接口
interface GameStore {
  // 状态
  gameState: GameState;
  players: Record<string, MechaData>;
  effects: Effect[];
  
  // 游戏流程控制
  startGame: () => void;
  pauseGame: () => void;
  resumeGame: () => void;
  endGame: (winner: string | null) => void;
  resetGame: () => void;
  nextRound: () => void;
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => void;
  updatePlayerState: (playerId: string, state: MechaData['state']) => void;
  updatePlayerFacing: (playerId: string, facingRight: boolean) => void;
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => void;
  updatePlayerDefending: (playerId: string, isDefending: boolean) => void;
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => void;
  dealDamage: (targetId: string, damage: number, attackerId?: string) => void;
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => void;
  setAnimationFrame: (playerId: string, frame: number) => void;
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => string;
  updateEffects: (deltaTime: number) => void;
  removeEffect: (effectId: string) => void;
  clearEffects: () => void;
  
  // 游戏时间
  tick: (deltaTime: number) => void;
}

// 创建Game Store
export const useGameStore = create<GameStore>((set, get) => ({
  // 初始状态
  gameState: initialGameState,
  players: {
    player1: createInitialMecha('player1', 'BLUE', 200),
    player2: createInitialMecha('player2', 'RED', 550),
  },
  effects: [],
  
  // 游戏流程控制
  startGame: () => set(produce(state => {
    state.gameState.phase = 'PLAYING';
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.winner = null;
  })),
  
  pauseGame: () => set(produce(state => {
    if (state.gameState.phase === 'PLAYING') {
      state.gameState.phase = 'PAUSED';
    }
  })),
  
  resumeGame: () => set(produce(state => {
    if (state.gameState.phase === 'PAUSED') {
      state.gameState.phase = 'PLAYING';
    }
  })),
  
  endGame: (winner: string | null) => set(produce(state => {
    state.gameState.phase = 'RESULT';
    state.gameState.winner = winner;
    if (winner && state.players[winner]) {
      state.players[winner].score += 1;
    }
  })),
  
  resetGame: () => set(produce(state => {
    state.gameState = initialGameState;
    state.players = {
      player1: createInitialMecha('player1', 'BLUE', 200),
      player2: createInitialMecha('player2', 'RED', 550),
    };
    state.effects = [];
  })),
  
  nextRound: () => set(produce(state => {
    state.gameState.round += 1;
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.phase = 'PLAYING';
    state.gameState.winner = null;
    
    // 重置玩家位置和状态
    Object.keys(state.players).forEach(playerId => {
      const player = state.players[playerId];
      player.health = player.maxHealth;
      player.position = playerId === 'player1' ? { x: 200, y: 400 } : { x: 550, y: 400 };
      player.velocity = { x: 0, y: 0 };
      player.state = 'IDLE';
      player.isDefending = false;
      player.isGrounded = false;
      player.attackCooldown = 0;
    });
    
    state.effects = [];
  })),
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => set(produce(state => {
    state.players[playerId].position = position;
    state.players[playerId].velocity = velocity;
  })),
  
  updatePlayerState: (playerId: string, mechaState: MechaData['state']) => set(produce(state => {
    state.players[playerId].state = mechaState;
  })),
  
  updatePlayerFacing: (playerId: string, facingRight: boolean) => set(produce(state => {
    state.players[playerId].facingRight = facingRight;
  })),
  
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => set(produce(state => {
    state.players[playerId].isGrounded = isGrounded;
  })),
  
  updatePlayerDefending: (playerId: string, isDefending: boolean) => set(produce(state => {
    state.players[playerId].isDefending = isDefending;
  })),
  
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => set(produce(state => {
    state.players[playerId].attackCooldown = cooldown;
  })),
  
  dealDamage: (targetId: string, damage: number) => set(produce(state => {
    const target = state.players[targetId];
    const actualDamage = target.isDefending ? Math.floor(damage * 0.5) : damage;
    target.health = Math.max(0, target.health - actualDamage);
    
    // 添加受击特效
    state.effects.push({
      id: `hit-${Date.now()}`,
      type: 'HIT',
      position: { ...target.position },
      duration: 300,
      timer: 0,
import { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
  winner: null,
};

// 游戏Store接口
interface GameStore {
  // 状态
  gameState: GameState;
  players: Record<string, MechaData>;
  effects: Effect[];
  
  // 游戏流程控制
  startGame: () => void;
  pauseGame: () => void;
  resumeGame: () => void;
  endGame: (winner: string | null) => void;
  resetGame: () => void;
  nextRound: () => void;
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => void;
  updatePlayerState: (playerId: string, state: MechaData['state']) => void;
  updatePlayerFacing: (playerId: string, facingRight: boolean) => void;
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => void;
  updatePlayerDefending: (playerId: string, isDefending: boolean) => void;
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => void;
  dealDamage: (targetId: string, damage: number, attackerId?: string) => void;
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => void;
  setAnimationFrame: (playerId: string, frame: number) => void;
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => string;
  updateEffects: (deltaTime: number) => void;
  removeEffect: (effectId: string) => void;
  clearEffects: () => void;
  
  // 游戏时间
  tick: (deltaTime: number) => void;
}

// 创建Game Store
export const useGameStore = create<GameStore>((set, get) => ({
  // 初始状态
  gameState: initialGameState,
  players: {
    player1: createInitialMecha('player1', 'BLUE', 200),
    player2: createInitialMecha('player2', 'RED', 550),
  },
  effects: [],
  
  // 游戏流程控制
  startGame: () => set(produce(state => {
    state.gameState.phase = 'PLAYING';
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.winner = null;
  })),
  
  pauseGame: () => set(produce(state => {
    if (state.gameState.phase === 'PLAYING') {
      state.gameState.phase = 'PAUSED';
    }
  })),
  
  resumeGame: () => set(produce(state => {
    if (state.gameState.phase === 'PAUSED') {
      state.gameState.phase = 'PLAYING';
    }
  })),
  
  endGame: (winner: string | null) => set(produce(state => {
    state.gameState.phase = 'RESULT';
    state.gameState.winner = winner;
    if (winner && state.players[winner]) {
      state.players[winner].score += 1;
    }
  })),
  
  resetGame: () => set(produce(state => {
    state.gameState = initialGameState;
    state.players = {
      player1: createInitialMecha('player1', 'BLUE', 200),
      player2: createInitialMecha('player2', 'RED', 550),
    };
    state.effects = [];
  })),
  
  nextRound: () => set(produce(state => {
    state.gameState.round += 1;
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.phase = 'PLAYING';
    state.gameState.winner = null;
    
    // 重置玩家位置和状态
    Object.keys(state.players).forEach(playerId => {
      const player = state.players[playerId];
      player.health = player.maxHealth;
      player.position = playerId === 'player1' ? { x: 200, y: 400 } : { x: 550, y: 400 };
      player.velocity = { x: 0, y: 0 };
      player.state = 'IDLE';
      player.isDefending = false;
      player.isGrounded = false;
      player.attackCooldown = 0;
    });
    
    state.effects = [];
  })),
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => set(produce(state => {
    state.players[playerId].position = position;
    state.players[playerId].velocity = velocity;
  })),
  
  updatePlayerState: (playerId: string, mechaState: MechaData['state']) => set(produce(state => {
    state.players[playerId].state = mechaState;
  })),
  
  updatePlayerFacing: (playerId: string, facingRight: boolean) => set(produce(state => {
    state.players[playerId].facingRight = facingRight;
  })),
  
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => set(produce(state => {
    state.players[playerId].isGrounded = isGrounded;
  })),
  
  updatePlayerDefending: (playerId: string, isDefending: boolean) => set(produce(state => {
    state.players[playerId].isDefending = isDefending;
  })),
  
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => set(produce(state => {
    state.players[playerId].attackCooldown = cooldown;
  })),
  
  dealDamage: (targetId: string, damage: number) => set(produce(state => {
    const target = state.players[targetId];
    const actualDamage = target.isDefending ? Math.floor(damage * 0.5) : damage;
    target.health = Math.max(0, target.health - actualDamage);
    
    // 添加受击特效
    state.effects.push({
      id: `hit-${Date.now()}`,
      type: 'HIT',
      position: { ...target.position },
      duration: 300,
      timer: 0,
      frame: 0,
    }import { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
  winner: null,
};

// 游戏Store接口
interface GameStore {
  // 状态
  gameState: GameState;
  players: Record<string, MechaData>;
  effects: Effect[];
  
  // 游戏流程控制
  startGame: () => void;
  pauseGame: () => void;
  resumeGame: () => void;
  endGame: (winner: string | null) => void;
  resetGame: () => void;
  nextRound: () => void;
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => void;
  updatePlayerState: (playerId: string, state: MechaData['state']) => void;
  updatePlayerFacing: (playerId: string, facingRight: boolean) => void;
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => void;
  updatePlayerDefending: (playerId: string, isDefending: boolean) => void;
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => void;
  dealDamage: (targetId: string, damage: number, attackerId?: string) => void;
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => void;
  setAnimationFrame: (playerId: string, frame: number) => void;
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => string;
  updateEffects: (deltaTime: number) => void;
  removeEffect: (effectId: string) => void;
  clearEffects: () => void;
  
  // 游戏时间
  tick: (deltaTime: number) => void;
}

// 创建Game Store
export const useGameStore = create<GameStore>((set, get) => ({
  // 初始状态
  gameState: initialGameState,
  players: {
    player1: createInitialMecha('player1', 'BLUE', 200),
    player2: createInitialMecha('player2', 'RED', 550),
  },
  effects: [],
  
  // 游戏流程控制
  startGame: () => set(produce(state => {
    state.gameState.phase = 'PLAYING';
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.winner = null;
  })),
  
  pauseGame: () => set(produce(state => {
    if (state.gameState.phase === 'PLAYING') {
      state.gameState.phase = 'PAUSED';
    }
  })),
  
  resumeGame: () => set(produce(state => {
    if (state.gameState.phase === 'PAUSED') {
      state.gameState.phase = 'PLAYING';
    }
  })),
  
  endGame: (winner: string | null) => set(produce(state => {
    state.gameState.phase = 'RESULT';
    state.gameState.winner = winner;
    if (winner && state.players[winner]) {
      state.players[winner].score += 1;
    }
  })),
  
  resetGame: () => set(produce(state => {
    state.gameState = initialGameState;
    state.players = {
      player1: createInitialMecha('player1', 'BLUE', 200),
      player2: createInitialMecha('player2', 'RED', 550),
    };
    state.effects = [];
  })),
  
  nextRound: () => set(produce(state => {
    state.gameState.round += 1;
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.phase = 'PLAYING';
    state.gameState.winner = null;
    
    // 重置玩家位置和状态
    Object.keys(state.players).forEach(playerId => {
      const player = state.players[playerId];
      player.health = player.maxHealth;
      player.position = playerId === 'player1' ? { x: 200, y: 400 } : { x: 550, y: 400 };
      player.velocity = { x: 0, y: 0 };
      player.state = 'IDLE';
      player.isDefending = false;
      player.isGrounded = false;
      player.attackCooldown = 0;
    });
    
    state.effects = [];
  })),
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => set(produce(state => {
    state.players[playerId].position = position;
    state.players[playerId].velocity = velocity;
  })),
  
  updatePlayerState: (playerId: string, mechaState: MechaData['state']) => set(produce(state => {
    state.players[playerId].state = mechaState;
  })),
  
  updatePlayerFacing: (playerId: string, facingRight: boolean) => set(produce(state => {
    state.players[playerId].facingRight = facingRight;
  })),
  
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => set(produce(state => {
    state.players[playerId].isGrounded = isGrounded;
  })),
  
  updatePlayerDefending: (playerId: string, isDefending: boolean) => set(produce(state => {
    state.players[playerId].isDefending = isDefending;
  })),
  
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => set(produce(state => {
    state.players[playerId].attackCooldown = cooldown;
  })),
  
  dealDamage: (targetId: string, damage: number) => set(produce(state => {
    const target = state.players[targetId];
    const actualDamage = target.isDefending ? Math.floor(damage * 0.5) : damage;
    target.health = Math.max(0, target.health - actualDamage);
    
    // 添加受击特效
    state.effects.push({
      id: `hit-${Date.now()}`,
      type: 'HIT',
      position: { ...target.position },
      duration: 300,
      timer: 0,
      frame: 0,
    });
    
    //import { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
  winner: null,
};

// 游戏Store接口
interface GameStore {
  // 状态
  gameState: GameState;
  players: Record<string, MechaData>;
  effects: Effect[];
  
  // 游戏流程控制
  startGame: () => void;
  pauseGame: () => void;
  resumeGame: () => void;
  endGame: (winner: string | null) => void;
  resetGame: () => void;
  nextRound: () => void;
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => void;
  updatePlayerState: (playerId: string, state: MechaData['state']) => void;
  updatePlayerFacing: (playerId: string, facingRight: boolean) => void;
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => void;
  updatePlayerDefending: (playerId: string, isDefending: boolean) => void;
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => void;
  dealDamage: (targetId: string, damage: number, attackerId?: string) => void;
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => void;
  setAnimationFrame: (playerId: string, frame: number) => void;
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => string;
  updateEffects: (deltaTime: number) => void;
  removeEffect: (effectId: string) => void;
  clearEffects: () => void;
  
  // 游戏时间
  tick: (deltaTime: number) => void;
}

// 创建Game Store
export const useGameStore = create<GameStore>((set, get) => ({
  // 初始状态
  gameState: initialGameState,
  players: {
    player1: createInitialMecha('player1', 'BLUE', 200),
    player2: createInitialMecha('player2', 'RED', 550),
  },
  effects: [],
  
  // 游戏流程控制
  startGame: () => set(produce(state => {
    state.gameState.phase = 'PLAYING';
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.winner = null;
  })),
  
  pauseGame: () => set(produce(state => {
    if (state.gameState.phase === 'PLAYING') {
      state.gameState.phase = 'PAUSED';
    }
  })),
  
  resumeGame: () => set(produce(state => {
    if (state.gameState.phase === 'PAUSED') {
      state.gameState.phase = 'PLAYING';
    }
  })),
  
  endGame: (winner: string | null) => set(produce(state => {
    state.gameState.phase = 'RESULT';
    state.gameState.winner = winner;
    if (winner && state.players[winner]) {
      state.players[winner].score += 1;
    }
  })),
  
  resetGame: () => set(produce(state => {
    state.gameState = initialGameState;
    state.players = {
      player1: createInitialMecha('player1', 'BLUE', 200),
      player2: createInitialMecha('player2', 'RED', 550),
    };
    state.effects = [];
  })),
  
  nextRound: () => set(produce(state => {
    state.gameState.round += 1;
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.phase = 'PLAYING';
    state.gameState.winner = null;
    
    // 重置玩家位置和状态
    Object.keys(state.players).forEach(playerId => {
      const player = state.players[playerId];
      player.health = player.maxHealth;
      player.position = playerId === 'player1' ? { x: 200, y: 400 } : { x: 550, y: 400 };
      player.velocity = { x: 0, y: 0 };
      player.state = 'IDLE';
      player.isDefending = false;
      player.isGrounded = false;
      player.attackCooldown = 0;
    });
    
    state.effects = [];
  })),
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => set(produce(state => {
    state.players[playerId].position = position;
    state.players[playerId].velocity = velocity;
  })),
  
  updatePlayerState: (playerId: string, mechaState: MechaData['state']) => set(produce(state => {
    state.players[playerId].state = mechaState;
  })),
  
  updatePlayerFacing: (playerId: string, facingRight: boolean) => set(produce(state => {
    state.players[playerId].facingRight = facingRight;
  })),
  
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => set(produce(state => {
    state.players[playerId].isGrounded = isGrounded;
  })),
  
  updatePlayerDefending: (playerId: string, isDefending: boolean) => set(produce(state => {
    state.players[playerId].isDefending = isDefending;
  })),
  
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => set(produce(state => {
    state.players[playerId].attackCooldown = cooldown;
  })),
  
  dealDamage: (targetId: string, damage: number) => set(produce(state => {
    const target = state.players[targetId];
    const actualDamage = target.isDefending ? Math.floor(damage * 0.5) : damage;
    target.health = Math.max(0, target.health - actualDamage);
    
    // 添加受击特效
    state.effects.push({
      id: `hit-${Date.now()}`,
      type: 'HIT',
      position: { ...target.position },
      duration: 300,
      timer: 0,
      frame: 0,
    });
    
    // 检查游戏结束
    ifimport { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
  winner: null,
};

// 游戏Store接口
interface GameStore {
  // 状态
  gameState: GameState;
  players: Record<string, MechaData>;
  effects: Effect[];
  
  // 游戏流程控制
  startGame: () => void;
  pauseGame: () => void;
  resumeGame: () => void;
  endGame: (winner: string | null) => void;
  resetGame: () => void;
  nextRound: () => void;
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => void;
  updatePlayerState: (playerId: string, state: MechaData['state']) => void;
  updatePlayerFacing: (playerId: string, facingRight: boolean) => void;
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => void;
  updatePlayerDefending: (playerId: string, isDefending: boolean) => void;
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => void;
  dealDamage: (targetId: string, damage: number, attackerId?: string) => void;
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => void;
  setAnimationFrame: (playerId: string, frame: number) => void;
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => string;
  updateEffects: (deltaTime: number) => void;
  removeEffect: (effectId: string) => void;
  clearEffects: () => void;
  
  // 游戏时间
  tick: (deltaTime: number) => void;
}

// 创建Game Store
export const useGameStore = create<GameStore>((set, get) => ({
  // 初始状态
  gameState: initialGameState,
  players: {
    player1: createInitialMecha('player1', 'BLUE', 200),
    player2: createInitialMecha('player2', 'RED', 550),
  },
  effects: [],
  
  // 游戏流程控制
  startGame: () => set(produce(state => {
    state.gameState.phase = 'PLAYING';
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.winner = null;
  })),
  
  pauseGame: () => set(produce(state => {
    if (state.gameState.phase === 'PLAYING') {
      state.gameState.phase = 'PAUSED';
    }
  })),
  
  resumeGame: () => set(produce(state => {
    if (state.gameState.phase === 'PAUSED') {
      state.gameState.phase = 'PLAYING';
    }
  })),
  
  endGame: (winner: string | null) => set(produce(state => {
    state.gameState.phase = 'RESULT';
    state.gameState.winner = winner;
    if (winner && state.players[winner]) {
      state.players[winner].score += 1;
    }
  })),
  
  resetGame: () => set(produce(state => {
    state.gameState = initialGameState;
    state.players = {
      player1: createInitialMecha('player1', 'BLUE', 200),
      player2: createInitialMecha('player2', 'RED', 550),
    };
    state.effects = [];
  })),
  
  nextRound: () => set(produce(state => {
    state.gameState.round += 1;
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.phase = 'PLAYING';
    state.gameState.winner = null;
    
    // 重置玩家位置和状态
    Object.keys(state.players).forEach(playerId => {
      const player = state.players[playerId];
      player.health = player.maxHealth;
      player.position = playerId === 'player1' ? { x: 200, y: 400 } : { x: 550, y: 400 };
      player.velocity = { x: 0, y: 0 };
      player.state = 'IDLE';
      player.isDefending = false;
      player.isGrounded = false;
      player.attackCooldown = 0;
    });
    
    state.effects = [];
  })),
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => set(produce(state => {
    state.players[playerId].position = position;
    state.players[playerId].velocity = velocity;
  })),
  
  updatePlayerState: (playerId: string, mechaState: MechaData['state']) => set(produce(state => {
    state.players[playerId].state = mechaState;
  })),
  
  updatePlayerFacing: (playerId: string, facingRight: boolean) => set(produce(state => {
    state.players[playerId].facingRight = facingRight;
  })),
  
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => set(produce(state => {
    state.players[playerId].isGrounded = isGrounded;
  })),
  
  updatePlayerDefending: (playerId: string, isDefending: boolean) => set(produce(state => {
    state.players[playerId].isDefending = isDefending;
  })),
  
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => set(produce(state => {
    state.players[playerId].attackCooldown = cooldown;
  })),
  
  dealDamage: (targetId: string, damage: number) => set(produce(state => {
    const target = state.players[targetId];
    const actualDamage = target.isDefending ? Math.floor(damage * 0.5) : damage;
    target.health = Math.max(0, target.health - actualDamage);
    
    // 添加受击特效
    state.effects.push({
      id: `hit-${Date.now()}`,
      type: 'HIT',
      position: { ...target.position },
      duration: 300,
      timer: 0,
      frame: 0,
    });
    
    // 检查游戏结束
    if (target.health === 0)import { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
  winner: null,
};

// 游戏Store接口
interface GameStore {
  // 状态
  gameState: GameState;
  players: Record<string, MechaData>;
  effects: Effect[];
  
  // 游戏流程控制
  startGame: () => void;
  pauseGame: () => void;
  resumeGame: () => void;
  endGame: (winner: string | null) => void;
  resetGame: () => void;
  nextRound: () => void;
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => void;
  updatePlayerState: (playerId: string, state: MechaData['state']) => void;
  updatePlayerFacing: (playerId: string, facingRight: boolean) => void;
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => void;
  updatePlayerDefending: (playerId: string, isDefending: boolean) => void;
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => void;
  dealDamage: (targetId: string, damage: number, attackerId?: string) => void;
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => void;
  setAnimationFrame: (playerId: string, frame: number) => void;
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => string;
  updateEffects: (deltaTime: number) => void;
  removeEffect: (effectId: string) => void;
  clearEffects: () => void;
  
  // 游戏时间
  tick: (deltaTime: number) => void;
}

// 创建Game Store
export const useGameStore = create<GameStore>((set, get) => ({
  // 初始状态
  gameState: initialGameState,
  players: {
    player1: createInitialMecha('player1', 'BLUE', 200),
    player2: createInitialMecha('player2', 'RED', 550),
  },
  effects: [],
  
  // 游戏流程控制
  startGame: () => set(produce(state => {
    state.gameState.phase = 'PLAYING';
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.winner = null;
  })),
  
  pauseGame: () => set(produce(state => {
    if (state.gameState.phase === 'PLAYING') {
      state.gameState.phase = 'PAUSED';
    }
  })),
  
  resumeGame: () => set(produce(state => {
    if (state.gameState.phase === 'PAUSED') {
      state.gameState.phase = 'PLAYING';
    }
  })),
  
  endGame: (winner: string | null) => set(produce(state => {
    state.gameState.phase = 'RESULT';
    state.gameState.winner = winner;
    if (winner && state.players[winner]) {
      state.players[winner].score += 1;
    }
  })),
  
  resetGame: () => set(produce(state => {
    state.gameState = initialGameState;
    state.players = {
      player1: createInitialMecha('player1', 'BLUE', 200),
      player2: createInitialMecha('player2', 'RED', 550),
    };
    state.effects = [];
  })),
  
  nextRound: () => set(produce(state => {
    state.gameState.round += 1;
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.phase = 'PLAYING';
    state.gameState.winner = null;
    
    // 重置玩家位置和状态
    Object.keys(state.players).forEach(playerId => {
      const player = state.players[playerId];
      player.health = player.maxHealth;
      player.position = playerId === 'player1' ? { x: 200, y: 400 } : { x: 550, y: 400 };
      player.velocity = { x: 0, y: 0 };
      player.state = 'IDLE';
      player.isDefending = false;
      player.isGrounded = false;
      player.attackCooldown = 0;
    });
    
    state.effects = [];
  })),
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => set(produce(state => {
    state.players[playerId].position = position;
    state.players[playerId].velocity = velocity;
  })),
  
  updatePlayerState: (playerId: string, mechaState: MechaData['state']) => set(produce(state => {
    state.players[playerId].state = mechaState;
  })),
  
  updatePlayerFacing: (playerId: string, facingRight: boolean) => set(produce(state => {
    state.players[playerId].facingRight = facingRight;
  })),
  
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => set(produce(state => {
    state.players[playerId].isGrounded = isGrounded;
  })),
  
  updatePlayerDefending: (playerId: string, isDefending: boolean) => set(produce(state => {
    state.players[playerId].isDefending = isDefending;
  })),
  
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => set(produce(state => {
    state.players[playerId].attackCooldown = cooldown;
  })),
  
  dealDamage: (targetId: string, damage: number) => set(produce(state => {
    const target = state.players[targetId];
    const actualDamage = target.isDefending ? Math.floor(damage * 0.5) : damage;
    target.health = Math.max(0, target.health - actualDamage);
    
    // 添加受击特效
    state.effects.push({
      id: `hit-${Date.now()}`,
      type: 'HIT',
      position: { ...target.position },
      duration: 300,
      timer: 0,
      frame: 0,
    });
    
    // 检查游戏结束
    if (target.health === 0) {
      const winnerId =import { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
  winner: null,
};

// 游戏Store接口
interface GameStore {
  // 状态
  gameState: GameState;
  players: Record<string, MechaData>;
  effects: Effect[];
  
  // 游戏流程控制
  startGame: () => void;
  pauseGame: () => void;
  resumeGame: () => void;
  endGame: (winner: string | null) => void;
  resetGame: () => void;
  nextRound: () => void;
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => void;
  updatePlayerState: (playerId: string, state: MechaData['state']) => void;
  updatePlayerFacing: (playerId: string, facingRight: boolean) => void;
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => void;
  updatePlayerDefending: (playerId: string, isDefending: boolean) => void;
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => void;
  dealDamage: (targetId: string, damage: number, attackerId?: string) => void;
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => void;
  setAnimationFrame: (playerId: string, frame: number) => void;
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => string;
  updateEffects: (deltaTime: number) => void;
  removeEffect: (effectId: string) => void;
  clearEffects: () => void;
  
  // 游戏时间
  tick: (deltaTime: number) => void;
}

// 创建Game Store
export const useGameStore = create<GameStore>((set, get) => ({
  // 初始状态
  gameState: initialGameState,
  players: {
    player1: createInitialMecha('player1', 'BLUE', 200),
    player2: createInitialMecha('player2', 'RED', 550),
  },
  effects: [],
  
  // 游戏流程控制
  startGame: () => set(produce(state => {
    state.gameState.phase = 'PLAYING';
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.winner = null;
  })),
  
  pauseGame: () => set(produce(state => {
    if (state.gameState.phase === 'PLAYING') {
      state.gameState.phase = 'PAUSED';
    }
  })),
  
  resumeGame: () => set(produce(state => {
    if (state.gameState.phase === 'PAUSED') {
      state.gameState.phase = 'PLAYING';
    }
  })),
  
  endGame: (winner: string | null) => set(produce(state => {
    state.gameState.phase = 'RESULT';
    state.gameState.winner = winner;
    if (winner && state.players[winner]) {
      state.players[winner].score += 1;
    }
  })),
  
  resetGame: () => set(produce(state => {
    state.gameState = initialGameState;
    state.players = {
      player1: createInitialMecha('player1', 'BLUE', 200),
      player2: createInitialMecha('player2', 'RED', 550),
    };
    state.effects = [];
  })),
  
  nextRound: () => set(produce(state => {
    state.gameState.round += 1;
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.phase = 'PLAYING';
    state.gameState.winner = null;
    
    // 重置玩家位置和状态
    Object.keys(state.players).forEach(playerId => {
      const player = state.players[playerId];
      player.health = player.maxHealth;
      player.position = playerId === 'player1' ? { x: 200, y: 400 } : { x: 550, y: 400 };
      player.velocity = { x: 0, y: 0 };
      player.state = 'IDLE';
      player.isDefending = false;
      player.isGrounded = false;
      player.attackCooldown = 0;
    });
    
    state.effects = [];
  })),
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => set(produce(state => {
    state.players[playerId].position = position;
    state.players[playerId].velocity = velocity;
  })),
  
  updatePlayerState: (playerId: string, mechaState: MechaData['state']) => set(produce(state => {
    state.players[playerId].state = mechaState;
  })),
  
  updatePlayerFacing: (playerId: string, facingRight: boolean) => set(produce(state => {
    state.players[playerId].facingRight = facingRight;
  })),
  
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => set(produce(state => {
    state.players[playerId].isGrounded = isGrounded;
  })),
  
  updatePlayerDefending: (playerId: string, isDefending: boolean) => set(produce(state => {
    state.players[playerId].isDefending = isDefending;
  })),
  
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => set(produce(state => {
    state.players[playerId].attackCooldown = cooldown;
  })),
  
  dealDamage: (targetId: string, damage: number) => set(produce(state => {
    const target = state.players[targetId];
    const actualDamage = target.isDefending ? Math.floor(damage * 0.5) : damage;
    target.health = Math.max(0, target.health - actualDamage);
    
    // 添加受击特效
    state.effects.push({
      id: `hit-${Date.now()}`,
      type: 'HIT',
      position: { ...target.position },
      duration: 300,
      timer: 0,
      frame: 0,
    });
    
    // 检查游戏结束
    if (target.health === 0) {
      const winnerId = targetId === 'player1'import { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
  winner: null,
};

// 游戏Store接口
interface GameStore {
  // 状态
  gameState: GameState;
  players: Record<string, MechaData>;
  effects: Effect[];
  
  // 游戏流程控制
  startGame: () => void;
  pauseGame: () => void;
  resumeGame: () => void;
  endGame: (winner: string | null) => void;
  resetGame: () => void;
  nextRound: () => void;
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => void;
  updatePlayerState: (playerId: string, state: MechaData['state']) => void;
  updatePlayerFacing: (playerId: string, facingRight: boolean) => void;
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => void;
  updatePlayerDefending: (playerId: string, isDefending: boolean) => void;
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => void;
  dealDamage: (targetId: string, damage: number, attackerId?: string) => void;
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => void;
  setAnimationFrame: (playerId: string, frame: number) => void;
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => string;
  updateEffects: (deltaTime: number) => void;
  removeEffect: (effectId: string) => void;
  clearEffects: () => void;
  
  // 游戏时间
  tick: (deltaTime: number) => void;
}

// 创建Game Store
export const useGameStore = create<GameStore>((set, get) => ({
  // 初始状态
  gameState: initialGameState,
  players: {
    player1: createInitialMecha('player1', 'BLUE', 200),
    player2: createInitialMecha('player2', 'RED', 550),
  },
  effects: [],
  
  // 游戏流程控制
  startGame: () => set(produce(state => {
    state.gameState.phase = 'PLAYING';
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.winner = null;
  })),
  
  pauseGame: () => set(produce(state => {
    if (state.gameState.phase === 'PLAYING') {
      state.gameState.phase = 'PAUSED';
    }
  })),
  
  resumeGame: () => set(produce(state => {
    if (state.gameState.phase === 'PAUSED') {
      state.gameState.phase = 'PLAYING';
    }
  })),
  
  endGame: (winner: string | null) => set(produce(state => {
    state.gameState.phase = 'RESULT';
    state.gameState.winner = winner;
    if (winner && state.players[winner]) {
      state.players[winner].score += 1;
    }
  })),
  
  resetGame: () => set(produce(state => {
    state.gameState = initialGameState;
    state.players = {
      player1: createInitialMecha('player1', 'BLUE', 200),
      player2: createInitialMecha('player2', 'RED', 550),
    };
    state.effects = [];
  })),
  
  nextRound: () => set(produce(state => {
    state.gameState.round += 1;
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.phase = 'PLAYING';
    state.gameState.winner = null;
    
    // 重置玩家位置和状态
    Object.keys(state.players).forEach(playerId => {
      const player = state.players[playerId];
      player.health = player.maxHealth;
      player.position = playerId === 'player1' ? { x: 200, y: 400 } : { x: 550, y: 400 };
      player.velocity = { x: 0, y: 0 };
      player.state = 'IDLE';
      player.isDefending = false;
      player.isGrounded = false;
      player.attackCooldown = 0;
    });
    
    state.effects = [];
  })),
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => set(produce(state => {
    state.players[playerId].position = position;
    state.players[playerId].velocity = velocity;
  })),
  
  updatePlayerState: (playerId: string, mechaState: MechaData['state']) => set(produce(state => {
    state.players[playerId].state = mechaState;
  })),
  
  updatePlayerFacing: (playerId: string, facingRight: boolean) => set(produce(state => {
    state.players[playerId].facingRight = facingRight;
  })),
  
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => set(produce(state => {
    state.players[playerId].isGrounded = isGrounded;
  })),
  
  updatePlayerDefending: (playerId: string, isDefending: boolean) => set(produce(state => {
    state.players[playerId].isDefending = isDefending;
  })),
  
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => set(produce(state => {
    state.players[playerId].attackCooldown = cooldown;
  })),
  
  dealDamage: (targetId: string, damage: number) => set(produce(state => {
    const target = state.players[targetId];
    const actualDamage = target.isDefending ? Math.floor(damage * 0.5) : damage;
    target.health = Math.max(0, target.health - actualDamage);
    
    // 添加受击特效
    state.effects.push({
      id: `hit-${Date.now()}`,
      type: 'HIT',
      position: { ...target.position },
      duration: 300,
      timer: 0,
      frame: 0,
    });
    
    // 检查游戏结束
    if (target.health === 0) {
      const winnerId = targetId === 'player1' ? 'player2' : 'import { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
  winner: null,
};

// 游戏Store接口
interface GameStore {
  // 状态
  gameState: GameState;
  players: Record<string, MechaData>;
  effects: Effect[];
  
  // 游戏流程控制
  startGame: () => void;
  pauseGame: () => void;
  resumeGame: () => void;
  endGame: (winner: string | null) => void;
  resetGame: () => void;
  nextRound: () => void;
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => void;
  updatePlayerState: (playerId: string, state: MechaData['state']) => void;
  updatePlayerFacing: (playerId: string, facingRight: boolean) => void;
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => void;
  updatePlayerDefending: (playerId: string, isDefending: boolean) => void;
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => void;
  dealDamage: (targetId: string, damage: number, attackerId?: string) => void;
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => void;
  setAnimationFrame: (playerId: string, frame: number) => void;
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => string;
  updateEffects: (deltaTime: number) => void;
  removeEffect: (effectId: string) => void;
  clearEffects: () => void;
  
  // 游戏时间
  tick: (deltaTime: number) => void;
}

// 创建Game Store
export const useGameStore = create<GameStore>((set, get) => ({
  // 初始状态
  gameState: initialGameState,
  players: {
    player1: createInitialMecha('player1', 'BLUE', 200),
    player2: createInitialMecha('player2', 'RED', 550),
  },
  effects: [],
  
  // 游戏流程控制
  startGame: () => set(produce(state => {
    state.gameState.phase = 'PLAYING';
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.winner = null;
  })),
  
  pauseGame: () => set(produce(state => {
    if (state.gameState.phase === 'PLAYING') {
      state.gameState.phase = 'PAUSED';
    }
  })),
  
  resumeGame: () => set(produce(state => {
    if (state.gameState.phase === 'PAUSED') {
      state.gameState.phase = 'PLAYING';
    }
  })),
  
  endGame: (winner: string | null) => set(produce(state => {
    state.gameState.phase = 'RESULT';
    state.gameState.winner = winner;
    if (winner && state.players[winner]) {
      state.players[winner].score += 1;
    }
  })),
  
  resetGame: () => set(produce(state => {
    state.gameState = initialGameState;
    state.players = {
      player1: createInitialMecha('player1', 'BLUE', 200),
      player2: createInitialMecha('player2', 'RED', 550),
    };
    state.effects = [];
  })),
  
  nextRound: () => set(produce(state => {
    state.gameState.round += 1;
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.phase = 'PLAYING';
    state.gameState.winner = null;
    
    // 重置玩家位置和状态
    Object.keys(state.players).forEach(playerId => {
      const player = state.players[playerId];
      player.health = player.maxHealth;
      player.position = playerId === 'player1' ? { x: 200, y: 400 } : { x: 550, y: 400 };
      player.velocity = { x: 0, y: 0 };
      player.state = 'IDLE';
      player.isDefending = false;
      player.isGrounded = false;
      player.attackCooldown = 0;
    });
    
    state.effects = [];
  })),
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => set(produce(state => {
    state.players[playerId].position = position;
    state.players[playerId].velocity = velocity;
  })),
  
  updatePlayerState: (playerId: string, mechaState: MechaData['state']) => set(produce(state => {
    state.players[playerId].state = mechaState;
  })),
  
  updatePlayerFacing: (playerId: string, facingRight: boolean) => set(produce(state => {
    state.players[playerId].facingRight = facingRight;
  })),
  
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => set(produce(state => {
    state.players[playerId].isGrounded = isGrounded;
  })),
  
  updatePlayerDefending: (playerId: string, isDefending: boolean) => set(produce(state => {
    state.players[playerId].isDefending = isDefending;
  })),
  
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => set(produce(state => {
    state.players[playerId].attackCooldown = cooldown;
  })),
  
  dealDamage: (targetId: string, damage: number) => set(produce(state => {
    const target = state.players[targetId];
    const actualDamage = target.isDefending ? Math.floor(damage * 0.5) : damage;
    target.health = Math.max(0, target.health - actualDamage);
    
    // 添加受击特效
    state.effects.push({
      id: `hit-${Date.now()}`,
      type: 'HIT',
      position: { ...target.position },
      duration: 300,
      timer: 0,
      frame: 0,
    });
    
    // 检查游戏结束
    if (target.health === 0) {
      const winnerId = targetId === 'player1' ? 'player2' : 'player1';
      stateimport { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
  winner: null,
};

// 游戏Store接口
interface GameStore {
  // 状态
  gameState: GameState;
  players: Record<string, MechaData>;
  effects: Effect[];
  
  // 游戏流程控制
  startGame: () => void;
  pauseGame: () => void;
  resumeGame: () => void;
  endGame: (winner: string | null) => void;
  resetGame: () => void;
  nextRound: () => void;
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => void;
  updatePlayerState: (playerId: string, state: MechaData['state']) => void;
  updatePlayerFacing: (playerId: string, facingRight: boolean) => void;
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => void;
  updatePlayerDefending: (playerId: string, isDefending: boolean) => void;
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => void;
  dealDamage: (targetId: string, damage: number, attackerId?: string) => void;
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => void;
  setAnimationFrame: (playerId: string, frame: number) => void;
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => string;
  updateEffects: (deltaTime: number) => void;
  removeEffect: (effectId: string) => void;
  clearEffects: () => void;
  
  // 游戏时间
  tick: (deltaTime: number) => void;
}

// 创建Game Store
export const useGameStore = create<GameStore>((set, get) => ({
  // 初始状态
  gameState: initialGameState,
  players: {
    player1: createInitialMecha('player1', 'BLUE', 200),
    player2: createInitialMecha('player2', 'RED', 550),
  },
  effects: [],
  
  // 游戏流程控制
  startGame: () => set(produce(state => {
    state.gameState.phase = 'PLAYING';
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.winner = null;
  })),
  
  pauseGame: () => set(produce(state => {
    if (state.gameState.phase === 'PLAYING') {
      state.gameState.phase = 'PAUSED';
    }
  })),
  
  resumeGame: () => set(produce(state => {
    if (state.gameState.phase === 'PAUSED') {
      state.gameState.phase = 'PLAYING';
    }
  })),
  
  endGame: (winner: string | null) => set(produce(state => {
    state.gameState.phase = 'RESULT';
    state.gameState.winner = winner;
    if (winner && state.players[winner]) {
      state.players[winner].score += 1;
    }
  })),
  
  resetGame: () => set(produce(state => {
    state.gameState = initialGameState;
    state.players = {
      player1: createInitialMecha('player1', 'BLUE', 200),
      player2: createInitialMecha('player2', 'RED', 550),
    };
    state.effects = [];
  })),
  
  nextRound: () => set(produce(state => {
    state.gameState.round += 1;
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.phase = 'PLAYING';
    state.gameState.winner = null;
    
    // 重置玩家位置和状态
    Object.keys(state.players).forEach(playerId => {
      const player = state.players[playerId];
      player.health = player.maxHealth;
      player.position = playerId === 'player1' ? { x: 200, y: 400 } : { x: 550, y: 400 };
      player.velocity = { x: 0, y: 0 };
      player.state = 'IDLE';
      player.isDefending = false;
      player.isGrounded = false;
      player.attackCooldown = 0;
    });
    
    state.effects = [];
  })),
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => set(produce(state => {
    state.players[playerId].position = position;
    state.players[playerId].velocity = velocity;
  })),
  
  updatePlayerState: (playerId: string, mechaState: MechaData['state']) => set(produce(state => {
    state.players[playerId].state = mechaState;
  })),
  
  updatePlayerFacing: (playerId: string, facingRight: boolean) => set(produce(state => {
    state.players[playerId].facingRight = facingRight;
  })),
  
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => set(produce(state => {
    state.players[playerId].isGrounded = isGrounded;
  })),
  
  updatePlayerDefending: (playerId: string, isDefending: boolean) => set(produce(state => {
    state.players[playerId].isDefending = isDefending;
  })),
  
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => set(produce(state => {
    state.players[playerId].attackCooldown = cooldown;
  })),
  
  dealDamage: (targetId: string, damage: number) => set(produce(state => {
    const target = state.players[targetId];
    const actualDamage = target.isDefending ? Math.floor(damage * 0.5) : damage;
    target.health = Math.max(0, target.health - actualDamage);
    
    // 添加受击特效
    state.effects.push({
      id: `hit-${Date.now()}`,
      type: 'HIT',
      position: { ...target.position },
      duration: 300,
      timer: 0,
      frame: 0,
    });
    
    // 检查游戏结束
    if (target.health === 0) {
      const winnerId = targetId === 'player1' ? 'player2' : 'player1';
      state.gameState.phase = 'RESULT'import { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
  winner: null,
};

// 游戏Store接口
interface GameStore {
  // 状态
  gameState: GameState;
  players: Record<string, MechaData>;
  effects: Effect[];
  
  // 游戏流程控制
  startGame: () => void;
  pauseGame: () => void;
  resumeGame: () => void;
  endGame: (winner: string | null) => void;
  resetGame: () => void;
  nextRound: () => void;
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => void;
  updatePlayerState: (playerId: string, state: MechaData['state']) => void;
  updatePlayerFacing: (playerId: string, facingRight: boolean) => void;
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => void;
  updatePlayerDefending: (playerId: string, isDefending: boolean) => void;
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => void;
  dealDamage: (targetId: string, damage: number, attackerId?: string) => void;
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => void;
  setAnimationFrame: (playerId: string, frame: number) => void;
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => string;
  updateEffects: (deltaTime: number) => void;
  removeEffect: (effectId: string) => void;
  clearEffects: () => void;
  
  // 游戏时间
  tick: (deltaTime: number) => void;
}

// 创建Game Store
export const useGameStore = create<GameStore>((set, get) => ({
  // 初始状态
  gameState: initialGameState,
  players: {
    player1: createInitialMecha('player1', 'BLUE', 200),
    player2: createInitialMecha('player2', 'RED', 550),
  },
  effects: [],
  
  // 游戏流程控制
  startGame: () => set(produce(state => {
    state.gameState.phase = 'PLAYING';
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.winner = null;
  })),
  
  pauseGame: () => set(produce(state => {
    if (state.gameState.phase === 'PLAYING') {
      state.gameState.phase = 'PAUSED';
    }
  })),
  
  resumeGame: () => set(produce(state => {
    if (state.gameState.phase === 'PAUSED') {
      state.gameState.phase = 'PLAYING';
    }
  })),
  
  endGame: (winner: string | null) => set(produce(state => {
    state.gameState.phase = 'RESULT';
    state.gameState.winner = winner;
    if (winner && state.players[winner]) {
      state.players[winner].score += 1;
    }
  })),
  
  resetGame: () => set(produce(state => {
    state.gameState = initialGameState;
    state.players = {
      player1: createInitialMecha('player1', 'BLUE', 200),
      player2: createInitialMecha('player2', 'RED', 550),
    };
    state.effects = [];
  })),
  
  nextRound: () => set(produce(state => {
    state.gameState.round += 1;
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.phase = 'PLAYING';
    state.gameState.winner = null;
    
    // 重置玩家位置和状态
    Object.keys(state.players).forEach(playerId => {
      const player = state.players[playerId];
      player.health = player.maxHealth;
      player.position = playerId === 'player1' ? { x: 200, y: 400 } : { x: 550, y: 400 };
      player.velocity = { x: 0, y: 0 };
      player.state = 'IDLE';
      player.isDefending = false;
      player.isGrounded = false;
      player.attackCooldown = 0;
    });
    
    state.effects = [];
  })),
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => set(produce(state => {
    state.players[playerId].position = position;
    state.players[playerId].velocity = velocity;
  })),
  
  updatePlayerState: (playerId: string, mechaState: MechaData['state']) => set(produce(state => {
    state.players[playerId].state = mechaState;
  })),
  
  updatePlayerFacing: (playerId: string, facingRight: boolean) => set(produce(state => {
    state.players[playerId].facingRight = facingRight;
  })),
  
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => set(produce(state => {
    state.players[playerId].isGrounded = isGrounded;
  })),
  
  updatePlayerDefending: (playerId: string, isDefending: boolean) => set(produce(state => {
    state.players[playerId].isDefending = isDefending;
  })),
  
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => set(produce(state => {
    state.players[playerId].attackCooldown = cooldown;
  })),
  
  dealDamage: (targetId: string, damage: number) => set(produce(state => {
    const target = state.players[targetId];
    const actualDamage = target.isDefending ? Math.floor(damage * 0.5) : damage;
    target.health = Math.max(0, target.health - actualDamage);
    
    // 添加受击特效
    state.effects.push({
      id: `hit-${Date.now()}`,
      type: 'HIT',
      position: { ...target.position },
      duration: 300,
      timer: 0,
      frame: 0,
    });
    
    // 检查游戏结束
    if (target.health === 0) {
      const winnerId = targetId === 'player1' ? 'player2' : 'player1';
      state.gameState.phase = 'RESULT';
      state.gameState.wimport { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
  winner: null,
};

// 游戏Store接口
interface GameStore {
  // 状态
  gameState: GameState;
  players: Record<string, MechaData>;
  effects: Effect[];
  
  // 游戏流程控制
  startGame: () => void;
  pauseGame: () => void;
  resumeGame: () => void;
  endGame: (winner: string | null) => void;
  resetGame: () => void;
  nextRound: () => void;
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => void;
  updatePlayerState: (playerId: string, state: MechaData['state']) => void;
  updatePlayerFacing: (playerId: string, facingRight: boolean) => void;
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => void;
  updatePlayerDefending: (playerId: string, isDefending: boolean) => void;
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => void;
  dealDamage: (targetId: string, damage: number, attackerId?: string) => void;
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => void;
  setAnimationFrame: (playerId: string, frame: number) => void;
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => string;
  updateEffects: (deltaTime: number) => void;
  removeEffect: (effectId: string) => void;
  clearEffects: () => void;
  
  // 游戏时间
  tick: (deltaTime: number) => void;
}

// 创建Game Store
export const useGameStore = create<GameStore>((set, get) => ({
  // 初始状态
  gameState: initialGameState,
  players: {
    player1: createInitialMecha('player1', 'BLUE', 200),
    player2: createInitialMecha('player2', 'RED', 550),
  },
  effects: [],
  
  // 游戏流程控制
  startGame: () => set(produce(state => {
    state.gameState.phase = 'PLAYING';
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.winner = null;
  })),
  
  pauseGame: () => set(produce(state => {
    if (state.gameState.phase === 'PLAYING') {
      state.gameState.phase = 'PAUSED';
    }
  })),
  
  resumeGame: () => set(produce(state => {
    if (state.gameState.phase === 'PAUSED') {
      state.gameState.phase = 'PLAYING';
    }
  })),
  
  endGame: (winner: string | null) => set(produce(state => {
    state.gameState.phase = 'RESULT';
    state.gameState.winner = winner;
    if (winner && state.players[winner]) {
      state.players[winner].score += 1;
    }
  })),
  
  resetGame: () => set(produce(state => {
    state.gameState = initialGameState;
    state.players = {
      player1: createInitialMecha('player1', 'BLUE', 200),
      player2: createInitialMecha('player2', 'RED', 550),
    };
    state.effects = [];
  })),
  
  nextRound: () => set(produce(state => {
    state.gameState.round += 1;
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.phase = 'PLAYING';
    state.gameState.winner = null;
    
    // 重置玩家位置和状态
    Object.keys(state.players).forEach(playerId => {
      const player = state.players[playerId];
      player.health = player.maxHealth;
      player.position = playerId === 'player1' ? { x: 200, y: 400 } : { x: 550, y: 400 };
      player.velocity = { x: 0, y: 0 };
      player.state = 'IDLE';
      player.isDefending = false;
      player.isGrounded = false;
      player.attackCooldown = 0;
    });
    
    state.effects = [];
  })),
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => set(produce(state => {
    state.players[playerId].position = position;
    state.players[playerId].velocity = velocity;
  })),
  
  updatePlayerState: (playerId: string, mechaState: MechaData['state']) => set(produce(state => {
    state.players[playerId].state = mechaState;
  })),
  
  updatePlayerFacing: (playerId: string, facingRight: boolean) => set(produce(state => {
    state.players[playerId].facingRight = facingRight;
  })),
  
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => set(produce(state => {
    state.players[playerId].isGrounded = isGrounded;
  })),
  
  updatePlayerDefending: (playerId: string, isDefending: boolean) => set(produce(state => {
    state.players[playerId].isDefending = isDefending;
  })),
  
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => set(produce(state => {
    state.players[playerId].attackCooldown = cooldown;
  })),
  
  dealDamage: (targetId: string, damage: number) => set(produce(state => {
    const target = state.players[targetId];
    const actualDamage = target.isDefending ? Math.floor(damage * 0.5) : damage;
    target.health = Math.max(0, target.health - actualDamage);
    
    // 添加受击特效
    state.effects.push({
      id: `hit-${Date.now()}`,
      type: 'HIT',
      position: { ...target.position },
      duration: 300,
      timer: 0,
      frame: 0,
    });
    
    // 检查游戏结束
    if (target.health === 0) {
      const winnerId = targetId === 'player1' ? 'player2' : 'player1';
      state.gameState.phase = 'RESULT';
      state.gameState.winner = winnerId;
      stateimport { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
  winner: null,
};

// 游戏Store接口
interface GameStore {
  // 状态
  gameState: GameState;
  players: Record<string, MechaData>;
  effects: Effect[];
  
  // 游戏流程控制
  startGame: () => void;
  pauseGame: () => void;
  resumeGame: () => void;
  endGame: (winner: string | null) => void;
  resetGame: () => void;
  nextRound: () => void;
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => void;
  updatePlayerState: (playerId: string, state: MechaData['state']) => void;
  updatePlayerFacing: (playerId: string, facingRight: boolean) => void;
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => void;
  updatePlayerDefending: (playerId: string, isDefending: boolean) => void;
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => void;
  dealDamage: (targetId: string, damage: number, attackerId?: string) => void;
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => void;
  setAnimationFrame: (playerId: string, frame: number) => void;
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => string;
  updateEffects: (deltaTime: number) => void;
  removeEffect: (effectId: string) => void;
  clearEffects: () => void;
  
  // 游戏时间
  tick: (deltaTime: number) => void;
}

// 创建Game Store
export const useGameStore = create<GameStore>((set, get) => ({
  // 初始状态
  gameState: initialGameState,
  players: {
    player1: createInitialMecha('player1', 'BLUE', 200),
    player2: createInitialMecha('player2', 'RED', 550),
  },
  effects: [],
  
  // 游戏流程控制
  startGame: () => set(produce(state => {
    state.gameState.phase = 'PLAYING';
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.winner = null;
  })),
  
  pauseGame: () => set(produce(state => {
    if (state.gameState.phase === 'PLAYING') {
      state.gameState.phase = 'PAUSED';
    }
  })),
  
  resumeGame: () => set(produce(state => {
    if (state.gameState.phase === 'PAUSED') {
      state.gameState.phase = 'PLAYING';
    }
  })),
  
  endGame: (winner: string | null) => set(produce(state => {
    state.gameState.phase = 'RESULT';
    state.gameState.winner = winner;
    if (winner && state.players[winner]) {
      state.players[winner].score += 1;
    }
  })),
  
  resetGame: () => set(produce(state => {
    state.gameState = initialGameState;
    state.players = {
      player1: createInitialMecha('player1', 'BLUE', 200),
      player2: createInitialMecha('player2', 'RED', 550),
    };
    state.effects = [];
  })),
  
  nextRound: () => set(produce(state => {
    state.gameState.round += 1;
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.phase = 'PLAYING';
    state.gameState.winner = null;
    
    // 重置玩家位置和状态
    Object.keys(state.players).forEach(playerId => {
      const player = state.players[playerId];
      player.health = player.maxHealth;
      player.position = playerId === 'player1' ? { x: 200, y: 400 } : { x: 550, y: 400 };
      player.velocity = { x: 0, y: 0 };
      player.state = 'IDLE';
      player.isDefending = false;
      player.isGrounded = false;
      player.attackCooldown = 0;
    });
    
    state.effects = [];
  })),
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => set(produce(state => {
    state.players[playerId].position = position;
    state.players[playerId].velocity = velocity;
  })),
  
  updatePlayerState: (playerId: string, mechaState: MechaData['state']) => set(produce(state => {
    state.players[playerId].state = mechaState;
  })),
  
  updatePlayerFacing: (playerId: string, facingRight: boolean) => set(produce(state => {
    state.players[playerId].facingRight = facingRight;
  })),
  
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => set(produce(state => {
    state.players[playerId].isGrounded = isGrounded;
  })),
  
  updatePlayerDefending: (playerId: string, isDefending: boolean) => set(produce(state => {
    state.players[playerId].isDefending = isDefending;
  })),
  
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => set(produce(state => {
    state.players[playerId].attackCooldown = cooldown;
  })),
  
  dealDamage: (targetId: string, damage: number) => set(produce(state => {
    const target = state.players[targetId];
    const actualDamage = target.isDefending ? Math.floor(damage * 0.5) : damage;
    target.health = Math.max(0, target.health - actualDamage);
    
    // 添加受击特效
    state.effects.push({
      id: `hit-${Date.now()}`,
      type: 'HIT',
      position: { ...target.position },
      duration: 300,
      timer: 0,
      frame: 0,
    });
    
    // 检查游戏结束
    if (target.health === 0) {
      const winnerId = targetId === 'player1' ? 'player2' : 'player1';
      state.gameState.phase = 'RESULT';
      state.gameState.winner = winnerId;
      state.players[winnerId].score +=import { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
  winner: null,
};

// 游戏Store接口
interface GameStore {
  // 状态
  gameState: GameState;
  players: Record<string, MechaData>;
  effects: Effect[];
  
  // 游戏流程控制
  startGame: () => void;
  pauseGame: () => void;
  resumeGame: () => void;
  endGame: (winner: string | null) => void;
  resetGame: () => void;
  nextRound: () => void;
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => void;
  updatePlayerState: (playerId: string, state: MechaData['state']) => void;
  updatePlayerFacing: (playerId: string, facingRight: boolean) => void;
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => void;
  updatePlayerDefending: (playerId: string, isDefending: boolean) => void;
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => void;
  dealDamage: (targetId: string, damage: number, attackerId?: string) => void;
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => void;
  setAnimationFrame: (playerId: string, frame: number) => void;
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => string;
  updateEffects: (deltaTime: number) => void;
  removeEffect: (effectId: string) => void;
  clearEffects: () => void;
  
  // 游戏时间
  tick: (deltaTime: number) => void;
}

// 创建Game Store
export const useGameStore = create<GameStore>((set, get) => ({
  // 初始状态
  gameState: initialGameState,
  players: {
    player1: createInitialMecha('player1', 'BLUE', 200),
    player2: createInitialMecha('player2', 'RED', 550),
  },
  effects: [],
  
  // 游戏流程控制
  startGame: () => set(produce(state => {
    state.gameState.phase = 'PLAYING';
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.winner = null;
  })),
  
  pauseGame: () => set(produce(state => {
    if (state.gameState.phase === 'PLAYING') {
      state.gameState.phase = 'PAUSED';
    }
  })),
  
  resumeGame: () => set(produce(state => {
    if (state.gameState.phase === 'PAUSED') {
      state.gameState.phase = 'PLAYING';
    }
  })),
  
  endGame: (winner: string | null) => set(produce(state => {
    state.gameState.phase = 'RESULT';
    state.gameState.winner = winner;
    if (winner && state.players[winner]) {
      state.players[winner].score += 1;
    }
  })),
  
  resetGame: () => set(produce(state => {
    state.gameState = initialGameState;
    state.players = {
      player1: createInitialMecha('player1', 'BLUE', 200),
      player2: createInitialMecha('player2', 'RED', 550),
    };
    state.effects = [];
  })),
  
  nextRound: () => set(produce(state => {
    state.gameState.round += 1;
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.phase = 'PLAYING';
    state.gameState.winner = null;
    
    // 重置玩家位置和状态
    Object.keys(state.players).forEach(playerId => {
      const player = state.players[playerId];
      player.health = player.maxHealth;
      player.position = playerId === 'player1' ? { x: 200, y: 400 } : { x: 550, y: 400 };
      player.velocity = { x: 0, y: 0 };
      player.state = 'IDLE';
      player.isDefending = false;
      player.isGrounded = false;
      player.attackCooldown = 0;
    });
    
    state.effects = [];
  })),
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => set(produce(state => {
    state.players[playerId].position = position;
    state.players[playerId].velocity = velocity;
  })),
  
  updatePlayerState: (playerId: string, mechaState: MechaData['state']) => set(produce(state => {
    state.players[playerId].state = mechaState;
  })),
  
  updatePlayerFacing: (playerId: string, facingRight: boolean) => set(produce(state => {
    state.players[playerId].facingRight = facingRight;
  })),
  
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => set(produce(state => {
    state.players[playerId].isGrounded = isGrounded;
  })),
  
  updatePlayerDefending: (playerId: string, isDefending: boolean) => set(produce(state => {
    state.players[playerId].isDefending = isDefending;
  })),
  
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => set(produce(state => {
    state.players[playerId].attackCooldown = cooldown;
  })),
  
  dealDamage: (targetId: string, damage: number) => set(produce(state => {
    const target = state.players[targetId];
    const actualDamage = target.isDefending ? Math.floor(damage * 0.5) : damage;
    target.health = Math.max(0, target.health - actualDamage);
    
    // 添加受击特效
    state.effects.push({
      id: `hit-${Date.now()}`,
      type: 'HIT',
      position: { ...target.position },
      duration: 300,
      timer: 0,
      frame: 0,
    });
    
    // 检查游戏结束
    if (target.health === 0) {
      const winnerId = targetId === 'player1' ? 'player2' : 'player1';
      state.gameState.phase = 'RESULT';
      state.gameState.winner = winnerId;
      state.players[winnerId].score += 1;
    }
import { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
  winner: null,
};

// 游戏Store接口
interface GameStore {
  // 状态
  gameState: GameState;
  players: Record<string, MechaData>;
  effects: Effect[];
  
  // 游戏流程控制
  startGame: () => void;
  pauseGame: () => void;
  resumeGame: () => void;
  endGame: (winner: string | null) => void;
  resetGame: () => void;
  nextRound: () => void;
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => void;
  updatePlayerState: (playerId: string, state: MechaData['state']) => void;
  updatePlayerFacing: (playerId: string, facingRight: boolean) => void;
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => void;
  updatePlayerDefending: (playerId: string, isDefending: boolean) => void;
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => void;
  dealDamage: (targetId: string, damage: number, attackerId?: string) => void;
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => void;
  setAnimationFrame: (playerId: string, frame: number) => void;
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => string;
  updateEffects: (deltaTime: number) => void;
  removeEffect: (effectId: string) => void;
  clearEffects: () => void;
  
  // 游戏时间
  tick: (deltaTime: number) => void;
}

// 创建Game Store
export const useGameStore = create<GameStore>((set, get) => ({
  // 初始状态
  gameState: initialGameState,
  players: {
    player1: createInitialMecha('player1', 'BLUE', 200),
    player2: createInitialMecha('player2', 'RED', 550),
  },
  effects: [],
  
  // 游戏流程控制
  startGame: () => set(produce(state => {
    state.gameState.phase = 'PLAYING';
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.winner = null;
  })),
  
  pauseGame: () => set(produce(state => {
    if (state.gameState.phase === 'PLAYING') {
      state.gameState.phase = 'PAUSED';
    }
  })),
  
  resumeGame: () => set(produce(state => {
    if (state.gameState.phase === 'PAUSED') {
      state.gameState.phase = 'PLAYING';
    }
  })),
  
  endGame: (winner: string | null) => set(produce(state => {
    state.gameState.phase = 'RESULT';
    state.gameState.winner = winner;
    if (winner && state.players[winner]) {
      state.players[winner].score += 1;
    }
  })),
  
  resetGame: () => set(produce(state => {
    state.gameState = initialGameState;
    state.players = {
      player1: createInitialMecha('player1', 'BLUE', 200),
      player2: createInitialMecha('player2', 'RED', 550),
    };
    state.effects = [];
  })),
  
  nextRound: () => set(produce(state => {
    state.gameState.round += 1;
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.phase = 'PLAYING';
    state.gameState.winner = null;
    
    // 重置玩家位置和状态
    Object.keys(state.players).forEach(playerId => {
      const player = state.players[playerId];
      player.health = player.maxHealth;
      player.position = playerId === 'player1' ? { x: 200, y: 400 } : { x: 550, y: 400 };
      player.velocity = { x: 0, y: 0 };
      player.state = 'IDLE';
      player.isDefending = false;
      player.isGrounded = false;
      player.attackCooldown = 0;
    });
    
    state.effects = [];
  })),
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => set(produce(state => {
    state.players[playerId].position = position;
    state.players[playerId].velocity = velocity;
  })),
  
  updatePlayerState: (playerId: string, mechaState: MechaData['state']) => set(produce(state => {
    state.players[playerId].state = mechaState;
  })),
  
  updatePlayerFacing: (playerId: string, facingRight: boolean) => set(produce(state => {
    state.players[playerId].facingRight = facingRight;
  })),
  
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => set(produce(state => {
    state.players[playerId].isGrounded = isGrounded;
  })),
  
  updatePlayerDefending: (playerId: string, isDefending: boolean) => set(produce(state => {
    state.players[playerId].isDefending = isDefending;
  })),
  
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => set(produce(state => {
    state.players[playerId].attackCooldown = cooldown;
  })),
  
  dealDamage: (targetId: string, damage: number) => set(produce(state => {
    const target = state.players[targetId];
    const actualDamage = target.isDefending ? Math.floor(damage * 0.5) : damage;
    target.health = Math.max(0, target.health - actualDamage);
    
    // 添加受击特效
    state.effects.push({
      id: `hit-${Date.now()}`,
      type: 'HIT',
      position: { ...target.position },
      duration: 300,
      timer: 0,
      frame: 0,
    });
    
    // 检查游戏结束
    if (target.health === 0) {
      const winnerId = targetId === 'player1' ? 'player2' : 'player1';
      state.gameState.phase = 'RESULT';
      state.gameState.winner = winnerId;
      state.players[winnerId].score += 1;
    }
  })),
  
import { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
  winner: null,
};

// 游戏Store接口
interface GameStore {
  // 状态
  gameState: GameState;
  players: Record<string, MechaData>;
  effects: Effect[];
  
  // 游戏流程控制
  startGame: () => void;
  pauseGame: () => void;
  resumeGame: () => void;
  endGame: (winner: string | null) => void;
  resetGame: () => void;
  nextRound: () => void;
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => void;
  updatePlayerState: (playerId: string, state: MechaData['state']) => void;
  updatePlayerFacing: (playerId: string, facingRight: boolean) => void;
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => void;
  updatePlayerDefending: (playerId: string, isDefending: boolean) => void;
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => void;
  dealDamage: (targetId: string, damage: number, attackerId?: string) => void;
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => void;
  setAnimationFrame: (playerId: string, frame: number) => void;
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => string;
  updateEffects: (deltaTime: number) => void;
  removeEffect: (effectId: string) => void;
  clearEffects: () => void;
  
  // 游戏时间
  tick: (deltaTime: number) => void;
}

// 创建Game Store
export const useGameStore = create<GameStore>((set, get) => ({
  // 初始状态
  gameState: initialGameState,
  players: {
    player1: createInitialMecha('player1', 'BLUE', 200),
    player2: createInitialMecha('player2', 'RED', 550),
  },
  effects: [],
  
  // 游戏流程控制
  startGame: () => set(produce(state => {
    state.gameState.phase = 'PLAYING';
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.winner = null;
  })),
  
  pauseGame: () => set(produce(state => {
    if (state.gameState.phase === 'PLAYING') {
      state.gameState.phase = 'PAUSED';
    }
  })),
  
  resumeGame: () => set(produce(state => {
    if (state.gameState.phase === 'PAUSED') {
      state.gameState.phase = 'PLAYING';
    }
  })),
  
  endGame: (winner: string | null) => set(produce(state => {
    state.gameState.phase = 'RESULT';
    state.gameState.winner = winner;
    if (winner && state.players[winner]) {
      state.players[winner].score += 1;
    }
  })),
  
  resetGame: () => set(produce(state => {
    state.gameState = initialGameState;
    state.players = {
      player1: createInitialMecha('player1', 'BLUE', 200),
      player2: createInitialMecha('player2', 'RED', 550),
    };
    state.effects = [];
  })),
  
  nextRound: () => set(produce(state => {
    state.gameState.round += 1;
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.phase = 'PLAYING';
    state.gameState.winner = null;
    
    // 重置玩家位置和状态
    Object.keys(state.players).forEach(playerId => {
      const player = state.players[playerId];
      player.health = player.maxHealth;
      player.position = playerId === 'player1' ? { x: 200, y: 400 } : { x: 550, y: 400 };
      player.velocity = { x: 0, y: 0 };
      player.state = 'IDLE';
      player.isDefending = false;
      player.isGrounded = false;
      player.attackCooldown = 0;
    });
    
    state.effects = [];
  })),
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => set(produce(state => {
    state.players[playerId].position = position;
    state.players[playerId].velocity = velocity;
  })),
  
  updatePlayerState: (playerId: string, mechaState: MechaData['state']) => set(produce(state => {
    state.players[playerId].state = mechaState;
  })),
  
  updatePlayerFacing: (playerId: string, facingRight: boolean) => set(produce(state => {
    state.players[playerId].facingRight = facingRight;
  })),
  
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => set(produce(state => {
    state.players[playerId].isGrounded = isGrounded;
  })),
  
  updatePlayerDefending: (playerId: string, isDefending: boolean) => set(produce(state => {
    state.players[playerId].isDefending = isDefending;
  })),
  
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => set(produce(state => {
    state.players[playerId].attackCooldown = cooldown;
  })),
  
  dealDamage: (targetId: string, damage: number) => set(produce(state => {
    const target = state.players[targetId];
    const actualDamage = target.isDefending ? Math.floor(damage * 0.5) : damage;
    target.health = Math.max(0, target.health - actualDamage);
    
    // 添加受击特效
    state.effects.push({
      id: `hit-${Date.now()}`,
      type: 'HIT',
      position: { ...target.position },
      duration: 300,
      timer: 0,
      frame: 0,
    });
    
    // 检查游戏结束
    if (target.health === 0) {
      const winnerId = targetId === 'player1' ? 'player2' : 'player1';
      state.gameState.phase = 'RESULT';
      state.gameState.winner = winnerId;
      state.players[winnerId].score += 1;
    }
  })),
  
  // 动画
  updateimport { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
  winner: null,
};

// 游戏Store接口
interface GameStore {
  // 状态
  gameState: GameState;
  players: Record<string, MechaData>;
  effects: Effect[];
  
  // 游戏流程控制
  startGame: () => void;
  pauseGame: () => void;
  resumeGame: () => void;
  endGame: (winner: string | null) => void;
  resetGame: () => void;
  nextRound: () => void;
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => void;
  updatePlayerState: (playerId: string, state: MechaData['state']) => void;
  updatePlayerFacing: (playerId: string, facingRight: boolean) => void;
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => void;
  updatePlayerDefending: (playerId: string, isDefending: boolean) => void;
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => void;
  dealDamage: (targetId: string, damage: number, attackerId?: string) => void;
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => void;
  setAnimationFrame: (playerId: string, frame: number) => void;
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => string;
  updateEffects: (deltaTime: number) => void;
  removeEffect: (effectId: string) => void;
  clearEffects: () => void;
  
  // 游戏时间
  tick: (deltaTime: number) => void;
}

// 创建Game Store
export const useGameStore = create<GameStore>((set, get) => ({
  // 初始状态
  gameState: initialGameState,
  players: {
    player1: createInitialMecha('player1', 'BLUE', 200),
    player2: createInitialMecha('player2', 'RED', 550),
  },
  effects: [],
  
  // 游戏流程控制
  startGame: () => set(produce(state => {
    state.gameState.phase = 'PLAYING';
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.winner = null;
  })),
  
  pauseGame: () => set(produce(state => {
    if (state.gameState.phase === 'PLAYING') {
      state.gameState.phase = 'PAUSED';
    }
  })),
  
  resumeGame: () => set(produce(state => {
    if (state.gameState.phase === 'PAUSED') {
      state.gameState.phase = 'PLAYING';
    }
  })),
  
  endGame: (winner: string | null) => set(produce(state => {
    state.gameState.phase = 'RESULT';
    state.gameState.winner = winner;
    if (winner && state.players[winner]) {
      state.players[winner].score += 1;
    }
  })),
  
  resetGame: () => set(produce(state => {
    state.gameState = initialGameState;
    state.players = {
      player1: createInitialMecha('player1', 'BLUE', 200),
      player2: createInitialMecha('player2', 'RED', 550),
    };
    state.effects = [];
  })),
  
  nextRound: () => set(produce(state => {
    state.gameState.round += 1;
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.phase = 'PLAYING';
    state.gameState.winner = null;
    
    // 重置玩家位置和状态
    Object.keys(state.players).forEach(playerId => {
      const player = state.players[playerId];
      player.health = player.maxHealth;
      player.position = playerId === 'player1' ? { x: 200, y: 400 } : { x: 550, y: 400 };
      player.velocity = { x: 0, y: 0 };
      player.state = 'IDLE';
      player.isDefending = false;
      player.isGrounded = false;
      player.attackCooldown = 0;
    });
    
    state.effects = [];
  })),
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => set(produce(state => {
    state.players[playerId].position = position;
    state.players[playerId].velocity = velocity;
  })),
  
  updatePlayerState: (playerId: string, mechaState: MechaData['state']) => set(produce(state => {
    state.players[playerId].state = mechaState;
  })),
  
  updatePlayerFacing: (playerId: string, facingRight: boolean) => set(produce(state => {
    state.players[playerId].facingRight = facingRight;
  })),
  
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => set(produce(state => {
    state.players[playerId].isGrounded = isGrounded;
  })),
  
  updatePlayerDefending: (playerId: string, isDefending: boolean) => set(produce(state => {
    state.players[playerId].isDefending = isDefending;
  })),
  
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => set(produce(state => {
    state.players[playerId].attackCooldown = cooldown;
  })),
  
  dealDamage: (targetId: string, damage: number) => set(produce(state => {
    const target = state.players[targetId];
    const actualDamage = target.isDefending ? Math.floor(damage * 0.5) : damage;
    target.health = Math.max(0, target.health - actualDamage);
    
    // 添加受击特效
    state.effects.push({
      id: `hit-${Date.now()}`,
      type: 'HIT',
      position: { ...target.position },
      duration: 300,
      timer: 0,
      frame: 0,
    });
    
    // 检查游戏结束
    if (target.health === 0) {
      const winnerId = targetId === 'player1' ? 'player2' : 'player1';
      state.gameState.phase = 'RESULT';
      state.gameState.winner = winnerId;
      state.players[winnerId].score += 1;
    }
  })),
  
  // 动画
  updateAnimation: (playerId: stringimport { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
  winner: null,
};

// 游戏Store接口
interface GameStore {
  // 状态
  gameState: GameState;
  players: Record<string, MechaData>;
  effects: Effect[];
  
  // 游戏流程控制
  startGame: () => void;
  pauseGame: () => void;
  resumeGame: () => void;
  endGame: (winner: string | null) => void;
  resetGame: () => void;
  nextRound: () => void;
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => void;
  updatePlayerState: (playerId: string, state: MechaData['state']) => void;
  updatePlayerFacing: (playerId: string, facingRight: boolean) => void;
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => void;
  updatePlayerDefending: (playerId: string, isDefending: boolean) => void;
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => void;
  dealDamage: (targetId: string, damage: number, attackerId?: string) => void;
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => void;
  setAnimationFrame: (playerId: string, frame: number) => void;
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => string;
  updateEffects: (deltaTime: number) => void;
  removeEffect: (effectId: string) => void;
  clearEffects: () => void;
  
  // 游戏时间
  tick: (deltaTime: number) => void;
}

// 创建Game Store
export const useGameStore = create<GameStore>((set, get) => ({
  // 初始状态
  gameState: initialGameState,
  players: {
    player1: createInitialMecha('player1', 'BLUE', 200),
    player2: createInitialMecha('player2', 'RED', 550),
  },
  effects: [],
  
  // 游戏流程控制
  startGame: () => set(produce(state => {
    state.gameState.phase = 'PLAYING';
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.winner = null;
  })),
  
  pauseGame: () => set(produce(state => {
    if (state.gameState.phase === 'PLAYING') {
      state.gameState.phase = 'PAUSED';
    }
  })),
  
  resumeGame: () => set(produce(state => {
    if (state.gameState.phase === 'PAUSED') {
      state.gameState.phase = 'PLAYING';
    }
  })),
  
  endGame: (winner: string | null) => set(produce(state => {
    state.gameState.phase = 'RESULT';
    state.gameState.winner = winner;
    if (winner && state.players[winner]) {
      state.players[winner].score += 1;
    }
  })),
  
  resetGame: () => set(produce(state => {
    state.gameState = initialGameState;
    state.players = {
      player1: createInitialMecha('player1', 'BLUE', 200),
      player2: createInitialMecha('player2', 'RED', 550),
    };
    state.effects = [];
  })),
  
  nextRound: () => set(produce(state => {
    state.gameState.round += 1;
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.phase = 'PLAYING';
    state.gameState.winner = null;
    
    // 重置玩家位置和状态
    Object.keys(state.players).forEach(playerId => {
      const player = state.players[playerId];
      player.health = player.maxHealth;
      player.position = playerId === 'player1' ? { x: 200, y: 400 } : { x: 550, y: 400 };
      player.velocity = { x: 0, y: 0 };
      player.state = 'IDLE';
      player.isDefending = false;
      player.isGrounded = false;
      player.attackCooldown = 0;
    });
    
    state.effects = [];
  })),
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => set(produce(state => {
    state.players[playerId].position = position;
    state.players[playerId].velocity = velocity;
  })),
  
  updatePlayerState: (playerId: string, mechaState: MechaData['state']) => set(produce(state => {
    state.players[playerId].state = mechaState;
  })),
  
  updatePlayerFacing: (playerId: string, facingRight: boolean) => set(produce(state => {
    state.players[playerId].facingRight = facingRight;
  })),
  
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => set(produce(state => {
    state.players[playerId].isGrounded = isGrounded;
  })),
  
  updatePlayerDefending: (playerId: string, isDefending: boolean) => set(produce(state => {
    state.players[playerId].isDefending = isDefending;
  })),
  
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => set(produce(state => {
    state.players[playerId].attackCooldown = cooldown;
  })),
  
  dealDamage: (targetId: string, damage: number) => set(produce(state => {
    const target = state.players[targetId];
    const actualDamage = target.isDefending ? Math.floor(damage * 0.5) : damage;
    target.health = Math.max(0, target.health - actualDamage);
    
    // 添加受击特效
    state.effects.push({
      id: `hit-${Date.now()}`,
      type: 'HIT',
      position: { ...target.position },
      duration: 300,
      timer: 0,
      frame: 0,
    });
    
    // 检查游戏结束
    if (target.health === 0) {
      const winnerId = targetId === 'player1' ? 'player2' : 'player1';
      state.gameState.phase = 'RESULT';
      state.gameState.winner = winnerId;
      state.players[winnerId].score += 1;
    }
  })),
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) =>import { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
  winner: null,
};

// 游戏Store接口
interface GameStore {
  // 状态
  gameState: GameState;
  players: Record<string, MechaData>;
  effects: Effect[];
  
  // 游戏流程控制
  startGame: () => void;
  pauseGame: () => void;
  resumeGame: () => void;
  endGame: (winner: string | null) => void;
  resetGame: () => void;
  nextRound: () => void;
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => void;
  updatePlayerState: (playerId: string, state: MechaData['state']) => void;
  updatePlayerFacing: (playerId: string, facingRight: boolean) => void;
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => void;
  updatePlayerDefending: (playerId: string, isDefending: boolean) => void;
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => void;
  dealDamage: (targetId: string, damage: number, attackerId?: string) => void;
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => void;
  setAnimationFrame: (playerId: string, frame: number) => void;
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => string;
  updateEffects: (deltaTime: number) => void;
  removeEffect: (effectId: string) => void;
  clearEffects: () => void;
  
  // 游戏时间
  tick: (deltaTime: number) => void;
}

// 创建Game Store
export const useGameStore = create<GameStore>((set, get) => ({
  // 初始状态
  gameState: initialGameState,
  players: {
    player1: createInitialMecha('player1', 'BLUE', 200),
    player2: createInitialMecha('player2', 'RED', 550),
  },
  effects: [],
  
  // 游戏流程控制
  startGame: () => set(produce(state => {
    state.gameState.phase = 'PLAYING';
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.winner = null;
  })),
  
  pauseGame: () => set(produce(state => {
    if (state.gameState.phase === 'PLAYING') {
      state.gameState.phase = 'PAUSED';
    }
  })),
  
  resumeGame: () => set(produce(state => {
    if (state.gameState.phase === 'PAUSED') {
      state.gameState.phase = 'PLAYING';
    }
  })),
  
  endGame: (winner: string | null) => set(produce(state => {
    state.gameState.phase = 'RESULT';
    state.gameState.winner = winner;
    if (winner && state.players[winner]) {
      state.players[winner].score += 1;
    }
  })),
  
  resetGame: () => set(produce(state => {
    state.gameState = initialGameState;
    state.players = {
      player1: createInitialMecha('player1', 'BLUE', 200),
      player2: createInitialMecha('player2', 'RED', 550),
    };
    state.effects = [];
  })),
  
  nextRound: () => set(produce(state => {
    state.gameState.round += 1;
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.phase = 'PLAYING';
    state.gameState.winner = null;
    
    // 重置玩家位置和状态
    Object.keys(state.players).forEach(playerId => {
      const player = state.players[playerId];
      player.health = player.maxHealth;
      player.position = playerId === 'player1' ? { x: 200, y: 400 } : { x: 550, y: 400 };
      player.velocity = { x: 0, y: 0 };
      player.state = 'IDLE';
      player.isDefending = false;
      player.isGrounded = false;
      player.attackCooldown = 0;
    });
    
    state.effects = [];
  })),
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => set(produce(state => {
    state.players[playerId].position = position;
    state.players[playerId].velocity = velocity;
  })),
  
  updatePlayerState: (playerId: string, mechaState: MechaData['state']) => set(produce(state => {
    state.players[playerId].state = mechaState;
  })),
  
  updatePlayerFacing: (playerId: string, facingRight: boolean) => set(produce(state => {
    state.players[playerId].facingRight = facingRight;
  })),
  
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => set(produce(state => {
    state.players[playerId].isGrounded = isGrounded;
  })),
  
  updatePlayerDefending: (playerId: string, isDefending: boolean) => set(produce(state => {
    state.players[playerId].isDefending = isDefending;
  })),
  
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => set(produce(state => {
    state.players[playerId].attackCooldown = cooldown;
  })),
  
  dealDamage: (targetId: string, damage: number) => set(produce(state => {
    const target = state.players[targetId];
    const actualDamage = target.isDefending ? Math.floor(damage * 0.5) : damage;
    target.health = Math.max(0, target.health - actualDamage);
    
    // 添加受击特效
    state.effects.push({
      id: `hit-${Date.now()}`,
      type: 'HIT',
      position: { ...target.position },
      duration: 300,
      timer: 0,
      frame: 0,
    });
    
    // 检查游戏结束
    if (target.health === 0) {
      const winnerId = targetId === 'player1' ? 'player2' : 'player1';
      state.gameState.phase = 'RESULT';
      state.gameState.winner = winnerId;
      state.players[winnerId].score += 1;
    }
  })),
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => set(produce(state => {
import { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
  winner: null,
};

// 游戏Store接口
interface GameStore {
  // 状态
  gameState: GameState;
  players: Record<string, MechaData>;
  effects: Effect[];
  
  // 游戏流程控制
  startGame: () => void;
  pauseGame: () => void;
  resumeGame: () => void;
  endGame: (winner: string | null) => void;
  resetGame: () => void;
  nextRound: () => void;
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => void;
  updatePlayerState: (playerId: string, state: MechaData['state']) => void;
  updatePlayerFacing: (playerId: string, facingRight: boolean) => void;
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => void;
  updatePlayerDefending: (playerId: string, isDefending: boolean) => void;
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => void;
  dealDamage: (targetId: string, damage: number, attackerId?: string) => void;
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => void;
  setAnimationFrame: (playerId: string, frame: number) => void;
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => string;
  updateEffects: (deltaTime: number) => void;
  removeEffect: (effectId: string) => void;
  clearEffects: () => void;
  
  // 游戏时间
  tick: (deltaTime: number) => void;
}

// 创建Game Store
export const useGameStore = create<GameStore>((set, get) => ({
  // 初始状态
  gameState: initialGameState,
  players: {
    player1: createInitialMecha('player1', 'BLUE', 200),
    player2: createInitialMecha('player2', 'RED', 550),
  },
  effects: [],
  
  // 游戏流程控制
  startGame: () => set(produce(state => {
    state.gameState.phase = 'PLAYING';
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.winner = null;
  })),
  
  pauseGame: () => set(produce(state => {
    if (state.gameState.phase === 'PLAYING') {
      state.gameState.phase = 'PAUSED';
    }
  })),
  
  resumeGame: () => set(produce(state => {
    if (state.gameState.phase === 'PAUSED') {
      state.gameState.phase = 'PLAYING';
    }
  })),
  
  endGame: (winner: string | null) => set(produce(state => {
    state.gameState.phase = 'RESULT';
    state.gameState.winner = winner;
    if (winner && state.players[winner]) {
      state.players[winner].score += 1;
    }
  })),
  
  resetGame: () => set(produce(state => {
    state.gameState = initialGameState;
    state.players = {
      player1: createInitialMecha('player1', 'BLUE', 200),
      player2: createInitialMecha('player2', 'RED', 550),
    };
    state.effects = [];
  })),
  
  nextRound: () => set(produce(state => {
    state.gameState.round += 1;
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.phase = 'PLAYING';
    state.gameState.winner = null;
    
    // 重置玩家位置和状态
    Object.keys(state.players).forEach(playerId => {
      const player = state.players[playerId];
      player.health = player.maxHealth;
      player.position = playerId === 'player1' ? { x: 200, y: 400 } : { x: 550, y: 400 };
      player.velocity = { x: 0, y: 0 };
      player.state = 'IDLE';
      player.isDefending = false;
      player.isGrounded = false;
      player.attackCooldown = 0;
    });
    
    state.effects = [];
  })),
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => set(produce(state => {
    state.players[playerId].position = position;
    state.players[playerId].velocity = velocity;
  })),
  
  updatePlayerState: (playerId: string, mechaState: MechaData['state']) => set(produce(state => {
    state.players[playerId].state = mechaState;
  })),
  
  updatePlayerFacing: (playerId: string, facingRight: boolean) => set(produce(state => {
    state.players[playerId].facingRight = facingRight;
  })),
  
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => set(produce(state => {
    state.players[playerId].isGrounded = isGrounded;
  })),
  
  updatePlayerDefending: (playerId: string, isDefending: boolean) => set(produce(state => {
    state.players[playerId].isDefending = isDefending;
  })),
  
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => set(produce(state => {
    state.players[playerId].attackCooldown = cooldown;
  })),
  
  dealDamage: (targetId: string, damage: number) => set(produce(state => {
    const target = state.players[targetId];
    const actualDamage = target.isDefending ? Math.floor(damage * 0.5) : damage;
    target.health = Math.max(0, target.health - actualDamage);
    
    // 添加受击特效
    state.effects.push({
      id: `hit-${Date.now()}`,
      type: 'HIT',
      position: { ...target.position },
      duration: 300,
      timer: 0,
      frame: 0,
    });
    
    // 检查游戏结束
    if (target.health === 0) {
      const winnerId = targetId === 'player1' ? 'player2' : 'player1';
      state.gameState.phase = 'RESULT';
      state.gameState.winner = winnerId;
      state.players[winnerId].score += 1;
    }
  })),
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => set(produce(state => {
    const player = state.players[playerimport { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
  winner: null,
};

// 游戏Store接口
interface GameStore {
  // 状态
  gameState: GameState;
  players: Record<string, MechaData>;
  effects: Effect[];
  
  // 游戏流程控制
  startGame: () => void;
  pauseGame: () => void;
  resumeGame: () => void;
  endGame: (winner: string | null) => void;
  resetGame: () => void;
  nextRound: () => void;
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => void;
  updatePlayerState: (playerId: string, state: MechaData['state']) => void;
  updatePlayerFacing: (playerId: string, facingRight: boolean) => void;
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => void;
  updatePlayerDefending: (playerId: string, isDefending: boolean) => void;
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => void;
  dealDamage: (targetId: string, damage: number, attackerId?: string) => void;
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => void;
  setAnimationFrame: (playerId: string, frame: number) => void;
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => string;
  updateEffects: (deltaTime: number) => void;
  removeEffect: (effectId: string) => void;
  clearEffects: () => void;
  
  // 游戏时间
  tick: (deltaTime: number) => void;
}

// 创建Game Store
export const useGameStore = create<GameStore>((set, get) => ({
  // 初始状态
  gameState: initialGameState,
  players: {
    player1: createInitialMecha('player1', 'BLUE', 200),
    player2: createInitialMecha('player2', 'RED', 550),
  },
  effects: [],
  
  // 游戏流程控制
  startGame: () => set(produce(state => {
    state.gameState.phase = 'PLAYING';
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.winner = null;
  })),
  
  pauseGame: () => set(produce(state => {
    if (state.gameState.phase === 'PLAYING') {
      state.gameState.phase = 'PAUSED';
    }
  })),
  
  resumeGame: () => set(produce(state => {
    if (state.gameState.phase === 'PAUSED') {
      state.gameState.phase = 'PLAYING';
    }
  })),
  
  endGame: (winner: string | null) => set(produce(state => {
    state.gameState.phase = 'RESULT';
    state.gameState.winner = winner;
    if (winner && state.players[winner]) {
      state.players[winner].score += 1;
    }
  })),
  
  resetGame: () => set(produce(state => {
    state.gameState = initialGameState;
    state.players = {
      player1: createInitialMecha('player1', 'BLUE', 200),
      player2: createInitialMecha('player2', 'RED', 550),
    };
    state.effects = [];
  })),
  
  nextRound: () => set(produce(state => {
    state.gameState.round += 1;
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.phase = 'PLAYING';
    state.gameState.winner = null;
    
    // 重置玩家位置和状态
    Object.keys(state.players).forEach(playerId => {
      const player = state.players[playerId];
      player.health = player.maxHealth;
      player.position = playerId === 'player1' ? { x: 200, y: 400 } : { x: 550, y: 400 };
      player.velocity = { x: 0, y: 0 };
      player.state = 'IDLE';
      player.isDefending = false;
      player.isGrounded = false;
      player.attackCooldown = 0;
    });
    
    state.effects = [];
  })),
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => set(produce(state => {
    state.players[playerId].position = position;
    state.players[playerId].velocity = velocity;
  })),
  
  updatePlayerState: (playerId: string, mechaState: MechaData['state']) => set(produce(state => {
    state.players[playerId].state = mechaState;
  })),
  
  updatePlayerFacing: (playerId: string, facingRight: boolean) => set(produce(state => {
    state.players[playerId].facingRight = facingRight;
  })),
  
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => set(produce(state => {
    state.players[playerId].isGrounded = isGrounded;
  })),
  
  updatePlayerDefending: (playerId: string, isDefending: boolean) => set(produce(state => {
    state.players[playerId].isDefending = isDefending;
  })),
  
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => set(produce(state => {
    state.players[playerId].attackCooldown = cooldown;
  })),
  
  dealDamage: (targetId: string, damage: number) => set(produce(state => {
    const target = state.players[targetId];
    const actualDamage = target.isDefending ? Math.floor(damage * 0.5) : damage;
    target.health = Math.max(0, target.health - actualDamage);
    
    // 添加受击特效
    state.effects.push({
      id: `hit-${Date.now()}`,
      type: 'HIT',
      position: { ...target.position },
      duration: 300,
      timer: 0,
      frame: 0,
    });
    
    // 检查游戏结束
    if (target.health === 0) {
      const winnerId = targetId === 'player1' ? 'player2' : 'player1';
      state.gameState.phase = 'RESULT';
      state.gameState.winner = winnerId;
      state.players[winnerId].score += 1;
    }
  })),
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => set(produce(state => {
    const player = state.players[playerId];
    player.animationTimerimport { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
  winner: null,
};

// 游戏Store接口
interface GameStore {
  // 状态
  gameState: GameState;
  players: Record<string, MechaData>;
  effects: Effect[];
  
  // 游戏流程控制
  startGame: () => void;
  pauseGame: () => void;
  resumeGame: () => void;
  endGame: (winner: string | null) => void;
  resetGame: () => void;
  nextRound: () => void;
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => void;
  updatePlayerState: (playerId: string, state: MechaData['state']) => void;
  updatePlayerFacing: (playerId: string, facingRight: boolean) => void;
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => void;
  updatePlayerDefending: (playerId: string, isDefending: boolean) => void;
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => void;
  dealDamage: (targetId: string, damage: number, attackerId?: string) => void;
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => void;
  setAnimationFrame: (playerId: string, frame: number) => void;
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => string;
  updateEffects: (deltaTime: number) => void;
  removeEffect: (effectId: string) => void;
  clearEffects: () => void;
  
  // 游戏时间
  tick: (deltaTime: number) => void;
}

// 创建Game Store
export const useGameStore = create<GameStore>((set, get) => ({
  // 初始状态
  gameState: initialGameState,
  players: {
    player1: createInitialMecha('player1', 'BLUE', 200),
    player2: createInitialMecha('player2', 'RED', 550),
  },
  effects: [],
  
  // 游戏流程控制
  startGame: () => set(produce(state => {
    state.gameState.phase = 'PLAYING';
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.winner = null;
  })),
  
  pauseGame: () => set(produce(state => {
    if (state.gameState.phase === 'PLAYING') {
      state.gameState.phase = 'PAUSED';
    }
  })),
  
  resumeGame: () => set(produce(state => {
    if (state.gameState.phase === 'PAUSED') {
      state.gameState.phase = 'PLAYING';
    }
  })),
  
  endGame: (winner: string | null) => set(produce(state => {
    state.gameState.phase = 'RESULT';
    state.gameState.winner = winner;
    if (winner && state.players[winner]) {
      state.players[winner].score += 1;
    }
  })),
  
  resetGame: () => set(produce(state => {
    state.gameState = initialGameState;
    state.players = {
      player1: createInitialMecha('player1', 'BLUE', 200),
      player2: createInitialMecha('player2', 'RED', 550),
    };
    state.effects = [];
  })),
  
  nextRound: () => set(produce(state => {
    state.gameState.round += 1;
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.phase = 'PLAYING';
    state.gameState.winner = null;
    
    // 重置玩家位置和状态
    Object.keys(state.players).forEach(playerId => {
      const player = state.players[playerId];
      player.health = player.maxHealth;
      player.position = playerId === 'player1' ? { x: 200, y: 400 } : { x: 550, y: 400 };
      player.velocity = { x: 0, y: 0 };
      player.state = 'IDLE';
      player.isDefending = false;
      player.isGrounded = false;
      player.attackCooldown = 0;
    });
    
    state.effects = [];
  })),
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => set(produce(state => {
    state.players[playerId].position = position;
    state.players[playerId].velocity = velocity;
  })),
  
  updatePlayerState: (playerId: string, mechaState: MechaData['state']) => set(produce(state => {
    state.players[playerId].state = mechaState;
  })),
  
  updatePlayerFacing: (playerId: string, facingRight: boolean) => set(produce(state => {
    state.players[playerId].facingRight = facingRight;
  })),
  
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => set(produce(state => {
    state.players[playerId].isGrounded = isGrounded;
  })),
  
  updatePlayerDefending: (playerId: string, isDefending: boolean) => set(produce(state => {
    state.players[playerId].isDefending = isDefending;
  })),
  
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => set(produce(state => {
    state.players[playerId].attackCooldown = cooldown;
  })),
  
  dealDamage: (targetId: string, damage: number) => set(produce(state => {
    const target = state.players[targetId];
    const actualDamage = target.isDefending ? Math.floor(damage * 0.5) : damage;
    target.health = Math.max(0, target.health - actualDamage);
    
    // 添加受击特效
    state.effects.push({
      id: `hit-${Date.now()}`,
      type: 'HIT',
      position: { ...target.position },
      duration: 300,
      timer: 0,
      frame: 0,
    });
    
    // 检查游戏结束
    if (target.health === 0) {
      const winnerId = targetId === 'player1' ? 'player2' : 'player1';
      state.gameState.phase = 'RESULT';
      state.gameState.winner = winnerId;
      state.players[winnerId].score += 1;
    }
  })),
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => set(produce(state => {
    const player = state.players[playerId];
    player.animationTimer += deltaTime;
  }import { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
  winner: null,
};

// 游戏Store接口
interface GameStore {
  // 状态
  gameState: GameState;
  players: Record<string, MechaData>;
  effects: Effect[];
  
  // 游戏流程控制
  startGame: () => void;
  pauseGame: () => void;
  resumeGame: () => void;
  endGame: (winner: string | null) => void;
  resetGame: () => void;
  nextRound: () => void;
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => void;
  updatePlayerState: (playerId: string, state: MechaData['state']) => void;
  updatePlayerFacing: (playerId: string, facingRight: boolean) => void;
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => void;
  updatePlayerDefending: (playerId: string, isDefending: boolean) => void;
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => void;
  dealDamage: (targetId: string, damage: number, attackerId?: string) => void;
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => void;
  setAnimationFrame: (playerId: string, frame: number) => void;
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => string;
  updateEffects: (deltaTime: number) => void;
  removeEffect: (effectId: string) => void;
  clearEffects: () => void;
  
  // 游戏时间
  tick: (deltaTime: number) => void;
}

// 创建Game Store
export const useGameStore = create<GameStore>((set, get) => ({
  // 初始状态
  gameState: initialGameState,
  players: {
    player1: createInitialMecha('player1', 'BLUE', 200),
    player2: createInitialMecha('player2', 'RED', 550),
  },
  effects: [],
  
  // 游戏流程控制
  startGame: () => set(produce(state => {
    state.gameState.phase = 'PLAYING';
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.winner = null;
  })),
  
  pauseGame: () => set(produce(state => {
    if (state.gameState.phase === 'PLAYING') {
      state.gameState.phase = 'PAUSED';
    }
  })),
  
  resumeGame: () => set(produce(state => {
    if (state.gameState.phase === 'PAUSED') {
      state.gameState.phase = 'PLAYING';
    }
  })),
  
  endGame: (winner: string | null) => set(produce(state => {
    state.gameState.phase = 'RESULT';
    state.gameState.winner = winner;
    if (winner && state.players[winner]) {
      state.players[winner].score += 1;
    }
  })),
  
  resetGame: () => set(produce(state => {
    state.gameState = initialGameState;
    state.players = {
      player1: createInitialMecha('player1', 'BLUE', 200),
      player2: createInitialMecha('player2', 'RED', 550),
    };
    state.effects = [];
  })),
  
  nextRound: () => set(produce(state => {
    state.gameState.round += 1;
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.phase = 'PLAYING';
    state.gameState.winner = null;
    
    // 重置玩家位置和状态
    Object.keys(state.players).forEach(playerId => {
      const player = state.players[playerId];
      player.health = player.maxHealth;
      player.position = playerId === 'player1' ? { x: 200, y: 400 } : { x: 550, y: 400 };
      player.velocity = { x: 0, y: 0 };
      player.state = 'IDLE';
      player.isDefending = false;
      player.isGrounded = false;
      player.attackCooldown = 0;
    });
    
    state.effects = [];
  })),
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => set(produce(state => {
    state.players[playerId].position = position;
    state.players[playerId].velocity = velocity;
  })),
  
  updatePlayerState: (playerId: string, mechaState: MechaData['state']) => set(produce(state => {
    state.players[playerId].state = mechaState;
  })),
  
  updatePlayerFacing: (playerId: string, facingRight: boolean) => set(produce(state => {
    state.players[playerId].facingRight = facingRight;
  })),
  
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => set(produce(state => {
    state.players[playerId].isGrounded = isGrounded;
  })),
  
  updatePlayerDefending: (playerId: string, isDefending: boolean) => set(produce(state => {
    state.players[playerId].isDefending = isDefending;
  })),
  
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => set(produce(state => {
    state.players[playerId].attackCooldown = cooldown;
  })),
  
  dealDamage: (targetId: string, damage: number) => set(produce(state => {
    const target = state.players[targetId];
    const actualDamage = target.isDefending ? Math.floor(damage * 0.5) : damage;
    target.health = Math.max(0, target.health - actualDamage);
    
    // 添加受击特效
    state.effects.push({
      id: `hit-${Date.now()}`,
      type: 'HIT',
      position: { ...target.position },
      duration: 300,
      timer: 0,
      frame: 0,
    });
    
    // 检查游戏结束
    if (target.health === 0) {
      const winnerId = targetId === 'player1' ? 'player2' : 'player1';
      state.gameState.phase = 'RESULT';
      state.gameState.winner = winnerId;
      state.players[winnerId].score += 1;
    }
  })),
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => set(produce(state => {
    const player = state.players[playerId];
    player.animationTimer += deltaTime;
  })),
  
  setimport { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
  winner: null,
};

// 游戏Store接口
interface GameStore {
  // 状态
  gameState: GameState;
  players: Record<string, MechaData>;
  effects: Effect[];
  
  // 游戏流程控制
  startGame: () => void;
  pauseGame: () => void;
  resumeGame: () => void;
  endGame: (winner: string | null) => void;
  resetGame: () => void;
  nextRound: () => void;
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => void;
  updatePlayerState: (playerId: string, state: MechaData['state']) => void;
  updatePlayerFacing: (playerId: string, facingRight: boolean) => void;
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => void;
  updatePlayerDefending: (playerId: string, isDefending: boolean) => void;
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => void;
  dealDamage: (targetId: string, damage: number, attackerId?: string) => void;
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => void;
  setAnimationFrame: (playerId: string, frame: number) => void;
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => string;
  updateEffects: (deltaTime: number) => void;
  removeEffect: (effectId: string) => void;
  clearEffects: () => void;
  
  // 游戏时间
  tick: (deltaTime: number) => void;
}

// 创建Game Store
export const useGameStore = create<GameStore>((set, get) => ({
  // 初始状态
  gameState: initialGameState,
  players: {
    player1: createInitialMecha('player1', 'BLUE', 200),
    player2: createInitialMecha('player2', 'RED', 550),
  },
  effects: [],
  
  // 游戏流程控制
  startGame: () => set(produce(state => {
    state.gameState.phase = 'PLAYING';
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.winner = null;
  })),
  
  pauseGame: () => set(produce(state => {
    if (state.gameState.phase === 'PLAYING') {
      state.gameState.phase = 'PAUSED';
    }
  })),
  
  resumeGame: () => set(produce(state => {
    if (state.gameState.phase === 'PAUSED') {
      state.gameState.phase = 'PLAYING';
    }
  })),
  
  endGame: (winner: string | null) => set(produce(state => {
    state.gameState.phase = 'RESULT';
    state.gameState.winner = winner;
    if (winner && state.players[winner]) {
      state.players[winner].score += 1;
    }
  })),
  
  resetGame: () => set(produce(state => {
    state.gameState = initialGameState;
    state.players = {
      player1: createInitialMecha('player1', 'BLUE', 200),
      player2: createInitialMecha('player2', 'RED', 550),
    };
    state.effects = [];
  })),
  
  nextRound: () => set(produce(state => {
    state.gameState.round += 1;
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.phase = 'PLAYING';
    state.gameState.winner = null;
    
    // 重置玩家位置和状态
    Object.keys(state.players).forEach(playerId => {
      const player = state.players[playerId];
      player.health = player.maxHealth;
      player.position = playerId === 'player1' ? { x: 200, y: 400 } : { x: 550, y: 400 };
      player.velocity = { x: 0, y: 0 };
      player.state = 'IDLE';
      player.isDefending = false;
      player.isGrounded = false;
      player.attackCooldown = 0;
    });
    
    state.effects = [];
  })),
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => set(produce(state => {
    state.players[playerId].position = position;
    state.players[playerId].velocity = velocity;
  })),
  
  updatePlayerState: (playerId: string, mechaState: MechaData['state']) => set(produce(state => {
    state.players[playerId].state = mechaState;
  })),
  
  updatePlayerFacing: (playerId: string, facingRight: boolean) => set(produce(state => {
    state.players[playerId].facingRight = facingRight;
  })),
  
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => set(produce(state => {
    state.players[playerId].isGrounded = isGrounded;
  })),
  
  updatePlayerDefending: (playerId: string, isDefending: boolean) => set(produce(state => {
    state.players[playerId].isDefending = isDefending;
  })),
  
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => set(produce(state => {
    state.players[playerId].attackCooldown = cooldown;
  })),
  
  dealDamage: (targetId: string, damage: number) => set(produce(state => {
    const target = state.players[targetId];
    const actualDamage = target.isDefending ? Math.floor(damage * 0.5) : damage;
    target.health = Math.max(0, target.health - actualDamage);
    
    // 添加受击特效
    state.effects.push({
      id: `hit-${Date.now()}`,
      type: 'HIT',
      position: { ...target.position },
      duration: 300,
      timer: 0,
      frame: 0,
    });
    
    // 检查游戏结束
    if (target.health === 0) {
      const winnerId = targetId === 'player1' ? 'player2' : 'player1';
      state.gameState.phase = 'RESULT';
      state.gameState.winner = winnerId;
      state.players[winnerId].score += 1;
    }
  })),
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => set(produce(state => {
    const player = state.players[playerId];
    player.animationTimer += deltaTime;
  })),
  
  setAnimationFrame: (playerId:import { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
  winner: null,
};

// 游戏Store接口
interface GameStore {
  // 状态
  gameState: GameState;
  players: Record<string, MechaData>;
  effects: Effect[];
  
  // 游戏流程控制
  startGame: () => void;
  pauseGame: () => void;
  resumeGame: () => void;
  endGame: (winner: string | null) => void;
  resetGame: () => void;
  nextRound: () => void;
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => void;
  updatePlayerState: (playerId: string, state: MechaData['state']) => void;
  updatePlayerFacing: (playerId: string, facingRight: boolean) => void;
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => void;
  updatePlayerDefending: (playerId: string, isDefending: boolean) => void;
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => void;
  dealDamage: (targetId: string, damage: number, attackerId?: string) => void;
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => void;
  setAnimationFrame: (playerId: string, frame: number) => void;
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => string;
  updateEffects: (deltaTime: number) => void;
  removeEffect: (effectId: string) => void;
  clearEffects: () => void;
  
  // 游戏时间
  tick: (deltaTime: number) => void;
}

// 创建Game Store
export const useGameStore = create<GameStore>((set, get) => ({
  // 初始状态
  gameState: initialGameState,
  players: {
    player1: createInitialMecha('player1', 'BLUE', 200),
    player2: createInitialMecha('player2', 'RED', 550),
  },
  effects: [],
  
  // 游戏流程控制
  startGame: () => set(produce(state => {
    state.gameState.phase = 'PLAYING';
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.winner = null;
  })),
  
  pauseGame: () => set(produce(state => {
    if (state.gameState.phase === 'PLAYING') {
      state.gameState.phase = 'PAUSED';
    }
  })),
  
  resumeGame: () => set(produce(state => {
    if (state.gameState.phase === 'PAUSED') {
      state.gameState.phase = 'PLAYING';
    }
  })),
  
  endGame: (winner: string | null) => set(produce(state => {
    state.gameState.phase = 'RESULT';
    state.gameState.winner = winner;
    if (winner && state.players[winner]) {
      state.players[winner].score += 1;
    }
  })),
  
  resetGame: () => set(produce(state => {
    state.gameState = initialGameState;
    state.players = {
      player1: createInitialMecha('player1', 'BLUE', 200),
      player2: createInitialMecha('player2', 'RED', 550),
    };
    state.effects = [];
  })),
  
  nextRound: () => set(produce(state => {
    state.gameState.round += 1;
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.phase = 'PLAYING';
    state.gameState.winner = null;
    
    // 重置玩家位置和状态
    Object.keys(state.players).forEach(playerId => {
      const player = state.players[playerId];
      player.health = player.maxHealth;
      player.position = playerId === 'player1' ? { x: 200, y: 400 } : { x: 550, y: 400 };
      player.velocity = { x: 0, y: 0 };
      player.state = 'IDLE';
      player.isDefending = false;
      player.isGrounded = false;
      player.attackCooldown = 0;
    });
    
    state.effects = [];
  })),
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => set(produce(state => {
    state.players[playerId].position = position;
    state.players[playerId].velocity = velocity;
  })),
  
  updatePlayerState: (playerId: string, mechaState: MechaData['state']) => set(produce(state => {
    state.players[playerId].state = mechaState;
  })),
  
  updatePlayerFacing: (playerId: string, facingRight: boolean) => set(produce(state => {
    state.players[playerId].facingRight = facingRight;
  })),
  
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => set(produce(state => {
    state.players[playerId].isGrounded = isGrounded;
  })),
  
  updatePlayerDefending: (playerId: string, isDefending: boolean) => set(produce(state => {
    state.players[playerId].isDefending = isDefending;
  })),
  
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => set(produce(state => {
    state.players[playerId].attackCooldown = cooldown;
  })),
  
  dealDamage: (targetId: string, damage: number) => set(produce(state => {
    const target = state.players[targetId];
    const actualDamage = target.isDefending ? Math.floor(damage * 0.5) : damage;
    target.health = Math.max(0, target.health - actualDamage);
    
    // 添加受击特效
    state.effects.push({
      id: `hit-${Date.now()}`,
      type: 'HIT',
      position: { ...target.position },
      duration: 300,
      timer: 0,
      frame: 0,
    });
    
    // 检查游戏结束
    if (target.health === 0) {
      const winnerId = targetId === 'player1' ? 'player2' : 'player1';
      state.gameState.phase = 'RESULT';
      state.gameState.winner = winnerId;
      state.players[winnerId].score += 1;
    }
  })),
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => set(produce(state => {
    const player = state.players[playerId];
    player.animationTimer += deltaTime;
  })),
  
  setAnimationFrame: (playerId: string, frame: number) =>import { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
  winner: null,
};

// 游戏Store接口
interface GameStore {
  // 状态
  gameState: GameState;
  players: Record<string, MechaData>;
  effects: Effect[];
  
  // 游戏流程控制
  startGame: () => void;
  pauseGame: () => void;
  resumeGame: () => void;
  endGame: (winner: string | null) => void;
  resetGame: () => void;
  nextRound: () => void;
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => void;
  updatePlayerState: (playerId: string, state: MechaData['state']) => void;
  updatePlayerFacing: (playerId: string, facingRight: boolean) => void;
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => void;
  updatePlayerDefending: (playerId: string, isDefending: boolean) => void;
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => void;
  dealDamage: (targetId: string, damage: number, attackerId?: string) => void;
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => void;
  setAnimationFrame: (playerId: string, frame: number) => void;
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => string;
  updateEffects: (deltaTime: number) => void;
  removeEffect: (effectId: string) => void;
  clearEffects: () => void;
  
  // 游戏时间
  tick: (deltaTime: number) => void;
}

// 创建Game Store
export const useGameStore = create<GameStore>((set, get) => ({
  // 初始状态
  gameState: initialGameState,
  players: {
    player1: createInitialMecha('player1', 'BLUE', 200),
    player2: createInitialMecha('player2', 'RED', 550),
  },
  effects: [],
  
  // 游戏流程控制
  startGame: () => set(produce(state => {
    state.gameState.phase = 'PLAYING';
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.winner = null;
  })),
  
  pauseGame: () => set(produce(state => {
    if (state.gameState.phase === 'PLAYING') {
      state.gameState.phase = 'PAUSED';
    }
  })),
  
  resumeGame: () => set(produce(state => {
    if (state.gameState.phase === 'PAUSED') {
      state.gameState.phase = 'PLAYING';
    }
  })),
  
  endGame: (winner: string | null) => set(produce(state => {
    state.gameState.phase = 'RESULT';
    state.gameState.winner = winner;
    if (winner && state.players[winner]) {
      state.players[winner].score += 1;
    }
  })),
  
  resetGame: () => set(produce(state => {
    state.gameState = initialGameState;
    state.players = {
      player1: createInitialMecha('player1', 'BLUE', 200),
      player2: createInitialMecha('player2', 'RED', 550),
    };
    state.effects = [];
  })),
  
  nextRound: () => set(produce(state => {
    state.gameState.round += 1;
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.phase = 'PLAYING';
    state.gameState.winner = null;
    
    // 重置玩家位置和状态
    Object.keys(state.players).forEach(playerId => {
      const player = state.players[playerId];
      player.health = player.maxHealth;
      player.position = playerId === 'player1' ? { x: 200, y: 400 } : { x: 550, y: 400 };
      player.velocity = { x: 0, y: 0 };
      player.state = 'IDLE';
      player.isDefending = false;
      player.isGrounded = false;
      player.attackCooldown = 0;
    });
    
    state.effects = [];
  })),
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => set(produce(state => {
    state.players[playerId].position = position;
    state.players[playerId].velocity = velocity;
  })),
  
  updatePlayerState: (playerId: string, mechaState: MechaData['state']) => set(produce(state => {
    state.players[playerId].state = mechaState;
  })),
  
  updatePlayerFacing: (playerId: string, facingRight: boolean) => set(produce(state => {
    state.players[playerId].facingRight = facingRight;
  })),
  
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => set(produce(state => {
    state.players[playerId].isGrounded = isGrounded;
  })),
  
  updatePlayerDefending: (playerId: string, isDefending: boolean) => set(produce(state => {
    state.players[playerId].isDefending = isDefending;
  })),
  
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => set(produce(state => {
    state.players[playerId].attackCooldown = cooldown;
  })),
  
  dealDamage: (targetId: string, damage: number) => set(produce(state => {
    const target = state.players[targetId];
    const actualDamage = target.isDefending ? Math.floor(damage * 0.5) : damage;
    target.health = Math.max(0, target.health - actualDamage);
    
    // 添加受击特效
    state.effects.push({
      id: `hit-${Date.now()}`,
      type: 'HIT',
      position: { ...target.position },
      duration: 300,
      timer: 0,
      frame: 0,
    });
    
    // 检查游戏结束
    if (target.health === 0) {
      const winnerId = targetId === 'player1' ? 'player2' : 'player1';
      state.gameState.phase = 'RESULT';
      state.gameState.winner = winnerId;
      state.players[winnerId].score += 1;
    }
  })),
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => set(produce(state => {
    const player = state.players[playerId];
    player.animationTimer += deltaTime;
  })),
  
  setAnimationFrame: (playerId: string, frame: number) => set(produce(state => {
import { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
  winner: null,
};

// 游戏Store接口
interface GameStore {
  // 状态
  gameState: GameState;
  players: Record<string, MechaData>;
  effects: Effect[];
  
  // 游戏流程控制
  startGame: () => void;
  pauseGame: () => void;
  resumeGame: () => void;
  endGame: (winner: string | null) => void;
  resetGame: () => void;
  nextRound: () => void;
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => void;
  updatePlayerState: (playerId: string, state: MechaData['state']) => void;
  updatePlayerFacing: (playerId: string, facingRight: boolean) => void;
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => void;
  updatePlayerDefending: (playerId: string, isDefending: boolean) => void;
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => void;
  dealDamage: (targetId: string, damage: number, attackerId?: string) => void;
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => void;
  setAnimationFrame: (playerId: string, frame: number) => void;
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => string;
  updateEffects: (deltaTime: number) => void;
  removeEffect: (effectId: string) => void;
  clearEffects: () => void;
  
  // 游戏时间
  tick: (deltaTime: number) => void;
}

// 创建Game Store
export const useGameStore = create<GameStore>((set, get) => ({
  // 初始状态
  gameState: initialGameState,
  players: {
    player1: createInitialMecha('player1', 'BLUE', 200),
    player2: createInitialMecha('player2', 'RED', 550),
  },
  effects: [],
  
  // 游戏流程控制
  startGame: () => set(produce(state => {
    state.gameState.phase = 'PLAYING';
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.winner = null;
  })),
  
  pauseGame: () => set(produce(state => {
    if (state.gameState.phase === 'PLAYING') {
      state.gameState.phase = 'PAUSED';
    }
  })),
  
  resumeGame: () => set(produce(state => {
    if (state.gameState.phase === 'PAUSED') {
      state.gameState.phase = 'PLAYING';
    }
  })),
  
  endGame: (winner: string | null) => set(produce(state => {
    state.gameState.phase = 'RESULT';
    state.gameState.winner = winner;
    if (winner && state.players[winner]) {
      state.players[winner].score += 1;
    }
  })),
  
  resetGame: () => set(produce(state => {
    state.gameState = initialGameState;
    state.players = {
      player1: createInitialMecha('player1', 'BLUE', 200),
      player2: createInitialMecha('player2', 'RED', 550),
    };
    state.effects = [];
  })),
  
  nextRound: () => set(produce(state => {
    state.gameState.round += 1;
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.phase = 'PLAYING';
    state.gameState.winner = null;
    
    // 重置玩家位置和状态
    Object.keys(state.players).forEach(playerId => {
      const player = state.players[playerId];
      player.health = player.maxHealth;
      player.position = playerId === 'player1' ? { x: 200, y: 400 } : { x: 550, y: 400 };
      player.velocity = { x: 0, y: 0 };
      player.state = 'IDLE';
      player.isDefending = false;
      player.isGrounded = false;
      player.attackCooldown = 0;
    });
    
    state.effects = [];
  })),
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => set(produce(state => {
    state.players[playerId].position = position;
    state.players[playerId].velocity = velocity;
  })),
  
  updatePlayerState: (playerId: string, mechaState: MechaData['state']) => set(produce(state => {
    state.players[playerId].state = mechaState;
  })),
  
  updatePlayerFacing: (playerId: string, facingRight: boolean) => set(produce(state => {
    state.players[playerId].facingRight = facingRight;
  })),
  
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => set(produce(state => {
    state.players[playerId].isGrounded = isGrounded;
  })),
  
  updatePlayerDefending: (playerId: string, isDefending: boolean) => set(produce(state => {
    state.players[playerId].isDefending = isDefending;
  })),
  
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => set(produce(state => {
    state.players[playerId].attackCooldown = cooldown;
  })),
  
  dealDamage: (targetId: string, damage: number) => set(produce(state => {
    const target = state.players[targetId];
    const actualDamage = target.isDefending ? Math.floor(damage * 0.5) : damage;
    target.health = Math.max(0, target.health - actualDamage);
    
    // 添加受击特效
    state.effects.push({
      id: `hit-${Date.now()}`,
      type: 'HIT',
      position: { ...target.position },
      duration: 300,
      timer: 0,
      frame: 0,
    });
    
    // 检查游戏结束
    if (target.health === 0) {
      const winnerId = targetId === 'player1' ? 'player2' : 'player1';
      state.gameState.phase = 'RESULT';
      state.gameState.winner = winnerId;
      state.players[winnerId].score += 1;
    }
  })),
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => set(produce(state => {
    const player = state.players[playerId];
    player.animationTimer += deltaTime;
  })),
  
  setAnimationFrame: (playerId: string, frame: number) => set(produce(state => {
    state.players[playerId].animationimport { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
  winner: null,
};

// 游戏Store接口
interface GameStore {
  // 状态
  gameState: GameState;
  players: Record<string, MechaData>;
  effects: Effect[];
  
  // 游戏流程控制
  startGame: () => void;
  pauseGame: () => void;
  resumeGame: () => void;
  endGame: (winner: string | null) => void;
  resetGame: () => void;
  nextRound: () => void;
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => void;
  updatePlayerState: (playerId: string, state: MechaData['state']) => void;
  updatePlayerFacing: (playerId: string, facingRight: boolean) => void;
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => void;
  updatePlayerDefending: (playerId: string, isDefending: boolean) => void;
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => void;
  dealDamage: (targetId: string, damage: number, attackerId?: string) => void;
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => void;
  setAnimationFrame: (playerId: string, frame: number) => void;
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => string;
  updateEffects: (deltaTime: number) => void;
  removeEffect: (effectId: string) => void;
  clearEffects: () => void;
  
  // 游戏时间
  tick: (deltaTime: number) => void;
}

// 创建Game Store
export const useGameStore = create<GameStore>((set, get) => ({
  // 初始状态
  gameState: initialGameState,
  players: {
    player1: createInitialMecha('player1', 'BLUE', 200),
    player2: createInitialMecha('player2', 'RED', 550),
  },
  effects: [],
  
  // 游戏流程控制
  startGame: () => set(produce(state => {
    state.gameState.phase = 'PLAYING';
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.winner = null;
  })),
  
  pauseGame: () => set(produce(state => {
    if (state.gameState.phase === 'PLAYING') {
      state.gameState.phase = 'PAUSED';
    }
  })),
  
  resumeGame: () => set(produce(state => {
    if (state.gameState.phase === 'PAUSED') {
      state.gameState.phase = 'PLAYING';
    }
  })),
  
  endGame: (winner: string | null) => set(produce(state => {
    state.gameState.phase = 'RESULT';
    state.gameState.winner = winner;
    if (winner && state.players[winner]) {
      state.players[winner].score += 1;
    }
  })),
  
  resetGame: () => set(produce(state => {
    state.gameState = initialGameState;
    state.players = {
      player1: createInitialMecha('player1', 'BLUE', 200),
      player2: createInitialMecha('player2', 'RED', 550),
    };
    state.effects = [];
  })),
  
  nextRound: () => set(produce(state => {
    state.gameState.round += 1;
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.phase = 'PLAYING';
    state.gameState.winner = null;
    
    // 重置玩家位置和状态
    Object.keys(state.players).forEach(playerId => {
      const player = state.players[playerId];
      player.health = player.maxHealth;
      player.position = playerId === 'player1' ? { x: 200, y: 400 } : { x: 550, y: 400 };
      player.velocity = { x: 0, y: 0 };
      player.state = 'IDLE';
      player.isDefending = false;
      player.isGrounded = false;
      player.attackCooldown = 0;
    });
    
    state.effects = [];
  })),
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => set(produce(state => {
    state.players[playerId].position = position;
    state.players[playerId].velocity = velocity;
  })),
  
  updatePlayerState: (playerId: string, mechaState: MechaData['state']) => set(produce(state => {
    state.players[playerId].state = mechaState;
  })),
  
  updatePlayerFacing: (playerId: string, facingRight: boolean) => set(produce(state => {
    state.players[playerId].facingRight = facingRight;
  })),
  
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => set(produce(state => {
    state.players[playerId].isGrounded = isGrounded;
  })),
  
  updatePlayerDefending: (playerId: string, isDefending: boolean) => set(produce(state => {
    state.players[playerId].isDefending = isDefending;
  })),
  
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => set(produce(state => {
    state.players[playerId].attackCooldown = cooldown;
  })),
  
  dealDamage: (targetId: string, damage: number) => set(produce(state => {
    const target = state.players[targetId];
    const actualDamage = target.isDefending ? Math.floor(damage * 0.5) : damage;
    target.health = Math.max(0, target.health - actualDamage);
    
    // 添加受击特效
    state.effects.push({
      id: `hit-${Date.now()}`,
      type: 'HIT',
      position: { ...target.position },
      duration: 300,
      timer: 0,
      frame: 0,
    });
    
    // 检查游戏结束
    if (target.health === 0) {
      const winnerId = targetId === 'player1' ? 'player2' : 'player1';
      state.gameState.phase = 'RESULT';
      state.gameState.winner = winnerId;
      state.players[winnerId].score += 1;
    }
  })),
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => set(produce(state => {
    const player = state.players[playerId];
    player.animationTimer += deltaTime;
  })),
  
  setAnimationFrame: (playerId: string, frame: number) => set(produce(state => {
    state.players[playerId].animationFrame = frame;
  }import { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
  winner: null,
};

// 游戏Store接口
interface GameStore {
  // 状态
  gameState: GameState;
  players: Record<string, MechaData>;
  effects: Effect[];
  
  // 游戏流程控制
  startGame: () => void;
  pauseGame: () => void;
  resumeGame: () => void;
  endGame: (winner: string | null) => void;
  resetGame: () => void;
  nextRound: () => void;
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => void;
  updatePlayerState: (playerId: string, state: MechaData['state']) => void;
  updatePlayerFacing: (playerId: string, facingRight: boolean) => void;
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => void;
  updatePlayerDefending: (playerId: string, isDefending: boolean) => void;
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => void;
  dealDamage: (targetId: string, damage: number, attackerId?: string) => void;
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => void;
  setAnimationFrame: (playerId: string, frame: number) => void;
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => string;
  updateEffects: (deltaTime: number) => void;
  removeEffect: (effectId: string) => void;
  clearEffects: () => void;
  
  // 游戏时间
  tick: (deltaTime: number) => void;
}

// 创建Game Store
export const useGameStore = create<GameStore>((set, get) => ({
  // 初始状态
  gameState: initialGameState,
  players: {
    player1: createInitialMecha('player1', 'BLUE', 200),
    player2: createInitialMecha('player2', 'RED', 550),
  },
  effects: [],
  
  // 游戏流程控制
  startGame: () => set(produce(state => {
    state.gameState.phase = 'PLAYING';
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.winner = null;
  })),
  
  pauseGame: () => set(produce(state => {
    if (state.gameState.phase === 'PLAYING') {
      state.gameState.phase = 'PAUSED';
    }
  })),
  
  resumeGame: () => set(produce(state => {
    if (state.gameState.phase === 'PAUSED') {
      state.gameState.phase = 'PLAYING';
    }
  })),
  
  endGame: (winner: string | null) => set(produce(state => {
    state.gameState.phase = 'RESULT';
    state.gameState.winner = winner;
    if (winner && state.players[winner]) {
      state.players[winner].score += 1;
    }
  })),
  
  resetGame: () => set(produce(state => {
    state.gameState = initialGameState;
    state.players = {
      player1: createInitialMecha('player1', 'BLUE', 200),
      player2: createInitialMecha('player2', 'RED', 550),
    };
    state.effects = [];
  })),
  
  nextRound: () => set(produce(state => {
    state.gameState.round += 1;
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.phase = 'PLAYING';
    state.gameState.winner = null;
    
    // 重置玩家位置和状态
    Object.keys(state.players).forEach(playerId => {
      const player = state.players[playerId];
      player.health = player.maxHealth;
      player.position = playerId === 'player1' ? { x: 200, y: 400 } : { x: 550, y: 400 };
      player.velocity = { x: 0, y: 0 };
      player.state = 'IDLE';
      player.isDefending = false;
      player.isGrounded = false;
      player.attackCooldown = 0;
    });
    
    state.effects = [];
  })),
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => set(produce(state => {
    state.players[playerId].position = position;
    state.players[playerId].velocity = velocity;
  })),
  
  updatePlayerState: (playerId: string, mechaState: MechaData['state']) => set(produce(state => {
    state.players[playerId].state = mechaState;
  })),
  
  updatePlayerFacing: (playerId: string, facingRight: boolean) => set(produce(state => {
    state.players[playerId].facingRight = facingRight;
  })),
  
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => set(produce(state => {
    state.players[playerId].isGrounded = isGrounded;
  })),
  
  updatePlayerDefending: (playerId: string, isDefending: boolean) => set(produce(state => {
    state.players[playerId].isDefending = isDefending;
  })),
  
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => set(produce(state => {
    state.players[playerId].attackCooldown = cooldown;
  })),
  
  dealDamage: (targetId: string, damage: number) => set(produce(state => {
    const target = state.players[targetId];
    const actualDamage = target.isDefending ? Math.floor(damage * 0.5) : damage;
    target.health = Math.max(0, target.health - actualDamage);
    
    // 添加受击特效
    state.effects.push({
      id: `hit-${Date.now()}`,
      type: 'HIT',
      position: { ...target.position },
      duration: 300,
      timer: 0,
      frame: 0,
    });
    
    // 检查游戏结束
    if (target.health === 0) {
      const winnerId = targetId === 'player1' ? 'player2' : 'player1';
      state.gameState.phase = 'RESULT';
      state.gameState.winner = winnerId;
      state.players[winnerId].score += 1;
    }
  })),
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => set(produce(state => {
    const player = state.players[playerId];
    player.animationTimer += deltaTime;
  })),
  
  setAnimationFrame: (playerId: string, frame: number) => set(produce(state => {
    state.players[playerId].animationFrame = frame;
  })),
  
  //import { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
  winner: null,
};

// 游戏Store接口
interface GameStore {
  // 状态
  gameState: GameState;
  players: Record<string, MechaData>;
  effects: Effect[];
  
  // 游戏流程控制
  startGame: () => void;
  pauseGame: () => void;
  resumeGame: () => void;
  endGame: (winner: string | null) => void;
  resetGame: () => void;
  nextRound: () => void;
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => void;
  updatePlayerState: (playerId: string, state: MechaData['state']) => void;
  updatePlayerFacing: (playerId: string, facingRight: boolean) => void;
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => void;
  updatePlayerDefending: (playerId: string, isDefending: boolean) => void;
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => void;
  dealDamage: (targetId: string, damage: number, attackerId?: string) => void;
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => void;
  setAnimationFrame: (playerId: string, frame: number) => void;
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => string;
  updateEffects: (deltaTime: number) => void;
  removeEffect: (effectId: string) => void;
  clearEffects: () => void;
  
  // 游戏时间
  tick: (deltaTime: number) => void;
}

// 创建Game Store
export const useGameStore = create<GameStore>((set, get) => ({
  // 初始状态
  gameState: initialGameState,
  players: {
    player1: createInitialMecha('player1', 'BLUE', 200),
    player2: createInitialMecha('player2', 'RED', 550),
  },
  effects: [],
  
  // 游戏流程控制
  startGame: () => set(produce(state => {
    state.gameState.phase = 'PLAYING';
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.winner = null;
  })),
  
  pauseGame: () => set(produce(state => {
    if (state.gameState.phase === 'PLAYING') {
      state.gameState.phase = 'PAUSED';
    }
  })),
  
  resumeGame: () => set(produce(state => {
    if (state.gameState.phase === 'PAUSED') {
      state.gameState.phase = 'PLAYING';
    }
  })),
  
  endGame: (winner: string | null) => set(produce(state => {
    state.gameState.phase = 'RESULT';
    state.gameState.winner = winner;
    if (winner && state.players[winner]) {
      state.players[winner].score += 1;
    }
  })),
  
  resetGame: () => set(produce(state => {
    state.gameState = initialGameState;
    state.players = {
      player1: createInitialMecha('player1', 'BLUE', 200),
      player2: createInitialMecha('player2', 'RED', 550),
    };
    state.effects = [];
  })),
  
  nextRound: () => set(produce(state => {
    state.gameState.round += 1;
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.phase = 'PLAYING';
    state.gameState.winner = null;
    
    // 重置玩家位置和状态
    Object.keys(state.players).forEach(playerId => {
      const player = state.players[playerId];
      player.health = player.maxHealth;
      player.position = playerId === 'player1' ? { x: 200, y: 400 } : { x: 550, y: 400 };
      player.velocity = { x: 0, y: 0 };
      player.state = 'IDLE';
      player.isDefending = false;
      player.isGrounded = false;
      player.attackCooldown = 0;
    });
    
    state.effects = [];
  })),
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => set(produce(state => {
    state.players[playerId].position = position;
    state.players[playerId].velocity = velocity;
  })),
  
  updatePlayerState: (playerId: string, mechaState: MechaData['state']) => set(produce(state => {
    state.players[playerId].state = mechaState;
  })),
  
  updatePlayerFacing: (playerId: string, facingRight: boolean) => set(produce(state => {
    state.players[playerId].facingRight = facingRight;
  })),
  
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => set(produce(state => {
    state.players[playerId].isGrounded = isGrounded;
  })),
  
  updatePlayerDefending: (playerId: string, isDefending: boolean) => set(produce(state => {
    state.players[playerId].isDefending = isDefending;
  })),
  
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => set(produce(state => {
    state.players[playerId].attackCooldown = cooldown;
  })),
  
  dealDamage: (targetId: string, damage: number) => set(produce(state => {
    const target = state.players[targetId];
    const actualDamage = target.isDefending ? Math.floor(damage * 0.5) : damage;
    target.health = Math.max(0, target.health - actualDamage);
    
    // 添加受击特效
    state.effects.push({
      id: `hit-${Date.now()}`,
      type: 'HIT',
      position: { ...target.position },
      duration: 300,
      timer: 0,
      frame: 0,
    });
    
    // 检查游戏结束
    if (target.health === 0) {
      const winnerId = targetId === 'player1' ? 'player2' : 'player1';
      state.gameState.phase = 'RESULT';
      state.gameState.winner = winnerId;
      state.players[winnerId].score += 1;
    }
  })),
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => set(produce(state => {
    const player = state.players[playerId];
    player.animationTimer += deltaTime;
  })),
  
  setAnimationFrame: (playerId: string, frame: number) => set(produce(state => {
    state.players[playerId].animationFrame = frame;
  })),
  
  // 特效
  addEffect:import { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
  winner: null,
};

// 游戏Store接口
interface GameStore {
  // 状态
  gameState: GameState;
  players: Record<string, MechaData>;
  effects: Effect[];
  
  // 游戏流程控制
  startGame: () => void;
  pauseGame: () => void;
  resumeGame: () => void;
  endGame: (winner: string | null) => void;
  resetGame: () => void;
  nextRound: () => void;
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => void;
  updatePlayerState: (playerId: string, state: MechaData['state']) => void;
  updatePlayerFacing: (playerId: string, facingRight: boolean) => void;
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => void;
  updatePlayerDefending: (playerId: string, isDefending: boolean) => void;
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => void;
  dealDamage: (targetId: string, damage: number, attackerId?: string) => void;
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => void;
  setAnimationFrame: (playerId: string, frame: number) => void;
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => string;
  updateEffects: (deltaTime: number) => void;
  removeEffect: (effectId: string) => void;
  clearEffects: () => void;
  
  // 游戏时间
  tick: (deltaTime: number) => void;
}

// 创建Game Store
export const useGameStore = create<GameStore>((set, get) => ({
  // 初始状态
  gameState: initialGameState,
  players: {
    player1: createInitialMecha('player1', 'BLUE', 200),
    player2: createInitialMecha('player2', 'RED', 550),
  },
  effects: [],
  
  // 游戏流程控制
  startGame: () => set(produce(state => {
    state.gameState.phase = 'PLAYING';
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.winner = null;
  })),
  
  pauseGame: () => set(produce(state => {
    if (state.gameState.phase === 'PLAYING') {
      state.gameState.phase = 'PAUSED';
    }
  })),
  
  resumeGame: () => set(produce(state => {
    if (state.gameState.phase === 'PAUSED') {
      state.gameState.phase = 'PLAYING';
    }
  })),
  
  endGame: (winner: string | null) => set(produce(state => {
    state.gameState.phase = 'RESULT';
    state.gameState.winner = winner;
    if (winner && state.players[winner]) {
      state.players[winner].score += 1;
    }
  })),
  
  resetGame: () => set(produce(state => {
    state.gameState = initialGameState;
    state.players = {
      player1: createInitialMecha('player1', 'BLUE', 200),
      player2: createInitialMecha('player2', 'RED', 550),
    };
    state.effects = [];
  })),
  
  nextRound: () => set(produce(state => {
    state.gameState.round += 1;
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.phase = 'PLAYING';
    state.gameState.winner = null;
    
    // 重置玩家位置和状态
    Object.keys(state.players).forEach(playerId => {
      const player = state.players[playerId];
      player.health = player.maxHealth;
      player.position = playerId === 'player1' ? { x: 200, y: 400 } : { x: 550, y: 400 };
      player.velocity = { x: 0, y: 0 };
      player.state = 'IDLE';
      player.isDefending = false;
      player.isGrounded = false;
      player.attackCooldown = 0;
    });
    
    state.effects = [];
  })),
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => set(produce(state => {
    state.players[playerId].position = position;
    state.players[playerId].velocity = velocity;
  })),
  
  updatePlayerState: (playerId: string, mechaState: MechaData['state']) => set(produce(state => {
    state.players[playerId].state = mechaState;
  })),
  
  updatePlayerFacing: (playerId: string, facingRight: boolean) => set(produce(state => {
    state.players[playerId].facingRight = facingRight;
  })),
  
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => set(produce(state => {
    state.players[playerId].isGrounded = isGrounded;
  })),
  
  updatePlayerDefending: (playerId: string, isDefending: boolean) => set(produce(state => {
    state.players[playerId].isDefending = isDefending;
  })),
  
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => set(produce(state => {
    state.players[playerId].attackCooldown = cooldown;
  })),
  
  dealDamage: (targetId: string, damage: number) => set(produce(state => {
    const target = state.players[targetId];
    const actualDamage = target.isDefending ? Math.floor(damage * 0.5) : damage;
    target.health = Math.max(0, target.health - actualDamage);
    
    // 添加受击特效
    state.effects.push({
      id: `hit-${Date.now()}`,
      type: 'HIT',
      position: { ...target.position },
      duration: 300,
      timer: 0,
      frame: 0,
    });
    
    // 检查游戏结束
    if (target.health === 0) {
      const winnerId = targetId === 'player1' ? 'player2' : 'player1';
      state.gameState.phase = 'RESULT';
      state.gameState.winner = winnerId;
      state.players[winnerId].score += 1;
    }
  })),
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => set(produce(state => {
    const player = state.players[playerId];
    player.animationTimer += deltaTime;
  })),
  
  setAnimationFrame: (playerId: string, frame: number) => set(produce(state => {
    state.players[playerId].animationFrame = frame;
  })),
  
  // 特效
  addEffect: (effect: Omit<Effect,import { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
  winner: null,
};

// 游戏Store接口
interface GameStore {
  // 状态
  gameState: GameState;
  players: Record<string, MechaData>;
  effects: Effect[];
  
  // 游戏流程控制
  startGame: () => void;
  pauseGame: () => void;
  resumeGame: () => void;
  endGame: (winner: string | null) => void;
  resetGame: () => void;
  nextRound: () => void;
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => void;
  updatePlayerState: (playerId: string, state: MechaData['state']) => void;
  updatePlayerFacing: (playerId: string, facingRight: boolean) => void;
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => void;
  updatePlayerDefending: (playerId: string, isDefending: boolean) => void;
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => void;
  dealDamage: (targetId: string, damage: number, attackerId?: string) => void;
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => void;
  setAnimationFrame: (playerId: string, frame: number) => void;
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => string;
  updateEffects: (deltaTime: number) => void;
  removeEffect: (effectId: string) => void;
  clearEffects: () => void;
  
  // 游戏时间
  tick: (deltaTime: number) => void;
}

// 创建Game Store
export const useGameStore = create<GameStore>((set, get) => ({
  // 初始状态
  gameState: initialGameState,
  players: {
    player1: createInitialMecha('player1', 'BLUE', 200),
    player2: createInitialMecha('player2', 'RED', 550),
  },
  effects: [],
  
  // 游戏流程控制
  startGame: () => set(produce(state => {
    state.gameState.phase = 'PLAYING';
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.winner = null;
  })),
  
  pauseGame: () => set(produce(state => {
    if (state.gameState.phase === 'PLAYING') {
      state.gameState.phase = 'PAUSED';
    }
  })),
  
  resumeGame: () => set(produce(state => {
    if (state.gameState.phase === 'PAUSED') {
      state.gameState.phase = 'PLAYING';
    }
  })),
  
  endGame: (winner: string | null) => set(produce(state => {
    state.gameState.phase = 'RESULT';
    state.gameState.winner = winner;
    if (winner && state.players[winner]) {
      state.players[winner].score += 1;
    }
  })),
  
  resetGame: () => set(produce(state => {
    state.gameState = initialGameState;
    state.players = {
      player1: createInitialMecha('player1', 'BLUE', 200),
      player2: createInitialMecha('player2', 'RED', 550),
    };
    state.effects = [];
  })),
  
  nextRound: () => set(produce(state => {
    state.gameState.round += 1;
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.phase = 'PLAYING';
    state.gameState.winner = null;
    
    // 重置玩家位置和状态
    Object.keys(state.players).forEach(playerId => {
      const player = state.players[playerId];
      player.health = player.maxHealth;
      player.position = playerId === 'player1' ? { x: 200, y: 400 } : { x: 550, y: 400 };
      player.velocity = { x: 0, y: 0 };
      player.state = 'IDLE';
      player.isDefending = false;
      player.isGrounded = false;
      player.attackCooldown = 0;
    });
    
    state.effects = [];
  })),
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => set(produce(state => {
    state.players[playerId].position = position;
    state.players[playerId].velocity = velocity;
  })),
  
  updatePlayerState: (playerId: string, mechaState: MechaData['state']) => set(produce(state => {
    state.players[playerId].state = mechaState;
  })),
  
  updatePlayerFacing: (playerId: string, facingRight: boolean) => set(produce(state => {
    state.players[playerId].facingRight = facingRight;
  })),
  
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => set(produce(state => {
    state.players[playerId].isGrounded = isGrounded;
  })),
  
  updatePlayerDefending: (playerId: string, isDefending: boolean) => set(produce(state => {
    state.players[playerId].isDefending = isDefending;
  })),
  
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => set(produce(state => {
    state.players[playerId].attackCooldown = cooldown;
  })),
  
  dealDamage: (targetId: string, damage: number) => set(produce(state => {
    const target = state.players[targetId];
    const actualDamage = target.isDefending ? Math.floor(damage * 0.5) : damage;
    target.health = Math.max(0, target.health - actualDamage);
    
    // 添加受击特效
    state.effects.push({
      id: `hit-${Date.now()}`,
      type: 'HIT',
      position: { ...target.position },
      duration: 300,
      timer: 0,
      frame: 0,
    });
    
    // 检查游戏结束
    if (target.health === 0) {
      const winnerId = targetId === 'player1' ? 'player2' : 'player1';
      state.gameState.phase = 'RESULT';
      state.gameState.winner = winnerId;
      state.players[winnerId].score += 1;
    }
  })),
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => set(produce(state => {
    const player = state.players[playerId];
    player.animationTimer += deltaTime;
  })),
  
  setAnimationFrame: (playerId: string, frame: number) => set(produce(state => {
    state.players[playerId].animationFrame = frame;
  })),
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'import { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
  winner: null,
};

// 游戏Store接口
interface GameStore {
  // 状态
  gameState: GameState;
  players: Record<string, MechaData>;
  effects: Effect[];
  
  // 游戏流程控制
  startGame: () => void;
  pauseGame: () => void;
  resumeGame: () => void;
  endGame: (winner: string | null) => void;
  resetGame: () => void;
  nextRound: () => void;
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => void;
  updatePlayerState: (playerId: string, state: MechaData['state']) => void;
  updatePlayerFacing: (playerId: string, facingRight: boolean) => void;
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => void;
  updatePlayerDefending: (playerId: string, isDefending: boolean) => void;
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => void;
  dealDamage: (targetId: string, damage: number, attackerId?: string) => void;
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => void;
  setAnimationFrame: (playerId: string, frame: number) => void;
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => string;
  updateEffects: (deltaTime: number) => void;
  removeEffect: (effectId: string) => void;
  clearEffects: () => void;
  
  // 游戏时间
  tick: (deltaTime: number) => void;
}

// 创建Game Store
export const useGameStore = create<GameStore>((set, get) => ({
  // 初始状态
  gameState: initialGameState,
  players: {
    player1: createInitialMecha('player1', 'BLUE', 200),
    player2: createInitialMecha('player2', 'RED', 550),
  },
  effects: [],
  
  // 游戏流程控制
  startGame: () => set(produce(state => {
    state.gameState.phase = 'PLAYING';
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.winner = null;
  })),
  
  pauseGame: () => set(produce(state => {
    if (state.gameState.phase === 'PLAYING') {
      state.gameState.phase = 'PAUSED';
    }
  })),
  
  resumeGame: () => set(produce(state => {
    if (state.gameState.phase === 'PAUSED') {
      state.gameState.phase = 'PLAYING';
    }
  })),
  
  endGame: (winner: string | null) => set(produce(state => {
    state.gameState.phase = 'RESULT';
    state.gameState.winner = winner;
    if (winner && state.players[winner]) {
      state.players[winner].score += 1;
    }
  })),
  
  resetGame: () => set(produce(state => {
    state.gameState = initialGameState;
    state.players = {
      player1: createInitialMecha('player1', 'BLUE', 200),
      player2: createInitialMecha('player2', 'RED', 550),
    };
    state.effects = [];
  })),
  
  nextRound: () => set(produce(state => {
    state.gameState.round += 1;
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.phase = 'PLAYING';
    state.gameState.winner = null;
    
    // 重置玩家位置和状态
    Object.keys(state.players).forEach(playerId => {
      const player = state.players[playerId];
      player.health = player.maxHealth;
      player.position = playerId === 'player1' ? { x: 200, y: 400 } : { x: 550, y: 400 };
      player.velocity = { x: 0, y: 0 };
      player.state = 'IDLE';
      player.isDefending = false;
      player.isGrounded = false;
      player.attackCooldown = 0;
    });
    
    state.effects = [];
  })),
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => set(produce(state => {
    state.players[playerId].position = position;
    state.players[playerId].velocity = velocity;
  })),
  
  updatePlayerState: (playerId: string, mechaState: MechaData['state']) => set(produce(state => {
    state.players[playerId].state = mechaState;
  })),
  
  updatePlayerFacing: (playerId: string, facingRight: boolean) => set(produce(state => {
    state.players[playerId].facingRight = facingRight;
  })),
  
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => set(produce(state => {
    state.players[playerId].isGrounded = isGrounded;
  })),
  
  updatePlayerDefending: (playerId: string, isDefending: boolean) => set(produce(state => {
    state.players[playerId].isDefending = isDefending;
  })),
  
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => set(produce(state => {
    state.players[playerId].attackCooldown = cooldown;
  })),
  
  dealDamage: (targetId: string, damage: number) => set(produce(state => {
    const target = state.players[targetId];
    const actualDamage = target.isDefending ? Math.floor(damage * 0.5) : damage;
    target.health = Math.max(0, target.health - actualDamage);
    
    // 添加受击特效
    state.effects.push({
      id: `hit-${Date.now()}`,
      type: 'HIT',
      position: { ...target.position },
      duration: 300,
      timer: 0,
      frame: 0,
    });
    
    // 检查游戏结束
    if (target.health === 0) {
      const winnerId = targetId === 'player1' ? 'player2' : 'player1';
      state.gameState.phase = 'RESULT';
      state.gameState.winner = winnerId;
      state.players[winnerId].score += 1;
    }
  })),
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => set(produce(state => {
    const player = state.players[playerId];
    player.animationTimer += deltaTime;
  })),
  
  setAnimationFrame: (playerId: string, frame: number) => set(produce(state => {
    state.players[playerId].animationFrame = frame;
  })),
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => {
    const idimport { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
  winner: null,
};

// 游戏Store接口
interface GameStore {
  // 状态
  gameState: GameState;
  players: Record<string, MechaData>;
  effects: Effect[];
  
  // 游戏流程控制
  startGame: () => void;
  pauseGame: () => void;
  resumeGame: () => void;
  endGame: (winner: string | null) => void;
  resetGame: () => void;
  nextRound: () => void;
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => void;
  updatePlayerState: (playerId: string, state: MechaData['state']) => void;
  updatePlayerFacing: (playerId: string, facingRight: boolean) => void;
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => void;
  updatePlayerDefending: (playerId: string, isDefending: boolean) => void;
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => void;
  dealDamage: (targetId: string, damage: number, attackerId?: string) => void;
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => void;
  setAnimationFrame: (playerId: string, frame: number) => void;
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => string;
  updateEffects: (deltaTime: number) => void;
  removeEffect: (effectId: string) => void;
  clearEffects: () => void;
  
  // 游戏时间
  tick: (deltaTime: number) => void;
}

// 创建Game Store
export const useGameStore = create<GameStore>((set, get) => ({
  // 初始状态
  gameState: initialGameState,
  players: {
    player1: createInitialMecha('player1', 'BLUE', 200),
    player2: createInitialMecha('player2', 'RED', 550),
  },
  effects: [],
  
  // 游戏流程控制
  startGame: () => set(produce(state => {
    state.gameState.phase = 'PLAYING';
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.winner = null;
  })),
  
  pauseGame: () => set(produce(state => {
    if (state.gameState.phase === 'PLAYING') {
      state.gameState.phase = 'PAUSED';
    }
  })),
  
  resumeGame: () => set(produce(state => {
    if (state.gameState.phase === 'PAUSED') {
      state.gameState.phase = 'PLAYING';
    }
  })),
  
  endGame: (winner: string | null) => set(produce(state => {
    state.gameState.phase = 'RESULT';
    state.gameState.winner = winner;
    if (winner && state.players[winner]) {
      state.players[winner].score += 1;
    }
  })),
  
  resetGame: () => set(produce(state => {
    state.gameState = initialGameState;
    state.players = {
      player1: createInitialMecha('player1', 'BLUE', 200),
      player2: createInitialMecha('player2', 'RED', 550),
    };
    state.effects = [];
  })),
  
  nextRound: () => set(produce(state => {
    state.gameState.round += 1;
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.phase = 'PLAYING';
    state.gameState.winner = null;
    
    // 重置玩家位置和状态
    Object.keys(state.players).forEach(playerId => {
      const player = state.players[playerId];
      player.health = player.maxHealth;
      player.position = playerId === 'player1' ? { x: 200, y: 400 } : { x: 550, y: 400 };
      player.velocity = { x: 0, y: 0 };
      player.state = 'IDLE';
      player.isDefending = false;
      player.isGrounded = false;
      player.attackCooldown = 0;
    });
    
    state.effects = [];
  })),
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => set(produce(state => {
    state.players[playerId].position = position;
    state.players[playerId].velocity = velocity;
  })),
  
  updatePlayerState: (playerId: string, mechaState: MechaData['state']) => set(produce(state => {
    state.players[playerId].state = mechaState;
  })),
  
  updatePlayerFacing: (playerId: string, facingRight: boolean) => set(produce(state => {
    state.players[playerId].facingRight = facingRight;
  })),
  
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => set(produce(state => {
    state.players[playerId].isGrounded = isGrounded;
  })),
  
  updatePlayerDefending: (playerId: string, isDefending: boolean) => set(produce(state => {
    state.players[playerId].isDefending = isDefending;
  })),
  
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => set(produce(state => {
    state.players[playerId].attackCooldown = cooldown;
  })),
  
  dealDamage: (targetId: string, damage: number) => set(produce(state => {
    const target = state.players[targetId];
    const actualDamage = target.isDefending ? Math.floor(damage * 0.5) : damage;
    target.health = Math.max(0, target.health - actualDamage);
    
    // 添加受击特效
    state.effects.push({
      id: `hit-${Date.now()}`,
      type: 'HIT',
      position: { ...target.position },
      duration: 300,
      timer: 0,
      frame: 0,
    });
    
    // 检查游戏结束
    if (target.health === 0) {
      const winnerId = targetId === 'player1' ? 'player2' : 'player1';
      state.gameState.phase = 'RESULT';
      state.gameState.winner = winnerId;
      state.players[winnerId].score += 1;
    }
  })),
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => set(produce(state => {
    const player = state.players[playerId];
    player.animationTimer += deltaTime;
  })),
  
  setAnimationFrame: (playerId: string, frame: number) => set(produce(state => {
    state.players[playerId].animationFrame = frame;
  })),
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => {
    const id = `effect-${Date.now()import { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
  winner: null,
};

// 游戏Store接口
interface GameStore {
  // 状态
  gameState: GameState;
  players: Record<string, MechaData>;
  effects: Effect[];
  
  // 游戏流程控制
  startGame: () => void;
  pauseGame: () => void;
  resumeGame: () => void;
  endGame: (winner: string | null) => void;
  resetGame: () => void;
  nextRound: () => void;
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => void;
  updatePlayerState: (playerId: string, state: MechaData['state']) => void;
  updatePlayerFacing: (playerId: string, facingRight: boolean) => void;
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => void;
  updatePlayerDefending: (playerId: string, isDefending: boolean) => void;
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => void;
  dealDamage: (targetId: string, damage: number, attackerId?: string) => void;
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => void;
  setAnimationFrame: (playerId: string, frame: number) => void;
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => string;
  updateEffects: (deltaTime: number) => void;
  removeEffect: (effectId: string) => void;
  clearEffects: () => void;
  
  // 游戏时间
  tick: (deltaTime: number) => void;
}

// 创建Game Store
export const useGameStore = create<GameStore>((set, get) => ({
  // 初始状态
  gameState: initialGameState,
  players: {
    player1: createInitialMecha('player1', 'BLUE', 200),
    player2: createInitialMecha('player2', 'RED', 550),
  },
  effects: [],
  
  // 游戏流程控制
  startGame: () => set(produce(state => {
    state.gameState.phase = 'PLAYING';
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.winner = null;
  })),
  
  pauseGame: () => set(produce(state => {
    if (state.gameState.phase === 'PLAYING') {
      state.gameState.phase = 'PAUSED';
    }
  })),
  
  resumeGame: () => set(produce(state => {
    if (state.gameState.phase === 'PAUSED') {
      state.gameState.phase = 'PLAYING';
    }
  })),
  
  endGame: (winner: string | null) => set(produce(state => {
    state.gameState.phase = 'RESULT';
    state.gameState.winner = winner;
    if (winner && state.players[winner]) {
      state.players[winner].score += 1;
    }
  })),
  
  resetGame: () => set(produce(state => {
    state.gameState = initialGameState;
    state.players = {
      player1: createInitialMecha('player1', 'BLUE', 200),
      player2: createInitialMecha('player2', 'RED', 550),
    };
    state.effects = [];
  })),
  
  nextRound: () => set(produce(state => {
    state.gameState.round += 1;
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.phase = 'PLAYING';
    state.gameState.winner = null;
    
    // 重置玩家位置和状态
    Object.keys(state.players).forEach(playerId => {
      const player = state.players[playerId];
      player.health = player.maxHealth;
      player.position = playerId === 'player1' ? { x: 200, y: 400 } : { x: 550, y: 400 };
      player.velocity = { x: 0, y: 0 };
      player.state = 'IDLE';
      player.isDefending = false;
      player.isGrounded = false;
      player.attackCooldown = 0;
    });
    
    state.effects = [];
  })),
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => set(produce(state => {
    state.players[playerId].position = position;
    state.players[playerId].velocity = velocity;
  })),
  
  updatePlayerState: (playerId: string, mechaState: MechaData['state']) => set(produce(state => {
    state.players[playerId].state = mechaState;
  })),
  
  updatePlayerFacing: (playerId: string, facingRight: boolean) => set(produce(state => {
    state.players[playerId].facingRight = facingRight;
  })),
  
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => set(produce(state => {
    state.players[playerId].isGrounded = isGrounded;
  })),
  
  updatePlayerDefending: (playerId: string, isDefending: boolean) => set(produce(state => {
    state.players[playerId].isDefending = isDefending;
  })),
  
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => set(produce(state => {
    state.players[playerId].attackCooldown = cooldown;
  })),
  
  dealDamage: (targetId: string, damage: number) => set(produce(state => {
    const target = state.players[targetId];
    const actualDamage = target.isDefending ? Math.floor(damage * 0.5) : damage;
    target.health = Math.max(0, target.health - actualDamage);
    
    // 添加受击特效
    state.effects.push({
      id: `hit-${Date.now()}`,
      type: 'HIT',
      position: { ...target.position },
      duration: 300,
      timer: 0,
      frame: 0,
    });
    
    // 检查游戏结束
    if (target.health === 0) {
      const winnerId = targetId === 'player1' ? 'player2' : 'player1';
      state.gameState.phase = 'RESULT';
      state.gameState.winner = winnerId;
      state.players[winnerId].score += 1;
    }
  })),
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => set(produce(state => {
    const player = state.players[playerId];
    player.animationTimer += deltaTime;
  })),
  
  setAnimationFrame: (playerId: string, frame: number) => set(produce(state => {
    state.players[playerId].animationFrame = frame;
  })),
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => {
    const id = `effect-${Date.now()}-${Math.random().toString(import { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
  winner: null,
};

// 游戏Store接口
interface GameStore {
  // 状态
  gameState: GameState;
  players: Record<string, MechaData>;
  effects: Effect[];
  
  // 游戏流程控制
  startGame: () => void;
  pauseGame: () => void;
  resumeGame: () => void;
  endGame: (winner: string | null) => void;
  resetGame: () => void;
  nextRound: () => void;
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => void;
  updatePlayerState: (playerId: string, state: MechaData['state']) => void;
  updatePlayerFacing: (playerId: string, facingRight: boolean) => void;
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => void;
  updatePlayerDefending: (playerId: string, isDefending: boolean) => void;
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => void;
  dealDamage: (targetId: string, damage: number, attackerId?: string) => void;
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => void;
  setAnimationFrame: (playerId: string, frame: number) => void;
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => string;
  updateEffects: (deltaTime: number) => void;
  removeEffect: (effectId: string) => void;
  clearEffects: () => void;
  
  // 游戏时间
  tick: (deltaTime: number) => void;
}

// 创建Game Store
export const useGameStore = create<GameStore>((set, get) => ({
  // 初始状态
  gameState: initialGameState,
  players: {
    player1: createInitialMecha('player1', 'BLUE', 200),
    player2: createInitialMecha('player2', 'RED', 550),
  },
  effects: [],
  
  // 游戏流程控制
  startGame: () => set(produce(state => {
    state.gameState.phase = 'PLAYING';
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.winner = null;
  })),
  
  pauseGame: () => set(produce(state => {
    if (state.gameState.phase === 'PLAYING') {
      state.gameState.phase = 'PAUSED';
    }
  })),
  
  resumeGame: () => set(produce(state => {
    if (state.gameState.phase === 'PAUSED') {
      state.gameState.phase = 'PLAYING';
    }
  })),
  
  endGame: (winner: string | null) => set(produce(state => {
    state.gameState.phase = 'RESULT';
    state.gameState.winner = winner;
    if (winner && state.players[winner]) {
      state.players[winner].score += 1;
    }
  })),
  
  resetGame: () => set(produce(state => {
    state.gameState = initialGameState;
    state.players = {
      player1: createInitialMecha('player1', 'BLUE', 200),
      player2: createInitialMecha('player2', 'RED', 550),
    };
    state.effects = [];
  })),
  
  nextRound: () => set(produce(state => {
    state.gameState.round += 1;
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.phase = 'PLAYING';
    state.gameState.winner = null;
    
    // 重置玩家位置和状态
    Object.keys(state.players).forEach(playerId => {
      const player = state.players[playerId];
      player.health = player.maxHealth;
      player.position = playerId === 'player1' ? { x: 200, y: 400 } : { x: 550, y: 400 };
      player.velocity = { x: 0, y: 0 };
      player.state = 'IDLE';
      player.isDefending = false;
      player.isGrounded = false;
      player.attackCooldown = 0;
    });
    
    state.effects = [];
  })),
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => set(produce(state => {
    state.players[playerId].position = position;
    state.players[playerId].velocity = velocity;
  })),
  
  updatePlayerState: (playerId: string, mechaState: MechaData['state']) => set(produce(state => {
    state.players[playerId].state = mechaState;
  })),
  
  updatePlayerFacing: (playerId: string, facingRight: boolean) => set(produce(state => {
    state.players[playerId].facingRight = facingRight;
  })),
  
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => set(produce(state => {
    state.players[playerId].isGrounded = isGrounded;
  })),
  
  updatePlayerDefending: (playerId: string, isDefending: boolean) => set(produce(state => {
    state.players[playerId].isDefending = isDefending;
  })),
  
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => set(produce(state => {
    state.players[playerId].attackCooldown = cooldown;
  })),
  
  dealDamage: (targetId: string, damage: number) => set(produce(state => {
    const target = state.players[targetId];
    const actualDamage = target.isDefending ? Math.floor(damage * 0.5) : damage;
    target.health = Math.max(0, target.health - actualDamage);
    
    // 添加受击特效
    state.effects.push({
      id: `hit-${Date.now()}`,
      type: 'HIT',
      position: { ...target.position },
      duration: 300,
      timer: 0,
      frame: 0,
    });
    
    // 检查游戏结束
    if (target.health === 0) {
      const winnerId = targetId === 'player1' ? 'player2' : 'player1';
      state.gameState.phase = 'RESULT';
      state.gameState.winner = winnerId;
      state.players[winnerId].score += 1;
    }
  })),
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => set(produce(state => {
    const player = state.players[playerId];
    player.animationTimer += deltaTime;
  })),
  
  setAnimationFrame: (playerId: string, frame: number) => set(produce(state => {
    state.players[playerId].animationFrame = frame;
  })),
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => {
    const id = `effect-${Date.now()}-${Math.random().toString(36).substr(2, 9import { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
  winner: null,
};

// 游戏Store接口
interface GameStore {
  // 状态
  gameState: GameState;
  players: Record<string, MechaData>;
  effects: Effect[];
  
  // 游戏流程控制
  startGame: () => void;
  pauseGame: () => void;
  resumeGame: () => void;
  endGame: (winner: string | null) => void;
  resetGame: () => void;
  nextRound: () => void;
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => void;
  updatePlayerState: (playerId: string, state: MechaData['state']) => void;
  updatePlayerFacing: (playerId: string, facingRight: boolean) => void;
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => void;
  updatePlayerDefending: (playerId: string, isDefending: boolean) => void;
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => void;
  dealDamage: (targetId: string, damage: number, attackerId?: string) => void;
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => void;
  setAnimationFrame: (playerId: string, frame: number) => void;
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => string;
  updateEffects: (deltaTime: number) => void;
  removeEffect: (effectId: string) => void;
  clearEffects: () => void;
  
  // 游戏时间
  tick: (deltaTime: number) => void;
}

// 创建Game Store
export const useGameStore = create<GameStore>((set, get) => ({
  // 初始状态
  gameState: initialGameState,
  players: {
    player1: createInitialMecha('player1', 'BLUE', 200),
    player2: createInitialMecha('player2', 'RED', 550),
  },
  effects: [],
  
  // 游戏流程控制
  startGame: () => set(produce(state => {
    state.gameState.phase = 'PLAYING';
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.winner = null;
  })),
  
  pauseGame: () => set(produce(state => {
    if (state.gameState.phase === 'PLAYING') {
      state.gameState.phase = 'PAUSED';
    }
  })),
  
  resumeGame: () => set(produce(state => {
    if (state.gameState.phase === 'PAUSED') {
      state.gameState.phase = 'PLAYING';
    }
  })),
  
  endGame: (winner: string | null) => set(produce(state => {
    state.gameState.phase = 'RESULT';
    state.gameState.winner = winner;
    if (winner && state.players[winner]) {
      state.players[winner].score += 1;
    }
  })),
  
  resetGame: () => set(produce(state => {
    state.gameState = initialGameState;
    state.players = {
      player1: createInitialMecha('player1', 'BLUE', 200),
      player2: createInitialMecha('player2', 'RED', 550),
    };
    state.effects = [];
  })),
  
  nextRound: () => set(produce(state => {
    state.gameState.round += 1;
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.phase = 'PLAYING';
    state.gameState.winner = null;
    
    // 重置玩家位置和状态
    Object.keys(state.players).forEach(playerId => {
      const player = state.players[playerId];
      player.health = player.maxHealth;
      player.position = playerId === 'player1' ? { x: 200, y: 400 } : { x: 550, y: 400 };
      player.velocity = { x: 0, y: 0 };
      player.state = 'IDLE';
      player.isDefending = false;
      player.isGrounded = false;
      player.attackCooldown = 0;
    });
    
    state.effects = [];
  })),
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => set(produce(state => {
    state.players[playerId].position = position;
    state.players[playerId].velocity = velocity;
  })),
  
  updatePlayerState: (playerId: string, mechaState: MechaData['state']) => set(produce(state => {
    state.players[playerId].state = mechaState;
  })),
  
  updatePlayerFacing: (playerId: string, facingRight: boolean) => set(produce(state => {
    state.players[playerId].facingRight = facingRight;
  })),
  
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => set(produce(state => {
    state.players[playerId].isGrounded = isGrounded;
  })),
  
  updatePlayerDefending: (playerId: string, isDefending: boolean) => set(produce(state => {
    state.players[playerId].isDefending = isDefending;
  })),
  
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => set(produce(state => {
    state.players[playerId].attackCooldown = cooldown;
  })),
  
  dealDamage: (targetId: string, damage: number) => set(produce(state => {
    const target = state.players[targetId];
    const actualDamage = target.isDefending ? Math.floor(damage * 0.5) : damage;
    target.health = Math.max(0, target.health - actualDamage);
    
    // 添加受击特效
    state.effects.push({
      id: `hit-${Date.now()}`,
      type: 'HIT',
      position: { ...target.position },
      duration: 300,
      timer: 0,
      frame: 0,
    });
    
    // 检查游戏结束
    if (target.health === 0) {
      const winnerId = targetId === 'player1' ? 'player2' : 'player1';
      state.gameState.phase = 'RESULT';
      state.gameState.winner = winnerId;
      state.players[winnerId].score += 1;
    }
  })),
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => set(produce(state => {
    const player = state.players[playerId];
    player.animationTimer += deltaTime;
  })),
  
  setAnimationFrame: (playerId: string, frame: number) => set(produce(state => {
    state.players[playerId].animationFrame = frame;
  })),
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => {
    const id = `effect-${Date.now()}-${Math.random().toString(36).substr(2, 9)}`;
    set(proimport { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
  winner: null,
};

// 游戏Store接口
interface GameStore {
  // 状态
  gameState: GameState;
  players: Record<string, MechaData>;
  effects: Effect[];
  
  // 游戏流程控制
  startGame: () => void;
  pauseGame: () => void;
  resumeGame: () => void;
  endGame: (winner: string | null) => void;
  resetGame: () => void;
  nextRound: () => void;
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => void;
  updatePlayerState: (playerId: string, state: MechaData['state']) => void;
  updatePlayerFacing: (playerId: string, facingRight: boolean) => void;
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => void;
  updatePlayerDefending: (playerId: string, isDefending: boolean) => void;
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => void;
  dealDamage: (targetId: string, damage: number, attackerId?: string) => void;
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => void;
  setAnimationFrame: (playerId: string, frame: number) => void;
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => string;
  updateEffects: (deltaTime: number) => void;
  removeEffect: (effectId: string) => void;
  clearEffects: () => void;
  
  // 游戏时间
  tick: (deltaTime: number) => void;
}

// 创建Game Store
export const useGameStore = create<GameStore>((set, get) => ({
  // 初始状态
  gameState: initialGameState,
  players: {
    player1: createInitialMecha('player1', 'BLUE', 200),
    player2: createInitialMecha('player2', 'RED', 550),
  },
  effects: [],
  
  // 游戏流程控制
  startGame: () => set(produce(state => {
    state.gameState.phase = 'PLAYING';
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.winner = null;
  })),
  
  pauseGame: () => set(produce(state => {
    if (state.gameState.phase === 'PLAYING') {
      state.gameState.phase = 'PAUSED';
    }
  })),
  
  resumeGame: () => set(produce(state => {
    if (state.gameState.phase === 'PAUSED') {
      state.gameState.phase = 'PLAYING';
    }
  })),
  
  endGame: (winner: string | null) => set(produce(state => {
    state.gameState.phase = 'RESULT';
    state.gameState.winner = winner;
    if (winner && state.players[winner]) {
      state.players[winner].score += 1;
    }
  })),
  
  resetGame: () => set(produce(state => {
    state.gameState = initialGameState;
    state.players = {
      player1: createInitialMecha('player1', 'BLUE', 200),
      player2: createInitialMecha('player2', 'RED', 550),
    };
    state.effects = [];
  })),
  
  nextRound: () => set(produce(state => {
    state.gameState.round += 1;
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.phase = 'PLAYING';
    state.gameState.winner = null;
    
    // 重置玩家位置和状态
    Object.keys(state.players).forEach(playerId => {
      const player = state.players[playerId];
      player.health = player.maxHealth;
      player.position = playerId === 'player1' ? { x: 200, y: 400 } : { x: 550, y: 400 };
      player.velocity = { x: 0, y: 0 };
      player.state = 'IDLE';
      player.isDefending = false;
      player.isGrounded = false;
      player.attackCooldown = 0;
    });
    
    state.effects = [];
  })),
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => set(produce(state => {
    state.players[playerId].position = position;
    state.players[playerId].velocity = velocity;
  })),
  
  updatePlayerState: (playerId: string, mechaState: MechaData['state']) => set(produce(state => {
    state.players[playerId].state = mechaState;
  })),
  
  updatePlayerFacing: (playerId: string, facingRight: boolean) => set(produce(state => {
    state.players[playerId].facingRight = facingRight;
  })),
  
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => set(produce(state => {
    state.players[playerId].isGrounded = isGrounded;
  })),
  
  updatePlayerDefending: (playerId: string, isDefending: boolean) => set(produce(state => {
    state.players[playerId].isDefending = isDefending;
  })),
  
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => set(produce(state => {
    state.players[playerId].attackCooldown = cooldown;
  })),
  
  dealDamage: (targetId: string, damage: number) => set(produce(state => {
    const target = state.players[targetId];
    const actualDamage = target.isDefending ? Math.floor(damage * 0.5) : damage;
    target.health = Math.max(0, target.health - actualDamage);
    
    // 添加受击特效
    state.effects.push({
      id: `hit-${Date.now()}`,
      type: 'HIT',
      position: { ...target.position },
      duration: 300,
      timer: 0,
      frame: 0,
    });
    
    // 检查游戏结束
    if (target.health === 0) {
      const winnerId = targetId === 'player1' ? 'player2' : 'player1';
      state.gameState.phase = 'RESULT';
      state.gameState.winner = winnerId;
      state.players[winnerId].score += 1;
    }
  })),
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => set(produce(state => {
    const player = state.players[playerId];
    player.animationTimer += deltaTime;
  })),
  
  setAnimationFrame: (playerId: string, frame: number) => set(produce(state => {
    state.players[playerId].animationFrame = frame;
  })),
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => {
    const id = `effect-${Date.now()}-${Math.random().toString(36).substr(2, 9)}`;
    set(produce(state => {
      stateimport { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
  winner: null,
};

// 游戏Store接口
interface GameStore {
  // 状态
  gameState: GameState;
  players: Record<string, MechaData>;
  effects: Effect[];
  
  // 游戏流程控制
  startGame: () => void;
  pauseGame: () => void;
  resumeGame: () => void;
  endGame: (winner: string | null) => void;
  resetGame: () => void;
  nextRound: () => void;
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => void;
  updatePlayerState: (playerId: string, state: MechaData['state']) => void;
  updatePlayerFacing: (playerId: string, facingRight: boolean) => void;
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => void;
  updatePlayerDefending: (playerId: string, isDefending: boolean) => void;
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => void;
  dealDamage: (targetId: string, damage: number, attackerId?: string) => void;
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => void;
  setAnimationFrame: (playerId: string, frame: number) => void;
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => string;
  updateEffects: (deltaTime: number) => void;
  removeEffect: (effectId: string) => void;
  clearEffects: () => void;
  
  // 游戏时间
  tick: (deltaTime: number) => void;
}

// 创建Game Store
export const useGameStore = create<GameStore>((set, get) => ({
  // 初始状态
  gameState: initialGameState,
  players: {
    player1: createInitialMecha('player1', 'BLUE', 200),
    player2: createInitialMecha('player2', 'RED', 550),
  },
  effects: [],
  
  // 游戏流程控制
  startGame: () => set(produce(state => {
    state.gameState.phase = 'PLAYING';
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.winner = null;
  })),
  
  pauseGame: () => set(produce(state => {
    if (state.gameState.phase === 'PLAYING') {
      state.gameState.phase = 'PAUSED';
    }
  })),
  
  resumeGame: () => set(produce(state => {
    if (state.gameState.phase === 'PAUSED') {
      state.gameState.phase = 'PLAYING';
    }
  })),
  
  endGame: (winner: string | null) => set(produce(state => {
    state.gameState.phase = 'RESULT';
    state.gameState.winner = winner;
    if (winner && state.players[winner]) {
      state.players[winner].score += 1;
    }
  })),
  
  resetGame: () => set(produce(state => {
    state.gameState = initialGameState;
    state.players = {
      player1: createInitialMecha('player1', 'BLUE', 200),
      player2: createInitialMecha('player2', 'RED', 550),
    };
    state.effects = [];
  })),
  
  nextRound: () => set(produce(state => {
    state.gameState.round += 1;
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.phase = 'PLAYING';
    state.gameState.winner = null;
    
    // 重置玩家位置和状态
    Object.keys(state.players).forEach(playerId => {
      const player = state.players[playerId];
      player.health = player.maxHealth;
      player.position = playerId === 'player1' ? { x: 200, y: 400 } : { x: 550, y: 400 };
      player.velocity = { x: 0, y: 0 };
      player.state = 'IDLE';
      player.isDefending = false;
      player.isGrounded = false;
      player.attackCooldown = 0;
    });
    
    state.effects = [];
  })),
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => set(produce(state => {
    state.players[playerId].position = position;
    state.players[playerId].velocity = velocity;
  })),
  
  updatePlayerState: (playerId: string, mechaState: MechaData['state']) => set(produce(state => {
    state.players[playerId].state = mechaState;
  })),
  
  updatePlayerFacing: (playerId: string, facingRight: boolean) => set(produce(state => {
    state.players[playerId].facingRight = facingRight;
  })),
  
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => set(produce(state => {
    state.players[playerId].isGrounded = isGrounded;
  })),
  
  updatePlayerDefending: (playerId: string, isDefending: boolean) => set(produce(state => {
    state.players[playerId].isDefending = isDefending;
  })),
  
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => set(produce(state => {
    state.players[playerId].attackCooldown = cooldown;
  })),
  
  dealDamage: (targetId: string, damage: number) => set(produce(state => {
    const target = state.players[targetId];
    const actualDamage = target.isDefending ? Math.floor(damage * 0.5) : damage;
    target.health = Math.max(0, target.health - actualDamage);
    
    // 添加受击特效
    state.effects.push({
      id: `hit-${Date.now()}`,
      type: 'HIT',
      position: { ...target.position },
      duration: 300,
      timer: 0,
      frame: 0,
    });
    
    // 检查游戏结束
    if (target.health === 0) {
      const winnerId = targetId === 'player1' ? 'player2' : 'player1';
      state.gameState.phase = 'RESULT';
      state.gameState.winner = winnerId;
      state.players[winnerId].score += 1;
    }
  })),
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => set(produce(state => {
    const player = state.players[playerId];
    player.animationTimer += deltaTime;
  })),
  
  setAnimationFrame: (playerId: string, frame: number) => set(produce(state => {
    state.players[playerId].animationFrame = frame;
  })),
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => {
    const id = `effect-${Date.now()}-${Math.random().toString(36).substr(2, 9)}`;
    set(produce(state => {
      state.effects.push({
        ...import { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
  winner: null,
};

// 游戏Store接口
interface GameStore {
  // 状态
  gameState: GameState;
  players: Record<string, MechaData>;
  effects: Effect[];
  
  // 游戏流程控制
  startGame: () => void;
  pauseGame: () => void;
  resumeGame: () => void;
  endGame: (winner: string | null) => void;
  resetGame: () => void;
  nextRound: () => void;
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => void;
  updatePlayerState: (playerId: string, state: MechaData['state']) => void;
  updatePlayerFacing: (playerId: string, facingRight: boolean) => void;
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => void;
  updatePlayerDefending: (playerId: string, isDefending: boolean) => void;
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => void;
  dealDamage: (targetId: string, damage: number, attackerId?: string) => void;
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => void;
  setAnimationFrame: (playerId: string, frame: number) => void;
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => string;
  updateEffects: (deltaTime: number) => void;
  removeEffect: (effectId: string) => void;
  clearEffects: () => void;
  
  // 游戏时间
  tick: (deltaTime: number) => void;
}

// 创建Game Store
export const useGameStore = create<GameStore>((set, get) => ({
  // 初始状态
  gameState: initialGameState,
  players: {
    player1: createInitialMecha('player1', 'BLUE', 200),
    player2: createInitialMecha('player2', 'RED', 550),
  },
  effects: [],
  
  // 游戏流程控制
  startGame: () => set(produce(state => {
    state.gameState.phase = 'PLAYING';
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.winner = null;
  })),
  
  pauseGame: () => set(produce(state => {
    if (state.gameState.phase === 'PLAYING') {
      state.gameState.phase = 'PAUSED';
    }
  })),
  
  resumeGame: () => set(produce(state => {
    if (state.gameState.phase === 'PAUSED') {
      state.gameState.phase = 'PLAYING';
    }
  })),
  
  endGame: (winner: string | null) => set(produce(state => {
    state.gameState.phase = 'RESULT';
    state.gameState.winner = winner;
    if (winner && state.players[winner]) {
      state.players[winner].score += 1;
    }
  })),
  
  resetGame: () => set(produce(state => {
    state.gameState = initialGameState;
    state.players = {
      player1: createInitialMecha('player1', 'BLUE', 200),
      player2: createInitialMecha('player2', 'RED', 550),
    };
    state.effects = [];
  })),
  
  nextRound: () => set(produce(state => {
    state.gameState.round += 1;
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.phase = 'PLAYING';
    state.gameState.winner = null;
    
    // 重置玩家位置和状态
    Object.keys(state.players).forEach(playerId => {
      const player = state.players[playerId];
      player.health = player.maxHealth;
      player.position = playerId === 'player1' ? { x: 200, y: 400 } : { x: 550, y: 400 };
      player.velocity = { x: 0, y: 0 };
      player.state = 'IDLE';
      player.isDefending = false;
      player.isGrounded = false;
      player.attackCooldown = 0;
    });
    
    state.effects = [];
  })),
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => set(produce(state => {
    state.players[playerId].position = position;
    state.players[playerId].velocity = velocity;
  })),
  
  updatePlayerState: (playerId: string, mechaState: MechaData['state']) => set(produce(state => {
    state.players[playerId].state = mechaState;
  })),
  
  updatePlayerFacing: (playerId: string, facingRight: boolean) => set(produce(state => {
    state.players[playerId].facingRight = facingRight;
  })),
  
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => set(produce(state => {
    state.players[playerId].isGrounded = isGrounded;
  })),
  
  updatePlayerDefending: (playerId: string, isDefending: boolean) => set(produce(state => {
    state.players[playerId].isDefending = isDefending;
  })),
  
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => set(produce(state => {
    state.players[playerId].attackCooldown = cooldown;
  })),
  
  dealDamage: (targetId: string, damage: number) => set(produce(state => {
    const target = state.players[targetId];
    const actualDamage = target.isDefending ? Math.floor(damage * 0.5) : damage;
    target.health = Math.max(0, target.health - actualDamage);
    
    // 添加受击特效
    state.effects.push({
      id: `hit-${Date.now()}`,
      type: 'HIT',
      position: { ...target.position },
      duration: 300,
      timer: 0,
      frame: 0,
    });
    
    // 检查游戏结束
    if (target.health === 0) {
      const winnerId = targetId === 'player1' ? 'player2' : 'player1';
      state.gameState.phase = 'RESULT';
      state.gameState.winner = winnerId;
      state.players[winnerId].score += 1;
    }
  })),
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => set(produce(state => {
    const player = state.players[playerId];
    player.animationTimer += deltaTime;
  })),
  
  setAnimationFrame: (playerId: string, frame: number) => set(produce(state => {
    state.players[playerId].animationFrame = frame;
  })),
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => {
    const id = `effect-${Date.now()}-${Math.random().toString(36).substr(2, 9)}`;
    set(produce(state => {
      state.effects.push({
        ...effect,
        id,
import { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
  winner: null,
};

// 游戏Store接口
interface GameStore {
  // 状态
  gameState: GameState;
  players: Record<string, MechaData>;
  effects: Effect[];
  
  // 游戏流程控制
  startGame: () => void;
  pauseGame: () => void;
  resumeGame: () => void;
  endGame: (winner: string | null) => void;
  resetGame: () => void;
  nextRound: () => void;
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => void;
  updatePlayerState: (playerId: string, state: MechaData['state']) => void;
  updatePlayerFacing: (playerId: string, facingRight: boolean) => void;
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => void;
  updatePlayerDefending: (playerId: string, isDefending: boolean) => void;
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => void;
  dealDamage: (targetId: string, damage: number, attackerId?: string) => void;
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => void;
  setAnimationFrame: (playerId: string, frame: number) => void;
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => string;
  updateEffects: (deltaTime: number) => void;
  removeEffect: (effectId: string) => void;
  clearEffects: () => void;
  
  // 游戏时间
  tick: (deltaTime: number) => void;
}

// 创建Game Store
export const useGameStore = create<GameStore>((set, get) => ({
  // 初始状态
  gameState: initialGameState,
  players: {
    player1: createInitialMecha('player1', 'BLUE', 200),
    player2: createInitialMecha('player2', 'RED', 550),
  },
  effects: [],
  
  // 游戏流程控制
  startGame: () => set(produce(state => {
    state.gameState.phase = 'PLAYING';
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.winner = null;
  })),
  
  pauseGame: () => set(produce(state => {
    if (state.gameState.phase === 'PLAYING') {
      state.gameState.phase = 'PAUSED';
    }
  })),
  
  resumeGame: () => set(produce(state => {
    if (state.gameState.phase === 'PAUSED') {
      state.gameState.phase = 'PLAYING';
    }
  })),
  
  endGame: (winner: string | null) => set(produce(state => {
    state.gameState.phase = 'RESULT';
    state.gameState.winner = winner;
    if (winner && state.players[winner]) {
      state.players[winner].score += 1;
    }
  })),
  
  resetGame: () => set(produce(state => {
    state.gameState = initialGameState;
    state.players = {
      player1: createInitialMecha('player1', 'BLUE', 200),
      player2: createInitialMecha('player2', 'RED', 550),
    };
    state.effects = [];
  })),
  
  nextRound: () => set(produce(state => {
    state.gameState.round += 1;
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.phase = 'PLAYING';
    state.gameState.winner = null;
    
    // 重置玩家位置和状态
    Object.keys(state.players).forEach(playerId => {
      const player = state.players[playerId];
      player.health = player.maxHealth;
      player.position = playerId === 'player1' ? { x: 200, y: 400 } : { x: 550, y: 400 };
      player.velocity = { x: 0, y: 0 };
      player.state = 'IDLE';
      player.isDefending = false;
      player.isGrounded = false;
      player.attackCooldown = 0;
    });
    
    state.effects = [];
  })),
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => set(produce(state => {
    state.players[playerId].position = position;
    state.players[playerId].velocity = velocity;
  })),
  
  updatePlayerState: (playerId: string, mechaState: MechaData['state']) => set(produce(state => {
    state.players[playerId].state = mechaState;
  })),
  
  updatePlayerFacing: (playerId: string, facingRight: boolean) => set(produce(state => {
    state.players[playerId].facingRight = facingRight;
  })),
  
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => set(produce(state => {
    state.players[playerId].isGrounded = isGrounded;
  })),
  
  updatePlayerDefending: (playerId: string, isDefending: boolean) => set(produce(state => {
    state.players[playerId].isDefending = isDefending;
  })),
  
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => set(produce(state => {
    state.players[playerId].attackCooldown = cooldown;
  })),
  
  dealDamage: (targetId: string, damage: number) => set(produce(state => {
    const target = state.players[targetId];
    const actualDamage = target.isDefending ? Math.floor(damage * 0.5) : damage;
    target.health = Math.max(0, target.health - actualDamage);
    
    // 添加受击特效
    state.effects.push({
      id: `hit-${Date.now()}`,
      type: 'HIT',
      position: { ...target.position },
      duration: 300,
      timer: 0,
      frame: 0,
    });
    
    // 检查游戏结束
    if (target.health === 0) {
      const winnerId = targetId === 'player1' ? 'player2' : 'player1';
      state.gameState.phase = 'RESULT';
      state.gameState.winner = winnerId;
      state.players[winnerId].score += 1;
    }
  })),
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => set(produce(state => {
    const player = state.players[playerId];
    player.animationTimer += deltaTime;
  })),
  
  setAnimationFrame: (playerId: string, frame: number) => set(produce(state => {
    state.players[playerId].animationFrame = frame;
  })),
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => {
    const id = `effect-${Date.now()}-${Math.random().toString(36).substr(2, 9)}`;
    set(produce(state => {
      state.effects.push({
        ...effect,
        id,
        timer: 0,
import { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
  winner: null,
};

// 游戏Store接口
interface GameStore {
  // 状态
  gameState: GameState;
  players: Record<string, MechaData>;
  effects: Effect[];
  
  // 游戏流程控制
  startGame: () => void;
  pauseGame: () => void;
  resumeGame: () => void;
  endGame: (winner: string | null) => void;
  resetGame: () => void;
  nextRound: () => void;
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => void;
  updatePlayerState: (playerId: string, state: MechaData['state']) => void;
  updatePlayerFacing: (playerId: string, facingRight: boolean) => void;
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => void;
  updatePlayerDefending: (playerId: string, isDefending: boolean) => void;
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => void;
  dealDamage: (targetId: string, damage: number, attackerId?: string) => void;
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => void;
  setAnimationFrame: (playerId: string, frame: number) => void;
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => string;
  updateEffects: (deltaTime: number) => void;
  removeEffect: (effectId: string) => void;
  clearEffects: () => void;
  
  // 游戏时间
  tick: (deltaTime: number) => void;
}

// 创建Game Store
export const useGameStore = create<GameStore>((set, get) => ({
  // 初始状态
  gameState: initialGameState,
  players: {
    player1: createInitialMecha('player1', 'BLUE', 200),
    player2: createInitialMecha('player2', 'RED', 550),
  },
  effects: [],
  
  // 游戏流程控制
  startGame: () => set(produce(state => {
    state.gameState.phase = 'PLAYING';
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.winner = null;
  })),
  
  pauseGame: () => set(produce(state => {
    if (state.gameState.phase === 'PLAYING') {
      state.gameState.phase = 'PAUSED';
    }
  })),
  
  resumeGame: () => set(produce(state => {
    if (state.gameState.phase === 'PAUSED') {
      state.gameState.phase = 'PLAYING';
    }
  })),
  
  endGame: (winner: string | null) => set(produce(state => {
    state.gameState.phase = 'RESULT';
    state.gameState.winner = winner;
    if (winner && state.players[winner]) {
      state.players[winner].score += 1;
    }
  })),
  
  resetGame: () => set(produce(state => {
    state.gameState = initialGameState;
    state.players = {
      player1: createInitialMecha('player1', 'BLUE', 200),
      player2: createInitialMecha('player2', 'RED', 550),
    };
    state.effects = [];
  })),
  
  nextRound: () => set(produce(state => {
    state.gameState.round += 1;
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.phase = 'PLAYING';
    state.gameState.winner = null;
    
    // 重置玩家位置和状态
    Object.keys(state.players).forEach(playerId => {
      const player = state.players[playerId];
      player.health = player.maxHealth;
      player.position = playerId === 'player1' ? { x: 200, y: 400 } : { x: 550, y: 400 };
      player.velocity = { x: 0, y: 0 };
      player.state = 'IDLE';
      player.isDefending = false;
      player.isGrounded = false;
      player.attackCooldown = 0;
    });
    
    state.effects = [];
  })),
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => set(produce(state => {
    state.players[playerId].position = position;
    state.players[playerId].velocity = velocity;
  })),
  
  updatePlayerState: (playerId: string, mechaState: MechaData['state']) => set(produce(state => {
    state.players[playerId].state = mechaState;
  })),
  
  updatePlayerFacing: (playerId: string, facingRight: boolean) => set(produce(state => {
    state.players[playerId].facingRight = facingRight;
  })),
  
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => set(produce(state => {
    state.players[playerId].isGrounded = isGrounded;
  })),
  
  updatePlayerDefending: (playerId: string, isDefending: boolean) => set(produce(state => {
    state.players[playerId].isDefending = isDefending;
  })),
  
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => set(produce(state => {
    state.players[playerId].attackCooldown = cooldown;
  })),
  
  dealDamage: (targetId: string, damage: number) => set(produce(state => {
    const target = state.players[targetId];
    const actualDamage = target.isDefending ? Math.floor(damage * 0.5) : damage;
    target.health = Math.max(0, target.health - actualDamage);
    
    // 添加受击特效
    state.effects.push({
      id: `hit-${Date.now()}`,
      type: 'HIT',
      position: { ...target.position },
      duration: 300,
      timer: 0,
      frame: 0,
    });
    
    // 检查游戏结束
    if (target.health === 0) {
      const winnerId = targetId === 'player1' ? 'player2' : 'player1';
      state.gameState.phase = 'RESULT';
      state.gameState.winner = winnerId;
      state.players[winnerId].score += 1;
    }
  })),
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => set(produce(state => {
    const player = state.players[playerId];
    player.animationTimer += deltaTime;
  })),
  
  setAnimationFrame: (playerId: string, frame: number) => set(produce(state => {
    state.players[playerId].animationFrame = frame;
  })),
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => {
    const id = `effect-${Date.now()}-${Math.random().toString(36).substr(2, 9)}`;
    set(produce(state => {
      state.effects.push({
        ...effect,
        id,
        timer: 0,
      });
    }));
import { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
  winner: null,
};

// 游戏Store接口
interface GameStore {
  // 状态
  gameState: GameState;
  players: Record<string, MechaData>;
  effects: Effect[];
  
  // 游戏流程控制
  startGame: () => void;
  pauseGame: () => void;
  resumeGame: () => void;
  endGame: (winner: string | null) => void;
  resetGame: () => void;
  nextRound: () => void;
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => void;
  updatePlayerState: (playerId: string, state: MechaData['state']) => void;
  updatePlayerFacing: (playerId: string, facingRight: boolean) => void;
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => void;
  updatePlayerDefending: (playerId: string, isDefending: boolean) => void;
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => void;
  dealDamage: (targetId: string, damage: number, attackerId?: string) => void;
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => void;
  setAnimationFrame: (playerId: string, frame: number) => void;
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => string;
  updateEffects: (deltaTime: number) => void;
  removeEffect: (effectId: string) => void;
  clearEffects: () => void;
  
  // 游戏时间
  tick: (deltaTime: number) => void;
}

// 创建Game Store
export const useGameStore = create<GameStore>((set, get) => ({
  // 初始状态
  gameState: initialGameState,
  players: {
    player1: createInitialMecha('player1', 'BLUE', 200),
    player2: createInitialMecha('player2', 'RED', 550),
  },
  effects: [],
  
  // 游戏流程控制
  startGame: () => set(produce(state => {
    state.gameState.phase = 'PLAYING';
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.winner = null;
  })),
  
  pauseGame: () => set(produce(state => {
    if (state.gameState.phase === 'PLAYING') {
      state.gameState.phase = 'PAUSED';
    }
  })),
  
  resumeGame: () => set(produce(state => {
    if (state.gameState.phase === 'PAUSED') {
      state.gameState.phase = 'PLAYING';
    }
  })),
  
  endGame: (winner: string | null) => set(produce(state => {
    state.gameState.phase = 'RESULT';
    state.gameState.winner = winner;
    if (winner && state.players[winner]) {
      state.players[winner].score += 1;
    }
  })),
  
  resetGame: () => set(produce(state => {
    state.gameState = initialGameState;
    state.players = {
      player1: createInitialMecha('player1', 'BLUE', 200),
      player2: createInitialMecha('player2', 'RED', 550),
    };
    state.effects = [];
  })),
  
  nextRound: () => set(produce(state => {
    state.gameState.round += 1;
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.phase = 'PLAYING';
    state.gameState.winner = null;
    
    // 重置玩家位置和状态
    Object.keys(state.players).forEach(playerId => {
      const player = state.players[playerId];
      player.health = player.maxHealth;
      player.position = playerId === 'player1' ? { x: 200, y: 400 } : { x: 550, y: 400 };
      player.velocity = { x: 0, y: 0 };
      player.state = 'IDLE';
      player.isDefending = false;
      player.isGrounded = false;
      player.attackCooldown = 0;
    });
    
    state.effects = [];
  })),
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => set(produce(state => {
    state.players[playerId].position = position;
    state.players[playerId].velocity = velocity;
  })),
  
  updatePlayerState: (playerId: string, mechaState: MechaData['state']) => set(produce(state => {
    state.players[playerId].state = mechaState;
  })),
  
  updatePlayerFacing: (playerId: string, facingRight: boolean) => set(produce(state => {
    state.players[playerId].facingRight = facingRight;
  })),
  
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => set(produce(state => {
    state.players[playerId].isGrounded = isGrounded;
  })),
  
  updatePlayerDefending: (playerId: string, isDefending: boolean) => set(produce(state => {
    state.players[playerId].isDefending = isDefending;
  })),
  
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => set(produce(state => {
    state.players[playerId].attackCooldown = cooldown;
  })),
  
  dealDamage: (targetId: string, damage: number) => set(produce(state => {
    const target = state.players[targetId];
    const actualDamage = target.isDefending ? Math.floor(damage * 0.5) : damage;
    target.health = Math.max(0, target.health - actualDamage);
    
    // 添加受击特效
    state.effects.push({
      id: `hit-${Date.now()}`,
      type: 'HIT',
      position: { ...target.position },
      duration: 300,
      timer: 0,
      frame: 0,
    });
    
    // 检查游戏结束
    if (target.health === 0) {
      const winnerId = targetId === 'player1' ? 'player2' : 'player1';
      state.gameState.phase = 'RESULT';
      state.gameState.winner = winnerId;
      state.players[winnerId].score += 1;
    }
  })),
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => set(produce(state => {
    const player = state.players[playerId];
    player.animationTimer += deltaTime;
  })),
  
  setAnimationFrame: (playerId: string, frame: number) => set(produce(state => {
    state.players[playerId].animationFrame = frame;
  })),
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => {
    const id = `effect-${Date.now()}-${Math.random().toString(36).substr(2, 9)}`;
    set(produce(state => {
      state.effects.push({
        ...effect,
        id,
        timer: 0,
      });
    }));
    return id;
  },
import { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
  winner: null,
};

// 游戏Store接口
interface GameStore {
  // 状态
  gameState: GameState;
  players: Record<string, MechaData>;
  effects: Effect[];
  
  // 游戏流程控制
  startGame: () => void;
  pauseGame: () => void;
  resumeGame: () => void;
  endGame: (winner: string | null) => void;
  resetGame: () => void;
  nextRound: () => void;
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => void;
  updatePlayerState: (playerId: string, state: MechaData['state']) => void;
  updatePlayerFacing: (playerId: string, facingRight: boolean) => void;
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => void;
  updatePlayerDefending: (playerId: string, isDefending: boolean) => void;
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => void;
  dealDamage: (targetId: string, damage: number, attackerId?: string) => void;
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => void;
  setAnimationFrame: (playerId: string, frame: number) => void;
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => string;
  updateEffects: (deltaTime: number) => void;
  removeEffect: (effectId: string) => void;
  clearEffects: () => void;
  
  // 游戏时间
  tick: (deltaTime: number) => void;
}

// 创建Game Store
export const useGameStore = create<GameStore>((set, get) => ({
  // 初始状态
  gameState: initialGameState,
  players: {
    player1: createInitialMecha('player1', 'BLUE', 200),
    player2: createInitialMecha('player2', 'RED', 550),
  },
  effects: [],
  
  // 游戏流程控制
  startGame: () => set(produce(state => {
    state.gameState.phase = 'PLAYING';
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.winner = null;
  })),
  
  pauseGame: () => set(produce(state => {
    if (state.gameState.phase === 'PLAYING') {
      state.gameState.phase = 'PAUSED';
    }
  })),
  
  resumeGame: () => set(produce(state => {
    if (state.gameState.phase === 'PAUSED') {
      state.gameState.phase = 'PLAYING';
    }
  })),
  
  endGame: (winner: string | null) => set(produce(state => {
    state.gameState.phase = 'RESULT';
    state.gameState.winner = winner;
    if (winner && state.players[winner]) {
      state.players[winner].score += 1;
    }
  })),
  
  resetGame: () => set(produce(state => {
    state.gameState = initialGameState;
    state.players = {
      player1: createInitialMecha('player1', 'BLUE', 200),
      player2: createInitialMecha('player2', 'RED', 550),
    };
    state.effects = [];
  })),
  
  nextRound: () => set(produce(state => {
    state.gameState.round += 1;
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.phase = 'PLAYING';
    state.gameState.winner = null;
    
    // 重置玩家位置和状态
    Object.keys(state.players).forEach(playerId => {
      const player = state.players[playerId];
      player.health = player.maxHealth;
      player.position = playerId === 'player1' ? { x: 200, y: 400 } : { x: 550, y: 400 };
      player.velocity = { x: 0, y: 0 };
      player.state = 'IDLE';
      player.isDefending = false;
      player.isGrounded = false;
      player.attackCooldown = 0;
    });
    
    state.effects = [];
  })),
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => set(produce(state => {
    state.players[playerId].position = position;
    state.players[playerId].velocity = velocity;
  })),
  
  updatePlayerState: (playerId: string, mechaState: MechaData['state']) => set(produce(state => {
    state.players[playerId].state = mechaState;
  })),
  
  updatePlayerFacing: (playerId: string, facingRight: boolean) => set(produce(state => {
    state.players[playerId].facingRight = facingRight;
  })),
  
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => set(produce(state => {
    state.players[playerId].isGrounded = isGrounded;
  })),
  
  updatePlayerDefending: (playerId: string, isDefending: boolean) => set(produce(state => {
    state.players[playerId].isDefending = isDefending;
  })),
  
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => set(produce(state => {
    state.players[playerId].attackCooldown = cooldown;
  })),
  
  dealDamage: (targetId: string, damage: number) => set(produce(state => {
    const target = state.players[targetId];
    const actualDamage = target.isDefending ? Math.floor(damage * 0.5) : damage;
    target.health = Math.max(0, target.health - actualDamage);
    
    // 添加受击特效
    state.effects.push({
      id: `hit-${Date.now()}`,
      type: 'HIT',
      position: { ...target.position },
      duration: 300,
      timer: 0,
      frame: 0,
    });
    
    // 检查游戏结束
    if (target.health === 0) {
      const winnerId = targetId === 'player1' ? 'player2' : 'player1';
      state.gameState.phase = 'RESULT';
      state.gameState.winner = winnerId;
      state.players[winnerId].score += 1;
    }
  })),
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => set(produce(state => {
    const player = state.players[playerId];
    player.animationTimer += deltaTime;
  })),
  
  setAnimationFrame: (playerId: string, frame: number) => set(produce(state => {
    state.players[playerId].animationFrame = frame;
  })),
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => {
    const id = `effect-${Date.now()}-${Math.random().toString(36).substr(2, 9)}`;
    set(produce(state => {
      state.effects.push({
        ...effect,
        id,
        timer: 0,
      });
    }));
    return id;
  },
  
  updateEffects:import { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
  winner: null,
};

// 游戏Store接口
interface GameStore {
  // 状态
  gameState: GameState;
  players: Record<string, MechaData>;
  effects: Effect[];
  
  // 游戏流程控制
  startGame: () => void;
  pauseGame: () => void;
  resumeGame: () => void;
  endGame: (winner: string | null) => void;
  resetGame: () => void;
  nextRound: () => void;
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => void;
  updatePlayerState: (playerId: string, state: MechaData['state']) => void;
  updatePlayerFacing: (playerId: string, facingRight: boolean) => void;
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => void;
  updatePlayerDefending: (playerId: string, isDefending: boolean) => void;
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => void;
  dealDamage: (targetId: string, damage: number, attackerId?: string) => void;
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => void;
  setAnimationFrame: (playerId: string, frame: number) => void;
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => string;
  updateEffects: (deltaTime: number) => void;
  removeEffect: (effectId: string) => void;
  clearEffects: () => void;
  
  // 游戏时间
  tick: (deltaTime: number) => void;
}

// 创建Game Store
export const useGameStore = create<GameStore>((set, get) => ({
  // 初始状态
  gameState: initialGameState,
  players: {
    player1: createInitialMecha('player1', 'BLUE', 200),
    player2: createInitialMecha('player2', 'RED', 550),
  },
  effects: [],
  
  // 游戏流程控制
  startGame: () => set(produce(state => {
    state.gameState.phase = 'PLAYING';
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.winner = null;
  })),
  
  pauseGame: () => set(produce(state => {
    if (state.gameState.phase === 'PLAYING') {
      state.gameState.phase = 'PAUSED';
    }
  })),
  
  resumeGame: () => set(produce(state => {
    if (state.gameState.phase === 'PAUSED') {
      state.gameState.phase = 'PLAYING';
    }
  })),
  
  endGame: (winner: string | null) => set(produce(state => {
    state.gameState.phase = 'RESULT';
    state.gameState.winner = winner;
    if (winner && state.players[winner]) {
      state.players[winner].score += 1;
    }
  })),
  
  resetGame: () => set(produce(state => {
    state.gameState = initialGameState;
    state.players = {
      player1: createInitialMecha('player1', 'BLUE', 200),
      player2: createInitialMecha('player2', 'RED', 550),
    };
    state.effects = [];
  })),
  
  nextRound: () => set(produce(state => {
    state.gameState.round += 1;
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.phase = 'PLAYING';
    state.gameState.winner = null;
    
    // 重置玩家位置和状态
    Object.keys(state.players).forEach(playerId => {
      const player = state.players[playerId];
      player.health = player.maxHealth;
      player.position = playerId === 'player1' ? { x: 200, y: 400 } : { x: 550, y: 400 };
      player.velocity = { x: 0, y: 0 };
      player.state = 'IDLE';
      player.isDefending = false;
      player.isGrounded = false;
      player.attackCooldown = 0;
    });
    
    state.effects = [];
  })),
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => set(produce(state => {
    state.players[playerId].position = position;
    state.players[playerId].velocity = velocity;
  })),
  
  updatePlayerState: (playerId: string, mechaState: MechaData['state']) => set(produce(state => {
    state.players[playerId].state = mechaState;
  })),
  
  updatePlayerFacing: (playerId: string, facingRight: boolean) => set(produce(state => {
    state.players[playerId].facingRight = facingRight;
  })),
  
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => set(produce(state => {
    state.players[playerId].isGrounded = isGrounded;
  })),
  
  updatePlayerDefending: (playerId: string, isDefending: boolean) => set(produce(state => {
    state.players[playerId].isDefending = isDefending;
  })),
  
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => set(produce(state => {
    state.players[playerId].attackCooldown = cooldown;
  })),
  
  dealDamage: (targetId: string, damage: number) => set(produce(state => {
    const target = state.players[targetId];
    const actualDamage = target.isDefending ? Math.floor(damage * 0.5) : damage;
    target.health = Math.max(0, target.health - actualDamage);
    
    // 添加受击特效
    state.effects.push({
      id: `hit-${Date.now()}`,
      type: 'HIT',
      position: { ...target.position },
      duration: 300,
      timer: 0,
      frame: 0,
    });
    
    // 检查游戏结束
    if (target.health === 0) {
      const winnerId = targetId === 'player1' ? 'player2' : 'player1';
      state.gameState.phase = 'RESULT';
      state.gameState.winner = winnerId;
      state.players[winnerId].score += 1;
    }
  })),
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => set(produce(state => {
    const player = state.players[playerId];
    player.animationTimer += deltaTime;
  })),
  
  setAnimationFrame: (playerId: string, frame: number) => set(produce(state => {
    state.players[playerId].animationFrame = frame;
  })),
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => {
    const id = `effect-${Date.now()}-${Math.random().toString(36).substr(2, 9)}`;
    set(produce(state => {
      state.effects.push({
        ...effect,
        id,
        timer: 0,
      });
    }));
    return id;
  },
  
  updateEffects: (deltaTime: number) =>import { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
  winner: null,
};

// 游戏Store接口
interface GameStore {
  // 状态
  gameState: GameState;
  players: Record<string, MechaData>;
  effects: Effect[];
  
  // 游戏流程控制
  startGame: () => void;
  pauseGame: () => void;
  resumeGame: () => void;
  endGame: (winner: string | null) => void;
  resetGame: () => void;
  nextRound: () => void;
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => void;
  updatePlayerState: (playerId: string, state: MechaData['state']) => void;
  updatePlayerFacing: (playerId: string, facingRight: boolean) => void;
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => void;
  updatePlayerDefending: (playerId: string, isDefending: boolean) => void;
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => void;
  dealDamage: (targetId: string, damage: number, attackerId?: string) => void;
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => void;
  setAnimationFrame: (playerId: string, frame: number) => void;
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => string;
  updateEffects: (deltaTime: number) => void;
  removeEffect: (effectId: string) => void;
  clearEffects: () => void;
  
  // 游戏时间
  tick: (deltaTime: number) => void;
}

// 创建Game Store
export const useGameStore = create<GameStore>((set, get) => ({
  // 初始状态
  gameState: initialGameState,
  players: {
    player1: createInitialMecha('player1', 'BLUE', 200),
    player2: createInitialMecha('player2', 'RED', 550),
  },
  effects: [],
  
  // 游戏流程控制
  startGame: () => set(produce(state => {
    state.gameState.phase = 'PLAYING';
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.winner = null;
  })),
  
  pauseGame: () => set(produce(state => {
    if (state.gameState.phase === 'PLAYING') {
      state.gameState.phase = 'PAUSED';
    }
  })),
  
  resumeGame: () => set(produce(state => {
    if (state.gameState.phase === 'PAUSED') {
      state.gameState.phase = 'PLAYING';
    }
  })),
  
  endGame: (winner: string | null) => set(produce(state => {
    state.gameState.phase = 'RESULT';
    state.gameState.winner = winner;
    if (winner && state.players[winner]) {
      state.players[winner].score += 1;
    }
  })),
  
  resetGame: () => set(produce(state => {
    state.gameState = initialGameState;
    state.players = {
      player1: createInitialMecha('player1', 'BLUE', 200),
      player2: createInitialMecha('player2', 'RED', 550),
    };
    state.effects = [];
  })),
  
  nextRound: () => set(produce(state => {
    state.gameState.round += 1;
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.phase = 'PLAYING';
    state.gameState.winner = null;
    
    // 重置玩家位置和状态
    Object.keys(state.players).forEach(playerId => {
      const player = state.players[playerId];
      player.health = player.maxHealth;
      player.position = playerId === 'player1' ? { x: 200, y: 400 } : { x: 550, y: 400 };
      player.velocity = { x: 0, y: 0 };
      player.state = 'IDLE';
      player.isDefending = false;
      player.isGrounded = false;
      player.attackCooldown = 0;
    });
    
    state.effects = [];
  })),
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => set(produce(state => {
    state.players[playerId].position = position;
    state.players[playerId].velocity = velocity;
  })),
  
  updatePlayerState: (playerId: string, mechaState: MechaData['state']) => set(produce(state => {
    state.players[playerId].state = mechaState;
  })),
  
  updatePlayerFacing: (playerId: string, facingRight: boolean) => set(produce(state => {
    state.players[playerId].facingRight = facingRight;
  })),
  
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => set(produce(state => {
    state.players[playerId].isGrounded = isGrounded;
  })),
  
  updatePlayerDefending: (playerId: string, isDefending: boolean) => set(produce(state => {
    state.players[playerId].isDefending = isDefending;
  })),
  
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => set(produce(state => {
    state.players[playerId].attackCooldown = cooldown;
  })),
  
  dealDamage: (targetId: string, damage: number) => set(produce(state => {
    const target = state.players[targetId];
    const actualDamage = target.isDefending ? Math.floor(damage * 0.5) : damage;
    target.health = Math.max(0, target.health - actualDamage);
    
    // 添加受击特效
    state.effects.push({
      id: `hit-${Date.now()}`,
      type: 'HIT',
      position: { ...target.position },
      duration: 300,
      timer: 0,
      frame: 0,
    });
    
    // 检查游戏结束
    if (target.health === 0) {
      const winnerId = targetId === 'player1' ? 'player2' : 'player1';
      state.gameState.phase = 'RESULT';
      state.gameState.winner = winnerId;
      state.players[winnerId].score += 1;
    }
  })),
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => set(produce(state => {
    const player = state.players[playerId];
    player.animationTimer += deltaTime;
  })),
  
  setAnimationFrame: (playerId: string, frame: number) => set(produce(state => {
    state.players[playerId].animationFrame = frame;
  })),
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => {
    const id = `effect-${Date.now()}-${Math.random().toString(36).substr(2, 9)}`;
    set(produce(state => {
      state.effects.push({
        ...effect,
        id,
        timer: 0,
      });
    }));
    return id;
  },
  
  updateEffects: (deltaTime: number) => set(produce(state => {
import { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
  winner: null,
};

// 游戏Store接口
interface GameStore {
  // 状态
  gameState: GameState;
  players: Record<string, MechaData>;
  effects: Effect[];
  
  // 游戏流程控制
  startGame: () => void;
  pauseGame: () => void;
  resumeGame: () => void;
  endGame: (winner: string | null) => void;
  resetGame: () => void;
  nextRound: () => void;
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => void;
  updatePlayerState: (playerId: string, state: MechaData['state']) => void;
  updatePlayerFacing: (playerId: string, facingRight: boolean) => void;
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => void;
  updatePlayerDefending: (playerId: string, isDefending: boolean) => void;
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => void;
  dealDamage: (targetId: string, damage: number, attackerId?: string) => void;
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => void;
  setAnimationFrame: (playerId: string, frame: number) => void;
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => string;
  updateEffects: (deltaTime: number) => void;
  removeEffect: (effectId: string) => void;
  clearEffects: () => void;
  
  // 游戏时间
  tick: (deltaTime: number) => void;
}

// 创建Game Store
export const useGameStore = create<GameStore>((set, get) => ({
  // 初始状态
  gameState: initialGameState,
  players: {
    player1: createInitialMecha('player1', 'BLUE', 200),
    player2: createInitialMecha('player2', 'RED', 550),
  },
  effects: [],
  
  // 游戏流程控制
  startGame: () => set(produce(state => {
    state.gameState.phase = 'PLAYING';
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.winner = null;
  })),
  
  pauseGame: () => set(produce(state => {
    if (state.gameState.phase === 'PLAYING') {
      state.gameState.phase = 'PAUSED';
    }
  })),
  
  resumeGame: () => set(produce(state => {
    if (state.gameState.phase === 'PAUSED') {
      state.gameState.phase = 'PLAYING';
    }
  })),
  
  endGame: (winner: string | null) => set(produce(state => {
    state.gameState.phase = 'RESULT';
    state.gameState.winner = winner;
    if (winner && state.players[winner]) {
      state.players[winner].score += 1;
    }
  })),
  
  resetGame: () => set(produce(state => {
    state.gameState = initialGameState;
    state.players = {
      player1: createInitialMecha('player1', 'BLUE', 200),
      player2: createInitialMecha('player2', 'RED', 550),
    };
    state.effects = [];
  })),
  
  nextRound: () => set(produce(state => {
    state.gameState.round += 1;
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.phase = 'PLAYING';
    state.gameState.winner = null;
    
    // 重置玩家位置和状态
    Object.keys(state.players).forEach(playerId => {
      const player = state.players[playerId];
      player.health = player.maxHealth;
      player.position = playerId === 'player1' ? { x: 200, y: 400 } : { x: 550, y: 400 };
      player.velocity = { x: 0, y: 0 };
      player.state = 'IDLE';
      player.isDefending = false;
      player.isGrounded = false;
      player.attackCooldown = 0;
    });
    
    state.effects = [];
  })),
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => set(produce(state => {
    state.players[playerId].position = position;
    state.players[playerId].velocity = velocity;
  })),
  
  updatePlayerState: (playerId: string, mechaState: MechaData['state']) => set(produce(state => {
    state.players[playerId].state = mechaState;
  })),
  
  updatePlayerFacing: (playerId: string, facingRight: boolean) => set(produce(state => {
    state.players[playerId].facingRight = facingRight;
  })),
  
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => set(produce(state => {
    state.players[playerId].isGrounded = isGrounded;
  })),
  
  updatePlayerDefending: (playerId: string, isDefending: boolean) => set(produce(state => {
    state.players[playerId].isDefending = isDefending;
  })),
  
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => set(produce(state => {
    state.players[playerId].attackCooldown = cooldown;
  })),
  
  dealDamage: (targetId: string, damage: number) => set(produce(state => {
    const target = state.players[targetId];
    const actualDamage = target.isDefending ? Math.floor(damage * 0.5) : damage;
    target.health = Math.max(0, target.health - actualDamage);
    
    // 添加受击特效
    state.effects.push({
      id: `hit-${Date.now()}`,
      type: 'HIT',
      position: { ...target.position },
      duration: 300,
      timer: 0,
      frame: 0,
    });
    
    // 检查游戏结束
    if (target.health === 0) {
      const winnerId = targetId === 'player1' ? 'player2' : 'player1';
      state.gameState.phase = 'RESULT';
      state.gameState.winner = winnerId;
      state.players[winnerId].score += 1;
    }
  })),
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => set(produce(state => {
    const player = state.players[playerId];
    player.animationTimer += deltaTime;
  })),
  
  setAnimationFrame: (playerId: string, frame: number) => set(produce(state => {
    state.players[playerId].animationFrame = frame;
  })),
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => {
    const id = `effect-${Date.now()}-${Math.random().toString(36).substr(2, 9)}`;
    set(produce(state => {
      state.effects.push({
        ...effect,
        id,
        timer: 0,
      });
    }));
    return id;
  },
  
  updateEffects: (deltaTime: number) => set(produce(state => {
    state.effects = state.effects.filterimport { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
  winner: null,
};

// 游戏Store接口
interface GameStore {
  // 状态
  gameState: GameState;
  players: Record<string, MechaData>;
  effects: Effect[];
  
  // 游戏流程控制
  startGame: () => void;
  pauseGame: () => void;
  resumeGame: () => void;
  endGame: (winner: string | null) => void;
  resetGame: () => void;
  nextRound: () => void;
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => void;
  updatePlayerState: (playerId: string, state: MechaData['state']) => void;
  updatePlayerFacing: (playerId: string, facingRight: boolean) => void;
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => void;
  updatePlayerDefending: (playerId: string, isDefending: boolean) => void;
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => void;
  dealDamage: (targetId: string, damage: number, attackerId?: string) => void;
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => void;
  setAnimationFrame: (playerId: string, frame: number) => void;
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => string;
  updateEffects: (deltaTime: number) => void;
  removeEffect: (effectId: string) => void;
  clearEffects: () => void;
  
  // 游戏时间
  tick: (deltaTime: number) => void;
}

// 创建Game Store
export const useGameStore = create<GameStore>((set, get) => ({
  // 初始状态
  gameState: initialGameState,
  players: {
    player1: createInitialMecha('player1', 'BLUE', 200),
    player2: createInitialMecha('player2', 'RED', 550),
  },
  effects: [],
  
  // 游戏流程控制
  startGame: () => set(produce(state => {
    state.gameState.phase = 'PLAYING';
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.winner = null;
  })),
  
  pauseGame: () => set(produce(state => {
    if (state.gameState.phase === 'PLAYING') {
      state.gameState.phase = 'PAUSED';
    }
  })),
  
  resumeGame: () => set(produce(state => {
    if (state.gameState.phase === 'PAUSED') {
      state.gameState.phase = 'PLAYING';
    }
  })),
  
  endGame: (winner: string | null) => set(produce(state => {
    state.gameState.phase = 'RESULT';
    state.gameState.winner = winner;
    if (winner && state.players[winner]) {
      state.players[winner].score += 1;
    }
  })),
  
  resetGame: () => set(produce(state => {
    state.gameState = initialGameState;
    state.players = {
      player1: createInitialMecha('player1', 'BLUE', 200),
      player2: createInitialMecha('player2', 'RED', 550),
    };
    state.effects = [];
  })),
  
  nextRound: () => set(produce(state => {
    state.gameState.round += 1;
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.phase = 'PLAYING';
    state.gameState.winner = null;
    
    // 重置玩家位置和状态
    Object.keys(state.players).forEach(playerId => {
      const player = state.players[playerId];
      player.health = player.maxHealth;
      player.position = playerId === 'player1' ? { x: 200, y: 400 } : { x: 550, y: 400 };
      player.velocity = { x: 0, y: 0 };
      player.state = 'IDLE';
      player.isDefending = false;
      player.isGrounded = false;
      player.attackCooldown = 0;
    });
    
    state.effects = [];
  })),
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => set(produce(state => {
    state.players[playerId].position = position;
    state.players[playerId].velocity = velocity;
  })),
  
  updatePlayerState: (playerId: string, mechaState: MechaData['state']) => set(produce(state => {
    state.players[playerId].state = mechaState;
  })),
  
  updatePlayerFacing: (playerId: string, facingRight: boolean) => set(produce(state => {
    state.players[playerId].facingRight = facingRight;
  })),
  
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => set(produce(state => {
    state.players[playerId].isGrounded = isGrounded;
  })),
  
  updatePlayerDefending: (playerId: string, isDefending: boolean) => set(produce(state => {
    state.players[playerId].isDefending = isDefending;
  })),
  
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => set(produce(state => {
    state.players[playerId].attackCooldown = cooldown;
  })),
  
  dealDamage: (targetId: string, damage: number) => set(produce(state => {
    const target = state.players[targetId];
    const actualDamage = target.isDefending ? Math.floor(damage * 0.5) : damage;
    target.health = Math.max(0, target.health - actualDamage);
    
    // 添加受击特效
    state.effects.push({
      id: `hit-${Date.now()}`,
      type: 'HIT',
      position: { ...target.position },
      duration: 300,
      timer: 0,
      frame: 0,
    });
    
    // 检查游戏结束
    if (target.health === 0) {
      const winnerId = targetId === 'player1' ? 'player2' : 'player1';
      state.gameState.phase = 'RESULT';
      state.gameState.winner = winnerId;
      state.players[winnerId].score += 1;
    }
  })),
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => set(produce(state => {
    const player = state.players[playerId];
    player.animationTimer += deltaTime;
  })),
  
  setAnimationFrame: (playerId: string, frame: number) => set(produce(state => {
    state.players[playerId].animationFrame = frame;
  })),
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => {
    const id = `effect-${Date.now()}-${Math.random().toString(36).substr(2, 9)}`;
    set(produce(state => {
      state.effects.push({
        ...effect,
        id,
        timer: 0,
      });
    }));
    return id;
  },
  
  updateEffects: (deltaTime: number) => set(produce(state => {
    state.effects = state.effects.filter(effect => {
      effect.timerimport { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
  winner: null,
};

// 游戏Store接口
interface GameStore {
  // 状态
  gameState: GameState;
  players: Record<string, MechaData>;
  effects: Effect[];
  
  // 游戏流程控制
  startGame: () => void;
  pauseGame: () => void;
  resumeGame: () => void;
  endGame: (winner: string | null) => void;
  resetGame: () => void;
  nextRound: () => void;
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => void;
  updatePlayerState: (playerId: string, state: MechaData['state']) => void;
  updatePlayerFacing: (playerId: string, facingRight: boolean) => void;
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => void;
  updatePlayerDefending: (playerId: string, isDefending: boolean) => void;
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => void;
  dealDamage: (targetId: string, damage: number, attackerId?: string) => void;
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => void;
  setAnimationFrame: (playerId: string, frame: number) => void;
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => string;
  updateEffects: (deltaTime: number) => void;
  removeEffect: (effectId: string) => void;
  clearEffects: () => void;
  
  // 游戏时间
  tick: (deltaTime: number) => void;
}

// 创建Game Store
export const useGameStore = create<GameStore>((set, get) => ({
  // 初始状态
  gameState: initialGameState,
  players: {
    player1: createInitialMecha('player1', 'BLUE', 200),
    player2: createInitialMecha('player2', 'RED', 550),
  },
  effects: [],
  
  // 游戏流程控制
  startGame: () => set(produce(state => {
    state.gameState.phase = 'PLAYING';
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.winner = null;
  })),
  
  pauseGame: () => set(produce(state => {
    if (state.gameState.phase === 'PLAYING') {
      state.gameState.phase = 'PAUSED';
    }
  })),
  
  resumeGame: () => set(produce(state => {
    if (state.gameState.phase === 'PAUSED') {
      state.gameState.phase = 'PLAYING';
    }
  })),
  
  endGame: (winner: string | null) => set(produce(state => {
    state.gameState.phase = 'RESULT';
    state.gameState.winner = winner;
    if (winner && state.players[winner]) {
      state.players[winner].score += 1;
    }
  })),
  
  resetGame: () => set(produce(state => {
    state.gameState = initialGameState;
    state.players = {
      player1: createInitialMecha('player1', 'BLUE', 200),
      player2: createInitialMecha('player2', 'RED', 550),
    };
    state.effects = [];
  })),
  
  nextRound: () => set(produce(state => {
    state.gameState.round += 1;
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.phase = 'PLAYING';
    state.gameState.winner = null;
    
    // 重置玩家位置和状态
    Object.keys(state.players).forEach(playerId => {
      const player = state.players[playerId];
      player.health = player.maxHealth;
      player.position = playerId === 'player1' ? { x: 200, y: 400 } : { x: 550, y: 400 };
      player.velocity = { x: 0, y: 0 };
      player.state = 'IDLE';
      player.isDefending = false;
      player.isGrounded = false;
      player.attackCooldown = 0;
    });
    
    state.effects = [];
  })),
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => set(produce(state => {
    state.players[playerId].position = position;
    state.players[playerId].velocity = velocity;
  })),
  
  updatePlayerState: (playerId: string, mechaState: MechaData['state']) => set(produce(state => {
    state.players[playerId].state = mechaState;
  })),
  
  updatePlayerFacing: (playerId: string, facingRight: boolean) => set(produce(state => {
    state.players[playerId].facingRight = facingRight;
  })),
  
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => set(produce(state => {
    state.players[playerId].isGrounded = isGrounded;
  })),
  
  updatePlayerDefending: (playerId: string, isDefending: boolean) => set(produce(state => {
    state.players[playerId].isDefending = isDefending;
  })),
  
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => set(produce(state => {
    state.players[playerId].attackCooldown = cooldown;
  })),
  
  dealDamage: (targetId: string, damage: number) => set(produce(state => {
    const target = state.players[targetId];
    const actualDamage = target.isDefending ? Math.floor(damage * 0.5) : damage;
    target.health = Math.max(0, target.health - actualDamage);
    
    // 添加受击特效
    state.effects.push({
      id: `hit-${Date.now()}`,
      type: 'HIT',
      position: { ...target.position },
      duration: 300,
      timer: 0,
      frame: 0,
    });
    
    // 检查游戏结束
    if (target.health === 0) {
      const winnerId = targetId === 'player1' ? 'player2' : 'player1';
      state.gameState.phase = 'RESULT';
      state.gameState.winner = winnerId;
      state.players[winnerId].score += 1;
    }
  })),
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => set(produce(state => {
    const player = state.players[playerId];
    player.animationTimer += deltaTime;
  })),
  
  setAnimationFrame: (playerId: string, frame: number) => set(produce(state => {
    state.players[playerId].animationFrame = frame;
  })),
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => {
    const id = `effect-${Date.now()}-${Math.random().toString(36).substr(2, 9)}`;
    set(produce(state => {
      state.effects.push({
        ...effect,
        id,
        timer: 0,
      });
    }));
    return id;
  },
  
  updateEffects: (deltaTime: number) => set(produce(state => {
    state.effects = state.effects.filter(effect => {
      effect.timer += deltaTime;
      return effectimport { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
  winner: null,
};

// 游戏Store接口
interface GameStore {
  // 状态
  gameState: GameState;
  players: Record<string, MechaData>;
  effects: Effect[];
  
  // 游戏流程控制
  startGame: () => void;
  pauseGame: () => void;
  resumeGame: () => void;
  endGame: (winner: string | null) => void;
  resetGame: () => void;
  nextRound: () => void;
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => void;
  updatePlayerState: (playerId: string, state: MechaData['state']) => void;
  updatePlayerFacing: (playerId: string, facingRight: boolean) => void;
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => void;
  updatePlayerDefending: (playerId: string, isDefending: boolean) => void;
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => void;
  dealDamage: (targetId: string, damage: number, attackerId?: string) => void;
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => void;
  setAnimationFrame: (playerId: string, frame: number) => void;
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => string;
  updateEffects: (deltaTime: number) => void;
  removeEffect: (effectId: string) => void;
  clearEffects: () => void;
  
  // 游戏时间
  tick: (deltaTime: number) => void;
}

// 创建Game Store
export const useGameStore = create<GameStore>((set, get) => ({
  // 初始状态
  gameState: initialGameState,
  players: {
    player1: createInitialMecha('player1', 'BLUE', 200),
    player2: createInitialMecha('player2', 'RED', 550),
  },
  effects: [],
  
  // 游戏流程控制
  startGame: () => set(produce(state => {
    state.gameState.phase = 'PLAYING';
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.winner = null;
  })),
  
  pauseGame: () => set(produce(state => {
    if (state.gameState.phase === 'PLAYING') {
      state.gameState.phase = 'PAUSED';
    }
  })),
  
  resumeGame: () => set(produce(state => {
    if (state.gameState.phase === 'PAUSED') {
      state.gameState.phase = 'PLAYING';
    }
  })),
  
  endGame: (winner: string | null) => set(produce(state => {
    state.gameState.phase = 'RESULT';
    state.gameState.winner = winner;
    if (winner && state.players[winner]) {
      state.players[winner].score += 1;
    }
  })),
  
  resetGame: () => set(produce(state => {
    state.gameState = initialGameState;
    state.players = {
      player1: createInitialMecha('player1', 'BLUE', 200),
      player2: createInitialMecha('player2', 'RED', 550),
    };
    state.effects = [];
  })),
  
  nextRound: () => set(produce(state => {
    state.gameState.round += 1;
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.phase = 'PLAYING';
    state.gameState.winner = null;
    
    // 重置玩家位置和状态
    Object.keys(state.players).forEach(playerId => {
      const player = state.players[playerId];
      player.health = player.maxHealth;
      player.position = playerId === 'player1' ? { x: 200, y: 400 } : { x: 550, y: 400 };
      player.velocity = { x: 0, y: 0 };
      player.state = 'IDLE';
      player.isDefending = false;
      player.isGrounded = false;
      player.attackCooldown = 0;
    });
    
    state.effects = [];
  })),
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => set(produce(state => {
    state.players[playerId].position = position;
    state.players[playerId].velocity = velocity;
  })),
  
  updatePlayerState: (playerId: string, mechaState: MechaData['state']) => set(produce(state => {
    state.players[playerId].state = mechaState;
  })),
  
  updatePlayerFacing: (playerId: string, facingRight: boolean) => set(produce(state => {
    state.players[playerId].facingRight = facingRight;
  })),
  
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => set(produce(state => {
    state.players[playerId].isGrounded = isGrounded;
  })),
  
  updatePlayerDefending: (playerId: string, isDefending: boolean) => set(produce(state => {
    state.players[playerId].isDefending = isDefending;
  })),
  
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => set(produce(state => {
    state.players[playerId].attackCooldown = cooldown;
  })),
  
  dealDamage: (targetId: string, damage: number) => set(produce(state => {
    const target = state.players[targetId];
    const actualDamage = target.isDefending ? Math.floor(damage * 0.5) : damage;
    target.health = Math.max(0, target.health - actualDamage);
    
    // 添加受击特效
    state.effects.push({
      id: `hit-${Date.now()}`,
      type: 'HIT',
      position: { ...target.position },
      duration: 300,
      timer: 0,
      frame: 0,
    });
    
    // 检查游戏结束
    if (target.health === 0) {
      const winnerId = targetId === 'player1' ? 'player2' : 'player1';
      state.gameState.phase = 'RESULT';
      state.gameState.winner = winnerId;
      state.players[winnerId].score += 1;
    }
  })),
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => set(produce(state => {
    const player = state.players[playerId];
    player.animationTimer += deltaTime;
  })),
  
  setAnimationFrame: (playerId: string, frame: number) => set(produce(state => {
    state.players[playerId].animationFrame = frame;
  })),
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => {
    const id = `effect-${Date.now()}-${Math.random().toString(36).substr(2, 9)}`;
    set(produce(state => {
      state.effects.push({
        ...effect,
        id,
        timer: 0,
      });
    }));
    return id;
  },
  
  updateEffects: (deltaTime: number) => set(produce(state => {
    state.effects = state.effects.filter(effect => {
      effect.timer += deltaTime;
      return effect.timer < effect.duration;
    }import { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
  winner: null,
};

// 游戏Store接口
interface GameStore {
  // 状态
  gameState: GameState;
  players: Record<string, MechaData>;
  effects: Effect[];
  
  // 游戏流程控制
  startGame: () => void;
  pauseGame: () => void;
  resumeGame: () => void;
  endGame: (winner: string | null) => void;
  resetGame: () => void;
  nextRound: () => void;
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => void;
  updatePlayerState: (playerId: string, state: MechaData['state']) => void;
  updatePlayerFacing: (playerId: string, facingRight: boolean) => void;
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => void;
  updatePlayerDefending: (playerId: string, isDefending: boolean) => void;
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => void;
  dealDamage: (targetId: string, damage: number, attackerId?: string) => void;
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => void;
  setAnimationFrame: (playerId: string, frame: number) => void;
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => string;
  updateEffects: (deltaTime: number) => void;
  removeEffect: (effectId: string) => void;
  clearEffects: () => void;
  
  // 游戏时间
  tick: (deltaTime: number) => void;
}

// 创建Game Store
export const useGameStore = create<GameStore>((set, get) => ({
  // 初始状态
  gameState: initialGameState,
  players: {
    player1: createInitialMecha('player1', 'BLUE', 200),
    player2: createInitialMecha('player2', 'RED', 550),
  },
  effects: [],
  
  // 游戏流程控制
  startGame: () => set(produce(state => {
    state.gameState.phase = 'PLAYING';
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.winner = null;
  })),
  
  pauseGame: () => set(produce(state => {
    if (state.gameState.phase === 'PLAYING') {
      state.gameState.phase = 'PAUSED';
    }
  })),
  
  resumeGame: () => set(produce(state => {
    if (state.gameState.phase === 'PAUSED') {
      state.gameState.phase = 'PLAYING';
    }
  })),
  
  endGame: (winner: string | null) => set(produce(state => {
    state.gameState.phase = 'RESULT';
    state.gameState.winner = winner;
    if (winner && state.players[winner]) {
      state.players[winner].score += 1;
    }
  })),
  
  resetGame: () => set(produce(state => {
    state.gameState = initialGameState;
    state.players = {
      player1: createInitialMecha('player1', 'BLUE', 200),
      player2: createInitialMecha('player2', 'RED', 550),
    };
    state.effects = [];
  })),
  
  nextRound: () => set(produce(state => {
    state.gameState.round += 1;
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.phase = 'PLAYING';
    state.gameState.winner = null;
    
    // 重置玩家位置和状态
    Object.keys(state.players).forEach(playerId => {
      const player = state.players[playerId];
      player.health = player.maxHealth;
      player.position = playerId === 'player1' ? { x: 200, y: 400 } : { x: 550, y: 400 };
      player.velocity = { x: 0, y: 0 };
      player.state = 'IDLE';
      player.isDefending = false;
      player.isGrounded = false;
      player.attackCooldown = 0;
    });
    
    state.effects = [];
  })),
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => set(produce(state => {
    state.players[playerId].position = position;
    state.players[playerId].velocity = velocity;
  })),
  
  updatePlayerState: (playerId: string, mechaState: MechaData['state']) => set(produce(state => {
    state.players[playerId].state = mechaState;
  })),
  
  updatePlayerFacing: (playerId: string, facingRight: boolean) => set(produce(state => {
    state.players[playerId].facingRight = facingRight;
  })),
  
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => set(produce(state => {
    state.players[playerId].isGrounded = isGrounded;
  })),
  
  updatePlayerDefending: (playerId: string, isDefending: boolean) => set(produce(state => {
    state.players[playerId].isDefending = isDefending;
  })),
  
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => set(produce(state => {
    state.players[playerId].attackCooldown = cooldown;
  })),
  
  dealDamage: (targetId: string, damage: number) => set(produce(state => {
    const target = state.players[targetId];
    const actualDamage = target.isDefending ? Math.floor(damage * 0.5) : damage;
    target.health = Math.max(0, target.health - actualDamage);
    
    // 添加受击特效
    state.effects.push({
      id: `hit-${Date.now()}`,
      type: 'HIT',
      position: { ...target.position },
      duration: 300,
      timer: 0,
      frame: 0,
    });
    
    // 检查游戏结束
    if (target.health === 0) {
      const winnerId = targetId === 'player1' ? 'player2' : 'player1';
      state.gameState.phase = 'RESULT';
      state.gameState.winner = winnerId;
      state.players[winnerId].score += 1;
    }
  })),
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => set(produce(state => {
    const player = state.players[playerId];
    player.animationTimer += deltaTime;
  })),
  
  setAnimationFrame: (playerId: string, frame: number) => set(produce(state => {
    state.players[playerId].animationFrame = frame;
  })),
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => {
    const id = `effect-${Date.now()}-${Math.random().toString(36).substr(2, 9)}`;
    set(produce(state => {
      state.effects.push({
        ...effect,
        id,
        timer: 0,
      });
    }));
    return id;
  },
  
  updateEffects: (deltaTime: number) => set(produce(state => {
    state.effects = state.effects.filter(effect => {
      effect.timer += deltaTime;
      return effect.timer < effect.duration;
    });
  })),
  
import { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
  winner: null,
};

// 游戏Store接口
interface GameStore {
  // 状态
  gameState: GameState;
  players: Record<string, MechaData>;
  effects: Effect[];
  
  // 游戏流程控制
  startGame: () => void;
  pauseGame: () => void;
  resumeGame: () => void;
  endGame: (winner: string | null) => void;
  resetGame: () => void;
  nextRound: () => void;
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => void;
  updatePlayerState: (playerId: string, state: MechaData['state']) => void;
  updatePlayerFacing: (playerId: string, facingRight: boolean) => void;
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => void;
  updatePlayerDefending: (playerId: string, isDefending: boolean) => void;
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => void;
  dealDamage: (targetId: string, damage: number, attackerId?: string) => void;
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => void;
  setAnimationFrame: (playerId: string, frame: number) => void;
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => string;
  updateEffects: (deltaTime: number) => void;
  removeEffect: (effectId: string) => void;
  clearEffects: () => void;
  
  // 游戏时间
  tick: (deltaTime: number) => void;
}

// 创建Game Store
export const useGameStore = create<GameStore>((set, get) => ({
  // 初始状态
  gameState: initialGameState,
  players: {
    player1: createInitialMecha('player1', 'BLUE', 200),
    player2: createInitialMecha('player2', 'RED', 550),
  },
  effects: [],
  
  // 游戏流程控制
  startGame: () => set(produce(state => {
    state.gameState.phase = 'PLAYING';
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.winner = null;
  })),
  
  pauseGame: () => set(produce(state => {
    if (state.gameState.phase === 'PLAYING') {
      state.gameState.phase = 'PAUSED';
    }
  })),
  
  resumeGame: () => set(produce(state => {
    if (state.gameState.phase === 'PAUSED') {
      state.gameState.phase = 'PLAYING';
    }
  })),
  
  endGame: (winner: string | null) => set(produce(state => {
    state.gameState.phase = 'RESULT';
    state.gameState.winner = winner;
    if (winner && state.players[winner]) {
      state.players[winner].score += 1;
    }
  })),
  
  resetGame: () => set(produce(state => {
    state.gameState = initialGameState;
    state.players = {
      player1: createInitialMecha('player1', 'BLUE', 200),
      player2: createInitialMecha('player2', 'RED', 550),
    };
    state.effects = [];
  })),
  
  nextRound: () => set(produce(state => {
    state.gameState.round += 1;
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.phase = 'PLAYING';
    state.gameState.winner = null;
    
    // 重置玩家位置和状态
    Object.keys(state.players).forEach(playerId => {
      const player = state.players[playerId];
      player.health = player.maxHealth;
      player.position = playerId === 'player1' ? { x: 200, y: 400 } : { x: 550, y: 400 };
      player.velocity = { x: 0, y: 0 };
      player.state = 'IDLE';
      player.isDefending = false;
      player.isGrounded = false;
      player.attackCooldown = 0;
    });
    
    state.effects = [];
  })),
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => set(produce(state => {
    state.players[playerId].position = position;
    state.players[playerId].velocity = velocity;
  })),
  
  updatePlayerState: (playerId: string, mechaState: MechaData['state']) => set(produce(state => {
    state.players[playerId].state = mechaState;
  })),
  
  updatePlayerFacing: (playerId: string, facingRight: boolean) => set(produce(state => {
    state.players[playerId].facingRight = facingRight;
  })),
  
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => set(produce(state => {
    state.players[playerId].isGrounded = isGrounded;
  })),
  
  updatePlayerDefending: (playerId: string, isDefending: boolean) => set(produce(state => {
    state.players[playerId].isDefending = isDefending;
  })),
  
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => set(produce(state => {
    state.players[playerId].attackCooldown = cooldown;
  })),
  
  dealDamage: (targetId: string, damage: number) => set(produce(state => {
    const target = state.players[targetId];
    const actualDamage = target.isDefending ? Math.floor(damage * 0.5) : damage;
    target.health = Math.max(0, target.health - actualDamage);
    
    // 添加受击特效
    state.effects.push({
      id: `hit-${Date.now()}`,
      type: 'HIT',
      position: { ...target.position },
      duration: 300,
      timer: 0,
      frame: 0,
    });
    
    // 检查游戏结束
    if (target.health === 0) {
      const winnerId = targetId === 'player1' ? 'player2' : 'player1';
      state.gameState.phase = 'RESULT';
      state.gameState.winner = winnerId;
      state.players[winnerId].score += 1;
    }
  })),
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => set(produce(state => {
    const player = state.players[playerId];
    player.animationTimer += deltaTime;
  })),
  
  setAnimationFrame: (playerId: string, frame: number) => set(produce(state => {
    state.players[playerId].animationFrame = frame;
  })),
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => {
    const id = `effect-${Date.now()}-${Math.random().toString(36).substr(2, 9)}`;
    set(produce(state => {
      state.effects.push({
        ...effect,
        id,
        timer: 0,
      });
    }));
    return id;
  },
  
  updateEffects: (deltaTime: number) => set(produce(state => {
    state.effects = state.effects.filter(effect => {
      effect.timer += deltaTime;
      return effect.timer < effect.duration;
    });
  })),
  
  removeEffect: (effectIdimport { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
  winner: null,
};

// 游戏Store接口
interface GameStore {
  // 状态
  gameState: GameState;
  players: Record<string, MechaData>;
  effects: Effect[];
  
  // 游戏流程控制
  startGame: () => void;
  pauseGame: () => void;
  resumeGame: () => void;
  endGame: (winner: string | null) => void;
  resetGame: () => void;
  nextRound: () => void;
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => void;
  updatePlayerState: (playerId: string, state: MechaData['state']) => void;
  updatePlayerFacing: (playerId: string, facingRight: boolean) => void;
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => void;
  updatePlayerDefending: (playerId: string, isDefending: boolean) => void;
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => void;
  dealDamage: (targetId: string, damage: number, attackerId?: string) => void;
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => void;
  setAnimationFrame: (playerId: string, frame: number) => void;
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => string;
  updateEffects: (deltaTime: number) => void;
  removeEffect: (effectId: string) => void;
  clearEffects: () => void;
  
  // 游戏时间
  tick: (deltaTime: number) => void;
}

// 创建Game Store
export const useGameStore = create<GameStore>((set, get) => ({
  // 初始状态
  gameState: initialGameState,
  players: {
    player1: createInitialMecha('player1', 'BLUE', 200),
    player2: createInitialMecha('player2', 'RED', 550),
  },
  effects: [],
  
  // 游戏流程控制
  startGame: () => set(produce(state => {
    state.gameState.phase = 'PLAYING';
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.winner = null;
  })),
  
  pauseGame: () => set(produce(state => {
    if (state.gameState.phase === 'PLAYING') {
      state.gameState.phase = 'PAUSED';
    }
  })),
  
  resumeGame: () => set(produce(state => {
    if (state.gameState.phase === 'PAUSED') {
      state.gameState.phase = 'PLAYING';
    }
  })),
  
  endGame: (winner: string | null) => set(produce(state => {
    state.gameState.phase = 'RESULT';
    state.gameState.winner = winner;
    if (winner && state.players[winner]) {
      state.players[winner].score += 1;
    }
  })),
  
  resetGame: () => set(produce(state => {
    state.gameState = initialGameState;
    state.players = {
      player1: createInitialMecha('player1', 'BLUE', 200),
      player2: createInitialMecha('player2', 'RED', 550),
    };
    state.effects = [];
  })),
  
  nextRound: () => set(produce(state => {
    state.gameState.round += 1;
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.phase = 'PLAYING';
    state.gameState.winner = null;
    
    // 重置玩家位置和状态
    Object.keys(state.players).forEach(playerId => {
      const player = state.players[playerId];
      player.health = player.maxHealth;
      player.position = playerId === 'player1' ? { x: 200, y: 400 } : { x: 550, y: 400 };
      player.velocity = { x: 0, y: 0 };
      player.state = 'IDLE';
      player.isDefending = false;
      player.isGrounded = false;
      player.attackCooldown = 0;
    });
    
    state.effects = [];
  })),
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => set(produce(state => {
    state.players[playerId].position = position;
    state.players[playerId].velocity = velocity;
  })),
  
  updatePlayerState: (playerId: string, mechaState: MechaData['state']) => set(produce(state => {
    state.players[playerId].state = mechaState;
  })),
  
  updatePlayerFacing: (playerId: string, facingRight: boolean) => set(produce(state => {
    state.players[playerId].facingRight = facingRight;
  })),
  
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => set(produce(state => {
    state.players[playerId].isGrounded = isGrounded;
  })),
  
  updatePlayerDefending: (playerId: string, isDefending: boolean) => set(produce(state => {
    state.players[playerId].isDefending = isDefending;
  })),
  
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => set(produce(state => {
    state.players[playerId].attackCooldown = cooldown;
  })),
  
  dealDamage: (targetId: string, damage: number) => set(produce(state => {
    const target = state.players[targetId];
    const actualDamage = target.isDefending ? Math.floor(damage * 0.5) : damage;
    target.health = Math.max(0, target.health - actualDamage);
    
    // 添加受击特效
    state.effects.push({
      id: `hit-${Date.now()}`,
      type: 'HIT',
      position: { ...target.position },
      duration: 300,
      timer: 0,
      frame: 0,
    });
    
    // 检查游戏结束
    if (target.health === 0) {
      const winnerId = targetId === 'player1' ? 'player2' : 'player1';
      state.gameState.phase = 'RESULT';
      state.gameState.winner = winnerId;
      state.players[winnerId].score += 1;
    }
  })),
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => set(produce(state => {
    const player = state.players[playerId];
    player.animationTimer += deltaTime;
  })),
  
  setAnimationFrame: (playerId: string, frame: number) => set(produce(state => {
    state.players[playerId].animationFrame = frame;
  })),
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => {
    const id = `effect-${Date.now()}-${Math.random().toString(36).substr(2, 9)}`;
    set(produce(state => {
      state.effects.push({
        ...effect,
        id,
        timer: 0,
      });
    }));
    return id;
  },
  
  updateEffects: (deltaTime: number) => set(produce(state => {
    state.effects = state.effects.filter(effect => {
      effect.timer += deltaTime;
      return effect.timer < effect.duration;
    });
  })),
  
  removeEffect: (effectId: string) => set(produce(stateimport { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
  winner: null,
};

// 游戏Store接口
interface GameStore {
  // 状态
  gameState: GameState;
  players: Record<string, MechaData>;
  effects: Effect[];
  
  // 游戏流程控制
  startGame: () => void;
  pauseGame: () => void;
  resumeGame: () => void;
  endGame: (winner: string | null) => void;
  resetGame: () => void;
  nextRound: () => void;
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => void;
  updatePlayerState: (playerId: string, state: MechaData['state']) => void;
  updatePlayerFacing: (playerId: string, facingRight: boolean) => void;
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => void;
  updatePlayerDefending: (playerId: string, isDefending: boolean) => void;
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => void;
  dealDamage: (targetId: string, damage: number, attackerId?: string) => void;
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => void;
  setAnimationFrame: (playerId: string, frame: number) => void;
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => string;
  updateEffects: (deltaTime: number) => void;
  removeEffect: (effectId: string) => void;
  clearEffects: () => void;
  
  // 游戏时间
  tick: (deltaTime: number) => void;
}

// 创建Game Store
export const useGameStore = create<GameStore>((set, get) => ({
  // 初始状态
  gameState: initialGameState,
  players: {
    player1: createInitialMecha('player1', 'BLUE', 200),
    player2: createInitialMecha('player2', 'RED', 550),
  },
  effects: [],
  
  // 游戏流程控制
  startGame: () => set(produce(state => {
    state.gameState.phase = 'PLAYING';
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.winner = null;
  })),
  
  pauseGame: () => set(produce(state => {
    if (state.gameState.phase === 'PLAYING') {
      state.gameState.phase = 'PAUSED';
    }
  })),
  
  resumeGame: () => set(produce(state => {
    if (state.gameState.phase === 'PAUSED') {
      state.gameState.phase = 'PLAYING';
    }
  })),
  
  endGame: (winner: string | null) => set(produce(state => {
    state.gameState.phase = 'RESULT';
    state.gameState.winner = winner;
    if (winner && state.players[winner]) {
      state.players[winner].score += 1;
    }
  })),
  
  resetGame: () => set(produce(state => {
    state.gameState = initialGameState;
    state.players = {
      player1: createInitialMecha('player1', 'BLUE', 200),
      player2: createInitialMecha('player2', 'RED', 550),
    };
    state.effects = [];
  })),
  
  nextRound: () => set(produce(state => {
    state.gameState.round += 1;
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.phase = 'PLAYING';
    state.gameState.winner = null;
    
    // 重置玩家位置和状态
    Object.keys(state.players).forEach(playerId => {
      const player = state.players[playerId];
      player.health = player.maxHealth;
      player.position = playerId === 'player1' ? { x: 200, y: 400 } : { x: 550, y: 400 };
      player.velocity = { x: 0, y: 0 };
      player.state = 'IDLE';
      player.isDefending = false;
      player.isGrounded = false;
      player.attackCooldown = 0;
    });
    
    state.effects = [];
  })),
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => set(produce(state => {
    state.players[playerId].position = position;
    state.players[playerId].velocity = velocity;
  })),
  
  updatePlayerState: (playerId: string, mechaState: MechaData['state']) => set(produce(state => {
    state.players[playerId].state = mechaState;
  })),
  
  updatePlayerFacing: (playerId: string, facingRight: boolean) => set(produce(state => {
    state.players[playerId].facingRight = facingRight;
  })),
  
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => set(produce(state => {
    state.players[playerId].isGrounded = isGrounded;
  })),
  
  updatePlayerDefending: (playerId: string, isDefending: boolean) => set(produce(state => {
    state.players[playerId].isDefending = isDefending;
  })),
  
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => set(produce(state => {
    state.players[playerId].attackCooldown = cooldown;
  })),
  
  dealDamage: (targetId: string, damage: number) => set(produce(state => {
    const target = state.players[targetId];
    const actualDamage = target.isDefending ? Math.floor(damage * 0.5) : damage;
    target.health = Math.max(0, target.health - actualDamage);
    
    // 添加受击特效
    state.effects.push({
      id: `hit-${Date.now()}`,
      type: 'HIT',
      position: { ...target.position },
      duration: 300,
      timer: 0,
      frame: 0,
    });
    
    // 检查游戏结束
    if (target.health === 0) {
      const winnerId = targetId === 'player1' ? 'player2' : 'player1';
      state.gameState.phase = 'RESULT';
      state.gameState.winner = winnerId;
      state.players[winnerId].score += 1;
    }
  })),
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => set(produce(state => {
    const player = state.players[playerId];
    player.animationTimer += deltaTime;
  })),
  
  setAnimationFrame: (playerId: string, frame: number) => set(produce(state => {
    state.players[playerId].animationFrame = frame;
  })),
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => {
    const id = `effect-${Date.now()}-${Math.random().toString(36).substr(2, 9)}`;
    set(produce(state => {
      state.effects.push({
        ...effect,
        id,
        timer: 0,
      });
    }));
    return id;
  },
  
  updateEffects: (deltaTime: number) => set(produce(state => {
    state.effects = state.effects.filter(effect => {
      effect.timer += deltaTime;
      return effect.timer < effect.duration;
    });
  })),
  
  removeEffect: (effectId: string) => set(produce(state => {
    state.effects =import { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
  winner: null,
};

// 游戏Store接口
interface GameStore {
  // 状态
  gameState: GameState;
  players: Record<string, MechaData>;
  effects: Effect[];
  
  // 游戏流程控制
  startGame: () => void;
  pauseGame: () => void;
  resumeGame: () => void;
  endGame: (winner: string | null) => void;
  resetGame: () => void;
  nextRound: () => void;
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => void;
  updatePlayerState: (playerId: string, state: MechaData['state']) => void;
  updatePlayerFacing: (playerId: string, facingRight: boolean) => void;
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => void;
  updatePlayerDefending: (playerId: string, isDefending: boolean) => void;
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => void;
  dealDamage: (targetId: string, damage: number, attackerId?: string) => void;
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => void;
  setAnimationFrame: (playerId: string, frame: number) => void;
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => string;
  updateEffects: (deltaTime: number) => void;
  removeEffect: (effectId: string) => void;
  clearEffects: () => void;
  
  // 游戏时间
  tick: (deltaTime: number) => void;
}

// 创建Game Store
export const useGameStore = create<GameStore>((set, get) => ({
  // 初始状态
  gameState: initialGameState,
  players: {
    player1: createInitialMecha('player1', 'BLUE', 200),
    player2: createInitialMecha('player2', 'RED', 550),
  },
  effects: [],
  
  // 游戏流程控制
  startGame: () => set(produce(state => {
    state.gameState.phase = 'PLAYING';
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.winner = null;
  })),
  
  pauseGame: () => set(produce(state => {
    if (state.gameState.phase === 'PLAYING') {
      state.gameState.phase = 'PAUSED';
    }
  })),
  
  resumeGame: () => set(produce(state => {
    if (state.gameState.phase === 'PAUSED') {
      state.gameState.phase = 'PLAYING';
    }
  })),
  
  endGame: (winner: string | null) => set(produce(state => {
    state.gameState.phase = 'RESULT';
    state.gameState.winner = winner;
    if (winner && state.players[winner]) {
      state.players[winner].score += 1;
    }
  })),
  
  resetGame: () => set(produce(state => {
    state.gameState = initialGameState;
    state.players = {
      player1: createInitialMecha('player1', 'BLUE', 200),
      player2: createInitialMecha('player2', 'RED', 550),
    };
    state.effects = [];
  })),
  
  nextRound: () => set(produce(state => {
    state.gameState.round += 1;
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.phase = 'PLAYING';
    state.gameState.winner = null;
    
    // 重置玩家位置和状态
    Object.keys(state.players).forEach(playerId => {
      const player = state.players[playerId];
      player.health = player.maxHealth;
      player.position = playerId === 'player1' ? { x: 200, y: 400 } : { x: 550, y: 400 };
      player.velocity = { x: 0, y: 0 };
      player.state = 'IDLE';
      player.isDefending = false;
      player.isGrounded = false;
      player.attackCooldown = 0;
    });
    
    state.effects = [];
  })),
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => set(produce(state => {
    state.players[playerId].position = position;
    state.players[playerId].velocity = velocity;
  })),
  
  updatePlayerState: (playerId: string, mechaState: MechaData['state']) => set(produce(state => {
    state.players[playerId].state = mechaState;
  })),
  
  updatePlayerFacing: (playerId: string, facingRight: boolean) => set(produce(state => {
    state.players[playerId].facingRight = facingRight;
  })),
  
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => set(produce(state => {
    state.players[playerId].isGrounded = isGrounded;
  })),
  
  updatePlayerDefending: (playerId: string, isDefending: boolean) => set(produce(state => {
    state.players[playerId].isDefending = isDefending;
  })),
  
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => set(produce(state => {
    state.players[playerId].attackCooldown = cooldown;
  })),
  
  dealDamage: (targetId: string, damage: number) => set(produce(state => {
    const target = state.players[targetId];
    const actualDamage = target.isDefending ? Math.floor(damage * 0.5) : damage;
    target.health = Math.max(0, target.health - actualDamage);
    
    // 添加受击特效
    state.effects.push({
      id: `hit-${Date.now()}`,
      type: 'HIT',
      position: { ...target.position },
      duration: 300,
      timer: 0,
      frame: 0,
    });
    
    // 检查游戏结束
    if (target.health === 0) {
      const winnerId = targetId === 'player1' ? 'player2' : 'player1';
      state.gameState.phase = 'RESULT';
      state.gameState.winner = winnerId;
      state.players[winnerId].score += 1;
    }
  })),
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => set(produce(state => {
    const player = state.players[playerId];
    player.animationTimer += deltaTime;
  })),
  
  setAnimationFrame: (playerId: string, frame: number) => set(produce(state => {
    state.players[playerId].animationFrame = frame;
  })),
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => {
    const id = `effect-${Date.now()}-${Math.random().toString(36).substr(2, 9)}`;
    set(produce(state => {
      state.effects.push({
        ...effect,
        id,
        timer: 0,
      });
    }));
    return id;
  },
  
  updateEffects: (deltaTime: number) => set(produce(state => {
    state.effects = state.effects.filter(effect => {
      effect.timer += deltaTime;
      return effect.timer < effect.duration;
    });
  })),
  
  removeEffect: (effectId: string) => set(produce(state => {
    state.effects = state.effects.filter(e => e.idimport { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
  winner: null,
};

// 游戏Store接口
interface GameStore {
  // 状态
  gameState: GameState;
  players: Record<string, MechaData>;
  effects: Effect[];
  
  // 游戏流程控制
  startGame: () => void;
  pauseGame: () => void;
  resumeGame: () => void;
  endGame: (winner: string | null) => void;
  resetGame: () => void;
  nextRound: () => void;
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => void;
  updatePlayerState: (playerId: string, state: MechaData['state']) => void;
  updatePlayerFacing: (playerId: string, facingRight: boolean) => void;
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => void;
  updatePlayerDefending: (playerId: string, isDefending: boolean) => void;
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => void;
  dealDamage: (targetId: string, damage: number, attackerId?: string) => void;
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => void;
  setAnimationFrame: (playerId: string, frame: number) => void;
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => string;
  updateEffects: (deltaTime: number) => void;
  removeEffect: (effectId: string) => void;
  clearEffects: () => void;
  
  // 游戏时间
  tick: (deltaTime: number) => void;
}

// 创建Game Store
export const useGameStore = create<GameStore>((set, get) => ({
  // 初始状态
  gameState: initialGameState,
  players: {
    player1: createInitialMecha('player1', 'BLUE', 200),
    player2: createInitialMecha('player2', 'RED', 550),
  },
  effects: [],
  
  // 游戏流程控制
  startGame: () => set(produce(state => {
    state.gameState.phase = 'PLAYING';
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.winner = null;
  })),
  
  pauseGame: () => set(produce(state => {
    if (state.gameState.phase === 'PLAYING') {
      state.gameState.phase = 'PAUSED';
    }
  })),
  
  resumeGame: () => set(produce(state => {
    if (state.gameState.phase === 'PAUSED') {
      state.gameState.phase = 'PLAYING';
    }
  })),
  
  endGame: (winner: string | null) => set(produce(state => {
    state.gameState.phase = 'RESULT';
    state.gameState.winner = winner;
    if (winner && state.players[winner]) {
      state.players[winner].score += 1;
    }
  })),
  
  resetGame: () => set(produce(state => {
    state.gameState = initialGameState;
    state.players = {
      player1: createInitialMecha('player1', 'BLUE', 200),
      player2: createInitialMecha('player2', 'RED', 550),
    };
    state.effects = [];
  })),
  
  nextRound: () => set(produce(state => {
    state.gameState.round += 1;
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.phase = 'PLAYING';
    state.gameState.winner = null;
    
    // 重置玩家位置和状态
    Object.keys(state.players).forEach(playerId => {
      const player = state.players[playerId];
      player.health = player.maxHealth;
      player.position = playerId === 'player1' ? { x: 200, y: 400 } : { x: 550, y: 400 };
      player.velocity = { x: 0, y: 0 };
      player.state = 'IDLE';
      player.isDefending = false;
      player.isGrounded = false;
      player.attackCooldown = 0;
    });
    
    state.effects = [];
  })),
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => set(produce(state => {
    state.players[playerId].position = position;
    state.players[playerId].velocity = velocity;
  })),
  
  updatePlayerState: (playerId: string, mechaState: MechaData['state']) => set(produce(state => {
    state.players[playerId].state = mechaState;
  })),
  
  updatePlayerFacing: (playerId: string, facingRight: boolean) => set(produce(state => {
    state.players[playerId].facingRight = facingRight;
  })),
  
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => set(produce(state => {
    state.players[playerId].isGrounded = isGrounded;
  })),
  
  updatePlayerDefending: (playerId: string, isDefending: boolean) => set(produce(state => {
    state.players[playerId].isDefending = isDefending;
  })),
  
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => set(produce(state => {
    state.players[playerId].attackCooldown = cooldown;
  })),
  
  dealDamage: (targetId: string, damage: number) => set(produce(state => {
    const target = state.players[targetId];
    const actualDamage = target.isDefending ? Math.floor(damage * 0.5) : damage;
    target.health = Math.max(0, target.health - actualDamage);
    
    // 添加受击特效
    state.effects.push({
      id: `hit-${Date.now()}`,
      type: 'HIT',
      position: { ...target.position },
      duration: 300,
      timer: 0,
      frame: 0,
    });
    
    // 检查游戏结束
    if (target.health === 0) {
      const winnerId = targetId === 'player1' ? 'player2' : 'player1';
      state.gameState.phase = 'RESULT';
      state.gameState.winner = winnerId;
      state.players[winnerId].score += 1;
    }
  })),
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => set(produce(state => {
    const player = state.players[playerId];
    player.animationTimer += deltaTime;
  })),
  
  setAnimationFrame: (playerId: string, frame: number) => set(produce(state => {
    state.players[playerId].animationFrame = frame;
  })),
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => {
    const id = `effect-${Date.now()}-${Math.random().toString(36).substr(2, 9)}`;
    set(produce(state => {
      state.effects.push({
        ...effect,
        id,
        timer: 0,
      });
    }));
    return id;
  },
  
  updateEffects: (deltaTime: number) => set(produce(state => {
    state.effects = state.effects.filter(effect => {
      effect.timer += deltaTime;
      return effect.timer < effect.duration;
    });
  })),
  
  removeEffect: (effectId: string) => set(produce(state => {
    state.effects = state.effects.filter(e => e.id !== effectId);
  }import { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
  winner: null,
};

// 游戏Store接口
interface GameStore {
  // 状态
  gameState: GameState;
  players: Record<string, MechaData>;
  effects: Effect[];
  
  // 游戏流程控制
  startGame: () => void;
  pauseGame: () => void;
  resumeGame: () => void;
  endGame: (winner: string | null) => void;
  resetGame: () => void;
  nextRound: () => void;
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => void;
  updatePlayerState: (playerId: string, state: MechaData['state']) => void;
  updatePlayerFacing: (playerId: string, facingRight: boolean) => void;
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => void;
  updatePlayerDefending: (playerId: string, isDefending: boolean) => void;
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => void;
  dealDamage: (targetId: string, damage: number, attackerId?: string) => void;
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => void;
  setAnimationFrame: (playerId: string, frame: number) => void;
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => string;
  updateEffects: (deltaTime: number) => void;
  removeEffect: (effectId: string) => void;
  clearEffects: () => void;
  
  // 游戏时间
  tick: (deltaTime: number) => void;
}

// 创建Game Store
export const useGameStore = create<GameStore>((set, get) => ({
  // 初始状态
  gameState: initialGameState,
  players: {
    player1: createInitialMecha('player1', 'BLUE', 200),
    player2: createInitialMecha('player2', 'RED', 550),
  },
  effects: [],
  
  // 游戏流程控制
  startGame: () => set(produce(state => {
    state.gameState.phase = 'PLAYING';
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.winner = null;
  })),
  
  pauseGame: () => set(produce(state => {
    if (state.gameState.phase === 'PLAYING') {
      state.gameState.phase = 'PAUSED';
    }
  })),
  
  resumeGame: () => set(produce(state => {
    if (state.gameState.phase === 'PAUSED') {
      state.gameState.phase = 'PLAYING';
    }
  })),
  
  endGame: (winner: string | null) => set(produce(state => {
    state.gameState.phase = 'RESULT';
    state.gameState.winner = winner;
    if (winner && state.players[winner]) {
      state.players[winner].score += 1;
    }
  })),
  
  resetGame: () => set(produce(state => {
    state.gameState = initialGameState;
    state.players = {
      player1: createInitialMecha('player1', 'BLUE', 200),
      player2: createInitialMecha('player2', 'RED', 550),
    };
    state.effects = [];
  })),
  
  nextRound: () => set(produce(state => {
    state.gameState.round += 1;
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.phase = 'PLAYING';
    state.gameState.winner = null;
    
    // 重置玩家位置和状态
    Object.keys(state.players).forEach(playerId => {
      const player = state.players[playerId];
      player.health = player.maxHealth;
      player.position = playerId === 'player1' ? { x: 200, y: 400 } : { x: 550, y: 400 };
      player.velocity = { x: 0, y: 0 };
      player.state = 'IDLE';
      player.isDefending = false;
      player.isGrounded = false;
      player.attackCooldown = 0;
    });
    
    state.effects = [];
  })),
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => set(produce(state => {
    state.players[playerId].position = position;
    state.players[playerId].velocity = velocity;
  })),
  
  updatePlayerState: (playerId: string, mechaState: MechaData['state']) => set(produce(state => {
    state.players[playerId].state = mechaState;
  })),
  
  updatePlayerFacing: (playerId: string, facingRight: boolean) => set(produce(state => {
    state.players[playerId].facingRight = facingRight;
  })),
  
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => set(produce(state => {
    state.players[playerId].isGrounded = isGrounded;
  })),
  
  updatePlayerDefending: (playerId: string, isDefending: boolean) => set(produce(state => {
    state.players[playerId].isDefending = isDefending;
  })),
  
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => set(produce(state => {
    state.players[playerId].attackCooldown = cooldown;
  })),
  
  dealDamage: (targetId: string, damage: number) => set(produce(state => {
    const target = state.players[targetId];
    const actualDamage = target.isDefending ? Math.floor(damage * 0.5) : damage;
    target.health = Math.max(0, target.health - actualDamage);
    
    // 添加受击特效
    state.effects.push({
      id: `hit-${Date.now()}`,
      type: 'HIT',
      position: { ...target.position },
      duration: 300,
      timer: 0,
      frame: 0,
    });
    
    // 检查游戏结束
    if (target.health === 0) {
      const winnerId = targetId === 'player1' ? 'player2' : 'player1';
      state.gameState.phase = 'RESULT';
      state.gameState.winner = winnerId;
      state.players[winnerId].score += 1;
    }
  })),
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => set(produce(state => {
    const player = state.players[playerId];
    player.animationTimer += deltaTime;
  })),
  
  setAnimationFrame: (playerId: string, frame: number) => set(produce(state => {
    state.players[playerId].animationFrame = frame;
  })),
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => {
    const id = `effect-${Date.now()}-${Math.random().toString(36).substr(2, 9)}`;
    set(produce(state => {
      state.effects.push({
        ...effect,
        id,
        timer: 0,
      });
    }));
    return id;
  },
  
  updateEffects: (deltaTime: number) => set(produce(state => {
    state.effects = state.effects.filter(effect => {
      effect.timer += deltaTime;
      return effect.timer < effect.duration;
    });
  })),
  
  removeEffect: (effectId: string) => set(produce(state => {
    state.effects = state.effects.filter(e => e.id !== effectId);
  })),
  
  clearimport { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
  winner: null,
};

// 游戏Store接口
interface GameStore {
  // 状态
  gameState: GameState;
  players: Record<string, MechaData>;
  effects: Effect[];
  
  // 游戏流程控制
  startGame: () => void;
  pauseGame: () => void;
  resumeGame: () => void;
  endGame: (winner: string | null) => void;
  resetGame: () => void;
  nextRound: () => void;
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => void;
  updatePlayerState: (playerId: string, state: MechaData['state']) => void;
  updatePlayerFacing: (playerId: string, facingRight: boolean) => void;
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => void;
  updatePlayerDefending: (playerId: string, isDefending: boolean) => void;
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => void;
  dealDamage: (targetId: string, damage: number, attackerId?: string) => void;
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => void;
  setAnimationFrame: (playerId: string, frame: number) => void;
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => string;
  updateEffects: (deltaTime: number) => void;
  removeEffect: (effectId: string) => void;
  clearEffects: () => void;
  
  // 游戏时间
  tick: (deltaTime: number) => void;
}

// 创建Game Store
export const useGameStore = create<GameStore>((set, get) => ({
  // 初始状态
  gameState: initialGameState,
  players: {
    player1: createInitialMecha('player1', 'BLUE', 200),
    player2: createInitialMecha('player2', 'RED', 550),
  },
  effects: [],
  
  // 游戏流程控制
  startGame: () => set(produce(state => {
    state.gameState.phase = 'PLAYING';
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.winner = null;
  })),
  
  pauseGame: () => set(produce(state => {
    if (state.gameState.phase === 'PLAYING') {
      state.gameState.phase = 'PAUSED';
    }
  })),
  
  resumeGame: () => set(produce(state => {
    if (state.gameState.phase === 'PAUSED') {
      state.gameState.phase = 'PLAYING';
    }
  })),
  
  endGame: (winner: string | null) => set(produce(state => {
    state.gameState.phase = 'RESULT';
    state.gameState.winner = winner;
    if (winner && state.players[winner]) {
      state.players[winner].score += 1;
    }
  })),
  
  resetGame: () => set(produce(state => {
    state.gameState = initialGameState;
    state.players = {
      player1: createInitialMecha('player1', 'BLUE', 200),
      player2: createInitialMecha('player2', 'RED', 550),
    };
    state.effects = [];
  })),
  
  nextRound: () => set(produce(state => {
    state.gameState.round += 1;
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.phase = 'PLAYING';
    state.gameState.winner = null;
    
    // 重置玩家位置和状态
    Object.keys(state.players).forEach(playerId => {
      const player = state.players[playerId];
      player.health = player.maxHealth;
      player.position = playerId === 'player1' ? { x: 200, y: 400 } : { x: 550, y: 400 };
      player.velocity = { x: 0, y: 0 };
      player.state = 'IDLE';
      player.isDefending = false;
      player.isGrounded = false;
      player.attackCooldown = 0;
    });
    
    state.effects = [];
  })),
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => set(produce(state => {
    state.players[playerId].position = position;
    state.players[playerId].velocity = velocity;
  })),
  
  updatePlayerState: (playerId: string, mechaState: MechaData['state']) => set(produce(state => {
    state.players[playerId].state = mechaState;
  })),
  
  updatePlayerFacing: (playerId: string, facingRight: boolean) => set(produce(state => {
    state.players[playerId].facingRight = facingRight;
  })),
  
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => set(produce(state => {
    state.players[playerId].isGrounded = isGrounded;
  })),
  
  updatePlayerDefending: (playerId: string, isDefending: boolean) => set(produce(state => {
    state.players[playerId].isDefending = isDefending;
  })),
  
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => set(produce(state => {
    state.players[playerId].attackCooldown = cooldown;
  })),
  
  dealDamage: (targetId: string, damage: number) => set(produce(state => {
    const target = state.players[targetId];
    const actualDamage = target.isDefending ? Math.floor(damage * 0.5) : damage;
    target.health = Math.max(0, target.health - actualDamage);
    
    // 添加受击特效
    state.effects.push({
      id: `hit-${Date.now()}`,
      type: 'HIT',
      position: { ...target.position },
      duration: 300,
      timer: 0,
      frame: 0,
    });
    
    // 检查游戏结束
    if (target.health === 0) {
      const winnerId = targetId === 'player1' ? 'player2' : 'player1';
      state.gameState.phase = 'RESULT';
      state.gameState.winner = winnerId;
      state.players[winnerId].score += 1;
    }
  })),
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => set(produce(state => {
    const player = state.players[playerId];
    player.animationTimer += deltaTime;
  })),
  
  setAnimationFrame: (playerId: string, frame: number) => set(produce(state => {
    state.players[playerId].animationFrame = frame;
  })),
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => {
    const id = `effect-${Date.now()}-${Math.random().toString(36).substr(2, 9)}`;
    set(produce(state => {
      state.effects.push({
        ...effect,
        id,
        timer: 0,
      });
    }));
    return id;
  },
  
  updateEffects: (deltaTime: number) => set(produce(state => {
    state.effects = state.effects.filter(effect => {
      effect.timer += deltaTime;
      return effect.timer < effect.duration;
    });
  })),
  
  removeEffect: (effectId: string) => set(produce(state => {
    state.effects = state.effects.filter(e => e.id !== effectId);
  })),
  
  clearEffects: () => set(produceimport { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
  winner: null,
};

// 游戏Store接口
interface GameStore {
  // 状态
  gameState: GameState;
  players: Record<string, MechaData>;
  effects: Effect[];
  
  // 游戏流程控制
  startGame: () => void;
  pauseGame: () => void;
  resumeGame: () => void;
  endGame: (winner: string | null) => void;
  resetGame: () => void;
  nextRound: () => void;
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => void;
  updatePlayerState: (playerId: string, state: MechaData['state']) => void;
  updatePlayerFacing: (playerId: string, facingRight: boolean) => void;
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => void;
  updatePlayerDefending: (playerId: string, isDefending: boolean) => void;
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => void;
  dealDamage: (targetId: string, damage: number, attackerId?: string) => void;
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => void;
  setAnimationFrame: (playerId: string, frame: number) => void;
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => string;
  updateEffects: (deltaTime: number) => void;
  removeEffect: (effectId: string) => void;
  clearEffects: () => void;
  
  // 游戏时间
  tick: (deltaTime: number) => void;
}

// 创建Game Store
export const useGameStore = create<GameStore>((set, get) => ({
  // 初始状态
  gameState: initialGameState,
  players: {
    player1: createInitialMecha('player1', 'BLUE', 200),
    player2: createInitialMecha('player2', 'RED', 550),
  },
  effects: [],
  
  // 游戏流程控制
  startGame: () => set(produce(state => {
    state.gameState.phase = 'PLAYING';
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.winner = null;
  })),
  
  pauseGame: () => set(produce(state => {
    if (state.gameState.phase === 'PLAYING') {
      state.gameState.phase = 'PAUSED';
    }
  })),
  
  resumeGame: () => set(produce(state => {
    if (state.gameState.phase === 'PAUSED') {
      state.gameState.phase = 'PLAYING';
    }
  })),
  
  endGame: (winner: string | null) => set(produce(state => {
    state.gameState.phase = 'RESULT';
    state.gameState.winner = winner;
    if (winner && state.players[winner]) {
      state.players[winner].score += 1;
    }
  })),
  
  resetGame: () => set(produce(state => {
    state.gameState = initialGameState;
    state.players = {
      player1: createInitialMecha('player1', 'BLUE', 200),
      player2: createInitialMecha('player2', 'RED', 550),
    };
    state.effects = [];
  })),
  
  nextRound: () => set(produce(state => {
    state.gameState.round += 1;
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.phase = 'PLAYING';
    state.gameState.winner = null;
    
    // 重置玩家位置和状态
    Object.keys(state.players).forEach(playerId => {
      const player = state.players[playerId];
      player.health = player.maxHealth;
      player.position = playerId === 'player1' ? { x: 200, y: 400 } : { x: 550, y: 400 };
      player.velocity = { x: 0, y: 0 };
      player.state = 'IDLE';
      player.isDefending = false;
      player.isGrounded = false;
      player.attackCooldown = 0;
    });
    
    state.effects = [];
  })),
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => set(produce(state => {
    state.players[playerId].position = position;
    state.players[playerId].velocity = velocity;
  })),
  
  updatePlayerState: (playerId: string, mechaState: MechaData['state']) => set(produce(state => {
    state.players[playerId].state = mechaState;
  })),
  
  updatePlayerFacing: (playerId: string, facingRight: boolean) => set(produce(state => {
    state.players[playerId].facingRight = facingRight;
  })),
  
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => set(produce(state => {
    state.players[playerId].isGrounded = isGrounded;
  })),
  
  updatePlayerDefending: (playerId: string, isDefending: boolean) => set(produce(state => {
    state.players[playerId].isDefending = isDefending;
  })),
  
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => set(produce(state => {
    state.players[playerId].attackCooldown = cooldown;
  })),
  
  dealDamage: (targetId: string, damage: number) => set(produce(state => {
    const target = state.players[targetId];
    const actualDamage = target.isDefending ? Math.floor(damage * 0.5) : damage;
    target.health = Math.max(0, target.health - actualDamage);
    
    // 添加受击特效
    state.effects.push({
      id: `hit-${Date.now()}`,
      type: 'HIT',
      position: { ...target.position },
      duration: 300,
      timer: 0,
      frame: 0,
    });
    
    // 检查游戏结束
    if (target.health === 0) {
      const winnerId = targetId === 'player1' ? 'player2' : 'player1';
      state.gameState.phase = 'RESULT';
      state.gameState.winner = winnerId;
      state.players[winnerId].score += 1;
    }
  })),
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => set(produce(state => {
    const player = state.players[playerId];
    player.animationTimer += deltaTime;
  })),
  
  setAnimationFrame: (playerId: string, frame: number) => set(produce(state => {
    state.players[playerId].animationFrame = frame;
  })),
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => {
    const id = `effect-${Date.now()}-${Math.random().toString(36).substr(2, 9)}`;
    set(produce(state => {
      state.effects.push({
        ...effect,
        id,
        timer: 0,
      });
    }));
    return id;
  },
  
  updateEffects: (deltaTime: number) => set(produce(state => {
    state.effects = state.effects.filter(effect => {
      effect.timer += deltaTime;
      return effect.timer < effect.duration;
    });
  })),
  
  removeEffect: (effectId: string) => set(produce(state => {
    state.effects = state.effects.filter(e => e.id !== effectId);
  })),
  
  clearEffects: () => set(produce(state => {
    state.effectsimport { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
  winner: null,
};

// 游戏Store接口
interface GameStore {
  // 状态
  gameState: GameState;
  players: Record<string, MechaData>;
  effects: Effect[];
  
  // 游戏流程控制
  startGame: () => void;
  pauseGame: () => void;
  resumeGame: () => void;
  endGame: (winner: string | null) => void;
  resetGame: () => void;
  nextRound: () => void;
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => void;
  updatePlayerState: (playerId: string, state: MechaData['state']) => void;
  updatePlayerFacing: (playerId: string, facingRight: boolean) => void;
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => void;
  updatePlayerDefending: (playerId: string, isDefending: boolean) => void;
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => void;
  dealDamage: (targetId: string, damage: number, attackerId?: string) => void;
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => void;
  setAnimationFrame: (playerId: string, frame: number) => void;
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => string;
  updateEffects: (deltaTime: number) => void;
  removeEffect: (effectId: string) => void;
  clearEffects: () => void;
  
  // 游戏时间
  tick: (deltaTime: number) => void;
}

// 创建Game Store
export const useGameStore = create<GameStore>((set, get) => ({
  // 初始状态
  gameState: initialGameState,
  players: {
    player1: createInitialMecha('player1', 'BLUE', 200),
    player2: createInitialMecha('player2', 'RED', 550),
  },
  effects: [],
  
  // 游戏流程控制
  startGame: () => set(produce(state => {
    state.gameState.phase = 'PLAYING';
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.winner = null;
  })),
  
  pauseGame: () => set(produce(state => {
    if (state.gameState.phase === 'PLAYING') {
      state.gameState.phase = 'PAUSED';
    }
  })),
  
  resumeGame: () => set(produce(state => {
    if (state.gameState.phase === 'PAUSED') {
      state.gameState.phase = 'PLAYING';
    }
  })),
  
  endGame: (winner: string | null) => set(produce(state => {
    state.gameState.phase = 'RESULT';
    state.gameState.winner = winner;
    if (winner && state.players[winner]) {
      state.players[winner].score += 1;
    }
  })),
  
  resetGame: () => set(produce(state => {
    state.gameState = initialGameState;
    state.players = {
      player1: createInitialMecha('player1', 'BLUE', 200),
      player2: createInitialMecha('player2', 'RED', 550),
    };
    state.effects = [];
  })),
  
  nextRound: () => set(produce(state => {
    state.gameState.round += 1;
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.phase = 'PLAYING';
    state.gameState.winner = null;
    
    // 重置玩家位置和状态
    Object.keys(state.players).forEach(playerId => {
      const player = state.players[playerId];
      player.health = player.maxHealth;
      player.position = playerId === 'player1' ? { x: 200, y: 400 } : { x: 550, y: 400 };
      player.velocity = { x: 0, y: 0 };
      player.state = 'IDLE';
      player.isDefending = false;
      player.isGrounded = false;
      player.attackCooldown = 0;
    });
    
    state.effects = [];
  })),
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => set(produce(state => {
    state.players[playerId].position = position;
    state.players[playerId].velocity = velocity;
  })),
  
  updatePlayerState: (playerId: string, mechaState: MechaData['state']) => set(produce(state => {
    state.players[playerId].state = mechaState;
  })),
  
  updatePlayerFacing: (playerId: string, facingRight: boolean) => set(produce(state => {
    state.players[playerId].facingRight = facingRight;
  })),
  
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => set(produce(state => {
    state.players[playerId].isGrounded = isGrounded;
  })),
  
  updatePlayerDefending: (playerId: string, isDefending: boolean) => set(produce(state => {
    state.players[playerId].isDefending = isDefending;
  })),
  
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => set(produce(state => {
    state.players[playerId].attackCooldown = cooldown;
  })),
  
  dealDamage: (targetId: string, damage: number) => set(produce(state => {
    const target = state.players[targetId];
    const actualDamage = target.isDefending ? Math.floor(damage * 0.5) : damage;
    target.health = Math.max(0, target.health - actualDamage);
    
    // 添加受击特效
    state.effects.push({
      id: `hit-${Date.now()}`,
      type: 'HIT',
      position: { ...target.position },
      duration: 300,
      timer: 0,
      frame: 0,
    });
    
    // 检查游戏结束
    if (target.health === 0) {
      const winnerId = targetId === 'player1' ? 'player2' : 'player1';
      state.gameState.phase = 'RESULT';
      state.gameState.winner = winnerId;
      state.players[winnerId].score += 1;
    }
  })),
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => set(produce(state => {
    const player = state.players[playerId];
    player.animationTimer += deltaTime;
  })),
  
  setAnimationFrame: (playerId: string, frame: number) => set(produce(state => {
    state.players[playerId].animationFrame = frame;
  })),
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => {
    const id = `effect-${Date.now()}-${Math.random().toString(36).substr(2, 9)}`;
    set(produce(state => {
      state.effects.push({
        ...effect,
        id,
        timer: 0,
      });
    }));
    return id;
  },
  
  updateEffects: (deltaTime: number) => set(produce(state => {
    state.effects = state.effects.filter(effect => {
      effect.timer += deltaTime;
      return effect.timer < effect.duration;
    });
  })),
  
  removeEffect: (effectId: string) => set(produce(state => {
    state.effects = state.effects.filter(e => e.id !== effectId);
  })),
  
  clearEffects: () => set(produce(state => {
    state.effects = [];
  })),import { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
  winner: null,
};

// 游戏Store接口
interface GameStore {
  // 状态
  gameState: GameState;
  players: Record<string, MechaData>;
  effects: Effect[];
  
  // 游戏流程控制
  startGame: () => void;
  pauseGame: () => void;
  resumeGame: () => void;
  endGame: (winner: string | null) => void;
  resetGame: () => void;
  nextRound: () => void;
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => void;
  updatePlayerState: (playerId: string, state: MechaData['state']) => void;
  updatePlayerFacing: (playerId: string, facingRight: boolean) => void;
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => void;
  updatePlayerDefending: (playerId: string, isDefending: boolean) => void;
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => void;
  dealDamage: (targetId: string, damage: number, attackerId?: string) => void;
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => void;
  setAnimationFrame: (playerId: string, frame: number) => void;
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => string;
  updateEffects: (deltaTime: number) => void;
  removeEffect: (effectId: string) => void;
  clearEffects: () => void;
  
  // 游戏时间
  tick: (deltaTime: number) => void;
}

// 创建Game Store
export const useGameStore = create<GameStore>((set, get) => ({
  // 初始状态
  gameState: initialGameState,
  players: {
    player1: createInitialMecha('player1', 'BLUE', 200),
    player2: createInitialMecha('player2', 'RED', 550),
  },
  effects: [],
  
  // 游戏流程控制
  startGame: () => set(produce(state => {
    state.gameState.phase = 'PLAYING';
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.winner = null;
  })),
  
  pauseGame: () => set(produce(state => {
    if (state.gameState.phase === 'PLAYING') {
      state.gameState.phase = 'PAUSED';
    }
  })),
  
  resumeGame: () => set(produce(state => {
    if (state.gameState.phase === 'PAUSED') {
      state.gameState.phase = 'PLAYING';
    }
  })),
  
  endGame: (winner: string | null) => set(produce(state => {
    state.gameState.phase = 'RESULT';
    state.gameState.winner = winner;
    if (winner && state.players[winner]) {
      state.players[winner].score += 1;
    }
  })),
  
  resetGame: () => set(produce(state => {
    state.gameState = initialGameState;
    state.players = {
      player1: createInitialMecha('player1', 'BLUE', 200),
      player2: createInitialMecha('player2', 'RED', 550),
    };
    state.effects = [];
  })),
  
  nextRound: () => set(produce(state => {
    state.gameState.round += 1;
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.phase = 'PLAYING';
    state.gameState.winner = null;
    
    // 重置玩家位置和状态
    Object.keys(state.players).forEach(playerId => {
      const player = state.players[playerId];
      player.health = player.maxHealth;
      player.position = playerId === 'player1' ? { x: 200, y: 400 } : { x: 550, y: 400 };
      player.velocity = { x: 0, y: 0 };
      player.state = 'IDLE';
      player.isDefending = false;
      player.isGrounded = false;
      player.attackCooldown = 0;
    });
    
    state.effects = [];
  })),
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => set(produce(state => {
    state.players[playerId].position = position;
    state.players[playerId].velocity = velocity;
  })),
  
  updatePlayerState: (playerId: string, mechaState: MechaData['state']) => set(produce(state => {
    state.players[playerId].state = mechaState;
  })),
  
  updatePlayerFacing: (playerId: string, facingRight: boolean) => set(produce(state => {
    state.players[playerId].facingRight = facingRight;
  })),
  
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => set(produce(state => {
    state.players[playerId].isGrounded = isGrounded;
  })),
  
  updatePlayerDefending: (playerId: string, isDefending: boolean) => set(produce(state => {
    state.players[playerId].isDefending = isDefending;
  })),
  
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => set(produce(state => {
    state.players[playerId].attackCooldown = cooldown;
  })),
  
  dealDamage: (targetId: string, damage: number) => set(produce(state => {
    const target = state.players[targetId];
    const actualDamage = target.isDefending ? Math.floor(damage * 0.5) : damage;
    target.health = Math.max(0, target.health - actualDamage);
    
    // 添加受击特效
    state.effects.push({
      id: `hit-${Date.now()}`,
      type: 'HIT',
      position: { ...target.position },
      duration: 300,
      timer: 0,
      frame: 0,
    });
    
    // 检查游戏结束
    if (target.health === 0) {
      const winnerId = targetId === 'player1' ? 'player2' : 'player1';
      state.gameState.phase = 'RESULT';
      state.gameState.winner = winnerId;
      state.players[winnerId].score += 1;
    }
  })),
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => set(produce(state => {
    const player = state.players[playerId];
    player.animationTimer += deltaTime;
  })),
  
  setAnimationFrame: (playerId: string, frame: number) => set(produce(state => {
    state.players[playerId].animationFrame = frame;
  })),
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => {
    const id = `effect-${Date.now()}-${Math.random().toString(36).substr(2, 9)}`;
    set(produce(state => {
      state.effects.push({
        ...effect,
        id,
        timer: 0,
      });
    }));
    return id;
  },
  
  updateEffects: (deltaTime: number) => set(produce(state => {
    state.effects = state.effects.filter(effect => {
      effect.timer += deltaTime;
      return effect.timer < effect.duration;
    });
  })),
  
  removeEffect: (effectId: string) => set(produce(state => {
    state.effects = state.effects.filter(e => e.id !== effectId);
  })),
  
  clearEffects: () => set(produce(state => {
    state.effects = [];
  })),
  
  //import { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
  winner: null,
};

// 游戏Store接口
interface GameStore {
  // 状态
  gameState: GameState;
  players: Record<string, MechaData>;
  effects: Effect[];
  
  // 游戏流程控制
  startGame: () => void;
  pauseGame: () => void;
  resumeGame: () => void;
  endGame: (winner: string | null) => void;
  resetGame: () => void;
  nextRound: () => void;
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => void;
  updatePlayerState: (playerId: string, state: MechaData['state']) => void;
  updatePlayerFacing: (playerId: string, facingRight: boolean) => void;
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => void;
  updatePlayerDefending: (playerId: string, isDefending: boolean) => void;
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => void;
  dealDamage: (targetId: string, damage: number, attackerId?: string) => void;
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => void;
  setAnimationFrame: (playerId: string, frame: number) => void;
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => string;
  updateEffects: (deltaTime: number) => void;
  removeEffect: (effectId: string) => void;
  clearEffects: () => void;
  
  // 游戏时间
  tick: (deltaTime: number) => void;
}

// 创建Game Store
export const useGameStore = create<GameStore>((set, get) => ({
  // 初始状态
  gameState: initialGameState,
  players: {
    player1: createInitialMecha('player1', 'BLUE', 200),
    player2: createInitialMecha('player2', 'RED', 550),
  },
  effects: [],
  
  // 游戏流程控制
  startGame: () => set(produce(state => {
    state.gameState.phase = 'PLAYING';
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.winner = null;
  })),
  
  pauseGame: () => set(produce(state => {
    if (state.gameState.phase === 'PLAYING') {
      state.gameState.phase = 'PAUSED';
    }
  })),
  
  resumeGame: () => set(produce(state => {
    if (state.gameState.phase === 'PAUSED') {
      state.gameState.phase = 'PLAYING';
    }
  })),
  
  endGame: (winner: string | null) => set(produce(state => {
    state.gameState.phase = 'RESULT';
    state.gameState.winner = winner;
    if (winner && state.players[winner]) {
      state.players[winner].score += 1;
    }
  })),
  
  resetGame: () => set(produce(state => {
    state.gameState = initialGameState;
    state.players = {
      player1: createInitialMecha('player1', 'BLUE', 200),
      player2: createInitialMecha('player2', 'RED', 550),
    };
    state.effects = [];
  })),
  
  nextRound: () => set(produce(state => {
    state.gameState.round += 1;
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.phase = 'PLAYING';
    state.gameState.winner = null;
    
    // 重置玩家位置和状态
    Object.keys(state.players).forEach(playerId => {
      const player = state.players[playerId];
      player.health = player.maxHealth;
      player.position = playerId === 'player1' ? { x: 200, y: 400 } : { x: 550, y: 400 };
      player.velocity = { x: 0, y: 0 };
      player.state = 'IDLE';
      player.isDefending = false;
      player.isGrounded = false;
      player.attackCooldown = 0;
    });
    
    state.effects = [];
  })),
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => set(produce(state => {
    state.players[playerId].position = position;
    state.players[playerId].velocity = velocity;
  })),
  
  updatePlayerState: (playerId: string, mechaState: MechaData['state']) => set(produce(state => {
    state.players[playerId].state = mechaState;
  })),
  
  updatePlayerFacing: (playerId: string, facingRight: boolean) => set(produce(state => {
    state.players[playerId].facingRight = facingRight;
  })),
  
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => set(produce(state => {
    state.players[playerId].isGrounded = isGrounded;
  })),
  
  updatePlayerDefending: (playerId: string, isDefending: boolean) => set(produce(state => {
    state.players[playerId].isDefending = isDefending;
  })),
  
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => set(produce(state => {
    state.players[playerId].attackCooldown = cooldown;
  })),
  
  dealDamage: (targetId: string, damage: number) => set(produce(state => {
    const target = state.players[targetId];
    const actualDamage = target.isDefending ? Math.floor(damage * 0.5) : damage;
    target.health = Math.max(0, target.health - actualDamage);
    
    // 添加受击特效
    state.effects.push({
      id: `hit-${Date.now()}`,
      type: 'HIT',
      position: { ...target.position },
      duration: 300,
      timer: 0,
      frame: 0,
    });
    
    // 检查游戏结束
    if (target.health === 0) {
      const winnerId = targetId === 'player1' ? 'player2' : 'player1';
      state.gameState.phase = 'RESULT';
      state.gameState.winner = winnerId;
      state.players[winnerId].score += 1;
    }
  })),
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => set(produce(state => {
    const player = state.players[playerId];
    player.animationTimer += deltaTime;
  })),
  
  setAnimationFrame: (playerId: string, frame: number) => set(produce(state => {
    state.players[playerId].animationFrame = frame;
  })),
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => {
    const id = `effect-${Date.now()}-${Math.random().toString(36).substr(2, 9)}`;
    set(produce(state => {
      state.effects.push({
        ...effect,
        id,
        timer: 0,
      });
    }));
    return id;
  },
  
  updateEffects: (deltaTime: number) => set(produce(state => {
    state.effects = state.effects.filter(effect => {
      effect.timer += deltaTime;
      return effect.timer < effect.duration;
    });
  })),
  
  removeEffect: (effectId: string) => set(produce(state => {
    state.effects = state.effects.filter(e => e.id !== effectId);
  })),
  
  clearEffects: () => set(produce(state => {
    state.effects = [];
  })),
  
  // 游戏时间
  tick:import { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
  winner: null,
};

// 游戏Store接口
interface GameStore {
  // 状态
  gameState: GameState;
  players: Record<string, MechaData>;
  effects: Effect[];
  
  // 游戏流程控制
  startGame: () => void;
  pauseGame: () => void;
  resumeGame: () => void;
  endGame: (winner: string | null) => void;
  resetGame: () => void;
  nextRound: () => void;
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => void;
  updatePlayerState: (playerId: string, state: MechaData['state']) => void;
  updatePlayerFacing: (playerId: string, facingRight: boolean) => void;
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => void;
  updatePlayerDefending: (playerId: string, isDefending: boolean) => void;
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => void;
  dealDamage: (targetId: string, damage: number, attackerId?: string) => void;
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => void;
  setAnimationFrame: (playerId: string, frame: number) => void;
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => string;
  updateEffects: (deltaTime: number) => void;
  removeEffect: (effectId: string) => void;
  clearEffects: () => void;
  
  // 游戏时间
  tick: (deltaTime: number) => void;
}

// 创建Game Store
export const useGameStore = create<GameStore>((set, get) => ({
  // 初始状态
  gameState: initialGameState,
  players: {
    player1: createInitialMecha('player1', 'BLUE', 200),
    player2: createInitialMecha('player2', 'RED', 550),
  },
  effects: [],
  
  // 游戏流程控制
  startGame: () => set(produce(state => {
    state.gameState.phase = 'PLAYING';
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.winner = null;
  })),
  
  pauseGame: () => set(produce(state => {
    if (state.gameState.phase === 'PLAYING') {
      state.gameState.phase = 'PAUSED';
    }
  })),
  
  resumeGame: () => set(produce(state => {
    if (state.gameState.phase === 'PAUSED') {
      state.gameState.phase = 'PLAYING';
    }
  })),
  
  endGame: (winner: string | null) => set(produce(state => {
    state.gameState.phase = 'RESULT';
    state.gameState.winner = winner;
    if (winner && state.players[winner]) {
      state.players[winner].score += 1;
    }
  })),
  
  resetGame: () => set(produce(state => {
    state.gameState = initialGameState;
    state.players = {
      player1: createInitialMecha('player1', 'BLUE', 200),
      player2: createInitialMecha('player2', 'RED', 550),
    };
    state.effects = [];
  })),
  
  nextRound: () => set(produce(state => {
    state.gameState.round += 1;
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.phase = 'PLAYING';
    state.gameState.winner = null;
    
    // 重置玩家位置和状态
    Object.keys(state.players).forEach(playerId => {
      const player = state.players[playerId];
      player.health = player.maxHealth;
      player.position = playerId === 'player1' ? { x: 200, y: 400 } : { x: 550, y: 400 };
      player.velocity = { x: 0, y: 0 };
      player.state = 'IDLE';
      player.isDefending = false;
      player.isGrounded = false;
      player.attackCooldown = 0;
    });
    
    state.effects = [];
  })),
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => set(produce(state => {
    state.players[playerId].position = position;
    state.players[playerId].velocity = velocity;
  })),
  
  updatePlayerState: (playerId: string, mechaState: MechaData['state']) => set(produce(state => {
    state.players[playerId].state = mechaState;
  })),
  
  updatePlayerFacing: (playerId: string, facingRight: boolean) => set(produce(state => {
    state.players[playerId].facingRight = facingRight;
  })),
  
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => set(produce(state => {
    state.players[playerId].isGrounded = isGrounded;
  })),
  
  updatePlayerDefending: (playerId: string, isDefending: boolean) => set(produce(state => {
    state.players[playerId].isDefending = isDefending;
  })),
  
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => set(produce(state => {
    state.players[playerId].attackCooldown = cooldown;
  })),
  
  dealDamage: (targetId: string, damage: number) => set(produce(state => {
    const target = state.players[targetId];
    const actualDamage = target.isDefending ? Math.floor(damage * 0.5) : damage;
    target.health = Math.max(0, target.health - actualDamage);
    
    // 添加受击特效
    state.effects.push({
      id: `hit-${Date.now()}`,
      type: 'HIT',
      position: { ...target.position },
      duration: 300,
      timer: 0,
      frame: 0,
    });
    
    // 检查游戏结束
    if (target.health === 0) {
      const winnerId = targetId === 'player1' ? 'player2' : 'player1';
      state.gameState.phase = 'RESULT';
      state.gameState.winner = winnerId;
      state.players[winnerId].score += 1;
    }
  })),
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => set(produce(state => {
    const player = state.players[playerId];
    player.animationTimer += deltaTime;
  })),
  
  setAnimationFrame: (playerId: string, frame: number) => set(produce(state => {
    state.players[playerId].animationFrame = frame;
  })),
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => {
    const id = `effect-${Date.now()}-${Math.random().toString(36).substr(2, 9)}`;
    set(produce(state => {
      state.effects.push({
        ...effect,
        id,
        timer: 0,
      });
    }));
    return id;
  },
  
  updateEffects: (deltaTime: number) => set(produce(state => {
    state.effects = state.effects.filter(effect => {
      effect.timer += deltaTime;
      return effect.timer < effect.duration;
    });
  })),
  
  removeEffect: (effectId: string) => set(produce(state => {
    state.effects = state.effects.filter(e => e.id !== effectId);
  })),
  
  clearEffects: () => set(produce(state => {
    state.effects = [];
  })),
  
  // 游戏时间
  tick: (deltaTime: number) =>import { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
  winner: null,
};

// 游戏Store接口
interface GameStore {
  // 状态
  gameState: GameState;
  players: Record<string, MechaData>;
  effects: Effect[];
  
  // 游戏流程控制
  startGame: () => void;
  pauseGame: () => void;
  resumeGame: () => void;
  endGame: (winner: string | null) => void;
  resetGame: () => void;
  nextRound: () => void;
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => void;
  updatePlayerState: (playerId: string, state: MechaData['state']) => void;
  updatePlayerFacing: (playerId: string, facingRight: boolean) => void;
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => void;
  updatePlayerDefending: (playerId: string, isDefending: boolean) => void;
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => void;
  dealDamage: (targetId: string, damage: number, attackerId?: string) => void;
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => void;
  setAnimationFrame: (playerId: string, frame: number) => void;
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => string;
  updateEffects: (deltaTime: number) => void;
  removeEffect: (effectId: string) => void;
  clearEffects: () => void;
  
  // 游戏时间
  tick: (deltaTime: number) => void;
}

// 创建Game Store
export const useGameStore = create<GameStore>((set, get) => ({
  // 初始状态
  gameState: initialGameState,
  players: {
    player1: createInitialMecha('player1', 'BLUE', 200),
    player2: createInitialMecha('player2', 'RED', 550),
  },
  effects: [],
  
  // 游戏流程控制
  startGame: () => set(produce(state => {
    state.gameState.phase = 'PLAYING';
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.winner = null;
  })),
  
  pauseGame: () => set(produce(state => {
    if (state.gameState.phase === 'PLAYING') {
      state.gameState.phase = 'PAUSED';
    }
  })),
  
  resumeGame: () => set(produce(state => {
    if (state.gameState.phase === 'PAUSED') {
      state.gameState.phase = 'PLAYING';
    }
  })),
  
  endGame: (winner: string | null) => set(produce(state => {
    state.gameState.phase = 'RESULT';
    state.gameState.winner = winner;
    if (winner && state.players[winner]) {
      state.players[winner].score += 1;
    }
  })),
  
  resetGame: () => set(produce(state => {
    state.gameState = initialGameState;
    state.players = {
      player1: createInitialMecha('player1', 'BLUE', 200),
      player2: createInitialMecha('player2', 'RED', 550),
    };
    state.effects = [];
  })),
  
  nextRound: () => set(produce(state => {
    state.gameState.round += 1;
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.phase = 'PLAYING';
    state.gameState.winner = null;
    
    // 重置玩家位置和状态
    Object.keys(state.players).forEach(playerId => {
      const player = state.players[playerId];
      player.health = player.maxHealth;
      player.position = playerId === 'player1' ? { x: 200, y: 400 } : { x: 550, y: 400 };
      player.velocity = { x: 0, y: 0 };
      player.state = 'IDLE';
      player.isDefending = false;
      player.isGrounded = false;
      player.attackCooldown = 0;
    });
    
    state.effects = [];
  })),
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => set(produce(state => {
    state.players[playerId].position = position;
    state.players[playerId].velocity = velocity;
  })),
  
  updatePlayerState: (playerId: string, mechaState: MechaData['state']) => set(produce(state => {
    state.players[playerId].state = mechaState;
  })),
  
  updatePlayerFacing: (playerId: string, facingRight: boolean) => set(produce(state => {
    state.players[playerId].facingRight = facingRight;
  })),
  
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => set(produce(state => {
    state.players[playerId].isGrounded = isGrounded;
  })),
  
  updatePlayerDefending: (playerId: string, isDefending: boolean) => set(produce(state => {
    state.players[playerId].isDefending = isDefending;
  })),
  
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => set(produce(state => {
    state.players[playerId].attackCooldown = cooldown;
  })),
  
  dealDamage: (targetId: string, damage: number) => set(produce(state => {
    const target = state.players[targetId];
    const actualDamage = target.isDefending ? Math.floor(damage * 0.5) : damage;
    target.health = Math.max(0, target.health - actualDamage);
    
    // 添加受击特效
    state.effects.push({
      id: `hit-${Date.now()}`,
      type: 'HIT',
      position: { ...target.position },
      duration: 300,
      timer: 0,
      frame: 0,
    });
    
    // 检查游戏结束
    if (target.health === 0) {
      const winnerId = targetId === 'player1' ? 'player2' : 'player1';
      state.gameState.phase = 'RESULT';
      state.gameState.winner = winnerId;
      state.players[winnerId].score += 1;
    }
  })),
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => set(produce(state => {
    const player = state.players[playerId];
    player.animationTimer += deltaTime;
  })),
  
  setAnimationFrame: (playerId: string, frame: number) => set(produce(state => {
    state.players[playerId].animationFrame = frame;
  })),
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => {
    const id = `effect-${Date.now()}-${Math.random().toString(36).substr(2, 9)}`;
    set(produce(state => {
      state.effects.push({
        ...effect,
        id,
        timer: 0,
      });
    }));
    return id;
  },
  
  updateEffects: (deltaTime: number) => set(produce(state => {
    state.effects = state.effects.filter(effect => {
      effect.timer += deltaTime;
      return effect.timer < effect.duration;
    });
  })),
  
  removeEffect: (effectId: string) => set(produce(state => {
    state.effects = state.effects.filter(e => e.id !== effectId);
  })),
  
  clearEffects: () => set(produce(state => {
    state.effects = [];
  })),
  
  // 游戏时间
  tick: (deltaTime: number) => set(produce(state => {
import { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
  winner: null,
};

// 游戏Store接口
interface GameStore {
  // 状态
  gameState: GameState;
  players: Record<string, MechaData>;
  effects: Effect[];
  
  // 游戏流程控制
  startGame: () => void;
  pauseGame: () => void;
  resumeGame: () => void;
  endGame: (winner: string | null) => void;
  resetGame: () => void;
  nextRound: () => void;
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => void;
  updatePlayerState: (playerId: string, state: MechaData['state']) => void;
  updatePlayerFacing: (playerId: string, facingRight: boolean) => void;
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => void;
  updatePlayerDefending: (playerId: string, isDefending: boolean) => void;
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => void;
  dealDamage: (targetId: string, damage: number, attackerId?: string) => void;
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => void;
  setAnimationFrame: (playerId: string, frame: number) => void;
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => string;
  updateEffects: (deltaTime: number) => void;
  removeEffect: (effectId: string) => void;
  clearEffects: () => void;
  
  // 游戏时间
  tick: (deltaTime: number) => void;
}

// 创建Game Store
export const useGameStore = create<GameStore>((set, get) => ({
  // 初始状态
  gameState: initialGameState,
  players: {
    player1: createInitialMecha('player1', 'BLUE', 200),
    player2: createInitialMecha('player2', 'RED', 550),
  },
  effects: [],
  
  // 游戏流程控制
  startGame: () => set(produce(state => {
    state.gameState.phase = 'PLAYING';
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.winner = null;
  })),
  
  pauseGame: () => set(produce(state => {
    if (state.gameState.phase === 'PLAYING') {
      state.gameState.phase = 'PAUSED';
    }
  })),
  
  resumeGame: () => set(produce(state => {
    if (state.gameState.phase === 'PAUSED') {
      state.gameState.phase = 'PLAYING';
    }
  })),
  
  endGame: (winner: string | null) => set(produce(state => {
    state.gameState.phase = 'RESULT';
    state.gameState.winner = winner;
    if (winner && state.players[winner]) {
      state.players[winner].score += 1;
    }
  })),
  
  resetGame: () => set(produce(state => {
    state.gameState = initialGameState;
    state.players = {
      player1: createInitialMecha('player1', 'BLUE', 200),
      player2: createInitialMecha('player2', 'RED', 550),
    };
    state.effects = [];
  })),
  
  nextRound: () => set(produce(state => {
    state.gameState.round += 1;
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.phase = 'PLAYING';
    state.gameState.winner = null;
    
    // 重置玩家位置和状态
    Object.keys(state.players).forEach(playerId => {
      const player = state.players[playerId];
      player.health = player.maxHealth;
      player.position = playerId === 'player1' ? { x: 200, y: 400 } : { x: 550, y: 400 };
      player.velocity = { x: 0, y: 0 };
      player.state = 'IDLE';
      player.isDefending = false;
      player.isGrounded = false;
      player.attackCooldown = 0;
    });
    
    state.effects = [];
  })),
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => set(produce(state => {
    state.players[playerId].position = position;
    state.players[playerId].velocity = velocity;
  })),
  
  updatePlayerState: (playerId: string, mechaState: MechaData['state']) => set(produce(state => {
    state.players[playerId].state = mechaState;
  })),
  
  updatePlayerFacing: (playerId: string, facingRight: boolean) => set(produce(state => {
    state.players[playerId].facingRight = facingRight;
  })),
  
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => set(produce(state => {
    state.players[playerId].isGrounded = isGrounded;
  })),
  
  updatePlayerDefending: (playerId: string, isDefending: boolean) => set(produce(state => {
    state.players[playerId].isDefending = isDefending;
  })),
  
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => set(produce(state => {
    state.players[playerId].attackCooldown = cooldown;
  })),
  
  dealDamage: (targetId: string, damage: number) => set(produce(state => {
    const target = state.players[targetId];
    const actualDamage = target.isDefending ? Math.floor(damage * 0.5) : damage;
    target.health = Math.max(0, target.health - actualDamage);
    
    // 添加受击特效
    state.effects.push({
      id: `hit-${Date.now()}`,
      type: 'HIT',
      position: { ...target.position },
      duration: 300,
      timer: 0,
      frame: 0,
    });
    
    // 检查游戏结束
    if (target.health === 0) {
      const winnerId = targetId === 'player1' ? 'player2' : 'player1';
      state.gameState.phase = 'RESULT';
      state.gameState.winner = winnerId;
      state.players[winnerId].score += 1;
    }
  })),
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => set(produce(state => {
    const player = state.players[playerId];
    player.animationTimer += deltaTime;
  })),
  
  setAnimationFrame: (playerId: string, frame: number) => set(produce(state => {
    state.players[playerId].animationFrame = frame;
  })),
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => {
    const id = `effect-${Date.now()}-${Math.random().toString(36).substr(2, 9)}`;
    set(produce(state => {
      state.effects.push({
        ...effect,
        id,
        timer: 0,
      });
    }));
    return id;
  },
  
  updateEffects: (deltaTime: number) => set(produce(state => {
    state.effects = state.effects.filter(effect => {
      effect.timer += deltaTime;
      return effect.timer < effect.duration;
    });
  })),
  
  removeEffect: (effectId: string) => set(produce(state => {
    state.effects = state.effects.filter(e => e.id !== effectId);
  })),
  
  clearEffects: () => set(produce(state => {
    state.effects = [];
  })),
  
  // 游戏时间
  tick: (deltaTime: number) => set(produce(state => {
    if (state.gameState.phaseimport { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
  winner: null,
};

// 游戏Store接口
interface GameStore {
  // 状态
  gameState: GameState;
  players: Record<string, MechaData>;
  effects: Effect[];
  
  // 游戏流程控制
  startGame: () => void;
  pauseGame: () => void;
  resumeGame: () => void;
  endGame: (winner: string | null) => void;
  resetGame: () => void;
  nextRound: () => void;
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => void;
  updatePlayerState: (playerId: string, state: MechaData['state']) => void;
  updatePlayerFacing: (playerId: string, facingRight: boolean) => void;
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => void;
  updatePlayerDefending: (playerId: string, isDefending: boolean) => void;
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => void;
  dealDamage: (targetId: string, damage: number, attackerId?: string) => void;
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => void;
  setAnimationFrame: (playerId: string, frame: number) => void;
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => string;
  updateEffects: (deltaTime: number) => void;
  removeEffect: (effectId: string) => void;
  clearEffects: () => void;
  
  // 游戏时间
  tick: (deltaTime: number) => void;
}

// 创建Game Store
export const useGameStore = create<GameStore>((set, get) => ({
  // 初始状态
  gameState: initialGameState,
  players: {
    player1: createInitialMecha('player1', 'BLUE', 200),
    player2: createInitialMecha('player2', 'RED', 550),
  },
  effects: [],
  
  // 游戏流程控制
  startGame: () => set(produce(state => {
    state.gameState.phase = 'PLAYING';
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.winner = null;
  })),
  
  pauseGame: () => set(produce(state => {
    if (state.gameState.phase === 'PLAYING') {
      state.gameState.phase = 'PAUSED';
    }
  })),
  
  resumeGame: () => set(produce(state => {
    if (state.gameState.phase === 'PAUSED') {
      state.gameState.phase = 'PLAYING';
    }
  })),
  
  endGame: (winner: string | null) => set(produce(state => {
    state.gameState.phase = 'RESULT';
    state.gameState.winner = winner;
    if (winner && state.players[winner]) {
      state.players[winner].score += 1;
    }
  })),
  
  resetGame: () => set(produce(state => {
    state.gameState = initialGameState;
    state.players = {
      player1: createInitialMecha('player1', 'BLUE', 200),
      player2: createInitialMecha('player2', 'RED', 550),
    };
    state.effects = [];
  })),
  
  nextRound: () => set(produce(state => {
    state.gameState.round += 1;
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.phase = 'PLAYING';
    state.gameState.winner = null;
    
    // 重置玩家位置和状态
    Object.keys(state.players).forEach(playerId => {
      const player = state.players[playerId];
      player.health = player.maxHealth;
      player.position = playerId === 'player1' ? { x: 200, y: 400 } : { x: 550, y: 400 };
      player.velocity = { x: 0, y: 0 };
      player.state = 'IDLE';
      player.isDefending = false;
      player.isGrounded = false;
      player.attackCooldown = 0;
    });
    
    state.effects = [];
  })),
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => set(produce(state => {
    state.players[playerId].position = position;
    state.players[playerId].velocity = velocity;
  })),
  
  updatePlayerState: (playerId: string, mechaState: MechaData['state']) => set(produce(state => {
    state.players[playerId].state = mechaState;
  })),
  
  updatePlayerFacing: (playerId: string, facingRight: boolean) => set(produce(state => {
    state.players[playerId].facingRight = facingRight;
  })),
  
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => set(produce(state => {
    state.players[playerId].isGrounded = isGrounded;
  })),
  
  updatePlayerDefending: (playerId: string, isDefending: boolean) => set(produce(state => {
    state.players[playerId].isDefending = isDefending;
  })),
  
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => set(produce(state => {
    state.players[playerId].attackCooldown = cooldown;
  })),
  
  dealDamage: (targetId: string, damage: number) => set(produce(state => {
    const target = state.players[targetId];
    const actualDamage = target.isDefending ? Math.floor(damage * 0.5) : damage;
    target.health = Math.max(0, target.health - actualDamage);
    
    // 添加受击特效
    state.effects.push({
      id: `hit-${Date.now()}`,
      type: 'HIT',
      position: { ...target.position },
      duration: 300,
      timer: 0,
      frame: 0,
    });
    
    // 检查游戏结束
    if (target.health === 0) {
      const winnerId = targetId === 'player1' ? 'player2' : 'player1';
      state.gameState.phase = 'RESULT';
      state.gameState.winner = winnerId;
      state.players[winnerId].score += 1;
    }
  })),
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => set(produce(state => {
    const player = state.players[playerId];
    player.animationTimer += deltaTime;
  })),
  
  setAnimationFrame: (playerId: string, frame: number) => set(produce(state => {
    state.players[playerId].animationFrame = frame;
  })),
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => {
    const id = `effect-${Date.now()}-${Math.random().toString(36).substr(2, 9)}`;
    set(produce(state => {
      state.effects.push({
        ...effect,
        id,
        timer: 0,
      });
    }));
    return id;
  },
  
  updateEffects: (deltaTime: number) => set(produce(state => {
    state.effects = state.effects.filter(effect => {
      effect.timer += deltaTime;
      return effect.timer < effect.duration;
    });
  })),
  
  removeEffect: (effectId: string) => set(produce(state => {
    state.effects = state.effects.filter(e => e.id !== effectId);
  })),
  
  clearEffects: () => set(produce(state => {
    state.effects = [];
  })),
  
  // 游戏时间
  tick: (deltaTime: number) => set(produce(state => {
    if (state.gameState.phase === 'PLAYING') {
import { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
  winner: null,
};

// 游戏Store接口
interface GameStore {
  // 状态
  gameState: GameState;
  players: Record<string, MechaData>;
  effects: Effect[];
  
  // 游戏流程控制
  startGame: () => void;
  pauseGame: () => void;
  resumeGame: () => void;
  endGame: (winner: string | null) => void;
  resetGame: () => void;
  nextRound: () => void;
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => void;
  updatePlayerState: (playerId: string, state: MechaData['state']) => void;
  updatePlayerFacing: (playerId: string, facingRight: boolean) => void;
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => void;
  updatePlayerDefending: (playerId: string, isDefending: boolean) => void;
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => void;
  dealDamage: (targetId: string, damage: number, attackerId?: string) => void;
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => void;
  setAnimationFrame: (playerId: string, frame: number) => void;
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => string;
  updateEffects: (deltaTime: number) => void;
  removeEffect: (effectId: string) => void;
  clearEffects: () => void;
  
  // 游戏时间
  tick: (deltaTime: number) => void;
}

// 创建Game Store
export const useGameStore = create<GameStore>((set, get) => ({
  // 初始状态
  gameState: initialGameState,
  players: {
    player1: createInitialMecha('player1', 'BLUE', 200),
    player2: createInitialMecha('player2', 'RED', 550),
  },
  effects: [],
  
  // 游戏流程控制
  startGame: () => set(produce(state => {
    state.gameState.phase = 'PLAYING';
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.winner = null;
  })),
  
  pauseGame: () => set(produce(state => {
    if (state.gameState.phase === 'PLAYING') {
      state.gameState.phase = 'PAUSED';
    }
  })),
  
  resumeGame: () => set(produce(state => {
    if (state.gameState.phase === 'PAUSED') {
      state.gameState.phase = 'PLAYING';
    }
  })),
  
  endGame: (winner: string | null) => set(produce(state => {
    state.gameState.phase = 'RESULT';
    state.gameState.winner = winner;
    if (winner && state.players[winner]) {
      state.players[winner].score += 1;
    }
  })),
  
  resetGame: () => set(produce(state => {
    state.gameState = initialGameState;
    state.players = {
      player1: createInitialMecha('player1', 'BLUE', 200),
      player2: createInitialMecha('player2', 'RED', 550),
    };
    state.effects = [];
  })),
  
  nextRound: () => set(produce(state => {
    state.gameState.round += 1;
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.phase = 'PLAYING';
    state.gameState.winner = null;
    
    // 重置玩家位置和状态
    Object.keys(state.players).forEach(playerId => {
      const player = state.players[playerId];
      player.health = player.maxHealth;
      player.position = playerId === 'player1' ? { x: 200, y: 400 } : { x: 550, y: 400 };
      player.velocity = { x: 0, y: 0 };
      player.state = 'IDLE';
      player.isDefending = false;
      player.isGrounded = false;
      player.attackCooldown = 0;
    });
    
    state.effects = [];
  })),
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => set(produce(state => {
    state.players[playerId].position = position;
    state.players[playerId].velocity = velocity;
  })),
  
  updatePlayerState: (playerId: string, mechaState: MechaData['state']) => set(produce(state => {
    state.players[playerId].state = mechaState;
  })),
  
  updatePlayerFacing: (playerId: string, facingRight: boolean) => set(produce(state => {
    state.players[playerId].facingRight = facingRight;
  })),
  
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => set(produce(state => {
    state.players[playerId].isGrounded = isGrounded;
  })),
  
  updatePlayerDefending: (playerId: string, isDefending: boolean) => set(produce(state => {
    state.players[playerId].isDefending = isDefending;
  })),
  
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => set(produce(state => {
    state.players[playerId].attackCooldown = cooldown;
  })),
  
  dealDamage: (targetId: string, damage: number) => set(produce(state => {
    const target = state.players[targetId];
    const actualDamage = target.isDefending ? Math.floor(damage * 0.5) : damage;
    target.health = Math.max(0, target.health - actualDamage);
    
    // 添加受击特效
    state.effects.push({
      id: `hit-${Date.now()}`,
      type: 'HIT',
      position: { ...target.position },
      duration: 300,
      timer: 0,
      frame: 0,
    });
    
    // 检查游戏结束
    if (target.health === 0) {
      const winnerId = targetId === 'player1' ? 'player2' : 'player1';
      state.gameState.phase = 'RESULT';
      state.gameState.winner = winnerId;
      state.players[winnerId].score += 1;
    }
  })),
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => set(produce(state => {
    const player = state.players[playerId];
    player.animationTimer += deltaTime;
  })),
  
  setAnimationFrame: (playerId: string, frame: number) => set(produce(state => {
    state.players[playerId].animationFrame = frame;
  })),
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => {
    const id = `effect-${Date.now()}-${Math.random().toString(36).substr(2, 9)}`;
    set(produce(state => {
      state.effects.push({
        ...effect,
        id,
        timer: 0,
      });
    }));
    return id;
  },
  
  updateEffects: (deltaTime: number) => set(produce(state => {
    state.effects = state.effects.filter(effect => {
      effect.timer += deltaTime;
      return effect.timer < effect.duration;
    });
  })),
  
  removeEffect: (effectId: string) => set(produce(state => {
    state.effects = state.effects.filter(e => e.id !== effectId);
  })),
  
  clearEffects: () => set(produce(state => {
    state.effects = [];
  })),
  
  // 游戏时间
  tick: (deltaTime: number) => set(produce(state => {
    if (state.gameState.phase === 'PLAYING') {
      state.gameState.timeRemaining =import { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
  winner: null,
};

// 游戏Store接口
interface GameStore {
  // 状态
  gameState: GameState;
  players: Record<string, MechaData>;
  effects: Effect[];
  
  // 游戏流程控制
  startGame: () => void;
  pauseGame: () => void;
  resumeGame: () => void;
  endGame: (winner: string | null) => void;
  resetGame: () => void;
  nextRound: () => void;
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => void;
  updatePlayerState: (playerId: string, state: MechaData['state']) => void;
  updatePlayerFacing: (playerId: string, facingRight: boolean) => void;
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => void;
  updatePlayerDefending: (playerId: string, isDefending: boolean) => void;
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => void;
  dealDamage: (targetId: string, damage: number, attackerId?: string) => void;
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => void;
  setAnimationFrame: (playerId: string, frame: number) => void;
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => string;
  updateEffects: (deltaTime: number) => void;
  removeEffect: (effectId: string) => void;
  clearEffects: () => void;
  
  // 游戏时间
  tick: (deltaTime: number) => void;
}

// 创建Game Store
export const useGameStore = create<GameStore>((set, get) => ({
  // 初始状态
  gameState: initialGameState,
  players: {
    player1: createInitialMecha('player1', 'BLUE', 200),
    player2: createInitialMecha('player2', 'RED', 550),
  },
  effects: [],
  
  // 游戏流程控制
  startGame: () => set(produce(state => {
    state.gameState.phase = 'PLAYING';
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.winner = null;
  })),
  
  pauseGame: () => set(produce(state => {
    if (state.gameState.phase === 'PLAYING') {
      state.gameState.phase = 'PAUSED';
    }
  })),
  
  resumeGame: () => set(produce(state => {
    if (state.gameState.phase === 'PAUSED') {
      state.gameState.phase = 'PLAYING';
    }
  })),
  
  endGame: (winner: string | null) => set(produce(state => {
    state.gameState.phase = 'RESULT';
    state.gameState.winner = winner;
    if (winner && state.players[winner]) {
      state.players[winner].score += 1;
    }
  })),
  
  resetGame: () => set(produce(state => {
    state.gameState = initialGameState;
    state.players = {
      player1: createInitialMecha('player1', 'BLUE', 200),
      player2: createInitialMecha('player2', 'RED', 550),
    };
    state.effects = [];
  })),
  
  nextRound: () => set(produce(state => {
    state.gameState.round += 1;
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.phase = 'PLAYING';
    state.gameState.winner = null;
    
    // 重置玩家位置和状态
    Object.keys(state.players).forEach(playerId => {
      const player = state.players[playerId];
      player.health = player.maxHealth;
      player.position = playerId === 'player1' ? { x: 200, y: 400 } : { x: 550, y: 400 };
      player.velocity = { x: 0, y: 0 };
      player.state = 'IDLE';
      player.isDefending = false;
      player.isGrounded = false;
      player.attackCooldown = 0;
    });
    
    state.effects = [];
  })),
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => set(produce(state => {
    state.players[playerId].position = position;
    state.players[playerId].velocity = velocity;
  })),
  
  updatePlayerState: (playerId: string, mechaState: MechaData['state']) => set(produce(state => {
    state.players[playerId].state = mechaState;
  })),
  
  updatePlayerFacing: (playerId: string, facingRight: boolean) => set(produce(state => {
    state.players[playerId].facingRight = facingRight;
  })),
  
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => set(produce(state => {
    state.players[playerId].isGrounded = isGrounded;
  })),
  
  updatePlayerDefending: (playerId: string, isDefending: boolean) => set(produce(state => {
    state.players[playerId].isDefending = isDefending;
  })),
  
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => set(produce(state => {
    state.players[playerId].attackCooldown = cooldown;
  })),
  
  dealDamage: (targetId: string, damage: number) => set(produce(state => {
    const target = state.players[targetId];
    const actualDamage = target.isDefending ? Math.floor(damage * 0.5) : damage;
    target.health = Math.max(0, target.health - actualDamage);
    
    // 添加受击特效
    state.effects.push({
      id: `hit-${Date.now()}`,
      type: 'HIT',
      position: { ...target.position },
      duration: 300,
      timer: 0,
      frame: 0,
    });
    
    // 检查游戏结束
    if (target.health === 0) {
      const winnerId = targetId === 'player1' ? 'player2' : 'player1';
      state.gameState.phase = 'RESULT';
      state.gameState.winner = winnerId;
      state.players[winnerId].score += 1;
    }
  })),
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => set(produce(state => {
    const player = state.players[playerId];
    player.animationTimer += deltaTime;
  })),
  
  setAnimationFrame: (playerId: string, frame: number) => set(produce(state => {
    state.players[playerId].animationFrame = frame;
  })),
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => {
    const id = `effect-${Date.now()}-${Math.random().toString(36).substr(2, 9)}`;
    set(produce(state => {
      state.effects.push({
        ...effect,
        id,
        timer: 0,
      });
    }));
    return id;
  },
  
  updateEffects: (deltaTime: number) => set(produce(state => {
    state.effects = state.effects.filter(effect => {
      effect.timer += deltaTime;
      return effect.timer < effect.duration;
    });
  })),
  
  removeEffect: (effectId: string) => set(produce(state => {
    state.effects = state.effects.filter(e => e.id !== effectId);
  })),
  
  clearEffects: () => set(produce(state => {
    state.effects = [];
  })),
  
  // 游戏时间
  tick: (deltaTime: number) => set(produce(state => {
    if (state.gameState.phase === 'PLAYING') {
      state.gameState.timeRemaining = Math.max(0, state.gameimport { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
  winner: null,
};

// 游戏Store接口
interface GameStore {
  // 状态
  gameState: GameState;
  players: Record<string, MechaData>;
  effects: Effect[];
  
  // 游戏流程控制
  startGame: () => void;
  pauseGame: () => void;
  resumeGame: () => void;
  endGame: (winner: string | null) => void;
  resetGame: () => void;
  nextRound: () => void;
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => void;
  updatePlayerState: (playerId: string, state: MechaData['state']) => void;
  updatePlayerFacing: (playerId: string, facingRight: boolean) => void;
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => void;
  updatePlayerDefending: (playerId: string, isDefending: boolean) => void;
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => void;
  dealDamage: (targetId: string, damage: number, attackerId?: string) => void;
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => void;
  setAnimationFrame: (playerId: string, frame: number) => void;
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => string;
  updateEffects: (deltaTime: number) => void;
  removeEffect: (effectId: string) => void;
  clearEffects: () => void;
  
  // 游戏时间
  tick: (deltaTime: number) => void;
}

// 创建Game Store
export const useGameStore = create<GameStore>((set, get) => ({
  // 初始状态
  gameState: initialGameState,
  players: {
    player1: createInitialMecha('player1', 'BLUE', 200),
    player2: createInitialMecha('player2', 'RED', 550),
  },
  effects: [],
  
  // 游戏流程控制
  startGame: () => set(produce(state => {
    state.gameState.phase = 'PLAYING';
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.winner = null;
  })),
  
  pauseGame: () => set(produce(state => {
    if (state.gameState.phase === 'PLAYING') {
      state.gameState.phase = 'PAUSED';
    }
  })),
  
  resumeGame: () => set(produce(state => {
    if (state.gameState.phase === 'PAUSED') {
      state.gameState.phase = 'PLAYING';
    }
  })),
  
  endGame: (winner: string | null) => set(produce(state => {
    state.gameState.phase = 'RESULT';
    state.gameState.winner = winner;
    if (winner && state.players[winner]) {
      state.players[winner].score += 1;
    }
  })),
  
  resetGame: () => set(produce(state => {
    state.gameState = initialGameState;
    state.players = {
      player1: createInitialMecha('player1', 'BLUE', 200),
      player2: createInitialMecha('player2', 'RED', 550),
    };
    state.effects = [];
  })),
  
  nextRound: () => set(produce(state => {
    state.gameState.round += 1;
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.phase = 'PLAYING';
    state.gameState.winner = null;
    
    // 重置玩家位置和状态
    Object.keys(state.players).forEach(playerId => {
      const player = state.players[playerId];
      player.health = player.maxHealth;
      player.position = playerId === 'player1' ? { x: 200, y: 400 } : { x: 550, y: 400 };
      player.velocity = { x: 0, y: 0 };
      player.state = 'IDLE';
      player.isDefending = false;
      player.isGrounded = false;
      player.attackCooldown = 0;
    });
    
    state.effects = [];
  })),
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => set(produce(state => {
    state.players[playerId].position = position;
    state.players[playerId].velocity = velocity;
  })),
  
  updatePlayerState: (playerId: string, mechaState: MechaData['state']) => set(produce(state => {
    state.players[playerId].state = mechaState;
  })),
  
  updatePlayerFacing: (playerId: string, facingRight: boolean) => set(produce(state => {
    state.players[playerId].facingRight = facingRight;
  })),
  
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => set(produce(state => {
    state.players[playerId].isGrounded = isGrounded;
  })),
  
  updatePlayerDefending: (playerId: string, isDefending: boolean) => set(produce(state => {
    state.players[playerId].isDefending = isDefending;
  })),
  
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => set(produce(state => {
    state.players[playerId].attackCooldown = cooldown;
  })),
  
  dealDamage: (targetId: string, damage: number) => set(produce(state => {
    const target = state.players[targetId];
    const actualDamage = target.isDefending ? Math.floor(damage * 0.5) : damage;
    target.health = Math.max(0, target.health - actualDamage);
    
    // 添加受击特效
    state.effects.push({
      id: `hit-${Date.now()}`,
      type: 'HIT',
      position: { ...target.position },
      duration: 300,
      timer: 0,
      frame: 0,
    });
    
    // 检查游戏结束
    if (target.health === 0) {
      const winnerId = targetId === 'player1' ? 'player2' : 'player1';
      state.gameState.phase = 'RESULT';
      state.gameState.winner = winnerId;
      state.players[winnerId].score += 1;
    }
  })),
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => set(produce(state => {
    const player = state.players[playerId];
    player.animationTimer += deltaTime;
  })),
  
  setAnimationFrame: (playerId: string, frame: number) => set(produce(state => {
    state.players[playerId].animationFrame = frame;
  })),
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => {
    const id = `effect-${Date.now()}-${Math.random().toString(36).substr(2, 9)}`;
    set(produce(state => {
      state.effects.push({
        ...effect,
        id,
        timer: 0,
      });
    }));
    return id;
  },
  
  updateEffects: (deltaTime: number) => set(produce(state => {
    state.effects = state.effects.filter(effect => {
      effect.timer += deltaTime;
      return effect.timer < effect.duration;
    });
  })),
  
  removeEffect: (effectId: string) => set(produce(state => {
    state.effects = state.effects.filter(e => e.id !== effectId);
  })),
  
  clearEffects: () => set(produce(state => {
    state.effects = [];
  })),
  
  // 游戏时间
  tick: (deltaTime: number) => set(produce(state => {
    if (state.gameState.phase === 'PLAYING') {
      state.gameState.timeRemaining = Math.max(0, state.gameState.timeRemaining - deltaTime /import { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
  winner: null,
};

// 游戏Store接口
interface GameStore {
  // 状态
  gameState: GameState;
  players: Record<string, MechaData>;
  effects: Effect[];
  
  // 游戏流程控制
  startGame: () => void;
  pauseGame: () => void;
  resumeGame: () => void;
  endGame: (winner: string | null) => void;
  resetGame: () => void;
  nextRound: () => void;
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => void;
  updatePlayerState: (playerId: string, state: MechaData['state']) => void;
  updatePlayerFacing: (playerId: string, facingRight: boolean) => void;
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => void;
  updatePlayerDefending: (playerId: string, isDefending: boolean) => void;
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => void;
  dealDamage: (targetId: string, damage: number, attackerId?: string) => void;
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => void;
  setAnimationFrame: (playerId: string, frame: number) => void;
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => string;
  updateEffects: (deltaTime: number) => void;
  removeEffect: (effectId: string) => void;
  clearEffects: () => void;
  
  // 游戏时间
  tick: (deltaTime: number) => void;
}

// 创建Game Store
export const useGameStore = create<GameStore>((set, get) => ({
  // 初始状态
  gameState: initialGameState,
  players: {
    player1: createInitialMecha('player1', 'BLUE', 200),
    player2: createInitialMecha('player2', 'RED', 550),
  },
  effects: [],
  
  // 游戏流程控制
  startGame: () => set(produce(state => {
    state.gameState.phase = 'PLAYING';
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.winner = null;
  })),
  
  pauseGame: () => set(produce(state => {
    if (state.gameState.phase === 'PLAYING') {
      state.gameState.phase = 'PAUSED';
    }
  })),
  
  resumeGame: () => set(produce(state => {
    if (state.gameState.phase === 'PAUSED') {
      state.gameState.phase = 'PLAYING';
    }
  })),
  
  endGame: (winner: string | null) => set(produce(state => {
    state.gameState.phase = 'RESULT';
    state.gameState.winner = winner;
    if (winner && state.players[winner]) {
      state.players[winner].score += 1;
    }
  })),
  
  resetGame: () => set(produce(state => {
    state.gameState = initialGameState;
    state.players = {
      player1: createInitialMecha('player1', 'BLUE', 200),
      player2: createInitialMecha('player2', 'RED', 550),
    };
    state.effects = [];
  })),
  
  nextRound: () => set(produce(state => {
    state.gameState.round += 1;
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.phase = 'PLAYING';
    state.gameState.winner = null;
    
    // 重置玩家位置和状态
    Object.keys(state.players).forEach(playerId => {
      const player = state.players[playerId];
      player.health = player.maxHealth;
      player.position = playerId === 'player1' ? { x: 200, y: 400 } : { x: 550, y: 400 };
      player.velocity = { x: 0, y: 0 };
      player.state = 'IDLE';
      player.isDefending = false;
      player.isGrounded = false;
      player.attackCooldown = 0;
    });
    
    state.effects = [];
  })),
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => set(produce(state => {
    state.players[playerId].position = position;
    state.players[playerId].velocity = velocity;
  })),
  
  updatePlayerState: (playerId: string, mechaState: MechaData['state']) => set(produce(state => {
    state.players[playerId].state = mechaState;
  })),
  
  updatePlayerFacing: (playerId: string, facingRight: boolean) => set(produce(state => {
    state.players[playerId].facingRight = facingRight;
  })),
  
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => set(produce(state => {
    state.players[playerId].isGrounded = isGrounded;
  })),
  
  updatePlayerDefending: (playerId: string, isDefending: boolean) => set(produce(state => {
    state.players[playerId].isDefending = isDefending;
  })),
  
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => set(produce(state => {
    state.players[playerId].attackCooldown = cooldown;
  })),
  
  dealDamage: (targetId: string, damage: number) => set(produce(state => {
    const target = state.players[targetId];
    const actualDamage = target.isDefending ? Math.floor(damage * 0.5) : damage;
    target.health = Math.max(0, target.health - actualDamage);
    
    // 添加受击特效
    state.effects.push({
      id: `hit-${Date.now()}`,
      type: 'HIT',
      position: { ...target.position },
      duration: 300,
      timer: 0,
      frame: 0,
    });
    
    // 检查游戏结束
    if (target.health === 0) {
      const winnerId = targetId === 'player1' ? 'player2' : 'player1';
      state.gameState.phase = 'RESULT';
      state.gameState.winner = winnerId;
      state.players[winnerId].score += 1;
    }
  })),
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => set(produce(state => {
    const player = state.players[playerId];
    player.animationTimer += deltaTime;
  })),
  
  setAnimationFrame: (playerId: string, frame: number) => set(produce(state => {
    state.players[playerId].animationFrame = frame;
  })),
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => {
    const id = `effect-${Date.now()}-${Math.random().toString(36).substr(2, 9)}`;
    set(produce(state => {
      state.effects.push({
        ...effect,
        id,
        timer: 0,
      });
    }));
    return id;
  },
  
  updateEffects: (deltaTime: number) => set(produce(state => {
    state.effects = state.effects.filter(effect => {
      effect.timer += deltaTime;
      return effect.timer < effect.duration;
    });
  })),
  
  removeEffect: (effectId: string) => set(produce(state => {
    state.effects = state.effects.filter(e => e.id !== effectId);
  })),
  
  clearEffects: () => set(produce(state => {
    state.effects = [];
  })),
  
  // 游戏时间
  tick: (deltaTime: number) => set(produce(state => {
    if (state.gameState.phase === 'PLAYING') {
      state.gameState.timeRemaining = Math.max(0, state.gameState.timeRemaining - deltaTime / 1000);
      
import { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
  winner: null,
};

// 游戏Store接口
interface GameStore {
  // 状态
  gameState: GameState;
  players: Record<string, MechaData>;
  effects: Effect[];
  
  // 游戏流程控制
  startGame: () => void;
  pauseGame: () => void;
  resumeGame: () => void;
  endGame: (winner: string | null) => void;
  resetGame: () => void;
  nextRound: () => void;
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => void;
  updatePlayerState: (playerId: string, state: MechaData['state']) => void;
  updatePlayerFacing: (playerId: string, facingRight: boolean) => void;
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => void;
  updatePlayerDefending: (playerId: string, isDefending: boolean) => void;
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => void;
  dealDamage: (targetId: string, damage: number, attackerId?: string) => void;
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => void;
  setAnimationFrame: (playerId: string, frame: number) => void;
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => string;
  updateEffects: (deltaTime: number) => void;
  removeEffect: (effectId: string) => void;
  clearEffects: () => void;
  
  // 游戏时间
  tick: (deltaTime: number) => void;
}

// 创建Game Store
export const useGameStore = create<GameStore>((set, get) => ({
  // 初始状态
  gameState: initialGameState,
  players: {
    player1: createInitialMecha('player1', 'BLUE', 200),
    player2: createInitialMecha('player2', 'RED', 550),
  },
  effects: [],
  
  // 游戏流程控制
  startGame: () => set(produce(state => {
    state.gameState.phase = 'PLAYING';
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.winner = null;
  })),
  
  pauseGame: () => set(produce(state => {
    if (state.gameState.phase === 'PLAYING') {
      state.gameState.phase = 'PAUSED';
    }
  })),
  
  resumeGame: () => set(produce(state => {
    if (state.gameState.phase === 'PAUSED') {
      state.gameState.phase = 'PLAYING';
    }
  })),
  
  endGame: (winner: string | null) => set(produce(state => {
    state.gameState.phase = 'RESULT';
    state.gameState.winner = winner;
    if (winner && state.players[winner]) {
      state.players[winner].score += 1;
    }
  })),
  
  resetGame: () => set(produce(state => {
    state.gameState = initialGameState;
    state.players = {
      player1: createInitialMecha('player1', 'BLUE', 200),
      player2: createInitialMecha('player2', 'RED', 550),
    };
    state.effects = [];
  })),
  
  nextRound: () => set(produce(state => {
    state.gameState.round += 1;
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.phase = 'PLAYING';
    state.gameState.winner = null;
    
    // 重置玩家位置和状态
    Object.keys(state.players).forEach(playerId => {
      const player = state.players[playerId];
      player.health = player.maxHealth;
      player.position = playerId === 'player1' ? { x: 200, y: 400 } : { x: 550, y: 400 };
      player.velocity = { x: 0, y: 0 };
      player.state = 'IDLE';
      player.isDefending = false;
      player.isGrounded = false;
      player.attackCooldown = 0;
    });
    
    state.effects = [];
  })),
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => set(produce(state => {
    state.players[playerId].position = position;
    state.players[playerId].velocity = velocity;
  })),
  
  updatePlayerState: (playerId: string, mechaState: MechaData['state']) => set(produce(state => {
    state.players[playerId].state = mechaState;
  })),
  
  updatePlayerFacing: (playerId: string, facingRight: boolean) => set(produce(state => {
    state.players[playerId].facingRight = facingRight;
  })),
  
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => set(produce(state => {
    state.players[playerId].isGrounded = isGrounded;
  })),
  
  updatePlayerDefending: (playerId: string, isDefending: boolean) => set(produce(state => {
    state.players[playerId].isDefending = isDefending;
  })),
  
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => set(produce(state => {
    state.players[playerId].attackCooldown = cooldown;
  })),
  
  dealDamage: (targetId: string, damage: number) => set(produce(state => {
    const target = state.players[targetId];
    const actualDamage = target.isDefending ? Math.floor(damage * 0.5) : damage;
    target.health = Math.max(0, target.health - actualDamage);
    
    // 添加受击特效
    state.effects.push({
      id: `hit-${Date.now()}`,
      type: 'HIT',
      position: { ...target.position },
      duration: 300,
      timer: 0,
      frame: 0,
    });
    
    // 检查游戏结束
    if (target.health === 0) {
      const winnerId = targetId === 'player1' ? 'player2' : 'player1';
      state.gameState.phase = 'RESULT';
      state.gameState.winner = winnerId;
      state.players[winnerId].score += 1;
    }
  })),
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => set(produce(state => {
    const player = state.players[playerId];
    player.animationTimer += deltaTime;
  })),
  
  setAnimationFrame: (playerId: string, frame: number) => set(produce(state => {
    state.players[playerId].animationFrame = frame;
  })),
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => {
    const id = `effect-${Date.now()}-${Math.random().toString(36).substr(2, 9)}`;
    set(produce(state => {
      state.effects.push({
        ...effect,
        id,
        timer: 0,
      });
    }));
    return id;
  },
  
  updateEffects: (deltaTime: number) => set(produce(state => {
    state.effects = state.effects.filter(effect => {
      effect.timer += deltaTime;
      return effect.timer < effect.duration;
    });
  })),
  
  removeEffect: (effectId: string) => set(produce(state => {
    state.effects = state.effects.filter(e => e.id !== effectId);
  })),
  
  clearEffects: () => set(produce(state => {
    state.effects = [];
  })),
  
  // 游戏时间
  tick: (deltaTime: number) => set(produce(state => {
    if (state.gameState.phase === 'PLAYING') {
      state.gameState.timeRemaining = Math.max(0, state.gameState.timeRemaining - deltaTime / 1000);
      
      // 时间到，判定import { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
  winner: null,
};

// 游戏Store接口
interface GameStore {
  // 状态
  gameState: GameState;
  players: Record<string, MechaData>;
  effects: Effect[];
  
  // 游戏流程控制
  startGame: () => void;
  pauseGame: () => void;
  resumeGame: () => void;
  endGame: (winner: string | null) => void;
  resetGame: () => void;
  nextRound: () => void;
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => void;
  updatePlayerState: (playerId: string, state: MechaData['state']) => void;
  updatePlayerFacing: (playerId: string, facingRight: boolean) => void;
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => void;
  updatePlayerDefending: (playerId: string, isDefending: boolean) => void;
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => void;
  dealDamage: (targetId: string, damage: number, attackerId?: string) => void;
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => void;
  setAnimationFrame: (playerId: string, frame: number) => void;
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => string;
  updateEffects: (deltaTime: number) => void;
  removeEffect: (effectId: string) => void;
  clearEffects: () => void;
  
  // 游戏时间
  tick: (deltaTime: number) => void;
}

// 创建Game Store
export const useGameStore = create<GameStore>((set, get) => ({
  // 初始状态
  gameState: initialGameState,
  players: {
    player1: createInitialMecha('player1', 'BLUE', 200),
    player2: createInitialMecha('player2', 'RED', 550),
  },
  effects: [],
  
  // 游戏流程控制
  startGame: () => set(produce(state => {
    state.gameState.phase = 'PLAYING';
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.winner = null;
  })),
  
  pauseGame: () => set(produce(state => {
    if (state.gameState.phase === 'PLAYING') {
      state.gameState.phase = 'PAUSED';
    }
  })),
  
  resumeGame: () => set(produce(state => {
    if (state.gameState.phase === 'PAUSED') {
      state.gameState.phase = 'PLAYING';
    }
  })),
  
  endGame: (winner: string | null) => set(produce(state => {
    state.gameState.phase = 'RESULT';
    state.gameState.winner = winner;
    if (winner && state.players[winner]) {
      state.players[winner].score += 1;
    }
  })),
  
  resetGame: () => set(produce(state => {
    state.gameState = initialGameState;
    state.players = {
      player1: createInitialMecha('player1', 'BLUE', 200),
      player2: createInitialMecha('player2', 'RED', 550),
    };
    state.effects = [];
  })),
  
  nextRound: () => set(produce(state => {
    state.gameState.round += 1;
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.phase = 'PLAYING';
    state.gameState.winner = null;
    
    // 重置玩家位置和状态
    Object.keys(state.players).forEach(playerId => {
      const player = state.players[playerId];
      player.health = player.maxHealth;
      player.position = playerId === 'player1' ? { x: 200, y: 400 } : { x: 550, y: 400 };
      player.velocity = { x: 0, y: 0 };
      player.state = 'IDLE';
      player.isDefending = false;
      player.isGrounded = false;
      player.attackCooldown = 0;
    });
    
    state.effects = [];
  })),
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => set(produce(state => {
    state.players[playerId].position = position;
    state.players[playerId].velocity = velocity;
  })),
  
  updatePlayerState: (playerId: string, mechaState: MechaData['state']) => set(produce(state => {
    state.players[playerId].state = mechaState;
  })),
  
  updatePlayerFacing: (playerId: string, facingRight: boolean) => set(produce(state => {
    state.players[playerId].facingRight = facingRight;
  })),
  
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => set(produce(state => {
    state.players[playerId].isGrounded = isGrounded;
  })),
  
  updatePlayerDefending: (playerId: string, isDefending: boolean) => set(produce(state => {
    state.players[playerId].isDefending = isDefending;
  })),
  
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => set(produce(state => {
    state.players[playerId].attackCooldown = cooldown;
  })),
  
  dealDamage: (targetId: string, damage: number) => set(produce(state => {
    const target = state.players[targetId];
    const actualDamage = target.isDefending ? Math.floor(damage * 0.5) : damage;
    target.health = Math.max(0, target.health - actualDamage);
    
    // 添加受击特效
    state.effects.push({
      id: `hit-${Date.now()}`,
      type: 'HIT',
      position: { ...target.position },
      duration: 300,
      timer: 0,
      frame: 0,
    });
    
    // 检查游戏结束
    if (target.health === 0) {
      const winnerId = targetId === 'player1' ? 'player2' : 'player1';
      state.gameState.phase = 'RESULT';
      state.gameState.winner = winnerId;
      state.players[winnerId].score += 1;
    }
  })),
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => set(produce(state => {
    const player = state.players[playerId];
    player.animationTimer += deltaTime;
  })),
  
  setAnimationFrame: (playerId: string, frame: number) => set(produce(state => {
    state.players[playerId].animationFrame = frame;
  })),
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => {
    const id = `effect-${Date.now()}-${Math.random().toString(36).substr(2, 9)}`;
    set(produce(state => {
      state.effects.push({
        ...effect,
        id,
        timer: 0,
      });
    }));
    return id;
  },
  
  updateEffects: (deltaTime: number) => set(produce(state => {
    state.effects = state.effects.filter(effect => {
      effect.timer += deltaTime;
      return effect.timer < effect.duration;
    });
  })),
  
  removeEffect: (effectId: string) => set(produce(state => {
    state.effects = state.effects.filter(e => e.id !== effectId);
  })),
  
  clearEffects: () => set(produce(state => {
    state.effects = [];
  })),
  
  // 游戏时间
  tick: (deltaTime: number) => set(produce(state => {
    if (state.gameState.phase === 'PLAYING') {
      state.gameState.timeRemaining = Math.max(0, state.gameState.timeRemaining - deltaTime / 1000);
      
      // 时间到，判定胜负
      if (state.gameimport { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
  winner: null,
};

// 游戏Store接口
interface GameStore {
  // 状态
  gameState: GameState;
  players: Record<string, MechaData>;
  effects: Effect[];
  
  // 游戏流程控制
  startGame: () => void;
  pauseGame: () => void;
  resumeGame: () => void;
  endGame: (winner: string | null) => void;
  resetGame: () => void;
  nextRound: () => void;
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => void;
  updatePlayerState: (playerId: string, state: MechaData['state']) => void;
  updatePlayerFacing: (playerId: string, facingRight: boolean) => void;
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => void;
  updatePlayerDefending: (playerId: string, isDefending: boolean) => void;
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => void;
  dealDamage: (targetId: string, damage: number, attackerId?: string) => void;
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => void;
  setAnimationFrame: (playerId: string, frame: number) => void;
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => string;
  updateEffects: (deltaTime: number) => void;
  removeEffect: (effectId: string) => void;
  clearEffects: () => void;
  
  // 游戏时间
  tick: (deltaTime: number) => void;
}

// 创建Game Store
export const useGameStore = create<GameStore>((set, get) => ({
  // 初始状态
  gameState: initialGameState,
  players: {
    player1: createInitialMecha('player1', 'BLUE', 200),
    player2: createInitialMecha('player2', 'RED', 550),
  },
  effects: [],
  
  // 游戏流程控制
  startGame: () => set(produce(state => {
    state.gameState.phase = 'PLAYING';
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.winner = null;
  })),
  
  pauseGame: () => set(produce(state => {
    if (state.gameState.phase === 'PLAYING') {
      state.gameState.phase = 'PAUSED';
    }
  })),
  
  resumeGame: () => set(produce(state => {
    if (state.gameState.phase === 'PAUSED') {
      state.gameState.phase = 'PLAYING';
    }
  })),
  
  endGame: (winner: string | null) => set(produce(state => {
    state.gameState.phase = 'RESULT';
    state.gameState.winner = winner;
    if (winner && state.players[winner]) {
      state.players[winner].score += 1;
    }
  })),
  
  resetGame: () => set(produce(state => {
    state.gameState = initialGameState;
    state.players = {
      player1: createInitialMecha('player1', 'BLUE', 200),
      player2: createInitialMecha('player2', 'RED', 550),
    };
    state.effects = [];
  })),
  
  nextRound: () => set(produce(state => {
    state.gameState.round += 1;
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.phase = 'PLAYING';
    state.gameState.winner = null;
    
    // 重置玩家位置和状态
    Object.keys(state.players).forEach(playerId => {
      const player = state.players[playerId];
      player.health = player.maxHealth;
      player.position = playerId === 'player1' ? { x: 200, y: 400 } : { x: 550, y: 400 };
      player.velocity = { x: 0, y: 0 };
      player.state = 'IDLE';
      player.isDefending = false;
      player.isGrounded = false;
      player.attackCooldown = 0;
    });
    
    state.effects = [];
  })),
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => set(produce(state => {
    state.players[playerId].position = position;
    state.players[playerId].velocity = velocity;
  })),
  
  updatePlayerState: (playerId: string, mechaState: MechaData['state']) => set(produce(state => {
    state.players[playerId].state = mechaState;
  })),
  
  updatePlayerFacing: (playerId: string, facingRight: boolean) => set(produce(state => {
    state.players[playerId].facingRight = facingRight;
  })),
  
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => set(produce(state => {
    state.players[playerId].isGrounded = isGrounded;
  })),
  
  updatePlayerDefending: (playerId: string, isDefending: boolean) => set(produce(state => {
    state.players[playerId].isDefending = isDefending;
  })),
  
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => set(produce(state => {
    state.players[playerId].attackCooldown = cooldown;
  })),
  
  dealDamage: (targetId: string, damage: number) => set(produce(state => {
    const target = state.players[targetId];
    const actualDamage = target.isDefending ? Math.floor(damage * 0.5) : damage;
    target.health = Math.max(0, target.health - actualDamage);
    
    // 添加受击特效
    state.effects.push({
      id: `hit-${Date.now()}`,
      type: 'HIT',
      position: { ...target.position },
      duration: 300,
      timer: 0,
      frame: 0,
    });
    
    // 检查游戏结束
    if (target.health === 0) {
      const winnerId = targetId === 'player1' ? 'player2' : 'player1';
      state.gameState.phase = 'RESULT';
      state.gameState.winner = winnerId;
      state.players[winnerId].score += 1;
    }
  })),
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => set(produce(state => {
    const player = state.players[playerId];
    player.animationTimer += deltaTime;
  })),
  
  setAnimationFrame: (playerId: string, frame: number) => set(produce(state => {
    state.players[playerId].animationFrame = frame;
  })),
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => {
    const id = `effect-${Date.now()}-${Math.random().toString(36).substr(2, 9)}`;
    set(produce(state => {
      state.effects.push({
        ...effect,
        id,
        timer: 0,
      });
    }));
    return id;
  },
  
  updateEffects: (deltaTime: number) => set(produce(state => {
    state.effects = state.effects.filter(effect => {
      effect.timer += deltaTime;
      return effect.timer < effect.duration;
    });
  })),
  
  removeEffect: (effectId: string) => set(produce(state => {
    state.effects = state.effects.filter(e => e.id !== effectId);
  })),
  
  clearEffects: () => set(produce(state => {
    state.effects = [];
  })),
  
  // 游戏时间
  tick: (deltaTime: number) => set(produce(state => {
    if (state.gameState.phase === 'PLAYING') {
      state.gameState.timeRemaining = Math.max(0, state.gameState.timeRemaining - deltaTime / 1000);
      
      // 时间到，判定胜负
      if (state.gameState.timeRemaining === 0)import { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
  winner: null,
};

// 游戏Store接口
interface GameStore {
  // 状态
  gameState: GameState;
  players: Record<string, MechaData>;
  effects: Effect[];
  
  // 游戏流程控制
  startGame: () => void;
  pauseGame: () => void;
  resumeGame: () => void;
  endGame: (winner: string | null) => void;
  resetGame: () => void;
  nextRound: () => void;
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => void;
  updatePlayerState: (playerId: string, state: MechaData['state']) => void;
  updatePlayerFacing: (playerId: string, facingRight: boolean) => void;
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => void;
  updatePlayerDefending: (playerId: string, isDefending: boolean) => void;
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => void;
  dealDamage: (targetId: string, damage: number, attackerId?: string) => void;
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => void;
  setAnimationFrame: (playerId: string, frame: number) => void;
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => string;
  updateEffects: (deltaTime: number) => void;
  removeEffect: (effectId: string) => void;
  clearEffects: () => void;
  
  // 游戏时间
  tick: (deltaTime: number) => void;
}

// 创建Game Store
export const useGameStore = create<GameStore>((set, get) => ({
  // 初始状态
  gameState: initialGameState,
  players: {
    player1: createInitialMecha('player1', 'BLUE', 200),
    player2: createInitialMecha('player2', 'RED', 550),
  },
  effects: [],
  
  // 游戏流程控制
  startGame: () => set(produce(state => {
    state.gameState.phase = 'PLAYING';
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.winner = null;
  })),
  
  pauseGame: () => set(produce(state => {
    if (state.gameState.phase === 'PLAYING') {
      state.gameState.phase = 'PAUSED';
    }
  })),
  
  resumeGame: () => set(produce(state => {
    if (state.gameState.phase === 'PAUSED') {
      state.gameState.phase = 'PLAYING';
    }
  })),
  
  endGame: (winner: string | null) => set(produce(state => {
    state.gameState.phase = 'RESULT';
    state.gameState.winner = winner;
    if (winner && state.players[winner]) {
      state.players[winner].score += 1;
    }
  })),
  
  resetGame: () => set(produce(state => {
    state.gameState = initialGameState;
    state.players = {
      player1: createInitialMecha('player1', 'BLUE', 200),
      player2: createInitialMecha('player2', 'RED', 550),
    };
    state.effects = [];
  })),
  
  nextRound: () => set(produce(state => {
    state.gameState.round += 1;
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.phase = 'PLAYING';
    state.gameState.winner = null;
    
    // 重置玩家位置和状态
    Object.keys(state.players).forEach(playerId => {
      const player = state.players[playerId];
      player.health = player.maxHealth;
      player.position = playerId === 'player1' ? { x: 200, y: 400 } : { x: 550, y: 400 };
      player.velocity = { x: 0, y: 0 };
      player.state = 'IDLE';
      player.isDefending = false;
      player.isGrounded = false;
      player.attackCooldown = 0;
    });
    
    state.effects = [];
  })),
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => set(produce(state => {
    state.players[playerId].position = position;
    state.players[playerId].velocity = velocity;
  })),
  
  updatePlayerState: (playerId: string, mechaState: MechaData['state']) => set(produce(state => {
    state.players[playerId].state = mechaState;
  })),
  
  updatePlayerFacing: (playerId: string, facingRight: boolean) => set(produce(state => {
    state.players[playerId].facingRight = facingRight;
  })),
  
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => set(produce(state => {
    state.players[playerId].isGrounded = isGrounded;
  })),
  
  updatePlayerDefending: (playerId: string, isDefending: boolean) => set(produce(state => {
    state.players[playerId].isDefending = isDefending;
  })),
  
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => set(produce(state => {
    state.players[playerId].attackCooldown = cooldown;
  })),
  
  dealDamage: (targetId: string, damage: number) => set(produce(state => {
    const target = state.players[targetId];
    const actualDamage = target.isDefending ? Math.floor(damage * 0.5) : damage;
    target.health = Math.max(0, target.health - actualDamage);
    
    // 添加受击特效
    state.effects.push({
      id: `hit-${Date.now()}`,
      type: 'HIT',
      position: { ...target.position },
      duration: 300,
      timer: 0,
      frame: 0,
    });
    
    // 检查游戏结束
    if (target.health === 0) {
      const winnerId = targetId === 'player1' ? 'player2' : 'player1';
      state.gameState.phase = 'RESULT';
      state.gameState.winner = winnerId;
      state.players[winnerId].score += 1;
    }
  })),
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => set(produce(state => {
    const player = state.players[playerId];
    player.animationTimer += deltaTime;
  })),
  
  setAnimationFrame: (playerId: string, frame: number) => set(produce(state => {
    state.players[playerId].animationFrame = frame;
  })),
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => {
    const id = `effect-${Date.now()}-${Math.random().toString(36).substr(2, 9)}`;
    set(produce(state => {
      state.effects.push({
        ...effect,
        id,
        timer: 0,
      });
    }));
    return id;
  },
  
  updateEffects: (deltaTime: number) => set(produce(state => {
    state.effects = state.effects.filter(effect => {
      effect.timer += deltaTime;
      return effect.timer < effect.duration;
    });
  })),
  
  removeEffect: (effectId: string) => set(produce(state => {
    state.effects = state.effects.filter(e => e.id !== effectId);
  })),
  
  clearEffects: () => set(produce(state => {
    state.effects = [];
  })),
  
  // 游戏时间
  tick: (deltaTime: number) => set(produce(state => {
    if (state.gameState.phase === 'PLAYING') {
      state.gameState.timeRemaining = Math.max(0, state.gameState.timeRemaining - deltaTime / 1000);
      
      // 时间到，判定胜负
      if (state.gameState.timeRemaining === 0) {
        const p1Healthimport { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
  winner: null,
};

// 游戏Store接口
interface GameStore {
  // 状态
  gameState: GameState;
  players: Record<string, MechaData>;
  effects: Effect[];
  
  // 游戏流程控制
  startGame: () => void;
  pauseGame: () => void;
  resumeGame: () => void;
  endGame: (winner: string | null) => void;
  resetGame: () => void;
  nextRound: () => void;
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => void;
  updatePlayerState: (playerId: string, state: MechaData['state']) => void;
  updatePlayerFacing: (playerId: string, facingRight: boolean) => void;
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => void;
  updatePlayerDefending: (playerId: string, isDefending: boolean) => void;
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => void;
  dealDamage: (targetId: string, damage: number, attackerId?: string) => void;
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => void;
  setAnimationFrame: (playerId: string, frame: number) => void;
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => string;
  updateEffects: (deltaTime: number) => void;
  removeEffect: (effectId: string) => void;
  clearEffects: () => void;
  
  // 游戏时间
  tick: (deltaTime: number) => void;
}

// 创建Game Store
export const useGameStore = create<GameStore>((set, get) => ({
  // 初始状态
  gameState: initialGameState,
  players: {
    player1: createInitialMecha('player1', 'BLUE', 200),
    player2: createInitialMecha('player2', 'RED', 550),
  },
  effects: [],
  
  // 游戏流程控制
  startGame: () => set(produce(state => {
    state.gameState.phase = 'PLAYING';
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.winner = null;
  })),
  
  pauseGame: () => set(produce(state => {
    if (state.gameState.phase === 'PLAYING') {
      state.gameState.phase = 'PAUSED';
    }
  })),
  
  resumeGame: () => set(produce(state => {
    if (state.gameState.phase === 'PAUSED') {
      state.gameState.phase = 'PLAYING';
    }
  })),
  
  endGame: (winner: string | null) => set(produce(state => {
    state.gameState.phase = 'RESULT';
    state.gameState.winner = winner;
    if (winner && state.players[winner]) {
      state.players[winner].score += 1;
    }
  })),
  
  resetGame: () => set(produce(state => {
    state.gameState = initialGameState;
    state.players = {
      player1: createInitialMecha('player1', 'BLUE', 200),
      player2: createInitialMecha('player2', 'RED', 550),
    };
    state.effects = [];
  })),
  
  nextRound: () => set(produce(state => {
    state.gameState.round += 1;
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.phase = 'PLAYING';
    state.gameState.winner = null;
    
    // 重置玩家位置和状态
    Object.keys(state.players).forEach(playerId => {
      const player = state.players[playerId];
      player.health = player.maxHealth;
      player.position = playerId === 'player1' ? { x: 200, y: 400 } : { x: 550, y: 400 };
      player.velocity = { x: 0, y: 0 };
      player.state = 'IDLE';
      player.isDefending = false;
      player.isGrounded = false;
      player.attackCooldown = 0;
    });
    
    state.effects = [];
  })),
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => set(produce(state => {
    state.players[playerId].position = position;
    state.players[playerId].velocity = velocity;
  })),
  
  updatePlayerState: (playerId: string, mechaState: MechaData['state']) => set(produce(state => {
    state.players[playerId].state = mechaState;
  })),
  
  updatePlayerFacing: (playerId: string, facingRight: boolean) => set(produce(state => {
    state.players[playerId].facingRight = facingRight;
  })),
  
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => set(produce(state => {
    state.players[playerId].isGrounded = isGrounded;
  })),
  
  updatePlayerDefending: (playerId: string, isDefending: boolean) => set(produce(state => {
    state.players[playerId].isDefending = isDefending;
  })),
  
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => set(produce(state => {
    state.players[playerId].attackCooldown = cooldown;
  })),
  
  dealDamage: (targetId: string, damage: number) => set(produce(state => {
    const target = state.players[targetId];
    const actualDamage = target.isDefending ? Math.floor(damage * 0.5) : damage;
    target.health = Math.max(0, target.health - actualDamage);
    
    // 添加受击特效
    state.effects.push({
      id: `hit-${Date.now()}`,
      type: 'HIT',
      position: { ...target.position },
      duration: 300,
      timer: 0,
      frame: 0,
    });
    
    // 检查游戏结束
    if (target.health === 0) {
      const winnerId = targetId === 'player1' ? 'player2' : 'player1';
      state.gameState.phase = 'RESULT';
      state.gameState.winner = winnerId;
      state.players[winnerId].score += 1;
    }
  })),
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => set(produce(state => {
    const player = state.players[playerId];
    player.animationTimer += deltaTime;
  })),
  
  setAnimationFrame: (playerId: string, frame: number) => set(produce(state => {
    state.players[playerId].animationFrame = frame;
  })),
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => {
    const id = `effect-${Date.now()}-${Math.random().toString(36).substr(2, 9)}`;
    set(produce(state => {
      state.effects.push({
        ...effect,
        id,
        timer: 0,
      });
    }));
    return id;
  },
  
  updateEffects: (deltaTime: number) => set(produce(state => {
    state.effects = state.effects.filter(effect => {
      effect.timer += deltaTime;
      return effect.timer < effect.duration;
    });
  })),
  
  removeEffect: (effectId: string) => set(produce(state => {
    state.effects = state.effects.filter(e => e.id !== effectId);
  })),
  
  clearEffects: () => set(produce(state => {
    state.effects = [];
  })),
  
  // 游戏时间
  tick: (deltaTime: number) => set(produce(state => {
    if (state.gameState.phase === 'PLAYING') {
      state.gameState.timeRemaining = Math.max(0, state.gameState.timeRemaining - deltaTime / 1000);
      
      // 时间到，判定胜负
      if (state.gameState.timeRemaining === 0) {
        const p1Health = state.players.player1.health;
import { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
  winner: null,
};

// 游戏Store接口
interface GameStore {
  // 状态
  gameState: GameState;
  players: Record<string, MechaData>;
  effects: Effect[];
  
  // 游戏流程控制
  startGame: () => void;
  pauseGame: () => void;
  resumeGame: () => void;
  endGame: (winner: string | null) => void;
  resetGame: () => void;
  nextRound: () => void;
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => void;
  updatePlayerState: (playerId: string, state: MechaData['state']) => void;
  updatePlayerFacing: (playerId: string, facingRight: boolean) => void;
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => void;
  updatePlayerDefending: (playerId: string, isDefending: boolean) => void;
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => void;
  dealDamage: (targetId: string, damage: number, attackerId?: string) => void;
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => void;
  setAnimationFrame: (playerId: string, frame: number) => void;
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => string;
  updateEffects: (deltaTime: number) => void;
  removeEffect: (effectId: string) => void;
  clearEffects: () => void;
  
  // 游戏时间
  tick: (deltaTime: number) => void;
}

// 创建Game Store
export const useGameStore = create<GameStore>((set, get) => ({
  // 初始状态
  gameState: initialGameState,
  players: {
    player1: createInitialMecha('player1', 'BLUE', 200),
    player2: createInitialMecha('player2', 'RED', 550),
  },
  effects: [],
  
  // 游戏流程控制
  startGame: () => set(produce(state => {
    state.gameState.phase = 'PLAYING';
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.winner = null;
  })),
  
  pauseGame: () => set(produce(state => {
    if (state.gameState.phase === 'PLAYING') {
      state.gameState.phase = 'PAUSED';
    }
  })),
  
  resumeGame: () => set(produce(state => {
    if (state.gameState.phase === 'PAUSED') {
      state.gameState.phase = 'PLAYING';
    }
  })),
  
  endGame: (winner: string | null) => set(produce(state => {
    state.gameState.phase = 'RESULT';
    state.gameState.winner = winner;
    if (winner && state.players[winner]) {
      state.players[winner].score += 1;
    }
  })),
  
  resetGame: () => set(produce(state => {
    state.gameState = initialGameState;
    state.players = {
      player1: createInitialMecha('player1', 'BLUE', 200),
      player2: createInitialMecha('player2', 'RED', 550),
    };
    state.effects = [];
  })),
  
  nextRound: () => set(produce(state => {
    state.gameState.round += 1;
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.phase = 'PLAYING';
    state.gameState.winner = null;
    
    // 重置玩家位置和状态
    Object.keys(state.players).forEach(playerId => {
      const player = state.players[playerId];
      player.health = player.maxHealth;
      player.position = playerId === 'player1' ? { x: 200, y: 400 } : { x: 550, y: 400 };
      player.velocity = { x: 0, y: 0 };
      player.state = 'IDLE';
      player.isDefending = false;
      player.isGrounded = false;
      player.attackCooldown = 0;
    });
    
    state.effects = [];
  })),
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => set(produce(state => {
    state.players[playerId].position = position;
    state.players[playerId].velocity = velocity;
  })),
  
  updatePlayerState: (playerId: string, mechaState: MechaData['state']) => set(produce(state => {
    state.players[playerId].state = mechaState;
  })),
  
  updatePlayerFacing: (playerId: string, facingRight: boolean) => set(produce(state => {
    state.players[playerId].facingRight = facingRight;
  })),
  
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => set(produce(state => {
    state.players[playerId].isGrounded = isGrounded;
  })),
  
  updatePlayerDefending: (playerId: string, isDefending: boolean) => set(produce(state => {
    state.players[playerId].isDefending = isDefending;
  })),
  
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => set(produce(state => {
    state.players[playerId].attackCooldown = cooldown;
  })),
  
  dealDamage: (targetId: string, damage: number) => set(produce(state => {
    const target = state.players[targetId];
    const actualDamage = target.isDefending ? Math.floor(damage * 0.5) : damage;
    target.health = Math.max(0, target.health - actualDamage);
    
    // 添加受击特效
    state.effects.push({
      id: `hit-${Date.now()}`,
      type: 'HIT',
      position: { ...target.position },
      duration: 300,
      timer: 0,
      frame: 0,
    });
    
    // 检查游戏结束
    if (target.health === 0) {
      const winnerId = targetId === 'player1' ? 'player2' : 'player1';
      state.gameState.phase = 'RESULT';
      state.gameState.winner = winnerId;
      state.players[winnerId].score += 1;
    }
  })),
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => set(produce(state => {
    const player = state.players[playerId];
    player.animationTimer += deltaTime;
  })),
  
  setAnimationFrame: (playerId: string, frame: number) => set(produce(state => {
    state.players[playerId].animationFrame = frame;
  })),
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => {
    const id = `effect-${Date.now()}-${Math.random().toString(36).substr(2, 9)}`;
    set(produce(state => {
      state.effects.push({
        ...effect,
        id,
        timer: 0,
      });
    }));
    return id;
  },
  
  updateEffects: (deltaTime: number) => set(produce(state => {
    state.effects = state.effects.filter(effect => {
      effect.timer += deltaTime;
      return effect.timer < effect.duration;
    });
  })),
  
  removeEffect: (effectId: string) => set(produce(state => {
    state.effects = state.effects.filter(e => e.id !== effectId);
  })),
  
  clearEffects: () => set(produce(state => {
    state.effects = [];
  })),
  
  // 游戏时间
  tick: (deltaTime: number) => set(produce(state => {
    if (state.gameState.phase === 'PLAYING') {
      state.gameState.timeRemaining = Math.max(0, state.gameState.timeRemaining - deltaTime / 1000);
      
      // 时间到，判定胜负
      if (state.gameState.timeRemaining === 0) {
        const p1Health = state.players.player1.health;
        const p2Health = stateimport { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
  winner: null,
};

// 游戏Store接口
interface GameStore {
  // 状态
  gameState: GameState;
  players: Record<string, MechaData>;
  effects: Effect[];
  
  // 游戏流程控制
  startGame: () => void;
  pauseGame: () => void;
  resumeGame: () => void;
  endGame: (winner: string | null) => void;
  resetGame: () => void;
  nextRound: () => void;
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => void;
  updatePlayerState: (playerId: string, state: MechaData['state']) => void;
  updatePlayerFacing: (playerId: string, facingRight: boolean) => void;
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => void;
  updatePlayerDefending: (playerId: string, isDefending: boolean) => void;
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => void;
  dealDamage: (targetId: string, damage: number, attackerId?: string) => void;
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => void;
  setAnimationFrame: (playerId: string, frame: number) => void;
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => string;
  updateEffects: (deltaTime: number) => void;
  removeEffect: (effectId: string) => void;
  clearEffects: () => void;
  
  // 游戏时间
  tick: (deltaTime: number) => void;
}

// 创建Game Store
export const useGameStore = create<GameStore>((set, get) => ({
  // 初始状态
  gameState: initialGameState,
  players: {
    player1: createInitialMecha('player1', 'BLUE', 200),
    player2: createInitialMecha('player2', 'RED', 550),
  },
  effects: [],
  
  // 游戏流程控制
  startGame: () => set(produce(state => {
    state.gameState.phase = 'PLAYING';
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.winner = null;
  })),
  
  pauseGame: () => set(produce(state => {
    if (state.gameState.phase === 'PLAYING') {
      state.gameState.phase = 'PAUSED';
    }
  })),
  
  resumeGame: () => set(produce(state => {
    if (state.gameState.phase === 'PAUSED') {
      state.gameState.phase = 'PLAYING';
    }
  })),
  
  endGame: (winner: string | null) => set(produce(state => {
    state.gameState.phase = 'RESULT';
    state.gameState.winner = winner;
    if (winner && state.players[winner]) {
      state.players[winner].score += 1;
    }
  })),
  
  resetGame: () => set(produce(state => {
    state.gameState = initialGameState;
    state.players = {
      player1: createInitialMecha('player1', 'BLUE', 200),
      player2: createInitialMecha('player2', 'RED', 550),
    };
    state.effects = [];
  })),
  
  nextRound: () => set(produce(state => {
    state.gameState.round += 1;
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.phase = 'PLAYING';
    state.gameState.winner = null;
    
    // 重置玩家位置和状态
    Object.keys(state.players).forEach(playerId => {
      const player = state.players[playerId];
      player.health = player.maxHealth;
      player.position = playerId === 'player1' ? { x: 200, y: 400 } : { x: 550, y: 400 };
      player.velocity = { x: 0, y: 0 };
      player.state = 'IDLE';
      player.isDefending = false;
      player.isGrounded = false;
      player.attackCooldown = 0;
    });
    
    state.effects = [];
  })),
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => set(produce(state => {
    state.players[playerId].position = position;
    state.players[playerId].velocity = velocity;
  })),
  
  updatePlayerState: (playerId: string, mechaState: MechaData['state']) => set(produce(state => {
    state.players[playerId].state = mechaState;
  })),
  
  updatePlayerFacing: (playerId: string, facingRight: boolean) => set(produce(state => {
    state.players[playerId].facingRight = facingRight;
  })),
  
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => set(produce(state => {
    state.players[playerId].isGrounded = isGrounded;
  })),
  
  updatePlayerDefending: (playerId: string, isDefending: boolean) => set(produce(state => {
    state.players[playerId].isDefending = isDefending;
  })),
  
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => set(produce(state => {
    state.players[playerId].attackCooldown = cooldown;
  })),
  
  dealDamage: (targetId: string, damage: number) => set(produce(state => {
    const target = state.players[targetId];
    const actualDamage = target.isDefending ? Math.floor(damage * 0.5) : damage;
    target.health = Math.max(0, target.health - actualDamage);
    
    // 添加受击特效
    state.effects.push({
      id: `hit-${Date.now()}`,
      type: 'HIT',
      position: { ...target.position },
      duration: 300,
      timer: 0,
      frame: 0,
    });
    
    // 检查游戏结束
    if (target.health === 0) {
      const winnerId = targetId === 'player1' ? 'player2' : 'player1';
      state.gameState.phase = 'RESULT';
      state.gameState.winner = winnerId;
      state.players[winnerId].score += 1;
    }
  })),
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => set(produce(state => {
    const player = state.players[playerId];
    player.animationTimer += deltaTime;
  })),
  
  setAnimationFrame: (playerId: string, frame: number) => set(produce(state => {
    state.players[playerId].animationFrame = frame;
  })),
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => {
    const id = `effect-${Date.now()}-${Math.random().toString(36).substr(2, 9)}`;
    set(produce(state => {
      state.effects.push({
        ...effect,
        id,
        timer: 0,
      });
    }));
    return id;
  },
  
  updateEffects: (deltaTime: number) => set(produce(state => {
    state.effects = state.effects.filter(effect => {
      effect.timer += deltaTime;
      return effect.timer < effect.duration;
    });
  })),
  
  removeEffect: (effectId: string) => set(produce(state => {
    state.effects = state.effects.filter(e => e.id !== effectId);
  })),
  
  clearEffects: () => set(produce(state => {
    state.effects = [];
  })),
  
  // 游戏时间
  tick: (deltaTime: number) => set(produce(state => {
    if (state.gameState.phase === 'PLAYING') {
      state.gameState.timeRemaining = Math.max(0, state.gameState.timeRemaining - deltaTime / 1000);
      
      // 时间到，判定胜负
      if (state.gameState.timeRemaining === 0) {
        const p1Health = state.players.player1.health;
        const p2Health = state.players.player2.health;
        
import { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
  winner: null,
};

// 游戏Store接口
interface GameStore {
  // 状态
  gameState: GameState;
  players: Record<string, MechaData>;
  effects: Effect[];
  
  // 游戏流程控制
  startGame: () => void;
  pauseGame: () => void;
  resumeGame: () => void;
  endGame: (winner: string | null) => void;
  resetGame: () => void;
  nextRound: () => void;
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => void;
  updatePlayerState: (playerId: string, state: MechaData['state']) => void;
  updatePlayerFacing: (playerId: string, facingRight: boolean) => void;
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => void;
  updatePlayerDefending: (playerId: string, isDefending: boolean) => void;
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => void;
  dealDamage: (targetId: string, damage: number, attackerId?: string) => void;
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => void;
  setAnimationFrame: (playerId: string, frame: number) => void;
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => string;
  updateEffects: (deltaTime: number) => void;
  removeEffect: (effectId: string) => void;
  clearEffects: () => void;
  
  // 游戏时间
  tick: (deltaTime: number) => void;
}

// 创建Game Store
export const useGameStore = create<GameStore>((set, get) => ({
  // 初始状态
  gameState: initialGameState,
  players: {
    player1: createInitialMecha('player1', 'BLUE', 200),
    player2: createInitialMecha('player2', 'RED', 550),
  },
  effects: [],
  
  // 游戏流程控制
  startGame: () => set(produce(state => {
    state.gameState.phase = 'PLAYING';
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.winner = null;
  })),
  
  pauseGame: () => set(produce(state => {
    if (state.gameState.phase === 'PLAYING') {
      state.gameState.phase = 'PAUSED';
    }
  })),
  
  resumeGame: () => set(produce(state => {
    if (state.gameState.phase === 'PAUSED') {
      state.gameState.phase = 'PLAYING';
    }
  })),
  
  endGame: (winner: string | null) => set(produce(state => {
    state.gameState.phase = 'RESULT';
    state.gameState.winner = winner;
    if (winner && state.players[winner]) {
      state.players[winner].score += 1;
    }
  })),
  
  resetGame: () => set(produce(state => {
    state.gameState = initialGameState;
    state.players = {
      player1: createInitialMecha('player1', 'BLUE', 200),
      player2: createInitialMecha('player2', 'RED', 550),
    };
    state.effects = [];
  })),
  
  nextRound: () => set(produce(state => {
    state.gameState.round += 1;
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.phase = 'PLAYING';
    state.gameState.winner = null;
    
    // 重置玩家位置和状态
    Object.keys(state.players).forEach(playerId => {
      const player = state.players[playerId];
      player.health = player.maxHealth;
      player.position = playerId === 'player1' ? { x: 200, y: 400 } : { x: 550, y: 400 };
      player.velocity = { x: 0, y: 0 };
      player.state = 'IDLE';
      player.isDefending = false;
      player.isGrounded = false;
      player.attackCooldown = 0;
    });
    
    state.effects = [];
  })),
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => set(produce(state => {
    state.players[playerId].position = position;
    state.players[playerId].velocity = velocity;
  })),
  
  updatePlayerState: (playerId: string, mechaState: MechaData['state']) => set(produce(state => {
    state.players[playerId].state = mechaState;
  })),
  
  updatePlayerFacing: (playerId: string, facingRight: boolean) => set(produce(state => {
    state.players[playerId].facingRight = facingRight;
  })),
  
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => set(produce(state => {
    state.players[playerId].isGrounded = isGrounded;
  })),
  
  updatePlayerDefending: (playerId: string, isDefending: boolean) => set(produce(state => {
    state.players[playerId].isDefending = isDefending;
  })),
  
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => set(produce(state => {
    state.players[playerId].attackCooldown = cooldown;
  })),
  
  dealDamage: (targetId: string, damage: number) => set(produce(state => {
    const target = state.players[targetId];
    const actualDamage = target.isDefending ? Math.floor(damage * 0.5) : damage;
    target.health = Math.max(0, target.health - actualDamage);
    
    // 添加受击特效
    state.effects.push({
      id: `hit-${Date.now()}`,
      type: 'HIT',
      position: { ...target.position },
      duration: 300,
      timer: 0,
      frame: 0,
    });
    
    // 检查游戏结束
    if (target.health === 0) {
      const winnerId = targetId === 'player1' ? 'player2' : 'player1';
      state.gameState.phase = 'RESULT';
      state.gameState.winner = winnerId;
      state.players[winnerId].score += 1;
    }
  })),
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => set(produce(state => {
    const player = state.players[playerId];
    player.animationTimer += deltaTime;
  })),
  
  setAnimationFrame: (playerId: string, frame: number) => set(produce(state => {
    state.players[playerId].animationFrame = frame;
  })),
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => {
    const id = `effect-${Date.now()}-${Math.random().toString(36).substr(2, 9)}`;
    set(produce(state => {
      state.effects.push({
        ...effect,
        id,
        timer: 0,
      });
    }));
    return id;
  },
  
  updateEffects: (deltaTime: number) => set(produce(state => {
    state.effects = state.effects.filter(effect => {
      effect.timer += deltaTime;
      return effect.timer < effect.duration;
    });
  })),
  
  removeEffect: (effectId: string) => set(produce(state => {
    state.effects = state.effects.filter(e => e.id !== effectId);
  })),
  
  clearEffects: () => set(produce(state => {
    state.effects = [];
  })),
  
  // 游戏时间
  tick: (deltaTime: number) => set(produce(state => {
    if (state.gameState.phase === 'PLAYING') {
      state.gameState.timeRemaining = Math.max(0, state.gameState.timeRemaining - deltaTime / 1000);
      
      // 时间到，判定胜负
      if (state.gameState.timeRemaining === 0) {
        const p1Health = state.players.player1.health;
        const p2Health = state.players.player2.health;
        
        if (p1Health >import { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
  winner: null,
};

// 游戏Store接口
interface GameStore {
  // 状态
  gameState: GameState;
  players: Record<string, MechaData>;
  effects: Effect[];
  
  // 游戏流程控制
  startGame: () => void;
  pauseGame: () => void;
  resumeGame: () => void;
  endGame: (winner: string | null) => void;
  resetGame: () => void;
  nextRound: () => void;
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => void;
  updatePlayerState: (playerId: string, state: MechaData['state']) => void;
  updatePlayerFacing: (playerId: string, facingRight: boolean) => void;
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => void;
  updatePlayerDefending: (playerId: string, isDefending: boolean) => void;
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => void;
  dealDamage: (targetId: string, damage: number, attackerId?: string) => void;
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => void;
  setAnimationFrame: (playerId: string, frame: number) => void;
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => string;
  updateEffects: (deltaTime: number) => void;
  removeEffect: (effectId: string) => void;
  clearEffects: () => void;
  
  // 游戏时间
  tick: (deltaTime: number) => void;
}

// 创建Game Store
export const useGameStore = create<GameStore>((set, get) => ({
  // 初始状态
  gameState: initialGameState,
  players: {
    player1: createInitialMecha('player1', 'BLUE', 200),
    player2: createInitialMecha('player2', 'RED', 550),
  },
  effects: [],
  
  // 游戏流程控制
  startGame: () => set(produce(state => {
    state.gameState.phase = 'PLAYING';
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.winner = null;
  })),
  
  pauseGame: () => set(produce(state => {
    if (state.gameState.phase === 'PLAYING') {
      state.gameState.phase = 'PAUSED';
    }
  })),
  
  resumeGame: () => set(produce(state => {
    if (state.gameState.phase === 'PAUSED') {
      state.gameState.phase = 'PLAYING';
    }
  })),
  
  endGame: (winner: string | null) => set(produce(state => {
    state.gameState.phase = 'RESULT';
    state.gameState.winner = winner;
    if (winner && state.players[winner]) {
      state.players[winner].score += 1;
    }
  })),
  
  resetGame: () => set(produce(state => {
    state.gameState = initialGameState;
    state.players = {
      player1: createInitialMecha('player1', 'BLUE', 200),
      player2: createInitialMecha('player2', 'RED', 550),
    };
    state.effects = [];
  })),
  
  nextRound: () => set(produce(state => {
    state.gameState.round += 1;
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.phase = 'PLAYING';
    state.gameState.winner = null;
    
    // 重置玩家位置和状态
    Object.keys(state.players).forEach(playerId => {
      const player = state.players[playerId];
      player.health = player.maxHealth;
      player.position = playerId === 'player1' ? { x: 200, y: 400 } : { x: 550, y: 400 };
      player.velocity = { x: 0, y: 0 };
      player.state = 'IDLE';
      player.isDefending = false;
      player.isGrounded = false;
      player.attackCooldown = 0;
    });
    
    state.effects = [];
  })),
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => set(produce(state => {
    state.players[playerId].position = position;
    state.players[playerId].velocity = velocity;
  })),
  
  updatePlayerState: (playerId: string, mechaState: MechaData['state']) => set(produce(state => {
    state.players[playerId].state = mechaState;
  })),
  
  updatePlayerFacing: (playerId: string, facingRight: boolean) => set(produce(state => {
    state.players[playerId].facingRight = facingRight;
  })),
  
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => set(produce(state => {
    state.players[playerId].isGrounded = isGrounded;
  })),
  
  updatePlayerDefending: (playerId: string, isDefending: boolean) => set(produce(state => {
    state.players[playerId].isDefending = isDefending;
  })),
  
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => set(produce(state => {
    state.players[playerId].attackCooldown = cooldown;
  })),
  
  dealDamage: (targetId: string, damage: number) => set(produce(state => {
    const target = state.players[targetId];
    const actualDamage = target.isDefending ? Math.floor(damage * 0.5) : damage;
    target.health = Math.max(0, target.health - actualDamage);
    
    // 添加受击特效
    state.effects.push({
      id: `hit-${Date.now()}`,
      type: 'HIT',
      position: { ...target.position },
      duration: 300,
      timer: 0,
      frame: 0,
    });
    
    // 检查游戏结束
    if (target.health === 0) {
      const winnerId = targetId === 'player1' ? 'player2' : 'player1';
      state.gameState.phase = 'RESULT';
      state.gameState.winner = winnerId;
      state.players[winnerId].score += 1;
    }
  })),
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => set(produce(state => {
    const player = state.players[playerId];
    player.animationTimer += deltaTime;
  })),
  
  setAnimationFrame: (playerId: string, frame: number) => set(produce(state => {
    state.players[playerId].animationFrame = frame;
  })),
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => {
    const id = `effect-${Date.now()}-${Math.random().toString(36).substr(2, 9)}`;
    set(produce(state => {
      state.effects.push({
        ...effect,
        id,
        timer: 0,
      });
    }));
    return id;
  },
  
  updateEffects: (deltaTime: number) => set(produce(state => {
    state.effects = state.effects.filter(effect => {
      effect.timer += deltaTime;
      return effect.timer < effect.duration;
    });
  })),
  
  removeEffect: (effectId: string) => set(produce(state => {
    state.effects = state.effects.filter(e => e.id !== effectId);
  })),
  
  clearEffects: () => set(produce(state => {
    state.effects = [];
  })),
  
  // 游戏时间
  tick: (deltaTime: number) => set(produce(state => {
    if (state.gameState.phase === 'PLAYING') {
      state.gameState.timeRemaining = Math.max(0, state.gameState.timeRemaining - deltaTime / 1000);
      
      // 时间到，判定胜负
      if (state.gameState.timeRemaining === 0) {
        const p1Health = state.players.player1.health;
        const p2Health = state.players.player2.health;
        
        if (p1Health > p2Health) {
          stateimport { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
  winner: null,
};

// 游戏Store接口
interface GameStore {
  // 状态
  gameState: GameState;
  players: Record<string, MechaData>;
  effects: Effect[];
  
  // 游戏流程控制
  startGame: () => void;
  pauseGame: () => void;
  resumeGame: () => void;
  endGame: (winner: string | null) => void;
  resetGame: () => void;
  nextRound: () => void;
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => void;
  updatePlayerState: (playerId: string, state: MechaData['state']) => void;
  updatePlayerFacing: (playerId: string, facingRight: boolean) => void;
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => void;
  updatePlayerDefending: (playerId: string, isDefending: boolean) => void;
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => void;
  dealDamage: (targetId: string, damage: number, attackerId?: string) => void;
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => void;
  setAnimationFrame: (playerId: string, frame: number) => void;
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => string;
  updateEffects: (deltaTime: number) => void;
  removeEffect: (effectId: string) => void;
  clearEffects: () => void;
  
  // 游戏时间
  tick: (deltaTime: number) => void;
}

// 创建Game Store
export const useGameStore = create<GameStore>((set, get) => ({
  // 初始状态
  gameState: initialGameState,
  players: {
    player1: createInitialMecha('player1', 'BLUE', 200),
    player2: createInitialMecha('player2', 'RED', 550),
  },
  effects: [],
  
  // 游戏流程控制
  startGame: () => set(produce(state => {
    state.gameState.phase = 'PLAYING';
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.winner = null;
  })),
  
  pauseGame: () => set(produce(state => {
    if (state.gameState.phase === 'PLAYING') {
      state.gameState.phase = 'PAUSED';
    }
  })),
  
  resumeGame: () => set(produce(state => {
    if (state.gameState.phase === 'PAUSED') {
      state.gameState.phase = 'PLAYING';
    }
  })),
  
  endGame: (winner: string | null) => set(produce(state => {
    state.gameState.phase = 'RESULT';
    state.gameState.winner = winner;
    if (winner && state.players[winner]) {
      state.players[winner].score += 1;
    }
  })),
  
  resetGame: () => set(produce(state => {
    state.gameState = initialGameState;
    state.players = {
      player1: createInitialMecha('player1', 'BLUE', 200),
      player2: createInitialMecha('player2', 'RED', 550),
    };
    state.effects = [];
  })),
  
  nextRound: () => set(produce(state => {
    state.gameState.round += 1;
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.phase = 'PLAYING';
    state.gameState.winner = null;
    
    // 重置玩家位置和状态
    Object.keys(state.players).forEach(playerId => {
      const player = state.players[playerId];
      player.health = player.maxHealth;
      player.position = playerId === 'player1' ? { x: 200, y: 400 } : { x: 550, y: 400 };
      player.velocity = { x: 0, y: 0 };
      player.state = 'IDLE';
      player.isDefending = false;
      player.isGrounded = false;
      player.attackCooldown = 0;
    });
    
    state.effects = [];
  })),
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => set(produce(state => {
    state.players[playerId].position = position;
    state.players[playerId].velocity = velocity;
  })),
  
  updatePlayerState: (playerId: string, mechaState: MechaData['state']) => set(produce(state => {
    state.players[playerId].state = mechaState;
  })),
  
  updatePlayerFacing: (playerId: string, facingRight: boolean) => set(produce(state => {
    state.players[playerId].facingRight = facingRight;
  })),
  
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => set(produce(state => {
    state.players[playerId].isGrounded = isGrounded;
  })),
  
  updatePlayerDefending: (playerId: string, isDefending: boolean) => set(produce(state => {
    state.players[playerId].isDefending = isDefending;
  })),
  
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => set(produce(state => {
    state.players[playerId].attackCooldown = cooldown;
  })),
  
  dealDamage: (targetId: string, damage: number) => set(produce(state => {
    const target = state.players[targetId];
    const actualDamage = target.isDefending ? Math.floor(damage * 0.5) : damage;
    target.health = Math.max(0, target.health - actualDamage);
    
    // 添加受击特效
    state.effects.push({
      id: `hit-${Date.now()}`,
      type: 'HIT',
      position: { ...target.position },
      duration: 300,
      timer: 0,
      frame: 0,
    });
    
    // 检查游戏结束
    if (target.health === 0) {
      const winnerId = targetId === 'player1' ? 'player2' : 'player1';
      state.gameState.phase = 'RESULT';
      state.gameState.winner = winnerId;
      state.players[winnerId].score += 1;
    }
  })),
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => set(produce(state => {
    const player = state.players[playerId];
    player.animationTimer += deltaTime;
  })),
  
  setAnimationFrame: (playerId: string, frame: number) => set(produce(state => {
    state.players[playerId].animationFrame = frame;
  })),
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => {
    const id = `effect-${Date.now()}-${Math.random().toString(36).substr(2, 9)}`;
    set(produce(state => {
      state.effects.push({
        ...effect,
        id,
        timer: 0,
      });
    }));
    return id;
  },
  
  updateEffects: (deltaTime: number) => set(produce(state => {
    state.effects = state.effects.filter(effect => {
      effect.timer += deltaTime;
      return effect.timer < effect.duration;
    });
  })),
  
  removeEffect: (effectId: string) => set(produce(state => {
    state.effects = state.effects.filter(e => e.id !== effectId);
  })),
  
  clearEffects: () => set(produce(state => {
    state.effects = [];
  })),
  
  // 游戏时间
  tick: (deltaTime: number) => set(produce(state => {
    if (state.gameState.phase === 'PLAYING') {
      state.gameState.timeRemaining = Math.max(0, state.gameState.timeRemaining - deltaTime / 1000);
      
      // 时间到，判定胜负
      if (state.gameState.timeRemaining === 0) {
        const p1Health = state.players.player1.health;
        const p2Health = state.players.player2.health;
        
        if (p1Health > p2Health) {
          state.gameState.winner = 'playerimport { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
  winner: null,
};

// 游戏Store接口
interface GameStore {
  // 状态
  gameState: GameState;
  players: Record<string, MechaData>;
  effects: Effect[];
  
  // 游戏流程控制
  startGame: () => void;
  pauseGame: () => void;
  resumeGame: () => void;
  endGame: (winner: string | null) => void;
  resetGame: () => void;
  nextRound: () => void;
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => void;
  updatePlayerState: (playerId: string, state: MechaData['state']) => void;
  updatePlayerFacing: (playerId: string, facingRight: boolean) => void;
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => void;
  updatePlayerDefending: (playerId: string, isDefending: boolean) => void;
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => void;
  dealDamage: (targetId: string, damage: number, attackerId?: string) => void;
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => void;
  setAnimationFrame: (playerId: string, frame: number) => void;
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => string;
  updateEffects: (deltaTime: number) => void;
  removeEffect: (effectId: string) => void;
  clearEffects: () => void;
  
  // 游戏时间
  tick: (deltaTime: number) => void;
}

// 创建Game Store
export const useGameStore = create<GameStore>((set, get) => ({
  // 初始状态
  gameState: initialGameState,
  players: {
    player1: createInitialMecha('player1', 'BLUE', 200),
    player2: createInitialMecha('player2', 'RED', 550),
  },
  effects: [],
  
  // 游戏流程控制
  startGame: () => set(produce(state => {
    state.gameState.phase = 'PLAYING';
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.winner = null;
  })),
  
  pauseGame: () => set(produce(state => {
    if (state.gameState.phase === 'PLAYING') {
      state.gameState.phase = 'PAUSED';
    }
  })),
  
  resumeGame: () => set(produce(state => {
    if (state.gameState.phase === 'PAUSED') {
      state.gameState.phase = 'PLAYING';
    }
  })),
  
  endGame: (winner: string | null) => set(produce(state => {
    state.gameState.phase = 'RESULT';
    state.gameState.winner = winner;
    if (winner && state.players[winner]) {
      state.players[winner].score += 1;
    }
  })),
  
  resetGame: () => set(produce(state => {
    state.gameState = initialGameState;
    state.players = {
      player1: createInitialMecha('player1', 'BLUE', 200),
      player2: createInitialMecha('player2', 'RED', 550),
    };
    state.effects = [];
  })),
  
  nextRound: () => set(produce(state => {
    state.gameState.round += 1;
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.phase = 'PLAYING';
    state.gameState.winner = null;
    
    // 重置玩家位置和状态
    Object.keys(state.players).forEach(playerId => {
      const player = state.players[playerId];
      player.health = player.maxHealth;
      player.position = playerId === 'player1' ? { x: 200, y: 400 } : { x: 550, y: 400 };
      player.velocity = { x: 0, y: 0 };
      player.state = 'IDLE';
      player.isDefending = false;
      player.isGrounded = false;
      player.attackCooldown = 0;
    });
    
    state.effects = [];
  })),
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => set(produce(state => {
    state.players[playerId].position = position;
    state.players[playerId].velocity = velocity;
  })),
  
  updatePlayerState: (playerId: string, mechaState: MechaData['state']) => set(produce(state => {
    state.players[playerId].state = mechaState;
  })),
  
  updatePlayerFacing: (playerId: string, facingRight: boolean) => set(produce(state => {
    state.players[playerId].facingRight = facingRight;
  })),
  
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => set(produce(state => {
    state.players[playerId].isGrounded = isGrounded;
  })),
  
  updatePlayerDefending: (playerId: string, isDefending: boolean) => set(produce(state => {
    state.players[playerId].isDefending = isDefending;
  })),
  
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => set(produce(state => {
    state.players[playerId].attackCooldown = cooldown;
  })),
  
  dealDamage: (targetId: string, damage: number) => set(produce(state => {
    const target = state.players[targetId];
    const actualDamage = target.isDefending ? Math.floor(damage * 0.5) : damage;
    target.health = Math.max(0, target.health - actualDamage);
    
    // 添加受击特效
    state.effects.push({
      id: `hit-${Date.now()}`,
      type: 'HIT',
      position: { ...target.position },
      duration: 300,
      timer: 0,
      frame: 0,
    });
    
    // 检查游戏结束
    if (target.health === 0) {
      const winnerId = targetId === 'player1' ? 'player2' : 'player1';
      state.gameState.phase = 'RESULT';
      state.gameState.winner = winnerId;
      state.players[winnerId].score += 1;
    }
  })),
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => set(produce(state => {
    const player = state.players[playerId];
    player.animationTimer += deltaTime;
  })),
  
  setAnimationFrame: (playerId: string, frame: number) => set(produce(state => {
    state.players[playerId].animationFrame = frame;
  })),
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => {
    const id = `effect-${Date.now()}-${Math.random().toString(36).substr(2, 9)}`;
    set(produce(state => {
      state.effects.push({
        ...effect,
        id,
        timer: 0,
      });
    }));
    return id;
  },
  
  updateEffects: (deltaTime: number) => set(produce(state => {
    state.effects = state.effects.filter(effect => {
      effect.timer += deltaTime;
      return effect.timer < effect.duration;
    });
  })),
  
  removeEffect: (effectId: string) => set(produce(state => {
    state.effects = state.effects.filter(e => e.id !== effectId);
  })),
  
  clearEffects: () => set(produce(state => {
    state.effects = [];
  })),
  
  // 游戏时间
  tick: (deltaTime: number) => set(produce(state => {
    if (state.gameState.phase === 'PLAYING') {
      state.gameState.timeRemaining = Math.max(0, state.gameState.timeRemaining - deltaTime / 1000);
      
      // 时间到，判定胜负
      if (state.gameState.timeRemaining === 0) {
        const p1Health = state.players.player1.health;
        const p2Health = state.players.player2.health;
        
        if (p1Health > p2Health) {
          state.gameState.winner = 'player1';
          state.playersimport { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
  winner: null,
};

// 游戏Store接口
interface GameStore {
  // 状态
  gameState: GameState;
  players: Record<string, MechaData>;
  effects: Effect[];
  
  // 游戏流程控制
  startGame: () => void;
  pauseGame: () => void;
  resumeGame: () => void;
  endGame: (winner: string | null) => void;
  resetGame: () => void;
  nextRound: () => void;
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => void;
  updatePlayerState: (playerId: string, state: MechaData['state']) => void;
  updatePlayerFacing: (playerId: string, facingRight: boolean) => void;
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => void;
  updatePlayerDefending: (playerId: string, isDefending: boolean) => void;
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => void;
  dealDamage: (targetId: string, damage: number, attackerId?: string) => void;
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => void;
  setAnimationFrame: (playerId: string, frame: number) => void;
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => string;
  updateEffects: (deltaTime: number) => void;
  removeEffect: (effectId: string) => void;
  clearEffects: () => void;
  
  // 游戏时间
  tick: (deltaTime: number) => void;
}

// 创建Game Store
export const useGameStore = create<GameStore>((set, get) => ({
  // 初始状态
  gameState: initialGameState,
  players: {
    player1: createInitialMecha('player1', 'BLUE', 200),
    player2: createInitialMecha('player2', 'RED', 550),
  },
  effects: [],
  
  // 游戏流程控制
  startGame: () => set(produce(state => {
    state.gameState.phase = 'PLAYING';
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.winner = null;
  })),
  
  pauseGame: () => set(produce(state => {
    if (state.gameState.phase === 'PLAYING') {
      state.gameState.phase = 'PAUSED';
    }
  })),
  
  resumeGame: () => set(produce(state => {
    if (state.gameState.phase === 'PAUSED') {
      state.gameState.phase = 'PLAYING';
    }
  })),
  
  endGame: (winner: string | null) => set(produce(state => {
    state.gameState.phase = 'RESULT';
    state.gameState.winner = winner;
    if (winner && state.players[winner]) {
      state.players[winner].score += 1;
    }
  })),
  
  resetGame: () => set(produce(state => {
    state.gameState = initialGameState;
    state.players = {
      player1: createInitialMecha('player1', 'BLUE', 200),
      player2: createInitialMecha('player2', 'RED', 550),
    };
    state.effects = [];
  })),
  
  nextRound: () => set(produce(state => {
    state.gameState.round += 1;
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.phase = 'PLAYING';
    state.gameState.winner = null;
    
    // 重置玩家位置和状态
    Object.keys(state.players).forEach(playerId => {
      const player = state.players[playerId];
      player.health = player.maxHealth;
      player.position = playerId === 'player1' ? { x: 200, y: 400 } : { x: 550, y: 400 };
      player.velocity = { x: 0, y: 0 };
      player.state = 'IDLE';
      player.isDefending = false;
      player.isGrounded = false;
      player.attackCooldown = 0;
    });
    
    state.effects = [];
  })),
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => set(produce(state => {
    state.players[playerId].position = position;
    state.players[playerId].velocity = velocity;
  })),
  
  updatePlayerState: (playerId: string, mechaState: MechaData['state']) => set(produce(state => {
    state.players[playerId].state = mechaState;
  })),
  
  updatePlayerFacing: (playerId: string, facingRight: boolean) => set(produce(state => {
    state.players[playerId].facingRight = facingRight;
  })),
  
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => set(produce(state => {
    state.players[playerId].isGrounded = isGrounded;
  })),
  
  updatePlayerDefending: (playerId: string, isDefending: boolean) => set(produce(state => {
    state.players[playerId].isDefending = isDefending;
  })),
  
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => set(produce(state => {
    state.players[playerId].attackCooldown = cooldown;
  })),
  
  dealDamage: (targetId: string, damage: number) => set(produce(state => {
    const target = state.players[targetId];
    const actualDamage = target.isDefending ? Math.floor(damage * 0.5) : damage;
    target.health = Math.max(0, target.health - actualDamage);
    
    // 添加受击特效
    state.effects.push({
      id: `hit-${Date.now()}`,
      type: 'HIT',
      position: { ...target.position },
      duration: 300,
      timer: 0,
      frame: 0,
    });
    
    // 检查游戏结束
    if (target.health === 0) {
      const winnerId = targetId === 'player1' ? 'player2' : 'player1';
      state.gameState.phase = 'RESULT';
      state.gameState.winner = winnerId;
      state.players[winnerId].score += 1;
    }
  })),
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => set(produce(state => {
    const player = state.players[playerId];
    player.animationTimer += deltaTime;
  })),
  
  setAnimationFrame: (playerId: string, frame: number) => set(produce(state => {
    state.players[playerId].animationFrame = frame;
  })),
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => {
    const id = `effect-${Date.now()}-${Math.random().toString(36).substr(2, 9)}`;
    set(produce(state => {
      state.effects.push({
        ...effect,
        id,
        timer: 0,
      });
    }));
    return id;
  },
  
  updateEffects: (deltaTime: number) => set(produce(state => {
    state.effects = state.effects.filter(effect => {
      effect.timer += deltaTime;
      return effect.timer < effect.duration;
    });
  })),
  
  removeEffect: (effectId: string) => set(produce(state => {
    state.effects = state.effects.filter(e => e.id !== effectId);
  })),
  
  clearEffects: () => set(produce(state => {
    state.effects = [];
  })),
  
  // 游戏时间
  tick: (deltaTime: number) => set(produce(state => {
    if (state.gameState.phase === 'PLAYING') {
      state.gameState.timeRemaining = Math.max(0, state.gameState.timeRemaining - deltaTime / 1000);
      
      // 时间到，判定胜负
      if (state.gameState.timeRemaining === 0) {
        const p1Health = state.players.player1.health;
        const p2Health = state.players.player2.health;
        
        if (p1Health > p2Health) {
          state.gameState.winner = 'player1';
          state.players.player1.score += 1;
import { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
  winner: null,
};

// 游戏Store接口
interface GameStore {
  // 状态
  gameState: GameState;
  players: Record<string, MechaData>;
  effects: Effect[];
  
  // 游戏流程控制
  startGame: () => void;
  pauseGame: () => void;
  resumeGame: () => void;
  endGame: (winner: string | null) => void;
  resetGame: () => void;
  nextRound: () => void;
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => void;
  updatePlayerState: (playerId: string, state: MechaData['state']) => void;
  updatePlayerFacing: (playerId: string, facingRight: boolean) => void;
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => void;
  updatePlayerDefending: (playerId: string, isDefending: boolean) => void;
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => void;
  dealDamage: (targetId: string, damage: number, attackerId?: string) => void;
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => void;
  setAnimationFrame: (playerId: string, frame: number) => void;
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => string;
  updateEffects: (deltaTime: number) => void;
  removeEffect: (effectId: string) => void;
  clearEffects: () => void;
  
  // 游戏时间
  tick: (deltaTime: number) => void;
}

// 创建Game Store
export const useGameStore = create<GameStore>((set, get) => ({
  // 初始状态
  gameState: initialGameState,
  players: {
    player1: createInitialMecha('player1', 'BLUE', 200),
    player2: createInitialMecha('player2', 'RED', 550),
  },
  effects: [],
  
  // 游戏流程控制
  startGame: () => set(produce(state => {
    state.gameState.phase = 'PLAYING';
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.winner = null;
  })),
  
  pauseGame: () => set(produce(state => {
    if (state.gameState.phase === 'PLAYING') {
      state.gameState.phase = 'PAUSED';
    }
  })),
  
  resumeGame: () => set(produce(state => {
    if (state.gameState.phase === 'PAUSED') {
      state.gameState.phase = 'PLAYING';
    }
  })),
  
  endGame: (winner: string | null) => set(produce(state => {
    state.gameState.phase = 'RESULT';
    state.gameState.winner = winner;
    if (winner && state.players[winner]) {
      state.players[winner].score += 1;
    }
  })),
  
  resetGame: () => set(produce(state => {
    state.gameState = initialGameState;
    state.players = {
      player1: createInitialMecha('player1', 'BLUE', 200),
      player2: createInitialMecha('player2', 'RED', 550),
    };
    state.effects = [];
  })),
  
  nextRound: () => set(produce(state => {
    state.gameState.round += 1;
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.phase = 'PLAYING';
    state.gameState.winner = null;
    
    // 重置玩家位置和状态
    Object.keys(state.players).forEach(playerId => {
      const player = state.players[playerId];
      player.health = player.maxHealth;
      player.position = playerId === 'player1' ? { x: 200, y: 400 } : { x: 550, y: 400 };
      player.velocity = { x: 0, y: 0 };
      player.state = 'IDLE';
      player.isDefending = false;
      player.isGrounded = false;
      player.attackCooldown = 0;
    });
    
    state.effects = [];
  })),
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => set(produce(state => {
    state.players[playerId].position = position;
    state.players[playerId].velocity = velocity;
  })),
  
  updatePlayerState: (playerId: string, mechaState: MechaData['state']) => set(produce(state => {
    state.players[playerId].state = mechaState;
  })),
  
  updatePlayerFacing: (playerId: string, facingRight: boolean) => set(produce(state => {
    state.players[playerId].facingRight = facingRight;
  })),
  
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => set(produce(state => {
    state.players[playerId].isGrounded = isGrounded;
  })),
  
  updatePlayerDefending: (playerId: string, isDefending: boolean) => set(produce(state => {
    state.players[playerId].isDefending = isDefending;
  })),
  
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => set(produce(state => {
    state.players[playerId].attackCooldown = cooldown;
  })),
  
  dealDamage: (targetId: string, damage: number) => set(produce(state => {
    const target = state.players[targetId];
    const actualDamage = target.isDefending ? Math.floor(damage * 0.5) : damage;
    target.health = Math.max(0, target.health - actualDamage);
    
    // 添加受击特效
    state.effects.push({
      id: `hit-${Date.now()}`,
      type: 'HIT',
      position: { ...target.position },
      duration: 300,
      timer: 0,
      frame: 0,
    });
    
    // 检查游戏结束
    if (target.health === 0) {
      const winnerId = targetId === 'player1' ? 'player2' : 'player1';
      state.gameState.phase = 'RESULT';
      state.gameState.winner = winnerId;
      state.players[winnerId].score += 1;
    }
  })),
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => set(produce(state => {
    const player = state.players[playerId];
    player.animationTimer += deltaTime;
  })),
  
  setAnimationFrame: (playerId: string, frame: number) => set(produce(state => {
    state.players[playerId].animationFrame = frame;
  })),
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => {
    const id = `effect-${Date.now()}-${Math.random().toString(36).substr(2, 9)}`;
    set(produce(state => {
      state.effects.push({
        ...effect,
        id,
        timer: 0,
      });
    }));
    return id;
  },
  
  updateEffects: (deltaTime: number) => set(produce(state => {
    state.effects = state.effects.filter(effect => {
      effect.timer += deltaTime;
      return effect.timer < effect.duration;
    });
  })),
  
  removeEffect: (effectId: string) => set(produce(state => {
    state.effects = state.effects.filter(e => e.id !== effectId);
  })),
  
  clearEffects: () => set(produce(state => {
    state.effects = [];
  })),
  
  // 游戏时间
  tick: (deltaTime: number) => set(produce(state => {
    if (state.gameState.phase === 'PLAYING') {
      state.gameState.timeRemaining = Math.max(0, state.gameState.timeRemaining - deltaTime / 1000);
      
      // 时间到，判定胜负
      if (state.gameState.timeRemaining === 0) {
        const p1Health = state.players.player1.health;
        const p2Health = state.players.player2.health;
        
        if (p1Health > p2Health) {
          state.gameState.winner = 'player1';
          state.players.player1.score += 1;
        } else if (p2import { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
  winner: null,
};

// 游戏Store接口
interface GameStore {
  // 状态
  gameState: GameState;
  players: Record<string, MechaData>;
  effects: Effect[];
  
  // 游戏流程控制
  startGame: () => void;
  pauseGame: () => void;
  resumeGame: () => void;
  endGame: (winner: string | null) => void;
  resetGame: () => void;
  nextRound: () => void;
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => void;
  updatePlayerState: (playerId: string, state: MechaData['state']) => void;
  updatePlayerFacing: (playerId: string, facingRight: boolean) => void;
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => void;
  updatePlayerDefending: (playerId: string, isDefending: boolean) => void;
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => void;
  dealDamage: (targetId: string, damage: number, attackerId?: string) => void;
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => void;
  setAnimationFrame: (playerId: string, frame: number) => void;
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => string;
  updateEffects: (deltaTime: number) => void;
  removeEffect: (effectId: string) => void;
  clearEffects: () => void;
  
  // 游戏时间
  tick: (deltaTime: number) => void;
}

// 创建Game Store
export const useGameStore = create<GameStore>((set, get) => ({
  // 初始状态
  gameState: initialGameState,
  players: {
    player1: createInitialMecha('player1', 'BLUE', 200),
    player2: createInitialMecha('player2', 'RED', 550),
  },
  effects: [],
  
  // 游戏流程控制
  startGame: () => set(produce(state => {
    state.gameState.phase = 'PLAYING';
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.winner = null;
  })),
  
  pauseGame: () => set(produce(state => {
    if (state.gameState.phase === 'PLAYING') {
      state.gameState.phase = 'PAUSED';
    }
  })),
  
  resumeGame: () => set(produce(state => {
    if (state.gameState.phase === 'PAUSED') {
      state.gameState.phase = 'PLAYING';
    }
  })),
  
  endGame: (winner: string | null) => set(produce(state => {
    state.gameState.phase = 'RESULT';
    state.gameState.winner = winner;
    if (winner && state.players[winner]) {
      state.players[winner].score += 1;
    }
  })),
  
  resetGame: () => set(produce(state => {
    state.gameState = initialGameState;
    state.players = {
      player1: createInitialMecha('player1', 'BLUE', 200),
      player2: createInitialMecha('player2', 'RED', 550),
    };
    state.effects = [];
  })),
  
  nextRound: () => set(produce(state => {
    state.gameState.round += 1;
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.phase = 'PLAYING';
    state.gameState.winner = null;
    
    // 重置玩家位置和状态
    Object.keys(state.players).forEach(playerId => {
      const player = state.players[playerId];
      player.health = player.maxHealth;
      player.position = playerId === 'player1' ? { x: 200, y: 400 } : { x: 550, y: 400 };
      player.velocity = { x: 0, y: 0 };
      player.state = 'IDLE';
      player.isDefending = false;
      player.isGrounded = false;
      player.attackCooldown = 0;
    });
    
    state.effects = [];
  })),
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => set(produce(state => {
    state.players[playerId].position = position;
    state.players[playerId].velocity = velocity;
  })),
  
  updatePlayerState: (playerId: string, mechaState: MechaData['state']) => set(produce(state => {
    state.players[playerId].state = mechaState;
  })),
  
  updatePlayerFacing: (playerId: string, facingRight: boolean) => set(produce(state => {
    state.players[playerId].facingRight = facingRight;
  })),
  
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => set(produce(state => {
    state.players[playerId].isGrounded = isGrounded;
  })),
  
  updatePlayerDefending: (playerId: string, isDefending: boolean) => set(produce(state => {
    state.players[playerId].isDefending = isDefending;
  })),
  
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => set(produce(state => {
    state.players[playerId].attackCooldown = cooldown;
  })),
  
  dealDamage: (targetId: string, damage: number) => set(produce(state => {
    const target = state.players[targetId];
    const actualDamage = target.isDefending ? Math.floor(damage * 0.5) : damage;
    target.health = Math.max(0, target.health - actualDamage);
    
    // 添加受击特效
    state.effects.push({
      id: `hit-${Date.now()}`,
      type: 'HIT',
      position: { ...target.position },
      duration: 300,
      timer: 0,
      frame: 0,
    });
    
    // 检查游戏结束
    if (target.health === 0) {
      const winnerId = targetId === 'player1' ? 'player2' : 'player1';
      state.gameState.phase = 'RESULT';
      state.gameState.winner = winnerId;
      state.players[winnerId].score += 1;
    }
  })),
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => set(produce(state => {
    const player = state.players[playerId];
    player.animationTimer += deltaTime;
  })),
  
  setAnimationFrame: (playerId: string, frame: number) => set(produce(state => {
    state.players[playerId].animationFrame = frame;
  })),
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => {
    const id = `effect-${Date.now()}-${Math.random().toString(36).substr(2, 9)}`;
    set(produce(state => {
      state.effects.push({
        ...effect,
        id,
        timer: 0,
      });
    }));
    return id;
  },
  
  updateEffects: (deltaTime: number) => set(produce(state => {
    state.effects = state.effects.filter(effect => {
      effect.timer += deltaTime;
      return effect.timer < effect.duration;
    });
  })),
  
  removeEffect: (effectId: string) => set(produce(state => {
    state.effects = state.effects.filter(e => e.id !== effectId);
  })),
  
  clearEffects: () => set(produce(state => {
    state.effects = [];
  })),
  
  // 游戏时间
  tick: (deltaTime: number) => set(produce(state => {
    if (state.gameState.phase === 'PLAYING') {
      state.gameState.timeRemaining = Math.max(0, state.gameState.timeRemaining - deltaTime / 1000);
      
      // 时间到，判定胜负
      if (state.gameState.timeRemaining === 0) {
        const p1Health = state.players.player1.health;
        const p2Health = state.players.player2.health;
        
        if (p1Health > p2Health) {
          state.gameState.winner = 'player1';
          state.players.player1.score += 1;
        } else if (p2Health > p1Health) {
import { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
  winner: null,
};

// 游戏Store接口
interface GameStore {
  // 状态
  gameState: GameState;
  players: Record<string, MechaData>;
  effects: Effect[];
  
  // 游戏流程控制
  startGame: () => void;
  pauseGame: () => void;
  resumeGame: () => void;
  endGame: (winner: string | null) => void;
  resetGame: () => void;
  nextRound: () => void;
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => void;
  updatePlayerState: (playerId: string, state: MechaData['state']) => void;
  updatePlayerFacing: (playerId: string, facingRight: boolean) => void;
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => void;
  updatePlayerDefending: (playerId: string, isDefending: boolean) => void;
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => void;
  dealDamage: (targetId: string, damage: number, attackerId?: string) => void;
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => void;
  setAnimationFrame: (playerId: string, frame: number) => void;
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => string;
  updateEffects: (deltaTime: number) => void;
  removeEffect: (effectId: string) => void;
  clearEffects: () => void;
  
  // 游戏时间
  tick: (deltaTime: number) => void;
}

// 创建Game Store
export const useGameStore = create<GameStore>((set, get) => ({
  // 初始状态
  gameState: initialGameState,
  players: {
    player1: createInitialMecha('player1', 'BLUE', 200),
    player2: createInitialMecha('player2', 'RED', 550),
  },
  effects: [],
  
  // 游戏流程控制
  startGame: () => set(produce(state => {
    state.gameState.phase = 'PLAYING';
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.winner = null;
  })),
  
  pauseGame: () => set(produce(state => {
    if (state.gameState.phase === 'PLAYING') {
      state.gameState.phase = 'PAUSED';
    }
  })),
  
  resumeGame: () => set(produce(state => {
    if (state.gameState.phase === 'PAUSED') {
      state.gameState.phase = 'PLAYING';
    }
  })),
  
  endGame: (winner: string | null) => set(produce(state => {
    state.gameState.phase = 'RESULT';
    state.gameState.winner = winner;
    if (winner && state.players[winner]) {
      state.players[winner].score += 1;
    }
  })),
  
  resetGame: () => set(produce(state => {
    state.gameState = initialGameState;
    state.players = {
      player1: createInitialMecha('player1', 'BLUE', 200),
      player2: createInitialMecha('player2', 'RED', 550),
    };
    state.effects = [];
  })),
  
  nextRound: () => set(produce(state => {
    state.gameState.round += 1;
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.phase = 'PLAYING';
    state.gameState.winner = null;
    
    // 重置玩家位置和状态
    Object.keys(state.players).forEach(playerId => {
      const player = state.players[playerId];
      player.health = player.maxHealth;
      player.position = playerId === 'player1' ? { x: 200, y: 400 } : { x: 550, y: 400 };
      player.velocity = { x: 0, y: 0 };
      player.state = 'IDLE';
      player.isDefending = false;
      player.isGrounded = false;
      player.attackCooldown = 0;
    });
    
    state.effects = [];
  })),
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => set(produce(state => {
    state.players[playerId].position = position;
    state.players[playerId].velocity = velocity;
  })),
  
  updatePlayerState: (playerId: string, mechaState: MechaData['state']) => set(produce(state => {
    state.players[playerId].state = mechaState;
  })),
  
  updatePlayerFacing: (playerId: string, facingRight: boolean) => set(produce(state => {
    state.players[playerId].facingRight = facingRight;
  })),
  
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => set(produce(state => {
    state.players[playerId].isGrounded = isGrounded;
  })),
  
  updatePlayerDefending: (playerId: string, isDefending: boolean) => set(produce(state => {
    state.players[playerId].isDefending = isDefending;
  })),
  
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => set(produce(state => {
    state.players[playerId].attackCooldown = cooldown;
  })),
  
  dealDamage: (targetId: string, damage: number) => set(produce(state => {
    const target = state.players[targetId];
    const actualDamage = target.isDefending ? Math.floor(damage * 0.5) : damage;
    target.health = Math.max(0, target.health - actualDamage);
    
    // 添加受击特效
    state.effects.push({
      id: `hit-${Date.now()}`,
      type: 'HIT',
      position: { ...target.position },
      duration: 300,
      timer: 0,
      frame: 0,
    });
    
    // 检查游戏结束
    if (target.health === 0) {
      const winnerId = targetId === 'player1' ? 'player2' : 'player1';
      state.gameState.phase = 'RESULT';
      state.gameState.winner = winnerId;
      state.players[winnerId].score += 1;
    }
  })),
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => set(produce(state => {
    const player = state.players[playerId];
    player.animationTimer += deltaTime;
  })),
  
  setAnimationFrame: (playerId: string, frame: number) => set(produce(state => {
    state.players[playerId].animationFrame = frame;
  })),
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => {
    const id = `effect-${Date.now()}-${Math.random().toString(36).substr(2, 9)}`;
    set(produce(state => {
      state.effects.push({
        ...effect,
        id,
        timer: 0,
      });
    }));
    return id;
  },
  
  updateEffects: (deltaTime: number) => set(produce(state => {
    state.effects = state.effects.filter(effect => {
      effect.timer += deltaTime;
      return effect.timer < effect.duration;
    });
  })),
  
  removeEffect: (effectId: string) => set(produce(state => {
    state.effects = state.effects.filter(e => e.id !== effectId);
  })),
  
  clearEffects: () => set(produce(state => {
    state.effects = [];
  })),
  
  // 游戏时间
  tick: (deltaTime: number) => set(produce(state => {
    if (state.gameState.phase === 'PLAYING') {
      state.gameState.timeRemaining = Math.max(0, state.gameState.timeRemaining - deltaTime / 1000);
      
      // 时间到，判定胜负
      if (state.gameState.timeRemaining === 0) {
        const p1Health = state.players.player1.health;
        const p2Health = state.players.player2.health;
        
        if (p1Health > p2Health) {
          state.gameState.winner = 'player1';
          state.players.player1.score += 1;
        } else if (p2Health > p1Health) {
          state.gameState.winner =import { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
  winner: null,
};

// 游戏Store接口
interface GameStore {
  // 状态
  gameState: GameState;
  players: Record<string, MechaData>;
  effects: Effect[];
  
  // 游戏流程控制
  startGame: () => void;
  pauseGame: () => void;
  resumeGame: () => void;
  endGame: (winner: string | null) => void;
  resetGame: () => void;
  nextRound: () => void;
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => void;
  updatePlayerState: (playerId: string, state: MechaData['state']) => void;
  updatePlayerFacing: (playerId: string, facingRight: boolean) => void;
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => void;
  updatePlayerDefending: (playerId: string, isDefending: boolean) => void;
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => void;
  dealDamage: (targetId: string, damage: number, attackerId?: string) => void;
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => void;
  setAnimationFrame: (playerId: string, frame: number) => void;
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => string;
  updateEffects: (deltaTime: number) => void;
  removeEffect: (effectId: string) => void;
  clearEffects: () => void;
  
  // 游戏时间
  tick: (deltaTime: number) => void;
}

// 创建Game Store
export const useGameStore = create<GameStore>((set, get) => ({
  // 初始状态
  gameState: initialGameState,
  players: {
    player1: createInitialMecha('player1', 'BLUE', 200),
    player2: createInitialMecha('player2', 'RED', 550),
  },
  effects: [],
  
  // 游戏流程控制
  startGame: () => set(produce(state => {
    state.gameState.phase = 'PLAYING';
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.winner = null;
  })),
  
  pauseGame: () => set(produce(state => {
    if (state.gameState.phase === 'PLAYING') {
      state.gameState.phase = 'PAUSED';
    }
  })),
  
  resumeGame: () => set(produce(state => {
    if (state.gameState.phase === 'PAUSED') {
      state.gameState.phase = 'PLAYING';
    }
  })),
  
  endGame: (winner: string | null) => set(produce(state => {
    state.gameState.phase = 'RESULT';
    state.gameState.winner = winner;
    if (winner && state.players[winner]) {
      state.players[winner].score += 1;
    }
  })),
  
  resetGame: () => set(produce(state => {
    state.gameState = initialGameState;
    state.players = {
      player1: createInitialMecha('player1', 'BLUE', 200),
      player2: createInitialMecha('player2', 'RED', 550),
    };
    state.effects = [];
  })),
  
  nextRound: () => set(produce(state => {
    state.gameState.round += 1;
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.phase = 'PLAYING';
    state.gameState.winner = null;
    
    // 重置玩家位置和状态
    Object.keys(state.players).forEach(playerId => {
      const player = state.players[playerId];
      player.health = player.maxHealth;
      player.position = playerId === 'player1' ? { x: 200, y: 400 } : { x: 550, y: 400 };
      player.velocity = { x: 0, y: 0 };
      player.state = 'IDLE';
      player.isDefending = false;
      player.isGrounded = false;
      player.attackCooldown = 0;
    });
    
    state.effects = [];
  })),
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => set(produce(state => {
    state.players[playerId].position = position;
    state.players[playerId].velocity = velocity;
  })),
  
  updatePlayerState: (playerId: string, mechaState: MechaData['state']) => set(produce(state => {
    state.players[playerId].state = mechaState;
  })),
  
  updatePlayerFacing: (playerId: string, facingRight: boolean) => set(produce(state => {
    state.players[playerId].facingRight = facingRight;
  })),
  
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => set(produce(state => {
    state.players[playerId].isGrounded = isGrounded;
  })),
  
  updatePlayerDefending: (playerId: string, isDefending: boolean) => set(produce(state => {
    state.players[playerId].isDefending = isDefending;
  })),
  
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => set(produce(state => {
    state.players[playerId].attackCooldown = cooldown;
  })),
  
  dealDamage: (targetId: string, damage: number) => set(produce(state => {
    const target = state.players[targetId];
    const actualDamage = target.isDefending ? Math.floor(damage * 0.5) : damage;
    target.health = Math.max(0, target.health - actualDamage);
    
    // 添加受击特效
    state.effects.push({
      id: `hit-${Date.now()}`,
      type: 'HIT',
      position: { ...target.position },
      duration: 300,
      timer: 0,
      frame: 0,
    });
    
    // 检查游戏结束
    if (target.health === 0) {
      const winnerId = targetId === 'player1' ? 'player2' : 'player1';
      state.gameState.phase = 'RESULT';
      state.gameState.winner = winnerId;
      state.players[winnerId].score += 1;
    }
  })),
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => set(produce(state => {
    const player = state.players[playerId];
    player.animationTimer += deltaTime;
  })),
  
  setAnimationFrame: (playerId: string, frame: number) => set(produce(state => {
    state.players[playerId].animationFrame = frame;
  })),
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => {
    const id = `effect-${Date.now()}-${Math.random().toString(36).substr(2, 9)}`;
    set(produce(state => {
      state.effects.push({
        ...effect,
        id,
        timer: 0,
      });
    }));
    return id;
  },
  
  updateEffects: (deltaTime: number) => set(produce(state => {
    state.effects = state.effects.filter(effect => {
      effect.timer += deltaTime;
      return effect.timer < effect.duration;
    });
  })),
  
  removeEffect: (effectId: string) => set(produce(state => {
    state.effects = state.effects.filter(e => e.id !== effectId);
  })),
  
  clearEffects: () => set(produce(state => {
    state.effects = [];
  })),
  
  // 游戏时间
  tick: (deltaTime: number) => set(produce(state => {
    if (state.gameState.phase === 'PLAYING') {
      state.gameState.timeRemaining = Math.max(0, state.gameState.timeRemaining - deltaTime / 1000);
      
      // 时间到，判定胜负
      if (state.gameState.timeRemaining === 0) {
        const p1Health = state.players.player1.health;
        const p2Health = state.players.player2.health;
        
        if (p1Health > p2Health) {
          state.gameState.winner = 'player1';
          state.players.player1.score += 1;
        } else if (p2Health > p1Health) {
          state.gameState.winner = 'player2';
          stateimport { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
  winner: null,
};

// 游戏Store接口
interface GameStore {
  // 状态
  gameState: GameState;
  players: Record<string, MechaData>;
  effects: Effect[];
  
  // 游戏流程控制
  startGame: () => void;
  pauseGame: () => void;
  resumeGame: () => void;
  endGame: (winner: string | null) => void;
  resetGame: () => void;
  nextRound: () => void;
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => void;
  updatePlayerState: (playerId: string, state: MechaData['state']) => void;
  updatePlayerFacing: (playerId: string, facingRight: boolean) => void;
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => void;
  updatePlayerDefending: (playerId: string, isDefending: boolean) => void;
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => void;
  dealDamage: (targetId: string, damage: number, attackerId?: string) => void;
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => void;
  setAnimationFrame: (playerId: string, frame: number) => void;
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => string;
  updateEffects: (deltaTime: number) => void;
  removeEffect: (effectId: string) => void;
  clearEffects: () => void;
  
  // 游戏时间
  tick: (deltaTime: number) => void;
}

// 创建Game Store
export const useGameStore = create<GameStore>((set, get) => ({
  // 初始状态
  gameState: initialGameState,
  players: {
    player1: createInitialMecha('player1', 'BLUE', 200),
    player2: createInitialMecha('player2', 'RED', 550),
  },
  effects: [],
  
  // 游戏流程控制
  startGame: () => set(produce(state => {
    state.gameState.phase = 'PLAYING';
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.winner = null;
  })),
  
  pauseGame: () => set(produce(state => {
    if (state.gameState.phase === 'PLAYING') {
      state.gameState.phase = 'PAUSED';
    }
  })),
  
  resumeGame: () => set(produce(state => {
    if (state.gameState.phase === 'PAUSED') {
      state.gameState.phase = 'PLAYING';
    }
  })),
  
  endGame: (winner: string | null) => set(produce(state => {
    state.gameState.phase = 'RESULT';
    state.gameState.winner = winner;
    if (winner && state.players[winner]) {
      state.players[winner].score += 1;
    }
  })),
  
  resetGame: () => set(produce(state => {
    state.gameState = initialGameState;
    state.players = {
      player1: createInitialMecha('player1', 'BLUE', 200),
      player2: createInitialMecha('player2', 'RED', 550),
    };
    state.effects = [];
  })),
  
  nextRound: () => set(produce(state => {
    state.gameState.round += 1;
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.phase = 'PLAYING';
    state.gameState.winner = null;
    
    // 重置玩家位置和状态
    Object.keys(state.players).forEach(playerId => {
      const player = state.players[playerId];
      player.health = player.maxHealth;
      player.position = playerId === 'player1' ? { x: 200, y: 400 } : { x: 550, y: 400 };
      player.velocity = { x: 0, y: 0 };
      player.state = 'IDLE';
      player.isDefending = false;
      player.isGrounded = false;
      player.attackCooldown = 0;
    });
    
    state.effects = [];
  })),
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => set(produce(state => {
    state.players[playerId].position = position;
    state.players[playerId].velocity = velocity;
  })),
  
  updatePlayerState: (playerId: string, mechaState: MechaData['state']) => set(produce(state => {
    state.players[playerId].state = mechaState;
  })),
  
  updatePlayerFacing: (playerId: string, facingRight: boolean) => set(produce(state => {
    state.players[playerId].facingRight = facingRight;
  })),
  
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => set(produce(state => {
    state.players[playerId].isGrounded = isGrounded;
  })),
  
  updatePlayerDefending: (playerId: string, isDefending: boolean) => set(produce(state => {
    state.players[playerId].isDefending = isDefending;
  })),
  
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => set(produce(state => {
    state.players[playerId].attackCooldown = cooldown;
  })),
  
  dealDamage: (targetId: string, damage: number) => set(produce(state => {
    const target = state.players[targetId];
    const actualDamage = target.isDefending ? Math.floor(damage * 0.5) : damage;
    target.health = Math.max(0, target.health - actualDamage);
    
    // 添加受击特效
    state.effects.push({
      id: `hit-${Date.now()}`,
      type: 'HIT',
      position: { ...target.position },
      duration: 300,
      timer: 0,
      frame: 0,
    });
    
    // 检查游戏结束
    if (target.health === 0) {
      const winnerId = targetId === 'player1' ? 'player2' : 'player1';
      state.gameState.phase = 'RESULT';
      state.gameState.winner = winnerId;
      state.players[winnerId].score += 1;
    }
  })),
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => set(produce(state => {
    const player = state.players[playerId];
    player.animationTimer += deltaTime;
  })),
  
  setAnimationFrame: (playerId: string, frame: number) => set(produce(state => {
    state.players[playerId].animationFrame = frame;
  })),
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => {
    const id = `effect-${Date.now()}-${Math.random().toString(36).substr(2, 9)}`;
    set(produce(state => {
      state.effects.push({
        ...effect,
        id,
        timer: 0,
      });
    }));
    return id;
  },
  
  updateEffects: (deltaTime: number) => set(produce(state => {
    state.effects = state.effects.filter(effect => {
      effect.timer += deltaTime;
      return effect.timer < effect.duration;
    });
  })),
  
  removeEffect: (effectId: string) => set(produce(state => {
    state.effects = state.effects.filter(e => e.id !== effectId);
  })),
  
  clearEffects: () => set(produce(state => {
    state.effects = [];
  })),
  
  // 游戏时间
  tick: (deltaTime: number) => set(produce(state => {
    if (state.gameState.phase === 'PLAYING') {
      state.gameState.timeRemaining = Math.max(0, state.gameState.timeRemaining - deltaTime / 1000);
      
      // 时间到，判定胜负
      if (state.gameState.timeRemaining === 0) {
        const p1Health = state.players.player1.health;
        const p2Health = state.players.player2.health;
        
        if (p1Health > p2Health) {
          state.gameState.winner = 'player1';
          state.players.player1.score += 1;
        } else if (p2Health > p1Health) {
          state.gameState.winner = 'player2';
          state.players.player2.score += 1import { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
  winner: null,
};

// 游戏Store接口
interface GameStore {
  // 状态
  gameState: GameState;
  players: Record<string, MechaData>;
  effects: Effect[];
  
  // 游戏流程控制
  startGame: () => void;
  pauseGame: () => void;
  resumeGame: () => void;
  endGame: (winner: string | null) => void;
  resetGame: () => void;
  nextRound: () => void;
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => void;
  updatePlayerState: (playerId: string, state: MechaData['state']) => void;
  updatePlayerFacing: (playerId: string, facingRight: boolean) => void;
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => void;
  updatePlayerDefending: (playerId: string, isDefending: boolean) => void;
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => void;
  dealDamage: (targetId: string, damage: number, attackerId?: string) => void;
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => void;
  setAnimationFrame: (playerId: string, frame: number) => void;
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => string;
  updateEffects: (deltaTime: number) => void;
  removeEffect: (effectId: string) => void;
  clearEffects: () => void;
  
  // 游戏时间
  tick: (deltaTime: number) => void;
}

// 创建Game Store
export const useGameStore = create<GameStore>((set, get) => ({
  // 初始状态
  gameState: initialGameState,
  players: {
    player1: createInitialMecha('player1', 'BLUE', 200),
    player2: createInitialMecha('player2', 'RED', 550),
  },
  effects: [],
  
  // 游戏流程控制
  startGame: () => set(produce(state => {
    state.gameState.phase = 'PLAYING';
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.winner = null;
  })),
  
  pauseGame: () => set(produce(state => {
    if (state.gameState.phase === 'PLAYING') {
      state.gameState.phase = 'PAUSED';
    }
  })),
  
  resumeGame: () => set(produce(state => {
    if (state.gameState.phase === 'PAUSED') {
      state.gameState.phase = 'PLAYING';
    }
  })),
  
  endGame: (winner: string | null) => set(produce(state => {
    state.gameState.phase = 'RESULT';
    state.gameState.winner = winner;
    if (winner && state.players[winner]) {
      state.players[winner].score += 1;
    }
  })),
  
  resetGame: () => set(produce(state => {
    state.gameState = initialGameState;
    state.players = {
      player1: createInitialMecha('player1', 'BLUE', 200),
      player2: createInitialMecha('player2', 'RED', 550),
    };
    state.effects = [];
  })),
  
  nextRound: () => set(produce(state => {
    state.gameState.round += 1;
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.phase = 'PLAYING';
    state.gameState.winner = null;
    
    // 重置玩家位置和状态
    Object.keys(state.players).forEach(playerId => {
      const player = state.players[playerId];
      player.health = player.maxHealth;
      player.position = playerId === 'player1' ? { x: 200, y: 400 } : { x: 550, y: 400 };
      player.velocity = { x: 0, y: 0 };
      player.state = 'IDLE';
      player.isDefending = false;
      player.isGrounded = false;
      player.attackCooldown = 0;
    });
    
    state.effects = [];
  })),
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => set(produce(state => {
    state.players[playerId].position = position;
    state.players[playerId].velocity = velocity;
  })),
  
  updatePlayerState: (playerId: string, mechaState: MechaData['state']) => set(produce(state => {
    state.players[playerId].state = mechaState;
  })),
  
  updatePlayerFacing: (playerId: string, facingRight: boolean) => set(produce(state => {
    state.players[playerId].facingRight = facingRight;
  })),
  
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => set(produce(state => {
    state.players[playerId].isGrounded = isGrounded;
  })),
  
  updatePlayerDefending: (playerId: string, isDefending: boolean) => set(produce(state => {
    state.players[playerId].isDefending = isDefending;
  })),
  
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => set(produce(state => {
    state.players[playerId].attackCooldown = cooldown;
  })),
  
  dealDamage: (targetId: string, damage: number) => set(produce(state => {
    const target = state.players[targetId];
    const actualDamage = target.isDefending ? Math.floor(damage * 0.5) : damage;
    target.health = Math.max(0, target.health - actualDamage);
    
    // 添加受击特效
    state.effects.push({
      id: `hit-${Date.now()}`,
      type: 'HIT',
      position: { ...target.position },
      duration: 300,
      timer: 0,
      frame: 0,
    });
    
    // 检查游戏结束
    if (target.health === 0) {
      const winnerId = targetId === 'player1' ? 'player2' : 'player1';
      state.gameState.phase = 'RESULT';
      state.gameState.winner = winnerId;
      state.players[winnerId].score += 1;
    }
  })),
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => set(produce(state => {
    const player = state.players[playerId];
    player.animationTimer += deltaTime;
  })),
  
  setAnimationFrame: (playerId: string, frame: number) => set(produce(state => {
    state.players[playerId].animationFrame = frame;
  })),
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => {
    const id = `effect-${Date.now()}-${Math.random().toString(36).substr(2, 9)}`;
    set(produce(state => {
      state.effects.push({
        ...effect,
        id,
        timer: 0,
      });
    }));
    return id;
  },
  
  updateEffects: (deltaTime: number) => set(produce(state => {
    state.effects = state.effects.filter(effect => {
      effect.timer += deltaTime;
      return effect.timer < effect.duration;
    });
  })),
  
  removeEffect: (effectId: string) => set(produce(state => {
    state.effects = state.effects.filter(e => e.id !== effectId);
  })),
  
  clearEffects: () => set(produce(state => {
    state.effects = [];
  })),
  
  // 游戏时间
  tick: (deltaTime: number) => set(produce(state => {
    if (state.gameState.phase === 'PLAYING') {
      state.gameState.timeRemaining = Math.max(0, state.gameState.timeRemaining - deltaTime / 1000);
      
      // 时间到，判定胜负
      if (state.gameState.timeRemaining === 0) {
        const p1Health = state.players.player1.health;
        const p2Health = state.players.player2.health;
        
        if (p1Health > p2Health) {
          state.gameState.winner = 'player1';
          state.players.player1.score += 1;
        } else if (p2Health > p1Health) {
          state.gameState.winner = 'player2';
          state.players.player2.score += 1;
        }
        //import { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
  winner: null,
};

// 游戏Store接口
interface GameStore {
  // 状态
  gameState: GameState;
  players: Record<string, MechaData>;
  effects: Effect[];
  
  // 游戏流程控制
  startGame: () => void;
  pauseGame: () => void;
  resumeGame: () => void;
  endGame: (winner: string | null) => void;
  resetGame: () => void;
  nextRound: () => void;
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => void;
  updatePlayerState: (playerId: string, state: MechaData['state']) => void;
  updatePlayerFacing: (playerId: string, facingRight: boolean) => void;
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => void;
  updatePlayerDefending: (playerId: string, isDefending: boolean) => void;
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => void;
  dealDamage: (targetId: string, damage: number, attackerId?: string) => void;
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => void;
  setAnimationFrame: (playerId: string, frame: number) => void;
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => string;
  updateEffects: (deltaTime: number) => void;
  removeEffect: (effectId: string) => void;
  clearEffects: () => void;
  
  // 游戏时间
  tick: (deltaTime: number) => void;
}

// 创建Game Store
export const useGameStore = create<GameStore>((set, get) => ({
  // 初始状态
  gameState: initialGameState,
  players: {
    player1: createInitialMecha('player1', 'BLUE', 200),
    player2: createInitialMecha('player2', 'RED', 550),
  },
  effects: [],
  
  // 游戏流程控制
  startGame: () => set(produce(state => {
    state.gameState.phase = 'PLAYING';
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.winner = null;
  })),
  
  pauseGame: () => set(produce(state => {
    if (state.gameState.phase === 'PLAYING') {
      state.gameState.phase = 'PAUSED';
    }
  })),
  
  resumeGame: () => set(produce(state => {
    if (state.gameState.phase === 'PAUSED') {
      state.gameState.phase = 'PLAYING';
    }
  })),
  
  endGame: (winner: string | null) => set(produce(state => {
    state.gameState.phase = 'RESULT';
    state.gameState.winner = winner;
    if (winner && state.players[winner]) {
      state.players[winner].score += 1;
    }
  })),
  
  resetGame: () => set(produce(state => {
    state.gameState = initialGameState;
    state.players = {
      player1: createInitialMecha('player1', 'BLUE', 200),
      player2: createInitialMecha('player2', 'RED', 550),
    };
    state.effects = [];
  })),
  
  nextRound: () => set(produce(state => {
    state.gameState.round += 1;
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.phase = 'PLAYING';
    state.gameState.winner = null;
    
    // 重置玩家位置和状态
    Object.keys(state.players).forEach(playerId => {
      const player = state.players[playerId];
      player.health = player.maxHealth;
      player.position = playerId === 'player1' ? { x: 200, y: 400 } : { x: 550, y: 400 };
      player.velocity = { x: 0, y: 0 };
      player.state = 'IDLE';
      player.isDefending = false;
      player.isGrounded = false;
      player.attackCooldown = 0;
    });
    
    state.effects = [];
  })),
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => set(produce(state => {
    state.players[playerId].position = position;
    state.players[playerId].velocity = velocity;
  })),
  
  updatePlayerState: (playerId: string, mechaState: MechaData['state']) => set(produce(state => {
    state.players[playerId].state = mechaState;
  })),
  
  updatePlayerFacing: (playerId: string, facingRight: boolean) => set(produce(state => {
    state.players[playerId].facingRight = facingRight;
  })),
  
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => set(produce(state => {
    state.players[playerId].isGrounded = isGrounded;
  })),
  
  updatePlayerDefending: (playerId: string, isDefending: boolean) => set(produce(state => {
    state.players[playerId].isDefending = isDefending;
  })),
  
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => set(produce(state => {
    state.players[playerId].attackCooldown = cooldown;
  })),
  
  dealDamage: (targetId: string, damage: number) => set(produce(state => {
    const target = state.players[targetId];
    const actualDamage = target.isDefending ? Math.floor(damage * 0.5) : damage;
    target.health = Math.max(0, target.health - actualDamage);
    
    // 添加受击特效
    state.effects.push({
      id: `hit-${Date.now()}`,
      type: 'HIT',
      position: { ...target.position },
      duration: 300,
      timer: 0,
      frame: 0,
    });
    
    // 检查游戏结束
    if (target.health === 0) {
      const winnerId = targetId === 'player1' ? 'player2' : 'player1';
      state.gameState.phase = 'RESULT';
      state.gameState.winner = winnerId;
      state.players[winnerId].score += 1;
    }
  })),
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => set(produce(state => {
    const player = state.players[playerId];
    player.animationTimer += deltaTime;
  })),
  
  setAnimationFrame: (playerId: string, frame: number) => set(produce(state => {
    state.players[playerId].animationFrame = frame;
  })),
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => {
    const id = `effect-${Date.now()}-${Math.random().toString(36).substr(2, 9)}`;
    set(produce(state => {
      state.effects.push({
        ...effect,
        id,
        timer: 0,
      });
    }));
    return id;
  },
  
  updateEffects: (deltaTime: number) => set(produce(state => {
    state.effects = state.effects.filter(effect => {
      effect.timer += deltaTime;
      return effect.timer < effect.duration;
    });
  })),
  
  removeEffect: (effectId: string) => set(produce(state => {
    state.effects = state.effects.filter(e => e.id !== effectId);
  })),
  
  clearEffects: () => set(produce(state => {
    state.effects = [];
  })),
  
  // 游戏时间
  tick: (deltaTime: number) => set(produce(state => {
    if (state.gameState.phase === 'PLAYING') {
      state.gameState.timeRemaining = Math.max(0, state.gameState.timeRemaining - deltaTime / 1000);
      
      // 时间到，判定胜负
      if (state.gameState.timeRemaining === 0) {
        const p1Health = state.players.player1.health;
        const p2Health = state.players.player2.health;
        
        if (p1Health > p2Health) {
          state.gameState.winner = 'player1';
          state.players.player1.score += 1;
        } else if (p2Health > p1Health) {
          state.gameState.winner = 'player2';
          state.players.player2.score += 1;
        }
        // 平局不加分
        
import { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
  winner: null,
};

// 游戏Store接口
interface GameStore {
  // 状态
  gameState: GameState;
  players: Record<string, MechaData>;
  effects: Effect[];
  
  // 游戏流程控制
  startGame: () => void;
  pauseGame: () => void;
  resumeGame: () => void;
  endGame: (winner: string | null) => void;
  resetGame: () => void;
  nextRound: () => void;
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => void;
  updatePlayerState: (playerId: string, state: MechaData['state']) => void;
  updatePlayerFacing: (playerId: string, facingRight: boolean) => void;
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => void;
  updatePlayerDefending: (playerId: string, isDefending: boolean) => void;
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => void;
  dealDamage: (targetId: string, damage: number, attackerId?: string) => void;
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => void;
  setAnimationFrame: (playerId: string, frame: number) => void;
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => string;
  updateEffects: (deltaTime: number) => void;
  removeEffect: (effectId: string) => void;
  clearEffects: () => void;
  
  // 游戏时间
  tick: (deltaTime: number) => void;
}

// 创建Game Store
export const useGameStore = create<GameStore>((set, get) => ({
  // 初始状态
  gameState: initialGameState,
  players: {
    player1: createInitialMecha('player1', 'BLUE', 200),
    player2: createInitialMecha('player2', 'RED', 550),
  },
  effects: [],
  
  // 游戏流程控制
  startGame: () => set(produce(state => {
    state.gameState.phase = 'PLAYING';
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.winner = null;
  })),
  
  pauseGame: () => set(produce(state => {
    if (state.gameState.phase === 'PLAYING') {
      state.gameState.phase = 'PAUSED';
    }
  })),
  
  resumeGame: () => set(produce(state => {
    if (state.gameState.phase === 'PAUSED') {
      state.gameState.phase = 'PLAYING';
    }
  })),
  
  endGame: (winner: string | null) => set(produce(state => {
    state.gameState.phase = 'RESULT';
    state.gameState.winner = winner;
    if (winner && state.players[winner]) {
      state.players[winner].score += 1;
    }
  })),
  
  resetGame: () => set(produce(state => {
    state.gameState = initialGameState;
    state.players = {
      player1: createInitialMecha('player1', 'BLUE', 200),
      player2: createInitialMecha('player2', 'RED', 550),
    };
    state.effects = [];
  })),
  
  nextRound: () => set(produce(state => {
    state.gameState.round += 1;
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.phase = 'PLAYING';
    state.gameState.winner = null;
    
    // 重置玩家位置和状态
    Object.keys(state.players).forEach(playerId => {
      const player = state.players[playerId];
      player.health = player.maxHealth;
      player.position = playerId === 'player1' ? { x: 200, y: 400 } : { x: 550, y: 400 };
      player.velocity = { x: 0, y: 0 };
      player.state = 'IDLE';
      player.isDefending = false;
      player.isGrounded = false;
      player.attackCooldown = 0;
    });
    
    state.effects = [];
  })),
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => set(produce(state => {
    state.players[playerId].position = position;
    state.players[playerId].velocity = velocity;
  })),
  
  updatePlayerState: (playerId: string, mechaState: MechaData['state']) => set(produce(state => {
    state.players[playerId].state = mechaState;
  })),
  
  updatePlayerFacing: (playerId: string, facingRight: boolean) => set(produce(state => {
    state.players[playerId].facingRight = facingRight;
  })),
  
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => set(produce(state => {
    state.players[playerId].isGrounded = isGrounded;
  })),
  
  updatePlayerDefending: (playerId: string, isDefending: boolean) => set(produce(state => {
    state.players[playerId].isDefending = isDefending;
  })),
  
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => set(produce(state => {
    state.players[playerId].attackCooldown = cooldown;
  })),
  
  dealDamage: (targetId: string, damage: number) => set(produce(state => {
    const target = state.players[targetId];
    const actualDamage = target.isDefending ? Math.floor(damage * 0.5) : damage;
    target.health = Math.max(0, target.health - actualDamage);
    
    // 添加受击特效
    state.effects.push({
      id: `hit-${Date.now()}`,
      type: 'HIT',
      position: { ...target.position },
      duration: 300,
      timer: 0,
      frame: 0,
    });
    
    // 检查游戏结束
    if (target.health === 0) {
      const winnerId = targetId === 'player1' ? 'player2' : 'player1';
      state.gameState.phase = 'RESULT';
      state.gameState.winner = winnerId;
      state.players[winnerId].score += 1;
    }
  })),
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => set(produce(state => {
    const player = state.players[playerId];
    player.animationTimer += deltaTime;
  })),
  
  setAnimationFrame: (playerId: string, frame: number) => set(produce(state => {
    state.players[playerId].animationFrame = frame;
  })),
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => {
    const id = `effect-${Date.now()}-${Math.random().toString(36).substr(2, 9)}`;
    set(produce(state => {
      state.effects.push({
        ...effect,
        id,
        timer: 0,
      });
    }));
    return id;
  },
  
  updateEffects: (deltaTime: number) => set(produce(state => {
    state.effects = state.effects.filter(effect => {
      effect.timer += deltaTime;
      return effect.timer < effect.duration;
    });
  })),
  
  removeEffect: (effectId: string) => set(produce(state => {
    state.effects = state.effects.filter(e => e.id !== effectId);
  })),
  
  clearEffects: () => set(produce(state => {
    state.effects = [];
  })),
  
  // 游戏时间
  tick: (deltaTime: number) => set(produce(state => {
    if (state.gameState.phase === 'PLAYING') {
      state.gameState.timeRemaining = Math.max(0, state.gameState.timeRemaining - deltaTime / 1000);
      
      // 时间到，判定胜负
      if (state.gameState.timeRemaining === 0) {
        const p1Health = state.players.player1.health;
        const p2Health = state.players.player2.health;
        
        if (p1Health > p2Health) {
          state.gameState.winner = 'player1';
          state.players.player1.score += 1;
        } else if (p2Health > p1Health) {
          state.gameState.winner = 'player2';
          state.players.player2.score += 1;
        }
        // 平局不加分
        
        state.gameState.phase = 'import { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
  winner: null,
};

// 游戏Store接口
interface GameStore {
  // 状态
  gameState: GameState;
  players: Record<string, MechaData>;
  effects: Effect[];
  
  // 游戏流程控制
  startGame: () => void;
  pauseGame: () => void;
  resumeGame: () => void;
  endGame: (winner: string | null) => void;
  resetGame: () => void;
  nextRound: () => void;
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => void;
  updatePlayerState: (playerId: string, state: MechaData['state']) => void;
  updatePlayerFacing: (playerId: string, facingRight: boolean) => void;
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => void;
  updatePlayerDefending: (playerId: string, isDefending: boolean) => void;
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => void;
  dealDamage: (targetId: string, damage: number, attackerId?: string) => void;
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => void;
  setAnimationFrame: (playerId: string, frame: number) => void;
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => string;
  updateEffects: (deltaTime: number) => void;
  removeEffect: (effectId: string) => void;
  clearEffects: () => void;
  
  // 游戏时间
  tick: (deltaTime: number) => void;
}

// 创建Game Store
export const useGameStore = create<GameStore>((set, get) => ({
  // 初始状态
  gameState: initialGameState,
  players: {
    player1: createInitialMecha('player1', 'BLUE', 200),
    player2: createInitialMecha('player2', 'RED', 550),
  },
  effects: [],
  
  // 游戏流程控制
  startGame: () => set(produce(state => {
    state.gameState.phase = 'PLAYING';
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.winner = null;
  })),
  
  pauseGame: () => set(produce(state => {
    if (state.gameState.phase === 'PLAYING') {
      state.gameState.phase = 'PAUSED';
    }
  })),
  
  resumeGame: () => set(produce(state => {
    if (state.gameState.phase === 'PAUSED') {
      state.gameState.phase = 'PLAYING';
    }
  })),
  
  endGame: (winner: string | null) => set(produce(state => {
    state.gameState.phase = 'RESULT';
    state.gameState.winner = winner;
    if (winner && state.players[winner]) {
      state.players[winner].score += 1;
    }
  })),
  
  resetGame: () => set(produce(state => {
    state.gameState = initialGameState;
    state.players = {
      player1: createInitialMecha('player1', 'BLUE', 200),
      player2: createInitialMecha('player2', 'RED', 550),
    };
    state.effects = [];
  })),
  
  nextRound: () => set(produce(state => {
    state.gameState.round += 1;
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.phase = 'PLAYING';
    state.gameState.winner = null;
    
    // 重置玩家位置和状态
    Object.keys(state.players).forEach(playerId => {
      const player = state.players[playerId];
      player.health = player.maxHealth;
      player.position = playerId === 'player1' ? { x: 200, y: 400 } : { x: 550, y: 400 };
      player.velocity = { x: 0, y: 0 };
      player.state = 'IDLE';
      player.isDefending = false;
      player.isGrounded = false;
      player.attackCooldown = 0;
    });
    
    state.effects = [];
  })),
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => set(produce(state => {
    state.players[playerId].position = position;
    state.players[playerId].velocity = velocity;
  })),
  
  updatePlayerState: (playerId: string, mechaState: MechaData['state']) => set(produce(state => {
    state.players[playerId].state = mechaState;
  })),
  
  updatePlayerFacing: (playerId: string, facingRight: boolean) => set(produce(state => {
    state.players[playerId].facingRight = facingRight;
  })),
  
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => set(produce(state => {
    state.players[playerId].isGrounded = isGrounded;
  })),
  
  updatePlayerDefending: (playerId: string, isDefending: boolean) => set(produce(state => {
    state.players[playerId].isDefending = isDefending;
  })),
  
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => set(produce(state => {
    state.players[playerId].attackCooldown = cooldown;
  })),
  
  dealDamage: (targetId: string, damage: number) => set(produce(state => {
    const target = state.players[targetId];
    const actualDamage = target.isDefending ? Math.floor(damage * 0.5) : damage;
    target.health = Math.max(0, target.health - actualDamage);
    
    // 添加受击特效
    state.effects.push({
      id: `hit-${Date.now()}`,
      type: 'HIT',
      position: { ...target.position },
      duration: 300,
      timer: 0,
      frame: 0,
    });
    
    // 检查游戏结束
    if (target.health === 0) {
      const winnerId = targetId === 'player1' ? 'player2' : 'player1';
      state.gameState.phase = 'RESULT';
      state.gameState.winner = winnerId;
      state.players[winnerId].score += 1;
    }
  })),
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => set(produce(state => {
    const player = state.players[playerId];
    player.animationTimer += deltaTime;
  })),
  
  setAnimationFrame: (playerId: string, frame: number) => set(produce(state => {
    state.players[playerId].animationFrame = frame;
  })),
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => {
    const id = `effect-${Date.now()}-${Math.random().toString(36).substr(2, 9)}`;
    set(produce(state => {
      state.effects.push({
        ...effect,
        id,
        timer: 0,
      });
    }));
    return id;
  },
  
  updateEffects: (deltaTime: number) => set(produce(state => {
    state.effects = state.effects.filter(effect => {
      effect.timer += deltaTime;
      return effect.timer < effect.duration;
    });
  })),
  
  removeEffect: (effectId: string) => set(produce(state => {
    state.effects = state.effects.filter(e => e.id !== effectId);
  })),
  
  clearEffects: () => set(produce(state => {
    state.effects = [];
  })),
  
  // 游戏时间
  tick: (deltaTime: number) => set(produce(state => {
    if (state.gameState.phase === 'PLAYING') {
      state.gameState.timeRemaining = Math.max(0, state.gameState.timeRemaining - deltaTime / 1000);
      
      // 时间到，判定胜负
      if (state.gameState.timeRemaining === 0) {
        const p1Health = state.players.player1.health;
        const p2Health = state.players.player2.health;
        
        if (p1Health > p2Health) {
          state.gameState.winner = 'player1';
          state.players.player1.score += 1;
        } else if (p2Health > p1Health) {
          state.gameState.winner = 'player2';
          state.players.player2.score += 1;
        }
        // 平局不加分
        
        state.gameState.phase = 'RESULT';
      }
import { create } from 'zustand';
import { produce } from 'immer';
import type {
  GameState,
  MechaData,
  MechaType,
  GamePhase,
  Effect,
  Vector2D,
} from '@/types';
import { GAME_CONSTANTS, MECHA_CONFIG } from '@/types';

// 创建初始机甲数据
const createInitialMecha = (id: string, type: MechaType, x: number): MechaData => {
  const config = MECHA_CONFIG[type];
  return {
    id,
    type,
    name: config.name,
    health: config.maxHealth,
    maxHealth: config.maxHealth,
    attack: config.attack,
    defense: config.defense,
    speed: config.speed,
    jumpForce: config.jumpForce,
    position: { x, y: 400 },
    velocity: { x: 0, y: 0 },
    state: 'IDLE',
    facingRight: type === 'RED',
    isGrounded: false,
    isDefending: false,
    attackCooldown: 0,
    animationTimer: 0,
    animationFrame: 0,
    score: 0,
  };
};

// 初始游戏状态
const initialGameState: GameState = {
  phase: 'MENU',
  round: 1,
  maxRounds: 3,
  timeRemaining: GAME_CONSTANTS.ROUND_TIME,
  winner: null,
};

// 游戏Store接口
interface GameStore {
  // 状态
  gameState: GameState;
  players: Record<string, MechaData>;
  effects: Effect[];
  
  // 游戏流程控制
  startGame: () => void;
  pauseGame: () => void;
  resumeGame: () => void;
  endGame: (winner: string | null) => void;
  resetGame: () => void;
  nextRound: () => void;
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => void;
  updatePlayerState: (playerId: string, state: MechaData['state']) => void;
  updatePlayerFacing: (playerId: string, facingRight: boolean) => void;
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => void;
  updatePlayerDefending: (playerId: string, isDefending: boolean) => void;
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => void;
  dealDamage: (targetId: string, damage: number, attackerId?: string) => void;
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => void;
  setAnimationFrame: (playerId: string, frame: number) => void;
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => string;
  updateEffects: (deltaTime: number) => void;
  removeEffect: (effectId: string) => void;
  clearEffects: () => void;
  
  // 游戏时间
  tick: (deltaTime: number) => void;
}

// 创建Game Store
export const useGameStore = create<GameStore>((set, get) => ({
  // 初始状态
  gameState: initialGameState,
  players: {
    player1: createInitialMecha('player1', 'BLUE', 200),
    player2: createInitialMecha('player2', 'RED', 550),
  },
  effects: [],
  
  // 游戏流程控制
  startGame: () => set(produce(state => {
    state.gameState.phase = 'PLAYING';
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.winner = null;
  })),
  
  pauseGame: () => set(produce(state => {
    if (state.gameState.phase === 'PLAYING') {
      state.gameState.phase = 'PAUSED';
    }
  })),
  
  resumeGame: () => set(produce(state => {
    if (state.gameState.phase === 'PAUSED') {
      state.gameState.phase = 'PLAYING';
    }
  })),
  
  endGame: (winner: string | null) => set(produce(state => {
    state.gameState.phase = 'RESULT';
    state.gameState.winner = winner;
    if (winner && state.players[winner]) {
      state.players[winner].score += 1;
    }
  })),
  
  resetGame: () => set(produce(state => {
    state.gameState = initialGameState;
    state.players = {
      player1: createInitialMecha('player1', 'BLUE', 200),
      player2: createInitialMecha('player2', 'RED', 550),
    };
    state.effects = [];
  })),
  
  nextRound: () => set(produce(state => {
    state.gameState.round += 1;
    state.gameState.timeRemaining = GAME_CONSTANTS.ROUND_TIME;
    state.gameState.phase = 'PLAYING';
    state.gameState.winner = null;
    
    // 重置玩家位置和状态
    Object.keys(state.players).forEach(playerId => {
      const player = state.players[playerId];
      player.health = player.maxHealth;
      player.position = playerId === 'player1' ? { x: 200, y: 400 } : { x: 550, y: 400 };
      player.velocity = { x: 0, y: 0 };
      player.state = 'IDLE';
      player.isDefending = false;
      player.isGrounded = false;
      player.attackCooldown = 0;
    });
    
    state.effects = [];
  })),
  
  // 玩家操作
  updatePlayerPosition: (playerId: string, position: Vector2D, velocity: Vector2D) => set(produce(state => {
    state.players[playerId].position = position;
    state.players[playerId].velocity = velocity;
  })),
  
  updatePlayerState: (playerId: string, mechaState: MechaData['state']) => set(produce(state => {
    state.players[playerId].state = mechaState;
  })),
  
  updatePlayerFacing: (playerId: string, facingRight: boolean) => set(produce(state => {
    state.players[playerId].facingRight = facingRight;
  })),
  
  updatePlayerGrounded: (playerId: string, isGrounded: boolean) => set(produce(state => {
    state.players[playerId].isGrounded = isGrounded;
  })),
  
  updatePlayerDefending: (playerId: string, isDefending: boolean) => set(produce(state => {
    state.players[playerId].isDefending = isDefending;
  })),
  
  setPlayerAttackCooldown: (playerId: string, cooldown: number) => set(produce(state => {
    state.players[playerId].attackCooldown = cooldown;
  })),
  
  dealDamage: (targetId: string, damage: number) => set(produce(state => {
    const target = state.players[targetId];
    const actualDamage = target.isDefending ? Math.floor(damage * 0.5) : damage;
    target.health = Math.max(0, target.health - actualDamage);
    
    // 添加受击特效
    state.effects.push({
      id: `hit-${Date.now()}`,
      type: 'HIT',
      position: { ...target.position },
      duration: 300,
      timer: 0,
      frame: 0,
    });
    
    // 检查游戏结束
    if (target.health === 0) {
      const winnerId = targetId === 'player1' ? 'player2' : 'player1';
      state.gameState.phase = 'RESULT';
      state.gameState.winner = winnerId;
      state.players[winnerId].score += 1;
    }
  })),
  
  // 动画
  updateAnimation: (playerId: string, deltaTime: number) => set(produce(state => {
    const player = state.players[playerId];
    player.animationTimer += deltaTime;
  })),
  
  setAnimationFrame: (playerId: string, frame: number) => set(produce(state => {
    state.players[playerId].animationFrame = frame;
  })),
  
  // 特效
  addEffect: (effect: Omit<Effect, 'id' | 'timer'>) => {
    const id = `effect-${Date.now()}-${Math.random().toString(36).substr(2, 9)}`;
    set(produce(state => {
      state.effects.push({
        ...effect,
        id,
        timer: 0,
      });
    }));
    return id;
  },
  
  updateEffects: (deltaTime: number) => set(produce(state => {
    state.effects = state.effects.filter(effect => {
      effect.timer += deltaTime;
      return effect.timer < effect.duration;
    });
  })),
  
  removeEffect: (effectId: string) => set(produce(state => {
    state.effects = state.effects.filter(e => e.id !== effectId);
  })),
  
  clearEffects: () => set(produce(state => {
    state.effects = [];
  })),
  
  // 游戏时间
  tick: (deltaTime: number) => set(produce(state => {
    if (state.gameState.phase === 'PLAYING') {
      state.gameState.timeRemaining = Math.max(0, state.gameState.timeRemaining - deltaTime / 1000);
      
      // 时间到，判定胜负
      if (state.gameState.timeRemaining === 0) {
        const p1Health = state.players.player1.health;
        const p2Health = state.players.player2.health;
        
        if (p1Health > p2Health) {
          state.gameState.winner = 'player1';
          state.players.player1.score += 1;
        } else if (p2Health > p1Health) {
          state.gameState.winner = 'player2';
          state.players.player2.score += 1;
        }
        // 平局不加分
        
        state.gameState.phase = 'RESULT';
      }
    }
  })),
